{
  "_doc": [
    "THE MANAGER LEASE. Exactly one management session at a time, never two.",
    "",
    "Written by scripts/ops/manager_lease.py -- do not hand-edit; a hand-edit that",
    "breaks the JSON reads as `unreadable`, which REFUSES every claim until it is",
    "repaired or forced.",
    "",
    "READ THIS FILE BEFORE MANAGING ANYTHING:  python3 scripts/ops/manager_lease.py status",
    "",
    "Takeover is TIME-BASED. A session that dies cannot hand over, so nothing here",
    "depends on the outgoing manager cooperating. An outgoing manager still alive",
    "learns at its next heartbeat that it no longer holds the lease, and stands down.",
    "",
    "A CLAIM YOU DID NOT PUSH PROTECTS NOTHING -- another session reads `origin`.",
    "",
    "Sub-sessions do NOT depend on this. They keep running with no lease held; what",
    "pauses is SUPERVISION. docs/claude/work/SESSIONS.json is what a cold manager",
    "reads to pick them up. Design: docs/claude/work/objects/WO-20260901-PHASE-E.yaml"
  ],
  "schema_version": 1,
  "state": "held",
  "holder": "session_01HrmZ1RRNM4UnEUaFdrPEjj",
  "claimed_at": "2026-09-08T11:55:22Z",
  "heartbeat_at": "2026-09-09T03:40:35Z",
  "expires_at": "2026-09-09T05:10:35Z",
  "ttl_minutes": 90,
  "heartbeat_target_minutes": 30,
  "claimed_over_state": "expired",
  "forced": false,
  "force_reason": null,
  "previous_holder": "session_01HrmZ1RRNM4UnEUaFdrPEjj",
  "note": "Heartbeat 03:40:35Z, tool-stamped (54 min gap, ttl 90). QUIET PASS — `search_pull_requests is:open -head:automation` returns total_count 0, no sub-sessions running, nothing in flight. Synced with `git checkout main && git pull --ff-only`, the idiom BL-20260909-RESET-HARD-ORIGIN-MAIN-ON-A-FEATURE-BRANCH-TWICE-IN-50-MINUTES-AND-THE-WRITTEN-REMINDER-DID-NOT-HOLD proposes. || ⚠️ THE ONE TIMED THING: the digest clearance expires ~07:04Z — 6h after the receipt that landed 01:04Z — and when it does `digest-liveness` reds EVERY PR in the repo again. The interim move is proven twice: push a merge_slot claim naming its own branch onto the newest automation/work-digest-* branch (R2 carve-out, graded `a LANDING act, not an item`), then merge it. Four stuck receipt PRs remain (#11442 #11445 #11452 #11465). MI-208 is the durable fix and is ROUTED, not taken — .github/workflows/ is a worker path R2 bars the manager from authoring, and the SIBLING producers (error-feed-digest, session-reaper, reconcile-open-prs, probes, due-list, data-commit) arm the same route, so the denominator must be established before one file is fixed and declared done. || ⚠️ AND THE ID ABOVE WAS ELIDED IN THIS NOTE'S FIRST VERSION, WHICH FAILED CI. check_backlog_refs caught the id TRUNCATED AT THE WORD 'BRANCH' resolving to NOTHING (the bad form is deliberately NOT reproduced here — quoting it re-introduces the very failure, which is exactly what happened on the first fix attempt) — a note saying 'tracked by BL-X' where BL-X was never filed reads as tracked while being tracked by nobody. That is the SAME class MI-205b fixed at 00:49Z tonight ('expand a truncated backlog id so the tracking reference resolves'), recurring three hours later in a different file. ⚠️ NO ROW IS FILED FOR IT: the guard exists, it fired, and it named the exact id — a mechanism catching a typo is the mechanism WORKING, not a finding. What IS worth carrying is the process slip that let it reach CI: I ran manager-scope and pr-landing locally but NOT check_backlog_refs, having run it on the previous pass. Running a SUBSET of the guards is not running the guards."
}
