# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260824-PROTECTION-REPAIR-STAMP-HAS-NO-BRANCHING-CONSUMER
type: question
parent_intent: null
title: |-
  The protective-bracket repair stamp is written and QUERYABLE but nothing BRANCHES on it — the exit_price_source shape, filed by its own author at the moment of writing
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-24T08:45:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ['DECIDE the consumer explicitly, with a reason, from these candidates (not all of them): (a) /api/bot/trades/closed emits a repaired flag beside pnlProvenance, so the dashboard + Android can caveat a trade whose exit ran on a repaired bracket; (b) /performance excludes or separately reports repaired rows, the way pnlCoverage reports measured share; (c) the system-review reads it as a standing count; (d) nothing — the generic query IS the consumer, recorded as a deliberate decision rather than an omission.', "If (a) or (b): the flag must not collapse — a repaired trade whose repair landed both_legs_resting is a different claim from one that landed stop_only or call_failed, and a boolean 'repaired' would lose exactly the distinction the _last_verified column was added to keep.", 'Whichever is chosen, state the population it was measured over. Re-check first that the stamp has ACCRUED any rows at all: a consumer designed against an empty column is untested by construction.', "If the decision is (d), say so in CLAUDE.md's trades row and close this — an explicit 'the query is the consumer' is a valid answer; a silent one is not."]
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260824-PROTECTION-REPAIR-STAMP-HAS-NO-BRANCHING-CONSUMER
  status_at_migration: open
  severity: medium
  tier: '1'
  opened_by: claude (full-system-audit continuation, Tier-2 repair-stamp session)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260824-PROTECTION-REPAIR-STAMP-HAS-NO-BRANCHING-CONSUMER
verdict: null
