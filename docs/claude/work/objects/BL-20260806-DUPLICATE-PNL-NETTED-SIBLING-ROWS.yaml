# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260806-DUPLICATE-PNL-NETTED-SIBLING-ROWS
type: commitment
parent_intent: null
title: |-
  Duplicate netted pnl on sibling rows — $24.3k across 31 paper rows poisoning the calibration set + ML labels (real-money exposure bounded at $45.52)
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-06T14:30:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  (1) No unmarked byte-identical pnl cluster remains across sibling rows — a residual cluster that differs beyond 2dp is a rounding collision and does NOT count; and (2) the P1.x fidelity trust map has been re-run post-exclusion, so the affected leg carries a verdict computed over a population that no longer contains duplicated pnl.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260806-DUPLICATE-PNL-NETTED-SIBLING-ROWS
  status_at_migration: kept_open
  severity: high
  tier: '2'
  opened_by: claude (roadmap-workplan-update session lw6qf0)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260806-DUPLICATE-PNL-NETTED-SIBLING-ROWS
verdict: null
next_action_at_migration: |-
  Marking COMPLETE (29/33 applied; the 4 residual bybit_2 rows are demonstrated rounding collisions and should be left alone — see the 2026-08-12 update for the arithmetic). Row stays OPEN only for the two downstream re-validations: (b) re-run the P1.x fidelity trust map with the 29 rows now excluded — the htf_pullback_trend_2h/BTCUSDT verdict is unusable in either direction until it is redone; (c) determine whether any ML model was trained on labels spanning those rows, and whether its gate metrics move once they are excluded. Both are compute, not money-DB writes. Do NOT re-open the marking question without new evidence of a byte-identical cluster.
