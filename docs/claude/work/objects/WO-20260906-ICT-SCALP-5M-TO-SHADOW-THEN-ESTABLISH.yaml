id: WO-20260906-ICT-SCALP-5M-TO-SHADOW-THEN-ESTABLISH
type: question
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: >-
  ict_scalp_5m to shadow, then establish whether it should trade real money at all
stage: DECISION
lifecycle: in_flight
owner: session_01X3kyzCfUdFVimATxZFE8nd
opened_at: 2026-09-06
closed_at: null
review_trigger: >-
  Re-read at the next cycle. A captured idea is PARKED, not queued — it becomes
  work only when it is given an owner and moved out of `dormant`.
done_condition: >-
  ⚠️ PART (1) WAS SUPERSEDED BY THE OPERATOR ON 2026-09-06 — READ THIS BEFORE
  ACTING ON THE TEXT BELOW. The lever is NO LONGER `config/strategies.yaml
  execution: shadow`. Asked to choose between that and the narrower per-account
  lever, the operator chose the narrower one: REMOVE `ict_scalp_5m` FROM
  `bybit_2`'s `strategies:` LIST IN `config/accounts.yaml`, and LEAVE
  `config/strategies.yaml` ALONE so the leg keeps `execution: live`. Rationale:
  `execution:` is a per-STRATEGY gate and would also have stopped `bybit_1` and
  `bybit_portfolio`, and the paper evidence is wanted — it feeds the
  `bracket_geometry` sweep that part (2) turns on. ⚠️ CORRECTED AFTER CI:
  `bybit_portfolio` DOES follow the demote, because
  tests/test_paper_portfolio_accounts.py pins its roster to bybit_2's as an
  enforced ROSTER-SYNC invariant (the config list is literal and does not
  propagate, but the TEST does). The evidence is NOT lost — `bybit_1`, the
  FULL-ROSTER soak book, keeps the leg at `execution: live` and is the book
  soak evidence accrues on. DECIDED, Tier-3, scope
  `bybit_2` only, no condition; typed record in docs/claude/work/OPEN-PRS.json.
  Shipped as PR #11121; PR #11115, which implemented `execution: shadow`, is
  CLOSED unmerged and superseded. ⚠️ DO NOT "fix" this by setting
  `execution: shadow` — that would undo the operator's choice and stop both
  paper books. ⚠️ AND WHEN VERIFYING: order packages AND PAPER FILLS will keep
  appearing for this leg, because it is still live on bybit_1. That is
  the change working. Grade on `trades` rows carrying BOTH
  `strategy_name='ict_scalp_5m'` AND `account_id='bybit_2'`.
  ⚠️ ONE CORRECTION TO `why:` BELOW, which is load-bearing for part (2): its
  "ZERO shipped exit levers ... all read n/a" is right in conclusion and wrong
  in reasoning. Three levers (stale_stop, giveback_stop, exit_ladder) were
  genuinely swept and honestly FAILED; four (trail_geometry, trail_decay,
  vol_trail, regime_flip_exit) are INAPPLICABLE BY CONSTRUCTION — the strategy
  has no trailing stop to reshape; one (exit_head_ml) awaits a Tier-3 ship.
  EXACTLY ONE lever is both unrun and applicable: `bracket_geometry`. Reading
  all seven as neglect would spread part (2)'s effort across six dead ends.
  (Established in docs/research/ict-scalp-5m-real-money-verdict-2026-09-06.md.)
  ---
  ORIGINAL TEXT, KEPT AS THE RECORD OF WHAT WAS FIRST ASKED:
  TWO PARTS, and part 1 happens FIRST and FAST. (1) config/strategies.yaml sets execution: shadow on ict_scalp_5m -- Tier-3 and ALREADY OPERATOR-APPROVED for this specific change; open it ready with landing: hold and hold_text quoting the approval, and say in the PR that the approval is recorded in the manager checklist. Verify AFTER deploy that it stopped placing (a shadow leg still logs order packages) rather than assuming. (2) THEN the dive: should it trade real money at all? Establish what its edge is supposed to be, whether any backtest ever supported its bracket geometry, why a 54.1%-win leg loses money, and what would have to be true to put it back on live. !! DO NOT author a regime gate or a stop change from n=6 -- 1B's report explicitly refuses that and so must you. !! DO NOT use expectancyR anywhere: the R instrument is sign-inverted (PB-20260906-R-CONTAMINATION-QUANTIFIED).

why: >-
  OPERATOR DECISION 2026-09-06, via pop-up with the tradeoffs stated: 'Move it to shadow now'. ict_scalp_5m is 6 of 13 losses and -25.25 of -43.12 (58.6%) of the losing week on REAL MONEY, with NO config change since 2026-08-16. Its lifetime on bybit_2 is 37 trades / 54.1% WINS / -12.65 USD -- it wins more often than it loses and still loses money, which is payoff geometry, not hit rate. MEASURED in docs/research/exit-refinement-coverage.json: it has ZERO shipped exit levers. trail_geometry, trail_decay, vol_trail and regime_flip_exit all read n/a; exit_head_ml reads blocked:no_lever_consumer_in_unit (the strategy UNIT has no consumer for the lever); bracket_geometry reads PENDING, i.e. never swept. It is trading real money on a static bracket that no backtest ever tested, into a tape whose directional efficiency fell 4-6x.

source: >-
  operator (conversational)

blocked_on: []
blocked_on_basis: >-
  NOT_ASSESSED. ⚠️ This empty list is NOT the claim that nothing blocks this
  object — nobody has looked. Write a TRUE edge before moving it out of
  `dormant`; an invented edge is read by the constraint computation as a real
  blocker, and a false blocker is worse than a missing one.

captured_by: scripts/ops/capture_idea.py
