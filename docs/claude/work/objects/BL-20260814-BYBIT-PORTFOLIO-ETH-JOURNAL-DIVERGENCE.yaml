# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260814-BYBIT-PORTFOLIO-ETH-JOURNAL-DIVERGENCE
type: question
parent_intent: null
title: |-
  [bybit] bybit_portfolio ETHUSDT journal over-reports the exchange by 13.96 (35.01 vs 21.05) - at least one phantom open row on the paper-portfolio mirror, which is the book the UIs label 'Paper'
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-14T19:40:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  1. Measure the CURRENT extent, not this one line: for every Bybit account and symbol, compare summed open journal qty to the exchange position, and record the population (accounts x symbols scanned), not just the hits. A per-symbol table is the deliverable.
  2. Identify which specific open row(s) on bybit_portfolio/ETHUSDT are phantom and how they were created - the netting partial-close attribution reconciler (NETTING_ATTRIBUTION_MODE, currently annotate) already writes candidate rows to netting_attribution_soak.jsonl; check whether these appear there before hypothesising a new mechanism.
  3. Only then decide the writeback (Tier-2). Do NOT close a row on the strength of a single snapshot - the 2-observation confirm exists because an unconfirmed exchange read is not proof of flatness.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260814-BYBIT-PORTFOLIO-ETH-JOURNAL-DIVERGENCE
  status_at_migration: kept_open
  severity: medium
  tier: 1 (measurement + reconciliation); any journal writeback is Tier-2
  opened_by: claude (/system-review 2026-08-14, incidental sighting)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260814-BYBIT-PORTFOLIO-ETH-JOURNAL-DIVERGENCE
verdict: null
