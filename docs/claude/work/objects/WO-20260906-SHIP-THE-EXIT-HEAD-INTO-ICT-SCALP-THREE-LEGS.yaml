id: WO-20260906-SHIP-THE-EXIT-HEAD-INTO-ICT-SCALP-THREE-LEGS
type: commitment
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: >-
  Ship the M20 exit-head consumer into the ict_scalp unit (MI-146 recommendation #2) — wiring only, annotate-first, arming NOT taken
stage: DEPLOYMENT
lifecycle: in_flight
owner: session_01Lr1TDaQz2APJyYafBnsZen
opened_at: 2026-09-06
closed_at: null
review_trigger: >-
  On the next cycle, or when a 5m/15m ict_scalp exit-head artifact is published
  to runtime_logs/trainer_mirror/exit_head/ — that publication, not elapsed
  time, is what changes this object's state.
done_condition: >-
  TWO PARTS, and only the first is this object's to deliver. (1) SHIPPED: an
  exit-head consumer exists in src/units/strategies/ict_scalp.py that MIRRORS
  the donchian one rather than duplicating it (shared decision module, both
  call sites pinned by one test table), carries the SAME in-distribution guard,
  lands annotate-only behind a *_MODE knob, and moves the five matrix cells off
  the now-false `blocked:no_lever_consumer_in_unit` with the two exclusions
  encoded DISTINCTLY. (2) OBSERVED: a soak row read from the LIVE VM. !! (2) is
  NOT deliverable by this object today and that is a finding, not a slip — see
  `why`. !! ARMING IS EXPLICITLY OUT OF SCOPE and is blocked by a standing
  operator decision; do not let a successor read this object's completion as
  permission to arm.
why: >-
  MI-146 measured the whole 468-cell exit-lever matrix and found five cells that
  passed their gate and were blocked on MISSING CODE alone — all five on the
  ict_scalp unit, which contained zero exit-head code (verified this session,
  not taken on trust: `grep -c exit_head` returned 0 against 17 in
  trend_donchian.py, a positive control). The operator approved routing
  recommendation #2 ("Yes on MI 146", 2026-09-06). ict_scalp is also the family
  that best implements the bracket thesis (fixed tp_at_r 1.5, fixed stop,
  break-even ratchet), landing in a book where MI-144 measured 228 of 420
  gradeable real-money closes (54.3%) reaching a declared bracket with stop
  beating target 4.6 : 1.
outcome: >-
  SHIPPED as PR #11140 (draft, tier 3, landing `hold` — the manager routes the
  merge). Consumer in ict_scalp.monitor(); the firing rule FACTORED OUT to
  src/runtime/exit_head_apply.py with trend_donchian._exit_head_verdict reduced
  to a delegation shim, so the move is reviewable as a move and 75 parity
  assertions run BOTH call sites over one case table. 93 new assertions, 164
  passed across the affected suites. Matrix re-statused (7 cells, two exclusions
  encoded distinctly); MEASURED tp_geometry ratchet effect ZERO (396 live cells
  / 229 unstamped / ceiling 229, unchanged). Soak registered in OPEN-ITEMS.
  !! TWO FINDINGS THE DISPATCH DID NOT CARRY, both verified against the live
  system rather than the memo: (a) NO ict_scalp LEG CAN BE SCORED — the live
  mirror publishes only `1h` exit_head artifacts while every scalp leg is
  5m/15m, so the tf guard fail-closes on all 8 and the blocker MOVED from
  missing code to missing artifact rather than clearing; (b) all five candidate
  cells carry a 2026-08-23 `SHIP BLOCKED` operator decision that CANCELLED the
  queued Tier-3 ship, and for ict_scalp_sol_5m records that its pass does NOT
  survive re-partitioning. MI-146's memo omitted both.
source: >-
  operator (conversational, 2026-09-06 — "Yes on MI 146"), routed via the manager as MI-150
blocked_on:
  - kind: artifact
    ref: >-
      No ict_scalp exit-head artifact is published to the live mirror. Measured
      2026-09-06 on /api/diag/shadow_stats (last_seen 10:00:01Z): exactly two
      exit_head model_ids exist — exit-head-donchian-1h-v1 (advisory) and
      exit-head-donchian-peak-1h-v1 (shadow), count 52 each — and BOTH are 1h,
      while all 8 ict_scalp legs are 5m or 15m. maybe_score_exit_head's
      in-distribution tf guard therefore refuses every leg. This blocks the
      OBSERVED half of the done-condition only; the SHIPPED half is delivered.
    since: 2026-09-06
blocked_on_basis: >-
  ASSESSED 2026-09-06 by direct measurement of the live mirror's published
  artifact set via /api/diag/shadow_stats, cross-read against the timeframe of
  all 8 ict_scalp legs in config/strategies.yaml. This is a TRUE edge, not a
  placeholder: the named artifact set is what the tf guard reads, and the guard
  is what returns None. It clears when a 5m/15m scalp head is published, or
  when the operator decides none will be trained.
adjacent_work: >-
  !! WO-20260906-ICT-SCALP-5M-TO-SHADOW-THEN-ESTABLISH is IN FLIGHT on an
  overlapping leg (ict_scalp_5m, the BTC leg this object excludes as UNMEASURED).
  No file conflict is expected — that object changes an execution gate in
  config/strategies.yaml, this one changes src/units/strategies/ict_scalp.py —
  but a session touching either should read both. If ict_scalp_5m moves to
  `execution: shadow` its exit_head_ml cell leaves the matrix's LIVE population,
  which SHRINKS the tp_geometry ratchet denominator and will require lowering
  _unstamped_ceiling in that same diff
  (BL-20260824-DEMOTING-A-LEG-SILENTLY-LOOSENS-THE-M20-RATCHET). This object's
  own matrix edit does NOT move that denominator; that one will.
