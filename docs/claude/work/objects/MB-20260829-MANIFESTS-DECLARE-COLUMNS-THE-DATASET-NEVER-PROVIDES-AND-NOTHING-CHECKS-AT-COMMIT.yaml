# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: |-
  MB-20260829-MANIFESTS-DECLARE-COLUMNS-THE-DATASET-NEVER-PROVIDES-AND-NOTHING-CHECKS-AT-COMMIT
type: commitment
parent_intent: null
title: |-
  6 of the 8 non-training manifests fail because the MANIFEST asks for something the DATASET does not provide — the contract is checked only at train time, so a manifest merges clean and then silently never trains under rc=0
stage: EVIDENCE
lifecycle: dormant
owner: null
opened_at: '2026-08-29T18:00:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  A COMMIT-TIME check that a manifest's declared feature_columns (and target_column) are producible by its declared dataset family — the same shape as the guards this repo already runs for db resolvers, provenance consumers and collapsed states — so a manifest asking for a column no builder emits FAILS CI instead of failing silently on the trainer for a month. It must distinguish the four causes above rather than lumping them: an absent column (A) is a wiring bug, a structurally-constant one (B) is a manifest-timeframe mismatch, a degenerate label (C) is a data-coverage question, and a 0-row build (D) is not a manifest defect at all. PLUS: each of the 7 currently-failing manifests either trains again (observed as a manifest_ok row + a fresh registered run) or is explicitly retired with the measurement as the reason. Explicitly NOT sufficient: fixing the manifests one at a time and leaving the commit-time gap open — that is what leaves the next one to be found in 34 days.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/ml-review-backlog.json
  row_id: |-
    MB-20260829-MANIFESTS-DECLARE-COLUMNS-THE-DATASET-NEVER-PROVIDES-AND-NOTHING-CHECKS-AT-COMMIT
  status_at_migration: open
  severity: high
  tier: |-
    1 to establish + add the guard; editing an individual manifest's feature list is Tier-1/2 trainer-side
  opened_by: /system-review 2026-08-29
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/ml-review-backlog.json#MB-20260829-MANIFESTS-DECLARE-COLUMNS-THE-DATASET-NEVER-PROVIDES-AND-NOTHING-CHECKS-AT-COMMIT
verdict: null
next_action_at_migration: |-
  Propose the guard (scripts/ci/) with the four-way cause split. Before writing it, resolve the one genuinely puzzling instance: btc-regime-15m-lgbm-base-vt003-pcv-v1 is missing FOUR core columns that every 15m sibling has, on a 215,838-row dataset — that is more likely a dataset family/version wiring fault than a manifest typo, and if so the guard's shape should follow the real cause rather than assume the manifest is always the wrong side.
