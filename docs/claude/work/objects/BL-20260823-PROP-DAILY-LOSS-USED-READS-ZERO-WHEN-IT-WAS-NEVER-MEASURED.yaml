# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260823-PROP-DAILY-LOSS-USED-READS-ZERO-WHEN-IT-WAS-NEVER-MEASURED
type: question
parent_intent: null
title: |-
  `daily_loss_used_usd: 0.0` means BOTH 'no loss today' and 'today's loss was never measured' — and the second is what it meant on a day that lost $107
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-23'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ["`/api/bot/prop/status` gains a `daily_basis` (or equivalent) that is never collapsed, mirroring `status_freshness`'s vocabulary: `measured` (a day_start_balance is known and day_pnl is computed from it) / `fills_only` (no day_start_balance; day_pnl reflects only fills reported since the snapshot, so it is a LOWER BOUND) / `unmeasured` (neither available - we did not look). `distance_to_daily_loss_usd` must be readable as untrustworthy without deriving it from three other fields.", 'When the basis is not `measured`, the daily distance is still RETURNED (discarding the last known figure helps nobody, per the same rule that keeps stale rule_distance values) but the caveat travels INSIDE rule_distance as well as on the envelope - the pattern status_freshness already uses, so a consumer reading only the panel cannot miss it.', 'Register the new field with collapsed-state-guard so the producer must emit every state and a real consumer must branch on each.', 'Consumers updated: the dashboard Prop tab rule-distance panel must not render a `fills_only`/`unmeasured` daily distance the same way it renders a measured one.']
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260823-PROP-DAILY-LOSS-USED-READS-ZERO-WHEN-IT-WAS-NEVER-MEASURED
  status_at_migration: open
  severity: high
  tier: 1 (a read surface; the remedy is a distinguishable state, not a value change)
  opened_by: full-system-audit-2026-08-23
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260823-PROP-DAILY-LOSS-USED-READS-ZERO-WHEN-IT-WAS-NEVER-MEASURED
verdict: null
