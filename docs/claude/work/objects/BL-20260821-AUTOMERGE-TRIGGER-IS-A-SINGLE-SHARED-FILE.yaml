# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260821-AUTOMERGE-TRIGGER-IS-A-SINGLE-SHARED-FILE
type: question
parent_intent: null
title: |-
  claude-pr-automerge.yml triggers on ONE shared path (.github/pr-automerge-request), so every PR that requests auto-merge conflicts with every other one that does -- a guaranteed merge + full CI cycle per collision, unlike the sibling relays which already use a per-request directory
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-21T07:12:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ["Change the trigger to a per-request directory matching the sibling relays -- e.g. `paths: ['.github/pr-automerge-requests/**']` with one file per PR -- so two concurrent requests touch different paths and cannot conflict.", 'Keep the old single-file path in the trigger list for one transition window so an in-flight branch that already wrote the old file still fires, then remove it.', 'PROVE it with two branches: open two PRs that both request auto-merge, merge one, and confirm the second comes back mergeable rather than `dirty`.', "Check whether the relay reads the file's CONTENT anywhere before changing its shape; as of 2026-08-21 it appears to use the path only as a trigger, but that was read, not tested."]
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260821-AUTOMERGE-TRIGGER-IS-A-SINGLE-SHARED-FILE
  status_at_migration: open
  severity: high
  tier: 1 (CI / GitHub Actions)
  opened_by: 'claude (exit-integrity continuation, hit while requesting auto-merge for #10086)'
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260821-AUTOMERGE-TRIGGER-IS-A-SINGLE-SHARED-FILE
verdict: null
