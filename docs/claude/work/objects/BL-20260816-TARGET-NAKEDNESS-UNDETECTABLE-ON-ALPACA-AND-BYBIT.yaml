# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260816-TARGET-NAKEDNESS-UNDETECTABLE-ON-ALPACA-AND-BYBIT
type: question
parent_intent: null
title: |-
  The IB one-sided-coverage fix does NOT generalise: the Alpaca sweep and the Bybit sweep are both stop-only, and bybit_bracket_audit.py COLLECTS the target side but never lets it reach the verdict -- so it prints PROTECTED over a target-naked position
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-16T15:00:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ['An Alpaca counterpart to bybit_bracket_audit.py (read-only, alpaca_client_for + _open_orders_for_symbol), so the 13 measured positions can be graded at all -- this is the blocking step for the Alpaca half.', 'With it: a measurement, stated with its population, of how many of those 13 hold a stop and no take-profit. alpaca_live must be reported as could_not_look, never folded in as zero.', "Alpaca's naked sweep decides on a two-sided grade rather than the any-leg boolean; if has_protective_orders is kept, its docstring must forbid naked-detection use the way IB's now does, and the sweep must call something else.", "bybit_bracket_audit.py's verdict accounts for the target side it already collects: a stop-only book must NOT print PROTECTED. Either the verdict becomes two-sided or the label narrows to what it computed (e.g. SL_COVERED).", "Bybit's live protection state reports the target side explicitly (even as a separate field), so 'we did not grade it' is distinguishable from 'it is covered'.", 'A test per venue pinning that a STOP-ONLY book does NOT read as protected -- the single assertion that would have caught IB, Alpaca, Bybit and the audit tool alike.']
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260816-TARGET-NAKEDNESS-UNDETECTABLE-ON-ALPACA-AND-BYBIT
  status_at_migration: open
  severity: high
  tier: 2 (order path)
  opened_by: claude (sysrev-0816, follow-on audit to BL-20260816-COVERAGE-IS-ONE-SIDED)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260816-TARGET-NAKEDNESS-UNDETECTABLE-ON-ALPACA-AND-BYBIT
verdict: null
