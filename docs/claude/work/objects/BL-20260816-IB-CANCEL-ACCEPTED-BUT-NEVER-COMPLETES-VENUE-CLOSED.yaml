# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260816-IB-CANCEL-ACCEPTED-BUT-NEVER-COMPLETES-VENUE-CLOSED
type: question
parent_intent: null
title: |-
  IB ACCEPTS cancelOrder (retCode 0 'OK') and the order does NOT go away -- it moves to PendingCancel and stays there while the venue is shut; measured on order 6, and it is the same state order 378 has been stuck in for ~2.5h
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-16T15:02:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ['Observe the COMEX reopen (Sunday 22:00Z) and record what happens to orders 6 and 378: cancelled (hypothesis confirmed), still PendingCancel (hypothesis refuted, genuinely stuck — escalate), or FILLED (worst case — check immediately whether a naked short was opened).', "Confirm or refute the venue-closed hypothesis against IBKR's own documented behaviour for cancelling a resting DAY order outside session hours, so the next session does not re-derive it from two observations.", "If the cancels do complete at the reopen: document the expected latency so a future PendingCancel is not misread as a wedge, and give cancel_ib_order.py a way to say 'accepted, pending at venue' distinctly from 'not effective' — today both land as cancel_not_effective/exit 1, which collapses 'the venue has not got to it yet' into 'this failed'.", 'If they do NOT complete: this becomes a stuck-order-path finding needing a different remedy, and the naked-short exposure at every subsequent session open must be reassessed.', 'Separately: the ib_insync teardown emits `RuntimeError: Event loop is closed` from IB.__del__ after a successful run. Cosmetic here, but it prints a traceback on a clean path and could mask a real error in a future one.']
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260816-IB-CANCEL-ACCEPTED-BUT-NEVER-COMPLETES-VENUE-CLOSED
  status_at_migration: open
  severity: high
  tier: 2 (order path)
  opened_by: claude (sysrev-0816, operator-directed 'cancel order 6')
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260816-IB-CANCEL-ACCEPTED-BUT-NEVER-COMPLETES-VENUE-CLOSED
verdict: null
