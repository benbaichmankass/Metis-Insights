# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260816-TICK-COST-MAX-RESETS-EVERY-DEPLOY
type: question
parent_intent: null
title: |-
  tick_cost's max_ms and every per-hook max reset on each deploy, so the statistic the module says is load-bearing is systematically under-observed - the same defect just fixed for exit_loop_health
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-16T10:05:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  1. Decide the GRAIN first, and state the volume it implies: per-tick x 27 hooks, vs a per-cadence roll-up carrying each hook's max within the window, vs durable maxima only (smallest, but discards the distribution the exposure-soak lesson says you need).
  2. Whatever the grain, summary maxima must be computed over the whole file, not a page - the reader-side version of the same bias (see exit_interval_soak.read_soak_records).
  3. Preserve the offloop/on-loop segregation in the record; collapsing them would make an off-thread cost read as tick cost, which tick_cost.py's own docstring warns about.
  4. Until then, treat every quoted tick max as a LOWER BOUND over a stated ticks_measured, and do not compare maxima across processes with different sample sizes.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260816-TICK-COST-MAX-RESETS-EVERY-DEPLOY
  status_at_migration: open
  severity: high
  tier: '1'
  opened_by: claude (exit-eval-interval session, durability fix)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260816-TICK-COST-MAX-RESETS-EVERY-DEPLOY
verdict: null
