# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260817-FLEET-SWEEP-WF-COUNTS-INERT-FOLDS-AS-WINS
type: commitment
parent_intent: null
title: |-
  m20_fleet_exit_sweep's walk-forward reports `wf_summary: 6/6` on six folds where the lever changed NOTHING — and the folds do not cover the years the OOS delta comes from
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-17'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  OBSERVABLE: a fold in which the lever never fires must NOT be counted as a win. Either (a) `wf_wins`/`wf_summary` count only folds with a non-zero delta and report the inert count separately, or (b) a fold with `d_net_r == 0.0 and d_max_dd == 0.0` gets a distinct third state (not `ok: True`), registered with collapsed-state-guard so producer and consumer are both checked. PLUS a planted control: a cell whose folds are all inert must not come back `6/6`, demonstrated by RUNNING it — a quiet pass proves nothing. AND the fold window must state its coverage against the OOS window, so a delta originating outside every fold cannot read as walk-forward-confirmed.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260817-FLEET-SWEEP-WF-COUNTS-INERT-FOLDS-AS-WINS
  status_at_migration: open
  severity: medium
  tier: |-
    1 — research tooling. No live path. Any SHIP decision the verdict feeds is Tier-3 and unaffected by this filing.
  opened_by: overnight M20 session (2026-08-16/17)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260817-FLEET-SWEEP-WF-COUNTS-INERT-FOLDS-AS-WINS
verdict: null
next_action_at_migration: |-
  Not fixed here — filed with the measurement so the next session does not re-derive it. Note the cheap half is (a): the counts are already computed from `wf_folds`, which carries the per-fold deltas.
