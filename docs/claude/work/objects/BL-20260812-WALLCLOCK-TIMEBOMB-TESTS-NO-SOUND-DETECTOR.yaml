# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260812-WALLCLOCK-TIMEBOMB-TESTS-NO-SOUND-DETECTOR
type: question
parent_intent: null
title: |-
  Wall-clock time-bomb tests (fixed date literal vs a now()-relative window) have no sound detector — one instance reddened main on 2026-08-12 and a 234-file sweep found no second, but the sweep instrument produces FALSE POSITIVES and cannot be trusted as a clean bill of health
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-12T19:10:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  Either (a) a static guard that flags a hardcoded-literal-vs-now-window test and is proven on the pre-fix `test_newest_first` as a positive control, or (b) a documented decision that the class is rare enough to handle reactively — recorded WITH the sweep's method limitations above, so the next session does not re-derive the same broken dynamic detector and act on its false positives.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260812-WALLCLOCK-TIMEBOMB-TESTS-NO-SOUND-DETECTOR
  status_at_migration: kept_open
  severity: low
  tier: '1'
  opened_by: claude (M20 exit-loop session 2026-08-12)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260812-WALLCLOCK-TIMEBOMB-TESTS-NO-SOUND-DETECTOR
verdict: null
