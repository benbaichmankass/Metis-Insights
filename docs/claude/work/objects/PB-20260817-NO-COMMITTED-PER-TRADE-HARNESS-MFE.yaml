# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: PB-20260817-NO-COMMITTED-PER-TRADE-HARNESS-MFE
type: commitment
parent_intent: null
title: |-
  The committed sweep corpus carries no per-trade mfe_r, so P4's harness side needs a sweep re-run
stage: EVIDENCE
lifecycle: dormant
owner: null
opened_at: '2026-08-17'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  Either a committed per-leg mfe_r distribution (percentiles + n, not raw rows — the corpus should not carry per-trade volume), or a documented one-command sweep re-run with --emit-trades that m31_mfe_parity.py consumes directly.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/performance-review-backlog.json
  row_id: PB-20260817-NO-COMMITTED-PER-TRADE-HARNESS-MFE
  status_at_migration: kept_open
  severity: low
  tier: '1'
  opened_by: S-M31-P4-MFE-PARITY-2026-08-17
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/performance-review-backlog.json#PB-20260817-NO-COMMITTED-PER-TRADE-HARNESS-MFE
verdict: null
next_action_at_migration: |-
  Run a CAPPED (--tp-cap-pct 0.099) trainer-side sweep per telemetry-writing leg (trend_donchian + htf_pullback_trend_2h families only -- no other leg writes position_telemetry, so no other leg is comparable) with --emit-trades, feed each emit file to scripts/research/m31_harness_mfe_dist.py with its own --symbol/--timeframe, and commit docs/research/m31-harness-mfe-dist.jsonl. Then Check B needs only live soak depth. Do NOT generate it from data/backtest_candles.csv -- that is BTCUSDT 1-minute and ~10x out of the live cap_R regime; the aggregator will accept it if you pass the right flags, and the resulting artifact would be wrong in a way only --symbol/--timeframe would reveal.
