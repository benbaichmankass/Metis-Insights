# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260823-TWELVE-PULLBACK-LEGS-HAVE-NO-ENTRY-REGIME-CONDITION
type: question
parent_intent: null
title: |-
  12 pullback legs declare no adx_min, so they have NO ADX filter at entry — and 'declare adx_min to fix thesis_unknown' is an entry-behaviour change, not the cheap observability fix it was described as
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-23T16:20:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  A floor is DERIVED, not chosen. Required first: the ADX-at-entry distribution per leg over history for all 12, so the proposed adx_min is stated together with the fraction of historical entries it would have REFUSED. A value whose refusal rate is unmeasured does NOT close this row, and neither does porting 25/28/30 from the crypto legs. If the measurement shows a candidate floor refuses a material share of entries, it needs its own walk-forward before any Tier-3 flip -- at which point it is a strategy change, not a fix to thesis_unknown. Closing this row by declaring nothing is also acceptable IF the finding is recorded as 'these legs deliberately have no entry regime condition', since thesis_unknown is then correct behaviour rather than a gap.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260823-TWELVE-PULLBACK-LEGS-HAVE-NO-ENTRY-REGIME-CONDITION
  status_at_migration: open
  severity: medium
  tier: |-
    3 (config/strategies.yaml — declaring adx_min CHANGES ENTRY ADMISSION, it is not observability)
  opened_by: |-
    bracket-expectations-session (found while implementing an operator-approved change; the approval rested on my own mischaracterisation, which is corrected here)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260823-TWELVE-PULLBACK-LEGS-HAVE-NO-ENTRY-REGIME-CONDITION
verdict: null
