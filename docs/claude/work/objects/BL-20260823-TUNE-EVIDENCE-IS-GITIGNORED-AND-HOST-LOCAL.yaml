# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260823-TUNE-EVIDENCE-IS-GITIGNORED-AND-HOST-LOCAL
type: question
parent_intent: null
title: |-
  The tune-before-demote gate reads `runtime_logs/strategy_tunes/`, which is GITIGNORED — so a tuning attempt is only 'on record' on the host that ran it
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-23'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ['Tune results land somewhere TRACKED, so an attempt counts wherever the review runs. `comms/` is the existing precedent for exactly this shape — comms/gpu_spend_ledger.json and comms/broker_truth_ledger.json are both committed file-backed records read by an API route. A committed index (strategy, param, date, chosen value, beats_baseline, train_oos_consistent) would be small and would version with the config it advises on.', "OR: the rule states explicitly that sweeps must be run on the live VM, and the reader's canonical path is the one /api/bot/strategies/{name}/tune serves. That is a documentation fix rather than a code one and is legitimate — but it must be WRITTEN, because today the requirement is only inferable from which host happens to have the file.", "Either way, `None` must keep meaning 'we could not look' and must keep leaving the matrix verdict standing. A fix that softens on an unreadable directory is not a fix.", 'State the population when this closes: how many legs have an attempt on record on the VM, against the 52 enabled. A count of 2 (today, in a sandbox that does not count) is the honest starting number.']
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260823-TUNE-EVIDENCE-IS-GITIGNORED-AND-HOST-LOCAL
  status_at_migration: open
  severity: medium
  tier: 1 (tooling / evidence durability)
  opened_by: |-
    full-system-audit-2026-08-23 (found while running the first sweep under the new rule)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260823-TUNE-EVIDENCE-IS-GITIGNORED-AND-HOST-LOCAL
verdict: null
