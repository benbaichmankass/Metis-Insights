id: WO-20260908-COORDINATION-BOARD-IS-AT-GITHUB-S-2500
type: question
parent_intent: IN-20260901-OPERATING-LAYER
title: >-
  Coordination board is at GitHub's 2500-comment cap: successor issue plus re-point its two readers
stage: INTEGRITY
lifecycle: in_flight
owner: session_01GtvQEQnfwzz9yRdPF6sPFg
opened_at: 2026-09-08
closed_at: null

reconciliation: >-
  ⚠️ THIS OBJECT ABSORBED A DUPLICATE. `WO-20260908-THE-COORDINATION-BOARD-IS-AT-GITHUB-S`
  was captured for the SAME problem under a different intent and is now
  `superseded_by` this one. THIS one was kept because it is the object the
  operator approved and the spawn registry keys on
  (`pending-20260908T044700Z`) — but it was the WEAKER of the two on both axes
  it differed, so both were taken from the duplicate rather than the keep:
  (1) `parent_intent` moved IN-20260903-TRADING-SYSTEM-HEALTH ->
  IN-20260901-OPERATING-LAYER, because a cross-session coordination channel is
  operating-layer machinery and touches no trading behaviour; and (2) the
  `done_condition` below is the duplicate's, which is strictly stronger —
  it names the CLAUDE.md / SessionStart / skill surfaces and demands the
  staleness check be shown to FIRE, neither of which this object originally
  asked for. Keeping the weaker text and merely deleting the duplicate would
  have silently dropped scope, which is the failure mode a reconciliation is
  supposed to prevent.

review_trigger: >-
  While `in_flight`, read the PR. If this object returns to `dormant`, re-read
  at the next cycle.

done_condition: >-
  A successor board exists and is reachable for BOTH reads and writes from a
  normal session; merge-claim-audit.yml and scope-overlap-audit.yml are
  re-pointed at it and OBSERVED posting/reading successfully (a green run whose
  post step was skipped clears nothing); the mandatory session-start read in
  CLAUDE.md, the SessionStart contract and the session-coordination skill name
  the new board; and the staleness check can FIRE again, demonstrated against a
  deliberately-stale control. ⚠️ THE LAST CLAUSE IS THE ONE MOST EASILY
  MIS-CLEARED: `board_pointer.py --self-test` and
  `check_board_coherence.py --self-test` exercise the refusal paths against
  planted controls, but a green self-test is a HARNESS result and cannot reach
  a live board — it clears the "can fire" half and NOT the live half.

why: >-
  Issue #6927 writes 403 and reads return a board permanently frozen at
  2026-09-07T11:28:17Z. merge-claim-audit.yml fails silently on every merge;
  scope-overlap-audit.yml reads the frozen board as input. Operator granted a
  spawn-priority exception 2026-09-08.
  ⚠️ ONE CLAIM IN THE DISPATCH BRIEF WAS MEASURED AND DOES NOT HOLD, and it
  matters because it changes what had to be built. The brief stated
  scope-overlap-audit.yml "reports 0 from a live session for every PR" and that
  this "reads as no collision". It does not: `check_scope_overlap.assess`
  already refused an empty START set as `could_not_check` with an explicit
  denominator assertion, so a fully-frozen board produced a LOUD yellow on
  every PR, not a false green. The real defect was narrower and different — the
  yellow is permanent and indistinguishable from a tooling gap, so it is the
  desensitized-alarm class this repo has a P1 rule for, and during the first 24h
  after the freeze the window still contained pre-freeze STARTs that were graded
  as if live. The fix therefore had to make the board's DEATH detectable
  (a heartbeat), not merely re-point a number.

blocked_on: []
blocked_on_basis: >-
  ASSESSED 2026-09-08 by session_01GtvQEQnfwzz9yRdPF6sPFg. Nothing BLOCKS the
  change: the repo work is complete and self-tested. What it WAITS on is not a
  blocker in the typed sense but must not be read as done — see
  `waiting_on_basis`.
waiting_on_basis: >-
  Two steps this session cannot take itself. (1) PROVISIONING: creating the
  successor issue requires an issue write, and this session's GitHub MCP is
  read-only for issues — `issue_write method=create` returned
  `403 Resource not accessible by integration` (measured 2026-09-08, the same
  boundary as BL-20260901). That is why `board-rotate.yml` exists: the runner's
  token can do what no PM-side session can. (2) MERGE: this PR edits
  `board-post.yml` and `session_registry.py`, both in `check_pr_landing`'s
  LANDING_MACHINERY set, so R12 bars it from self-landing and a human must read
  it once.

captured_by: scripts/ops/capture_idea.py
