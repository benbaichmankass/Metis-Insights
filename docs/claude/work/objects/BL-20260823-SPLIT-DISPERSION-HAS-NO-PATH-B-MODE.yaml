# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260823-SPLIT-DISPERSION-HAS-NO-PATH-B-MODE
type: question
parent_intent: null
title: |-
  `m20_split_dispersion` grades on PATH A only, so on a Path B cell it returns `pass_fraction: 0.0` and `split_sensitive: false` — and filtering on that boolean reads the stability of a FAILURE as evidence of a pass
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-23T21:10:00+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  The tool grades a cell against the claim it actually makes: a `--path {a,b}` selector (or a per-cell path carried in from the sweep) choosing between `beats(...)` and the Path B claim (net_R up in both windows, drawdown RECORDED not gated), with the path named in the output so a verdict states which test produced it. `split_sensitive` must then describe stability of THAT verdict. NOT sufficient: documenting the caveat -- the field name is what misleads, and a reader filtering on it will not have read the docstring.
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260823-SPLIT-DISPERSION-HAS-NO-PATH-B-MODE
  status_at_migration: open
  severity: medium
  tier: 1 (research tooling — no runtime path)
  opened_by: |-
    bracket-expectations-session (first run of the tool on a Path B population, which only became possible the same day)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260823-SPLIT-DISPERSION-HAS-NO-PATH-B-MODE
verdict: null
