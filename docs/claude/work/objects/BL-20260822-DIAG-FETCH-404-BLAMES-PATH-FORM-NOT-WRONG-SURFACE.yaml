# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260822-DIAG-FETCH-404-BLAMES-PATH-FORM-NOT-WRONG-SURFACE
type: commitment
parent_intent: null
title: |-
  diag_fetch.sh's 404 message names path-form as the cause when the real cause is a non-diag surface
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-22'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  RESOLVED when diag_fetch.sh given a non-diag api/* path prints a message naming the surface mismatch, proven by a test asserting the old path-form wording is NOT emitted for that input (presence of the new string alone is insufficient -- both branches must be distinguishable).
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260822-DIAG-FETCH-404-BLAMES-PATH-FORM-NOT-WRONG-SURFACE
  status_at_migration: open
  severity: low
  tier: 1 (ops tooling diagnostics)
  opened_by: s4-pairs-control (session wrap)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260822-DIAG-FETCH-404-BLAMES-PATH-FORM-NOT-WRONG-SURFACE
verdict: null
next_action_at_migration: |-
  Branch the 404 message on whether the requested path was already outside the diag surface. If the caller's argument starts with api/ but not api/diag/, say so explicitly -- 'this tool serves /api/diag/* only; <path> is a bot route, fetch it directly' -- instead of the path-form advice. Do NOT silently widen the tool to proxy arbitrary routes: the diag bearer is surface-scoped, and a fetcher that sometimes attaches it to non-diag routes is a worse problem than a confusing 404.
