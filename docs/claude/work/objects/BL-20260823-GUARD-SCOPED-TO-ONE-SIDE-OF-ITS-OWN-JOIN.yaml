# MIGRATED from a review backlog by scripts/ops/migrate_backlog_to_work_objects.py
# (Phase C, 2026-09-01). Arrives `dormant`: carried, not started, not queued.
# ⚠️ `blocked_on: []` here is NOT a claim that nothing blocks this — see
# `blocked_on_basis` below. Write a TRUE edge before moving this out of dormant.
id: BL-20260823-GUARD-SCOPED-TO-ONE-SIDE-OF-ITS-OWN-JOIN
type: question
parent_intent: null
title: |-
  exit-mechanism-coverage-guard reads config/lever_reachability.json and is not triggered by it — measured COSMETIC today, but it becomes real the moment a step is added
stage: INTEGRITY
lifecycle: dormant
owner: null
opened_at: '2026-08-23T13:04:02.424882+00:00'
closed_at: null
review_trigger: |-
  Re-read when this is pulled under an intent, or when a session working a related area finds it. A dormant object is NOT a queued one — nothing is scheduled to pick this up, and that is the honest state rather than a backlog promise nobody is keeping.
done_condition: |-
  ['If a step is ever added to exit-mechanism-coverage-guard that SURFACES the reachability verdict (a plain run, or --json), add config/lever_reachability.json to its when.globs in the same change -- at that point the read becomes verdict-bearing and the guard would be scoped to one side of its own join.', "Re-run python3 scripts/ci/check_guard_glob_coverage.py after any edit to the guard table; it reuses run_guards.is_relevant rather than re-implementing the matcher, so it cannot drift from the runner's own definition of relevance.", 'Close by either (a) the glob being added alongside such a step, or (b) a recorded perturbation showing the current steps still do not move -- re-verified, not assumed from this row.']
blocked_on: []
blocked_on_basis: |-
  NOT_ASSESSED — migrated in bulk 2026-09-01; no dependency edge was derived for this row and none is claimed. The empty list above is therefore NOT the claim 'nothing blocks this'. ⚠️ Write a real edge before moving this out of `dormant`: an invented edge would be read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.
source:
  backlog: docs/claude/health-review-backlog.json
  row_id: BL-20260823-GUARD-SCOPED-TO-ONE-SIDE-OF-ITS-OWN-JOIN
  status_at_migration: open
  severity: low
  tier: 1 (CI scoping only — no runtime path)
  opened_by: full-system-audit-2026-08-23 (operator-requested guard glob audit)
  migrated_on: '2026-09-01'
  note: |-
    The backlog row remains the state of record for the FINDING and its updates; this object is the state of record for the WORK of dealing with it. New findings are still filed to the backlog through scripts/ops/backlog_append.py::append_row, never here.
evidence:
- |-
  docs/claude/health-review-backlog.json#BL-20260823-GUARD-SCOPED-TO-ONE-SIDE-OF-ITS-OWN-JOIN
verdict: null
