id: WO-20260912-M20-ACTIVE-TRADE-MANAGEMENT-HOLD-WINNERS-LONGER
type: question
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: >-
  M20 Active Trade Management: hold winners longer in the current regime, measured end to end
stage: EVIDENCE
lifecycle: in_flight
owner: session_01Wv1BdfEUnmCBZrhvUjeBzb
opened_at: 2026-09-12
closed_at: null
review_trigger: >-
  Re-read at the next cycle. A captured idea is PARKED, not queued — it becomes
  work only when it is given an owner and moved out of `dormant`.
done_condition: >-
  The active-management levers that govern when a WINNER is closed are enumerated, measured against the post-2026-08-27 population, and each is graded as it-helps / it-hurts / not-gradeable with a stated population. At least one lever reaches a Tier-3 proposal with IS/OOS + walk-forward evidence behind it. NOT done when a memo exists -- done when a proposal the operator can accept or reject exists.

why: >-
  MI-277 measured the mechanism: winner hold collapsed 5.17h -> 1.90h while LOSER hold ROSE and losses stayed at exactly 1R. The sizer and stops are intact; what changed is how long a winner is allowed to run. That is the M20 subject by name.

source: >-
  operator 2026-09-12: active management is what we are working on right now, and no sessions should stop until we have resolved that issue

blocked_on: []
blocked_on_basis: >-
  ASSESSED 2026-09-12 by the manager (session_01HoFquGdTrD3TTGbMMRc43t). The
  empty list is a POSITIVE finding here, not the NOT-ASSESSED default it
  replaces: nothing blocks this object today, because MI-278 (research lane) is RUNNING it right
  now (session_01Wv1BdfEUnmCBZrhvUjeBzb) and was observed working - read off `list_sessions`, not inferred
  from the registry row. This is a statement about TODAY and about THIS object
  only. It is NOT a forecast: a Tier-2 or Tier-3 gate reached later in the
  queue is a real future blocker and must be written as a typed edge WHEN IT
  BINDS, never pre-emptively - a false blocker is worse than a missing one,
  and an anticipated one is a false one until the work arrives at it.

captured_by: scripts/ops/capture_idea.py

# ── Appended by the owning session (session_01Wv1BdfEUnmCBZrhvUjeBzb), 2026-09-12 ──
# The manager's object above is kept VERBATIM and is authoritative: it carries the
# operator's own directive and the manager's blocked_on_basis. This session created a
# competing copy before the manager's had landed on main (the registry row it was told
# to confirm was absent, so it could not tell). That copy is DISCARDED, not merged --
# only the unit breakdown below is carried across.
unit_breakdown: >-
  U1 enumerate what CAN end a winner, per leg, with what arms it. U2 attribute what DID,
  provenance-graded, and name the gap where it cannot be attributed. U3 counterfactual on
  the config-exact harness, IS/OOS with a yearly walk-forward, at the exit-refinement gate
  (Path A or Path B) -- not at a correlation bar. U4 one Tier-3 proposal per lever that
  cleared U3, evidence attached, routed to the manager. U5 the MFE/MAE-in-ATR instrument
  (BL-20260911-MFE-OVER-MAE-IN-ATR-UNITS-IS-A-LEADING-INDICATOR-NOBODY-COMPUTES).

progress:
  U1: >-
    DELIVERED, PR #11864. Inventory over 55 declared / 44 enabled+live legs; monitor-unit map
    IMPORTED from pipeline.monitor_unit_for; every call site ASSERTED against the unit source;
    cross-checked against 1000 journal rows, 24 pairs confirmed, 0 contradictions. giveback_stop
    -- the only lever that by construction fires on a trade that WAS winning -- is armed on 1 of
    44. Seven of eight ict_scalp_* legs arm exactly {tp_cross, sl_cross, breakeven_ratchet} and
    that unit reads no trail_mult. HEADLINE IS THE INSTRUMENT: reconciler_filled is 84 of 294
    closes and can never name a mechanism by construction (the reconciler watches the ENTRY
    order); _classify_broker_exit refuses on 27 of 27 broker-truth rows because each fill lands a
    median 0.55-0.79bp SHORT of the level its strict inequality tests.
  U2: >-
    DELIVERED, PR #11874. Over 183 closes / 49 winners with created_at >= 2026-08-27: NO EXIT
    LEVER IS CUTTING WINNERS SHORT -- the lever family is 3 of 49, and the two giveback_stop
    fires banked +$2,471.70. THE GAP IS THE FINDING: of the 31 winners short of their declared
    tp_r, 17 (55%) carry an unattributable or contaminated label. That share is INVARIANT to the
    label-recovery tolerance across 0.000R-0.250R, so it is a measurement and not a threshold
    artifact. MI-277 section 2.2 is reported UNGRADEABLE (its ratio swings 1.18-1.98 at n~10),
    and MI-277 section 3.3's cap_r figures could not be reproduced, so that table is deliberately
    NOT adjudicated.
  U3: >-
    RESHAPED BY U2's RESULT, and the change is deliberate rather than a drift from the brief.
    The brief says counterfactual "the mechanism(s) U2 implicates"; U2 implicates NONE, so
    running the briefed unit would measure a mechanism found idle. The only mechanism with
    enough attributed mass is the TAKE-PROFIT (18 of 49 winners ended exactly there; the stop
    family banked another 6 at 12-13% of declared target), so U3 tests target geometry and the
    trailing stop that precedes it. It must treat any live-population firing rate as a LOWER
    BOUND, because 43% of the window is unattributable or contaminated.
  U32: >-
    DELIVERED. scripts/research/e35_binomial_package_denominator.py re-derives
    e35_break_attribution's one-sided binomial per ORDER PACKAGE. NOT A REPEAT OF U31: this
    statistic takes TWO terms from row counts -- k,n from the E35 arm's POST rows AND the null p
    from the family-matched CONTROL's own rows/rows rate -- so fan-out reaches the sample size
    and the null hypothesis by different paths and one rows->packages swap cannot attribute the
    move. Four cells, threshold-free attribution. MEASURED pooled bybit_*, 204 rows / 155
    packages (1.3161x, 27 packages spanning >1 account, 4 packages disagreeing on win/loss, 0
    straddling the deploy, 0 rows without a package id): the four cells span 0.0243-0.1779 and
    ONE crosses alpha while both PURE bases read 0.0586 and 0.1010. Graded `offsetting` -- the
    substitutions push OPPOSITE ways and cancel, so the published p is stable by coincidence, not
    robustness; a plain rows-vs-packages re-derivation would have reported no change and been
    wrong to stop. THE DESCRIPTION AND THE INFERENCE DO NOT MOVE TOGETHER: the e35 delta is
    robust (-55.0 -> -57.6pp) while the FAMILY-MATCHED CONTROL'S DELTA FLIPS SIGN (+0.8 ->
    -8.3pp) -- opposite conclusions about whether the break is e35-specific, on 3 pre packages,
    which is a finding about that arm's fragility and NOT a new verdict. Per-account every cell
    collapses to one number at inflation ~1.0, reproducing U31's transferable finding that
    fan-out is CROSS-account and pooling is the exposure; bybit_1's residual 1.0362x is three
    ict_scalp_* packages writing several rows on ONE account, lands entirely in control_other,
    and is NOT fan-out. Carried a behaviour-preserving refactor of the subject (arm_of/ARMS
    extracted from grade()), proven byte-identical on the real 1000-row pull.
    !! THE UNIT'S OWN CORRECTION, and it corrects this object too: re-measured with U12's
    instrument, price_path_not_deduped reads 21 -- UNCHANGED since U12 named it. U19, U31 and
    U32 each built a re-derivation BESIDE the exposed surface rather than changing it, so
    nothing has left the list. U31's board note said '17 of 21 remain' and U32's own START said
    '16'; both are wrong and the true count has never moved. Filed as
    BL-20260913-THE-FANOUT-SURFACE-COUNT-HAS-BEEN-QUOTED-AS-SHRINKING-BY-THREE-CONSECUTIVE-UNITS-AND-THE-INSTRUMENT-SAYS-IT-HAS-NEVER-MOVED.
    Population fingerprint is BYTE-IDENTICAL to U31's (rowset sha256:1752f298..., order
    sha256:b073ee2c..., ids 5731->4732), so the two memos differ by method and never by pull --
    U26's stanza paying off on its first re-use. Memo:
    docs/research/m20-u32-e35-binomial-package-denominator-2026-09-13.md

