id: WO-20260912-M20-ACTIVE-TRADE-MANAGEMENT-HOLD-WINNERS-LONGER
type: question
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: 'M20 Active Trade Management: hold winners longer in the current regime, measured end to end'
stage: EVIDENCE
lifecycle: in_flight
owner: session_01Wv1BdfEUnmCBZrhvUjeBzb
opened_at: 2026-09-12
closed_at: null
review_trigger: Re-read at the next cycle. A captured idea is PARKED, not queued — it becomes work only
  when it is given an owner and moved out of `dormant`.
done_condition: The active-management levers that govern when a WINNER is closed are enumerated, measured
  against the post-2026-08-27 population, and each is graded as it-helps / it-hurts / not-gradeable with
  a stated population. At least one lever reaches a Tier-3 proposal with IS/OOS + walk-forward evidence
  behind it. NOT done when a memo exists -- done when a proposal the operator can accept or reject exists.
why: 'MI-277 measured the mechanism: winner hold collapsed 5.17h -> 1.90h while LOSER hold ROSE and losses
  stayed at exactly 1R. The sizer and stops are intact; what changed is how long a winner is allowed to
  run. That is the M20 subject by name.'
source: 'operator 2026-09-12: active management is what we are working on right now, and no sessions should
  stop until we have resolved that issue'
blocked_on: []
blocked_on_basis: 'ASSESSED 2026-09-12 by the manager (session_01HoFquGdTrD3TTGbMMRc43t). The empty list
  is a POSITIVE finding here, not the NOT-ASSESSED default it replaces: nothing blocks this object today,
  because MI-278 (research lane) is RUNNING it right now (session_01Wv1BdfEUnmCBZrhvUjeBzb) and was observed
  working - read off `list_sessions`, not inferred from the registry row. This is a statement about TODAY
  and about THIS object only. It is NOT a forecast: a Tier-2 or Tier-3 gate reached later in the queue
  is a real future blocker and must be written as a typed edge WHEN IT BINDS, never pre-emptively - a
  false blocker is worse than a missing one, and an anticipated one is a false one until the work arrives
  at it.'
evidence:
- docs/research/m20-u2-winner-close-attribution-2026-09-12.md
- docs/research/m20-u2b-risk-denominator-ratchet-2026-09-12.md
- docs/research/m20-u3-scalp-target-sweepable-2026-09-12.md
- docs/research/m20-u4-scalp-exit-proposals-2026-09-12.md
- docs/research/m20-u5-excursion-regime-instrument-2026-09-12.md
note: 'PROGRESS 2026-09-12, by the owner. The done_condition is NOT met: no Tier-3 proposal exists yet,
  and that is correct rather than behind schedule -- U3b''s screening arm cannot support one. What is
  established:

  (1) ENUMERATED. U1 (PR #11864, not yet merged) inventoried every mechanism that can end a winner across
  55 declared / 44 enabled+live legs, with the monitor-unit map IMPORTED from pipeline.monitor_unit_for
  and every claimed call site ASSERTED against the unit source. A mechanism fires only if the unit implements
  the call site AND the YAML declares the key; on that basis giveback_stop -- the only lever that by construction
  fires on a trade that WAS winning -- is armed on 1 leg of 44.

  (2) MEASURED, AND IT REFUTES THE BRIEFED HYPOTHESIS. U2 attributed every winning close over the post-2026-08-27
  population (183 closes / 49 winners): the whole exit-lever family accounts for 3 of 49, and the two
  giveback_stop fires BANKED +$2,471.70. No lever is cutting winners short. The briefed U3 was "the counterfactual
  for the mechanism U2 implicates" and U2 implicates none, so U3 was re-pointed at target geometry and
  the trailing stop.

  (3) THE INSTRUMENT WAS PARTLY BROKEN AND IS NOW REPAIRED. U2b established that order_packages.sl is
  NOT entry-frozen -- the break-even ratchet amends it in place -- so an R denominator read off it shrinks
  selectively on WINNERS and never on losers, which is exactly the asymmetry a winner-vs-loser R comparison
  rests on. MI-277''s winner-R collapse survives (-63% -> -49%) but its LEVELS were inflated 2.3x. This
  is the cycle priority applied literally: repair the measurement before acting on what it says.

  (4) U3b''s SCREENING ARM IS READ AND SETTLES NOTHING, BY CONSTRUCTION. 0 of 28 target cells cleared
  the IS/OOS gate over 4 legs, and on 4 of 4 the dominant direction REVERSES between IS and OOS -- period
  character, not a lever. But every cell ran under the harness''s 24-bar force-close, which production
  has on NO ict_scalp leg, and that defect is not neutral across this grid: it truncates preferentially
  the trades a wider target would have won. A negative here is EXPECTED whether or not the lever is real,
  so it must not be reported as "the target sweep came back negative". The PARITY arm (timeout_bars=100000)
  is dispatched and is the only arm a hold-length proposal may cite.

  WHAT IS NOT ESTABLISHED, stated so it is not read as covered: the break-even ratchet -- now the leading
  candidate lever, and newly sweepable as a be_off cell -- has NOT been run at parity either; and no lever
  has yet reached the yearly walk-forward that the exit-refinement gate requires BEFORE a Tier-3 proposal.
  Two of four units are merged; U1 and U3 both declare landing: hold and need a human merge click.'
decision_requests:
- id: DEC-20260912-ICT-SCALP-XRP-15M-TARGET
  question: 'MI-278 swept every exit lever the ict_scalp family arms. ONE cell survived the full M20 gate:
    ict_scalp_xrp_15m tp_at_r 1.5 -> 1.25 (take profit SOONER). Ship it, or record a reject?'
  urgency: normal
  asked_on: '2026-09-12'
  asked_by: session_01Wv1BdfEUnmCBZrhvUjeBzb
  context: 'TIER-3, one field on one leg, nothing else changes. EVIDENCE: run 34684742080, m27_data XRPUSDT
    15m, split 2025-07-01, config-exact base, timeout_bars=100000 (LIVE PARITY). IS dR +5.82 dDD -2.53
    (n198), OOS dR +5.93 dDD -3.72 (n117) -- the ONLY cell in the whole workstream to improve net_R AND
    maxDD in BOTH windows -- then yearly walk-forward 3/4 usable folds (need 3) -> PASS. n_OOS 117 clears
    the MIN_OOS_TRADES=25 floor. [!] THE DIRECTION IS OPPOSITE TO THE QUESTION THAT STARTED THIS WORK.
    MI-277 measured winner hold collapsing 5.17h -> 1.90h and this object is titled "hold winners longer".
    Nothing tested makes winners run longer and survives; the one survivor takes profit sooner. [!] FOUR
    THINGS THAT MUST TRAVEL WITH ANY YES. (1) It passes by EXACTLY the minimum -- 3 of 4 usable folds,
    and 2021/2022 are SKIPPED because those years are empty on m27_data, so one fold moving flips it to
    a fail. (2) PATH A ONLY: the gate has two qualifying paths and the sweep graded one; Path B''s capital-efficiency
    evidence was not recorded when this ran. (3) The same narrowing FAILS on other legs (btc_5m at parity:
    +31.42R IS, -10.71R OOS), so there is no family-level claim. (4) xrp_15m is a modest book -- base
    OOS total_R 6.48 over 116 trades -- so +5.93R OOS is large relatively and small absolutely. [!] WHAT
    IS NOT PROPOSED AND WHY: the break-even ratchet is REFUTED (0 of 7 legs at parity; disarming costs
    R in-sample and RAISES in-sample maxDD on 6 of 7, i.e. it is buying the protection it is meant to
    buy); widening family-wide is refused (5 of 7 parity legs negative in BOTH windows); the stop buffer
    is BUILT and UNRUN, so no claim; avax_5m''s two widening candidates are HELD pending their walk-forward
    because their pass rests on a maxDD gain that is n=1 in EPISODES. Full evidence: docs/research/m20-u4-scalp-exit-proposals-2026-09-12.md
    and docs/research/m20-u3-scalp-target-sweepable-2026-09-12.md.'
  options:
  - key: ship_xrp_15m_tp125
    label: Ship it — xrp_15m tp_at_r 1.5 -> 1.25
    implication: A one-field Tier-3 PR, landing hold, merged by a human, then /api/bot/config read back
      to confirm ict-git-sync carried it to the running trader (a merge is not a deploy), then an OPEN-ITEMS
      row requiring a closed xrp_15m trade that actually reached the new 1.25R target against broker truth
      -- deployed is not observed.
  - key: reject_single_leg
    label: Reject — one leg on a minimum-margin walk-forward is not enough
    implication: 'Nothing is owed. The coverage cell records passed_unshipped, which is the honest state:
      measured, cleared the gate, not shipped. The evidence stays in U3''s memo.'
  - key: wait_for_path_b
    label: Wait — re-run with Path B recorded, then decide
    implication: Costs one sweep (~40 min). The gate has two qualifying paths and this verdict used one,
      so a Path B re-run could either confirm it or surface cells this arm discarded. It cannot overturn
      the Path A pass, only add to it.
- id: DEC-20260912-ICT-SCALP-PARITY-STAMP
  question: 'May an ict_scalp backtest number be quoted without stating whether it was produced at LIVE
    PARITY? Measured: the harness''s 24-bar force-close is worth up to 99% of a leg-window''s entire reported
    profit, and it CHANGES verdicts.'
  urgency: normal
  asked_on: '2026-09-12'
  asked_by: session_01Wv1BdfEUnmCBZrhvUjeBzb
  context: 'Rated ABOVE any single leg''s target by the unit that measured it. Production has NO time-based
    exit on any ict_scalp leg; the harness force-closes at 24 bars. MEASURED: on sol_15m OOS the baseline
    goes +20.46R -> +0.20R at parity (99% of the reported profit is the clock), while eth_15m OOS goes
    the other way (+15.73 -> +19.13), so the effect varies in SIGN by leg and window and is not a fleet
    constant. It is not cosmetic: removing the clock FLIPPED xrp_15m tp1.25R from non-candidate to the
    walk-forward survivor above, and moved individual cells by up to 23.17R. [!] AND THE COVERAGE MATRIX
    IS BLIND TO IT: 160 of 320 tested verdicts address the VENUE-CAP parity axis and only 3 of 320 address
    the TIME-EXIT axis -- 0 of 24 on ict_scalp. The matrix already has a working discipline for declaring
    one parity axis (193 cells literally say LIVE PARITY); it was simply never applied to this one, so
    the remedy is applying an existing mechanism rather than building one.'
  options:
  - key: require_parity_stamp
    label: Require a parity stamp on every ict_scalp verdict
    implication: Every new cell already emits one (the [timeout IS x% OOS y%] readout shipped in this
      unit). The cost is re-stating the ~24 existing ict_scalp cells as measured-at-24-bar, or re-running
      them. Makes a screening verdict impossible to mistake for a parity one.
  - key: reprice_existing_only
    label: Re-run the existing ict_scalp cells at parity, no standing rule
    implication: Fixes the current matrix without adding a standing obligation. The next harness with
      a parity gap reproduces the problem.
  - key: accept_as_is
    label: Accept the gap and record the decision
    implication: 'Legitimate, and must be RECORDED as a decision rather than left looking like work about
      to start. Consequence stated: ict_scalp backtest numbers stay non-comparable to live, and a reader
      cannot tell which verdicts that affects.'
- id: DEC-20260912-ICT-SCALP-XRP-15M-STOP-BUFFER
  question: 'A SECOND cell has now survived the full M20 gate, on the SAME LEG: ict_scalp_xrp_15m atr_sl_buffer_mult
    0.20 -> 0.10 (stop TIGHTER). Ship it, ship the target one (DEC-20260912-ICT-SCALP-XRP-15M-TARGET),
    ship NEITHER -- but NOT BOTH. Which?'
  urgency: normal
  asked_on: '2026-09-12'
  asked_by: session_01Wv1BdfEUnmCBZrhvUjeBzb
  options:
  - key: ship_stop_buffer_only
    label: Ship the STOP only — ict_scalp_xrp_15m atr_sl_buffer_mult 0.20 -> 0.10
    implication: 'The better-measured of the two: IS dR +7.13 / dDD -1.02 (n=198), OOS dR +3.75 / dDD
      -0.15 (n=117), walk-forward 3 of 4 usable folds. A one-field Tier-3 PR, landing hold, merged by
      a human, then /api/bot/config read back to confirm ict-git-sync carried it to the running trader
      -- a merge is not a deploy -- then an OPEN-ITEMS row until a closed trade is observed under the
      new stop. It TIGHTENS the stop, so it also changes the R denominator on every subsequent xrp_15m
      trade: expectancyR before and after are not comparable without saying so.'
  - key: ship_target_only
    label: Ship the TARGET only — ict_scalp_xrp_15m tp_at_r 1.5 -> 1.25
    implication: IS dR +5.82 / dDD -2.53 (n=198), OOS dR +5.93 / dDD -3.72 (n=117), walk-forward 3 of
      4. Same landing shape. This is DEC-20260912-ICT-SCALP-XRP-15M-TARGET's option; choosing it here
      means answering that request and declining the stop one, not answering both.
  - key: ship_neither
    label: Ship neither — one leg of seven, both failing the same 2024 fold
    implication: 'A legitimate and defensible answer: the two survivors are on ONE leg out of seven, both
      fail the SAME 2024 fold, and both NARROW the trade while this object is titled hold winners longer.
      Choosing it CLOSES the question rather than deferring it, and the memo records that MI-278''s sweep
      produced no shippable change.'
  - key: commission_a_joint_sweep
    label: Neither yet — commission a joint (tp_at_r x atr_sl_buffer_mult) sweep first
    implication: 'The honest answer to why they are mutually exclusive: each was measured with the OTHER
      held at its live value, so their gains are NOT additive and the pair has never been tested together.
      A 2-D sweep on this one leg is cheap (the harness and both flags already exist) and would settle
      whether the two are the same edge counted twice. Cost: one more sweep round before anything ships.'
  context: 'TIER-3, one field on one leg, nothing else changes. EVIDENCE: run 34689877269, m27_data XRPUSDT
    15m, split 2025-07-01, config-exact base, timeout_bars=100000 (LIVE PARITY, measured -- the cell stamps
    [timeout IS 0% OOS 0%]). IS dR +7.13 dDD -1.02 (n199), OOS dR +3.75 dDD -0.15 (n117), then yearly
    walk-forward 3/4 usable folds (need 3) -> PASS [2023:PASS 2024:- 2025:PASS 2026:PASS]. n_OOS 117 clears
    MIN_OOS_TRADES=25. Population: 42 cells over 7 legs, 4 candidates on 3 legs, this is the only walk-forward
    survivor. [!] THIS IS WHY IT IS A SEPARATE DECISION AND NOT AN AMENDMENT: the two survivors are NOT
    ADDITIVE and must not both be shipped on this evidence. Each was measured independently against a
    base holding the other at its LIVE value; neither run measured the pair. They interact BY CONSTRUCTION
    -- on this family the stop distance IS the R unit (sl = sweep_extreme +/- atr_sl_buffer_mult * ATR),
    so narrowing it SHRINKS R, and tp_at_r is denominated in R. A ''1.25R'' target after the stop change
    is a materially different absolute move from the 1.25R that was measured. Applying both produces a
    geometry NEITHER cell tested, and the combined effect is not the sum of the two dRs. If both are wanted
    that is a THIRD sweep (a joint cell), which is the commission_a_joint_sweep option. [!] BOTH FAIL
    THE SAME FOLD -- 2024 carries neither -- so ''two survivors on one leg'' is WEAKER corroboration than
    it sounds, not stronger: it is a common-mode weakness in the evidence for both, not two independent
    confirmations. Both also pass by exactly the minimum, with 2021/2022 empty on m27_data, so one fold
    moving flips either. [!] MY PREFERENCE, STATED AS A PREFERENCE AND NOT A RESULT: marginally the stop
    buffer -- its IS gain is larger and its drawdown effect is negative in both windows, and it leaves
    the declared target at 1.5. BUT THE TARGET PROPOSAL HAS THE BETTER OOS ON BOTH METRICS (+5.93/-3.72
    against +3.75/-0.15), and OOS is the window that should carry more weight, so an operator who weights
    OOS over IS should pick the target. The preference is genuinely marginal and I am not going to manufacture
    a stronger reason than the evidence supports. [!] PATH A ONLY, for both -- Path B''s thresholds are
    operator-reserved and ungraded repo-wide, so neither has cleared a FULL gate. [!] AND NOTE WHAT THE
    STOP CHANGE DOES TO THE INSTRUMENT: it re-denominates every R figure this leg reports afterwards.
    Not a reason to reject it; a reason not to compare post-change R to pre-change R without saying so.
    FULL WRITE-UP: docs/research/m20-u4-scalp-exit-proposals-2026-09-12.md PROPOSAL 3 and the mutual-exclusion
    section; evidence in docs/research/m20-u3-scalp-target-sweepable-2026-09-12.md section 4q.'
captured_by: scripts/ops/capture_idea.py
unit_breakdown: U1 enumerate what CAN end a winner, per leg, with what arms it. U2 attribute what DID,
  provenance-graded, and name the gap where it cannot be attributed. U3 counterfactual on the config-exact
  harness, IS/OOS with a yearly walk-forward, at the exit-refinement gate (Path A or Path B) -- not at
  a correlation bar. U4 one Tier-3 proposal per lever that cleared U3, evidence attached, routed to the
  manager. U5 the MFE/MAE-in-ATR instrument (BL-20260911-MFE-OVER-MAE-IN-ATR-UNITS-IS-A-LEADING-INDICATOR-NOBODY-COMPUTES).
progress:
  U1: >-
    DELIVERED, PR #11864. Inventory over 55 declared / 44 enabled+live legs; monitor-unit map
    IMPORTED from pipeline.monitor_unit_for; every call site ASSERTED against the unit source;
    cross-checked against 1000 journal rows, 24 pairs confirmed, 0 contradictions. giveback_stop
    -- the only lever that by construction fires on a trade that WAS winning -- is armed on 1 of
    44. Seven of eight ict_scalp_* legs arm exactly {tp_cross, sl_cross, breakeven_ratchet} and
    that unit reads no trail_mult. HEADLINE IS THE INSTRUMENT: reconciler_filled is 84 of 294
    closes and can never name a mechanism by construction (the reconciler watches the ENTRY
    order); _classify_broker_exit refuses on 27 of 27 broker-truth rows because each fill lands a
    median 0.55-0.79bp SHORT of the level its strict inequality tests.
  U2: >-
    DELIVERED, PR #11874. Over 183 closes / 49 winners with created_at >= 2026-08-27: NO EXIT
    LEVER IS CUTTING WINNERS SHORT -- the lever family is 3 of 49, and the two giveback_stop
    fires banked +$2,471.70. THE GAP IS THE FINDING: of the 31 winners short of their declared
    tp_r, 17 (55%) carry an unattributable or contaminated label. That share is INVARIANT to the
    label-recovery tolerance across 0.000R-0.250R, so it is a measurement and not a threshold
    artifact. MI-277 section 2.2 is reported UNGRADEABLE (its ratio swings 1.18-1.98 at n~10),
    and MI-277 section 3.3's cap_r figures could not be reproduced, so that table is deliberately
    NOT adjudicated.
  U3: >-
    RESHAPED BY U2's RESULT, and the change is deliberate rather than a drift from the brief.
    The brief says counterfactual "the mechanism(s) U2 implicates"; U2 implicates NONE, so
    running the briefed unit would measure a mechanism found idle. The only mechanism with
    enough attributed mass is the TAKE-PROFIT (18 of 49 winners ended exactly there; the stop
    family banked another 6 at 12-13% of declared target), so U3 tests target geometry and the
    trailing stop that precedes it. It must treat any live-population firing rate as a LOWER
    BOUND, because 43% of the window is unattributable or contaminated.
  U31: >-
    DELIVERED, PR pending. THE CYCLE PRIORITY APPLIED TO THIS OBJECT'S OWN EVIDENCE: repair the
    measurement before acting on what it says. Working clause 1 of
    BL-20260911-A-RATE-PER-TRADE-ROW-COUNTS-ACCOUNT-FANOUT-AS-INDEPENDENT-AND-INFLATES-N-UNEQUALLY,
    whose 21 named surfaces were 19 unpaid. The most load-bearing was
    bleed_attribution_2026_09_11.py -- the script behind the -$38,851.81 figure the loud
    OI-20260911 rests on -- which reads order_package_id ZERO times (positive control: account_id
    twice) and, verified off the script rather than off prose, does NOT scope by account, so its
    arms are POOLED. THE FINDING, and it is this row's first conclusion change: pooled, the e35
    pre/post stop-rate Fisher goes p=8e-05 on ROWS to p=0.05703 on PACKAGES -- it crosses alpha and
    is the ONLY arm in the table that changes; the other three are neither_significant on both.
    !! THE MECHANISM IS NOT THE UNEQUAL INFLATION THIS ROW WAS FILED ABOUT: on this window the e35
    arms inflate almost equally (1.444x / 1.471x). It is ~a third fewer independent observations
    plus the post arm's stop-outs collapsing 15 rows -> 8 packages, 1.875x against its own 1.471x,
    i.e. concentrated in fanned packages. !! AND THE USEFUL HALF: scoped to bybit_1 the inflation is
    1.0581x and e35 is BYTE-IDENTICAL on both denominators, because FAN-OUT IS CROSS-ACCOUNT -- so a
    per-account reading is immune and the whole exposure is in the pooled arms. That says WHICH
    READING IS SAFE rather than passing verdict on the analysis.
    !! pnl_sum IS DELIBERATELY NOT RE-DERIVED. It is an ACCOUNT fact; a package denominator would
    UNDERSTATE money actually lost, and a session revising the headline DOWNWARD would commit the
    row's error in the opposite direction -- the likelier mistake, because a package denominator
    sounds uniformly more careful. The deliverable is the PARTITION (10 price_path / 9 account_fact
    / 1 both_stated / 0 undeclared), completeness-checked by CALLING summarise, not reading it.
    !! IT DOES NOT WEAKEN OI-20260911's HEADLINE and does not touch its clears_when. It narrows one
    supporting claim -- the e35 contrast, the row's own leading candidate -- which makes the row's
    existing framing MORE right: it already records that the bleed predates e35 by three days.
    !! A MISLABEL OF MY OWN, caught by running on live data rather than reading: a single verdict
    string returned `loses_significance` for an arm that was never significant (b4_geometry,
    0.3043 -> 0.4643) and `strengthens` for two that stayed non-significant. Sub-class A
    unprovenanced diagnostic output; fixed by branching on the actual condition -- two orthogonal
    never-collapsed facts, `significance` and `strength` -- with both live mislabels pinned as
    regression tests naming their own figures.
    WHAT REMAINS: 17 of 21 surfaces, three of them the Tier-2 dashboard routes (no live figure was
    re-graded -- that is a decision, not a side effect of a re-derivation).
    Evidence: docs/research/m20-u31-bleed-record-package-denominator-2026-09-13.md
  U32: >-
    DELIVERED. scripts/research/e35_binomial_package_denominator.py re-derives
    e35_break_attribution's one-sided binomial per ORDER PACKAGE. NOT A REPEAT OF U31: this
    statistic takes TWO terms from row counts -- k,n from the E35 arm's POST rows AND the null p
    from the family-matched CONTROL's own rows/rows rate -- so fan-out reaches the sample size
    and the null hypothesis by different paths and one rows->packages swap cannot attribute the
    move. Four cells, threshold-free attribution. MEASURED pooled bybit_*, 204 rows / 155
    packages (1.3161x, 27 packages spanning >1 account, 4 packages disagreeing on win/loss, 0
    straddling the deploy, 0 rows without a package id): the four cells span 0.0243-0.1779 and
    ONE crosses alpha while both PURE bases read 0.0586 and 0.1010. Graded `offsetting` -- the
    substitutions push OPPOSITE ways and cancel, so the published p is stable by coincidence, not
    robustness; a plain rows-vs-packages re-derivation would have reported no change and been
    wrong to stop. THE DESCRIPTION AND THE INFERENCE DO NOT MOVE TOGETHER: the e35 delta is
    robust (-55.0 -> -57.6pp) while the FAMILY-MATCHED CONTROL'S DELTA FLIPS SIGN (+0.8 ->
    -8.3pp) -- opposite conclusions about whether the break is e35-specific, on 3 pre packages,
    which is a finding about that arm's fragility and NOT a new verdict. Per-account every cell
    collapses to one number at inflation ~1.0, reproducing U31's transferable finding that
    fan-out is CROSS-account and pooling is the exposure; bybit_1's residual 1.0362x is three
    ict_scalp_* packages writing several rows on ONE account, lands entirely in control_other,
    and is NOT fan-out. Carried a behaviour-preserving refactor of the subject (arm_of/ARMS
    extracted from grade()), proven byte-identical on the real 1000-row pull.
    !! THE UNIT'S OWN CORRECTION, and it corrects this object too: re-measured with U12's
    instrument, price_path_not_deduped reads 21 -- UNCHANGED since U12 named it. U19, U31 and
    U32 each built a re-derivation BESIDE the exposed surface rather than changing it, so
    nothing has left the list. U31's board note said '17 of 21 remain' and U32's own START said
    '16'; both are wrong and the true count has never moved. Filed as
    BL-20260913-THE-FANOUT-SURFACE-COUNT-HAS-BEEN-QUOTED-AS-SHRINKING-BY-THREE-CONSECUTIVE-UNITS-AND-THE-INSTRUMENT-SAYS-IT-HAS-NEVER-MOVED.
    Population fingerprint is BYTE-IDENTICAL to U31's (rowset sha256:1752f298..., order
    sha256:b073ee2c..., ids 5731->4732), so the two memos differ by method and never by pull --
    U26's stanza paying off on its first re-use. Memo:
    docs/research/m20-u32-e35-binomial-package-denominator-2026-09-13.md
  U33: >-
    DELIVERED. Tested the candidate BL-20260912-THE-NETTING-ATTRIBUTED-PATH-CARRIES-THE-LARGEST-SHARE-OF-THE-WINNER-COLLAPSE-AND-IS-100-PERCENT-ESTIMATED named as untested
    and called 'a different unit'. CONFIRMED AT THE FIELD: _netting_rows_to_attribute computes
    take = min(qty, remaining) from excess = journal_qty - backed -- a subtraction of two
    AGGREGATES distributed by a leg_gone/FIFO heuristic, with no venue fill anywhere. That
    explains U16's sum_matched_unique = 0 of 12 in one line. !! AND THE FIELD SAYS THREE THINGS
    THE CANDIDATE DID NOT. (1) _netting_apply_close's PARTIAL branch does NOT close the row: it
    writes position_size = qty - take and leaves status='open', so the row closes LATER as sl /
    sl_cross / reconciler_filled -- and MI-277 annex A6, U9 and U16 ALL scope this contamination
    by exit_reason == 'netting_attributed', a filter structurally unable to see it. (2) The notes
    stamp that would reveal it is DROPPABLE: neither netting_attribution_basis nor
    netting_attributed_qty is in json_notes._DEFAULT_PROTECTED, whose own comments record THREE
    prior instances of exactly this cap shedding a load-bearing key. (3) src/runtime/provenance.py
    has NO QUANTITY AXIS -- position_size 0, quantity 0, size 0 against exit_price_source 12 --
    so classify_pnl grades the PRICE and is_measured() returns True for a row whose quantity was
    manufactured. MEASURED over 765 bybit rows of the same pull U31/U32 used (rowset
    sha256:1752f298...): filter matches 50, stamp census 52, filter misses 2, and 111 ungradeable
    from the journal. JOINED AGAINST A SECOND INDEPENDENT SOURCE -- the attribution soak, written
    before the DB is touched and not subject to the cap -- 4 of its 29 applied trades carry NO
    stamp at all, every one labelled with an ordinary exit reason. TOTAL INVISIBLE, BOTH SOURCES:
    6; the journal alone sees 2, so a journal-only audit under-counts 3x. ada_pullback_2h is an
    E35 LEG, so one lands in the e35 arm U32 measured; trend_donchian_eth and eth_pullback_2h in
    control_same_family; three of the six are ict_scalp_*, MI-277's own subject; and 5404 is on
    bybit_portfolio, which mirrors real-money bybit_2 by an enforced invariant. !! NO PUBLISHED
    FIGURE IS RESTATED and six rows will not move a headline -- what changes is the SCOPING RULE.
    !! NO CLOSE IS CLAIMED FALSE: option (b) of the parent row stays exhausted and was not
    re-attempted. !! THE 111 ARE BOUNDED, NOT RESOLVED -- the soak is itself a 1000-line tail of a
    3,879,840-byte file. !! NOTHING IS FIXED: order_monitor.py and json_notes.py are Tier-2 and
    untouched; two remedies are named in the backlog and neither is applied. Filed
    BL-20260913-NETTING-ATTRIBUTION-REWRITES-POSITION-SIZE-ON-A-ROW-THAT-STAYS-OPEN-SO-THE-EXIT-REASON-FILTER-EVERY-ANALYSIS-USES-CANNOT-SEE-IT
    and
    BL-20260913-PROVENANCE-GRADES-THE-PRICE-AND-HAS-NO-QUANTITY-AXIS-SO-IS-MEASURED-RETURNS-TRUE-FOR-A-ROW-WHOSE-QUANTITY-WAS-MANUFACTURED.
    Memo: docs/research/m20-u33-netting-position-size-basis-2026-09-13.md
  U36: >-
    DELIVERED. Adjudicated PB-20260906-R-CONTAMINATION-QUANTIFIED-AND-THE-HEADLINE-SIGN-IS-WRONG
    against the LIVE endpoint and CORRECTED IT: its criterion is unsound and the mechanism it
    root-caused is provably not operating. MEASURED 2026-09-13 on
    /api/bot/performance?window=30d (HTTP 200, 41 REAL-MONEY trades, since 2026-08-14T04:02:16Z):
    expectancyR +0.1113 against totalPnl -14.5552 and profitFactor 0.8251 -- the sign disagreement
    IS still live, so the row's criterion is not met. !! BUT rBasis READS 41/41 declaredInitial
    with storedStop 0, verified per leg as well as in the headline, so MI-144's
    r_multiple_provenanced is FULLY BINDING and the trailed-stop collapse cannot have touched any
    published R. rProvenance.contaminated still reads 11 and that is NOT a contradiction --
    classify_r grades the STORED STOP while rBasis says what the R was DIVIDED BY, a distinction
    the route's own comment draws. CONSEQUENCE: the row's remedy applied TODAY would exclude rows
    whose R the mechanism never reached. !! AND THE CRITERION IS UNSOUND ON ITS OWN TERMS: R is pnl
    over the trade's own risk, so a book whose winners are sized smaller than its losers MUST show
    positive mean R and negative dollars; requiring agreement would force a Tier-2 route the
    operator reads to publish a wrong number. WHAT EXPLAINS IT, per leg and summing exactly to the
    headline: dollars-per-R is 1.480 and 2.961 on the two POSITIVE-R legs against 3.793, 4.274 and
    4.577 on the three NEGATIVE-R legs -- TOTAL SEPARATION, gap 0.832, spread 3.09x, threshold-free.
    Every profitable leg is sized smaller per unit of risk than every losing one. Filed as
    BL-20260913-EVERY-PROFITABLE-LEG-IS-SIZED-SMALLER-PER-UNIT-OF-RISK-THAN-EVERY-LOSING-ONE-ON-REAL-MONEY
    (Tier-3, PROPOSES NOTHING). Sibling PB-20260821-R-AND-DOLLARS-DISAGREE-IN-SIGN annotated too,
    since PB-20260906 says to close them together. NOTHING IS CHANGED: /api/bot/performance is
    src/web/ Tier-2 and is one of the three dashboard routes U12 named as untouchable. Instrument:
    scripts/research/r_sign_split_basis.py (23 self-test + 23 pytest controls; 7 planted defects,
    all caught). Memo: docs/research/m20-u36-r-sign-is-risk-size-2026-09-13.md

  U34: >-
    DELIVERED. Worked PB-20260821-SLV-TREND-1H-ZERO-WINS-IN-13's next_action, which THREE prior
    sessions re-measured around without ever running. !! THE PACKETS ALREADY EXISTED AND NOBODY
    READ THEM: a daily cron has committed one index per day to comms/strategy_reviews/ since
    2026-09-01 -- ELEVEN of them -- and every one reads actionable: 0 over graded: 52. RAN THE
    PRODUCER FOR THE FLEET: scripts/ops/build_strategy_evidence.py over all 52 enabled legs, 52
    records committed to comms/strategy_evidence/ -- 19 MEASURED (12 faithful, 7 approximate), 10
    no_harness, 23 harness_failed, against a prior coverage of ONE (the builder's own verification
    case). That discharges the precondition
    BL-20260909-STRATEGY-REVIEW-PACKET-GRADES-EDGE-OFF-A-LIVE-WINDOW-WHICH-THE-PROMOTION-EVIDENCE-RULE-FORBIDS
    named in its own progress note.
    !! AND IT CHANGES NO VERDICT, PROVEN BY RUNNING IT RATHER THAN READING IT: with the records in
    place, htf_pullback_trend_2h (FAITHFUL, net_r_oos +2.7261, n=79) grades hold, and
    trend_donchian (net_r_oos -7.1079, n=75) ALSO grades hold. decide() consults the offline record
    only to choose between NO_OFFLINE_EVIDENCE and 'hold'; the KILL/DEMOTE matrix begins at
    `elif n < 30` and is gated on n_closed >= 20 LIVE closes -- the calendar-time accrual
    docs/CLAUDE-RULES-CANONICAL.md's promotion-evidence rule forbids. MI-216 shipped the producer
    with 'the producer comes FIRST'; the consumer was never re-pointed.
    !! TWO OF PB-20260821'S FOUR LEGS ARE ON REAL MONEY AND THAT ROW HAS NEVER SAID SO --
    eth_pullback_2h on bybit_2 and tlt_pullback_1h on alpaca_live, both execution: live -- while the
    row singles out slv_trend_1h, which is paper-only and already in shadow.
    !! AND BOTH LEGS THAT CAN BE GRADED SHOW A POSITIVE OFFLINE EDGE, the opposite of the row's
    premise; htf_pullback_trend_2h's +2.73 at n=79 FAITHFUL is the first evidence the mechanism has
    ever offered about a leg it demoted to shadow on 2026-08-23. NOT a promotion proposal -- Tier-3,
    not made here.
    !! SHADOW IS A ONE-WAY DOOR: both shadow legs grade horizon_class structurally_ungradeable with
    days_to_floor NULL, so the remedy that row proposes removes a leg from the surface that would
    ratify it.
    !! A NEW BLOCKER ON THE PRODUCER HALF: 13 of the 23 harness_failed are a FEED. Positive control
    -- data.binance.vision HTTP 200 while Yahoo returns 429 / SSLError -- so 19 equity/ETF and 5
    futures legs are unbuildable here, and yfinance is in requirements-backtest.txt but NOT
    requirements.txt. Filed as
    BL-20260913-THE-OFFLINE-EDGE-BUILDER-CANNOT-GRADE-ANY-EQUITY-OR-FUTURES-LEG-BECAUSE-YFINANCE-IS-NOT-IN-REQUIREMENTS-TXT.
    !! COMMITTING THE RECORDS MAKES THE NEXT INDEX LOOK HEALTHIER WHILE CHANGING NOTHING -- 18 legs
    move no_offline_evidence -> hold and actionable stays 0. Stated in the memo and in both backlog
    rows so the label change is not read as progress. NO Tier-3 change is proposed; decide() is
    untouched. Memo: docs/research/m20-u34-disposition-surface-cannot-grade-2026-09-13.md
  U38: >-
    DELIVERED. Worked
    BL-20260912-A-1000-ROW-TAIL-PULL-CHANGES-A-FANOUT-PACKAGES-MEMBERSHIP-WITHIN-HOURS-SO-A-PACKAGE-LEVEL-RATE-OVER-A-TAIL-IS-UNSTABLE-BY-CONSTRUCTION,
    whose own text says an honest negative closes it. IT DOES, AND THAT WAS THE SMALL HALF.
    !! THE TRUNCATION ANSWER IS A DERIVATION, NOT A RE-RUN (which the row rules out). /api/diag/journal
    is ORDER BY id DESC LIMIT n with no offset and no filter, so a missing member has an id BELOW the
    minimum; the pull is DENSE (0 gaps over 1000 ids) so it cannot be an interior row, and id order
    agrees with created_at order (0 inversions over 999 adjacent pairs) so a member below the edge was
    created no later than the edge row. On one live pull (ids 4732-5731, rowset sha256:5b1cacd6..., 793
    packages): ONE package of 793 (0.13%) within reach, 776 beyond, 16 unbounded_class, 0 ungradeable.
    Single-row packages are graded too and must be -- a truncated fan-out's remnant IS a single row; 0
    of 683 were within reach. The state is named beyond_observed_reach, never complete: it is a bound
    from an observed maximum (5.918s), not a proof.
    !! THE BIG HALF: order_package_id IS NOT A FAN-OUT KEY. SIXTEEN of the 110 multi-row packages
    (14.5%, 41 of 317 rows) repeat an account_id, and a package holding two rows for the SAME account
    cannot be one cross-account fan-out -- they are one id REUSED across re-entries (pkg-4f8140865a9a4441
    is four bybit_1/ict_scalp_sol_15m longs at 20:12, 05:05, 08:00, 11:07; pkg-4b0f61d469e64c00 is two
    ib_paper/mgc_trend_1h shorts 4.1 DAYS apart). The classes separate with NO THRESHOLD TO CHOOSE: the
    94 distinct-account packages max 5.918s spread (median 2.16s, zero above 60s) against 3907s-353436s
    -- a 662x gap with nothing in between. CONTIGUITY IS NOT THE TEST: 12 of 16 are non-contiguous but
    FOUR ARE CONTIGUOUS and one spans 105 ids.
    !! AND IT MOVES A PUBLISHED CAUSAL NUMBER, ONE-SIDEDLY. Running U15's own arm builder and U22's own
    band UNMODIFIED (positive control: 168 of 168 unit packages recognised), ALL TEN reuse units that
    fall inside U15's cells are in control_post and NONE in the other three, carrying SIX OF THAT CELL'S
    SEVEN disagreements (7 -> 1, n 71 -> 61). Band 3.8..29.6pp -> 6.1..23.7pp, WIDTH narrowing 32%. THE
    SIGN SURVIVES BOTH, so U15/U19/U22 are not contradicted on direction. NO EXCLUSION IS PROPOSED and
    the instrument refuses to -- a planted 'RECOMMENDATION: exclude' line is caught by its own controls.
    !! A SECOND, UNPLANNED FINDING: hypothesis (b) of
    BL-20260912-U15S-OWN-CODE-NO-LONGER-REPRODUCES-U15S-OWN-PUBLISHED-TABLE-ONE-CONTROL-PACKAGE-FLIPPED-AND-NOBODY-WOULD-HAVE-KNOWN is OBSERVED, not merely a
    candidate -- and it needed no retained pull, only two DECLARED fingerprints. U31/U32 declared order
    sha256:b073ee2c... / rowset sha256:1752f298... over ids 5731->4732; U38's pull hours later the same
    day has the SAME ids, the SAME order digest and a DIFFERENT rowset digest (sha256:5b1cacd6...), so at
    least one row was RE-STAMPED between them. It does NOT attribute the 30-vs-31 (digests cannot name
    the changed rows) and must not be read as closing that row. This is U26's stanza paying off a second
    time and the first time it discriminated anything.
    !! THREE TRUNCATION DETECTORS TESTED, ALL FAIL, EACH WITH A POSITIVE CONTROL: linked_trade_id points
    outside the observed member set 0 of 520 times; no order_packages field declares an expected fan-out
    width; and GET /api/bot/db/table/trades?filter_col=order_package_id -- which COULD complete a package
    -- carries Depends(require_session) and returns 401 to a DIAG_READ_TOKEN session (/api/diag/version
    -> 200, same host, same bearer). So the row's 'pull by id range rather than tail limit' candidate is
    NOT AVAILABLE to a research session.
    Filed BL-20260913-ORDER-PACKAGE-ID-IS-NOT-A-FANOUT-KEY-16-OF-110-MULTI-ROW-PACKAGES-REPEAT-AN-ACCOUNT-AND-CARRY-6-OF-7-DISAGREEMENTS-IN-ONE-DID-CELL.
    Instrument scripts/research/package_fanout_integrity.py (68 self-test + 33 pytest controls; 19
    defects planted and 19 caught -- two ESCAPED on the first pass and both were control gaps, not module
    bugs, now closed; 4 no-op plants fired zero). Nothing in src/, config/ or any unit file is touched.
    Memo: docs/research/m20-u38-order-package-id-is-not-a-fanout-key-2026-09-13.md
  U23: >-
    DELIVERED as the object's own done_condition deliverable -- a Tier-3 PROPOSAL, PR pending.
    IT PROPOSES CHANGING NO TARGET PARAMETER. U2 established the brief's premise is false (no exit
    lever is cutting winners short; the lever family is 3 of 49 winners and giveback_stop's two
    fires BANKED +$2,471.70), so the take-profit is the only lever with attributed mass -- 18 of 49
    winners ended exactly there. The M20 walk-forward arm set then STRUCTURALLY CANNOT grade it:
    measured over the complete 246 arms / 33 legs / 3 families, tp_geometry is PERFECTLY CONFOUNDED
    with family AND block_unit (uncapped <=> scalp <=> per_leg; capped <=> {donchian,pullback} <=>
    family_pooled), NO family carries both geometries, all 246 arms share tp_cap_pct 0.099 so there
    is no dose, and verdict tracks data volume (candidate median 23 folds / 288 OOS vs 16 / 222).
    The seductive 75%-vs-30% candidate-rate gap IS the scalp-vs-others gap -- the same 28 arms.
    THE ASK: two named research arms (a within-family geometry contrast, and a cap dose), with the
    acceptance criterion stated in advance so it cannot be chosen afterwards. Neither touches live
    money, config/ or the order path.
    !! THIS DOES NOT SATISFY done_condition AND SAYS SO: that condition requires a lever with IS/OOS
    + walk-forward BEHIND it, and the finding is that the walk-forward cannot support the lever.
    THE OBJECT STAYS in_flight. Closing it on this would be marking my own homework.
    !! It also WITHDRAWS a claim this session repeated in several board posts and PR bodies -- 'three
    decision requests on the work object are unanswered'. The object file contains the string
    'decision' ZERO times; the claim is unsupported and its origin is unknown.
    Memo: docs/research/m20-u23-tier3-proposal-target-geometry-2026-09-12.md.
    Audit: scripts/research/m20_target_geometry_evidence_audit.py (19 controls, 4 planted defects).
  U1: 'DELIVERED, PR #11864. Inventory over 55 declared / 44 enabled+live legs; monitor-unit map IMPORTED
    from pipeline.monitor_unit_for; every call site ASSERTED against the unit source; cross-checked against
    1000 journal rows, 24 pairs confirmed, 0 contradictions. giveback_stop -- the only lever that by construction
    fires on a trade that WAS winning -- is armed on 1 of 44. Seven of eight ict_scalp_* legs arm exactly
    {tp_cross, sl_cross, breakeven_ratchet} and that unit reads no trail_mult. HEADLINE IS THE INSTRUMENT:
    reconciler_filled is 84 of 294 closes and can never name a mechanism by construction (the reconciler
    watches the ENTRY order); _classify_broker_exit refuses on 27 of 27 broker-truth rows because each
    fill lands a median 0.55-0.79bp SHORT of the level its strict inequality tests.'
  U2: 'DELIVERED, PR #11874. Over 183 closes / 49 winners with created_at >= 2026-08-27: NO EXIT LEVER
    IS CUTTING WINNERS SHORT -- the lever family is 3 of 49, and the two giveback_stop fires banked +$2,471.70.
    THE GAP IS THE FINDING: of the 31 winners short of their declared tp_r, 17 (55%) carry an unattributable
    or contaminated label. That share is INVARIANT to the label-recovery tolerance across 0.000R-0.250R,
    so it is a measurement and not a threshold artifact. MI-277 section 2.2 is reported UNGRADEABLE (its
    ratio swings 1.18-1.98 at n~10), and MI-277 section 3.3''s cap_r figures could not be reproduced,
    so that table is deliberately NOT adjudicated.'
  U3: RESHAPED BY U2's RESULT, and the change is deliberate rather than a drift from the brief. The brief
    says counterfactual "the mechanism(s) U2 implicates"; U2 implicates NONE, so running the briefed unit
    would measure a mechanism found idle. The only mechanism with enough attributed mass is the TAKE-PROFIT
    (18 of 49 winners ended exactly there; the stop family banked another 6 at 12-13% of declared target),
    so U3 tests target geometry and the trailing stop that precedes it. It must treat any live-population
    firing rate as a LOWER BOUND, because 43% of the window is unattributable or contaminated.
