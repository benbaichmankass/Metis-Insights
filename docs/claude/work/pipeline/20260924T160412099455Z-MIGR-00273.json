{
  "due_when": {
    "check_every_days": 14,
    "clears_when": "OBSERVABLE: issue a diag request whose response EXCEEDS the byte budget and whose filter names a nonexistent column; the returned comment must still show filter_state='ignored_unknown_column' (or an explicit relay-level warning that certification fields were dropped). Demonstrate by running it — a passing case on a small response proves nothing, since the bug only exists above the budget.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260816-TRUNCATION-STRIPS-THE-FIELDS-THAT-CERTIFY-A-RESPONSE",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260816-TRUNCATION-STRIPS-THE-FIELDS-THAT-CERTIFY-A-RESPONSE",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260816-TRUNCATION-STRIPS-THE-FIELDS-THAT-CERTIFY-A-RESPONSE'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] vm-diag-snapshot truncation removes filter_state/total/count FIRST, because they sit after `rows` — a truncated response keeps the data and loses the fields that invalidate it"
}
