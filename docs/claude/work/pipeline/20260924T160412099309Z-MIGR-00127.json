{
  "due_when": {
    "check_every_days": 30,
    "clears_when": "Operator has exported the full account history from each real-money exchange AND Claude has ingested the historical closed trades into trade_journal.db (status/exit_price/pnl/closed_at) with provenance — OR the operator explicitly decides the pre-window historical gap is not worth backfilling and says to close this item.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260621-ACCOUNT-HISTORY-PULL",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260621-ACCOUNT-HISTORY-PULL",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260621-ACCOUNT-HISTORY-PULL'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] ASK OPERATOR: manually export FULL exchange account history (bybit_2 + all real-money accounts) so pre-7-day-window orphaned/missing trades can be entered into trade_journal.db"
}
