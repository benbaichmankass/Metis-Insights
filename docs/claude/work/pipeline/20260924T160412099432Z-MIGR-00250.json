{
  "due_when": {
    "check_every_days": 30,
    "clears_when": "BOTH: (1) the fold-offset/total-sort machinery is on `main` -- verified by `git merge-base --is-ancestor <commit> origin/main` returning success AND a non-zero `git show origin/main:scripts/research/m20_exit_head_round.py | grep -c fold-offset`; and (2) a launched screen RECORDS the code version it ran -- the round's `_round_meta` carries the git SHA of the checkout, so an arm and its control can be shown to share a code version rather than assumed to. (2) is the durable half: merging fixes today's instance, but only the stamp makes a future version drift visible in the artifact instead of in a crash. Verification: point at the merge commit and at one round_report.json carrying a git SHA.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260815-FOLD-DISPERSION-EVIDENCE-RUNS-ON-AN-UNMERGED-BRANCH",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260815-FOLD-DISPERSION-EVIDENCE-RUNS-ON-AN-UNMERGED-BRANCH",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260815-FOLD-DISPERSION-EVIDENCE-RUNS-ON-AN-UNMERGED-BRANCH'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] Every M20 fold-dispersion screen was produced by code that exists ONLY on an unmerged branch, and the trainer's checkout of that branch is unpinned and silently reverted mid-workstream."
}
