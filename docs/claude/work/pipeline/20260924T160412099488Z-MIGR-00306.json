{
  "due_when": {
    "check_every_days": 45,
    "clears_when": "Closed when a session OTHER than the one that wrote the fix records exit 0 plus the serving base, at the default network level, with no override. A second failure instead is the more valuable outcome and should reopen the parent row.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260820-DIAG-FETCH-CANONICAL-BASE-VERIFIED-FROM-ONE-SESSION-ONLY",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260820-DIAG-FETCH-CANONICAL-BASE-VERIFIED-FROM-ONE-SESSION-ONLY",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260820-DIAG-FETCH-CANONICAL-BASE-VERIFIED-FROM-ONE-SESSION-ONLY'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] The diag_fetch ordered-candidate fix is verified from exactly ONE session - the claim that matters ('a FRESH session's first diag_fetch succeeds with no override') has not been tested"
}
