{
  "due_when": {
    "check_every_days": 30,
    "clears_when": "#10666 merges and deploys, AND a real wedged-close episode on the live fleet is OBSERVED producing a widening page sequence rather than a fixed-cadence one -- counted from /api/bot/logs?level=error over the episode's own window, with the episode's duration stated so the count has a denominator. A passing unit test is NOT this observation. ⚠️ Do NOT close this on 'the pages stopped': pages stopping is equally consistent with the close path being fixed and the position never wedging again, which would tell you nothing about the pager. The clearing evidence needs a wedge that actually happened.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260901-CLOSE-FAIL-ALARM-DESENSITISED",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260901-CLOSE-FAIL-ALARM-DESENSITISED",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260901-CLOSE-FAIL-ALARM-DESENSITISED'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] The close-failure pager repeats on a modulo of the streak, so its rate is set by the exit-loop tick and never widens or caps — ~160 pages per wedged session"
}
