{
  "due_when": {
    "check_every_days": 30,
    "clears_when": "Make the read three-state at the HELPER (`orders` / `could_not_look` / `empty`) or give it a sibling that raises, then update every caller to branch on it -- `_cancel_resting_orders_for_symbol` and `_cancel_oca_group_for_symbol` at minimum. Ends when a forced `openTrades()` failure makes each of those callers report a read failure rather than a clean zero, asserted by a test per caller. Registering the state with `collapsed-state-guard` is the durable half.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260826-OPEN-TRADES-COLLAPSES-A-READ-FAILURE-INTO-AN-EMPTY-BOOK",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260826-OPEN-TRADES-COLLAPSES-A-READ-FAILURE-INTO-AN-EMPTY-BOOK",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260826-OPEN-TRADES-COLLAPSES-A-READ-FAILURE-INTO-AN-EMPTY-BOOK'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] IBClient._open_trades swallows the read exception and returns [] -- every caller reads 'we could not look' as 'no orders rest'"
}
