{
  "due_when": {
    "check_every_days": 7,
    "clears_when": "exit-head-donchian-1h-v1's live stage (shadow vs advisory) and its trainer model-registry presence are both confirmed via a fresh diag pull, reconciling the 2026-09-24 ml-review's 'order-influencing / no registry entry / silent ~52h' finding against this session's 2026-09-26 code-level 'stage=shadow' read -- and, if genuinely still shadow with real recovered_R on the table, an ml-review or drift-remediation lane evaluates the shadow->advisory promotion (ml/cli.py promote-stage) with RQ-20260922-007 as supporting evidence, subject to its own caveats (n=353 with no train/test split on this head artifact; the 73-trade evidence baseline IS a walk-forward OOS figure, this replay is a full-window, not provably out-of-sample, figure).",
    "kind": "observation"
  },
  "id": "PI-20260926-6M9R1WSA-0001",
  "next_action": "check_observation",
  "origin": {
    "kind": "research",
    "ref": "RQ-20260922-007 / research/results/RQ-20260922-007/36257896539.jsonl / comms/reviews/ml-review-20260924-210245.json",
    "rerun": "Re-run research-exit-head-replay-trainer.yml (workflow_dispatch, strategy=trend_donchian) and diff its landed stage field against a fresh trainer-vm-diag read of runtime_logs/trainer_mirror/exit_head/exit-head-donchian-1h-v1.json on both the trainer and the live VM; separately re-run the gate-check/stage-guard pass the 2026-09-24 ml-review used (trainer-vm-diag issue #12902-style) to see whether the model now has a registry entry and whether its shadow_predictions.jsonl rows are current (not still silent)."
  },
  "state": "queued",
  "what": "RQ-20260922-007 (research/results/RQ-20260922-007/36257896539.jsonl, verdict pass, attributable_to omitted_exit_head, recovered_R +3.7796 net-of-full-cost stack, head_simulated_net_r_oos 8.3813 vs evidence baseline 4.6017, n=353 trades over the full 1830d replay window, no train/test split available on this head artifact -- see note) does NOT translate into a config PR. Verified by reading code: trend_donchian / trend_donchian_sol / trend_donchian_eth already declare exit_head_model/threshold/action in config/strategies.yaml, and src/runtime/exit_head_apply.py + exit_head_shadow.py already wire the live monitor to CONSUME and (when gated open) APPLY the head -- live since 2026-07-12 (#6211/#6216/#6217), with harness/live parity enforced by shared would_exit_for/_feature_row functions (exit_head_replay.py imports them, never reimplements) and by tests/test_exit_head_apply_parity.py + tests/test_exit_head_feature_parity.py. The ONLY gate stopping a real close today is exit_head_apply.exit_head_verdict()'s `rec.get('stage') == 'advisory'` check: the mirrored artifact (runtime_logs/trainer_mirror/exit_head/exit-head-donchian-1h-v1.json) was read live on the trainer during this run's replay (2026-09-26) and reports stage='shadow'. export_exit_head.py's own docstring confirms every export defaults to stage='shadow' and that graduating to 'advisory' is a SEPARATE, deliberate ml/cli.py `promote-stage` action (Tier-3, operator-gated) -- not a git-tracked config/param, so no config PR is opened per this unit's own branching. Leg/account scope: trend_donchian (BTCUSDT 1h) trades bybit_2 (real money, Stage 2) with bybit_portfolio mirroring it exactly; trend_donchian_sol/_eth are separate legs routed via the operator- supervised prop manual bridge, not bybit_2/bybit_portfolio. UNRESOLVED DISCREPANCY, filed rather than silently reconciled: comms/reviews/ml-review-20260924-210245.json (2 days earlier) calls this SAME model_id one of '4 advisory (order-influencing) models' while ALSO reporting it has NO ENTRY in the trainer's own model registry (gate-check/stage-guard cannot evaluate it) and its predictions had been SILENT for ~52h. That is inconsistent with today's code-level read of stage='shadow' on the same model_id, and this session could not reconcile it without a fresh diag pull (out of this lane's scope: RQ-007 asked for a code/config read at minimum, not a live VM diag dispatch). Caveat correction: the dispatch's boilerplate 'fee-only harness, D1 not landed' caveat does NOT apply to this specific replay -- exit_head_replay.py's own cost_basis is fee 7.5bps + slippage 3.0bps + funding 1.0bps/8h (full venue cost stack via src.runtime.execution_costs, landed 2026-08-09 per code comment), not fee-only."
}
