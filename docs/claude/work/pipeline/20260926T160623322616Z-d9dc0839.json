{
  "due_when": {
    "clears_when": "PR #13002 (claude/options-legs-instrumentation) is merged and deployed, or closed",
    "kind": "event"
  },
  "id": "PI-20260926-KNSTSR8N-0003",
  "next_action": "ask_operator",
  "origin": {
    "kind": "review",
    "ref": "https://github.com/benbaichmankass/Metis-Insights/pull/12922",
    "rerun": "python3 scripts/ops/leg_flow_report.py --window-hours 28 --json | python3 -c 'import json,sys; r=json.load(sys.stdin); print([l for l in r[\"legs\"] if l[\"account_id\"]==\"alpaca_options_paper\"])'"
  },
  "routed_to": "PR #13002 (claude/options-legs-instrumentation)",
  "state": "routed",
  "what": "E18 triage, post-E35 rerun (window 2026-09-25T08:01:33Z -> 2026-09-26T12:01:33Z, 79h->28h; population 63 live legs): alpaca_options_paper/slv_pullback_1d (173 intents, 3 episodes) and alpaca_options_paper/gdx_pullback_1d (140 intents, 5 episodes) both grade `starved` (received=0, held_back=0, has_open_position=false) and are genuinely unexplained -- not election, not gating, not dry_run, not a symbol-fetch gap. Ruled out this session, each with evidence: (1) dry_run -- /api/bot/config shows alpaca_options_paper yaml_mode=live. (2) symbol-fetch -- config/accounts.yaml declares symbols: [SLV, GDX], matching both strategies exactly. (3) config-level gate -- config/strategies.yaml has both at execution=live, enabled=true; neither name appears in config/regime_policy.yaml (no off-cell can apply). (4) election collision -- the only other SLV strategy on this account, slv_trend_1h, is execution=shadow (excluded from live legs by enumerate_live_legs), and no other account strategy competes for GDX; the identical strategy (slv_pullback_1d) is `starved` with has_open_position=true (i.e. holding, not unreached) on alpaca_paper/alpaca_portfolio/alpaca_live simultaneously in the same window -- the SAME signal is clearly reaching and executing on 3 sibling accounts. (5) NOT an options-pipeline refusal -- src/units/accounts/execute.py:520-530 journals a `status=\"rejected\"` trades row on every refusal branch of place_options_expression (bad_package / contracts_read_failed / snapshot_read_failed / empty_chain / no_selection / sizing / place_failed), wrapped in try/except so a journaling failure only logs a warning, never silently drops the row -- confirmed historically true for this exact account (14 `rejected` trades rows for slv_trend_1h/slv_pullback_1d going back to 2026-09-09, pulled via journal?table=trades). Zero trades rows of ANY status exist for either leg on this account in the post-E35 window -- not even a rejection. Since a refusal inside place_options_expression always journals, and these two legs never reach even that refusal-journal step, the order package for (alpaca_options_paper, slv_pullback_1d) and (alpaca_options_paper, gdx_pullback_1d) appears to never reach execute_pkg at all this window -- i.e. the gap is upstream, in per-account dispatch/eligibility (multi_account_execute), not in the options-expression branch itself. Root cause not isolated within this Tier-1 read-only lane's scope; PROPOSED next step (Tier-2/3, touches order-path code, NOT shipped here): instrument or bisect multi_account_execute's per-account eligibility loop specifically for account_id=alpaca_options_paper to find where these two packages stop, before assuming the options branch itself is at fault. -- UPDATE 2026-09-26 (Tier-2 instrumentation lane, manager session_01Ljhs6sFAdWHdMDhJpL5aBP, ceiling $10): opened PR #13002 (branch claude/options-legs-instrumentation) with instrumentation only, no fix. Traced the path from the multiplexer through E35's per-account election to multi_account_execute and the options-expression branch: neither leg is in intents.DEFAULT_PRIORITIES, so E35's election is not in play for this pair -- pipeline._dispatch_rounds falls back to the single unscoped round and multi_account_execute(pkg) dispatches with account_scope=None. Inside multi_account_execute every exception path reachable once an account enters the per-account loop already calls log_rejection_to_journal (itself wrapped in its own try/except, cannot raise) -- RiskBreach, BelowVenueMinQtyRefusal, the generic except Exception, the zero-sized-qty and below-venue-min early-outs. The only two places that can drop an account with LITERALLY ZERO trace are both upstream of the loop: _dispatch_exclusion_reason's pre-loop eligibility filter (not_configured / symbol_exchange_routing / strategies_declared_empty / strategy_not_assigned / outside_account_scope, src/core/coordinator.py:1129-1172) and the in-loop account_type scope check. Both are correct BY DESIGN for the routine case (2026-05-08 operator directive; see tests/test_s029_pr1_account_strategy_filter.py) but excluded_by_account is only surfaced when the WHOLE round sizes empty -- one account dropped alongside three siblings that succeed left no trace anywhere. No cause was isolated statically for why THESE two (account, strategy) pairs specifically hit one of these reasons this window -- every config field checked (strategies: assignment, account_class, the options: block, SLV/GDX instrument-exchange routing) reads identically to the three working sibling accounts. PR #13002 adds a _log_dispatch_drop() trace at both silent points (WARNING for the anomalous reasons, DEBUG for the two routine/high-volume ones) plus tests -- observe-only, no dispatch-decision or journal-write change. Tier-2, landing=hold; awaiting operator OK for the manager to merge and deploy."
}
