{
  "due_when": {
    "check_every_days": 30,
    "clears_when": "Either (a) the guard refuses to run against a dirty tree for the files it scans, exiting with a distinct message naming the uncommitted paths (never a claim-shaped error), or (b) it resolves added lines and the annotation window against the SAME tree so line numbers and window both refer to one version. Verified by: with a correctly-annotated claim in a file, insert N unrelated lines ABOVE it without committing, run `python3 scripts/check_impossibility_claims.py --base origin/main`, and observe it either pass or emit the dirty-tree message -- NOT an unsubstantiated-claim error. Second check: any line number it does report must contain the quoted claim text (assert the file's line N contains the snippet the message quotes).",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260815-IMPOSSIBILITY-GUARD-PHANTOM-FINDING-ON-DIRTY-TREE",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260815-IMPOSSIBILITY-GUARD-PHANTOM-FINDING-ON-DIRTY-TREE",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260815-IMPOSSIBILITY-GUARD-PHANTOM-FINDING-ON-DIRTY-TREE'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] impossibility-claim-guard reports a phantom finding, at a wrong line number, when the working tree is dirty"
}
