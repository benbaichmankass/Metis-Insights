{
  "due_when": {
    "check_every_days": 45,
    "clears_when": "The audit can grade a lever from a CAPPED backtest's max_mfe_r / MFE distribution when no journal rows are available, keeping the three states distinct (reachable / inert / unmeasured) and stating which basis produced each verdict — a backtest-derived verdict must never be presented as a live-journal one. NOT sufficient: supplying journal rows by hand once; the point is that the cheap, large-n basis exists and the tool cannot consume it.",
    "kind": "observation",
    "last_checked": "2026-09-21"
  },
  "id": "BL-20260823-LEVER-REACHABILITY-AUDIT-BLIND-WITHOUT-JOURNAL-ROWS",
  "next_action": "check_observation",
  "origin": {
    "kind": "review",
    "ref": "docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json#BL-20260823-LEVER-REACHABILITY-AUDIT-BLIND-WITHOUT-JOURNAL-ROWS",
    "rerun": "python3 -c \"import json; d=json.load(open('docs/archive/2026-09-21-operating-reset/registers/health-review-backlog.json')); items=d['items'] if isinstance(d, dict) else d; print(json.dumps(next(i for i in items if i.get('id')=='BL-20260823-LEVER-REACHABILITY-AUDIT-BLIND-WITHOUT-JOURNAL-ROWS'), indent=2))\""
  },
  "state": "queued",
  "what": "[health] `lever_reachability_audit.py` grades all 8 declared levers `unmeasured` because it needs live journal rows, while the backtest already carries the answer over hundreds of trades"
}
