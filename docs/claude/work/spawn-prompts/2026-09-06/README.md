# Spawn dispatches — 2026-09-06 day shift

The exact text each sub-session was dispatched with, committed so a successor
manager can read what a session was actually told rather than inferring it from
its output. Written by manager `session_01HrmZ1RRNM4UnEUaFdrPEjj`.

Each file is the `session_registry.py register` prompt (the standing contract)
followed by a task-specific section under `## THE ACTUAL TASK`.

| file | registry key | work object |
|---|---|---|
| `1A-observation-sweep.md` | `pending-20260906T084622Z` | `WO-20260906-THE-OBSERVATION-SWEEP-54-OPEN-ITEMS-ROWS` |
| `1B-loss-attribution.md` | `pending-20260906T084729Z` | `WO-20260906-REAL-MONEY-WENT-0-FOR-13-THE` |
| `1C-live-alarms.md` | `pending-20260906T084730Z` | `WO-20260906-THREE-LIVE-ALARMS-ON-THE-TRADING-FLEET` |

⚠️ These are a RECORD, not a template. The standing-contract half is generated by
`session_registry.py::spawn_prompt` and will drift as that file is corrected —
regenerate it, never copy from here.
