{
 "schema_version": 1,
 "what_this_is": "The SHORT list every session reads at START — and its live contents are RENDERED INTO CLAUDE.md by scripts/ops/render_session_brief.py, because CLAUDE.md is the only channel that reliably reaches a session before its first tool call. Hooks do NOT run on Claude Code on the web (verified 2026-08-26: the SessionStart contract's output was absent from the session context; 1,379 consecutive 'Found 0 total hooks in registry' log lines on 2026-08-20), and CI guards fire at MERGE, which is far too late to make the work correct in the first place.",
 "every_row_declares_how_it_leaves": "`clears_when` names the OBSERVABLE condition that removes the row. 'the fix works' is not one; 'the mechanism is seen acting on a named trade' is. A row nobody can tell is finished never gets cleared.",
 "loud_means_report_it_every_session": "A row with `loud: true` must be REPORTED ON in the session's closing summary — checked and stated, not silently carried. Operator directive 2026-08-26: 'keep a LOUD backlog item for EVERY SESSION to check and report on it till it's implemented correctly and the mechanism has been verified.'",
 "updated_at": "2026-08-30T10:00:00+00:00",
 "items": [
  {
   "id": "OI-20260826-MHG-OVER-COVER-MECHANISM-UNVERIFIED",
   "opened": "2026-08-26",
   "kind": "monitoring",
   "summary": "The MHG disjoint-OCA over-cover was CLEARED by hand; the mechanism that should have caught and reported it is NOT yet proven.",
   "detail": "ib_paper MHG held a 29-lot position against TWO disjoint OCA groups — 58 lots of resting stop, 200%. OCA cancels only WITHIN a group, so one stop firing flattens the position and the other group's legs sell another 29 into a naked SHORT. The stranded leg was cancelled 2026-08-26 in a maintenance window (issues #10332-#10338) and the position now rests under ONE group (oca-protect-t4796, STP 6.364 + LMT 7.1415) matching journal trade 4796. THAT IS THE SYMPTOM CLEARED, NOT THE MECHANISM VERIFIED. Two mechanisms remain unproven on a real event: (1) _emit_stop_over_cover_alert — shipped 2026-08-25, reaches the operator feed, and has never fired on a NEW over-cover; the three ib_stop_over_cover rows in the feed predate the clear. (2) cancel-ib-order's reporting — it read the successful cancel as refused_by_venue (fixed 2026-08-26, BL-20260826-CANCEL-IB-ORDER-READS-ERROR-202-AS-A-REFUSAL), and that fix has run against a fake venue only. === 2026-08-26, session b9kyip — CRITERION (a) IS NOW SATISFIED; THE ROW STAYS OPEN because it requires BOTH. (a) MET, and verified the way the criterion demands — by FINDING THE ROW in /api/bot/logs?level=error, not by reading code. A NEW disjoint-group over-cover paged the operator feed at 2026-08-26T15:17:19Z: 'ib_paper/MGC: position 50.0 but resting STOP qty totals 101.0 (202%) across 2 DISJOINT OCA groups 834864174(clientId=597, other_session), oca-protect-t5007(clientId=497, this_session)'. A DIFFERENT symbol from the MHG event cleared by hand, so a genuinely new detection, not an echo. Population: the ERROR+ feed, limit=1000 returning 396 rows (a SHORT page, so the tail is proven), spanning 2026-08-20T10:58:50Z to 2026-08-26T15:17:19Z, containing 10 over-cover rows. (b) NOT met — cancel-ib-order has still not run against the real gateway. AND THE MGC PAGE NAMES WHY (b) IS HARD, which is the part to carry forward: one group was submitted by clientId 597 while the trader runs 497, and IB binds cancel rights to the submitter, so the trader CANNOT cancel that leg itself. Same shape as OO-20260825-IB-PAPER-MHG-STOP-OVER-COVER being 'blocked on WORK, not a decision' — a repair path that assumes the trader can cancel any leg it can SEE is not a repair path. SEPARATELY the Bybit half shipped 2026-08-26 IS ALSO FIRING: 'bybit_1/SOLUSDT: position 292.3 but resting SL legs total 2500.6 (855%)' at 09:37:59Z, the largest in the feed; reduceOnly, so NOT the naked-reverse hazard — the harm is the 20-leg cap and a dead trade's leg cutting a live position. bybit_1 is demo and ib_paper is paper, so NO REAL MONEY is exposed by either; stated so the severity is not over-read.",
   "clears_when": "BOTH: (a) a NEW disjoint-group over-cover is detected and PAGES the operator through outcomes.jsonl — verified by finding the row in /api/bot/logs?level=error, not by reading the code; and (b) cancel-ib-order is exercised against the real gateway and reports a cancel's outcome correctly. A synthetic test passing is NOT either half.",
   "why_it_cannot_be_closed_by_time": "A deployed fix and a working fix are indistinguishable from every surface we have. Only the mechanism firing on a real event settles it — the system-review skill's `unexercised_fixes` rule, applied here.",
   "refs": [
    "BL-20260816-IB-STOPS-OVER-COVER-IN-DISJOINT-OCA-GROUPS",
    "BL-20260826-CANCEL-IB-ORDER-READS-ERROR-202-AS-A-REFUSAL",
    "BL-20260820-OVERCOVER-REMEDIATION-CANCELLED-THE-JOURNAL-MATCHING-LEG"
   ],
   "check_every_days": 2,
   "verified_at": "2026-08-29",
   "observation": "2026-08-29 (r6-vm-residency): CHECKED THE PAGE PATH ON THE LIVE FEED, and the row does NOT clear. Read /api/bot/logs?level=error&limit=1000 -> 398 rows spanning 2026-08-20T13:24Z..2026-08-28T22:05Z (short of the cap, so NOT truncated). Positive control run first so a zero would have meant something: 'naked' 153, 'target_naked' 129. HALF (a), IB: SIX ib_stop_over_cover rows ARE present in the operator error feed — so the outcomes.jsonl->error-feed transport demonstrably works — but ALL SIX are dated 2026-08-26 and are the SAME events cleared by hand (MHG 300% @02:09:36, MHG 200% @08:14:05, MGC 202% @15:17:19 under the bare numeric group 834864174). NO NEW disjoint-group over-cover has been detected since, so (a) as written ('a NEW ... is detected and PAGES') is UNSATISFIED. The absence is consistent with the hand-clear holding, which is good news and is NOT the same as the mechanism being proven. STRONG ADJACENT EVIDENCE, deliberately not counted as (a): the SIBLING bybit_over_cover page (shipped 2026-08-26) HAS fired on genuinely new events — 18 rows, newest 2026-08-28T22:05:02Z, bybit_1/ETHUSDT position 0.76 vs resting SL legs 6.15 = 809% across 2 legs. That is the same emit()->Telegram/error-feed path working end-to-end on a NEW detection; it is the Bybit arm, not the IB arm, so it cannot clear this row. HALF (b): cancel-ib-order was NOT exercised against the real gateway this session — no new evidence; still fake-venue only. SIDE OBSERVATION, already owned so NOT re-filed: the bybit_1/ETHUSDT over-cover CONDITION is worsening (167% on 08-26 per BL-20260826-BYBIT-OVER-COVER-DETECTED-FOR-FOUR-WEEKS-AND-NEVER-PAGED -> 384%/378%/809% across 08-28). Its owner is BL-20260730-BYBIT1-XRP-LEG-OVERACCUM-WORSENING (kept_open, high, tier 2, 'remediation still unbuilt'); bybit_1 is the DEMO account. Filing a new row would duplicate it. || 2026-08-26: cleared the symptom by hand (issues #10332-#10338). MHG now rests under ONE OCA group (oca-protect-t4796: STP 6.364 + LMT 7.1415) matching journal trade 4796's declared stop 6.36407143, down from 58 lots across two disjoint groups. MECHANISM STILL UNOBSERVED: no NEW over-cover has been detected-and-paged since, and cancel-ib-order's 202 fix has run against a fake venue only. || 2026-08-26 (b9kyip, later): CRITERION (b) IS NOW EXERCISED ON THE REAL GATEWAY, and the row STILL stays open because (a) is not met. cancel-ib-order ran against the live ib_paper gateway on MGC order 422 (th",
   "last_checked": "2026-08-26"
  },
  {
   "id": "OI-20260826-JOURNAL-TRUST-COVERS-ONE-ACCOUNT",
   "opened": "2026-08-26",
   "kind": "background_awareness",
   "summary": "journalTrust now flags accounts whose journal disagrees with the broker — but the ledger it reads has exactly ONE account in it.",
   "detail": "/api/bot/trades/closed and /api/bot/performance now carry journalTrust, resolved from comms/broker_truth_ledger.json. That ledger lists bybit_2 and nothing else, because it is populated BY HAND from an operator's venue export. So every other account grades `no_record`, which means NOBODY HAS RECONCILED IT — never that it reconciles. A session reading `no_record` as a clean bill of health has reintroduced the exact bug the field was built to fix.",
   "clears_when": "Either every real-money account has been reconciled against its venue at least once (so `no_record` on a real-money account becomes a genuine anomaly), or an automated reconciliation replaces the hand-populated ledger.",
   "affects": "Any session quoting a per-account PnL figure. Read the journalTrust state before quoting a sum; `no_record` is not evidence of agreement.",
   "refs": [
    "BL-20260826-JOURNAL-READS-DO-NOT-CONSULT-THE-BROKER-TRUTH-LEDGER",
    "BL-20260713-BYBIT2-PNL-UNDERRECORD"
   ],
   "observed_live": "2026-08-26T10:10Z, deployed sha 510fbb6a: the flag works and its coverage limit is confirmed rather than assumed. bybit_2 reads known_divergent on 200/200 rows; bybit_1 / alpaca_paper / ib_paper all read no_record; /api/bot/performance?window=all reports accountsKnownDivergent:[bybit_2] and accountsUnrecorded:[alpaca_live]. Every account except one is therefore UNRECONCILED, which is what this row exists to keep visible — no_record is not a clean bill of health.",
   "verified_at": "2026-08-26",
   "observation": " || 2026-08-26 (b9kyip): re-checked on the deployed /api/bot/performance?window=all -- journalTrust reads {readState: read, accountsKnownDivergent: [bybit_2], accountsUnrecorded: [alpaca_live], accountsUnreadable: []}. Still exactly ONE reconciled account; unchanged, and the row stands."
  },
  {
   "id": "OI-20260826-DIAG-IB-ORDER-READ-IS-STALE",
   "opened": "2026-08-26",
   "kind": "background_awareness",
   "summary": "/api/diag/ib_open_orders serves a STALE, monotonically-growing order view. Do not conclude an order's absence or presence from it.",
   "detail": "ib_insync's openTrades() reads an ACCUMULATED cache and reqAllOpenOrders only ADDS to it, while cancel callbacks reach the SUBMITTING client only — so the read-only client the route uses never learns of a cancel it did not issue. Measured: the route and a fresh client differed by EXACTLY a cancelled pair while agreeing on all six other orders. The envelope now carries a stale_read_caveat. The ENFORCING path (the naked sweep, on the trader's own client) is unaffected.",
   "clears_when": "The route builds a FRESH client per read and a measurement shows it agreeing with a known-current view. Needs a real gateway to verify, so it cannot be closed from a sandbox.",
   "affects": "Any session reasoning about IB resting orders — including over-cover analysis, which is what it was used for on 2026-08-26.",
   "refs": [
    "BL-20260826-DIAG-IB-OPEN-ORDERS-SERVES-A-STALE-MONOTONIC-ORDER-VIEW"
   ],
   "observation": "2026-08-26 (b9kyip): the caveat BIT AGAIN, in the exact way the row predicts. After cancelling MGC order 422 against the real gateway the immediate verification re-read still showed the leg, contradicting the venue's own error-202 acknowledgement; two fresh-process dry runs (not_found, order count 8 -> 6) established the cancel HAD taken effect. Do not conclude an order's presence from this route -- verify against a FRESH client.",
   "verified_at": "2026-08-26"
  },
  {
   "id": "OI-20260826-SESSION-BRIEF-NEVER-READ-BY-A-FRESH-SESSION",
   "opened": "2026-08-26",
   "kind": "monitoring",
   "check_every_days": 3,
   "verified_at": "2026-08-29",
   "summary": "The generated SESSION-BRIEF block in CLAUDE.md is the mechanism this session shipped in place of a cap and an adjective, and NO fresh session has ever read it. Shipped and working are different states; only a real session acting on the block settles it.",
   "observation": "2026-08-29 (r6-vm-residency): THE TEST RAN, AND IT IS THE UNCONTAMINATED ONE THIS ROW ASKED FOR — but read the caveat, because it is the difference between 'the brief worked' and 'the brief was present'. MY PROMPT DID NOT NAME THE MHG ITEM. The 2026-08-28 handoff I was pasted names the dispatcher row and the Lane P row by id and lists seven backlog rows; it never mentions MHG, over-cover, or this brief. So the contamination the row warned about (the 08-26 handoff naming MHG explicitly) does NOT apply to me. WHAT I DID: the SESSION-BRIEF block reached me inlined in CLAUDE.md BEFORE my first tool call, reading '1 monitoring item(s) DUE' and naming OI-20260826-MHG-OVER-COVER-MECHANISM-UNVERIFIED with its clears_when. I acted on exactly that item — pulled /api/bot/logs?level=error with a positive control, established half (a) is UNSATISFIED (6 ib_stop_over_cover rows, all 08-26, all the same hand-cleared events; no NEW detection) and half (b) untouched, and wrote the observation into the register. THE HONEST CAVEAT: I cannot claim the brief was the sole CAUSE. CLAUDE.md's 'Every session' section separately instructs reading OPEN-ITEMS.json FIRST, and I did that as my first tool call — so the brief and the register reached me almost together and I cannot cleanly separate which drove the action. What IS clean: the brief arrived before any tool call, it named the due item, and the item was worked. That is the mechanism doing its job even if the counterfactual is not isolable. ONE OBSERVED WEAKNESS worth recording: the brief renders only what is DUE, so it was showing exactly ONE item; after I stamped that item the SAME render surfaced TWO others that had silently come due at their 3-day cadence. A session that stamps its due item and stops would never see them. The block is a snapshot of the register at render time, not a queue that drains — re-render after stamping. || 2026-08-26: verified only by SIMULATION, not by a session. render_session_brief --self-test shows the block is quiet on 08-26 and 08-27 and renders OI-20260826-MHG-OVER-COVER-MECHANISM-UNVERIFIED on 08-28 when its 2-day cadence expires. --check confirms the committed CLAUDE.md matches the registers. The block is currently EMPTY (nothing due), so a session reading it today observes nothing and demo",
   "clears_when": "A session that was NOT pointed at this mechanism by its prompt reports on a due monitoring item, citing the CLAUDE.md brief as where it saw it. WARNING: the handoff prompt written at the end of this session names the MHG item explicitly, so the FIRST session after this one cannot be the clean test -- it has been told. The uncontaminated observation is a later session that arrived cold. Record which it was.",
   "why_it_matters": "If the block is not read, the register is exactly the thing it replaced: a file that looks like a follow-up mechanism and is not one. That failure is invisible -- an unread brief and a brief with nothing due are byte-identical from outside, which is the 'we did not look' vs 'we looked and found nothing' collapse this repo has a guard family for.",
   "links": [
    "scripts/ops/render_session_brief.py",
    "docs/claude/RECURRENCE-LEDGER.json",
    "docs/sprint-logs/S-GATE0-MEASUREMENT-TRUST-2026-08-26.md"
   ],
   "last_checked": "2026-08-26"
  },
  {
   "id": "OI-20260826-STRAY-OCA-SWEEP-SHIPPED-BUT-UNARMED",
   "opened": "2026-08-26",
   "kind": "monitoring",
   "check_every_days": 3,
   "verified_at": "2026-08-29",
   "last_checked": "2026-08-29",
   "summary": "The stray-OCA-group sweep that fixes the over-cover GENERATOR shipped at annotate and is armed on NO account. Do not arm it before reading the MES finding below -- as written it would cancel MES's only protection.",
   "detail": "PRs #10352 + #10353 shipped src/runtime/stray_oca_groups.py and wired it into IBClient.place_protective. It closes the generator caught in the act at 2026-08-26T02:08:35Z on ib_paper/MHG: place_protective's keyed pre-cancel is scoped BY NAME to oca-protect-t<trade_id>, so a group resting under a legacy (oca-protect-<reqId>) or bare-numeric (e.g. 834864174) name is never a cancellation candidate and the re-arm mints a SECOND, non-mutually-cancelling group beside it -- 300% across 3 groups, with no 'no oca_key' warning, no Error 10147 and no survivor-join log, i.e. none of the three signatures of the previously-known defect. LIVE STATE: PROTECTION_STRAY_GROUP_MODE=annotate (the shipped default -- the decision runs in full and cancels NOTHING) and PROTECTION_STRAY_GROUP_ACCOUNTS is UNSET, which for this knob means NONE (deliberately the opposite polarity to CONVICTION_SIZING_ACCOUNTS / NETTING_ATTRIBUTION_ACCOUNTS). TWO THINGS TO READ BEFORE ARMING. (1) ib_paper/MES rests a single LEGACY-named group oca-protect-408, which the sweep classifies stray_unkeyed and would CANCEL -- and it is MES's ONLY protection. Arming without resolving that trades one safety defect for a worse one. (2) annotate writes NO durable soak file (unlike PROTECTION_REASSERT_MODE, which writes protection_reassert_soak.jsonl), only a trader-journal WARNING -- so 'observe first' means a journalctl pull, not a log endpoint.",
   "clears_when": "The sweep is armed on ib_paper AND a row is observed in which it classified a real leg and acted -- naming the trade, the stray group and the preserved group. Shipping is not the observable; a cancel that happened is. Alternatively, the row clears if the operator decides not to arm it, recorded as that decision.",
   "why_it_cannot_be_closed_by_time": "annotate and apply are byte-identical from every surface except the trader journal, so a sweep that is silently mis-scoped looks exactly like one that found nothing to do.",
   "observation": "CHECKED 2026-08-29 (session 01UTcLyMtHKYkhMiMKk1asbv). NOT CLEARED - still unarmed, and I did NOT arm it. TWO OBSERVATIONS, both reads. (1) THE MES BLOCKER STILL HOLDS AND IS WORSE THAN THIS ROW RECORDED. /api/diag/ib_open_orders?account_id=ib_paper at 2026-08-29T06:21:15Z returns EXACTLY 2 resting orders, both MES, both under the legacy-named group 'oca-protect-408': an LMT SELL 15 @ 8390.5 and an STP SELL 15 @ 7533.75. So the group the sweep would classify stray_unkeyed and CANCEL is not merely 'MES's only protection' (this row's wording) - it is a COMPLETE TWO-SIDED bracket, stop AND target, and it is the account's only resting protection of any kind. Arming without resolving it would remove both legs. CAVEAT, stated because the route requires it: the response carries stale_read_caveat=true (BL-20260826-DIAG-IB-OPEN-ORDERS-SERVES-A-STALE-MONOTONIC-ORDER-VIEW), so verify against a fresh client before ACTING. Staleness here would mean the orders are already gone, i.e. it can only make me OVER-state the blocker - the conservative direction for a do-not-arm conclusion. (2) THIS ROW'S OWN PRESCRIBED OBSERVATION METHOD DOES NOT WORK AT THE AVAILABLE PAGE SIZE, which is why 'observe first' has produced nothing in 3 days. The row says annotate writes no durable soak so observing means a journalctl pull. Measured: /api/diag/journalctl?unit=ict-trader-live.service&lines=1000 returns 1000 lines spanning 06:18:00-06:21:48Z - a 3 min 48 s window, because the trader is that chatty. Grep over it: 0 sweep verdict literals (stray_unkeyed/sibling_keyed/keep_target), 0 'oca-protect' strings, and 0 protective-placement lines. The sweep only runs on place_protective's KEYED path, which did not fire once in that window - so the silence is 'the trigger did not fire', NOT 'it fired and found nothing'. Those are different states. POSITIVE CONTROL, so the zero is not read as absence: the same grep over the same 1000 lines found 4 naked/coverage-sweep ERROR rows, so the probe finds things when they are there. CONSEQUENCE: this item cannot clear via the method it names - a sparse event cannot be caught in a ~4-minute page. Clearing it needs either a durable soak file (the shape PROTECTION_REASSERT_MODE already has via protection_reassert_soak.jsonl), or a journalctl pull bounded by --since rather than by line count.",
   "refs": [
    "BL-20260816-IB-STOPS-OVER-COVER-IN-DISJOINT-OCA-GROUPS",
    "BL-20260826-OPEN-TRADES-COLLAPSES-A-READ-FAILURE-INTO-AN-EMPTY-BOOK",
    "BL-20260826-A-STRAY-OCA-LEG-FILLED-AND-THE-JOURNAL-NEVER-LEARNED"
   ]
  },
  {
   "id": "OI-20260826-MGC-JOURNAL-QTY-DIVERGENT-UNOWNED",
   "opened": "2026-08-26",
   "kind": "background_awareness",
   "verified_at": "2026-08-26",
   "summary": "ib_paper/MGC: the exchange holds 50 and the journal declares 51, and NO IB-side detector covers that. It did not self-heal.",
   "detail": "After the 2026-08-26 stray-leg cancellation on ib_paper/MGC, the exchange position read short 50 while journal trade 5007 declares position_size 51 -- still divergent ~1h25m later (re-read 21:05Z), so this is not a settlement lag. CONTROL: MHG 29/29 and MES 15/15 agree exactly on the same read, so it is not a reader artefact. Protection rests at 51 against a position of 50 = 102%, which is over-covered by one lot. NOTHING DETECTS IT: the IB over-cover page requires DISJOINT OCA groups and there is only one group here, and journal_qty_divergent -- the detector that would catch exactly this -- exists only in _check_broker_naked_bybit_positions, never on the IB path. The likely origin is that a stray leg FILLED for one lot and the journal never learned (BL-20260826-A-STRAY-OCA-LEG-FILLED-AND-THE-JOURNAL-NEVER-LEARNED).",
   "clears_when": "Either the journal row is reconciled to the exchange quantity, or an IB-side journal_qty_divergent detector exists and reports on this position. A later read that happens to agree does NOT clear it -- the position closing would make the divergence unobservable rather than resolved.",
   "affects": "Any session quoting ib_paper position sizes or grading MGC protection coverage. 102% here is the journal over-declaring, not the venue over-protecting.",
   "refs": [
    "BL-20260826-A-STRAY-OCA-LEG-FILLED-AND-THE-JOURNAL-NEVER-LEARNED"
   ]
  },
  {
   "id": "OI-20260827-RESEARCH-QUEUE-DISPATCHER-NEVER-FIRED",
   "opened": "2026-08-27",
   "kind": "awaiting_verification",
   "summary": "The T2 research queue + dispatcher is built and green but has NEVER dispatched anything; its cron has never fired and its GPU route has never been exercised.",
   "detail": "PR #10365 (draft) ships research/queue/*.yaml, the R4 power gate (5 states), the routing decision (4 states), scripts/research/dispatch_queue.py and a daily cron workflow. 51 tests pass and guards are PASS 41/FAIL 0 — but EVERY dispatch so far has been a dry run or a stubbed _fire in a test. Nothing has been routed to a real runner, a real trainer job, or a real GPU burst. ⚠️ THE GPU PATH IS THE ONE THAT MATTERS: the operator chose 'fire everything including GPU', so the first real armed run is the first time this spends money unattended. Three gates are supposed to bound it — the burst workflow's ledger preflight against the $10/month cap, its GPU_BURST_ARMED gate, and the dispatcher's own --max-gpu-dispatches-per-run — and NONE of the three has been observed holding against this dispatcher. A test with a stubbed _fire proves the counter advances; it proves nothing about the ledger preflight. Also unproven: the last_dispatched_at stamp round-trip. It lands via the commit-to-main auto-merge PR, so a second dispatcher run inside that PR's window would RE-FIRE the same job. That race is documented in the module and has never been observed either way. ⚠️ OPERATOR DECISION 2026-08-27, AFTER #10365 MERGED (28e47d97): the dispatcher stays DRY. The daily 06:20Z cron passes no --fire and MUST NOT be armed until a session has read one real dry-run report and confirmed the routing and the power gate behave on live queue contents. Arming is then a deliberate follow-up, not a default. Do not read the merge as arming it, and do not flip `fire: true` on inference from a busy research task — the operator's stated reason is that none of the three spend gates has ever been observed holding against THIS dispatcher.",
   "clears_when": "BOTH: (a) a queued job is dispatched for real by the dispatcher (not a dry run, not a stubbed _fire) and its rows land in the store the job declares in `lands.store`, verified by reading that store — not by the workflow exiting 0; and (b) a recurring (non-`once`) job is observed NOT re-firing on the dispatcher run immediately after its first fire, which is the stamp race actually being exercised. Until a GPU-routed job has been fired once with the ledger preflight observed refusing or admitting it, do not describe the spend gates as verified."
  },
  {
   "id": "OI-20260827-LANE-P-COMPAT-VERDICTS-UNMEASURED",
   "opened": "2026-08-27",
   "kind": "awaiting_verification",
   "summary": "Lane P (#10364, Tier-3) is green and awaiting operator approval; NOBODY has measured which compat-matrix verdicts it actually moves.",
   "detail": "PR #10364 changes the standard arm so it stops grading an intra-day resetting brake as a terminal account-killer, and REFUSES (verdict UNGRADED) instead of sizing off a synthetic $10,000 when the live balance cannot be established. All four checks are green as of 2026-08-27T11:02Z. ⚠️ THE CASE FOR IT IS ARGUED FROM THE MECHANISM, NOT FROM A MEASURED BEFORE/AFTER. The session that wrote it could not run the compat matrix (no numpy in the sandbox), so the number of verdicts that flip, and their direction, is UNKNOWN. That is exactly the evidence an operator approving a Tier-3 sizing/gating change should have. research/queue/RQ-20260827-001.yaml exists precisely to produce it — a deterministic re-grade routed to a runner — but it cannot run until #10365 merges and the dispatcher is armed, so the two PRs are coupled in that one direction. ⚠️ STATUS MOVED 2026-08-27: #10364 was APPROVED AND MERGED (f2ea9e44) WITHOUT the before/after diff, which the operator is entitled to do and which this row records rather than smooths over — so the pending decision is settled and what remains is purely the unmeasured verification. #10365 has since merged, so RQ-20260827-001 now EXISTS in the queue and is loadable; it has not run, because the dispatcher is deliberately left dry (see the dispatcher row). The measurement is therefore blocked on the same arming decision, and the cheap unblock is to dispatch gld-compat-matrix.yml by hand rather than to arm the dispatcher for it.",
   "clears_when": "The before/after verdict diff has been produced and READ — by RQ-20260827-001 running, or by dispatching the compat matrix by hand — and its direction and magnitude recorded. ⚠️ The change is ALREADY LIVE ON MAIN, so this is no longer evidence-before-approval; it is checking a merged Tier-3 sizing/gating change did what its mechanism argued. If the diff shows verdicts moving in an unexpected direction, that is a revert conversation, not a note.",
   "observation": "⚠️ CORRECTED LATER THE SAME SESSION — MY EARLIER OBSERVATION ON THIS ROW WAS WRONG IN THE PERMISSIVE DIRECTION. I recorded 'EVERY MOVE IS MORE CONSERVATIVE; NOTHING moved skip->ROUTE'. That was true of the two ledgers measured and is FALSE IN GENERAL — they carried no drawdown deep enough to make the OLD static model declare a terminal breach, so only ONE of Lane P's two defects was being exercised. The drawdown half was then isolated (seed EVERY balance at 10,000 — the figure the BEFORE arm hardcodes — so the size term is numerically identical and only the drawdown code path differs; control 11/11 equal size). On a ledger carrying a sustained -20R drawdown it moves 10 of 11 accounts skip->ROUTE, with survival 0.800-0.857 -> EXACTLY 1.0 and p_breach 0.143-0.200 -> EXACTLY 0.0 on every one. breakout_1 (prop, declared ruleset) does NOT move — the control showing the change is correctly scoped to the standard arm. A stated prediction was FALSIFIED on the way and that is what exposed the mechanism: I predicted a spread-across-days drawdown would diverge and a same-day-concentrated one would not; BOTH diverged identically, so it is not a time-distribution effect — the standard arm simply cannot register a terminal breach any more. Had I run only the spread arm I would have reported a correct number under a wrong explanation. REVISED BOTTOM LINE: Lane P's two halves move verdicts in OPPOSITE directions — size resolution is MORE CONSERVATIVE (3 of 11 on a positive book, killing false ROUTEs off a synthetic $10k), the drawdown model is MORE PERMISSIVE (10 of 11 on a drawdown-heavy book). Both are defensible as corrections; neither is a regression on its own terms. But an operator approving this on my earlier sentence would have been misled. CONSEQUENCE FILED SEPARATELY: survival and p_breach are now CONSTANTS on standard accounts, so two of the gate's three criteria no longer discriminate, and BL-20260803-GLD-ALPACA-PORTFOLIO-SURVIVAL-SKIP's blocking finding is probably unreproducible — BL-20260829-LANE-P-MAY-VOID-THE-GLD-SURVIVAL-SKIP. || 2026-08-29 (r6-vm-residency, overnight): THE DIFF IS PRODUCED AND READ. Direction question ANSWERED — NO REVERT CONVERSATION. Full working: docs/research/lane-p-compat-verdict-diff-2026-08-29.md.\nWHY IT COULD RUN NOW: the 08-27 blocker was 'no numpy in the sandbox' — a missing DEPENDENCY, not a missing capability. pip install -r requirements-test.txt gives numpy 2.4.6 / pandas 3.0.5 / scipy / sklearn. The dispatcher was never needed and STAYS DRY.\nMETHOD: only the CODE varies — git worktree at f2ea9e44^ vs HEAD, with the ledger byte-identical (sha256 pinned), config/accounts.yaml COPIED INTO the before-tree so config drift cannot confound, one live /api/bot/accounts/balances read seeded into a local balance_snapshots table (DDL lifted VERBATIM from database.py:868, and read back through the repo's OWN Database.get_latest_balance_snapshots to confirm all 11), and --seed 20260829 --n-paths 300 throughout. Positive control first: account_rulesets.py differs between trees and standard_account_size.py is ABSENT in the before-tree, so the A/B is not measuring nothing. TWO ledgers (+213.5R and -12.2R) to bracket the outcome space.\nRESULT with real balances: POSITIVE ledger 3 of 11 verdicts move — alpaca_live ROUTE->skip (synthetic $10,000 -> real $200.10), bybit_2 ROUTE->skip ($10,000 -> $305.18, and bybit_2 is MAINNET), ib_live ROUTE->UNGRADED (balance unreadable). NEGATIVE ledger 1 of 11 — ib_live skip->UNGRADED. EVERY MOVE IS MORE CONSERVATIVE; NOTHING moved skip->ROUTE, nothing became more permissive. The two ROUTE->skip moves are the defect caught in the act on REAL MONEY: the synthetic $10k made a $200 and a $305 account look routable. The seven accounts holding $66k-$1.34M were graded against a SMALLER synthetic figure and are unchanged, which is why the magnitude is modest and concentrated exactly where the mechanism predicted.\n⚠️ ONLY ONE OF LANE P'S TWO DEFECTS SHOWS MOVEMENT. All of it is attributable to SIZE RESOLUTION; the drawdown-type half (static terminal floor -> intra-day resetting brake) moved NO verdict on either ledger. That is recorded as UNMEASURED, not as no-effect — these ledgers plausibly do not stress it, and a book with a deep early drawdown is the probe that would.\n⚠️ WHAT REMAINS: the ledger is SYNTHETIC. This measures which verdicts Lane P MOVES (the row's question), holding the book fixed; it is NOT the true gld_pullback_1h verdict set, which still wants gld-compat-matrix.yml dispatched (its emit half needs Yahoo, firewalled in-sandbox). Balances are one point-in-time snapshot (age 2391s) and bybit_2 at $305.18 sits near enough a threshold to be hour-sensitive. KEEPING THE ROW OPEN, NARROWED: the dangerous outcome (an unexpected direction) is definitively ruled out; what is left is the real-ledger magnitude. || ",
   "verified_at": "2026-08-29"
  },
  {
   "id": "OI-20260829-TRAINER-IS-NOW-A-DECIDED-DEPENDENCY-AND-IS-UNMONITORED",
   "opened": "2026-08-29",
   "kind": "monitoring",
   "loud": true,
   "summary": "The operator DECIDED (2026-08-29) to keep the trainer VM for the L2 order-flow capture. That converts the box from 'a candidate for retirement' into a STATED DEPENDENCY for a forward-only stream nothing can re-derive — and the disk is at 92% with NOTHING monitoring the capture. The decision is only as good as the box staying healthy, and no alarm covers it.",
   "detail": "DECISION: 'we can keep the training in the meantime' — clears_when option (c) of the retired OI-20260829-ORDERFLOW-CAPTURE-HOME-UNDECIDED: the trainer is kept for this purpose as a STATED decision rather than by default. Interim ('in the meantime'), not permanent. MEASURED THE SAME HOUR rather than carried from R6's day-old reading (trainer relays #10422 + #10423, read-only): the capture is ALIVE AND WRITING — datasets-out/market_microstructure/BTCUSDT/5m/v001/data.jsonl modified 2026-08-29T17:30:00Z against a same-command `date -u` of 17:30:17Z, i.e. 17 seconds before the reference clock, on the 5m bar boundary; 5.9 MB total; PID 728, 91.6 MB RSS, active since 2026-07-15. That is the standard the retired row demanded (a fresh row's ts, NOT the unit reporting active). Disk re-measured: 42G/45G, 3.8G free, 92%. THE CANONICAL PATH IS RECORDED HERE BECAUSE GUESSING IT COST A FALSE ALARM: probe #10422 looked in runtime_logs/orderflow/ (which does not exist), got an empty result, and an empty result there is byte-identical to a dead capture. It was distinguishable ONLY because #10423 carried a POSITIVE CONTROL (88 files modified in the same 2h window, so the probe demonstrably works). A monitor built on the wrong path pages forever or never. WHAT IS STILL UNCOVERED: zero orderflow references in any alerting code; absent from the diag unit allowlist; trainer_reachability_alert reads the PUBLISH timer's mirror mtime, which advances normally with the capture dead; and the process catches poll errors and continues, so `active (running)` is not evidence of capturing. `journalctl -u ict-orderflow-capture` shows '-- No entries --' over 3h, which is consistent with a quiet healthy service and would look IDENTICAL to a wedged one — so the journal is not a freshness signal either. COMPOUNDING: the capture writes into datasets-out/, INSIDE the 28G repo tree that IS the 92% disk — the tree that fills the disk and the stream that dies when it fills are the same tree.",
   "clears_when": "EITHER (a) an alarm exists that fires on the capture going stale — keyed on the DATA's freshness at the path named above, never on the unit's ActiveState — and it has been SHOWN to fire against planted staleness, not merely deployed; OR (b) the capture is relocated to a host where something already watches it, and is observed writing there; OR (c) the operator retires the flow-model family and says so, at which point none of this is load-bearing. Disk headroom recovering on its own does NOT clear this — the gap is the missing alarm, not the current free space.",
   "why_it_cannot_be_closed_by_time": "Waiting IS the failure mode. The capture is forward-only, so a silent stall is not recoverable by noticing late — the minutes are simply gone. Nothing pages, so the expected way this surfaces is a session happening to look, which is what happened today and is not a mechanism.",
   "refs": "docs/research/R6-VM-RESIDENCY-VERDICT-2026-08-28.md; BL-20260829-ORDERFLOW-CAPTURE-IS-IRREPLACEABLE-AND-UNMONITORED; BL-20260829-TRAINER-DISK-92-PCT-THREATENS-THE-UNBACKFILLABLE-CAPTURE; trainer diag #10422 + #10423; supersedes OI-20260829-ORDERFLOW-CAPTURE-HOME-UNDECIDED",
   "check_every_days": 3,
   "verified_at": "2026-08-29",
   "observation": "2026-08-29: opened on the operator's decision, with the capture and disk re-measured the same hour. Capture writing 17s before the reference clock; disk 3.8G free. CADENCE IS CHOSEN, NOT MEASURED: two free-space readings a day apart (3.9G → 3.8G) are two points, not a fill-rate trend, so 3 days is deliberately short until a trend exists to justify relaxing it. | /system-review 2026-08-29 18:15Z, merging concurrently: I re-measured the same hour and reached a WEAKER conclusion than the row above — I confirmed ict-orderflow-capture.service loaded/active/running but could NOT locate its write path (a find for l2/orderflow/depth/book dirs under /home/ubuntu returned nothing), so I could not verify it was WRITING. This row's author did verify that, which is the stronger evidence and the one to trust; recording my miss because it demonstrates the row's own point — ActiveState is reachable and data freshness is not, which is exactly why clears_when demands an alarm keyed on the DATA. WHERE THE 42G IS, which this row did not carry and whoever acts on the disk will want: datasets-out 12G (market_features 11G of it), .venv 5.4G, ml/experiments-runs 4.8G, runtime_logs 3.3G (m20_exit_head 2.5G alone). Noted as context, NOT as a route to clearing this row — its clears_when correctly says free space is not the gate.",
   "last_checked": "2026-08-29"
  },
  {
   "id": "OI-20260829-ALPACA-GOLIVE-BLOCKED-ON-T1-SETTLEMENT-MODEL",
   "opened": "2026-08-29",
   "kind": "background_awareness",
   "loud": true,
   "summary": "alpaca_live go-live: the T+1 model now EXISTS (PR #10408, merged + deployed 2026-08-29, running at `annotate`). The row stays OPEN because `clears_when` requires the model be SHOWN ACTING, and at `annotate` it binds nothing by construction — so shipping it cannot clear this. Operator settled the fork: CASH, permanently.",
   "detail": "FOUR DECISIONS TAKEN 2026-08-29, recorded here so no session re-asks them. The operator's complaint that prompted this — that cash had been said 'a million times' — is the cost of NOT having recorded it. (1) FUNDING MODE: CASH. Settled, not provisional. capacity.multiplier=1 was measured 2026-08-29T00:22:41Z. Consequences that follow and are NOT re-litigable: no shorting, gross-exposure multiple can never exceed 1.0x so max_gross_exposure_pct is decorative, and PDT is INERT (BL-20260823-NO-PDT-MODELLING-ON-ALPACA does not apply under this branch). (2) SIZE BOUND: a per-trade cap expressed as a PERCENT OF EQUITY, not a fixed dollar figure — chosen so it scales if the account is later funded rather than going stale, and so oversized intents are sized DOWN and still place rather than being refused by the venue. Value not yet chosen; Tier-3. (3) T+1: MODEL IT BEFORE ANY LEG GOES LIVE. The operator declined the cheaper 'restrict to 1d legs and defer' option I recommended. This is the blocker this row tracks. (4) FUNDING AMOUNT: keep $200.10 for now; revisit after real fills reconcile. At the 0.9 cash wall that is ~$180 usable, so any instrument priced above it is mechanically untradeable at one whole share — indicative prices from the retracted Phase 3 doc put GLD ~$423 and SPY ~$640 in that category, NOT re-measured. ROSTER INTENT: one cheap, affordable, non-USO, daily-cadence leg as an explicit PLUMBING TEST — to prove fills reconcile against exchange truth, that whole-share sizing behaves on a cash book, and that silent_refusal_alert grades the account healthy. It is NOT an edge bet and must not be reported as one: stripped of uso_trend_1h (excluded from real money on standing non-financial grounds), the 16-leg candidate set has NO positive measured record on either paper population. The specific leg is not yet chosen and adding it is Tier-3 in its own right — alpaca_live is mode: live with strategies: [], so that empty list is the ENTIRE gate and the first leg added IS the live-trading moment, with no mode flip in front of it. \n\nUPDATE 2026-08-29 (same day, later): THE MODEL SHIPPED — PR #10408, `src/runtime/cash_settlement.py`, merged and deployed (VM confirmed on the merge sha, restart_pending false). Basis is `min(venue_buying_power, venue_cash - our_unsettled)`, which is correct under all four combinations of (Alpaca nets unsettled proceeds / does not) x (we saw the sale / did not) — asserted as a 4-case parametrized test, because the distinguishing state has NEVER existed on this account (cash == buying_power == regt_buying_power == equity == 200.10, all four identical) and both paper mirrors are MARGIN accounts that cannot exhibit cash-account behaviour. Trading days come from the venue's own /v2/calendar because src/runtime/market_hours.py models no holidays and counting T+1 with it would credit proceeds a day EARLY across a holiday, manufacturing the violation. \n\nTHE REMAINING TWO DECISIONS ARE ALSO NOW TAKEN (2026-08-29, popups): per-trade cap = 100%, i.e. NO cap beyond the cash account's own 1.0x limit; `risk_pct` STAYS 0.05. Decision (2) in the block above is therefore RESOLVED and its 'value not yet chosen' is stale. Why that pair is coherent rather than reckless at this size, so nobody 'fixes' it: the sizer demands ~26.7 TLT shares (~11x the account) and the CASH BALANCE clamps it to 2, so realised risk at the declared stop is ~$0.75, not the $10 the 5% implies — confirmed by the observed qty 2.0 journal rows, not inferred. Plumbing-test leg CHOSEN: `tlt_pullback_1h` (TLT ~$83 so 2 shares fit; n=171, the best-evidenced leg on the roster). \n\nWHY THIS ROW STILL DOES NOT CLEAR, and what would: `clears_when` asks for a would-be order REFUSED with a logged cause. At `annotate` the gate computes and writes a soak row and changes no sizing, so it can never produce that observation. Clearing needs the Tier-2 flip to `apply` (allowlisted to alpaca_live only) AND a real case. WARNING FOR WHOEVER PICKS THIS UP: `would_have_reduced_usd` will read 0.00 on alpaca_live however long anyone waits — `recent_sales` requires status='closed' with a non-null exit price inside a 10-day window and that account has NONE (all 23 recent rows are `rejected`; its last real position closed in July). T+1 binds only AFTER a sale, so an account that has never traded cannot demonstrate it. That is circular by construction, not a gap. The mechanism evidence lives on the PAPER accounts, which do sell — 6 closed sales in 10 days, up to $111,069.37. \n\nA REPO TIMER NOW CARRIES THE PRECONDITION: PR #10412 adds `.github/workflows/alpaca-settlement-soak-watch.yml`, weekdays 14:30 UTC. It grades the soak against the newest alpaca dispatch FIRST, so an empty soak is never collapsed: `not_yet_exercised` (no dispatch since deploy — silent, not a failure) vs `never_wrote` (a dispatch DID happen — a finding, fails the job). This exists because the precondition was being carried by a session check-in, which dies with the session.",
   "clears_when": "T+1 settlement is MODELLED in code and the model is shown acting — a would-be order that the account could not settle is refused with a logged cause, observed on a real or replayed case, not merely unit-tested. A passing test is not an observation of the live mechanism. This row does NOT clear on the leg going live, and NOT on the funding decision.",
   "why_it_cannot_be_closed_by_time": "Nothing accrues that builds the model; it is work someone has to do, and until it exists the operator's own stated precondition for go-live is unmet. This lane has already stalled once by leaving a decided question looking open — that is exactly what this row replaces.",
   "refs": "docs/research/ALPACA-LIVE-GOLIVE-STATUS-2026-08-29.md (its §5 sequence predates these decisions and its rows 0/3/6 are stale — see the correction in that file); BL-20260823-ALPACA-CASH-ACCOUNT-SETTLEMENT-UNMODELLED (the work); BL-20260823-NO-PDT-MODELLING-ON-ALPACA (INERT under cash); BL-20260823-ALPACA-LIVE-STRATEGY-SET-IS-NET-NEGATIVE-EXCEPT-ONE-LEG (the should-we half); BL-20260829-SPLG-TREND-LONG-1D-EMITS-ZERO-TRADES-IN-730-DAYS; docs/research/alpaca-portfolio-roster-compat-gate-2026-08-29.md (the can-we half, 14 of 16 graded)",
   "verified_at": "2026-08-29",
   "observation": "2026-08-29: the model shipped, merged and deployed at `annotate`; it binds nothing and has produced no soak rows yet — US equities were shut (Saturday) and the newest alpaca dispatch, 2026-08-29T07:32:41Z, predates the deploy. So the soak's emptiness is 'we did not look', NOT 'the writer failed'; that distinction is what #10412 automates. Nothing armed, funded, routed or sized. alpaca_live remains strategies: [] and $200.10.",
   "last_checked": "2026-08-29"
  },
  {
   "id": "OI-20260829-PROP-RISK-GATE-SOAK-SHIPPED-BUT-UNEXERCISED",
   "opened": "2026-08-29",
   "kind": "monitoring",
   "loud": true,
   "check_every_days": 3,
   "verified_at": "2026-08-29",
   "last_checked": "2026-08-29",
   "observation": "Shipped this session in PR #10414 (CI green) and NOT yet exercised. The writer, its diag allowlist entry and 6 tests exist; no soak row has been produced by a real ticket, because that needs a prop ticket rendered with account_id set while mode != off.",
   "summary": "src/prop/prop_risk_gate.py promised an annotate-mode soak row from 2026-08-25 and wrote NOTHING (318 lines, zero file writes) — so the Tier-3 PROP_TICKET_RISK_GATE_MODE=enforce decision, on the one account that can be PERMANENTLY DISABLED, had no evidence behind it. The writer now exists. It is DEPLOYED, not PROVEN, and those are different facts.",
   "detail": "breakout_1 sat $55.00 from its $4,700 static-DD floor on the day this was found, down from $64.00 at the 08-25 review that BUILT the module, with distance_to_daily_loss_usd null (day_pnl_state realized_unreported). Every sibling *_MODE gate (netting_attribution, protection_reassert, cash_settlement, exit_lever) writes a soak row AND registers a read surface; this one had neither. It is the 4th recurrence of BL-20260825-ALERT-AND-CADENCE-STATE-FILES-SHIP-WITHOUT-A-READ-SURFACE and the 2nd of the exit_loop_health #8778 shape.\n\nWHY THIS IS A MONITORING ROW RATHER THAN A CLOSED ONE: a fix that is DEPLOYED and a fix that WORKS are indistinguishable from every surface we have — the code is on main, the tests pass, and none of that shows the mechanism ever ran. The review's own unexercised_fixes block exists for exactly this, and two OTHER fixes are sitting in it unexercised for the same reason. Do not accept a proxy: a green test is not the mechanism firing, and neither is the module importing.",
   "clears_when": "A real graded ticket produces a row in runtime_logs/prop_ticket_risk_soak.jsonl, READ BACK through /api/diag/log_file?name=prop_ticket_risk_soak — carrying a `state` from the four (within_cushion / exceeds_cushion / cushion_unknown / no_risk_declared), `annotated` recording the EFFECT, and `would_have_capped`. Explicitly NOT sufficient: the tests passing, the PR merging, the deploy sha matching, or the allowlist entry returning an empty file — an empty file is 'the writer has not run', which is the state this row tracks, not its resolution.",
   "why_it_cannot_be_closed_by_time": "Nothing about waiting makes a ticket get graded. The prop bridge is manual and low-volume, so the soak can stay empty for days while looking exactly like a broken writer — and until a row exists, the enforce decision it was built to inform still has nothing to read, which is the condition that made this worth fixing at all.",
   "refs": "PR #10414 · BL-20260829-PROP-RISK-GATE-PROMISED-A-SOAK-ROW-AND-WROTE-NOTHING · BL-20260825-ALERT-AND-CADENCE-STATE-FILES-SHIP-WITHOUT-A-READ-SURFACE"
  },
  {
   "id": "OI-20260830-E35-GEOMETRY-SHIPPED-TO-9-LEGS-NOT-YET-LIVE-VERIFIED",
   "opened": "2026-08-30",
   "kind": "monitoring",
   "loud": true,
   "check_every_days": 2,
   "verified_at": "2026-08-30",
   "last_checked": "2026-08-30",
   "observation": "2026-08-30 (session 01HYXKHpDQeWv3u4rjWWoL2J): HALF (a) IS NOW SATISFIED, half (b) is NOT, and the criterion itself was sharpened because it was clearable by a trade that proves nothing.\n\n(a) SATISFIED — measured, not inferred. All ELEVEN field values across the TEN legs read back from the live /api/bot/config match config/strategies.yaml on main, held leg included (trend_donchian_eth_prop still 2.5). /api/diag/version additionally shows the running web-api process loaded from git_sha 892c9a2c — the e35 shipping commit itself.\n\n(b) NOT SATISFIED, and the near-miss is recorded above in clears_when: the only closed real-money row on an e35 leg since the deploy (trade 4904) was opened nine days before it AND was an operator flatten. NOTHING HAS OPENED on any of the ten legs since the deploy — the donchian legs are inside their channels (measured: SOL close 104.64 within [100.53, 110.58]), and every recent audit row for them is an *_eval with side=none. So this is 'no trade yet', not 'the geometry is not applying'; the two are different and only (a) is evidence about the geometry.\n\nMerged to main. NOT yet confirmed on the running trader: the values reach the VM through ict-git-sync and config is hot-read, but no read of /api/bot/config has yet shown the new numbers, and no trade has yet opened under them.",
   "summary": "Tier-3, operator-approved 2026-08-30: e35 bracket geometry shipped to 9 legs (10 fields). THREE route to bybit_2 = REAL MONEY (trend_donchian, trend_donchian_eth_4h, trend_donchian_xrp_4h). It is DEPLOYED, not PROVEN, and those are different facts. A 10th approved leg (trend_donchian_eth_prop) was HELD — see detail.",
   "detail": "⚠️ THE 10TH APPROVED LEG WAS NOT SHIPPED. trend_donchian_eth_prop's sm1.5 cell is real (path_b_wf_pass, wf 5/6, +13.4198) but it is a config-only EXIT variant whose design premise is ENTRY-IDENTITY with trend_donchian_eth (atr_stop_mult is an entry param there, deliberately), it was graduated to execution: live on a prop EV/survival gate measured on that construction (relay #8975, run_ev_montecarlo under breakout.yaml, +$883 @ P(net>0)=0.8477 over 1,110 trades), and breakout_1 can be PERMANENTLY DISABLED by a drawdown breach. A stop change moves the R denominator, the size and the breach probability, so that gate would no longer describe what runs. Caught by test_unit_c_prop_exit_variants, which was RIGHT; the test was NOT weakened. Unblocks only on a fresh run_ev_montecarlo under breakout.yaml plus operator approval on that evidence.\n\n⚠️ TWO OF THE TEN SHIPPED CHANGE STRATEGY SHAPE, NOT STOP WIDTH, and one of those is real money: ada_pullback_2h tp_r 50.0 -> 4.0 and trend_donchian_xrp_4h tp_r 50.0 -> 3.0 introduce a take-profit where the leg previously had NONE (50 is the no-target sentinel). Capping a TREND leg at 3R truncates exactly the rare large winners trend-following depends on. The walk-forward passed it (path_b_wf_pass, wf 5/6) and the operator approved the full set; this row exists so the first real close on that leg is LOOKED AT rather than assumed fine.\n\n⚠️ avax_pullback_2h sm2 shipped at d_net_r +1.0084 against my own written recommendation to DROP it. The operator reaffirmed the full 15 after reading that. It is execution: shadow, so nothing live moves — recorded so the decision is attributable, not re-litigated.\n\n⚠️ EVERY ONE OF THESE RESTS ON A RUN WHOSE CLEAN-LEG CONTROL FAILED as specified, with a fold-level substitute the operator accepted on 2026-08-29. That is a weaker evidentiary base than B4 had, and it is the reason to watch rather than to wait.\n\nThe matrix cells are now `shipped`, so `matrix-bracket-values` enforces the 17 shipped bracket_geometry cells against config/strategies.yaml on every PR (state the population: the matrix carries 39 shipped cells across ALL levers; this guard covers the bracket_geometry column only, and per BL-20260829-MATRIX-BRACKET-VALUES-READS-ONLY-THE-BACKTICKED-CELL-SPELLING it reads only the backticked cell spelling. An earlier version of this row said \"all 18 shipped cells\" — wrong on both the count and the scope, in the direction of overstating coverage) — a silent drift back is now a CI failure rather than a discovery.",
   "clears_when": "BOTH: (a) /api/bot/config read back shows the new atr_stop_mult/tp_r on all 10 legs, confirming ict-git-sync carried them to the running trader; and (b) at least one closed trade on a real-money leg that was **OPENED AFTER the deploy of 892c9a2c**, read against broker truth. ⚠️ (b) SAYS *OPENED*, NOT *CLOSED*, AND THAT WORD IS THE WHOLE CRITERION — it previously read 'at least one CLOSED trade exists on a real-money leg under the new geometry', which a trade opened BEFORE the deploy satisfies on its face while carrying the OLD geometry. That is not hypothetical: trend_donchian_eth_4h trade 4904 (bybit_2, ETHUSDT) CLOSED 2026-08-30T09:48:15Z — after the deploy, on a real-money e35 leg — and is disqualified TWICE OVER. It was OPENED 2026-08-21T21:54:54Z, nine days earlier, so its bracket is the pre-e35 2.5 atr_stop_mult; and per PR #10482 it was an OPERATOR FLATTEN executed to satisfy the flat-symbol guard for the Bybit hedge switch, i.e. not a strategy exit at all. Its exit_reason reads 'reconciler_filled', which describes HOW the row closed and misdescribes WHY. A session applying the old wording could have cleared this row on it. Explicitly NOT sufficient: the PR merging, CI green, or the guard passing — the guard compares the matrix to the repo, and neither of them is the running trader. Also NOT sufficient: a closed trade whose pnlProvenance is 'fabricated' or 'unverified', or one closed by an operator action rather than by the strategy — read PB-20260830-XRP-4H-FIRST-CLOSE-UNDER-NEW-BRACKET-UNVERIFIED before grading any first close, and do NOT grade the exit off exit_reason alone.",
   "why_it_cannot_be_closed_by_time": "Nothing about waiting makes a config value reach a process or a trade close under it. The 4h legs open rarely, so the soak can look identical to a broken deploy for days — which is exactly the state this row distinguishes.",
   "refs": "PR #10444 (the re-check) · docs/research/e35-matrix-recheck-2026-08-29.md · BL-20260829-HARNESS-FORCE-CLOSES-TREND-PULLBACK-TRADES-ON-BAR-COUNT-AND-LIVE-NEVER-DOES · PB-20260830-XRP-4H-FIRST-CLOSE-UNDER-NEW-BRACKET-UNVERIFIED (the per-leg criteria for (b) on trend_donchian_xrp_4h — read it before doing the check, it says NOT to grade the exit off trades.exit_reason) · PR #10450 (892c9a2c, the ship)"
  },
  {
   "id": "OI-20260829-E35-REVERSED-LEGS-ARE-A-TIER-3-PROPOSAL-SET-NOT-APPLIED",
   "opened": "2026-08-29",
   "kind": "background_awareness",
   "loud": true,
   "summary": "The e35 matrix re-check found 15 SHIPPABLE gate-passing cells across 10 live legs the matrix had recorded as honest_negative/blocked. The matrix records them as `passed_unshipped`. NONE is applied — declaring any of that geometry in config/strategies.yaml is Tier-3 and needs the operator to approve the exact change.",
   "detail": "Direction reversed because the prior verdicts were measured on a harness that force-closed every trade at `timeout_bars` (default 200) while no live trend/pullback/squeeze unit implements a bar-count exit — the B9 mechanism, not a surprise. Largest: trend_donchian_eth_4h `sm2` (wf 6, +28.73R), trend_donchian_sol_4h `sm1.5` (wf 5, +27.72R), trend_donchian `sm2` (wf 5, +21.78R). Smallest: avax_pullback_2h `sm2` at +1.01R, which is a candidate to DROP rather than ship — it clears the gate on a margin that will not survive costs.\n\nTWO THINGS A PROPOSAL MUST CARRY, or it repeats what B4 had to be corrected for: the selection rule stated in ADVANCE (B4 used highest wf_wins_effective, tie-break d_net_r) with every value traced to its corpus row; and the § 1 caveat, because this whole run's CLEAN-leg control FAILED as specified and the operator accepted a fold-level substitute rather than a passing control.\n\nTimeout-carrying passing cells are excluded from the 15 and must never be totalled with them — they are evidence about the harness, not shippable candidates.",
   "clears_when": "Either the operator approves a specific per-leg geometry change and it ships with the matrix flipped to `shipped` (at which point `matrix-bracket-values` enforces the values), or the set is explicitly declined and the matrix records why. Explicitly NOT sufficient: the re-check PR merging — it changed records only, and `passed_unshipped` is a measurement, never a recommendation.",
   "refs": "PR #10441 · docs/research/e35-matrix-recheck-2026-08-29.md §§ 5,7 · BL-20260829-HARNESS-FORCE-CLOSES-TREND-PULLBACK-TRADES-ON-BAR-COUNT-AND-LIVE-NEVER-DOES"
  },
  {
   "id": "OI-20260830-BYBIT-HEDGE-MODE-ARMED-BUT-UNEXERCISED",
   "opened": "2026-08-30",
   "kind": "monitoring",
   "loud": true,
   "check_every_days": 1,
   "summary": "Bybit HEDGE position mode is now live on ALL EIGHT (account, symbol) pairs — bybit_1 SOL/ETH/BNB/BTC and bybit_2 BTC/ETH/XRP/ADA (bybit_2 is MAINNET). The mechanism is proven WIRED (a real pair opened with distinct books). It is NOT proven to PREVENT STRANDING, because that open never faced the condition that causes stranding.",
   "detail": "Tier-3, operator-approved 2026-08-30. Both halves are verified, and they are SEPARATE halves. VENUE - switch-bybit-position-mode (new action) on SOLUSDT (#10457) and ETHUSDT (#10458), each returning venue_mode_sent=3 ('Both Sides'; the switch endpoint's enum is NOT positionIdx, where hedge would be 1 - passing the positionIdx value would switch the wrong way while looking right), after.mode=hedge, position_idxs [1,2], switch_verified=true. ENV - BYBIT_HEDGE_MODE_SYMBOLS='bybit_1:SOLUSDT,bybit_1:ETHUSDT' confirmed in the RUNNING process via get-env (#10460: process == declared), not merely written to .env. BNBUSDT/BTCUSDT on the same account remain one-way. === WHY THIS IS A ROW AND NOT A CLOSED TASK. The evidence shows the venue ACCEPTS two books and that our config agrees with it. It does NOT show a market-neutral pair opening with both legs surviving, which is the entire point: BL-20260821-PAIRS-SOL-ETH-STRANDS-ON-EVERY-OPEN measured 36 opens / 28 half_open (~78%) over 1000 soak rows, stranded-leg split ETHUSDT 14 / SOLUSDT 14 - dead even, which is why BOTH symbols were switched rather than one. === THE COUPLING IS THE HAZARD. Venue-hedge + empty allowlist => no positionIdx is sent and Bybit REFUSES every order on that symbol; venue-one-way + armed allowlist => a positionIdx of 1/2 is refused the same way. Either half changing alone silently stops that symbol trading. A session touching either must change BOTH, and the env half needs a trader restart. === POPULATION. bybit_1 is account_class=paper, demo=true - no real money at risk. The bybit_2 mainnet account is a DIFFERENT Bybit UID (verified 2026-08-22, shared_uid_groups {}), untouched and still one-way. === ALSO PENDING ON THIS: the plan to move the pairs sleeve onto its own account is DEFERRED, not cancelled - the operator's condition is 'if the hedging is working, then we won't put the sleeves on their own account', and 'working' is not yet established.",
   "clears_when": "A pairs `open` event (pairs_sol_eth or pairs_bnb_btc) in which ALL THREE hold: (1) both legs are placed and the venue reports position_idx 1 or 2 for each, never 0; (2) no half_open and no stranded leg; and CRITICALLY (3) a DIRECTIONAL, non-pairs position was CONCURRENTLY OPEN on the same symbol at the moment of the open — i.e. the netting collision that causes stranding was actually present. === WHY (3) IS THE WHOLE CRITERION. Without an opposing directional position there is nothing for the pair leg to net against, so a clean open is the EXPECTED result in one-way mode too and therefore tells you nothing about hedge mode. This is not hypothetical: the 09:05:04Z open described in `observation` satisfied (1) and (2) perfectly and proved nothing, because bybit_1 held ZERO non-pairs open rows at the time (verified). An earlier draft of this clears_when asked only for 'a run of clean opens' and WOULD HAVE BEEN CLEARED by that open — which is exactly the failure this row exists to prevent. === ALSO NOT EVIDENCE: a bar where |z| < entry_z 2.0 opens nothing at all. Absence of stranding on a bar that placed no trade is not a negative result, it is no result.",
   "observation": "2026-08-30T09:05:04Z — FIRST HEDGE-MODE OPEN, and it is HALF the evidence. pairs_sol_eth opened with SOLUSDT short at position_idx 2 and ETHUSDT long at position_idx 1 — two distinct books, which was structurally impossible under one-way netting — both legs carrying both protective legs, zero half_open in the 40-row soak tail, no exchange_rejected. positionIdx correctly tracked the POSITION's direction rather than the order side, which is the single thing the resolver's docstring warns is easiest to get wrong. === WHAT IT DID NOT SHOW: bybit_1 held ZERO directional (non-pairs) open rows at that moment, so no netting collision was possible and this open would very likely have succeeded in one-way mode too. Wired, not proven. === SCOPE AS OF 09:57Z: all four bybit_1 symbols hedge (BNBUSDT #10465, BTCUSDT #10466 at 09:32Z) and all four bybit_2 symbols hedge (XRPUSDT #10473, ADAUSDT #10476, ETHUSDT #10477, BTCUSDT #10478), env armed to all eight pairs and verified in the running process. bybit_2 is MAINNET and required flattening two live real-money trades to satisfy the flat-symbol guard — id=4934 xrp_pullback_2h and id=4904 trend_donchian_eth_4h. ⚠️ CORRECTION 11:10Z — I first recorded these as -2.38 and -0.80 (-3.18 total). THE JOURNAL SAYS -2.4509 and -1.2872 (-3.7381 total), read back from /api/diag/journal. The figures I quoted were the flatten tool's own report at flatten time, not what was booked; the ETH one is out by 61%. AND THE TWO ARE NOT THE SAME KIND OF NUMBER: XRP's carries exit_price_source=bybit_closed_pnl (MEASURED broker truth), ETH's carries candle_at_close (ESTIMATED) — even though we caused that close ourselves, so a real Bybit fill for it exists and was not recovered, operator-authorized as a one-time operational action. === SECOND PAIR NOW COVERED: pairs_bnb_btc measured a 26% strand rate (5 half_open / 19 opens) over the last 1000 soak rows, 2026-08-21T17:04 to 2026-08-30T09:05; pairs_sol_eth measured 76% (28/37) over the same window. === ⚠️ bybit_2 CARRIES A RISK bybit_1 DOES NOT. The pairs sleeve is pinned to bybit_1 (config/pairs.yaml), so bybit_2 has no opposing-leg collision and no measured stranding — the switch there was operator-directed, not evidence-driven. order_monitor.py:5301 names bybit_2 explicitly as a one-way NETTING account and the netting-attribution reconciler is built on that premise; with 7 strategies over 4 symbols, opposing strategies will now hold SEPARATE long and short books instead of netting, changing margin consumption and gross exposure. Watch bybit_2 for netting-reconciler anomalies and unexpected gross exposure, not just for stranding. === ⚠️ 10:20Z — SIX OF THE EIGHT ARMED PAIRS HAVE NOT PLACED A SINGLE ORDER SINCE THE ARM, so their env/venue agreement is UNVERIFIED, and this row's own 'coupling is the hazard' paragraph is exactly what that risks. Measured over /api/diag/journal?table=trades&limit=400 (population: 400 newest rows, 2026-08-20T15:27Z to 2026-08-30T09:05Z): only TWO rows postdate the 08:29Z arm — bybit_1 SOLUSDT and bybit_1 ETHUSDT, the 09:05 pair open. bybit_1 BNB/BTC and all four bybit_2 symbols have placed nothing. === WHAT IS AND IS NOT ESTABLISHED. ZERO `exchange_rejected` rows appear anywhere in the 400 (all 176 refusals are plain `rejected`, the risk-manager refusal, and the newest predates the arm at 07:xx) — so nothing has been refused by the venue. But 'not refused' and 'confirmed to agree' are different statements, and only the 2 exercised pairs support the second: a pair that never attempted an order cannot have been refused. If any of the six disagrees, EVERY order on that symbol is refused the moment one is attempted, and on bybit_2 that is mainnet. === WHAT TO CHECK on the daily cadence: a first order on each of the six unexercised pairs, and specifically whether it carries a position_idx of 1 or 2 rather than being refused. Do NOT read continued quiet as health — a symbol that is refused looks identical to a symbol nothing wanted to trade, which is the collapsed state this repo files as a bug everywhere else. === 12:35Z — BOTH HALVES VERIFIED TO AGREE ON ALL EIGHT PAIRS, and the 'six unexercised pairs' worry from 10:20Z is RESOLVED as a false alarm I raised against my own record. get-env #10492 read the RUNNING process (/proc/<MainPID>/environ, the FLIP_CONFIDENCE_THRESHOLD precedent): BYBIT_HEDGE_MODE_SYMBOLS = 'bybit_1:SOLUSDT,bybit_1:ETHUSDT,bybit_1:BNBUSDT,bybit_1:BTCUSDT,bybit_2:BTCUSDT,bybit_2:ETHUSDT,bybit_2:XRPUSDT,bybit_2:ADAUSDT' with process == declared. That is exactly the eight pairs switched at the venue. === MY ERROR, recorded because the shape recurs: I re-read issue #10460 and treated its value as current. #10460 was taken at 08:29Z, BEFORE six of the eight venue switches, so it correctly showed only the two bybit_1 pairs. A partial read from an earlier point in the sequence is not the current state, and I raised a live-refusal alarm on real money from it. The fix was to read the process again, which took one dispatch. === THE COUPLING IS NOW POSITIVELY CONFIRMED IN BOTH DIRECTIONS. Armed side: 8 env pairs against 8 venue-hedge pairs. Unarmed side: bybit_portfolio holds XRPUSDT 6364.8 and ETHUSDT 7.76 at position_idx=0 (one-way, never switched) and is ABSENT from the allowlist — so no positionIdx is sent there, which is correct and is the state that would otherwise refuse every order on a third account nobody was watching. === AND THE 'IS THE RECONFIG BLOCKING TRADES?' QUESTION IS ANSWERED NO, on four independent lines: (1) the halves match; (2) bybit_portfolio is coherent; (3) every hedge symbol is EVALUATING normally — 9 *_eval audit rows each since 08:29Z across ADA/XRP/SOL/ETH/BNB/BTC — with zero exchange_rejected in the window; (4) bybit_1 SOL/ETH are proven end-to-end by the 09:05Z pair open at position_idx 2 and 1. ⚠️ (3) still is not proof for the six that have not ATTEMPTED an order — evaluating is not placing — so the clears_when criterion stands unchanged.",
   "verified_at": "2026-08-30"
  }
 ],
 "there_is_no_cap": "Deliberately UNCAPPED (operator-directed 2026-08-26: 'we don't want to cap the number of bugs we can track, we want to ensure that they are actually being tracked, fixed, and learned from'). An earlier version capped this at 12 rows; that was a bandaid — it bounded the LIST rather than making anything get read or fixed. What bounds it instead is that a `monitoring` row must be RE-OBSERVED on a cadence: it cannot be carried by doing nothing.",
 "monitoring_rows_are_enforced_by_observation": "kind:'monitoring' rows carry `check_every_days`, `verified_at` and `observation`. A row past its cadence is DUE and is rendered into CLAUDE.md, so the next session sees it in context before acting. Clearing it for another cycle costs one honest look: record WHAT YOU OBSERVED and today's date. It cannot be cleared by asserting progress — `observation` must state a thing that was seen. This replaces `loud`, which was just another alarm to walk past."
}
