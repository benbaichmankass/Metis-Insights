{
  "_comment": "RESEARCH-REVIEW backlog — the research/evidence pipeline, NOT trading performance. Split from performance-review-backlog.json 2026-08-30 on the same reasoning as the 2026-05-26 three-way review split: a different RUBRIC wants a different backlog. 'this sweep needs re-running at higher n' and 'this strategy's win rate slipped' are graded against different bars and would bury each other in one file. WHAT BELONGS HERE: a sweep that landed and nobody read; a queue job blocked or underpowered; a harness that produced a number nobody can reproduce; a corpus whose provenance is unclear; an analysis that must be re-run at higher n before it decides anything. WHAT DOES NOT: a strategy tweak (performance), a wiring bug (health), a model promotion (ml). FILE THROUGH scripts/ops/backlog_append.py::append_row, NEVER by hand — it round-trips the exact serialisation and refuses a row restating one already filed.",
  "schema_version": 1,
  "items": [
    {
      "id": "BL-20260830-M20-CORPUS-78-UNITS-NEVER-READ-MOST-UNDERPOWERED",
      "title": "78 landed research units were never read, and the m20 half is largely below its own power floor",
      "area": "research pipeline / M40 R3-R5",
      "severity": "medium",
      "tier": 1,
      "status": "open",
      "detail": "First run of scripts/research/research_disposition.py --report (2026-08-30) over the two committed corpora: 92 unread / 196 superseded_unread / 0 dispositioned. This session dispositioned the 14 e35 units from RQ-20260830-002, leaving 78 unread, essentially all m20. POPULATION for the power claim: over the newest run per leg, 318 m20 rows carry base_trades_OOS on 317 of them, and 247 sit BELOW the 49.06 floor implied by the queue's own declared power block (n >= (z_a/2 + z_b)^2 / d^2 at alpha=0.05, power=0.80, d=0.4). Observed values run 30-36 with one leg at 4. NOT A BUG IN THE CORPUS - the rows are honest measurements; the finding is that nobody has read them and that most of them cannot answer their own question at the n they hold.",
      "why_it_matters": "R1-R6 of the research architecture stop at 'landed'; no stage covered 'read', and no review skill referenced the corpora at all (grep returned 0 across all five). So compute was spent, rows were committed, and no decision was ever derived from them. An underpowered unit is worse than an unread one if it is read carelessly: R4's whole point is that an underpowered run manufactures a false finding, and 329 historical honest_negative verdicts already sit at a median OOS base of 33 trades.",
      "next_action": "In the next /performance-review, work stage 3 of the research-pipeline section: run --unread-only, grade each unit's n_oos against its declared floor FIRST, and disposition it as 'underpowered' (converting to a data-acquisition task) rather than reading a verdict off it. Do not batch-disposition to clear the count - append() refuses a vacuous reason.",
      "resolution_criteria": "Closes when every unit reported 'unread' by research_disposition.py carries a ledger entry with a non-vacuous reason, AND the units graded 'underpowered' have either an enqueued data-acquisition job or a stated reason why more n is unobtainable. Re-running the report and seeing a lower number is NOT sufficient - a superseding run also lowers it.",
      "opened_at": "2026-08-30",
      "opened_by": "session_018wqzuqBjxkiaEEBr8kJC59",
      "source": "first run of the research-disposition ledger"
    },
    {
      "id": "BL-20260830-E35-CORPUS-CARRIES-NO-SAMPLE-SIZE-SO-R4-CANNOT-GRADE-IT",
      "title": "The e35 bracket corpus records no OOS sample size, so its rows cannot be power-graded at all",
      "area": "research pipeline / M40 R1 results contract",
      "severity": "medium",
      "tier": 1,
      "status": "open",
      "detail": "R1 of docs/research/RESEARCH-WORKFLOW-ARCHITECTURE-2026-08-27.md makes a row inadmissible unless it carries 'its power: n, and the minimum detectable effect'. MEASURED over the whole e35 corpus (n=8,321 rows, 42 distinct keys): the ONLY key resembling a sample size is `split_target_oos`, present on 377 rows (4.5%) and carrying the value 50 - which is the sweep's TARGET, not the achieved out-of-sample trade count. There is no e35 equivalent of m20's `base_trades_OOS`. CONSEQUENCE, observed not predicted: the disposition ledger graded 78 unread units on 2026-08-30 and could power-grade every m20 unit (10 clearing the 49.06 floor, 40 below it) while 27 e35 units were left UNREAD because there is nothing in the row to grade them against. NOT a defect in the measurements themselves - the R-deltas and gate verdicts are honest; the gap is that the row cannot state the denominator its own verdict rests on.",
      "why_it_matters": "R4 makes the power gate BLOCKING precisely because an underpowered run costs compute AND manufactures a false finding. A corpus that cannot report n cannot be put through that gate, so every e35 verdict is consumed on trust. This is the same shape as the repo's own 'ALWAYS STATE THE POPULATION' rule: a number whose denominator is unavailable is not quotable, and 8,321 such rows are currently the largest research artifact the fleet holds.",
      "next_action": "Add the achieved OOS trade count to the e35 extractor (scripts/research/e35_corpus_extract.py) so new rows carry it, and decide explicitly whether historical rows are backfillable from the sweep reports or must stay ungradeable. Do NOT back-fill a zero or the target value - that would assert an observation nobody made, which is the fabrication class provenance.py exists to stop.",
      "resolution_criteria": "Closes when a NEW e35 sweep lands rows carrying an achieved OOS sample size AND research_disposition.py can power-grade them (N_FIELD['e35'] stops being None). Historical rows staying ungradeable is an acceptable outcome IF that is recorded as a deliberate decision rather than left implicit.",
      "opened_at": "2026-08-30",
      "opened_by": "session_018wqzuqBjxkiaEEBr8kJC59",
      "source": "first power-grading pass of the disposition ledger"
    },
    {
      "id": "BL-20260831-FEASIBILITY-OBSERVATIONS-ARE-BLIND-TO-RUN-CONFIG",
      "opened": "2026-08-31",
      "severity": "medium",
      "tier": 1,
      "status": "open",
      "area": "research queue / R4 admission gate",
      "summary": "research_queue.observed_n_by_leg keeps the BEST achieved n per leg with no notion of the run configuration that produced it, so a corpus-grounded feasibility check counts observations from a DIFFERENT config as evidence about the one about to run — and on a raise it deadlocks the unit that would produce the new evidence.",
      "detail": "FOUND 2026-08-31 while raising e35's `split_target_oos` 50 -> 60 for RQ-20260830-002.\n\n`observed_n_by_leg(corpus)` reduces the corpus to {leg: best n_oos} and `grade_power`'s corpus-grounded branch compares that against the power floor. Nothing in either function knows what run SETTINGS produced an observation, although the corpus rows carry them (`split_target_oos` is a column on every e35 row).\n\nTWO CONSEQUENCES, and the second is the dangerous one:\n(1) STALE-CONFIG EVIDENCE. After a raise, pre-raise rows remain the best observation for every leg, so a `source: corpus` check grades the NEW config against the OLD config's achieved n.\n(2) DEADLOCK. Measured on the 9 capable legs: pre-raise min_per_leg is 46 against a floor of 49.06, so grounding RQ-20260830-002 on the corpus today returns INFEASIBLE and BLOCKS the dispatch — the only thing that would produce post-raise evidence. Scoping observations by config does not fix it either: with zero matching rows the grade flips INFEASIBLE -> UNVERIFIABLE, which also blocks.\n\n⚠️ THE DEADLOCK HALF IS RESOLVED, THE BLINDNESS IS NOT — corrected the same day. This row was filed while `infeasible` was a REFUSAL. Hours later the operator directed leniency (\"I'd rather err on the lenient side here and not exclude tests that may [give] some insights\"), making `underpowered` and `infeasible` RUNNABLE. There is no deadlock any more: RQ-20260830-002 is now corpus-grounded, grades `infeasible` honestly, RUNS, and will flip to `cleared` on its own once a target-60 run lands ~56 per leg. The earlier workaround (declare `source: none` and have a human come back to switch it) is gone, and with it the 'carried forward in prose' hazard.\n\nWHAT REMAINS, and it is the original finding: observations are still blind to run configuration, so for exactly one cycle after any config change the gate grades the NEW config against OLD rows. Under leniency that is survivable rather than blocking — the unit runs and its verdict is stamped on its rows — but the grade is still wrong for that cycle, and a reader who does not know the raise happened will read the interim `infeasible` as evidence the raise failed. Under a future policy that blocks again, the deadlock returns exactly as described above.",
      "resolution_criteria": "`observed_n_by_leg` accepts a config scope (e.g. feasibility.observed_where: {split_target_oos: 60}) and counts only rows matching it, verified by a test that a pre-raise row does not satisfy a post-raise scope. ⚠️ The 'must not deadlock' half of the original criterion is NO LONGER the test — leniency removed it — but a scoped check with no matching rows must still resolve to a RUNNABLE state, or re-introducing config-scoping would re-introduce the block by the back door."
    },
    {
      "id": "BL-20260831-QUEUE-DISPATCHED-ROWS-CARRY-NO-IDENTITY-ON-THE-ONE-ROUTE-THAT-HAS-EVER-WORKED",
      "opened": "2026-08-31",
      "severity": "high",
      "tier": 1,
      "status": "open",
      "area": "research queue / R5 dispatch + corpus identity",
      "summary": "Across ALL THREE registered corpora — 9,744 rows — not one carries research_unit or research_power_state. That includes the 44 gld_compat rows written by the queue's ONE confirmed successful dispatch, and there they are unstampable BY CONSTRUCTION: gld-compat-matrix.yml declares no research_unit input at all.",
      "detail": "MEASURED 2026-08-31 by reading every row of the three corpora that scripts/research/research_disposition.py::CORPORA declares — e35 8,321 rows, m20 1,379, gld_compat 44. `research_unit` is None on 9,744 of 9,744 and `research_power_state` is None on 9,744 of 9,744. STATE THE POPULATION: this is the registered corpora only; a store not in CORPORA was not examined.\n\nTHE TWO CAUSES ARE DIFFERENT AND ONLY ONE IS A DEFECT.\n\n(a) e35 (8,321 rows) — NOT a defect. `e35-bracket-sweep.yml` DOES declare `research_unit` and `power_state` inputs and threads them as RESEARCH_UNIT / RESEARCH_POWER_STATE into the extractor step (lines 51, 367-368). Its rows are unstamped simply because no e35 run has ever been dispatched THROUGH the queue — every one was run by hand. The route works and has not been exercised.\n\n(b) gld_compat (44 rows) — THE DEFECT. `gld-compat-matrix.yml` contains no occurrence of `research_unit` anywhere, so it cannot accept the input. `dispatch_queue.py` injects the stamp only when the unit declares `research_unit` (line 183), and RQ-20260827-001 declares none — which is the ONLY reason that dispatch succeeded, since `gh workflow run -f` on an undeclared input is rejected outright. So the conditional injection is load-bearing and correct, AND it means this route's rows can never carry the identity.\n\nWHY THAT MATTERS RATHER THAN BEING A CURIOSITY. Run 33340458710 (event=schedule, 2026-08-30T22:56:22Z) dispatched RQ-20260827-001 and 11 rows landed in gld-compat-matrix-verdicts.jsonl 28s later — that evidence was used on 2026-08-31 to CLEAR OI-20260827-RESEARCH-QUEUE-DISPATCHER-NEVER-FIRED. The clearance stands: the dispatcher did fire and rows did land. But the rows are UNATTRIBUTABLE to the unit that produced them, so the queue's one proven route delivers exactly the thing the identity exists to prevent — a row from a graded job being byte-indistinguishable from an ungraded one. The extractor's own comment states the contract: 'Empty is NOT-QUEUE-DISPATCHED, never was a test.' Every gld row reads as not-queue-dispatched.\n\nNOT A TEST GAP IN THE OBVIOUS PLACE. tests/test_research_chain_end_to_end.py covers the dispatcher argv, the workflow env and the extractor across their seams, and it passes — because it exercises the e35-shaped path. Nothing asserts that a REGISTERED CORPUS's owning workflow can accept the stamp at all. That is the seam one level out.",
      "resolution_criteria": "EITHER (1) gld-compat-matrix.yml gains `research_unit` + `power_state` inputs and its writer stamps them onto every row it emits, verified by a queue-dispatched run whose landed rows carry the unit id — read from the corpus, not from the diff; OR (2) a guard asserts that every workflow named by a `run.workflow` in research/queue/*.yaml whose store is in research_disposition.CORPORA declares the stamp inputs, so the gap announces itself on the commit that creates it rather than in an audit. (2) is the durable half and (1) alone leaves the next route free to repeat it. NOT resolved by a passing unit test: the criterion is a stamped row in a committed corpus."
    },
    {
      "id": "BL-20260831-E35-CORPUS-SUPERSEDES-THE-BASELINE-ITS-MONTHLY-JOB-ASKS-ABOUT",
      "opened": "2026-08-31",
      "severity": "high",
      "tier": 1,
      "status": "open",
      "area": "research corpus retention / e35 + RQ-20260831-002",
      "summary": "e35_corpus_extract.measurement_key has NO time component, so a re-sweep SUPERSEDES the prior measurement of the same cell. RQ-20260831-002 is a MONTHLY job whose question is explicitly longitudinal — 'are the shipped cells still behaving as they did when they shipped?' — so running it destroys the baseline it compares against. The job cannot answer its own question from the store it writes to.",
      "detail": "MEASURED 2026-08-31 on a real queue-dispatched run (research-queue-dispatch 33388490432 -> e35-bracket-sweep 33388518030, landed as #10577), by diffing the corpus at 62641dfb against the post-landing commit.\n\nPopulation: rows whose `leg` is one of the five the run swept (iwm/mgc/qqq/scha/spy _1d).\n  before: 1,035 rows / 995 distinct (leg,cell) / max 2 rows per cell\n  after : 1,035 rows / 995 distinct (leg,cell) / max 2 rows per cell\n  cells touched by the run: 995, and ALL 995 already had a prior row\n  pre-probe rows surviving on a re-swept cell: 40\nSo ~955 prior measurements are no longer in the corpus. The total is unchanged because each incoming row replaced one.\n\nMECHANISM READ, NOT INFERRED — my first reading of the arithmetic was wrong and I checked. `merge()` builds `by_key = {r['measurement_key']: r for r in existing}` and lets incoming overwrite; its self-test asserts 're-extract supersedes' with superseded=2, added=0. `measurement_key` is `leg|cell|tp_cap_pct|split_mode|split_target_oos` and its own docstring says 'Re-running the same cell supersedes it'. THIS IS DELIBERATE AND DOCUMENTED. The defect is not in the extractor — it is that nobody joined that retention rule to what the monthly job asks. The 40 survivors differ on a key component (35 gated rows carry split_target_oos 50 while the rest carry None), which is the key behaving exactly as documented, NOT evidence that history is kept.\n\nWHY IT MATTERS BEYOND THIS JOB. `sweep_generated_at` is on every row, so a run is dated — but only the newest row per key survives, so a cell yields ONE timestamp, never a series. Any question of the form 'has this changed since it shipped?' is unanswerable from this corpus by construction, and RQ-20260831-002 is that question, on a monthly cadence, with a power.feasibility block that already concedes it cannot be powered. It would accumulate nothing: each month's run erases the prior month's row for the same cell.\n\nNOT CLAIMED: that the m20 corpus shares this. Its N_FIELD and key were not examined.",
      "resolution_criteria": "A DECISION recorded, then implemented — this is a design call, not a one-line fix. The options are (a) add a time component to measurement_key so re-sweeps accumulate, which grows the store without bound and needs a retention policy of its own; (b) keep superseding but write the superseded row to a history sidecar; (c) accept it and REWRITE RQ-20260831-002's question so it no longer claims to be longitudinal, since an accrual job that erases its own accrual is worse than no job. Resolved when the choice is written down with its reason AND a re-sweep is observed to leave the prior measurement readable (a) or (b), or the job's question no longer asks for it (c). A passing extractor test is not the criterion: the criterion is two dated measurements of one cell readable at the same time, or a question that does not need them."
    },
    {
      "id": "BL-20260831-28-E35-UNITS-ARE-PERMANENTLY-UNGRADEABLE-AND-THE-VERDICT-VOCABULARY-CANNOT-SAY-SO",
      "severity": "medium",
      "tier": 1,
      "opened": "2026-08-31",
      "status": "open",
      "area": "research/disposition",
      "summary": "28 of the 42 unread e35 units carry NO achieved OOS count at all (`base_oos_trades` null on every row), so the R4 power gate cannot grade them -- and they cannot be back-filled: the source report.json files are workflow artifacts, never committed, and a PM-side session has no artifact download. They are therefore stuck in `unread` indefinitely. The verdict vocabulary (actioned / no_action_warranted / underpowered / superseded) has no member for NEVER MEASURED. Recording them `underpowered` would assert a measurement nobody made -- it means 'measured, and too small' -- so the honest options today are to leave them unread (which reads as nobody looked) or to re-sweep, a multi-hour run the corpus exists to avoid. That is a collapsed state in the vocabulary itself: 'we measured n and it was short' and 'n was never recorded' are different facts with different remedies (acquire more data vs re-run the extractor).",
      "evidence": "Measured 2026-08-31 over docs/research/e35-bracket-corpus.jsonl via research_disposition.survey(). 42 unread units: 14 gradeable (n_oos 4..50), 28 with n_oos null. Of the 14, ALL were dispositioned this session -- 1 no_action_warranted (trend_donchian_sol_4h, n=50) and 13 underpowered -- leaving exactly the 28 ungradeable ones. The CORPORA docstring in research_disposition.py already records why the back-fill is impossible ('rows extracted before 2026-08-31 carry no achieved count and CANNOT be back-filled from a session'); what it does not say is that this leaves those units with no legal disposition.",
      "resolution_criteria": "EITHER a fifth verdict (e.g. `ungradeable`) exists and is refused as a terminal close, so a reader can record 'this unit predates the achieved-n stamp' without asserting a measurement -- with a test pinning it distinct from `underpowered` -- OR the 28 legs are re-swept and become gradeable, with the count of still-ungradeable units measured and stated afterwards. Closing this by recording the 28 as `underpowered` is NOT acceptable and is the specific outcome this row exists to prevent.",
      "refs": [
        "scripts/research/research_disposition.py",
        "docs/research/e35-bracket-corpus.jsonl",
        "OI-20260831-42-RESEARCH-UNITS-ARE-UNREAD-AND-NOBODY-IS-SCHEDULED-TO-READ-THEM"
      ]
    },
    {
      "id": "BL-20260831-DISPOSITION-N-OOS-TAKES-A-MAX-WHERE-POWER-STATE-TAKES-WORST-STATE-WINS",
      "severity": "low",
      "tier": 1,
      "opened": "2026-08-31",
      "status": "open",
      "area": "research/disposition",
      "summary": "`load_units` aggregates a unit's rows with OPPOSITE discipline on two fields that both feed the same power judgement. `power_state` is worst-state-wins, with a comment explaining that one clean row must not launder an accrual-tainted unit. `n_oos` two lines later is `max(u['n_oos'] or 0, int(v))` -- the most generous aggregator -- with no comment at all. If a unit's rows ever carried differing achieved-n values, the single largest would decide whether the unit clears the 49.06 floor, which is the same laundering the field above forbids.",
      "evidence": "Measured 2026-08-31: harmless TODAY and only by luck. Across all 14 gradeable unread e35 units, exactly 7 of 199 rows (3.5%) carry `base_oos_trades`, and each unit has exactly ONE distinct value -- those 7 rows are precisely the GATED rows (verified: the cell set carrying an achieved-n is identical to the cell set carrying a gate_verdict), because only a gated cell has an OOS split. So max() currently picks from a one-element set. The reading trap is separate and real: n_oos describes the GATED SUBSET, not the unit -- a reader who takes `n_oos=50` as the unit's evidence base is reading a number off 3.5% of the rows. This session nearly dispositioned trend_donchian_sol_4h on that misreading and caught it only by checking the denominator.",
      "resolution_criteria": "`n_oos` either documents why max() is correct for a leg-level constant, or becomes min()/worst-state-wins to match its neighbour; AND the reported field states its own denominator (e.g. rows_with_n beside n_oos) so a consumer cannot read a gated-subset statistic as the unit's. A test pins whichever discipline is chosen.",
      "refs": [
        "scripts/research/research_disposition.py::load_units"
      ]
    }
  ],
  "updated_at": "2026-08-31"
}
