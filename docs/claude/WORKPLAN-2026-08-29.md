# Work plan — 2026-08-29

**Supersedes [`WORKPLAN-2026-08-26.md`](WORKPLAN-2026-08-26.md) as the live plan.**
It does not replace its findings: every lane below carries forward, with status
re-checked against the field this session rather than copied. Where a row is
unchanged I say so instead of restating its evidence.

**Written as a handoff.** A fresh session should be able to pick any lane and act
without re-deriving what is already measured. Every number states its population;
anything I did not verify this session is marked as inherited.

---

## ⚠️ A CORRECTION TO MY OWN WORK THIS SESSION — read before using Lane B

Earlier today I measured `target_extension_soak` at **900 rows / 21 legs / 6 days,
900 of 900 `sentinel_no_expectation`**, and reported that *"the target-extension
lever cannot fire anywhere in the fleet"* and that the extend-the-winner half of
M20 is *"structurally dead."*

**That over-reads the evidence, and B3 in the 08-26 plan already established why.**
`evaluate_extension` returns `EXT_NO_EXPECTATION` **before** the approach gate, and
`not_approaching` is excluded from `_LOGGED_STATES`. So sentinel legs log on every
evaluation while real-target legs log **nothing at all** until price approaches
their target. An all-sentinel soak is therefore the **expected composition**, not a
finding about the lever.

**What the soak can and cannot say:**

| claim | supported? |
|---|---|
| The 4 real-target legs contributed zero rows in the window | ✅ yes |
| No real-target trade approached its target in the window | ✅ that is what zero rows means |
| The lever would never fire | ❌ **not distinguishable** — opposite follow-ups |

My reading reproduced the 08-26 composition on a fresher window (900 rows to their
n=856) and added nothing. Recorded because the mistake is instructive: I measured a
real thing, and then attached a conclusion the instrument cannot support.

**What IS independently true**, from `config/strategies.yaml` rather than the soak —
this part stands and I verified it this session:

| `tp_r` on the 52 **enabled** strategies | count |
|---|---|
| `>= 50` (sentinel — nothing to extend from) | **28** (23 of them `execution: live`) |
| absent entirely | **20** |
| a real declared target | **4** — and **3 are prop legs** |

The only non-prop live leg carrying a real target is **`xrp_pullback_2h`** (`tp_r: 3.0`).
So the population on which an extension lever could ever act is genuinely tiny. That
is a statement about **declared geometry**, not about the lever's behaviour, and the
two must not be merged again.

---

## What changed since the 08-26 plan

**Six PRs, all merged.** Five are Tier-1 or observe-only; **#10419 is Tier-3 and
changed real-money order geometry on 8 live legs.**

| PR | what | state |
|---|---|---|
| **#10408** | T+1 cash-settlement basis for `alpaca_live`. `basis = min(venue_buying_power, venue_cash − our_unsettled)`, correct under all four combinations of (Alpaca nets unsettled / does not) × (we saw the sale / did not). Ships at **`annotate`** — measures, binds nothing. | merged, deployed |
| **#10411** | The $200 sizing wall **confirmed on the live account**. 23 `alpaca_live` rows in the window, all `rejected`; `sized_qty=0 with balance=200.10` on exactly GLD/SPY/QQQ — the legs' own ATR stops, real balance. Discharges the caveat in `BL-20260826-…CANNOT-SIZE-ITS-LARGEST-SYMBOLS` for three of four symbols. **IWM had no row and stays unconfirmed.** | merged |
| **#10412** | A weekday repo timer that watches the settlement soak. Its content is the **denominator**: it reads the newest alpaca dispatch *before* grading, so an empty soak is never collapsed. ⚠️ A `schedule:` trigger only fires from the DEFAULT BRANCH, so this was not a timer until it merged. | merged |
| **#10413 / #10416** | This workplan + sprint log; the ROADMAP M15 decision landing. `doc-freshness` caught that `ROADMAP.md` had **zero** mentions of cash settlement or T+1 against four Tier-3 decisions taken. | merged |
| **#10419** | **M20 B4 — validated bracket geometry on 8 legs. TIER-3, REAL MONEY.** `atr_stop_mult 2.5 → 1.5` on several. See Lane B. | merged `91de68b9` |
| **#10420** | The `timeout_bars` harness-vs-live divergence: one backlog row + the `htf_pullback_trend_2h` docstring that claimed an exit its module does not implement. | merged `a05483e6` |

**A claim I narrowed in #10411, repeated here so it is not re-broadened:** unsizeable-symbol
refusals do **not** page per signal once the account is live. `silent_refusal_alert`
requires verdict `signalled_never_placed` — *every* gradeable row in the window
refused — so one placed order and it never fires. The hazard is **window-level**
(a roster whose legs are all unsizeable), not leg-level.

---

## Lane A — Alpaca real-money go-live (NEW; the operator's live thread)

`alpaca_live` is **`mode: live` with `strategies: []`** (`config/accounts.yaml:825`,
read from the field). ⚠️ **The empty list is the only thing keeping it flat.** There
is no second gate: adding the first leg IS the live-trading moment and is Tier-3.

⚠️ **`config/accounts.yaml:813` still says `mode: dry_run (CI-guarded)`** twelve
lines above the field that says `live`. Field beats comment; the comment is stale
and is A5 below.

### Operator decisions taken 2026-08-29 — do NOT re-litigate

| decision | value |
|---|---|
| Cash vs margin | **CASH.** Settled, permanent. Measured `multiplier == 1`. |
| Plumbing-test leg | **`tlt_pullback_1h`** — TLT ~$83 so 2 shares fit; n=171, the best-evidenced leg on the roster |
| Per-trade notional cap | **100% — no cap beyond the cash account's own 1.0× limit** |
| `risk_pct` | **stays 0.05** |
| Sequencing | **soak → Tier-2 `apply` flip → route the leg** |
| Funding | **$200.10**, unchanged |
| USO | **excluded from real money on non-financial grounds, permanently, whatever any backtest says** |

⚠️ **Why the no-cap + `risk_pct` 0.05 pair is coherent rather than reckless at this
size, so nobody "fixes" it.** Population: the 12 `alpaca_live` journal rows in
2026-08-20 → 2026-08-29 whose sizing SUCCEEDED (TLT 9, SLV 3) — the rows refused by
`account_mode_dry_run` rather than by the sizer. Every one of the 12 sized to
**qty 2.0**. Arithmetic on `risk_pct: 0.05` against equity $200.10 and TLT ~$83:
risk budget $10.005 over a declared stop distance of ~$0.375/share demands ~26.7
shares, i.e. ~11× the account — and the **cash balance clamps it to 2** ($165.86).
Realised risk at the declared stop is therefore **~$0.75**, not the $10 the 5%
implies. **Cash is the binding cap, not `risk_pct`**, and the 12 observed rows are
what establishes that rather than the arithmetic alone.

| # | item | tier | status |
|---|---|---|---|
| A1 | **Read the settlement soak once it has been exercised.** ⚠️ `would_have_reduced_usd` will read **0.00 on `alpaca_live` however long we wait** — `recent_sales` needs `status='closed'` with a non-null exit price in a 10-day window and that account has none. T+1 binds only after a sale. **The mechanism evidence is on the PAPER accounts**, which do sell (6 closed sales / 10 days, up to $111,069.37). | 1 | open — **automated**, #10412 |
| A2 | **Flip `ALPACA_CASH_SETTLEMENT_MODE` to `apply`, allowlisted to `alpaca_live` only.** ⚠️ `ALPACA_CASH_SETTLEMENT_ACCOUNTS` **empty means NONE**, deliberately the opposite of `CONVICTION_SIZING_ACCOUNTS`/`NETTING_ATTRIBUTION_ACCOUNTS`. Do not "harmonise" it. | 2 | open — needs one operator OK **after** A1 |
| A3 | **Route `tlt_pullback_1h` to `alpaca_live`.** The live-trading moment. | 3 | open — after A2 |
| A4 | **Confirm the first order end-to-end** — journal row, broker fill, protective bracket resting, and a settlement soak row showing the basis it was sized against. | 1 | open — after A3 |
| A5 | Fix the stale `mode: dry_run (CI-guarded)` comment at `accounts.yaml:813`. Config-touching, so not folded into a docs PR. | 1 | open, low |

**Timing:** A1 needs a US cash session. Nothing in Lane A can advance before
**Monday 2026-08-31**, and #10412 fires that day at 14:30 UTC on its own.

---

## Lane B — M20 Active Trade Management (the operator's stated priority)

B1/B2/B3 from the 08-26 plan are **closed** (see it for their evidence; B3's
conclusion is corrected at the top of this document). What remains is shipping.

**Coverage matrix state, re-counted this session** from
`docs/research/exit-refinement-coverage.json` (`updated_at` 2026-08-26T14:40Z),
52 legs × 9 levers:

| lever | `passed_unshipped` | `pending` |
|---|---:|---:|
| **`bracket_geometry`** | **4** | 9 |
| `stale_stop` | 2 | 0 |
| `trail_geometry` | 2 | 1 |
| `exit_ladder` | 1 | 3 |
| `exit_head_ml` | 1 | 3 |
| `vol_trail` | 1 | 2 |
| `trail_decay` | 1 | 0 |
| `giveback_stop` | 0 | 1 |
| `regime_flip_exit` | 0 | 1 |
| **total** | **12** | **20** |

⚠️ **RE-COUNTED 2026-08-29 after B4 shipped and B9 landed** (`updated_at`
2026-08-29T18:00Z). The table above previously read **22 / 20** with
`bracket_geometry` at **14**, counted before either. Both movements are real and
they are different kinds of thing:

- **−8** `bracket_geometry` cells moved `passed_unshipped` → **`shipped`**: PR
  #10419 declared them that morning and the matrix was never updated. ⚠️ **No guard
  caught that** — `matrix-config-agreement` covers 4 levers and `bracket_geometry` is
  not one (B10).
- **−2** moved → **`blocked:no_live_bar_count_exit`** (`mes_trend_long_1d`,
  `tlt_pullback_1d`): every cell that passed their gate prescribes a bar-count
  timeout no live unit implements (B9). **They were never shippable**, so this is a
  correction of the record, not progress against it.

The remaining 4 are `eth_pullback_2h`, `eth_pullback_prop_2h`, `gld_pullback_1h`
and `spy_pullback_1h` — **all four carry a `timeout_binding` caveat** and for three
of them the *best* cell is undeliverable while a weaker one stands. Read the cell's
note before shipping its headline number.

Everything else is `honest_negative` — tested and failed its gate, which is the
pipeline working, not a gap.

| # | item | tier | status |
|---|---|---|---|
| **B4** | ~~Ship the 14 validated `bracket_geometry` legs.~~ ✅ **SHIPPED 2026-08-29 — 8 of 14, [#10419](https://github.com/benbaichmankass/Metis-Insights/pull/10419), merged `91de68b9`.** 14 fields on `spy`/`qqq`/`iwm`/`scha_trend_long_1d`, `mgc_pullback_1d`, `uso_trend_1h` (tp_r + stop) and `tlt_pullback_1h`, `slv_trend_1h` (stop only). Selection rule: highest `wf_wins_effective`, tie-break `d_net_r`, over `wf_pass`/`path_b_wf_pass` cells; every value traces to `e35-bracket-corpus.jsonl` and each edit carries its cell/verdict/wf inline. Verified on `main` by counting the annotations — **14**. ⚠️ **TWO CORRECTIONS TO THIS ROW'S OWN CLAIM.** (a) It said bracket geometry is *"the only route by which the 48 target-less legs could ever gain one"* — of the 8 shipped only **6** declare a real `tp_r`; `tlt_pullback_1h` and `slv_trend_1h` are stop-only, and 5 of the 14 winning cells declare no target at all. The operator approved on the broader claim. (b) The remaining 6 are **not** merely unshipped — see B9. | 3 | ✅ **DONE** |
| B5 | **Re-read `target_extension_soak`** — now unblocked, but expect less than B4 promised: only **6** legs gained a declared target, so the real-target population went 4 → ~10 of 52, not 4 → 18. Give it enough live time for one of those legs to actually approach its target before reading; an all-sentinel result on day one means nothing. ⚠️ Read `expectation_state` **beside** `extension_state`, and quote **"rows written"**, never "times evaluated" (`BL-20260826-TARGET-EXTENSION-SOAK-IS-100PCT-SENTINEL-AND-CANNOT-YET-OBSERVE-THE-LEVER`). | 1 | **open — unblocked** |
| B6 | **The 8 remaining `passed_unshipped` cells.** ⚠️ **NOT independent of B4 any more, which is why the operator deferred it 2026-08-29:** two of them are **trail3 on `tlt_pullback_1h` and `uso_trend_1h`** — both legs whose `atr_stop_mult` B4 just changed underneath them (2.5 → 2.0). That trail was validated at the OLD stop, so shipping it would apply a validated-at-2.5 trail on top of a now-2.0 stop and the walk-forward would not transfer. **Re-sweep those two at the new stop before shipping them.** The other six touch legs B4 did not change and carry no such interaction: `qqq_trend_long_1d` vol_trail · `mhg_pullback_1d` stale12 · `ict_scalp_sol_15m` exit_ladder · `trend_donchian_eth_prop` exit_head_ml · the shadow-fleet stale_stop + trail_decay batch. ⚠️ **`trend_donchian_eth_prop` is NOT free of interaction either, discovered 2026-08-30 and NOT via this row** — the e35 ship held that leg's bracket cell back as `blocked:prop_ev_gate_would_be_invalidated`, and the reason generalises to **any** exit change on it, `exit_head_ml` included: the leg reached `execution: live` on a prop EV/survival gate (`run_ev_montecarlo` under `breakout.yaml`, relay #8975) measured on its CURRENT construction, and `breakout_1` is the one account a drawdown breach can permanently disable. Re-run that gate before shipping its cell. | 3 | open — **split it** |
| B7 | **The 20 `pending` cells** — not yet processed. Lower priority than shipping what is already validated. | 1 | open |
| B8 | ⚠️ **The matrix's evidence has no durable path** — 3,781 cells live inside expired CI artifacts (`BL-20260823-E35-SWEEP-EVIDENCE-HAS-NO-DURABLE-PATH`). This is the same root as Lane T and should be fixed there, not per-sweep. | 1 | open |
| **B9** | ✅ **MEASURED AND ANSWERED 2026-08-29** — `docs/research/timeout-bars-harness-vs-live-2026-08-29.md`; reproduce with `scripts/research/timeout_binding_audit.py`. **The code half stands**: the trend/pullback harnesses force-close at `min(entry_i + timeout_bars, n-1)` (default **200**; squeeze **48**) and no live unit for those families implements a bar-count exit, so live's effective timeout is **infinite**. **The blast radius this row said was "NOT established" is now established.** Population: `e35-bracket-corpus.jsonl`, 41 legs, **1,588 graded base-vs-`to400` geometry pairs** (11 `inert_equals_base` rows EXCLUDED, never zeroed). The default **BINDS on 439/1,588 pairs (27.6%) and on 18 of 41 legs**; 23 legs are CLEAN (it is provably inert there, so their base arm *is* live-parity) and **0** grade `no_power`. ⚠️ **This REFUTES the premise in `e35_bracket_geometry_sweep.py:117-120`** that the default is *"far outside the binding region"* — that generalised ONE leg's census off `exit_reason`, which is the loop's DEFAULT label and so conflates a bar-count exit with running off the end of the data. ✅ **All 8 legs B4 shipped to real money are CLEAN and their shipped cells carry no timeout component — #10419 is not affected**, verified by re-deriving each cell from the corpus and matching the inline annotation in `config/strategies.yaml` on all 8. ⚠️ **This row's own "4–6 cells" estimate was wrong in both directions**: `spy_pullback_1h` is not blocked (its `sm1.5_to400` ties the deliverable `sm1.5` at wf 5/6), three more are only *partly* blocked (best cell undeliverable, weaker one stands), and **five legs were missed** — `ada_pullback_2h`, `sol_pullback_2h`, `trend_donchian_1h`, `trend_donchian_sol_4h`, and **`squeeze_breakout_4h`, a third family this row's trend+pullback scope does not cover at all**. **10 of 27** legs' best validated cell is undeliverable; **exactly 2 matrix cells** move `passed_unshipped` → `blocked:no_live_bar_count_exit` (`mes_trend_long_1d`, `tlt_pullback_1d` — the only two where *every* passing cell prescribes a timeout). **RECOMMENDATION (the order-path decision this row asked for): the HARNESS should stop modelling it; live should NOT gain a time stop on this evidence** — the base arm is meant to be live-parity and on 18 legs is not, the fix is a research default rather than a Tier-3 order-path change, and its blast radius is bounded because it is a provable no-op on the 23 CLEAN legs. ⚠️ **But the timeout evidence is not worthless and part of it is uncontaminated**: 4 legs are CLEAN *and* blocked (`mes_trend_long_1d`, `tlt_pullback_1d`, `gld_pullback_1h`, `eth_pullback_2h`) — their base arm WAS live-parity and a shorter hold still beat it at the gate (`gld_pullback_1h` `tp6_sm1.5_to24` walks forward **6/6**). That is the honest case for a **separate, properly-scoped Tier-3 time-stop proposal**, not a reason to fold one into this. ⚠️ **Do NOT simply raise the default and re-grade**: re-running the 18 contaminated legs re-measures live configuration, several on `shipped` cells, and belongs behind the same gate as any other re-measurement. | 2/3 | ✅ **ANSWERED — Tier-3 proposal left OPEN for the operator** |
| **B10** | ⚠️ **`bracket_geometry` has NO staleness detector — NEW, found while doing B9.** `matrix-config-agreement` covers exactly **4** levers (`m20_fleet_exit_sweep.LEVER_DECLARED_KEYS`), so it was **green** while the matrix carried 14 `bracket_geometry` cells as `passed_unshipped` with **8 already shipped to real money** that morning. The exclusion is structurally right — `_arms()` tests key PRESENCE and every leg always declares `tp_r`/`atr_stop_mult`, so including it would demand `shipped` everywhere — but the column is undetected. The right detector is **value** agreement: the cell id encodes the values (`tp3_sm2` → `tp_r=3.0`, `atr_stop_mult=2.0`). The 8 stale cells were corrected by hand. ✅ **BUILT AND SHIPPED 2026-08-29 — `scripts/ci/check_matrix_bracket_values.py`, [#10437](https://github.com/benbaichmankass/Metis-Insights/pull/10437), merged `f4e750ab`**, registered in `run_guards.py` as **`matrix-bracket-values`** with a `--self-test` step. ⚠️ **This row said "the detector is not built" until 2026-08-30, five sessions after it was built** — stale in the dangerous direction, since the instruction a reader takes from it is to build a guard that already exists (`RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED`; on 2026-08-26 that same shape DESTROYED an existing, registered, working guard of the name being written). It has run green on every bracket-geometry change since, most recently **17 shipped cells** after [#10450](https://github.com/benbaichmankass/Metis-Insights/pull/10450). ⚠️ Its known limit is filed, not fixed: it reads only the **backticked** cell spelling (`BL-20260829-MATRIX-BRACKET-VALUES-READS-ONLY-THE-BACKTICKED-CELL-SPELLING`), so a bold-spelled cell id is unchecked. | 1 | ✅ **done** |
| **B11** | ⚠️ **A FALSE PREMISE in `check_harness_lever_coupling.py`, corrected in comment only.** Its `_TREND_UNMODELLED`/`_PB_UNMODELLED` registries list `timeout_bars` under a comment implying only the squeeze harness models it. **All three parsers carry `--timeout-bars`** (`backtest_trend.py:982`, `backtest_pullback.py:961`, `backtest_squeeze.py:545`). ⚠️ **The obvious fix is the wrong direction**: promoting the key to `_TREND_LEVER_FLAG`/`_PB_LEVER_FLAG` would stop `regime_debt_matrix` naming it in `omitted_levers` and **upgrade `mgc_pullback_1d`/`mhg_pullback_1d` from `approximate` to full fidelity for a key the live unit never reads** — those two legs declare `timeout_bars: 200` in config decoratively, with no reader. The entry stays; only its stated reason is corrected. `diagnostic-provenance` class: a defensible outcome via a false premise, load-bearing in the safe direction. | 1 | ✅ done (comment) |

---

## ⚖️ OPERATOR DECISIONS ON B9 — taken 2026-08-29, in-conversation. DO NOT RE-LITIGATE.

Marked **DECIDED** per `CLAUDE-RULES-CANONICAL` § "MEASURED / INFERRED / DECIDED":
a decision carries its date and what would reverse it, so it does not become folklore.

| # | DECIDED | reversal condition |
|---|---|---|
| **D1** | **The HARNESS stops modelling a bar-count exit; live does NOT gain one.** Scoped to the **e35 sweep's base arm only** — `e35_bracket_geometry_sweep.py` pins `--timeout-bars` to `NO_BAR_COUNT_EXIT`; **every harness argparse default is UNCHANGED.** | A walk-forward showing a time stop is worth having in production would reopen the *live* half (D1 does not decide that — see D1b). Reverse the sweep pin by deleting the `cell_args(..., NO_BAR_COUNT_EXIT)` wrap at the base-arm call site. |
| **D1b** | **The Tier-3 question — should live gain a bar-count exit — is NOT decided.** It stays open with 4 uncontaminated legs behind it (`mes_trend_long_1d`, `tlt_pullback_1d`, `gld_pullback_1h`, `eth_pullback_2h` are CLEAN *and* blocked; `gld_pullback_1h` `tp6_sm1.5_to24` walks forward 6/6). | Needs its own scoped proposal + walk-forward + operator approval. Not unlocked by D1. |
| **D2** | **Re-run the 18 CONTAMINATED legs, then DIFF old-vs-new verdicts and bring the diff for review BEFORE any config moves.** | The re-run itself is Tier-1; applying any resulting verdict change to `config/strategies.yaml` is Tier-3 and separately gated. |
| **D3** | **The two dead `timeout_bars: 200` config keys are DELETED** (`mgc_pullback_1d`, `mhg_pullback_1d`). | Re-add only alongside a real consumer. A key with no reader in `config/strategies.yaml` is indistinguishable from a live trading parameter. |
| **D4** | **Next priority is P1/P2 (the promotion gate)** — ahead of B6, B7 and B10. | Superseded by a later operator priority call. |

### ⚠️ D1's original framing was WRONG and was corrected before anything shipped

It was first put to the operator as *"flip the trend/pullback argparse default; blast radius
bounded to the 18 contaminated legs"*. **That bound holds only inside the e35 sweep.**
Verified 2026-08-29: **9 other entry points invoke those harnesses without passing
`--timeout-bars`** and so inherit the default —

`scripts/ml/strategy_tune_sweep.py` (M8, behind the live `/api/bot/strategies/{name}/tune`) ·
`scripts/ml/exit_head_replay.py` · `scripts/ml/m23_phase1_experiment.sh` ·
`scripts/research/m20_trail_resweep.py` (grades a **LIVE** lever) ·
`scripts/research/trend_harness_divergence.py` · `scripts/research/build_continuous_contract.py` ·
`scripts/ops/recombination_sweep.py` · `scripts/ops/m15_phase0_sweep.sh` · `scripts/ops/m15_ws_c_kfold.sh`

The B9 measurement covered the **e35 corpus only**, so those 9 are **unmeasured on this
axis** — that gap is unchanged and still open. The decision was re-put and narrowed to the
one consumer. ⚠️ **The fix does NOT go in `fleet.base_args`** — that helper is shared by
`m20_fleet_exit_sweep`, `regime_matrix`, `m20_exit_head_round`, `m21_entry_sweep` and
`m20_flip_replay_sweep`, so changing it would re-widen exactly the radius D1 narrowed.

⚠️ **The e35 pin covers SQUEEZE too, which extends D1's letter.** `squeeze_breakout_4h` is
one of the 41 corpus legs, measures CONTAMINATED, has no live bar-count reader, and its
harness default is 48. Pinning two families of three would leave one leg of a single corpus
on a different convention. Flagged, not buried; one line to exclude.

⚠️ **A near-miss worth keeping.** Mid-implementation `fleet.base_args` was found to contain
`("--timeout-bars", "timeout_bars")`, which looked like proof D3's keys were live. They are
in the **squeeze** and **fvg** branches only; trend/pullback never emit the flag — proven by
calling `base_args` on both legs with and without the key (flag absent either way, all other
args byte-identical). *"I found a reader, therefore the key is read"* was the wrong
inference, and one more grep settled it.

---

## Lane P — Promotion pipeline (carried forward; blocks every real-money graduation)

⚠️ **P1 WAS ALREADY DONE WHEN THIS ROW WAS WRITTEN, AND THE ROW SAID OTHERWISE
FOR THREE DAYS.** The line above used to read *"Unchanged from 08-26/08-27 and
not re-verified this session"* — and "not re-verified" is exactly how a
completed item keeps being re-listed. **Recommended order** put P1 at position
3, ahead of B6/B5, so the next session to follow this plan would have spent a
unit rediscovering a merged fix. Corrected 2026-08-30 by reading the code
before claiming the work; field beats comment.

| # | item | tier | status |
|---|---|---|---|
| P1 | Compat-matrix STANDARD arm | 1 | ✅ **DONE — both halves, and now OBSERVED working.** The drawdown model shipped in **#10364** (08-27): `_standard_ruleset` builds `drawdown_type="intraday_high"` + an explicit `drawdown_breach="refusal"`, and the synthetic `$10,000` default is gone (nullable `account_size_usd` + a `gradeable` property every consumer must check). Its residual — `BL-20260829-GLD-COMPAT-SUMMARY-CALLS-UNGRADED-A-REJECTION-AND-ITS-STANDARD-ARM-IS-INERT-ON-A-RUNNER`, the summary calling `UNGRADED` a rejection, and the arm being inert on a runner — shipped in **#10393** (`c1f50fc`, 08-29): a dedicated `UNGRADED` branch that says *"this is NOT a rejection … no routing conclusion follows in either direction"*, plus a real balance source. **Verified by exercising it, not by reading it** (run `33306014754`, 2026-08-30, `main`): balances fetched 11 accounts `source=db`, and **9 of 10 standard accounts graded** where all ten were `UNGRADED` on 08-29, with `size=measured`. The one refusal is `ib_live`, whose balance genuinely returned `None`/`api_ok=false` — the arm refuses *discriminatingly*, and no synthetic size came back. |
| P2 | A detector for **"signals, journals nothing"** | 1 | ✅ **SHIPPED AND DIAGNOSED** 2026-08-30 (#10485 `ea6e25a`). Detector: `dead_leg.signal_journal_state_for` + the audit consumer. **CAUSE ESTABLISHED — an ARBITRATION LOSS, not a wiring bug**: `trend_donchian_sol` and `trend_donchian_sol_prop` are the SAME 1h Donchian on SOLUSDT routed to two accounts, and `aggregate_intents` picks one winner per SYMBOL *globally, before account fan-out*. `allocator_soak` 2026-08-29T14:55:37Z: identical entry 105.33 / SL 103.82107143 / conf 0.9941, scores **6.811619 vs 5.906347**, `executed: trend_donchian_sol_prop`, `agree: false`. Two causes REFUTED, so nobody re-tests them: the leg IS loaded with an intent builder and SOLUSDT scope; and `regime_hard_gate`/`regime_shadow_gate` on SOLUSDT return **zero** rows over three days. **The remedy is P3 and is NOT done — the leg is still signalling into a void.** Follow-ups: the live alert is unbuilt (`BL-20260830-SIGNAL-JOURNAL-AXIS-HAS-NO-LIVE-ALERT-ONLY-AN-OFFLINE-AUDIT`) and the audit-side twin is fixed in #10495 (`BL-20260830-PIPELINE-RESULT-REPORTS-NO-SIGNAL-WHEN-TWO-LEGS-SIGNALLED-AND-LOST-ARBITRATION`). |
| P3 | Decide the arbitration scope — `aggregate_intents` picks ONE winner per SYMBOL globally, before account fan-out | 3 | 🟡 **MEASUREMENT SHIPPED AT `annotate`; THE REMEDY IS STILL NOT BUILT AND THE LEG IS STILL SILENT.** `arbitration_fanout.py` (pure) + its soak landed 2026-08-30 behind `ARBITRATION_FANOUT_MODE=annotate`, wired observe-only beside the M18 allocator soak, with its diag allowlist entry in the same commit. Read it at `/api/diag/log_file?name=arbitration_fanout_soak`. ⚠️ It measures **starvation** (which accounts held a candidate and did not get the winner), NOT which candidate each account would elect — electing one means re-running `aggregate_intents` per account, which re-enters `_hard_regime_gate` and would re-emit a `regime_hard_gate` row per account per tick, corrupting the would-have-gated/did-gate partition. So a `starved` row does NOT mean 'this account would have traded'. ⚠️ `apply` is **NOT implemented** (refused back to `annotate`, `apply_implemented: false` on every row); the real fan-out is a Tier-3 change to `aggregate_intents`' call SCOPE, gated on this soak's evidence + operator approval. ⚠️ `ARBITRATION_FANOUT_ACCOUNTS` EMPTY MEANS **NONE** — the opposite of `CONVICTION_SIZING_ACCOUNTS`/`NETTING_ATTRIBUTION_ACCOUNTS`, because this arms an order path. **THE DIRECTION CHOSEN, AND WHAT IS LEFT:** Operator chose **fan out per account** 2026-08-30. Blast radius MEASURED fleet-wide (`allocator_soak`, n=277): **113 of 137 disagreements (82.5%) are same-strategy-two-account collisions** — ETH donchian 56, SOL donchian 34, ETH pullback 23 — vs 24 genuine cross-strategy contests; twin rows carry higher mean regret (0.7664 vs 0.694923). Fanning out removes that 82.5% **by construction**. ⚠️ Account-**BLIND**, not prop-biased — prop wins both donchian pairs and LOSES ETH pullback; do not describe it as a funding-class rule. ⚠️ **Do NOT de-route either SOL leg**: the live scorer AND R2's backtest both say the `bybit_1` twin is the BETTER one, so the intuitive fix removes the wrong twin. MUST ship behind a `*_MODE` gate defaulting to `annotate` with an accounts allowlist whose EMPTY value means **NONE** (the `PROTECTION_REASSERT_ACCOUNTS` polarity — this arms an order path); the flip to `apply` stays operator-gated. |

⚠️ **P2 SHIPPED THE DETECTOR, NOT THE REMEDY.** `trend_donchian_sol` is still
signalling into a void right now. Why it journals nothing on `bybit_1` is
**undiagnosed** and is the actual Lane P follow-up.

⚠️ **A note for Lane A:** P1 is the standard-arm fix, and the alpaca compat verdicts
this session's predecessor produced came from that same arm. They were graded
`ROUTE`/`skip` on mean end-return, which is not affected by the `drawdown_type`
defect, but a future session widening the alpaca roster on compat evidence should
close P1 first.

---

## Lane C — Order-path correctness (carried forward, unverified this session)

| # | item | tier |
|---|---|---|
| C1 | Partial leg-id capture by CONTENT not novelty; the abstain must survive a genuine tie. Stage on `bybit_1`. | 2 |
| C2 | Flat-symbol Bybit position-mode read — demoted to nice-to-have | 1 |

---

## Lane R — Prop account (carried forward; one item re-measured today)

| # | item | tier | status |
|---|---|---|---|
| R1 | `breakout_1` cushion. **Re-measured 2026-08-29:** a SOLUSD long 49.00 @ 105.04 with a placed stop at 103.00 risks **$99.96** against a bot-confirmed **$55.00** to the $4,700 static-DD floor (`/api/bot/prop/status` `distance_to_dd_floor_usd: 55.0`). Reported to the operator, who **stood it down** — recorded, not re-raised. ⚠️ The daily-loss distance is `null` because day P&L was not reported: that limit is **unknown**, not clear. | — | operator-decided |
| R2 | The SOL twin is inverted — `trend_donchian_sol` EV +$1,162 / P 0.9137 vs the routed `trend_donchian_sol_prop` +$611 / 0.7603. ✅ **MECHANISM FOUND 2026-08-30 — R2 and `BL-20260830-TREND-DONCHIAN-SOL-SIGNALS-144-TIMES-AND-JOURNALS-NOTHING-ON-BYBIT-1` are the SAME defect from two ends, and must not be worked twice.** R2 observed the outcome; the cause is that `aggregate_intents` picks ONE winner per SYMBOL **globally, before account fan-out**, so the two legs — identical 1h Donchian on SOLUSDT, one routed to `bybit_1`, one to `breakout_1` — collide and the prop twin wins. Measured in `allocator_soak` 2026-08-29T14:55:37Z: identical entry 105.33 / SL 103.82107143 / confidence 0.9941, scores **6.811619 (sol) vs 5.906347 (sol_prop)**, `executed_strategy_id: trend_donchian_sol_prop`, `agree: false`. **The system's own scorer prefers the leg it does not route**, on 42 of 47 SOLUSDT ticks (89.4%). ⚠️ Do NOT de-route either leg on this evidence — the naive fix removes the *better* twin. The remedy is **P3** (arbitration scope), Tier-3. | 3 | open — cause known, remedy is P3 |
| R3 | No ETH-pullback leg emits a prop ticket at all | 3 | open |
| R4 | BTC is an unopened prop lane — gate a BTC leg after P1 | 3 | open |
| R5 | `breakout_routing.yaml::strategies` lists two de-routed legs and has no reader — decorative and stale | 1 | open, low |

---

## Lanes D / S / T / E — carried forward unchanged

Not re-verified this session; see the 08-26 plan for evidence.

- **D** — M16/M18: park on evidence, do not quietly continue. D2 is a **standing constraint**: do not build a third arbiter on inputs already measured negative.
- **S** — Roadmap legibility. S1 (give `IN PROGRESS` a meaning), S2 (move the 6,820-word M20 body out of its table cell), S3 (M6/M9/M10 carry no date at all).
- **T** — Trainer continuous/queued/monitorable. T1 disk (91.2% used; the dataset GC is **not** the remedy), T2 the standing queue design, T3 observability, T4 one standardized entry point. **T is the root of B8.**
- **E** — Operator-owed: `OO-20260825-BREAKOUT-1-BALANCE-REPORT` open and material (the manual bridge has no broker feed, so it is genuinely human).

---

## Recommended order

**Re-ordered 2026-08-29 after B4 shipped.**

1. **Lane A** on Monday, in its decided sequence — the only item with a hard
   calendar dependency, and automated up to A2. Everything else can wait; this
   cannot.
2. ~~**B9**~~ ✅ **ANSWERED 2026-08-29** — the matrix's `passed_unshipped` column no
   longer conflates "awaiting approval" with "impossible to ship": 2 cells moved to
   `blocked:no_live_bar_count_exit`, 8 stale cells moved to `shipped`, and every
   affected cell carries a `timeout_binding` note. **What remains open is the Tier-3
   half** — whether live should gain a bar-count exit — which is now a scoped
   proposal with 4 uncontaminated legs behind it, not a blocker on the matrix.
3. **P3 — the per-account arbitration fan-out.** Direction chosen by the
   operator 2026-08-30 and the blast radius is measured (82.5% of allocator
   disagreements are the artifact it removes). **This is now the highest-value
   open item in the plan**: it is the live cause of a leg that has signalled 144
   times and journalled nothing, and it re-bases M18's evidence. Tier-3, ships
   at `annotate` first. ~~**P1/P2**~~ ✅ **BOTH LANDED 2026-08-30.** P1 was already complete when
   this list was written (see the lane table); P2's detector shipped. What is
   left of Lane P is **P3** (Tier-3) plus the two follow-ups the P2 row names:
   the live alert, and diagnosing why `trend_donchian_sol` journals nothing.
4. **B6, split** — ship the six cells with no B4 interaction; re-sweep the two
   trail3 cells (`tlt_pullback_1h`, `uso_trend_1h`) at their new stop first.
   ⚠️ **B9 adds a precondition B6 did not have**: check each candidate leg's
   timeout status before shipping it. `tlt_pullback_1h` and `uso_trend_1h` are both
   CLEAN, so the re-sweep is the only interaction there — but `mhg_pullback_1d`
   (stale12) is CLEAN while `ict_scalp_sol_15m` and the shadow-fleet batch are
   **not in the e35 corpus at all** and so are ungraded on this axis.
5. **B5** — re-read the extension soak once a real-target leg has had live time
   to approach its target.
6. **T1** — trainer disk, before it forces itself.

**Not recommended:** B7 (pending cells) or new research frontiers while B9 leaves
the matrix's `passed_unshipped` column ambiguous. Adding more validated-but-
unshipped cells to a column that already conflates "awaiting approval" with
"impossible to ship" makes the loop Lane S exists to break worse, not better.

---

## Standing constraints carried forward

- Research-queue dispatcher **stays dry** until a session reads one real dry-run report.
- The offloaded model **stays at `candidate`**; a runner-trained model joining the live shadow fleet is Tier-2 and needs an explicit operator OK.
- The trainer VM is **not retired** — R6 verdict: `ict-orderflow-capture.service` needs 24/7 residency; the capture is forward-only and unbackfillable.
- **USO** is excluded from real money on non-financial grounds. Standing, survives any backtest result.
