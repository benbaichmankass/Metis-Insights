# Work plan — measure the instruments before trusting what they read

> **Status of this file.** Written 2026-08-26 on operator direction, replacing
> [`WORKPLAN-2026-08-21.md`](WORKPLAN-2026-08-21.md) (closed to a system audit).
> It is **not** a source of truth and outranks nothing: the hierarchy in
> [`docs/CLAUDE-RULES-CANONICAL.md`](../CLAUDE-RULES-CANONICAL.md) § "Document
> Priority" governs, and a skill or a Tier-3 gate beats this file every time.
>
> **It is a LIVING file.** Each session: read it, take the next unblocked item,
> update the Status column and the Session log **in the same PR**.

## Why this replaces the 08-21 plan

Operator directive, 2026-08-26: *"I wanna make sure that we have a coherent work
plan based on what actually exists, not what we think exists or what was supposed
to exist, but doesn't."*

The 08-21 plan was rewritten around this table:

| what | it said |
|---|---|
| closes via a **declared bracket** | **30 of 369 — 8.1%** |
| closes `reconciler_filled` | 148 — 40% |

⚠️ **THAT TABLE IS COMPUTED FROM `exit_reason`, AND `exit_reason` IS WRONG ABOUT
EXACTLY THIS.** Measured 2026-08-26 on `ict_scalp_5m` / `bybit_2` / closed rows
with exit_price + stop_loss + take_profit_1 present, n=28: **22 of 28 (78.6%)
exited within 0.05% of price of a declared bracket level**, and **11 of the 15
rows labelled `reconciler_filled` were among them** (4 at the stop, 7 at the
target). Stable across a 0.02–0.50% tolerance sweep (20/22/22/24/26/28), so it is
not a cutoff artifact.

The brackets fire. The *attribution* goes to the reconciler. So a plan built on
"the declared exits are not happening" was built on a label, not a measurement —
and it pointed work at an exit path that largely works.

**This is not a small correction to one number.** `/api/bot/performance` publishes
`perExitPath` specifically to cut coverage *by exit path*; `CLAUDE.md` records six
paths at 0.0 coverage read off this same field; and the structural-health reading
that *"64.7% of closes come from cleanup machinery"* is a claim about **mechanism**
sourced from a label now shown wrong for the majority of rows it is applied to.
Every one of those inherits the defect.

**Hence this plan's ordering principle: fix the instruments before acting on what
they read.** GATE 0 below is not overhead — it is the precondition for every other
lane being scored honestly, and the operator has since elevated it to the number-one
blocker in its own words.

---

## What is measured true, as of 2026-08-26

Each row states its population. Anything not listed here is not established.

| # | fact | population |
|---|---|---|
| 1 | `exit_reason` files fired brackets as reconciler closes — 22/28 land on a declared level. ⚠️ **Corroboration, not discovery** — measured 2026-08-22 at 91/155 on broker-truth rows, cause already named (see G1) | `ict_scalp_5m`/`bybit_2` closed, prices present, n=28 |
| 2 | Journal labels are **25.6% MEASURED**; win rate moves **9 points** with provenance (ESTIMATED 48.2% vs MEASURED 39.3%) | closed non-backtest, pnl NOT NULL, joined to a package, n=804 |
| 3 | **Selection is not learnable from any input tried** — confidence 0.522, ev_r 0.484, rr 0.469, ret_1h 0.487, hour_utc 0.508, +regime 0.5110, +cell 0.5077, all vs a pre-registered 0.55 gate | M18 expanding walk-forward, n_eval=1,236 |
| 4 | Partial leg-id capture is a racy snapshot diff — coverage falls with account busyness | filled rows since 2026-07-21: `bybit_2` 38/41 = 92.7% (6 strategies), `bybit_1` 272/333 = 81.7% (26) |
| 5 | 64% of `bybit_1`'s untracked legs are the **pairs sleeve**; ETHUSDT is the top untracked symbol | the 61 untracked rows above |
| 6 | Bybit **position mode** is now readable — but not for a **flat** symbol, which is the state a switch happens in | `_bybit_position_row`, both read paths skip `size <= 0` |
| 7 | **Hedge mode is not needed.** Partial mode covers same-direction overlap; the 4 opposite-direction cases are 2 pre-policy and 2 involving a since-demoted strategy | `bybit_2`/BTCUSDT cross-strategy overlaps, n=55 closed, 10 overlapping pairs |
| 8 | The BTC scalp is **flat, not bleeding** — its clean exclusive book is +$1.43/25 | `ict_scalp_5m`/`bybit_2`, trades no sibling could have moved |
| 9 | Fees are a real drag: **89% of BTC gross** over the trailing two weeks | `bybit_2` BTC exchange fills, 90d |
| 10 | `bybit_2` real balance **$296.59**, −9.3% off its 07-05 peak, ~flat since 07-01 | `balance_snapshots`, n=2,986, full history |

### The operator's inference, and where it lands

> *"If we don't have an indicator for when a policy should flip, what I'd infer is
> that the confidence score is bullshit, and we don't have a good predictor for
> the other things either."*

**Correct, and now measured rather than inferred.** Fact 3 is not about flipping —
it is about `confidence` as a quantity. It sits at **0.522 OOS** against a 0.55
gate, on n_eval=1,236, in an expanding walk-forward. That is the same scalar
**M16 (Unified Confidence Risk Architecture)** is built on and **M18** ranks with.

⚠️ **Do not over-read it either.** 0.522 is *not* zero and the measurement is
about **cross-candidate SELECTION** on this candidate set. It does not establish
that confidence is useless for sizing, for gating, or within a single strategy.
The honest statement is: *confidence has been measured for the one job two
milestones assigned it, and it cannot do that job.*

---

## GATE 0 — Trustworthy measurement. Everything else is blocked on this.

**Operator directive, 2026-08-26** (elevating this above every build item):

> *"It seems that we have a serious problem with the labeling and measuring and
> correctly reading information — the amount of mistakes that we keep going
> through over and over again just to find the correct data that we need in order
> to make decisions gives me very low confidence in the work that we're doing
> here. So we need to make sure that closing that gap is priority number one of
> the blockers for getting to the things that we actually need to be building."*

This is not a lane. **It is a gate, and no Lane B/C/D item ships before its
mechanical half is in place.** The justification is a rate, not a vibe: **seven
corrections in one session, every one the same class** — a stored field read as
the quantity it is named after — and in three cases the deciding value was **in
the row already being read**. Two more came from quoting a document section that
the same document retracts further down. The class row is
`BL-20260826-FOUR-WRONG-CONCLUSIONS-IN-ONE-SESSION-FROM-READING-A-STORED-LABEL-AS-THE-MEASUREMENT`.

⚠️ **"Always verify" was already canonical and in force for all seven.** It is
`RULE ONE`, it sits at the top of `CLAUDE.md`, and it prevented none of them —
because each read *felt* like verification. A journal query IS a measurement, of
the journal. **So this gate must be MECHANICAL. An eighth restatement of the rule
is not a remedy and must not be filed as one.**

| # | item | tier | status |
|---|---|---|---|
| **G1** | **Finish the exit-label re-classification.** ⚠️ **This is NOT new work and must not be re-scoped as if it were.** The defect was measured 2026-08-22, the mechanism is named, and one half is already fixed: `_sweep_pending_pnl_from_bybit` fills `exit_price` after the close and **never re-runs `_classify_broker_exit`**, so the label stays frozen at `reconciler_filled` — 91 of 155 broker-truth rows, with 181/181 mislabelled rows carrying no `exit_reason_source` as a 100% signature. The anchored-price sibling was repaired (`BL-20260823-EXIT-LABEL-FROZEN-ON-THE-ANCHORED-PRICE-PATH`, resolved); **the Bybit-sweep path is still open**. `exit_reason_source` already exists and is stamped at six sites in `order_monitor.py`. Remaining: re-classify on that path, then **re-derive `perExitPath`** — its coverage is computed over mislabelled buckets today. | 1–2 | ✅ **DONE 2026-08-26 — code SHIPPED (#10341) and the Tier-2 backfill APPLIED + live-verified.** ⚠️ This row's premise was already wrong when written — see the G1 correction below the table. `BL-20260822-EXIT-REASON-FROZEN-WHEN-PRICE-ARRIVES-LATE` + `BL-20260822-EXIT-ATTRIBUTION-UNDER-REPORTS-BRACKET-HITS` |
| **G2** | **Make an untrustworthy account's journal read announce itself.** `comms/broker_truth_ledger.json` ALREADY records which accounts under-record (`bybit_2`: −$262.52 wallet-truth vs ~−$33 journal). Nothing warns a reader querying those accounts' rows. Surface the discrepancy on the per-account read path (`/api/bot/trades/closed`, the Data Explorer). **This one would have caught mechanically the worst error of the session** — reporting `bybit_2` as "+$0.88, flat, no problem". | 1 | ✅ **SHIPPED 2026-08-26** (PR #10339) — `journalTrust` per row on `/api/bot/trades/closed` + an envelope block on `/api/bot/performance`, scoped to the accounts in the window. Three states, `no_record` explicitly NOT "trusted". ⚠️ **Ledger coverage is ONE account**, so the flag is only as good as what has been reconciled by hand — tracked as `OI-20260826-JOURNAL-TRUST-COVERS-ONE-ACCOUNT`. Writing the tests found `unreadable` was UNREACHABLE (the same collapse one layer down) and fixed it. |
| **G3** | **Attach provenance to any aggregate built on journal `pnl`.** Labels are 25.6% MEASURED and the win rate moves 9 points with provenance. A consumer receiving a sum or a rate must receive its coverage beside it — the `rCoverage`/`pnlCoverage` discipline, applied to the rest. Backtests are exempt: they never touch journal pnl. | 1 | ✅ **DONE 2026-08-26.** `src/web/api/_pnl_provenance.py` is the ONE owner; **all five** remaining aggregates call it — `/api/bot/stats`, `/api/pnl/history` (per DAY), `/api/bot/strategy/attribution`, `/api/bot/strategies`, `/api/pnl`. Each reuses its own aggregate's WHERE **verbatim**, so graded and summed rows cannot drift; definitions are **imported from `/performance`, not reinvented**. `backtests.py` and `pnl_broker_truth.py` are **exempt** — verified, neither reads journal `pnl`. |

| **G4** | **A guard for the population-less quantitative claim.** `CLAUDE-RULES-CANONICAL` § "Always state the population" is binding prose with no enforcement. Diff-scoped, same family as `diagnostic-provenance-guard`: a number asserted in a doc/report without a stated denominator is a finding. | 1 | ✅ **DONE 2026-08-26** — `scripts/ci/check_stated_population.py`, registered as `stated-population-guard` with `when: None` (always runs) and an 8-case self-test. ⚠️ **This row read `open` while the guard was already shipped and registered** — found by the one-line existence check before building it (`RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED`); the status was stale, the work was not missing. **Verified it can find a positive**, not merely pass: a bare `42.9%` in a watched path fires. ⚠️ **A measured blind spot is recorded and deliberately NOT fixed** — any incidental integer (an unrelated count, a **date**, a **backlog id**) suppresses the check, because the denominator test is "is there a second number". That reads alarming and the intuition is wrong: **population — all 875 watched files, 3,549 lines containing a percentage — exactly 9 (0.25%)** have a date/id as their only denominator, and most of those are legitimate (a year as a data label, `2011 (3.327%)`). Tightening would fire on correct lines, which is the alarm-fatigue failure. Pinned by `tests/test_stated_population_known_limits.py` (12 tests) so the misses stay deliberate. |
| **G5** | **Re-state the 08-21 plan's headline.** "8.1% of closes use a declared bracket" is the premise of the previous plan and is not established. | 1 | ✅ **DONE 2026-08-26** — re-derived on a STATED population (closed, non-backtest, **n=1,363**, ⚠️ *not* the table's unrecorded n=369, so the 8.1% is not reproduced). Declared bracket **2.1% → 16.1%** strict / **10.6% → 24.6%** incl. monitor crossings; **cleanup machinery 63.2% → 49.2%**. The companion "64.7% from cleanup" claim independently reproduces at **63.2% pre-backfill**, so the instrument was right about the SHAPE and wrong about the magnitude. ⚠️ **The concern does not go away** — 49.2% is still half of all closes. Written back into `WORKPLAN-2026-08-21.md` beside the original table. |
| **G6** | **Make the backlog searchable before filing into it.** It holds **949 rows**; no session reads it end-to-end, so a finding gets filed as a discovery when the defect, its mechanism, and half its fix are already recorded. That happened **while writing this very plan** — `BL-20260826-EXIT-REASON-LABELS-A-FIRED-BRACKET-AS-A-RECONCILER-CLOSE` was filed as new and is a duplicate of two 2026-08-22 rows, now re-statused `duplicate`. A keyword/similarity lookup in the `backlog_append` path (which every filing already goes through) that surfaces candidate related rows would have caught it at the moment of filing. ⚠️ The remedy is a LOOKUP, not a rule telling sessions to search harder — the same mechanical-not-exhortative test the rest of this gate is held to. | 1 | ✅ **SHIPPED 2026-08-26** (PR #10339) — `scripts/ops/backlog_search.py`; `backlog_append.append_row` now RAISES `SimilarRowExists` above 0.65 overlap and prints the candidates, with `similar_ok=True` as the deliberate way through (a **recurrence** is a valuable row and no lexical probe can tell it from a duplicate). Verified against the real case: the two duplicated rows score 0.80 / 0.70, the top two hits. Threshold CHOSEN from that one worked example, not tuned. |

### ⚠️ G1 CORRECTION (2026-08-26, session `b9kyip`) — read this before the row above

**The row says *"the Bybit-sweep path is still open"*. It was not, on the day the
row was written.** `_sweep_pending_pnl_from_bybit` has re-run
`_classify_broker_exit` since **#10262, merged 2026-08-25** — one day *earlier*.
Verified with `git log -S` on the marker, not by reading the comment. That
commit also shipped `scripts/ops/backfill_exit_labels.py` and the Tier-2
`backfill-exit-labels` action for the historical half.

So the plan's own number-one blocker was described from a stale reading — the
very class the gate exists to stop, committed by the gate's own charter. State
this plainly: **the work item was not re-scoped, it was re-measured, and most of
it already existed.**

**What was ACTUALLY open, measured on the live journal 2026-08-26** (Caddy diag
host; deployed sha `da5a7d6d` == `main`, `restart_pending: false`; every query
asserted `filter_state: applied`):

| finding | evidence |
|---|---|
| **The backfill tool was INERT and had never run.** `sys.path` walked two levels from `scripts/ops/`, landing on `scripts/`, so `import src` raised `ModuleNotFoundError`. Every sibling backfill uses `parents[2]`; this was the only one that did not. | **0 rows** carry `pre_backfill_exit_reason` and **0** carry `refused_unmeasured_price` — both written only by this tool — three days after it merged. Fail-SAFE (the wrapper aborts on its own self-test) but non-functional. |
| **No test could have caught that**, because the sibling tests load the script through `importlib`, and under pytest the repo root is already on `sys.path`. The test shape differed from production in exactly the broken dimension. | The new subprocess test FAILS on the original line; the three importlib-based branch tests pass either way. |
| **A third, unreadable marker state existed.** `dump_capped` trims the longest *unprotected* string and `exit_reason_source` was not in `_DEFAULT_PROTECTED`, which protects its two siblings. | **2 of the 25 rows carrying a resolved marker (8.0%)** store `"price_vs_p…"` — trades 4961 and 4978, both `_truncated: true`. Matches neither the resolved sentinel nor `unresolved`. |
| **The historical backlog is untouched and forward-only fixes cannot reach it.** | `exit_reason='reconciler_filled'`, **n=593**, all `status='closed'`, of 5,056 rows: 589 gradeable, **562 (95.4%) carry no `exit_reason_source`**. Only **53 rows in the whole table** have ever reached the classifier. |

**Shipped 2026-08-26** (`run_guards.py` PASS 42 · FAIL 0): the marker is
protected; the backfill runs and is tested as the wrapper invokes it; and
`perExitPath` publishes four **label-attestation counts** —
`labelAttested` / `labelRefused` / `labelUnresolved` / `labelUnattested` —
so a bucket whose own KEY is unverified says so. ⚠️ **No ratio is published,
deliberately:** an AUTHORED path (`sl_cross`, `pairs_*`) never reaches the
classifier, so unattested is CORRECT there, and a single rate would imply one
denominator across paths that do not share one.

**STILL OPEN — Tier-2, operator-gated.** The tool has never been applied. A dry
run against a **full replica of the live journal** (5,056 trades / 4,064
order_packages, rebuilt with the real schema types) projects:

```
eligible 481   (scanned 1,122 closed non-backtest non-reduce)
  relabel -> sl   144   (119 measured basis, 25 estimated)
  relabel -> tp    47   ( 32 measured basis, 15 estimated)
  REFUSED         105   fabricated price — a naive backfill would have
                        INVENTED 105 bracket hits from `local_markprice`
  unresolved      185   (105 measured, 80 estimated) — genuine non-bracket closes
ROWS THAT WOULD CHANGE exit_reason: 191
```

An independent provenance tally taken before running the script agreed exactly
(256 measured / 120 estimated classifiable), which is the cross-check that the
projection is not the script grading its own homework.

✅ **THE APPLY RUN LANDED 2026-08-26T11:53:45Z** (operator-approved in-conversation;
`system-action` issue #10342, wrapper exit 0, self-test 12/12). **The live run
reproduced the replica projection EXACTLY** — 191 relabelled (119+25 `sl`, 32+15
`tp`), 105 refused, 185 unresolved, 481 stamped; the only drift was `scanned`
1122→1123 and `skip_no_price` 4→5, one row that closed in between.

**Verified independently against the journal, not the run's own output, and every
figure reconciles by arithmetic:**

| | pre 11:53:04Z | post 11:54:28Z | check |
|---|---|---|---|
| rows carrying `pre_backfill_exit_reason` | 0 | **191** | = rows relabelled |
| rows stamped `refused_unmeasured_price` | 0 | **105** | |
| rows that have reached the classifier | 53 | **534** | 53 + 481 ✓ |
| `exit_reason='reconciler_filled'` | 594 | **403** | 594 − 191 ✓ |
| `exit_reason='sl'` | 21 | **165** | 21 + 144 ✓ |
| `exit_reason='tp'` | 7 | **54** | 7 + 47 ✓ |

**86 rows remain generic + unstamped, and that is CORRECT, not a shortfall** —
`intent_reduce` legs are excluded by guard 1 (their bracket can be INVERTED
relative to the order direction, so classifying one would mislabel it), plus the
5 rows with no price to classify against.

**The consumer half is live too.** On the deployed `/api/bot/performance?window=all`
— population: real-money, `pnl NOT NULL`, so smaller than the table-wide counts
above — `sl` reads **93/93 attested** and `tp` **28/28**, `reconciler_filled` is
**77 trades with 0 unattested** (3 refused, 74 unresolved: every remaining row has
now been LOOKED at), while the AUTHORED paths `vwap_cross` (102), `sl_cross` (53),
`tp_cross` (14), `exit_head` (5) and `backfill_closed_pnl_recovery` (21) are
**100% unattested — which is the correct answer** and is the live confirmation of
why no ratio is published.

**G5 is therefore UNBLOCKED** — the labels its headline is computed from are now
corrected.

**Gate exit condition:** G1 and G2 shipped and verified against live data; G3 and
G4 shipped or explicitly deferred with a recorded reason.

> ## STATUS 2026-08-26 (end of session) — READ THIS BEFORE THE TABLE ABOVE
>
> ✅ **GATE 0 IS CLEARED as of 2026-08-26.** All six planned items shipped —
> G1, G2, G3, G4, G6 — plus a G7 that did not exist when this plan was written,
> and G5, which G1 unblocked and which was re-derived the same day. **Lanes
> B/C/D are unblocked.** The rows below are kept as the record of how each
> landed; the two that read "OPEN" in the first draft are updated in place.
>
> | item | where it stands |
> |---|---|
> | **G1** | ✅ **DONE 2026-08-26** — code shipped (#10341) and the Tier-2 backfill **applied + live-verified** (#10342): 191 relabelled, 105 refused, every figure reconciling by arithmetic against a pre/post journal read. |
> | **G2** | ✅ **SHIPPED, MERGED AND LIVE-VERIFIED 2026-08-26T10:10Z.** Deployed sha `510fbb6a` == main, `restart_pending: false`. **Positive:** `/api/bot/trades/closed?account_id=bybit_2` returns **200/200 rows `known_divergent`**. **Negative control (the half that makes it evidence):** `bybit_1` 50/50, `alpaca_paper` 50/50 and `ib_paper` 16/16 all read `no_record` — so the field DISCRIMINATES and is not hardcoded. **Envelope:** `/api/bot/performance?window=all` → `readState: read`, `accountsKnownDivergent: [bybit_2]`, `accountsUnrecorded: [alpaca_live]`. It is already doing its job on a live number: that same window reports `totalPnl: −$37.89` **while flagging bybit_2**, whose broker truth is −$262.52 — the exact caveat whose absence produced this session's worst error. |
> | **G3** | ✅ **DONE 2026-08-26** — one owner (`_pnl_provenance.py`) wired into all five remaining aggregates. |
> | **G4** | ✅ **SHIPPED 2026-08-26** (operator-approved on the test *"if it's affecting things that are being read or filed before they're actually merged, then that's worth keeping"*). `scripts/ci/check_stated_population.py`, diff-scoped over `docs/` + the four registers. **A denominator is a NUMBER, not a word** — the first draft suppressed on `of` / `across` / `measured` and caught only 2 of 5 real claims from this session; requiring an actual count catches **4 of the 4** that state no population at all, with table rows and `65/147` forms quiet. ⚠️ **It cannot judge a WRONG denominator** — `"71 of 72 past-stop rows are bybit_2"` stays quiet, correctly, because it does state one; that it was the wrong one is the actual skill and no regex has it. This is the cheap half. |
> | **G5** | ✅ **DONE 2026-08-26** — unblocked by G1 and re-derived the same session on a STATED population (n=1,363): declared bracket 2.1% → 16.1% strict, cleanup 63.2% → 49.2%. |
> | **G6** | ✅ SHIPPED + merged. `backlog_append` refuses a row restating an existing one. It earned its keep the same hour: it surfaced `PB-20260807`, stopping a repeat of a documented artifact mistake. |
> | **G7 (new)** | ✅ SHIPPED (PR #10340) — **the session-start channel**. Not in the original plan because the plan assumed CI could carry the gate. It cannot: hooks do NOT run on Claude Code on the web, and CI fires at merge. `CLAUDE.md` is the only surface reaching a session before its first tool call, so the registers render INTO it. |
>
> ### G3 — what it measured, and the gate's exit (2026-08-26, session `b9kyip`)
>
> ⚠️ **Fact 2's 25.6% is population-specific and does NOT describe the headline.**
> Reproduced exactly (0.257 over the package-joined population, n=806), but the
> same classifier over the population `/api/bot/stats` actually reports —
> closed, non-backtest, `pnl NOT NULL`, **real-money**, n=431 — reads **0.768**.
>
> | population | n | MEASURED coverage | `totalPnl` | measured+est |
> |---|---|---|---|---|
> | real-money — what `/stats` headlines | 431 | **0.768** | −$23.22 | **−$45.63** |
> | all accounts incl. paper | 1,187 | 0.425 | +$143,685 | +$219,980 |
> | all accounts, package-joined (fact 2) | 806 | **0.257** | +$147,375 | +$221,790 |
>
> **So the case for coverage on `/stats` is the SUM, not the rate.** `totalPnL`
> nearly doubles (and keeps its sign) restricted to measured rows, while the
> real-money win rate moves **0.1pp** — this plan's "win rate moves 9 points" is
> a contrast between the ESTIMATED and MEASURED *buckets* on a different
> population and must not be carried across to this surface.
>
> **Cost measured, not assumed:** classifying the full closed population through
> `provenance.classify_pnl` costs **19.8 ms median / 434 KiB** — cheap for a
> 30s-polled route, and it reuses the canonical module rather than re-deriving
> the vocabulary in SQL.
>
> **Shipped across all five remaining aggregates** through ONE owner,
> `src/web/api/_pnl_provenance.py`: `/api/bot/stats`, `/api/pnl/history` (per
> DAY), `/api/bot/strategy/attribution`, `/api/bot/strategies`, `/api/pnl`. Each
> reuses its own aggregate's WHERE **verbatim**, so graded and summed rows cannot
> drift. `backtests.py` and `pnl_broker_truth.py` are **exempt** — verified,
> neither reads journal `pnl`.
>
> **Gate exit condition — MET 2026-08-26.** All six items shipped: G1 (code AND
> the Tier-2 backfill applied + live-verified), G2, G3, G4, G6, G7 — plus G5,
> which G1 unblocked and which was re-derived the same session. **Lanes B/C/D
> are unblocked.**
>
> ⚠️ **What "cleared" does and does not mean.** It means the instruments now
> state their own provenance — not that the numbers they report are good. The
> re-derived exit-path split still puts cleanup machinery at **671 of 1,363
> (49.2%)** of closes, and real-money `pnlCoverage` is **0.768** against **0.425**
> across all accounts (n=1,187). The gate was about being able to TRUST what we
> read. Acting on what it now says is Lane B/C/D's job.

## Why deep research before building is the right sequencing

The operator raised this as a possibility; the evidence supports it as the
default. Facts 1–3 are all instrument defects, not strategy defects: the exit
label is wrong, the PnL labels are 74% non-measured, and the one scalar two
milestones are built on has been measured at 0.522 against a 0.55 gate. **Building
on top of that produces confident work whose foundation nobody has checked** —
which is precisely the pattern that cost this session seven corrections and the
08-21 plan its premise.

## Lanes (all blocked on GATE 0's mechanical half)

### Lane B — M20 Active Trade Management (the operator's priority)

⚠️ **Its top blocker is infrastructure, not research.** Per `ROADMAP.md`, **25 of
the 69 remaining M20 cells are `no_free_lane_candle_feed`** — 36% of the remainder
and 51% of everything blocked — and all 25 are non-crypto while the free lane is a
crypto archive. Source decided (yfinance), to be proven **on a runner** because
Yahoo 429s from the sandbox. The same feed closes M31 P5 precondition 3b.

> ### ⚠️ B1 CORRECTION (2026-08-26) — the row above is stale; the feed already exists
>
> **B1 is not "build a feed". It is "dispatch a workflow that has been sitting
> there".** The non-crypto lane was proven on a runner on **2026-08-24**
> (`S-BRACKET-EXPECTATIONS-AND-CANDLE-FEED-2026-08-24`: daily 251 rows / 364 d of
> 365; intraday 3457 rows / 727 d of 1000), and `e35-bracket-sweep.yml` has
> per-leg fetching wired to it. Measured this session by running the sweep's
> **own** planner (`e35_shard_plan.py --ignore-missing-data`), not inferred from
> prose.
>
> **Population: the 25 cells recorded `blocked:no_free_lane_candle_feed` in
> `docs/research/exit-refinement-coverage.json` (`updated_at` 2026-08-24T15:55Z).
> All 25 sit in ONE lever column — `bracket_geometry` — across 25 distinct legs.**
>
> | | before this session | after |
> |---|---|---|
> | schedulable by the planner | **21** of 25 | **22** of 25 |
> | …resolve after the workflow's own fetch | 17 | **22** |
> | …**fetch-then-fail** (green job, zero legs measured) | **4** | **0** |
>
> ⚠️ **The 4 were failing GREEN**, which is why nobody had noticed: the workflow
> wrote `data/{SYMBOL}_{tf}.csv` while `resolve_data` applies `PROXY_DATA`
> (`MES→ES_F`, `MGC`/`XAUUSD→GC_F`) and looks only for the proxy stem, so the leg
> paid the fetch, read `data_missing`, and the sweep **exited 0 with `legs: []`**.
> The rollup then counted that empty report as a leg that had returned one.
> Fixed + backlogged as
> `BL-20260826-E35-RUNNER-WRITES-A-FILENAME-THE-SWEEP-NEVER-READS`; `MHG` joined
> the servable set as a consequence of routing `PROXY_DATA` symbols to yfinance.
>
> ⚠️ **`QLD`/`TQQQ` stay refused and that is CORRECT, not a gap** — a daily
> leverage reset means the path is not N × the underlying, so no proxy is honest.
> `ict_scalp_mgc_15m` is out of scope for this sweep's family filter. So the
> reachable ceiling on this lever is **22 of 25**, not 25.
>
> ⚠️ **THE CELLS ARE STILL BLOCKED.** Nothing here measured a bracket. The
> matrix must not be flipped until the sweep is dispatched and returns verdicts —
> a code change that marks cells resolved would assert a measurement nobody took.

| # | item | tier | status |
|---|---|---|---|
| B1 | ~~Stand up the non-crypto candle feed on a runner.~~ **Superseded — see the B1 CORRECTION above.** The lane exists and was proven 2026-08-24; the real gap was a filename mismatch that made 4 legs fail green, now fixed. | 1 | ✅ **DONE 2026-08-26** — fix #10344, verdicts #10345, corpus recovery + gate fix #10346. Run `32975514836`: **25/25 jobs green**, 22 legs measured, all 5 `PROXY_DATA` legs routed to yfinance under the proxy stem. **22 coverage cells closed on the gate's own verdicts — 12 `passed_unshipped`, 10 `honest_negative`.** ⚠️ **Quote the right cut:** the headline does NOT move (it counts `blocked` as closed); validated **85.1% → 89.9%**, resolved 81.8% → 83.8%, **+19 not +22** (the population is 44 *live* legs). The 3 still-blocked cells are the correctly-unservable ones, so the 22-of-25 ceiling held. |
| B2 | **Why do the M20 levers fire 1.3%?** Determine whether that is correct-and-rare or a wiring gap. | 1 | ✅ **ANSWERED 2026-08-26 — NOT a wiring gap.** `docs/research/m20-lever-firing-rate-2026-08-26.md`. **They fire rarely because almost nothing declares them: 4 of 52 enabled legs (7.7%)**, and `vol_trail` + `be_touch_arm` are declared by **zero**. The levers are YAML-declared default-OFF by design, so 13 firings from 4 declaring legs is the expected order of magnitude — there is no declared-but-not-firing leg, which is what a wiring gap looks like. ⚠️ **THE FIGURE IS MIS-FRAMED TWICE.** (a) `CLAUDE.md` contains no such number — the source is `S-SYSTEM-REVIEW-STRUCTURAL-2026-08-24.md:70`, and it says *17 closes*, not *17 lever firings*. (b) Only **2 of the 4** lever families can EVER appear in `exit_reason` — `trail_vol.py` has no close path at all and `be_touch_arm` books as a stop hit — so the statistic structurally measures half the levers, whatever the sample. **Re-measured post-G1-backfill: 13 lever closes of 1,029 (1.26%)** — real money 3 of 407, paper 10 of 622, `paperPortfolio` a subset and not added. Cross-checked on two independent surfaces (`perExitPath` and the Data Explorer with `filter_state: applied` asserted) which agree exactly. ⚠️ The **rate** reproduces, the **count** does not (13 vs 17) and the denominators differ (1,029 vs 1,324) — not the same population, not reconciled, and not invented. ⚠️ **Whether the firings were GOOD is unanswerable here: 1 of the 13 (7.7%) has a measured PnL.** `BL-20260826-M20-LEVER-FIRING-RATE-MEASURES-HALF-THE-LEVERS` |
| B3 | `target_extension_soak` is shipped and accruing at ANNOTATE. Read it: 28 of 52 enabled legs declare `tp_r >= 50` and have no target to extend from. | 1 | ✅ **READ 2026-08-26 — and the answer is that it CANNOT yet be answered.** Population: the COMPLETE soak, **n=856 rows**, 2026-08-23T10:12Z → 2026-08-26T12:47Z, 19 legs, 22 packages (completeness PROVEN by a short page, not assumed). **856/856 are `sentinel_no_expectation`/`no_expectation_declared`**; `target_r` is 50.0 on every row; **ZERO** rows in any informative state. That composition is **structural**: `evaluate_extension` returns `EXT_NO_EXPECTATION` *before* the approach gate, and `not_approaching` is excluded from `_LOGGED_STATES` — so sentinel legs log whenever evaluated while the 24 real-target legs log **nothing** until price approaches, and they contributed zero rows. ⚠️ **So the soak cannot distinguish "the lever would never fire" from "no real-target trade has approached its target"** — opposite follow-ups. ⚠️ **Row count is NOT event count**: one package carries 67 rows over 74.6 h with a median gap of 2576 s against a 30 s exit cadence, consistent with the in-process dedup set resetting on every trader restart — supported, not proven, and filed as such. Quote "rows written", never "times evaluated". `BL-20260826-TARGET-EXTENSION-SOAK-IS-100PCT-SENTINEL-AND-CANNOT-YET-OBSERVE-THE-LEVER` |

### vwap — parked, with a re-arm gate (operator-directed 2026-08-26)

`vwap` is OFF: `execution: shadow`, routed to **no account**, last trade
2026-06-07. Nothing is bleeding. It is parked, **not** closed.

⚠️ **Re-arming vwap — or reusing its `entry ± SL_STD_MULT × std_dev` stop
construction anywhere else — requires an exit-geometry review first**
([`docs/design/exit-mechanism-construction-PROCESS.md`](../design/exit-mechanism-construction-PROCESS.md)).
The evidence that review must answer is
`BL-20260826-ONE-THIRD-OF-MEASURED-CLOSES-LAND-BEYOND-THE-DECISION-TIME-STOP`:
**44.2% of its measured closes (65/147) landed past the decision-time stop**,
against 20.0% for the next strategy and 0–12.5% elsewhere. The netting
explanation is already REFUTED (−19.2pp, the wrong direction) — do not re-run
it. What is unresolved is whether that stop is simply too tight for a
mean-reverting entry, or whether enforcement failed; **those need opposite
fixes**, so the review must separate them before proposing either.

Verify liveness before quoting any of this as current:
`python3 scripts/ops/strategy_liveness.py vwap`.

### Lane C — Order-path correctness

| # | item | tier | status |
|---|---|---|---|
| C1 | **Partial leg-id capture by CONTENT, not novelty.** Match `triggerPrice` + `qty` against what this entry set, instead of abstaining whenever a concurrent entry appears. ⚠️ The abstain must survive a genuine tie — mis-attributing a leg id amends a LIVE stop on the wrong trade. Stage on `bybit_1`. **NOT closable via `orderLinkId`** — we never place these legs. | 2 | open · `BL-20260826-PARTIAL-LEG-ID-CAPTURE-IS-A-SNAPSHOT-DIFF-THAT-LOSES-THE-RACE-IT-MOST-NEEDS-TO-WIN` |
| C2 | Flat-symbol Bybit position-mode read. **Demoted to nice-to-have** — it blocked hedge mode, and fact 7 says hedge mode is not needed. | 1 | open, low |

### Lane D — M16 / M18: park on evidence, do not quietly continue

| # | item | tier | status |
|---|---|---|---|
| D1 | **Record against M16 that its core scalar has been measured and fails the job M18 assigned it** (fact 3). M16 reads "IN PROGRESS — research proposal (2026-06-15)" and has not moved in ten weeks. Either re-scope it to a use confidence *can* serve, or park it explicitly. Not a silent continuation. | 1 | open |
| D2 | **Do not build a third arbiter on these inputs.** Two exist, both measured negative. A new one needs a *new input* clearing 0.55 OOS at real n, and A2's provenance rule if scored on journal pnl. | — | standing constraint |

### Lane E — Operator-owed (**1 open**, `docs/claude/operator-owed-register.json`)

| id | owner class | status |
|---|---|---|
| `OO-20260825-BREAKOUT-1-BALANCE-REPORT` | physical_or_broker | **OPEN — and now material, not merely stale.** The prop bridge has no broker feed, so this is genuinely human. On 2026-08-26T22:18Z a position was opened against the unmeasured cushion: `breakout_1` SOLUSD long 25.00 @ 99.94 (fill id 36). At the ticket's declared stop it risks **$90.12** against **$64.00** to the static drawdown floor on the only snapshot on record (balance 4764.00, reported 2026-08-23T16:10Z, 78.2h old, freshness `stale`). On those figures a full stop-out **breaches the floor** and permanently disables the account. The figure is stale, not live — which is the whole reason the report is owed. |
| `OO-20260825-ALPACA-LIVE-DAILY-USD-200-DECISION` | judgement | ✅ **RESOLVED 2026-08-26** — operator: funding is $200, `daily_usd` stays 200. Recorded with its consequence: at that funding `daily_usd: 200` is **inert** (it equals 100% of the account while `daily_loss_pct: 0.1` binds first at $20) and arms only above $2,000 equity. No config file was touched. The sizing floor is filed separately and is NOT closed by it — `BL-20260826-ALPACA-LIVE-AT-200-USD-CANNOT-SIZE-ITS-LARGEST-SYMBOLS`. |
| `OO-20260825-DIAG-READ-TOKEN-ROTATION` | secret_origination | ✅ **RESOLVED 2026-08-26** on the operator's statement ("it's done"). ⚠️ Recorded as **stated, not verified** — a session cannot read a secret's value, so no measurement was available either way. |
| `OO-20260825-IB-PAPER-MHG-STOP-OVER-COVER` | defaulted_to_human | **WITHDRAWN** — blocked on work, not on a decision. An item nobody can approve does not belong in a register of things awaiting approval. |

---

## What this plan deliberately does NOT do

- **No demotion of `ict_scalp_5m`.** Operator-directed, and fact 8 supports it.
- **No hedge mode.** Fact 7.
- **No new ranker / arbiter.** Fact 3, and D2.
- **No minimum-R entry gate.** Derived and then refuted in-session: it would have
  removed the only profitable cohort (6 of 6 wins, +$22.52).

## Building practices carried forward from the 08-21 plan

One session at a time · one copy of every fact · PRs opened non-draft · this file
written down rather than left in chat · verification proportional to blast radius.

**Added 2026-08-26, from this session's own failure mode:** a stored field is not
the quantity it is named after. Before drawing a conclusion from `exit_reason`,
`pnl`, `confidence`, or a doc sentence, check the primary source — which in three
of this session's seven corrections was **in the row already being read**
(`position_size`, `exit_price`), and in two more was a section further down the
same document. See
`BL-20260826-FOUR-WRONG-CONCLUSIONS-IN-ONE-SESSION-FROM-READING-A-STORED-LABEL-AS-THE-MEASUREMENT`.

## Session log

| date | session | what moved |
|---|---|---|
| 2026-08-26 | `b9kyip` (cont.) | **Six PRs merged: #10345, #10346, #10347, #10352, #10353, #10354.** Lane B1 delivered (22 legs measured, 22 coverage cells closed); recovered **1,629** stranded e35 cells by union keyed on `measurement_key` (verified against the produced file: 8,211 rows / 8,211 unique keys / 0 duplicates) and fixed a gate that graded RAW wins while its own comment claimed otherwise. Corrected two stale `alpaca_live` register facts by reading the config, and filed the $200 sizing floor. **Caught the over-cover GENERATOR in the act** at 02:08:35Z on `ib_paper`/MHG — the keyed pre-cancel is name-scoped, so a legacy-named group is never a cancellation candidate — and shipped `stray_oca_groups.py` at `annotate` plus the allowlist that makes it stageable. Repaired the MGC venue state under a Tier-2 OK; recorded that the 50-vs-51 journal divergence did **not** self-heal. Logged one prop fill. **Nothing is armed.** 3 backlog rows filed, 2 OPEN-ITEMS rows added, 2 owed items resolved. |
| 2026-08-26 | `b9kyip` | **G1 code half + G3 first slice shipped.** Corrected this plan's G1 premise (the sweep fix landed in #10262 the day before). Found + fixed: the backfill tool was inert (`sys.path` two levels short, never run — 0 rows), the `exit_reason_source` marker was truncatable (2 live rows corrupted), and `perExitPath` had no label axis. Dry-run projection measured against a full journal replica. 2 backlog rows filed. Tier-2 apply run NOT done. |
| 2026-08-26 | `qhpxyh` | Plan written. Facts 1–10 measured. #10329 (stop/start-bot-service + Bybit position-mode read) merged. 9 backlog rows filed, 1 resolved with the opposite headline to its filing. No config changed. |
