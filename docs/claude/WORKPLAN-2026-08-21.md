# Work plan — the money path, after a cleanup gate

> **Status of this file.** Rewritten 2026-08-21T18:45Z on operator direction. It is
> **not** a source of truth and outranks nothing: the hierarchy in
> [`docs/CLAUDE-RULES-CANONICAL.md`](../CLAUDE-RULES-CANONICAL.md) § "Document
> Priority" governs, and a skill or a Tier-3 gate beats this file every time. (Its
> predecessor called itself "the single source of truth"; #10107 removed that
> correctly and it is not coming back.)
>
> **It is a LIVING file.** Each session: read it, take the next unblocked item,
> update the Status column and the Session log **in the same PR**.

## Why this was rewritten

Operator directive, 2026-08-21, after a day across three concurrent sessions.

**The work had become self-referential.** That day produced a guard about a doc
about workflows; a dedupe for an alert; a live falsifier for the dedupe; a fix for
the bug the falsifier's *design* surfaced; and tests proving the tests fail. Every
step was individually defensible. The aggregate spent three sessions on the
system's own observability.

**Meanwhile the system's measured condition did not move:**

| what | measured |
|---|---|
| closes via a **declared bracket** | **30 of 369 — 8.1%** |
| closes using the **entire M20 lever set** | **14 — 3.8%** |
| closes `reconciler_filled` | 148 — 40% |
| `pnlCoverage` (lifetime → 24h) | 0.37 → 0.33 → 0.11 → **0.06** |

The day's only money-path changes were `dcf5220b`'s Tier-2 fills timer and pairs
reorder. **That ratio is what this file exists to fix.**

## The four decisions (operator, 2026-08-21)

1. **Cleanup pass FIRST, then the money path.**
2. **Verification proportional to blast radius** — an explicit *stopping rule*.
3. **A dedicated triage pass, now** — every open backlog row dispositioned.
4. **One session at a time · one copy of every fact · PRs opened non-draft · this
   file written down** rather than left in chat.

---

## Building practices — the stopping rules

These bind every session, not just Phase 0.

### Verification is proportional to blast radius

| the change touches | what it must carry |
|---|---|
| **live order path · money-DB writes · risk/sizing · account mode** | falsification, a live positive control, the population stated. No shortcut. |
| **runtime observability that can refuse or resize a trade** | a test shown to fire on a synthetic positive |
| **notifications · docs · CI wiring · research tooling** | a unit test shown to fire. **Stop there.** |

**The rule that was missing: stop at the level where a wrong answer costs money.**
A swallowed Telegram is worth a unit test, not two real operator pings. If you are
verifying the verifier, you have gone one level too far — unless the thing being
verified can move money.

### One copy of every fact

A fact lives in exactly one place and is **derived**, never restated. On
2026-08-21 three "canonical" documents gave the required-check list as **15, 9 and
3**; only the last was true. Prefer reading branch protection, the config, or the
code over mirroring it.

### One session at a time

Serial unless the work is provably disjoint. Three concurrent sessions spent a
visible fraction of their effort on merge slots, board tail proofs and shared-file
three-way reconciliations. `session-coordination` still governs when concurrency
is genuinely warranted.

### Open PRs non-draft

Flipping draft→ready **after** CI passes re-runs every required check on the same
commit and leaves two same-SHA context sets, so `get_check_runs` can no longer be
read by name and conclusion alone — you must compare `started_at`. Cost ~12
minutes on #10109.

### Filing has to cost something

The three backlogs hold **984 rows**. ⚠️ **The open count in the previous version
of this line — "378 open (health 777/317 · performance 105/39 · ml 102/22)" — is not
reproducible, and neither is any other single number.** Measured 2026-08-21T19:5xZ on
`1b05353`:

| reading | count | what it is |
|---|--:|---|
| `status == "open"` exactly | **121** | health 108 · performance 10 · ml 3 |
| `status == "kept_open"` | **241** | reviewed and deliberately held |
| anything not in a terminal set | **410** | the widest defensible reading |

✅ **RESOLVED 2026-08-22 by 0.1 Step 0 (#10119).** All 984 rows now carry one of
six enum values and the count is a one-liner. **`open` = 150 · `kept_open` = 246 ·
`resolved` = 571 · `superseded` = 7 · `wont_fix` = 7 · `invalid` = 3** (measured on `ff25084`;
an earlier draft said 151/570, taken on a base predating #10117's merge — the one
row of difference is `BL-20260821-DEPLOY-WRAPPER-ASSERTS-A-BOUNCE-IT-DID-NOT-DO`,
which #10117 closed. Checked row-by-row rather than waved through). Fifty-two
rows were migrated under a **stated rule rather than 52 individual judgements**:
any text signalling remaining work ("pending", "awaiting", "queued", "still",
"remaining", "not_yet", "reopened") maps to an OPEN state, **failing toward open**
because an over-counted open set is safe and an under-counted one hides work; the
original wording is preserved verbatim in each row's `detail`, so every call is
re-litigable against the row itself. **And it reconciles the old figure** —
`open + kept_open` = **397**, which is what "378" was approximating.

**The cause is that `status` is uncontrolled free text.** The health file alone carries
**41 distinct status values**, several of which are whole sentences —
`"resolved (guard); follow-up OPEN, see resolution_criteria (3)"`,
`"code fix shipped; DATA REPAIR still open (52 rows written degraded)"`,
`"bybit_measured_clean_alpaca_unmeasurable_13_positions"`. Each was an honest attempt to
say something a two-state field could not hold, which is the **collapsed-state** class
this repo already names — the field could not express the state, so the state went into
prose the counter cannot read.

**This is why 333, 378 and 121 have all been quoted this month and each was right under
some filter.** It is also the reason Phase 0.1 has no machine-checkable done-condition
today: *"an open count you can defend row by row"* presumes the open set is computable,
and it is not. Normalising the field is therefore 0.1's **first step**, not a separate
item — see below.

One measured piece of good news, since the previous line implied otherwise: **0 open rows
lack `resolution_criteria`.** An earlier count of 71 was taken over a wider status filter
and does not hold on the strict set.

"Filing is cheap" is true and is exactly why the file grew past the point anyone reads it.
A row nobody will action is indistinguishable from noise and, worse, reads as *covered*.

---

## Phase 0 — the cleanup gate (do this FIRST)

**Nothing in Phase 1 starts until Phase 0's done-conditions are met.**

| # | Item | Done-condition | Status |
|---|---|---|---|
| 0.1 | **Disposition every open backlog row.** **Step 0 first: collapse `status` to a closed enum** (`open` · `kept_open` · `resolved` · `wont_fix` · `superseded`), moving every free-text qualifier into `detail` where it belongs. 41 distinct values today; the count is not computable until this lands. Then each open row becomes: fixed · `wont_fix` with a reason · or promoted into a numbered item here. | The enum holds (a checker rejects an unknown value), **and** the open count is reproducible from the file by a one-liner any session can re-run. **No row survives on "might matter someday".** | ◐ Step 0 ✅ #10119 |
| 0.2 | ⚠️ **THE COUNT WAS WRONG, AND THE TOOL THAT PRODUCED IT WAS BROKEN.** Measured 2026-08-22: **151 → 129** after fixing two structural blind spots in `check_unwired_artifacts.py` — **15% of the reported debt was never real.** (a) Python **module imports were invisible**: the matcher required `<stem>.py`, which import syntax cannot contain, so a tool a sibling *imports and calls* graded "no runner at all" (`scripts/backtest_pullback.py:249 import subbar_align`; `bin/backtest_ict.py:258 from scripts.s006_ict_synthetic_validate import main`). (b) **`bin/` was not in the runner corpus** — the directory whose entire purpose is entry points. Both fixed, self-test 10/10 → 13/13 including the inverse case (a stem in a COMMENT must STILL flag), so the widening cannot degrade into "match anywhere". **Remaining: 129 = 120 docs-only + 9 true orphans.** Not mass-deleted: 0.2's blunt "deleted, not documented" is the wrong rule for a research probe whose finding IS recorded in the doc that cites it — that is what `# wiring: manual-only - <reason>` exists for — and I will not delete 129 files on the say-so of a tool I found two defects in within the hour. ⚠️ **The clone is SHALLOW** (56 commits; every file dates to the boundary commit), so `git fetch --unshallow` is required before any file age is knowable — my first read of the orphans was worthless. Two of the 9 were created 2026-08-20 and are too fresh to call corpses. | Each is wired, or **deleted**. A script with no runner is deleted, not documented. | ☐ |
| 0.3 | ✅ **DONE.** ⚠️ **It was worse than "does not block": the registration ran `--self-test` ONLY, the scan never ran, and the comment claimed "diff-scoped: only a NEW/changed tool is judged" — describing behaviour the script had no mode to perform.** A guard registered, documented as blocking, blocking nothing — the same shape as the thing it hunts. Now `--base <ref>` judges only files a change ADDS under `scripts/`+`src/` (`--diff-filter=A`), fail-open when git cannot answer, and states its denominator. Falsified both directions on a planted script. | CI fails on a new module with no runner. | ✅ |
| 0.4 | ✅ **DONE — and the named instance was ALREADY collapsed.** Source of truth: `.github/workflows/branch-protection-sync.yml:137` → `REQUIRED_CONTEXTS='["pytest-collect","pytest-run","guards"]'` = **THREE**. The only active canonical statement (`CLAUDE-RULES-CANONICAL.md:1272`) matches it exactly, and `session-board.json` matches; every other hit is a HISTORICAL record (sprint logs, checkpoints, quoted GH006 errors at 9-of-9 and 3-of-3) which is correctly **not** collapsed — a record of what was true then is not a restatement of what is true now. ⚠️ **THE LIVE ERROR WAS OURS, NOT THE DOCS'.** Sessions — including me, repeatedly today, and at least one earlier session in a backlog note — say **"all four required checks"**. Four checks RUN; **three are required**. `repo-inventory` is advisory by design and documented as such. The fact had exactly one home and was still restated wrongly, because it was derived from **observation** (`get_check_runs` returns 4) rather than from the source. That is the failure mode 0.4 is about, and no amount of doc-collapsing prevents it. | Each fact has one home, derived. | ✅ |
| 0.5 | ✅ **DONE — MEASURED, and the premise does not hold.** 50 guards, **62.3 s total wall clock** (one run, this branch's diff). **33 of 50 finish under 0.5 s**; six ≥ 3 s carry 73% of the cost (`secret-scan` 12.6 · `collapsed-state` 10.5 · `json-extract` 6.7 · `artifact-validity` 6.7 · `cost-model-single-owner` 6.0 · `impossibility-claim` 3.2), and each of those is a whole-tree correctness scan whose value is not in question. **Nothing qualifies for retirement**: the done-condition is "diff-scoped and cheap, or justified" and the set is already cheap. For scale, the guard job is ~1 min against `pytest-run` at **~11 min** — the critical path is the test suite, not the guards, so retiring guards would optimise the wrong thing and lose real coverage. Recorded as a measurement rather than an action, which is the correct outcome for a retirement pass when the thing under review turns out not to need retiring. | Every guard is diff-scoped and cheap, or justified in one line. Retire what is neither. | ✅ |
| 0.6 | **Build the missing read surfaces that block DECIDED work.** Three instances in one day: `BL-20260821-NO-READ-SURFACE-FOR-TIMER-SCHEDULE` (manufactured a phantom high-severity defect), `BL-20260821-FETCH-COST-HAS-NO-VENUE-AXIS` (forced a bound where a measurement was wanted), `BL-20260821-NO-BYBIT-ACCOUNT-IDENTITY-READ-SURFACE` (blocks Wave 0.2 STEP 0). | Each decided-but-blocked item has a read surface, so it is answered by measurement rather than inference or deferral. **All three are Tier-1 — do not ask the operator for these.** | ✅ #10120 · #10121 · #10122 — all three named instances |

⚠️ **Guard against Phase 0 becoming the disease it treats.** It is a **retirement**
pass. If it starts producing new guards, new docs, or backlog rows *about* the
backlog, it has failed. The output is a smaller system, not a better-instrumented
one. 0.3 and 0.6 are the deliberate exceptions: one blocking detector that already
exists, and the reads that unblock decisions already made.

---

## Phase 1 — the money path

Only after Phase 0. **One item in flight.**

> **Exception on record:** T.1 was reordered **ahead of** Phase 0 by the operator
> and has shipped. That is the documented one-line reorder, not a precedent for
> jumping the gate — everything else still waits on Phase 0.

| # | Item | Row | Tier | Status |
|---|---|---|---|---|
| 1.0 | **Bound the IB fetch tail.** ⚠️ **ROOT CAUSE FOUND, AND IT IS NOT THE QUEUE TIMEOUT** — the hypothesis this row carried ("a single fetch reaches 25.9 s against a 29.0 s queue timeout") was **REFUTED**: `timed out waiting for the pinned IB thread` appears **0 times** in any journal window sampled. The real cause is `IBClient._probe_liveness`, which ran on **every** `connect()` — i.e. every IB fetch, cached handle included — at **6.5 s** a miss (`IB_PROBE_TIMEOUT_S` 5.0 + `IB_PROBE_RETRY_GAP_S` 1.5). Measured: **n = 75 attempt-1 timeouts / 2226 s** over four disjoint windows 01:30Z–07:40Z = 2.02/min = **488 s of blocking in 2226 s, 21.9% of wall clock**, with the condemning branch firing **ZERO** times. Isolated in one breaching pass by journal gaps: two IB fetches of **20.24 s and 20.23 s** = 40.5 s of a 51.4 s pass. Fix = `IB_PROBE_CACHE_S` (default 60 s): a cached handle's **successful** verdict is trusted for the window; a fresh connect always probes; a failure is never cached. | `BL-20260816-IB-QUEUE-TIMEOUT-EXCEEDS-EXIT-BUDGET` | 2 | ✅ **DONE — shipped (#10129) and LIVE-CONTROLLED.** Deployed `9c9235f6`, confirmed by **ancestry**. Probe rate **2.02/min → 0.00/min** (0 events / 1395 s across 3 windows; ~47 expected). `max_interval_ms` **89.35 s → 39.64 s**, breaches **1.82% → 0%** (n=147, one process, 08:34→09:42Z). |
| 1.1 | ◐ **RE-MEASURED ON BROKER TRUTH, AND THE FRAMING INVERTS: this is substantially an ATTRIBUTION defect, not a mechanism one.** Of the 323 main-path closes only **78 (24.1%)** carry a broker-truth exit price; the other 245 are estimated/fabricated and are **UNKNOWN**, excluded rather than counted. Judged by `_classify_broker_exit`'s **own** conservative inequality over those 78: the fill **crossed the declared stop 40 (51.3%)**, **crossed the declared target 6 (7.7%)**, sat between 32 (41.0%) — so **46 (59.0%) reached a declared level while only 15 (19.2%) are LABELLED decision-driven. An under-attribution gap of 31 rows: the brackets reach a level ~3× more often than the journal records.** ⚠️ **A first pass using a symmetric ±0.10-risk band reported 72.7% — that is WRONG and retracted** (a symmetric band counts a fill that stopped SHORT of the stop as a stop hit). **What survives:** the stop:target skew is real at **40:6 = 6.7:1**, and 41.0% genuinely exited between the brackets. Partial diagnosis: of 44 broker-truth `reconciler_filled` rows, **21 crossed a level and kept the generic label**; *wrong level source* **REFUTED** (package and trade levels agree 43/44) and *a different close path* **REFUTED** (`closed_by`/`closed_reason` identical across both groups). A 2026-08-08 price backfill leaving the derived reason stale is a **CANDIDATE but UNDERPOWERED — Fisher p=0.111 at n=44, not a finding**. Evidence: `docs/research/exit-attribution-broker-truth-2026-08-22.md`. | `BL-20260822-EXIT-ATTRIBUTION-UNDER-REPORTS-BRACKET-HITS` | 1 measured · 2/3 to repair rows | ◐ |
| 1.2 | **Scalps held 10–100× their design horizon.** `ict_scalp_mgc_15m` p90 **1082 bars (11.3 days on a 15m leg)**; `ict_scalp_sol_5m` p90 501; `qqq_pullback_1h` **median** 312. `trend_donchian` 4h is well-behaved, which rules out a measurement artifact. | `BL-20260821-SCALPS-HELD-10-TO-100X-THEIR-DESIGN-HORIZON` | 3 | ☐ |
| 1.3 | **A crossed take-profit booked as an estimate** — real-money trade 4863. Root cause (daily fills timer) fixed 2026-08-21; open until a *new* crossed-TP close is confirmed booked MEASURED. | `BL-20260821-ICTSCALP-TP-CROSSED-BOOKED-AS-ESTIMATE` | 2 | ☐ verify |
| 1.4 | ◐ **TIER-1 HALF ANSWERED — the size IS the margin ceiling, and the venue clamp has been INERT for nine days.** The row's Tier-1 question (*which basis produced 34k AVAX*) is read: `notes.margin_basis` on both stamped rows says `kind: venue_available`, and **`position_size` equals `max_qty_by_margin` to full float precision** (33997.92498997939 and 33265.434696282246). `risk.py:1232` clamps down to that cap, so **the emitted size is the margin ceiling itself, not a risk-derived number** — the basis is broker truth, so the ceiling is right; the defect is shipping a ceiling as a size. ⚠️ **The venue-max clamp exists, is wired (`execute.py:1346`, `prefer_live=True`), landed 2026-08-13T10:39:59Z — and did not fire.** At **2026-08-22T10:30:58Z** the trader sent 33,265.4 AVAX and Bybit bounced it (`ErrCode 10001`, `max_qty` = **22,000**); the clamp's own WARNING is **absent** from the journal window, so `venue_max` was `None`. The venue's own error proves a maximum exists ⇒ `None` = *the lookup did not resolve one* ⇒ `BL-20260814-VENUE-MAX-NONE-CANNOT-SAY-WE-COULD-NOT-LOOK`, firing again eight days after its own instance. ⚠️ **Which of the three paths to `None` was taken is NOT determinable from the journal, and that is the finding**: `_live_lot_rule` returning `None` logs **nothing**, `_from_live` raising logs at **DEBUG**, `_from_profile` returns `venue_max=None` **by construction** — and the success path never records which source answered. **So observability is now the blocking step and it is Tier-1** (`legalize_qty` already returns `source`; nothing logs it). **Two corrections to the row, both measured:** its *"20 rejections, 2026-08-12..14"* (and `precision.py`'s *"18 of 22"*) is really **21 rows spanning 2026-07-30 → 2026-08-22T10:30:59Z — still live today**; and its *"invisible half"* (accepted orders sized near the ceiling) is **not supported at the median** — accepted n=22, median **4,574.3 = 20.8% of the cap**, only 2 of 22 above 80% of it (largest 21,971.2 = 99.87%). ⚠️ Therefore **"zero rejections" alone will never prove this fixed** — 22 of 22 accepted orders were already under the cap with no clamp firing. ⚠️ **And fixing the clamp WITHOUT the strategy decision makes things worse, not better:** a working clamp caps the order at 22,000 and it **places** instead of being refused, converting 21 loud venue refusals into 21 silent ceiling-sized fills on the worst paper leg of the last 30d (−7,620.73 / 20 trades). That strategy half is split out as `PB-20260822-AVAX-SCALP-SIZED-OFF-MARGIN-NOT-RISK` (performance backlog, Tier-3), because `/performance-review` — not `/health-review` — is what drains it. | `BL-20260821-AVAX-SCALP-SIZES-ABOVE-THE-VENUE-MAXIMUM` · `BL-20260814-VENUE-MAX-NONE-CANNOT-SAY-WE-COULD-NOT-LOOK` | 1 measured · 1 observability · 2 clamp · 3 sizing | ◐ |

### Exit-mechanism measurement validity (found 2026-08-22, gates 1.1 and G.2)

| # | Item | Row | Tier | Status |
|---|---|---|---|---|
| 1.5 | **`order_packages.close_reason` is NOT the exit record — and item 1.1's headline is built on it.** `stuck_cascade_recovered` is **45.5%** of package closes in the 08-16..08-22 page and is a *bookkeeping repair*, not an exit: it force-closes a package row whose TRADE had already terminated. Re-measured at the trade level the plan's number **corroborates** on an independent population (`trades.exit_reason`, newest 500 of 1,292 closed, `filter_state` asserted `applied`: `reconciler_filled` **41.2%** vs the plan's 40%). ⚠️ **But state the split — the pairs sleeve flatters the aggregate.** Main order path **n=323: decision-driven 15.2%, `tp_cross` 3 rows = 0.9%, `sl_cross`+`sl` 9.6% — a stop:target ratio of 10.3 : 1**; pairs alone (n=177) is 42.4% decision-driven with **zero** `tp_cross`. **When 1.1 is next worked, recompute from `trades.exit_reason` with the split stated; do not re-derive from `close_reason`.** ⚠️ **This is a measurement PRECONDITION of 1.1, not a separate front** — the correct measurement is already taken and the plan's headline corroborated, so what remains is one sentence of discipline. Do not spend a session on it. ✅ **DONE 2026-08-22** — reproduced independently (main n=323 / pairs n=177, split stated) and it is now item 1.1's stated basis. | `BL-20260822-PACKAGE-CLOSE-REASON-IS-NOT-THE-EXIT-RECORD` | 1 | ✅ |
| 1.6 | ✅ **TRACED AND FIXED — and the row's feared half is REFUTED.** The mechanism is no longer a hypothesis: `_close_pair` (`pairs_executor.py:707`) writes `status='closed'` straight to the trade row and so never routes through `_close_trade_from_order_status`, the **only** place `_cascade_close_linked_package` runs — **zero** cascade calls exist anywhere in `pairs_executor.py`. Both arms reconcile **EXACTLY** on the live journal: pairs legs closed via the monitor **57 ↔ 57** packages with a real reason; closed via `_close_pair`/`intent_reduce` **120 ↔ 120** left for the sweep (109 swept + 11 not yet). `pairs_revert`/`pairs_stop`/`pairs_half_open_cleanup` appear on **99 trade rows and ZERO package rows**. Concentration pairs **61.6%** vs main **4.6%** (13.4×) — which is what rules out a tick-race. ⚠️ **The row's open question — does the strategy-monocle gate govern the isolated pairs path — is answered NO by a SECOND independent route:** `_has_open_package_for_strategy` is consulted at exactly **one** site — **`pipeline.py:611`** (imported at `:56`), the pipeline's signal-dispatch path — and pairs legs are not in the strategy roster (`pipeline.py` resolves any `pairs_` strategy to the `pairs_executor` monitor unit), so `run_pairs_tick` (`main.py:865`) never reaches it. ⚠️ **#10144 reached the same conclusion concurrently and independently, but named the caller `intent_multiplexer.py`, which is wrong** — that module imports a *different* monocle symbol (`_same_bar_entry_for_strategy`, its own `:560`) and contains **zero** references to `_has_open_package_for_strategy` (verified by an untruncated grep across `src/`). The conclusion is unaffected and now rests on two independent derivations; only the cited evidence needed fixing. Corrected here and in the row — field beats comment. **So the sleeve is NOT depending on a repair sweep to un-block itself** — the larger feared finding does not hold. **The sweep was NOT changed.** Fix is bookkeeping-only, guarded at both helper and call site so a package-write failure can never be read as a failed flatten. Tests 7/7 fail pre-fix, 7/7 pass post-fix. ✅ **LIVE POSITIVE CONTROL: PASS (2026-08-22T17:12Z, third attempt).** **POPULATION:** all **189** packages `strategy_name LIKE 'pairs_%'`, `filter_state` **and** `order_state` asserted `applied`; deploy boundary `e9dbd7b0` = 15:35:53Z. **Pre-deploy n=187 → ZERO** pairs-native reasons (`stuck_cascade_recovered` 109 · `reconciler_filled` 57 · null 21). **Post-deploy n=2 → ONE**: `pkg-a7041e63d2ac491b` (`pairs_sol_eth_a`, SOLUSDT), `close_reason` **`pairs_half_open_cleanup`**, `linked_trade_id` 4942 — and trade 4942 carries the **same** `exit_reason`, closed **242 ms** earlier. **Distinguished from the sweep on two independent grounds:** timing (the sweep runs on the monitor cadence, tens of seconds — 242 ms is the in-line cascade in `_close_pair`) and the reason **value** (the sweep writes `stuck_cascade_recovered`, this is not that). ⚠️ **Why the first two attempts failed, diagnosed not repeated:** the sleeve was **flat on all four pairs** from 12:10Z and had evaluated only **4 bars post-deploy, all `skip_flat`** — no pair was open, so no close could occur. Cadence for the next waiter: **38 `close` + 24 `half_open` = 62 events in the 12.4 days 08-10..08-22 (~5/day)**. And **`half_open` is the likelier first trigger, not `close`** — `_close_pair` is reached with `outcome="half_open_cleanup"` from `pairs_executor.py:992` and SOL/ETH strands on essentially every open (`BL-20260821-PAIRS-SOL-ETH-STRANDS-ON-EVERY-OPEN`); that is exactly what fired. **The sweep was still NOT changed.** | `BL-20260822-PAIRS-PACKAGES-CLOSED-BY-THE-STUCK-CASCADE-SWEEP` | 1 traced · 2 fixed | ✅ |
| 1.7 | ◐ **CLASSIFIED — and my own headline count was an over-report: THREE genuine gaps, not seven.** ⚠️ The first enumeration grepped for `_cascade_close_linked_package` **within 60 lines — one helper's NAME — when the question is whether the PACKAGE GETS CLOSED**. Four sites close it by calling `db.update_order_package` directly, invisible to that grep. Measuring the mechanism instead of the effect is the same class as the rest of this list. **Correctly handled (4), annotated in place:** `:3140` (the cascade helper) · `:3587` `_close_unattributable_orphan` (package closed at `:3605`) · `:5250` `_watchdog_stuck_strategies` (force-closed earlier on the SAME branch at `:5147`) · `:9054` `_close_options_row` (closed at `:9036`) — each with the trade's own reason. **Genuine gaps (3), annotated as gaps, NOT fixed:** `:2910` adopted-orphan-disappeared — ⚠️ **the intuitive exemption is FALSE and was measured false: 44 of 74 `adopted_orphan` rows carry a non-null `order_package_id`**, I assumed otherwise and the measurement caught it · `:7734` `_netting_apply_close` (no package reference of any kind; `netting_attributed` is 5.3% of main-path closes) · `execute.py:1719` intent_reduce (the largest: 48+10 main, +21 pairs). **Why not patched:** `_sweep_stuck_linked_packages` has **no age gate**, so a cascade advances the close by ~1 tick — but the monocle gate reads OPEN packages, so that tick is a real change to how soon a live strategy may signal again. Tier-2, prepared not shipped. **The argument the other way, for the operator:** the sweep fires `enqueue_stuck_package_sweep` on **every** miss, so with pairs at 109 and intent_reduce at ~79 that alarm has been firing routinely — closing the gaps would **quieten a real alarm**, not just tidy a label. | `BL-20260822-SEVEN-OF-EIGHT-TRADE-CLOSE-SITES-DO-NOT-CASCADE-THEIR-PACKAGE` | 1 classified · 2 per gap | ◐ |
| 1.8 | ✅ **SHIPPED BOTH HALVES 2026-08-22 (Tier-2, operator-approved 18:3xZ).** Forward fix: `_sweep_pending_pnl_from_bybit` now re-derives the label at the moment the price arrives — **three guards, each load-bearing**: `is_reduce_leg` derived exactly as at the other call site (the SELECT reads `setup_type`, which it did not before); only a row still carrying the **generic** reason is relabelled (the sweep selects on `pnl IS NULL` and can catch a row another path closed with a real reason); and the classification is wrapped so a label failure **can never lose the price write**. Historical half: `scripts/ops/reclassify_frozen_exit_reasons.py`, **annotate-first**, `--apply` required to write, `--provenance` scoping the WRITE and never the measurement, importing `_classify_broker_exit` rather than re-deriving it, recording `exit_reason_prior` so it is reversible. ⚠️ **NOT YET RUN against the live journal — it is staged.** Run `--provenance broker_truth` first (the 91 rows), read the annotate JSONL, widen only if warranted. **Verification:** 10 of 11 new unit tests fail pre-fix / 11 pass post-fix — ⚠️ the one that passes pre-fix is the *failure-guard* test, which passes trivially because before the fix there is no classifier to fail; 5 further tests cover the historical tool end-to-end on a real sqlite journal; **99 related tests pass**; `provenance-consumer-guard`, `collapsed-state-guard`, `canonical-db-resolver` all clean. ⚠️ **`unwired-artifacts` caught a real omission of mine** — the new script had no runner; it now declares `# wiring: manual-only`. **A live positive control is OWED**: the forward fix has not yet been exercised by a real late-price close. Original finding follows. `_close_trade_from_order_status`'s no-closed-pnl fallback (`order_monitor.py:6033`) hard-codes `reconciler_filled` and leaves `exit_price` NULL — correctly, there being no price. `_sweep_pending_pnl_from_bybit` (`:8323`) then selects exactly those rows (`status='closed' AND pnl IS NULL`), recovers the venue record, writes `exit_price`/`pnl`/`exit_price_source` — and **never touches `exit_reason`, never calls `_classify_broker_exit`.** Nothing else revisits the label. **POPULATION: 572** `reconciler_filled` rows (`filter_state` asserted `applied`, both pages), **395 gradeable** (172 no package + 5 no price **excluded, not counted**). On **broker-truth** prices, n=155: **91 mislabelled (58.7%)** — 83 `sl` + 8 `tp` — against 64 (**41.3%**) genuinely between. ⚠️ **Quote the 91, not the 181** — the wider figure rests partly on `local_markprice`, the FABRICATED class. **Two confirmations:** 181/181 mislabelled rows carry **no** `notes.exit_reason_source` (the marker the classifying branch stamps) — a 100% signature, not a tendency; and the **14** rows that *did* reach the classifier are all `unresolved` and **all 14 still grade `None` today**, so *'classified then the price moved'* contributes **zero** rows. **Fix, PREPARED NOT SHIPPED:** the sweep already holds `db`, the row and `avg_exit_price` — call the classifier there and add `exit_reason` + `exit_reason_source` to its `updates`. ⚠️ **`is_reduce_leg` must be carried across** (the sweep's SELECT does not read `setup_type`; omitting it mislabels reduces as `sl`/`tp`, the exact failure the exclusion exists to prevent). ⚠️ **Relabelling the 181 historical rows is a SEPARATE Tier-2 decision** — the 90 resting on estimated prices are a weaker case than the 91 on broker truth. ⚠️ **Scope not claimed:** all 181 are Bybit; the sweep is Bybit-specific, and no equivalent for IBKR/Alpaca is asserted. ⚠️ **CORRECTION, same session — the chain's SHAPE was already written down and I called it unnamed; that is WITHDRAWN.** `src/runtime/provenance.py`'s docstring (steps 1–3, 2026-07-30) already says the fallback pins the reason and that the classifier is *"downstream of a price the code deliberately refuses to fetch"*. **What is new:** it is scoped to **demo** (4733/4180 are `bybit_2`, **real money**), it is about the **PnL** (which **was fixed** on 2026-07-30) not the **label** (which was not), the later writer here is the **broker record** not a mark, and the **scale** was never measured. ⚠️ **And that docstring's step 1 is itself STALE** — corrected in the same PR: the demo branch was **narrowed** on 2026-07-30 (#8111) to resolve from the **fills store**, so it no longer "refuses to fetch". **This meets G.1 from the other side:** of the 181, `bybit_1` is **155** and only **73 (47.1%)** carry a broker-truth price — because demo now resolves from fills and `fills_pnl.py`'s 5% `QTY_TOLERANCE` rejects its netted closes. One population, two items. ⚠️ Not the same 47.1% as `provenance.py`/#8111 — different quantities that coincide. | `BL-20260822-EXIT-REASON-FROZEN-WHEN-PRICE-ARRIVES-LATE` | 1 measured · 2 shipped | ✅ (control owed) |

### Gating measurement (the only Wave-2 work that survives)

| # | Item | Why it gates | Status |
|---|---|---|---|
| G.1 | ◐ **PREMISE RESTATED ON ALL THREE COUNTS — it is not one number, not a timer problem, and the direction test as written is not answerable.** **(a) Not one number:** `/performance` is **real-money only** and reads `pnlCoverage` **0.78 lifetime** (n=400); `/trades/closed` real money is **180/200 = 0.90**. Over Bybit rows in the newest 500 closed of ALL classes it splits **bybit_2 0.622 · bybit_portfolio 0.294 · bybit_1 0.081** — and bybit_1 is **84%** of the closed population, so the alarming `0.06` is a paper-dominated blend. A figure over both funding classes is not a quantity about anything. **(b) The windows are NESTED**, so a 24h/7d/30d/all snapshot conflates recency with the fix; the only valid test is the same window across time, and at real-money volume (1–5 closes/day) the post-fix cohort aged ≥24h is **n=5 — NOT ANSWERABLE**, and labelled so rather than answered. **(c) The root cause is NOT fill capture:** `bybit_1` holds **541 fills** over 30d, the MOST of any account (bybit_2 90, bybit_portfolio 60), current to today, and `live_bybit_fill_accounts` explicitly includes paper Bybit accounts — so the hourly-timer change could not have moved it. **The rejecter is `fills_pnl.py`'s 5% qty gate:** under one-way netting ONE exchange fill closes a position spanning N journal rows, and the measured median fill/trade ratio on bybit_1's top symbols is **1.31–1.44 — a 31–44% overshoot against `QTY_TOLERANCE = 0.05`, 6–9× over** → `return None` → `candle_at_close`. bybit_2's fills are PARTIALS (0.14–0.80) that accumulate correctly, which is why it clears 0.622 on a fraction of the fills. ⚠️ Those ratios are per-symbol **medians, not a per-trade join** — stated as such. **This is why 1.1 is measurable on only 24.1% of the main path.** | `BL-20260822-FILLS-MATCHER-QTY-GATE-BLOCKS-NETTED-ACCOUNTS` | 1 measured · 2/3 to change the matcher | ◐ |
| G.2 | ⚠️ **PREMISE MAY BE INVERTED — do NOT action on the assumption the grader is wrong.** 1.1's broker-truth re-measurement found a reconciler close **often genuinely WAS a bracket hit** (46 of 78 reached a declared level against 15 labelled). So the grader may be closer to right than the `exit_reason` it is being checked against, and the defect sits in the label. Re-state G.2 against the corrected basis before working it. | Re-check before actioning | ☐ |

### ⚠️ Two refutations — do NOT rebuild these

- **An `ict_scalp` take-profit close path was measured and REFUTED.** Net +$6,237 is
  the residue of 29 trades that exited worse (+$20,137) against 23 that exited
  better (−$13,899) — 1.45:1, over 16% of trades, of which **$1.81 is real money**.
  Over-held scalps were net **positive** (+$1,772) vs within-horizon (−$48), so a
  naive max-hold exit could destroy value. **25 of the 29 shortfalls exited
  `reconciler_filled`** — the target was touched with nothing firing, which points
  at the bracket path, not the geometry. Tool:
  `scripts/research/tp_recovery_counterfactual.py`.
- **`CANDLE_CACHE_TTL_MAX_S` is NOT the exit-loop knob**, despite a backlog row's
  own `next_action` having said so. Measured at an 86 400 s cap, an IB frame still
  goes to the venue **5/5**. See Tier-3 item T.1 below for the real cause.

---

## Tier-3 items on record

**T.1 — exit-eval fetch cost. ✅ APPROVED · ✅ MERGED `1b05353` (#10114) · ✅ LIVE FALSIFIER PASSED 2026-08-22T04:48Z.**
Root-caused by `wave0-8g7443`; exact diff in
[`docs/research/exit-eval-fetch-attribution-2026-08-21.md`](../research/exit-eval-fetch-attribution-2026-08-21.md) § 6.
`_client_cache_key` excludes `interactive_brokers` from the connector memo, so a
fresh `IBMarketData` is built per request and the candle cache — keyed on a
per-object token — is **guaranteed to miss**. The one open IB 15m package was
fetched **281 times in 281 consecutive passes** (a working cache predicts ~132);
three non-IB frames land within **12%** of prediction. The pass is **96.7% fetch**,
and `EXIT_LOOP_INTERVAL_SECONDS` is inert (`interval == max(30 s, pass)` on 94.5%).

⚠️ **The stake is lower than this programme first stated.** The cost was described
as staleness *"behind live orders"*. The only account trading MES/MGC/MHG is
**`ib_paper` (`account_class: paper`)** — `ib_live` is `mode: dry_run`, shelved. So
the added staleness lands on **paper** legs while the faster pass benefits **every**
leg, including the real-money Bybit ones. Checked against `config/accounts.yaml`.

✅ **REORDERED AND SHIPPED 2026-08-21T19:0xZ.** The line above invited exactly this:
*"If you want it shipped ahead of Phase 0, say so — it is a one-line reorder, not a
re-decision."* The operator said so, so T.1 went **ahead of** Phase 0. Nothing about
the Tier-3 approval changed; only its queue position.

**Re-verified against current `main` before shipping**, as this item's own note
required — the original reading was taken at `a252119` and `main` had moved seven
times. It reproduces at a **larger** n: off-loop `fetch.15m` **1.002 per pass over
n=433 exit passes** (was 1.000 over 281), mean pass 42.3 s, max 95.8 s.

**Shipped:** the `_ib_connection_identity` resolver read by BOTH
`_build_ib_market_data` and `_client_cache_key`, plus
`tests/test_ib_candle_cache_memo.py`. The tests were **shown to fire against the
pre-fix file** — 5 failed / 3 passed pre-fix, 8 passed post-fix, and the 3 that pass
either way are the two non-IB controls and a structural assertion, so a failure
localises to IB rather than to the cache.

### ✅ The live falsifier PASSED — measured 2026-08-22T04:48Z

**Deployment confirmed first, not assumed:** the trader serves `926698c7`, and `1b05353`
is an **ancestor** of it — checked with `git merge-base --is-ancestor`, and the deployed
tree carries the memo branch. A falsifier read against undeployed code would have been
worthless.

**Population: ONE process, `23:08:50Z → 04:48:04Z` (5 h 39 m), n = 676 passes / 675
measured intervals.** A long-lived process is the condition under which the tail is *most*
likely to be drawn — the plan's own warning is that a short process reads reassuringly —
so this is a favourable-to-detection sample, not a lucky one.

| | before (n=433) | after (n=676) | |
|---|--:|--:|---|
| off-loop `fetch.15m` per pass | 1.002 | **0.494** | **−50.7%** |
| mean pass | 42.3 s | **4.89 s** | **8.6× faster** |
| max pass | 95.8 s | 69.1 s | −27.9% |
| `requirement_state` breaches | 28.9% ⚠️ | **5 / 675 = 0.74%** | ~39× fewer |

`fetch.cache_hit` now registers **n = 9,012**. The cache went from structurally unable to
hit, to hitting as the dominant path.

**`EXIT_LOOP_INTERVAL_SECONDS` is no longer inert.** It was documented as such because
`interval = max(30 s, pass)` and the pass was 42.3 s, so the configured cadence never
bound. At a 4.89 s pass the observed mean interval is **30.2 s** — the configured 30 s now
governs, which is the intended design and was not true before.

⚠️ **THE REQUIREMENT IS STILL BREACHED, and this is the honest headline.**
`requirement_state: breached`, `max_interval_ms` **88.76 s** against a 60 s requirement;
last breach 04:25:01Z. **T.1 did what it claimed and did not fix the requirement.** The
residual is the **tail**, exactly as #10114 predicted when it declined to project the max:
*"the mean follows from the fetch budget; the max is a queue-stall property of the
single-worker `_IB_FETCH_EXECUTOR`."* That prediction held. Single fetches still reach
**25.9 s** (`fetch.15m` max) and **22.6 s** (`fetch.1d` max) against
`_IB_FETCH_QUEUE_TIMEOUT_S` = 29.0 s.

**So the problem is now much narrower than it was, and it has a name:**
`BL-20260816-IB-QUEUE-TIMEOUT-EXCEEDS-EXIT-BUDGET`. Bounding that tail is the only thing
between this system and the 60 s requirement. **Promoted to item 1.0 below** — it is
ahead of 1.1 because it is the last step of work already done, not a new front.

⚠️ **One caveat on the comparison.** The 28.9% pre-figure is quoted from this plan; its own
`n` and process were not recorded beside it, and these counters are **per-process**, so the
two rates are not guaranteed to share a denominator. The direction and the order of
magnitude are not in doubt; the precise ratio is stated as approximate for that reason.

**T.2 — pairs naked-leg stranding.** Hedge mode on `bybit_1` (demo; `bybit_2` is
mainnet, so a mode switch cannot reach real money). Build the
`bybit_position_idx(account, symbol, side)` resolver with an **empty allowlist =
byte-for-byte no change**. ⚠️ The order path is `pybit`, not ccxt — the ccxt
convenience method does not sit on the path that changes. ⚠️ **STEP 0 is NOT
operator-blocked** (corrected by `wave0-8g7443`): "does `bybit_portfolio` share a
demo UID with `bybit_1`?" is blocked on a read surface that does not exist, and
building it is **Tier-1** — see Phase 0 item 0.6.

**T.3 — `slv_trend_1h`** (`PB-20260821-SLV-TREND-1H-ZERO-WINS-IN-13`) — investigate,
then recommend kill or fix. It keeps trading meanwhile.

✅ **INVESTIGATED 2026-08-22. NOTHING CHANGED — `execution: live` untouched, per the operator's 11:1xZ instruction.**
The measurement reproduces on a stated population (30d since 2026-07-23T12:55Z, paper block: 13 trades, 0 wins,
−$5,374.59, `pnlCoverage` 0.7692, `expectancyR` −1.1206). **But T.3's own pre-approval condition fails
independently of that instruction:** it required the leg be *"genuinely routed to real money"*, and read from
`config/accounts.yaml` every account carrying `slv_trend_1h` is paper or dry-run — `alpaca_paper`,
`alpaca_portfolio`, `alpaca_options_paper` (all paper) and `alpaca_live` (`mode: dry_run`). T.3's own fallback
text covers it: *"a leg that turns out to be already-shadow, **paper-only**, or n-too-small is a finding, not a
demote."* Both routes reach change-nothing.

⚠️ **The degenerate-confidence cause is ALREADY FIXED and the fix WORKS — I nearly reported the opposite.**
`min_confidence: 0.3` landed in `0093d2ba` on **2026-08-01** (operator-approved, #8255). Entry confidences on the
9 packages: 0.154 · 0.064 · 0.039 (**all before** 08-01) then 0.469 · 0.664 · 0.337 · 0.337 · 0.347 · 0.423
(**all after**). Zero violations post-landing. My first pass read *"3 of 9 below the floor"* and would have
reported it unenforced; **dating the commit is what corrected it, and that required `git fetch --unshallow`** —
the clone ships at 52 commits and `git log -S` silently attributes every change to the boundary commit.

**So the 13-trade population straddles a Tier-3 remediation.** Split at 08-01, package level: pre-floor 3 packages
−$421.65; post-floor 3 packages −$1,435.70 (plus 2 rejected, 1 open). ⚠️ **n=3 post-floor is UNDERPOWERED and is
labelled so** — it is not evidence the floor failed, nor that it worked.

⚠️ **The finding that moves this row out of "bad strategy":** all **six** closed packages exited
`exchange_flat_reconciled` — 6 of 6. No `sl_cross`, no `tp_cross`, no trail. **This leg has no decision-driven
exit in practice**, which makes it an instance of the exit/protection cluster and of item 1.1, not an independent
strategy verdict. A kill here would attribute to the strategy a loss its exit path is at least partly responsible
for. **Recommendation: do not decide its disposition on current evidence.** The M7 packet the row asks for was
**not** run this session and is not claimed as run.

**T.4 — ETH 15m regime head.** ❌ **THE PREMISE IS REFUTED — and "54 days at shadow" is days since
REGISTRATION.** Read 2026-08-22 from `/api/bot/ml/registry` (95 rows, `mirror_age_seconds` 43.8).
`eth-regime-15m-lgbm-fc-pcv-v1` is `deployment_bucket: OFFLINE`, and its `stage_history` is decisive:
`candidate → shadow` 2026-07-04, then **`shadow → candidate` 2026-07-19**, reason quoted verbatim from the row —
*"operator-approved 2026-07-19: powered RG4 NO_EDGE (0.476) — fc variant no better on ETH; demoted per M25 P4"*.
**14 days at shadow, ended by an operator-approved demotion on powered evidence; 33 days at candidate since.**
Registered 2026-07-01 = 51 days ago, which is where "54 days at shadow" comes from: days-since-registration
wearing the label days-at-shadow — the unprovenanced-diagnostic sub-class A this repo already names.

**No gate packet was run, and none is owed on the shadow→advisory question** — that question is not live for a
candidate-stage head, and promoting it would reverse an operator decision. The operator's instruction (*run the
packet, report, promote nothing*) lands on **promote nothing** by a shorter route than expected.

The two ETH 15m heads **actually** at shadow are no better: `eth-regime-15m-lgbm-xasset-v1` macro_f1 **0.4817**
and `eth-regime-15m-lgbm-selfonly-v901ctrl` **0.4576** (n_eval 35,054 each) — at or below chance for a two-class
head, both saturated (`recall_volatile` 0.935/0.949 against `precision_volatile` 0.324/0.315). The demoted fc head
is 0.5840. ⚠️ **There is no `eth-regime-15m-lgbm-fc-pcv-v2` in the registry at all** — ETH never got the
fresh-data v2 that BTC and SOL both carry at bucket `LIVE`. Whether it should is real, open, and **not scheduled
here**. Row: `MB-20260822-ETH-15M-HEAD-IS-AT-CANDIDATE-NOT-SHADOW`.

⚠️ Read via `deployment_bucket` + `stage_history`, **not** `status` — every registry row reports
`status: candidate` / `stage: None` (item 2.3, root-caused and unfixed). Reading `status` would have said the
whole fleet is unpromoted.

---

## M20 readiness — the resume path (added 2026-08-22, `s4-pairs-control`)

Full assessment, with every population stated:
[`docs/research/m20-readiness-2026-08-22.md`](../research/m20-readiness-2026-08-22.md).
Read it before scheduling M20 work — it exists so this is not re-derived.

**M20's done-condition is 85.3% resolved** (52 legs × 9 levers = 468 cells; 399 resolved,
**69 remaining**). The remainder is three different kinds of thing and **must not be
scheduled together**:

| kind | cells | needs |
|---|--:|---|
| **INFRA — buildable now**, `blocked:no_free_lane_candle_feed` | **25** | a non-crypto candle source |
| **WORK**, `pending` | 20 | ordinary pipeline runs, no new infra |
| **WAITING**, `insufficient_lifetime_trades` / `insufficient_base` / `insufficient_oos_base_at_derived_split` | 15 | soak depth — **nothing accelerates it** |
| other blocked | 9 | case by case |

⚠️ **The single biggest blocker is infrastructure, not research.**
`bracket_geometry :: blocked:no_free_lane_candle_feed` is **25 cells — 36% of the remainder
and 51% of everything blocked.** The free lane is `data.binance.vision`, a Binance **crypto**
archive, and `scripts/ops/fetch_backtest_candles.py` offers only `bybit` and `binance_vision`.
**All 25 blocked legs are non-crypto** (MES/MGC/MHG · 18 US equity/ETF legs · XAUUSD) with
**zero** crypto among them, so this is source COVERAGE, not a missing fetcher.

**Three of the four pieces already exist:** `yfinance>=0.2.0` is a declared dependency,
`scripts/research/m27/fetch_yfinance_5m.py` already emits the harness schema, and the
`MES→ES=F` / `MGC,XAUUSD→GC=F` / `MHG→HG=F` map exists **in the dashboard repo**. Missing:
`1d` support (**21 of the 25 legs are `1d` or `1h`**) and the map ported here.
⚠️ Yahoo returns **HTTP 429** from the sandbox — reachable but throttled; a runner is the
actual free lane and **that must be proven there, not assumed**.

**One feed serves two threads.** M31 P5 § 5 says 2a's artifact and 3b's walk-forward
*"should share one sweep rather than being scheduled apart"*, and 3b needs per-leg candles at
the live timeframes — the same gap.

**DECISION OWED (source choice), stated not resolved:** yfinance (free, already a dependency,
puller + map exist; unofficial, rate-limited) · Alpaca historical (official, creds present,
good US equities — **cannot serve MES/MGC/MHG/XAUUSD**) · IBKR (serves all of them, but it is
the money box and pacing-limited).

**Depth vs quality.** 2b is a soak-DEPTH problem. Items **1.8** (58.7% of adjudicable
`reconciler_filled` closes actually hit a declared level) and **G.1** (24.1% measurable) are
soak-QUALITY problems on the same rows. Depth accrued under them is depth M20 cannot use —
which is why those items sit on the M20 path rather than beside it.

⚠️ **"Clear the workplan" ≠ "the infra is correct."** The workplan cites 28 backlog ids (21
open) against **401** open/kept_open rows: **380 (94.8%) of the open set is outside it**,
including **8 critical** and 85 high. That is by design (the 2026-08-21 triage), but the 8
criticals are heavily M20-adjacent — over-covered IB stops in disjoint OCA groups, a
remediation that cancelled the journal-matching leg, `ict_scalp` with no take-profit close
path, 22 of 34 open trades closable only by a level fixed at entry.


## Phase 2 — the ML fleet and the seven items the 18:45Z rewrite dropped

**Operator direction 2026-08-21T~19:5xZ supersedes the previous "Parked" framing.** That
section said the ML fleet and the structural classes *"stay recorded in the backlogs —
Phase 0 dispositions them; it does not schedule them."* The operator has asked for the ML
work and the outstanding builds to be **scheduled**, not merely dispositioned. They are
scheduled here.

⚠️ **These seven were named in the predecessor plan and are absent from the 18:45Z
rewrite.** All seven were re-read from the backlog files on `1b05353` before being listed —
each exists, each is `open`, each carries `resolution_criteria`. None is being re-derived
from memory.

| # | Item | Row | File · sev | Tier | Status |
|---|---|---|---|---|---|
| 2.1 | ✅ **RESOLVED — and this row was STALE IN THE BLOCKING DIRECTION, which is the dangerous one.** It read *"93.6% / 3.09 GB free … ◐ MEASURED, not fixed — **needs operator call**"* while **three other surfaces already said otherwise**: the backlog row is `status: resolved`, the ROADMAP ledger records session `s1-q9kso`'s cleanup (**2.7 → 5.0 GB free, 95% → 89%**), and the live trainer measures **89.1% used / 5.26 GB free of 48.28 GB** with its mirror publishing at **age 66 s** (read 2026-08-22T18:1xZ). ⚠️ **The cost of the staleness is specific, not cosmetic:** this is the queue the next session reads, so a session planning M20's remaining work would have found the trainer marked unavailable and skipped the one sweep M31 P5 § 5 says 2a and 3b *"should share."* **The trainer is available.** ⚠️ **The operator call it was waiting on was ANSWERED, in the opposite direction to the plan:** the audit concluded **do not unpin** — the GC is pin-bound (`--min-age-days 0` gives an identical candidate set) and of the 25 non-canonical pins, `v520` backs the live BTC advisory head, `v530` the SOL advisory head, `v521` the ETH 15m head. ~2.9 GB theoretically available there against ~2.3 GB from housekeeping that risks nothing. Housekeeping was taken; the pins stand. ⚠️ **What does NOT reproduce:** the row's decay trend (free_gb 3.15 → 3.09 → 2.87, ~0.28 GB/24h). Post-cleanup the later reading is **higher** (5.00 → 5.26). That is **n=1 and not a trend** — it refutes *"still degrading"*, it does not establish *"recovering"*. Re-read before relying on headroom for a large sweep. | `MB-20260821-TRAINER-DISK-93PCT-AND-THE-GC-CANNOT-HELP` | ml · high | 1 (trainer is autonomous) | ✅ |
| 2.2 | **A cycle trained zero and seven manifests are stale** — 5 refusers, `cycles_24h` 2, 68 manifests otherwise OK. Downstream of 2.1 if the refusals are disk-caused; **that is the first thing to test, not assume.** | `MB-20260821-CYCLE-TRAINED-ZERO-AND-SEVEN-MANIFESTS-STALE` | ml · medium | 1 | ✅ PREMISE REFUTED #10125 |
| 2.3 | **The registry reports `status: candidate` for every model** — confirmed live this session: per-row `status`=candidate ×95 and `stage`=None ×95, against `deployment_bucket` OFFLINE 64 / SHADOW 28 / **LIVE 3**, with `stage_history` present on only 26 of 95. The enrichment is right and the raw field is a lie, so any consumer reading `status` sees a fleet with nothing promoted. | `BL-20260821-REGISTRY-STATUS-SAYS-CANDIDATE-FOR-EVERY-MODEL` | health · high | 1 | ◐ ROOT-CAUSED #10123 — fix is a consumer-contract change |
| 2.4 | **R and dollars disagree in sign** — a per-strategy `expectancyR` whose sign contradicts its `totalPnl`. Either the R normalisation or the PnL is wrong, and **both feed promotion decisions.** Gates any R-based gate. | `PB-20260821-R-AND-DOLLARS-DISAGREE-IN-SIGN` | performance · high | 1 to measure | ◐ ROOT-CAUSED #10118 — two hypotheses refuted first |
| 2.5 | **The auto-merge relay triggers on a single shared file**, so two concurrent requests conflict by construction. Its own criteria demand a **two-branch positive control**, not a code read. | `BL-20260821-AUTOMERGE-TRIGGER-IS-A-SINGLE-SHARED-FILE` | health · high | 1 | 🔄 **Fix shipped; row OPEN on criterion 3.** Trigger moved to `.github/pr-automerge-requests/**` (legacy path kept for a transition window); criterion 4 now TESTED not read (the path appears exactly twice in the workflow — a comment and the filter). Proved the DEFECT and the FIX with a real two-branch git A/B: shared file → CONFLICT on exactly that path, per-request dir → CLEAN. ⚠️ **Then PROVED ITSELF, and exposed a defect in my own fix**: the first glob was `**`, which matched the directory's own README — GitHub fired the relay and armed auto-merge on this very PR at 10:46:15Z (`auto_merge_enabled`, github-actions[bot]). Good news, the trigger demonstrably fires on the new path at zero cost; bad news, **a trigger a doc edit can fire is one that merges PRs nobody asked to merge**. Narrowed to `*.txt`, auto-merge disabled, merged by hand. Only remaining step: drop the legacy path once no open PR carries it. |
| 2.6 | **The Bybit bracket audit has no price axis** — it grades that a bracket exists, never that it sits where it was declared. A clean result is currently compatible with every stop being in the wrong place. **Directly relevant to 1.1**, which is why it is not merely tidy. | `BL-20260821-BYBIT-BRACKET-AUDIT-HAS-NO-PRICE-AXIS` | health · medium | 1 | ✅ #10118 |
| 2.7 | **The deploy wrapper asserts a bounce it did not do** — one fixed sentence on both the deployed and the no-op path. Every "deployed and verified" claim in this programme rests on that sentence. | `BL-20260821-DEPLOY-WRAPPER-ASSERTS-A-BOUNCE-IT-DID-NOT-DO` | health · medium | 1 | ✅ #10117 |

**Sequencing note.** 2.6 and 2.7 are *upstream of trusting this plan's own verifications* —
2.7 is how a deploy is confirmed and 2.6 is how a bracket is confirmed — so they run early
in the autonomous lane despite being `medium`. Severity ranks impact; it does not rank
order.

The remaining structural classes (built-and-never-wired, written-but-never-read, collapsed
state, alarm-that-cannot-fire, provenance decay, `close_reason` as a KPI) stay in the
backlogs and are dispositioned by 0.1. **0.3 lands the one structural fix whose detector
already exists.**

---

## Tier-3 pre-approvals — operator, 2026-08-21T~20:0xZ

**The default posture is unchanged: prepare only, flip nothing.** For 1.1, 1.2 and
anything not named below, the overnight deliverable is *the exact diff plus the evidence,
waiting for a yes.*

**Three carve-outs are pre-approved, each CONDITIONAL.** They are written here rather than
left in chat because a conditional Tier-3 approval that lives only in a conversation is
indistinguishable, later, from one I invented. The conditions are deliberately written so
that **failing them is a reportable outcome, not a judgement call I get to make at 03:00.**

| # | Pre-approved | The condition — ALL of it must hold | If it does not hold |
|---|---|---|---|
| **T.3** | Demote `slv_trend_1h` to `execution: shadow` | The zero-wins measurement **reproduces on a stated population** — n, window and account named — and the leg is genuinely routed to real money. `execution: shadow` only; the strategy keeps logging order packages. | **Report the measurement and change nothing.** A leg that turns out to be already-shadow, paper-only, or n-too-small is a finding, not a demote. |
| **T.4** | Promote the ETH 15m regime head shadow → advisory | The gate packet returns **`ready: true`** on its own criteria — `oos_edge` positive, `drift_clean` passing, `live_parity` sound, soak duration met. The same bar SOL cleared 2026-08-02 (#8294). | **Abstain and post the packet.** ⚠️ **Marginal is a NO.** BTC's own `-v2` swap hovered KS 0.20–0.25 across two days before clearing at 0.1269 — the precedent is to wait, not to round in favour. |
| **T.2** | Hedge mode on `bybit_1` + the `bybit_position_idx` resolver | `bybit_1` confirmed **demo** at the time of the change **from a read, not from this file**, and the allowlist ships **empty** — byte-for-byte no behaviour change until a later, separately-approved scoping. STEP 0's read surface (item 0.6) must exist and answer first. | **Build the read surface and stop.** If `bybit_1` cannot be confirmed demo from a read, that is a blocker, not a formality. |

## Operator decisions — 2026-08-22T~07:0xZ (four, answered at session close)

**These supersede the conditional pre-approvals above where they overlap.** Each was
answered by the operator directly; the evidence that unblocked #1 was gathered in-session
and is stated so it can be re-checked rather than inherited.

| # | Decision | Evidence / basis | What the next session may do |
|---|---|---|---|
| **T.2** | ✅ **Condition MET — SHIPPED INERT** (see session log) | A LIVE read of `/api/bot/config` (not this file): `bybit_1 account_class = "paper"`, `bybit_2 = "real_money"`. The 0.6 identity surface adds `shared_uid_groups: {}`, `unread_bybit_accounts: []` — three distinct UIDs, so `bybit_1` cannot reach the mainnet account. | Ship hedge mode + the `bybit_position_idx` resolver with an **EMPTY allowlist** (byte-for-byte no behaviour change). Constraint 1 still binds: a live positive control + a stated population BEFORE it counts as done. ⚠️ Re-read the account class at the time; do not inherit this line. |
| **Trainer disk** | ✅ **DONE — and the audit said DO NOT unpin** | 94.0% used / 2.87 GB free and falling; 111 of 115 dataset versions pinned by 41 manifests, so the GC cannot reach them. ⚠️ The disk→training-refusals link is **REFUTED** (`#10125`) — this is a standalone capacity item, not a cause of the refusals. | Audit which of the 41 manifests still genuinely need their pins, unpin the dead ones, let the existing GC reclaim. Reversible; no provisioning, no budget. Trainer is autonomous territory. |
| **#10081 · #10068** | ✅ **Drive both to green and merge** | Both are stale (35 / 40 commits behind) with **zero CI runs** — a bot-authored head never arms CI, so neither is "green", it is *unchecked*. Verified: **neither PR touches `src/` or `config/`** — #10081 is docs + automation artifacts, #10068 is research tooling + tests + docs. Merging both is **Tier-1**. | Refresh onto `main`, push one ordinary commit to ARM CI, mark non-draft, merge on verified green. |
| **Item 1.0** | ✅ **Pre-approved to ship on evidence** (Tier-2) | The 60 s requirement is still breached: `max_interval_ms` 84–89 s, reached within 50 passes. | May ship WITHOUT a further OK **once** it holds a stated-population measurement AND a live positive control showing `max_interval_ms` actually falls. Report the result; **revert if it does not.** Evidence first, then ship — not the reverse. |

⚠️ **Merging #10081 is NOT implementing it.** The PR is a **proposal document** (`docs/design/ib-protective-price-reconciler-PROPOSAL.md`); no code ships with it. The `apply`-mode repair touches IB protective legs and remains **Tier-3 and unbuilt**. The proposal's own § 7 says the tolerance is **unchosen** — n=3, one account, one day — so `annotate` runs first to produce the distribution, then `apply` is decided on it. That staging is the PROPOSAL's design, not a narrowing of the operator's approval.

---

**Three constraints binding on all three:**

1. **The pre-approval covers the change, not the verification.** Each still carries its
   blast-radius bar from the practices table, and T.2 touches the order path — so a live
   positive control and a stated population are required *before* it counts as done.
2. **`bybit_2` is mainnet and is not in scope for any of these.** T.2's safety rests
   entirely on `bybit_1` being demo; that is a fact to re-read at the time, not to inherit.
3. **A condition I am unsure about resolves to NO.** These were granted so work does not
   stall overnight, not so a marginal call gets made while nobody is awake. Reporting
   *"condition not met, here is what I measured"* is a **success** of this arrangement.

---

## Operator decisions — 2026-08-22T~11:1xZ (four more, answered mid-session)

⚠️ **SUPERSEDED IN PART — the 18:3xZ block below is the CURRENT direction.** These still supersede the 07:0xZ block, and the ordering call (*bugs and technical blockers before research*) still stands; where the two overlap, the later block wins.

| # | Decision | What was asked | What the next session must do |
|---|---|---|---|
| **Ordering** | ✅ **Bugs and technical blockers BEFORE research** | Which front next, given Phase 0 is closed and the research items (2.1–2.4) are open. | Do **not** open a research item while a CRITICAL exit/protection row is open. The cluster is: `ict_scalp` has no take-profit close path · IB protection is price-blind · 22 of 34 open trades have no decision-driven exit · item 1.1. |
| **`slv_trend_1h`** | ✅ **INVESTIGATE — keep it LIVE** (my demote recommendation was NOT taken) | I recommended demoting it to `shadow`. | **Do not touch its `execution:` gate.** Investigate why it behaves as it does and report; a demote needs a fresh Tier-3 approval on that report. |
| **ETH 15m (T.4)** | ✅ **Run the gate packet, REPORT, promote NOTHING** | T.4's conditional pre-approval. | Generating and reading the packet is Tier-1. The promotion itself stays operator-gated — bring the packet back, do not act on it. |
| **MGC / MES targets** | ✅ **Attach MES · CLOSE OUT MGC** | I recommended attaching both declared targets, then flagged that MGC's declared TP (4393.02) sits **233.88 pts BELOW** market (4626.9) — attaching it would be a MARKETABLE limit that instantly flattens 95 contracts. | Both are **scheduled for the Sunday 2026-08-23 22:00Z reopen** (COMEX/CME are shut; `venue_session` reads `closed` and the gate would defer). MES = `attach-ib-target`, MGC = `flatten-ib-position`. See "Scheduled broker actions" below. |

⚠️ **The MGC decision changed shape on evidence.** The operator's first answer was *attach both*; the hazard above was surfaced before acting, and the answer became *"add the mes, and let's just close out the mgc trade in that case"*. This is `BL-20260818-ATTACH-IB-TARGET-NO-THROUGH-MARKET-REFUSAL` firing live — the refusal exists precisely because a declared TP can drift to the wrong side of market while the position runs.

### Scheduled broker actions — Sunday 2026-08-23, 22:00Z (18:00 ET reopen)

Both are **Tier-2, operator-approved above**, both `apply: true`, both dry-run-clean already:

| action | issue (dry run) | payload | dry-run state |
|---|---|---|---|
| `attach-ib-target` `ib_paper` / **MES** | #10137 | `oca_group: oca-protect-336`, `declared_tp: 8390.59025`, `qty: 15.0`, `trade_id: 4350` | `state: ready` — all four refusals passed, `orders_on_symbol: 1` |
| `flatten-ib-position` `ib_paper` / **MGC** | #10138 | `SELL 95.0` against `long 95.0 @ 4367.05073684` | live position confirmed; planned close read back |

⚠️ **Two caveats to carry, not to re-derive:**
- The MGC dry run did **NOT** confirm the venue-defer prediction — it never reaches `IBClient.close`, so the session gate is unexercised on that path. The Sunday timing is what makes the point moot.
- MES's **resting stop is 7516.5 against a declared 7533.696** — a 17.196 pt / **$1,289.73** divergence. That is #10081's subject and is **deliberately untouched** by the target attach; do not fold the two.

**Prerequisite, now shipped:** `attach-ib-target`'s apply path connected on the trader's own execution clientId and could never have worked (`BL-20260822-ATTACH-IB-TARGET-USES-TRADER-CLIENTID`). Fixed in this PR. **The Sunday MES run is the live positive control that row is still owed.**

---

## Operator decisions — 2026-08-22T~18:3xZ (three, answered mid-session)

**These are the CURRENT direction and supersede the 11:1xZ block where they overlap.**

| # | Decision | What was asked | What the next session must do |
|---|---|---|---|
| **Candle source** | ✅ **yfinance** | Which non-crypto source closes the 25 `bracket_geometry :: blocked:no_free_lane_candle_feed` cells — yfinance vs Alpaca historical vs IBKR vs defer. | Build the free-lane feed on **yfinance**. Three pieces exist (`yfinance>=0.2.0` declared · `scripts/research/m27/fetch_yfinance_5m.py` already emits the harness schema · the `MES→ES=F` / `MGC,XAUUSD→GC=F` / `MHG→HG=F` map, **in the dashboard repo**). Missing: **`1d` support** (21 of the 25 legs are `1d`/`1h`) and the map ported here. ⚠️ **Prove it on a RUNNER** — Yahoo returns HTTP 429 from the sandbox. ⚠️ Carry the caveats: unofficial API, rate-limited, and the futures tickers are **continuous proxies**, not the exact MES/MGC contracts (defensible because R-normalised geometry is insensitive to contract size, and the dashboard already documents `ES=F` as deliberate — but say so, don't bury it). **Sequenced AFTER the criticals**, per the third decision. |
| **Item 1.8** | ✅ **Tier-2 APPROVED — forward-fix AND relabel history** | The exit-reason re-classification is a money-DB writeback. | ✅ **BOTH SHIPPED** this session. Forward fix in `_sweep_pending_pnl_from_bybit`; historical pass is `scripts/ops/reclassify_frozen_exit_reasons.py`, **annotate-first and provenance-scoped** per the operator's own steer that *"the 91 broker-truth rows are a much stronger case than the 90 resting on estimated prices."* ⚠️ **The historical pass has NOT been run against the live journal** — it is staged, and `--apply` is required to write. Run `--provenance broker_truth` first, read the annotate JSONL, then widen only if warranted. |
| **Next unit** | ✅ **The 8 critical rows outside the workplan** | Where to spend the next unit: the criticals · the candle feed · the 20 pending cells · a wider backlog triage. | Work the **8 critical open rows the workplan does not cite** — over-covered IB stops in disjoint OCA groups · a remediation that cancelled the journal-matching leg · `ict_scalp` with no take-profit close path · 22 of 34 open trades closable only by a level fixed at entry · the IB broker-pnl reader with no caller · mirror legs diverging in exit mechanism · price-blind protection coverage · `attach-ib-target` unable to express `filled`. The operator's framing: *"the infra is not working correctly while these stand."* |


## The autonomous lane — what runs overnight, and where it stops

**Purpose.** The operator may not be awake. This section says exactly what proceeds
without them, what must wait, and what is merely waiting on the clock. **The stopping
rules above still bind; this section only fixes the ORDER.**

### The three lanes

| lane | rule |
|---|---|
| 🟢 **AUTONOMOUS** | Tier-1: measurement, read surfaces, docs, tests, CI, trainer VM. Ship it, verify the post-state, log it. |
| 🔴 **OPERATOR-GATED** | Tier-3, and Tier-2 that touches the live order path. **Prepare the exact diff and the evidence, then STOP.** Never flip a Tier-3 gate on my own reading. |
| 🟡 **TIME-GATED** | Correct but not yet answerable — needs elapsed time or accrued rows. Re-read on a cadence; do not busy-wait, and do not assume the answer. |

### Order of work

**First, because it is time-gated and the clock has started:**

| when | item | lane | what a wrong answer costs |
|---|---|---|---|
| T+30 min, then hourly | **T.1 live falsifier.** Off-loop `fetch.15m` must fall from **1.002/pass** toward `interval/90` ≈ 0.47 on `/api/diag/tick_cost`. ⚠️ Read it **beside `passes`** — the counters are per-process and the trader restarts on every merge to `main`, so a short process can read reassuringly. **If it stays at ~1.0 the change did not take, and I report that.** | 🟡 | a merged change assumed to work |
| each read | **G.1 — `pnlCoverage` direction.** Must start rising after the hourly-fills fix. Not the level, the **direction**. | 🟡 | 1.1 and 1.2 become unmeasurable |

**Then, in this order — each is Tier-1 and none needs the operator:**

1. **2.7** deploy wrapper — *everything downstream claims "deployed" using this sentence.*
2. **2.6** bracket audit price axis — *1.1's evidence is only as good as this.*
3. **0.1 Step 0** status enum — *until this lands, no backlog count in this file is reproducible.*
4. **0.6** the three missing read surfaces — *each unblocks a decision already made.* Includes the `bybit_position_idx` read that T.2's STEP 0 needs.
5. **2.3** registry `status` — *cheap, high, and it misleads every ML consumer.*
6. **2.1 → 2.2** trainer disk, then the stale manifests — *test the causal link, do not assume it.*
7. **2.4** R-vs-dollars sign — *measure it; the fix may be Tier-3, the measurement is not.*
8. **2.5** auto-merge relay — *two-branch positive control, per its own criteria.*
9. **0.2 → 0.3 → 0.4 → 0.5** the rest of the cleanup gate.

**Then stop.** 1.1, 1.2, T.2, T.3, T.4 are 🔴. **Do not start them.** The overnight
deliverable for each is *the evidence and the exact diff, waiting for a yes* — which is
what T.1 looked like at 18:00Z and why it could ship in minutes once approved.

### What must NOT happen overnight

- **No Tier-3 gate flipped**, no `config/accounts.yaml` `mode:`, no `execution:` change, no risk cap, no model promoted past shadow.
- **No new guard, doc, or backlog row *about* the backlog.** Phase 0 is a retirement pass; that warning is the plan's own and it binds hardest when nobody is watching.
- **No concurrent session spawned.** One at a time is now policy; close with a handoff instead.
- **No "assume it deployed".** 2.7 exists because that assumption is currently unfalsifiable.

## Standing watch (glance; do not re-derive)

- **`pnlCoverage`** must start rising post-timer-fix. This is G.1's signal.
- **`pairs_soak`** — strand count unchanged until hedge mode lands; exposure per
  strand ~one tick, not ~60 min.
- **Exit-eval `requirement_state`** — read `max_interval_ms` **beside**
  `intervals_measured`. Per-process, so a short process reads reassuringly.
- **`alpaca_live`** — silenced 2026-08-21 by operator decision, both skips live and
  verified. Reachable with a **$0.10** balance: unfunded, not down.

## Session log

| Date | Session | Items advanced | PRs |
|---|---|---|---|
| 2026-08-22 | `s4-pairs-control` | **The owed pairs live control PASSED, and item 1.1's residue resolved onto a mechanism nobody had named.** **1.6 control PASS:** across all 189 `pairs_%` packages (`filter_state`+`order_state` asserted), **pre-deploy 187 → ZERO** pairs-native reasons, **post-deploy 2 → ONE** (`pairs_half_open_cleanup` on `pkg-a7041e63d2ac491b`, trade 4942 carrying the same reason **242 ms** earlier). Separated from the sweep on **two** grounds — timing and the reason value. ⚠️ **The two earlier not-yet-observable attempts are now diagnosed, not just repeated:** the sleeve was **flat on all four pairs** and had evaluated **4 bars, all `skip_flat`**, so no close could occur; and **`half_open` is the likelier trigger, not `close`**. **1.1 residue:** the surviving hypothesis (`_resolve_protective_levels` grading against another trade's bracket) is **REFUTED** — all three packages carry the correct `linked_trade_id` and levels identical to the trade's, so that branch is unreachable. **The classifier never ran:** the no-price fallback hard-codes the reason and `_sweep_pending_pnl_from_bybit` fills the price later without re-classifying. **91 of 155 (58.7%)** broker-truth `reconciler_filled` rows reached a declared level; **181 of 181** mislabelled rows carry no `exit_reason_source` — a 100% signature — and the **14** that did reach the classifier **all still grade `None`**, so later price refinement contributes **zero** rows and the prior session's elimination stands. 41.3% genuinely-between **independently reproduces** 1.1's 41.0%. ⚠️ **Quote the 91, not the 181.** **1.4:** the size **IS** the margin ceiling (`position_size == max_qty_by_margin` to full float precision, `kind: venue_available`), and the venue-max clamp has been **inert for nine days** — a live bounce at 10:30:58Z today with the clamp's WARNING absent. ⚠️ **Which of the three paths to `venue_max=None` was taken is undeterminable from the journal** (one logs nothing, one logs at DEBUG, one returns `None` by construction), so **observability is the blocking step and it is Tier-1**. **Two of that row's own claims corrected:** 21 rejections over 2026-07-30..08-22 (not 20 over 08-12..14), and the *"accepted orders sit near the ceiling"* claim is **unsupported at the median** (4,574.3 = 20.8% of cap; 2 of 22 above 80%). ⚠️ Therefore *zero rejections* alone can never prove 1.4 fixed. ⚠️ **A correction landed in the same session:** the chain's SHAPE was already in `provenance.py`'s docstring and I first called it unnamed — **withdrawn**; what is new is that the 2026-07-30 fix closed the **PnL** half and left the **label** half, that it is not confined to demo, and the scale. That docstring's own step 1 was **stale** (the demo branch was narrowed to resolve from fills) and is fixed. **The only `src/` edit is that docstring — no behaviour change.** The `exit_reason` re-classification is Tier-2 and prepared only, with the `is_reduce_leg` hazard recorded. Log: [`S-PAIRS-CONTROL-EXIT-RESIDUE-2026-08-22`](../sprint-logs/S-PAIRS-CONTROL-EXIT-RESIDUE-2026-08-22.md); evidence: [`exit-reason-frozen-at-close-2026-08-22`](../research/exit-reason-frozen-at-close-2026-08-22.md). | (this PR) |
| 2026-08-22 | `s3-exitpath` (2) | **G.1's premise restated on all three counts, and 1.7's headline count was my own over-report.** **G.1:** `pnlCoverage` is **not one number** — real money reads **0.78 lifetime / 0.90** on `/trades/closed`, while the blended alarm figure is dominated by `bybit_1` at **0.081** (84% of closes). The windows are **nested**, so a snapshot cannot show direction, and at real-money volume the post-fix cohort aged ≥24h is **n=5 — not answerable**. And the cause is **not fill capture**: `bybit_1` holds **541 fills**, the most of any account. The rejecter is `fills_pnl.py`'s **5% qty gate** — a netted close fills a position spanning N journal rows, measured median fill/trade **1.31–1.44 = a 31–44% overshoot, 6–9× the tolerance** → `candle_at_close`; `bybit_2`'s fills are partials that accumulate and it clears 0.622 on a fraction of the fills. **This is why item 1.1 is measurable on only 24.1% of the main path.** ⚠️ Ratios are per-symbol **medians, not a per-trade join**. Fix is **Tier-2/3 and deliberately not attempted** — widening the tolerance would stamp `exchange_fill` (a MEASURED grade) on a row whose economics were inferred, which is worse than the honest estimate it replaces. **1.7:** ⚠️ **three genuine gaps, not seven** — my detector grepped for a **helper's NAME** rather than for the **outcome**, and four sites close the package by calling `update_order_package` directly. All eight now classified and annotated in place. ⚠️ **I assumed 'an adopted orphan has no package' and the measurement refuted it — 44 of 74 carry one.** The three gaps stay Tier-2 (a cascade advances the monocle gate's unblock by ~1 tick), with the counter-argument recorded: the sweep alerts on **every** miss, so closing them quietens a standing alarm. **Also fixed 0.3's stale status glyph** — its text and the session log both said DONE while the Status column read ☐. | (this PR) |
| 2026-08-22 | `s3-exitpath` | **Item 1.1 re-measured on broker truth — and the framing INVERTS.** Of 323 main-path closes only **78 (24.1%)** have a broker-truth exit price. Judged by `_classify_broker_exit`'s **own** inequality: **46 (59.0%) reached a declared level** (stop 40 / target 6) against **15 (19.2%) labelled** decision-driven — **an under-attribution gap of 31 rows; brackets reach a level ~3× more often than the journal says.** So 1.1 is substantially an **attribution** defect, not a mechanism one. ⚠️ **I retract my own first number:** a symmetric ±0.10-risk band gave 72.7%, which wrongly counts a fill stopping SHORT of the stop as a stop hit — the production inequality gives 59.0%. **What survives:** stop:target **6.7:1** on real fills, and 41.0% genuinely exiting between the brackets. Two candidate causes **REFUTED** (wrong level source — package/trade levels agree 43/44; different close path — `closed_by` identical on both groups); the backfill hypothesis is **UNDERPOWERED and labelled so** (Fisher p=0.111, n=44), leaving a clean 3-row residue — **now RESOLVED, and the surviving hypothesis REFUTED (2026-08-22, `s4-pairs-control`).** It was *not* `_resolve_protective_levels` grading against another trade's bracket: each of 4928 / 4733 / 4180 has a linked package with the correct `linked_trade_id` and positive `sl`/`tp` **identical** to the trade's own, so that fallback branch is unreachable — and on those levels all three satisfy the production inequality for `'sl'`. **The classifier never ran.** `_close_trade_from_order_status`'s no-closed-pnl fallback (`order_monitor.py:6033`) hard-codes `reconciler_filled` and leaves `exit_price` NULL; `_sweep_pending_pnl_from_bybit` (`:8323`) later fills the price and **never re-runs `_classify_broker_exit`**. Measured: **91 of 155 (58.7%)** broker-truth `reconciler_filled` rows reached a declared level, and **181 of 181** mislabelled rows carry **no** `notes.exit_reason_source` — a 100% signature. The 41.3% genuinely-between **independently reproduces this item's own 41.0%**. Tracked for fix as `BL-20260822-EXIT-REASON-FROZEN-WHEN-PRICE-ARRIVES-LATE` (Tier-2, prepared not shipped). **Item 1.6 TRACED AND FIXED, and its feared half REFUTED** — `_close_pair` never routes through the only cascade site (zero cascade calls in `pairs_executor.py`), with both arms reconciling **exactly** (57↔57, 120↔120) and 99 pairs-native exit reasons reaching **zero** packages. The monocle gate **cannot see a pairs leg** (one call site, `pipeline.py:611`, which `run_pairs_tick` never reaches) — so the sleeve is **not** propped up by the repair sweep. **The sweep was not changed.** ⚠️ **My own test caught a weakness in my own fix** — the isolation depended on the helper's guard, so it is now enforced at the call site too. 7/7 fail pre-fix, 7/7 pass post-fix. **Generalised:** only **1 of 8** literal close sites cascades — pairs was the instance, not the class (and that 8 is a **lower bound**: 20 further `update_trade` sites pass a dict variable the matcher cannot read). ⚠️ **Live positive control ATTEMPTED TWICE at session close — NOT YET OBSERVABLE** (deploy confirmed by ancestry; zero pairs packages closed since, newest 12:10:22Z). The armed 17:33Z check-in was **deleted** so nothing fires into a closed session — re-take it from the row. Log: [`S-EXIT-ATTRIBUTION-2026-08-22`](../sprint-logs/S-EXIT-ATTRIBUTION-2026-08-22.md); evidence: [`exit-attribution-broker-truth-2026-08-22`](../research/exit-attribution-broker-truth-2026-08-22.md). | #10145 |
| 2026-08-22 | `s2-exitclust` | **The exit/protection cluster, and two premises that did not survive contact.** **(1)** `BL-20260820-PROTECTION-COVERAGE-IS-PRICE-BLIND` criterion 5 answered: **all three venues are price-blind** — Bybit's Full-mode branch returns `covered_qty == size` on any `stopLoss` *string* that is non-empty and not `"0"`; Alpaca's `protection_state` returns **booleans**. ⚠️ **The row's instance is `ib_paper`; `bybit_2` is MAINNET** — 9 live Bybit positions, 2 on real money. And the axis was not merely unread but **UNREADABLE**: no Bybit open-orders surface existed. Built `/api/diag/bybit_open_orders` (#10142) carrying **both** the Full-mode position stop and the Partial legs, because Full mode has no resting order and an orders-only surface would grade a protected position **naked**. Criterion 4 (the price axis on the *enforcing* path) is Tier-2/3 and deliberately **not** proposed. **(2) T.4's premise REFUTED** — the ETH 15m fc head is at **candidate**, demoted from shadow 2026-07-19 by operator approval on a powered NO_EDGE; "54 days at shadow" is days since **registration**. Promoted nothing. **(3) T.3 investigated, `execution:` untouched** — every account carrying `slv_trend_1h` is paper or dry-run, so T.3's own real-money condition fails; the `min_confidence: 0.3` floor landed 2026-08-01 and **works** (zero violations post-landing); post-floor n=3 is **underpowered and labelled so**; and **6 of 6** of its closes are `exchange_flat_reconciled`. **(4) New:** the stuck-cascade *second-line-of-defence* sweep carries **45.5%** of package closes and is **concentrated** — pairs 42/61 (`pairs_bnb_btc_a` **13/13**) against `trend_donchian_*` **0/14** — which rules out the tick-race explanation and points at the isolated pairs order path. ⚠️ **Three of my own errors, each caught before it reached a conclusion:** a falsification run that reported the wrong break's signature from a stale file; "the floor is unenforced", corrected only by unshallowing the clone; and reading a 422 body as an empty result. | #10142 |
| 2026-08-22 | `s1-q9kso` | **`attach-ib-target` was NEVER able to place a target** — its apply path called `ib_client_for(cfg, readonly=False)`, resolving the TRADER's execution clientId (497); IBKR refuses a duplicate rather than evicting, so the live MES apply (#10139) died on `Error 326` + a 120 s breaker trip **inside the ops process**. ⚠️ **No live harm — verified, not assumed:** `/api/diag/ib_state` all three clients `connected` / `consecutive_failures: 0`, `bot_running: true`, `tick_age_seconds: 1.2`. **The shape is the finding:** the DRY RUN never builds a client, so it reported `state: ready` with every refusal passed — a repair action for a live-protection gap that read as available right up to the moment it was needed, shipped 2026-08-16 and never once exercised against a running trader. Its sibling `flatten_ib_position.py` had `_ib_ops_client_id()` from the start **and a test asserting the band**; this one had neither. Fixed + falsified (2 failed pre-fix, 2 pass post-fix) and the test asserts the WIRING as well as the band — a correct helper that is never passed to the factory fixes nothing. `cancel_ib_order.py` checked and **not** affected (it connects as the owner deliberately, with an explicit `_TRADER_CLIENT_ID_CEILING`). **Four more operator decisions landed** (see the 11:1xZ block): bugs before research · `slv_trend_1h` stays LIVE · ETH 15m packet is report-only · **MES attach + MGC FLATTEN scheduled for the Sunday reopen** (MGC's declared TP is 233.88 pts the wrong side of market — attaching it would have been marketable). | (this PR) |
| 2026-08-22 | `s1-q9kso` | **Trainer disk DONE — 2.7 → 5.0 GB free (95% → 89%)**, and the audit reached the OPPOSITE conclusion to the plan: **do not unpin.** The GC is pin-bound (confirmed: `--min-age-days 0` gives the IDENTICAL candidate set), and of the 25 non-canonical pins `v520` backs the **live BTC advisory** head, `v530` the **SOL advisory** head, `v521` the **ETH 15m head T.4 is about** — ~2.9 GB theoretically available there against ~2.3 GB from housekeeping that risks nothing. Reclaimed instead: journald 400M · `/tmp` >7d 1.5 GB (28,625 files) · a dir named `SUPERSEDED_wrong_dataset` 50M · `git gc` 294M. Evidence proved untouched (datasets-out/experiments-runs/m20_exit_head unchanged, GC report byte-identical). ⚠️ **The plan's "41 manifests" does not reproduce** — the GC's own summary says `manifest_pins: 41, manifests_total: 76`: 41 is the count of distinct PINS. **0.3 DONE.** ⚠️ **Two errors of mine, both caught and corrected in-session:** (a) I attributed the unaccounted ~5 GB to `.git` as if measured — it was 339 MB; the real answer is `.venv` 5.4 GB, now closed by a byte-exact reconciliation (children 26.41 GB = parent 26.41 GB, 55 entries); (b) my heavy-job guard was `pgrep … \| head -5 && HEAVY=1`, which fires on no match because `head` exits 0 on empty input — a **pipe masking an exit code**, skipping `git gc` on a false positive. **Three of today's incidents are one class: an operation whose failure is indistinguishable from its success** (that pipe · the `curl` 403 a `\|\| echo '{}'` turned into "no check runs visible" · `check_suite.completed` reading green while `pytest-run` had 10 min left, on 3 PRs running). | #10129 · #10130 · (this PR) |
| 2026-08-22 | `s1-q9kso` | **1.0 DONE — root-caused, the row's own hypothesis REFUTED, shipped, live-controlled.** The queue timeout it blamed fires **0 times**; the cost was `_probe_liveness` running once per FETCH (n=75 / 2226 s = **21.9% of wall clock**, condemning branch **0**). Post-deploy: probe rate **0 / 1395 s** (~47 expected), `max_interval_ms` **89.35 → 39.64 s**, breaches **1.82% → 0%**. ⚠️ **Stated honestly: the breach-count comparison ALONE is underpowered at n=147** (expected 2.67, p=0.069). The powered tests are passes>30 s (0 vs 4.59 expected, **p=0.010** — and it is the mechanistically correct threshold given the derived identity) and the probe rate. **`interval = max(0, 30000 − pass_prev) + pass_curr` holds to ≤182 ms over n=990** — an identity, so cold start is excluded (pass #1 is 37–47 s but cannot breach). A per-pass BUDGET was considered and **rejected**: it caps the metric by skipping legs, worsening the latency it claims to fix. **T.2 SHIPPED INERT** — `bybit_position_idx` resolver + 5 call sites, EMPTY allowlist, byte-for-byte. ⚠️ The row's "SECOND DEFECT" (half-open check behind the bar dedup) was found **ALREADY FIXED** — verified in code, not assumed. | #10129 · (this PR) |
| 2026-08-22 | `s1-q9kso` | **1.0 root-caused, and the row's own hypothesis REFUTED.** The 60 s breach is *fully determined* by pass duration: `interval = max(0, 30000 − pass_prev) + pass_curr` reproduces every observation to **≤ 182 ms over n = 990** — it is a derivation, not a fit. **Baseline (state the population): the newest 1000 rows of `exit_interval_soak.jsonl`, 2026-08-21T23:26:33Z → 2026-08-22T07:51:23Z, 10 processes, n = 991 measured intervals** — mean 30.06 s · p90 38.2 s · p99 67.6 s · **max 89.35 s** · **18 breaches = 1.82%**; `pass_ms` p50 1.60 s · p90 10.97 s · max 69.14 s. Two sub-populations, separated: **pass #1 of a process is 37–47 s (8 of 9 over 30 s) and never breaches** (no prior interval), so cold start is NOT the cause; the 18 breaches sit at pass #6…#721, i.e. **steady state**. Cause isolated to `_probe_liveness` (above). ⚠️ **A per-pass wall-clock BUDGET was considered and REJECTED**: truncating a pass would improve `max_interval_ms` while making the *skipped legs'* real latency worse — gaming the metric. The fix removes work instead of hiding it. | (this PR) |
| 2026-08-22 | `wave0-8g7443` | **T.1 LIVE-FALSIFIED** (n=676: exit pass 42.3 s → 4.89 s, breaches 28.9% → 0.74%; re-confirmed on a 2nd process at mean 5.58 s) — ⚠️ **the 60 s requirement is STILL breached at the tail** (`max_interval_ms` 84–89 s, reached within 50 passes, so it is NOT a rare event): **1.0 is the next item**. Plan synthesized + 7 dropped items restored + autonomous lane added; **0.1 Step 0** (41 free-text `status` values → a 6-value enum, on the EXISTING claim-basis-guard); **0.6** all three read surfaces; **2.6/2.7** shipped; **2.1/2.3/2.4** root-caused; **2.2 premise REFUTED**. Close-out `doc-freshness` found **3 surfaces still calling the IB exclusion live — drift this session introduced**, incl. the function's own docstring. ⚠️ **T.2/T.3/T.4 pre-approvals NOT exercised.** | #10114 · #10115 · #10117 · #10118 · #10119 · #10120 · #10121 · #10122 · #10123 · #10124 · #10125 · #10126 |
| 2026-08-21 | `dcf5220b` | Review + report; Tier-2 fills timer + pairs reorder shipped **and post-state verified**; `alpaca_live` silenced; 16 rows filed; the predecessor plan | #10088 · #10095 · #10101 · #10106 · #10107 |
| 2026-08-21 | `wave0-8g7443` | T.1 root-caused → Tier-3 diff **approved, not implemented**; the TTL remedy measured and refuted; stake corrected to paper-only; T.2 STEP 0 re-classified as a missing Tier-1 read, not an operator block; 3 measurement-gap rows filed. `src/` untouched. | #10110 |
| 2026-08-21 | `wave0-8g7443` | **T.1 SHIPPED** (`1b05353`) — IB connector memo; re-verified on current `main` at n=433 before shipping; tests shown to fire pre-fix; a refuted-premise test from S-033 corrected after **CI caught what my local run had skipped**. Backlog population re-measured (378 → not reproducible; `status` is free text, 41 values) and 0.1 given a computable Step 0. **The seven items the 18:45Z rewrite dropped restored as Phase 2**, each re-read from the files. Autonomous lane written. | #10114 |
| 2026-08-21 | `spsxq6` | Workflow-catalog guard; required contexts corrected 15/9 → 3; OCI live falsifier run (both arms); self-ping sentinel fixed; **this rewrite** | #10094 · #10100 · #10104 · #10105 · #10109 |
