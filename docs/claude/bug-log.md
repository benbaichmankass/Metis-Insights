# Bug log

A running ledger of bugs found and fixed in this repo, plus the
architectural concern each one surfaces. Reviewed at the start of every
planning sprint to spot recurring trouble spots.

## How to append

Every time a bug is identified and fixed:

1. Add a row to the **Active bugs (chronological)** table below.
2. Fill every column. If you don't have a value, write `unknown`.
3. Tag with the architectural-concern category in column "concern" so
   `grep -i "<category>" docs/claude/bug-log.md` surfaces clusters.
4. Commit alongside (or immediately after) the fix PR. The bug log
   landing later is fine — what matters is it lands.

If a bug recurs, add a *new* row referencing the original (`see #BUG-NNN`).
Don't edit history.

### Architectural-concern categories

Pick the closest match. New categories are fine; just be consistent.

- `auth` — authentication, token handling, JWT, allowlist, session
- `config` — YAML / env var / settings precedence, defaults, drift
- `data` — OHLCV fetch, parsing, schema, DB, journals, caches
- `deploy` — systemd, sudoers, polkit, restart logic, install scripts
- `git` — gitignore, branch / merge / rebase, conflicts, force-push
- `markdown` — Telegram parse_mode, HTML escaping, template rendering
- `risk` — risk caps, position sizing, drawdown enforcement
- `telegram` — bot wiring, MagicMock stubs, command handlers, pings
- `tests` — fixtures, stubs, conftest, dependency leakage between tests
- `tls` — egress allowlist, cert chains, proxy, network reachability
- `ui` — web dashboard, HTMX fragments, static files, templates

## Active bugs (chronological, newest first)

| ID | Date | Sprint | Area | Symptom | Root cause | Fix (PR) | Concern | Notes |
|---|---|---|---|---|---|---|---|---|
| BUG-066 | 2026-05-10 | S-047 T6 (D8 — this PR) | architecture / spot-margin / strategy lifecycle | Meta-entry: family root cause for BUG-046 / BUG-049 / BUG-048 — three 2026-05-03/04 bugs whose symptoms looked unrelated (open packages stacking with no trades; VWAP going silent; trade #24 sitting open for 8 h while the wallet was flat) all stemmed from the same architectural gap: **spot-margin accounts on Bybit have no exchange-side close path** (no SL/TP, no `position` record), but the codebase had been built assuming there was one. Without a strategy-driven close + a reconciler that can see borrow lines as positions, every long-open spot-margin trade either silenced its strategy (BUG-046/049) or sat as a phantom DB-open while the exchange showed flat (BUG-048). | Three structural omissions across the live-order path: (1) **execute.py** submitted spot orders without `isLeverage=1`, so shorts borrow nothing — the bot didn't even know it had a borrow line. (2) **VWAP `monitor()`** returned only break-even verdicts; it had no close-path verdicts (`tp_partial`, `vwap_close`), so a spot-margin trade with no exchange-side SL/TP would never close. (3) **The reconciler** queried `get_positions(category=...)` for every account, which returns empty on spot-margin — so DB-open / exchange-open mismatches were invisible. The three bugs above were each a different visible symptom of the same structural hole. | S-047 T1–T6 (this sprint) — structural fix landed across 7 PRs: T1 (`config/accounts.yaml` declares `bybit_2` as `market_type: spot-margin`), T2 (`RiskManager.position_size` computes spot-margin sizing with USDT collateral + liquidation-buffer + borrow-fee budget, refusal shape mirrors the existing daily-loss-budget shape), T3 (`execute.py::_submit_order` adds `isLeverage=1` for spot-margin shorts; coordinator's direction-aware balance update follows), T4 (VWAP `monitor()` rewrite with full close-path verdicts), T5 (`order_monitor.py::_reconcile_open_trades` consults `_spot_margin_open_positions` synthesised from `get_wallet_balance` for spot-margin accounts; companion S-055 `_reconcile_orphan_borrows` and S-060 `_reconcile_orphan_positions` reapers), T6 (this PR — `docs/runbooks/spot-margin.md` + this bug-log entry). Subsequent reconciler hardening — S-058 (totalEquity fallback, PR #575), S-059 (stuck-strategy watchdog respects exchange position state, PR #582), and the closed-flat invariant reconciler now in alert-only soak (PR #679) — all build on the T5 reconciler's spot-margin awareness. | architecture | high-severity-by-direction (the 2026-05-03/04 outage was the visible tip; the structural hole would have re-surfaced with every new spot-margin pair until S-047 closed it). **Architectural lesson:** when an exchange contract differs across categories (spot vs spot-margin vs linear vs inverse), every code path that touches positions / orders / the close cycle must branch on the category. Pre-S-047 the codebase had partial awareness — `modify_open_order` knew spot couldn't take SL/TP (correctly refused), but `_submit_order`, the VWAP monitor, and the reconciler did not. Recurring shape with BUG-061 (spot Market orders rejected for SL/TP — same "spot-vs-derivatives contract drift" family). Defensive idea: a contract-coverage matrix maintained in `docs/runbooks/spot-margin.md` § 3 (now shipped) — every `(category, action)` cell lists the code path that handles it; a new category requires updating every cell. Cross-references: BUG-046 (gate-side symptom), BUG-049 (strategy-silence symptom), BUG-048 (env-render-drift symptom that masked the reconciler half), BUG-061 (spot SL/TP submit symptom on the unrelated cash-spot account `bybit_1`). |
| BUG-065 | 2026-05-10 | S-067 (silent-empty error path audit & hardening) | api / read-path / error handling | Multiple read-path endpoints under `src/web/api/routers/` and helpers under `src/web/runtime_status.py` were swallowing structural failures (schema mismatches, locked DBs, malformed YAMLs, psutil import failures) and surfacing them as fabricated zeroes / empty lists / empty dicts / `None` — indistinguishable from the legitimate "no data yet" / "0 readings" cases. Two canonical examples already on record: PR #627 (`/api/bot/positions` returned `[]` for the entire lifetime of the endpoint because a blanket `except Exception` swallowed a column-rename `OperationalError`), PR #629 (`/api/bot/signals` dropped `price` because the writer-alias fan-out wasn't covered, with no log to surface the gap). The 2026-05-10 24h trade-performance review found the same pattern in 5 more places affecting the dashboard's `pnl24h` header strip, the diag/journal endpoint, the diag/snapshot vm_health, the bot/config Settings tab, and the runtime-status producer's per-account live/dry override. | Recurring `except Exception:` / `except sqlite3.Error:` / `except (OSError, json.JSONDecodeError):` blocks that returned a "shape-correct empty" sentinel without logging. The shape-correct return tricked every downstream caller (and every test) into seeing the failure as a normal "no data" path. The architectural hole is that there was no contract for how a read-path helper should signal "I tried but the underlying source is broken" — only "I succeeded" or "I returned the right shape". | This sprint shipped 5 PRs: #642 (sprint prompt + audit `docs/audits/silent-empty-2026-05-10.md`), #643 (CP-2 — 3 trust-corroding fixes: `dashboard.py::_pnl_stats`, `diag.py::_journal_select`, `diag.py::_vm_health`), #644 (CP-3 — 2 trust-corroding fixes: `bot_config.py::_read_yaml` with new `config_load_errors` payload field, `runtime_status.py::build_status` `dry_run_overrides` block via existing `_swallow_runtime_status` helper), #645 (CP-3 borderline — log-only on 7 silent-but-correct sentinels under `dashboard.py`, `diag.py`, `bot_config.py`, `runtime_status.py`, `liquidity_state.py`), #646 (CP-4 — `scripts/check_silent_empty_in_diff.py` + `.github/workflows/silent-empty-guard.yml` CI guard with 13 unit tests + `# allow-silent: <reason>` override). #647 (CP-5 sprint close) + #648 (handoff prompt) closed the sprint. | data / config / observability | high-severity-by-direction (2026-05-10 24h trade-performance review surfaced corrupted dashboard P&L and unobservable open exposure during the window — see § 2 of the analysis). **Architectural lesson:** "shape-correct empty" sentinels on read-path helpers are a contract bug whenever the caller can't distinguish them from a real "no data" case. Every read-path endpoint needs three explicit paths: (a) success with data, (b) legitimate empty (e.g. DB file does not exist yet on a fresh install), (c) loud failure (logged + raised or 503). The audit doc lifts `_db_info_payload` as the canonical pattern for "endpoint whose job is reporting failures": surface the per-step error string into the payload rather than swallow it. Defensive idea (now shipped as CP-4): a CI guard that flags any new broad-except handler in `src/web/api/`, `src/units/db/`, or `src/web/runtime_status.py` unless the line carries an explicit `# allow-silent: <reason>` comment. Follow-up sprints filed in `docs/sprints/sprint-067-prompt.md` § 8 / `docs/claude/next-session-prompt.md` cover the live-order-path layer (`order_monitor.py`, `pipeline.py`, etc. — Tier 2 with operator ack per fix), the test-fixture extraction, the closed-→-exchange-flat invariant reconciler (Tier 2), the process-wide env-gate purge (Tier 2 — same risk class as PR #630), the deploy restart contract universalisation, the exchange-fills P&L attribution job, and the daily one-trade audit auto-task. |
| BUG-064 | 2026-05-09 | 2026-05-08-all-models-training (S-050 ship + ad-hoc turtle tune) | strategy / parameter tuning | Turtle Soup `atr_stop_mult=0.35` was on the wrong side of the parameter optimum on 38 months of BTCUSDT 15 m data: full-sample Sharpe +0.80, expectancy +0.16 R/trade, 51.5 % win rate — profitable but materially below what a single-knob change could deliver. No prior systematic tuning run on record for turtle_soup. | The 0.35 default was carried forward from `strategies/turtle_soup_mtf_v1.py:38-55` (PR C1, S-012) without empirical validation. At 0.35 the stop sits slightly past the post-sweep wick, capturing legitimate sweep depth as a "stop run" rather than a fakeout invalidation — i.e. trades that should have been winners exit at the stop. Sweep over {0.25, 0.30, 0.35, 0.45, 0.60} showed monotonic Sharpe improvement from 0.45 down to 0.30, with 0.30 sitting at the high-cadence edge of the 0.25-0.30 quality plateau. | PR #558 (squashed `9a7bdf3`) — `src/units/strategies/turtle_soup.py::_DEFAULTS["atr_stop_mult"]: 0.30` + explicit `atr_stop_mult: 0.30` line in `config/strategies.yaml::strategies.turtle_soup`. Provenance comment in the strategy module references this entry and `experiments/2026-05-08-all-models-training/RECOMMENDATIONS.md` § T4. Existing tests pass unchanged — the parameter is already test-isolated via `cfg.get("atr_stop_mult")`. | strategy / config | low-severity (already-profitable strategy nudged to better quality, not an outage). Expected impact: full-sample Sharpe +0.80 → +1.33, win rate 51.5 → 56.3 %, expectancy +0.16 → +0.27 R/trade, walk-forward OOS +0.25 → +1.22 (OOS *better* than IS — regime-robust). Cadence essentially unchanged (33 → 32 trades over 38 months). **Architectural lesson:** strategy parameters carried from a legacy port deserve at least one systematic sweep before they're treated as canonical defaults. Recurring trouble spot — the prior 2026-05-03 VWAP run found a similar "default on the wrong side of the optimum" shape for `ENTRY_STD_THRESHOLD`. Defensive idea: the recurring "Strategy Improvement Review" cadence in `milestone-state.md` § Standing sessions should include a "have we ever tuned X?" register so untuned parameters surface during planning rather than at year-2 audit time. ETHUSDT not tested in this run (no sandbox-reachable ETH archive); promote `0.30` across symbols only after an ETH re-validation. |
| BUG-063 | 2026-05-09 | 2026-05-08-all-models-training (S-050 early ship) | strategy / regime fragility | VWAP mean-reversion on BTCUSDT 5 m on a 38-month dataset (Jan 2023 → Feb 2026, 332 k bars from the qashdev/btc Binance Vision mirror) was **structurally unprofitable**: full-sample Sharpe -0.39, expectancy -0.007 R/trade, max DD -152 R. Same fragility the prior `2026-05-07-vwap-accuracy` run flagged in walk-forward (OOS Sharpe degraded to +0.24 on the last 30 % of the 365-day window) — but on a longer window the regime composition tilts even more against pure mean reversion. The 2024 ETF-driven trend, the late-2025 rally, and the early-2026 drawdown all punish a fade-to-VWAP without a regime gate. | The live strategy fades every σ-band excursion regardless of higher-timeframe trend context. In a sustained directional regime, fades against the trend get run over before reverting; the σ band fires repeatedly into the same losing side. The `2026-05-07-vwap-accuracy` run identified this and proposed a 4 h EMA-200 ±1 % HTF gate (Phase 2 / S-050) but gated it on "Phase 1 merged + ≥ 30 days live metrics" per `milestone-state.md`. The 30-day gate was waived this round once the 38-month backtest showed Phase-2 was no longer a quality lift but the difference between profitable and not. | PR #558 (squashed `9a7bdf3`) — `src/units/strategies/vwap.py::build_vwap_signal` accepts optional `htf_close`, `htf_ema`, `htf_band_pct` kwargs; new constant `HTF_BAND_PCT_DEFAULT = 0.02`. Gate fires between side-resolution and meta-build; HTF state recorded in `meta` (`htf_close`, `htf_ema`, `htf_band_pct`, `htf_blocked`) for the audit log. `src/runtime/pipeline.py::_build_vwap_signal` reads `vwap.htf_trend_filter.{enabled,htf_timeframe,ema_period,band_pct}` from `config/strategies.yaml`, fetches HTF candles, computes the EMA, threads through; HTF fetch failure degrades to no-gate (WARNING log) rather than blocking the strategy. New `tests/test_vwap_strategy.py::TestHtfTrendGate` — 9 tests covering each gate arm. Default band raised from the originally-proposed 0.010 to 0.020 — band sweep showed +0.35 Sharpe + 13 % cadence recovery at no other-metric cost. | strategy / regime / config | high-severity-by-direction (a profitable strategy on the prior 12-month window was unprofitable on a 38-month window — the live system was likely in the early stages of the same fragility before the deploy). Expected impact: full-sample Sharpe -0.39 → +2.47, walk-forward OOS Sharpe +0.22 → +1.10, cadence drop ≈ 49 % (only counter-trend fades get filtered). **Architectural lesson:** mean-reversion strategies need an explicit regime filter; the absence of one is a latent regime-fragility bug, not just "room for improvement". Recurring trouble spot — every prior VWAP tuning run flagged this; only the 38-month backtest made the cost concrete enough to override the 30-days-live gate. Defensive idea: bake a "regime classification" field into the audit log on every signal (`meta.htf_blocked` is the start), so future analyses can quantify regime-attributed PnL without re-running backtests. Cross-references: `experiments/2026-05-07-vwap-accuracy/RECOMMENDATIONS.md` (origin), `experiments/2026-05-08-all-models-training/RECOMMENDATIONS.md` (this run). Phase-3 follow-up (HTF reference 4 h → 1 h EMA-200, +0.4 Sharpe expected per V3) is queued in `milestone-state.md` and gated on ≥ 30 days of Phase-2 live metrics. Live-mode invariant ping required (PR touches `src/runtime/pipeline.py` + strategy module). |
| BUG-062 | 2026-05-07 | S-045 (this PR) | tests / CI | After S-044 shipped `pytest-collect`, the workflow had to ride advisory because 45 test files (every test transitively importing `src/bot/comms_handler.py` or `src/bot/claude_bridge.py`) failed collection with `ModuleNotFoundError: No module named 'telegram.error'; 'telegram' is not a package`. A second cluster of 7 web-API test files failed with the same shape against `fastapi.testclient`. | Two related test-isolation bugs: (1) `tests/conftest.py` stubbed only `telegram` and `telegram.ext` as bare `MagicMock()` modules — no entry for `telegram.error` (used by `comms_handler.TelegramError`) or `telegram.constants` (used by `claude_bridge.ChatAction`). Bare-MagicMock leaves `sys.modules["telegram"]` looking like a leaf, so Python's import machinery refuses submodule lookups with "telegram is not a package". (2) `tests/test_bot_web_sweep.py` guarded its fastapi MagicMock stub with `if "fastapi" not in sys.modules:` instead of "try a real import; only stub if absent" — so when the file collected before any real-fastapi consumer, the stub poisoned `sys.modules` for the rest of the run. | This PR — extended `tests/conftest.py` to stub `telegram.error` (with a real `Exception` subclass for `TelegramError` so `except TelegramError` clauses don't crash) + `telegram.constants.ChatAction` + `telegram.ext.MessageHandler` / `filters`; cross-linked `telegram.error` and `telegram.constants` onto the parent `telegram` namespace. Replaced `test_bot_web_sweep.py`'s fastapi/auth `if "X" not in sys.modules` guards with `try/except ImportError` shape. `pytest --collect-only -q tests/ --ignore=tests/test_main_loop.py` now passes with 0 errors (was 45 → 7 → 0). T2 of S-045 drops the `\|\| true` shim from `pytest-collect.yml`. | tests / config | medium severity — CI never caught real collection regressions because `pytest-collect` was advisory. **Architectural lesson:** `if "<mod>" not in sys.modules` is the wrong guard for "stub if missing" — it tests "has anything in this run touched the name" not "is the real package available". `try: import X; except ImportError: stub` is the correct shape. Recurring trouble spot: every previous test file that hand-rolled a stub used the same buggy guard; some of them (`test_s027_comms_handler.py` etc.) work today only because they happen to be the first to grab the name. Defensive idea: add a CI helper script that flags any `sys.modules.setdefault(...)` or `if "X" not in sys.modules:` pattern in test files and asks the author to convert it to the try/except shape. |
| BUG-061 | 2026-05-07 | claude/fix-order-execution-error-Afamp (one-off, this PR) | live-trading / exchange contract | Live trading fully blocked on `bybit_2` (vwap, BTCUSDT spot). Every entry rejected by Bybit V5 with `retCode 170130 — Data sent for parameter '' is not valid`. Liveness watchdog fired (`5 actionable signals fired in the last 1h, but 0 trades landed`). Production payload: `{"category": "spot", "symbol": "BTCUSDT", "side": "Buy", "orderType": "Market", "qty": "0.005", "stopLoss": "80793.4", "takeProfit": "81157.2", "marketUnit": "baseCoin"}`. (see #BUG-057, #BUG-060) | Bybit V5's `/v5/order/create` only accepts `stopLoss`/`takeProfit` on **Limit** spot orders — passing them on a Market spot order returns 170130 ("Data sent for parameter '' is not valid", with empty parameter name). The codebase already encoded this restriction for the modify path (`modify_open_order` refuses spot with the comment "set_trading_stop not supported for spot — monitor loop must close via close_open_position") but the **submit** paths (`_submit_order` and `_submit_test_order` in `src/units/accounts/execute.py`) still serialised SL/TP into the kwargs unconditionally. The BUG-057/BUG-060 chain fixed price precision but did not revisit whether SL/TP should be on the spot Market call at all. | This PR — `_submit_order` / `_submit_test_order` now branch on `category`: spot Market entries omit `stopLoss`/`takeProfit` (the S-030 monitor loop enforces them via `close_open_position`); linear/inverse entries keep the quantized SL/TP. Regression assertions added to `tests/test_spot_category_routing.py::test_spot_account_routes_category_spot_with_marketUnit` (asserts both keys absent on spot) and `test_linear_account_routes_category_linear_without_marketUnit` (asserts both keys present on linear so the spot fix does not regress derivatives). | risk / config | high severity — every live VWAP signal on `bybit_2` since the BUG-060 PR landed has been rejected by the exchange. No funds at risk (rejection happens at exchange validation), but the bot has been silently failing to execute. **Architectural lesson:** when an exchange contract differs across categories (here: spot vs derivatives), every code path that touches that contract must branch on the category — not just one. The `modify_open_order` path was correctly aware of the spot restriction; the submit path was not. Defensive idea for follow-up: add a Bybit-V5 contract test that constructs the exact payload for each `(category, orderType)` combo and pins which fields are allowed, so future code paths can't accidentally include disallowed fields. Live-mode invariant ping required (PR touches `src/units/accounts/*`). |
| BUG-060 | 2026-05-06 | claude/fix-order-price-decimals-VjCAW (one-off, this PR) | live-trading / precision | Bybit V5 continued rejecting BTCUSDT VWAP orders with `retCode 170134 — Order price has too many decimals` even after BUG-057's PR #420 fix. Payload showed `stopLoss: "81221.38"`, `takeProfit: "81660.46"` — values already quantized to 2 decimal places, yet Bybit still rejected them. (see #BUG-057) | PR #420's fix added a static map with `("BTCUSDT", "spot"): "0.01"` as the tick size entry, but `get_tick_size` checked the static map BEFORE the live `get_instruments_info` lookup. If Bybit's real `priceFilter.tickSize` for BTCUSDT spot is larger than `0.01` (which the continued 170134 rejections confirm), values quantized to 0.01-precision are correctly-formed floats that do not align to the true tick boundary — so Bybit's validator rejects them. The static map silently blocked the live API from ever correcting the stale value. | this PR — inverted `get_tick_size` priority order in `src/units/accounts/precision.py`: cache (2-hour TTL per Bybit's recommendation) → live `get_instruments_info` → static map → `0.01` fallback. The static map is now outage-resilience only, not the primary source. Added `invalidate_tick_cache(symbol, category)` called by both 170134 error handlers in `_submit_order` and `_submit_test_order` (`src/units/accounts/execute.py`) — ensures the next order forces a fresh live lookup after any 170134 rejection, making the bot self-healing if the tick ever changes again. Updated test suite to match the new live-first contract and added cache isolation guards to prevent cross-test contamination. Added `docs/integrations/bybit-ai-skill.md` (Bybit V5 integration reference including precision rules and 170134 documentation). | risk / data | see #BUG-057. The defensive idea from BUG-057's notes ("extend the static map as new symbols enter accounts.yaml") was implemented as intended — but a static-first lookup defeats the purpose of the live API entirely. **Architectural lesson:** a static map intended as a fallback must not be consulted before the live source. Any codebase that has both a live lookup AND a static fallback should write the priority order as a comment and test that the live source wins when both are available. The 2-hour TTL cache is per Bybit's own recommendation for `instruments-info` polling frequency. The `invalidate_tick_cache` self-healing mechanism means a single 170134 rejection triggers a fresh live lookup on the very next order attempt — no operator intervention required. |
| BUG-059 | 2026-05-06 | claude/route-claude-pings-to-bridge-bot (one-off, this PR) | telegram / routing | Operator pointed out (alongside the duplicate-pings issue) that Claude session pings — checkpoints, blockers, sprint completes, training-stage transitions — were arriving from `@bict_trading_bot` (the trading bot) instead of `@claude_ict_comms_bot` (the Claude bridge). Trading alerts ("Order execution failed …") correctly fire from the trading bot, but Claude session pings should ride on the Claude bridge per the new workplan's two-bot separation. | The S-019 design predates the new workplan: `notify_on_pull.py` enqueued every Claude session ping (blockers, drained `pending-pings.jsonl`, training-stage commits, checkpoint commits) into `runtime_logs/pending_pings/`, and only `telegram_query_bot.py::_drain_pending_pings` (the trading bot, `ict-telegram-bot.service`) drained that directory. `claude_bridge.py` had no drain loop at all — it was inbound-only (free-text → Anthropic + recurring-session triggers). So 100% of Claude→operator pings rode on the wrong bot. | This PR — split the inbox: added `runtime_logs/pending_claude_pings/` (drained by `claude_bridge.py`, gitignored) for Claude session pings; left `runtime_logs/pending_pings/` untouched (trading bot keeps draining it for trade alerts via `execution_diagnostics`, `liveness_watchdog`, `order_monitor`). `scripts/send_ping.py::enqueue` gained a `target: "trader" \| "claude"` arg (default `"trader"` for backward-compat with every existing producer); CLI `--target` flag added. `notify_on_pull.py::main` now passes `target="claude"` for every ping it emits. `claude_bridge.py` gained `_drain_pending_claude_pings` (mirror of the trading bot's drain loop, same atomic + retry semantics) wired via `app.job_queue.run_repeating(...)`. New tests: `test_enqueue_target_claude_writes_to_claude_inbox`, `test_enqueue_target_trader_keeps_default_inbox`, `test_enqueue_rejects_invalid_target`, `test_main_cli_target_claude_routes_via_flag`, `test_claude_bridge_drain_sends_each_file_and_deletes`, `test_claude_bridge_drain_skips_when_empty`, `test_claude_bridge_drain_renames_malformed_to_broken`, `test_claude_bridge_drain_leaves_file_on_send_failure`, `test_notify_on_pull_routes_to_claude_target` (end-to-end). 63 / 63 green across `test_send_ping.py`, `test_notify_on_pull.py`, `test_order_price_precision.py`. | telegram | medium severity — bot identity confusion (operator unsure which messages are Claude-channel vs trader-channel). Architectural lesson: when a service splits responsibilities into two distinct bots/channels, every producer must explicitly pick its channel — a default-everything-to-the-old-bot fallback hides the split. The new `target=` parameter makes the routing decision explicit at every call site. Cross-references: BUG-058 (the duplicate-pings fix landed first so the Claude-channel rerouting starts on a clean dedupe baseline). |
| BUG-058 | 2026-05-06 | claude/start-next-sprint-e6V6E (one-off, this PR) | telegram / pings | Operator reported "I keep getting old pings every time there's a merge". Every successful `git pull` on the VM re-fired the **same** 2026-05-06 S-014 blocker ping (`@bict_trading_bot` "BLOCKED — needs PM input … M2 PR #1 login form fetch + JWT pre-expiry timer (PM REVIEW)") that had already been delivered + acted on at S-014 close. Pings were correctly routed (per CLAUDE.md the trading bot owns Claude→operator pings); the duplicate firing on each merge was the actual bug. | `docs/claude/pending-pings.jsonl` is git-tracked. `scripts/notify_on_pull.py::_drain_pending_pings` read every line on every pull but never tracked which lines had been delivered — the pre-fix comment in the function explicitly said *"truncation is the responsibility of the follow-up commit (so reruns are idempotent if the truncation commit hasn't landed yet, the same line will re-fire)"*. No follow-up truncation commit was ever wired into the workflow. The S-014 blocker line landed on 2026-05-06 (commit `bbace53`, PR #416) and re-fired on every subsequent merge (#417, #418, #419, #420, #421, #422). | This PR — added VM-local hash-based dedupe: `_line_hash(raw)` (sha256 of stripped JSON), `_load_delivered_hashes()` / `_record_delivered_hash()` against `runtime_logs/pending_pings_delivered.txt` (NOT git-tracked, lives on the VM only). `_drain_pending_pings(path, delivered=…)` skips any line whose hash is in *delivered*; `main()` records the hash after a successful enqueue. Truncated `pending-pings.jsonl` to empty in the same PR to clear the stale S-014 line. New tests: `test_drain_skips_already_delivered_lines`, `test_record_and_load_delivered_hashes_round_trip`, `test_line_hash_stable_across_calls`, `test_main_does_not_re_enqueue_already_delivered_pending_line` (end-to-end regression — same line, two consecutive `main()` calls, second call enqueues 0 pings). 31 / 31 green. | telegram | medium severity — operator-noise impact only (no trading impact), but eroded trust in the ping channel. **Architectural lesson:** any append-only contract that relies on a *separate, later commit* for cleanup will leak — the cleanup commit is the bug. Replace with self-cleaning state local to the consumer (here, the VM-local delivered-hashes file). Recurring shape with BUG-048 (config-render drift). Defensive idea: when adding a new producer/consumer pattern, write the dedupe-or-truncate step into the same code path as the producer — never into a hypothetical follow-up commit. |
| BUG-057 | 2026-05-06 | claude/fix-order-decimals-rZsmg (one-off bug fix) | live-trading routing / config | Operator reported every live BTCUSDT VWAP order on `bybit_2` was rejected by Bybit V5 with `retCode 170134 — Order price has too many decimals`. Multi-account dispatch ran fine, risk approved + sized correctly, but the exchange refused the POST. Production payload showed `stopLoss: "81199.1764251841"` and `takeProfit: "81899.98467877129"` — 10 and 13 decimals respectively against BTCUSDT spot's 0.01 tick. | Strategies (`src/units/strategies/vwap.py:260-265` and the same pattern in other strategies) compute SL/TP from raw float arithmetic (`entry - sl_std_mult * std_dev`, `vwap`) so the values carry binary-float noise. The executor's three Bybit submission paths (`src/units/accounts/execute.py` `_submit_order`, `_submit_test_order`, `modify_open_order`) serialised the floats with `str(order["sl"])` / `str(order["tp"])` straight into the request body. Qty had a precision floor via `RiskManager.position_size` (`qty_precision`, default 3), but **no equivalent for price** — `grep -r 'tick_size\|priceFilter\|round_price\|quantize' src/` returned nothing. Bybit's V5 spec enforces alignment to `priceFilter.tickSize`. | #420 — added `src/units/accounts/precision.py` with a `(symbol, category) → tickSize` static map for the actively routed pairs (BTCUSDT/ETHUSDT/SOLUSDT × spot/linear), a Bybit V5 `get_instruments_info` live lookup for unknown symbols (cached for the process lifetime), and a conservative `0.01` fallback for outage-resilience. `quantize_price()` (`Decimal` ROUND_HALF_UP on the tick quotient) is wired into all three Bybit submission paths in `execute.py`. New regression suite `tests/test_order_price_precision.py` (13 tests) pins the production rejection values, both spot (0.01) and linear (0.10) ticks, the live-lookup-then-cache contract, and the fallback paths. | risk / config | medium severity — every live VWAP signal on BTCUSDT spot since the perp→spot migration (BUG-056) was rejected by the exchange. The dispatch path silently swallowed the rejection and the diagnostic ping fired per-tick; no funds at risk but no live trades either. **Recurring shape with the qty-precision history**: Bybit's filters enforce numeric alignment for both qty and price, but only qty had a quantizer. Defensive idea: extend the static map as new symbols enter `config/accounts.yaml` (CI guard worth considering — fail if a symbol routes via execute.py without an entry in `_STATIC_TICK_SIZE`). Live-mode invariant ping fired (PR touches `src/units/accounts/*`). |
| BUG-056 | 2026-05-06 | claude/fix-spot-trading-bzsbd | live-trading routing / config | Operator reported the trader was placing **BTCUSDT perpetual futures** (USDT-margined linear contracts) instead of the spot cash market that matches their wallet (`bybit_1` holds BTC + USDT, `bybit_2` holds USDT only). Every live order, modify-SL/TP, close-position, and get-positions call routed to Bybit's V5 derivatives endpoints. | Five hardcoded `category="linear"` literals in the order path (`src/units/accounts/execute.py` lines 192/315/530/587 — `_submit_test_order`, `_submit_order`, `modify_open_order`, `close_open_position`) plus `src/units/accounts/clients.py:210` (`account_open_positions`). No per-account / per-symbol `market_type` field existed — `accounts.yaml` had no way to express "this account trades the cash market". `processor.get_price()` also queried perpetual ticker data. The original commit predates the operator's directive to trade spot; the perp routing was never re-validated. | this PR — added `market_type: spot \| linear` field to `config/accounts.yaml` (default `spot`); added `_bybit_category(account_cfg)` helper in `execute.py`; threaded `market_type` through `TradingAccount`, `load_accounts`, and `Coordinator.multi_account_execute`'s `account_cfg` dict; replaced the 5 hardcoded sites with the resolver; spot orders pin `marketUnit="baseCoin"`; spot `modify_open_order` refuses cleanly (Bybit `set_trading_stop` is derivatives-only); spot `close_open_position` drops `reduceOnly`; spot `account_open_positions` returns `[]` (no derivative-style positions); spot `get_price` queries the spot ticker. New regression suite `tests/test_spot_category_routing.py` (15 tests) + 2 fixture pins on existing tests that assumed perp routing. | config / live-mode | high severity — every live BTCUSDT trade since the perp-default era was placed on the wrong instrument. Recurring shape with the BUG-039/BUG-045 family: implicit defaults that override per-account intent. Defensive idea: extend `scripts/check_dry_run_in_diff.py` (or a new guard) to fail CI on any added `category=("linear"\|"spot"\|"inverse")` literal in `src/units/` — every category routing decision must read from `account_cfg["market_type"]` via `_bybit_category()`. Live-mode invariant ping required (PR touches `src/units/accounts/*`). |
| BUG-055 | 2026-05-05 | claude/sharp-volta-gG894 (hardening session 3) | deploy / docs | `scripts/deploy_pull_restart.sh` and `scripts/run_smoke_once.sh` comment blocks described `ALLOW_LIVE_TRADING in the env` as a smoke safety rail. After BUG-039 removed that env var, the comments were false and would mislead future operators. | Stale documentation — comment was written before BUG-039's 2026-05-03 architectural change. | This PR — updated both comments to reference the qty cap + per-account `mode:` in `accounts.yaml` as the actual safety rails. | config | low-severity; docs-only. |
| BUG-054 | 2026-05-05 | claude/sharp-volta-gG894 (hardening session 3) | config / deploy | `scripts/print_runtime_profile.py` printed `DRY_RUN=<value>` and `ALLOW_LIVE_TRADING=<value>` in its diagnostic output. Both fields are absent from the env since BUG-039, so the output was always `DRY_RUN=None` and `ALLOW_LIVE_TRADING=`. Misleading for operators using this script as a health check. | Stale code — these two `items.append` lines were not removed alongside BUG-039's env cleanup. | This PR — removed the two stale lines; added a comment explaining why they're gone. | config | low-severity; diagnostic script only. |
| BUG-053 | 2026-05-05 | claude/sharp-volta-gG894 (hardening session 3) | architecture / docs | `src/units/accounts/execute.py` module-level docstring (lines 9, 13) described the dry-run path as "simulate when `DRY_RUN=true` or client is None" and "When client is None and `DRY_RUN` is not explicitly set" — implying the module still reads the `DRY_RUN` env var. Actual code at line 63 explicitly says "DRY_RUN env var is no longer consulted". Misleading for developers auditing the mode-flag plumbing. | Stale docstring — not updated when the env-var mode gate was removed per BUG-039 (operator directive 2026-05-03). The working code was correct but the docstring lagged. | This PR — updated the module docstring to accurately describe client=None as the dry-run trigger and point to `config/accounts.yaml` `mode:` as the authoritative gate. | architecture | low-severity; doc drift. Recurring shape: when behavior changes, module-level docstrings are often not updated (see BUG-047 for the message-drift variant). |
| BUG-052 | 2026-05-05 | claude/sharp-volta-gG894 (hardening session 3) | deploy / config | `scripts/startup_env_check.py` (wired into `deploy/ict-env-check.service`) listed `MODE` in `REQUIRED_STRINGS` and `DRY_RUN`, `ALLOW_LIVE_TRADING` in `SAFETY_FLAGS`. Since BUG-039 removed all three from the env, `MODE` caused the script to emit `MISSING MODE = NOT SET` and exit code 1 on every VM boot, along with a false "Trader will NOT start" Telegram notification. `DRY_RUN` and `ALLOW_LIVE_TRADING` always appeared as `NOT SET` in the safety-flags section. | Script not updated when BUG-039 removed the three env vars (operator directive 2026-05-03). `ict-env-check.service` is declared `Before=ict-trader-live.service` but NOT in the trader's `Requires=`/`After=` chain, so the false alarm did not block the trader — it only spammed the operator with a spurious Telegram notification at every boot. | This PR — removed `"MODE"` from `REQUIRED_STRINGS`; removed `"DRY_RUN"` and `"ALLOW_LIVE_TRADING"` from `SAFETY_FLAGS`. | deploy | medium severity — causes a false-alarm Telegram notification at every VM boot but does not block the trader. Recurring shape with BUG-039: when env vars are removed, every script + service that checks for them must be updated in the same PR. |
| BUG-051 | 2026-05-05 | claude/sharp-volta-gG894 (hardening session 3) | config / deploy | `scripts/smoke_test_trade.py` (invoked by `scripts/run_smoke_once.sh` via `deploy/ict-smoke-once.service`) had a hard block: if `ALLOW_LIVE_TRADING` env var was absent or not truthy, the script exited with code 2 and refused to run. Since BUG-039 removed that env var from `.env.example` and the VM's `.env`, the live smoke was effectively broken — it could never run without the operator manually setting a stale env var. Additionally, `_dispatch()` injected `DRY_RUN=true` and `ALLOW_LIVE_TRADING=false` into the settings dict when `--dry-run` was passed, but `safe_place_order` (the callee) ignores these keys — the injection was a no-op but confused future readers. | Stale guard — the `ALLOW_LIVE_TRADING` check and the settings injection were written before BUG-039's single-source-of-truth refactor. After the refactor, the env var was no longer the safety rail (the per-account `mode:` in `accounts.yaml` is), but the script was not updated. | This PR — removed the `os.environ.get("ALLOW_LIVE_TRADING")` check from `main()`; replaced the settings injection in `_dispatch()` with a comment explaining that `client=None` is the actual dry-run mechanism; updated docstring; updated two tests (`test_live_without_allow_flag_refuses` → `test_no_allow_live_env_no_longer_blocks`, `test_dispatch_dry_run_passes_safety_settings` → `test_dispatch_dry_run_uses_none_client`). | config | medium severity — live smoke test was silently broken on the VM. Recurring shape with BUG-039 (when env vars are removed, all scripts and their guards must be updated). |
| BUG-050 | 2026-05-04 | claude/hardening-session-2-execute-coordinator (hardening session 2) | architecture / dead code | Hardening Session 2 audit of `execute.py` and Coordinator found `close_all_bybit_positions` in `src/bot/telegram_query_bot.py` (line 453) and `close_all_bybit_positions_for_strategy` in `src/units/ui/data_loaders.py` (line 865) both call `client.place_order()` directly, bypassing `execute_pkg`. Architecture rule 5 violation: bot is a thin shell; direct exchange SDK calls from the bot layer are forbidden. | Both functions are dead code in the production path. The `/closeall` Telegram command routes through `cmd_closeall` → `_do_closeall_strategy` → `processor.close_open_positions` (fixed in S-031 PR4). The dead functions remain in the codebase and are only exercised by legacy tests (`tests/test_telegram_query_bot.py:1812`, `tests/test_data_loaders.py:613`). They were not removed in S-031 because the cleanup task was deferred per cleanup-policy (dead code = separate focused PR). | Not yet fixed. Dead code — no live-trading impact. Cleanup sprint needed: remove `close_all_bybit_positions` from the bot + its tests, remove `close_all_bybit_positions_for_strategy` from data_loaders + its tests. Both functions should be replaced by the `processor.close_open_positions` path (already correct). | architecture | S-031 PR4 fixed the PRODUCTION path but left dead code in place. Recurring shape: cleanup is deferred and dead code accumulates. The architecture audit found it because the audit reads every function end-to-end, not just the hot path. |
| BUG-049 | 2026-05-04 | claude/fix-trading-bot-push-o3J4w (ad-hoc) | dispatch / strategy lifecycle | vwap stopped sending signals after 2026-05-03 23:06:10 UTC (~8h+ silence). Operator `/signals` showed no entries since that timestamp; `/packages` showed 10 open packages with no linked trades. No errors visible, trader appeared healthy. | The BUG-046 gate (`_has_open_package_for_strategy` in `src/runtime/pipeline.py`) queries `order_packages WHERE strategy_name=? AND status='open'` to prevent duplicate live trades. Any `order_packages` row with `status='open'` blocks the strategy — including rows with `linked_trade_id IS NULL` (packages that were logged by the strategy unit before dispatch, but for which the risk manager rejected or dispatch failed and no trade was ever placed at the broker). These "never-executed" packages are not swept by the reconciler (which reads only the `trades` table). They accumulate and silence the strategy indefinitely. The 10 packages visible via `/packages` (which queries `WHERE status='open' AND linked_trade_id IS NULL`) were exactly the blocker set. | Immediate hotfix: `notebooks/operator/sweep_unlinked_packages.ipynb` — SSH to VM, preview all `order_packages WHERE status='open' AND linked_trade_id IS NULL` grouped by strategy, then with `CONFIRM=True` mark them `status='orphaned'` (auditable via `meta.orphaned_by`). No trader restart needed — the BUG-046 gate re-reads the DB on every tick, so the strategy unblocks within one tick. Code fix deferred: modifying `_has_open_package_for_strategy` to skip packages with `linked_trade_id IS NULL` (no real broker position) requires a ping-PR (touches `pipeline.py` per CLAUDE.md § Live-mode invariant). Deferred to a follow-up sprint. | architecture | architectural lesson: the BUG-046 gate was designed to prevent duplicate live trades, but it scopes on `order_packages.status='open'` which conflates "never executed" (no broker position) with "executing" (real open position at broker). The distinguishing field is `linked_trade_id`. A package with `linked_trade_id IS NULL` represents a signal that was logged but never dispatched — the risk manager or executor rejected it — yet the gate treats it as a live trade and silences the strategy. Recurring family: BUG-041 (ghost `trades` rows), BUG-044 (packages without rejection rows), BUG-046 (gate missing), BUG-049 (gate over-broad). Cross-references: BUG-046 (the gate this bug exposes the over-broadness of); BUG-044 (the early-out dispatch branches that leave packages with no `linked_trade_id`); BUG-042 (reconciler that looks at `trades` table, not `order_packages`). |
| BUG-048 | 2026-05-04 | claude/fix-trading-bot-push-o3J4w (PRs #397, #398) | config / env render drift | Trade #24 (`vwap → bybit_2 @ 2026-05-03 23:06:10 UTC`, BTCUSDT short qty=0.01) sat as `status='open'` in `trade_journal.db::trades` for ~8 hours despite `bybit_2` reporting `Open: 0` and `/accounts_status` showing balance=$179.78. The BUG-042 monitor-loop reconciler should have auto-orphaned the row within one tick — it did not. Operator originally diagnosed it as a notebook-push problem (false `*.ipynb` is gitignored hypothesis); the actual symptom was deeper. | Doc drift between the canonical `.env.example` and both render paths. PR #389 set `MONITOR_RECONCILE_ENABLED=true` in `.env.example` (the operator-approved canonical default), but neither `scripts/render_env_from_master.py::build_live` nor `notebooks/operator/rotate_api_keys.ipynb::PRODUCTION_DEFAULTS` was updated to emit it. Every key rotation produced a `.env` without the flag → trader read `os.environ.get("MONITOR_RECONCILE_ENABLED", "false")` → `false` → `_reconcile_open_trades` returned no-op → ghost trades accumulated. Sibling false-flag in the operator's first push attempt: a previous diagnostic cell blamed `.gitignore`; verified false — `*.ipynb` is not in `.gitignore` and `notebooks/**/*.ipynb` is tracked normally. The actual push-cell failure mode was `subprocess.run(...)` without `capture_output`, swallowing git's stderr. | PR #397 — `notebooks/operator/push_notebook_to_repo.ipynb` (robust Colab→repo helper that captures stdout+stderr+exit on every git invocation, validates the source notebook is non-empty + valid JSON + nbformat-shaped, runs `git check-ignore` as a pre-flight, bails loudly if `git diff --cached --quiet` returns 0 after add). PR #398 — emit `MONITOR_RECONCILE_ENABLED=true` from both render paths + new `test_monitor_reconcile_enabled_is_true` regression guard (`tests/test_render_env_from_master.py`). 68/68 green. Verified end-to-end on 2026-05-04 07:00 UTC: operator ran the updated rotate_api_keys.ipynb, the new `.env` shipped to the VM, services restarted, and on the next monitor tick the reconciler emitted `🧹 Monitor reconciler — orphaned trade swept` for trade #24 and flipped it to `status='orphaned' / exit_reason='reconciler'`. `/last5` then showed `📭 No trades found` and `/packages` showed #24 with `status=orphaned`. | config | architectural lesson: when `.env.example` is treated as the canonical spec but the renderers are separately maintained, drift is invisible until a feature-flag-gated subsystem silently no-ops in production. Recurring shape with BUG-024 / BUG-026 (config-file path drift) and BUG-039 / BUG-045 (single-source-of-truth drift between layers). Defensive idea: a contract test that diffs the keys in `.env.example` against the keys emitted by `build_live(FAKE_DATA)` and fails on any drift in either direction (allowing an explicit ignore-list for known dev-only / per-account vars). Cross-references: BUG-042 (the reconciler this fix made operational); PR #389 (the .env.example flip that exposed the drift); the operator's gitignore mis-diagnosis is itself a recurring shape (mistaking a swallowed-stderr push failure for a path-gate failure) — addressed by the PR #397 helper which surfaces the real failure mode unconditionally. |
| BUG-047 | 2026-05-04 | claude/hardening-session-1 (recurring hardening) | tests | `test_s028_vwap_execute_routing.py::TestExecutionFailureDiagnosticPing::test_live_mode_missing_creds_emits_ping_and_does_not_silently_dry_run` failed asserting `"missing API credentials"` in the result error string | BUG-034 + BUG-045 rewrites changed the coordinator's client-construction-failed message from "missing API credentials" to `"account '...' is not fully configured: api_key_env=..."`. The test assertion was not updated alongside the message change, so it passed in the pre-fix era and silently lagged behind. | this PR — updated assertion to check `"not fully configured"` which is always present in the coordinator's not-fully-configured message. | tests | recurring shape with BUG-020 ("stale-text drift" — when a message changes, tests asserting on substrings of it must be updated in the same PR). Lesson: when a coordinator error message string is changed, grep for test assertions on substrings of the old message before merging. |
| BUG-042 | 2026-05-03 | claude/bug-042-monitor-loop-reconciler (PRs #384, #385, this PR) | monitor-loop / trade lifecycle | DB rows for closed positions remained `status='open'` indefinitely after the exchange flattened them (TP/SL hit, manual close, bot restart during close). The pre-PR-2 monitor loop had no reconciler comparing the DB's `status='open'` set against the exchange's actual live positions. Operator saw `/packages` listing orphaned open rows with no close event, and `/last5` surfacing ghost trades. A one-shot cleanup notebook (PR #367) patched historical rows but did not prevent new ghosts. Root symptom surface overlaps with BUG-041 (pre-#357 ghost rows) and BUG-044 (no rejection row on early-out dispatch). | Two compounding gaps: (a) no canonical `_log_trade_to_journal` write-back for RiskManager refusals (fixed by PR #357 / BUG-041); (b) no always-on reconciler comparing `trades.status='open'` against the exchange's live position snapshot, so any race between monitor tick and exchange-side close left a DB row stuck. BUG-042 tracked the permanent fix for (b). Also required lifting `account_open_positions` from the UI unit to the accounts unit (PR #384) so the reconciler could call the canonical exchange-read path. | PR #384 (BUG-042 PR 1/3 — `account_open_positions` lifted from `src/units/ui/data_loaders.py` to `src/units/accounts/clients.py`); PR #385 (BUG-042 PR 2/3 — `_reconcile_open_trades(db)` in `src/runtime/order_monitor.py`, gated by `MONITOR_RECONCILE_ENABLED=false`); this PR (BUG-042 PR 3/3 — runbook `docs/runbooks/monitor-reconciler.md` + `MONITOR_RECONCILE_ENABLED` flipped to `true` in `.env.example`). 15 tests in `tests/test_monitor_reconciler.py`. Cross-references: PR #357 (canonical `_log_trade_to_journal`), PR #367 (one-shot cleanup notebook), CP-2026-05-03-22 (sprint kickoff + PRs 1+2 merged). | architecture | architectural lesson: trade lifecycle is a two-sided contract — the DB records intent on placement, but only the exchange knows the outcome on close. Prevention (PR #357) + one-shot cleanup (PR #367) are necessary but insufficient; an always-on reconciler is the only way to keep both views consistent across bot restarts and race conditions. Skip rules prevent false positives: dry-run accounts skipped (no exchange to query), accounts with missing creds skipped (don't orphan rows on a transient read error), accounts absent from `accounts.yaml` skipped (operator cleans up manually). Operator manual-override SQL documented in `docs/runbooks/monitor-reconciler.md`. |
| BUG-046 | 2026-05-03 | claude/strategy-monocle-pr1-open-gate (PR #387) | dispatch / strategy lifecycle | Operator's `/packages` snapshot at 22:38 showed 10+ open VWAP packages with no linked trades — every actionable VWAP tick was stacking a new package because the strategy unit had no concept of "I already have an open trade, I should be monitoring not generating". Compounded the BUG-045 silent-dry-run shape (each stacked package fanned out to all 3 accounts, producing 2 rejection rows per tick + one silent dry on bybit_2) | The pipeline's multi-account dispatch fast-path called `_signal_to_order_package(signal, settings)` unconditionally on every actionable signal. The strategy unit's signal builders (`build_vwap_signal`, `turtle_soup_signal_builder`, etc.) had no DB awareness — they fired whenever VWAP / killzone / ICT conditions were met, regardless of in-flight packages. The architectural intent (per CLAUDE.md § "Architecture rules" § 2: strategies "monitor" open packages) was implemented for the *update* path (`order_monitor` calls `monitor()` on open packages) but not the *gate* path (refuse to log a duplicate package while one is open) | this PR — `_has_open_package_for_strategy(strategy)` helper in `pipeline.py` consults `Database.get_order_packages_by_strategy(strategy, status='open')` before `_signal_to_order_package`. Non-empty match short-circuits dispatch with `status='skipped' / reason='open_package_exists'`. 8 regression tests cover open / closed / cascaded / DB-failure / unknown-strategy / multi-open. Tier 1 — pure dispatch-side gate, single read-only DB query. PRs 2 + 3 of the strategy-monocle sprint wire the close path (partial-close verdict shape + dispatcher-driven exchange close) | architecture | architectural lesson: "strategies generate, log, monitor" (CLAUDE.md § 2) implies a per-strategy state machine with at most one open package at a time. Pre-fix the codebase enforced the *update* half of that contract (monitor loop) but not the *gate* half (no new packages while one is open). Recurring shape with BUG-039 (single-source-of-truth) — convention drift between two layers that disagree on which is authoritative. Cross-references: BUG-045 (silent dry-run that masked the symptom — once that fix landed, the duplicate packages became visible), BUG-042 (reconciler that catches stale opens), CP-2026-05-03-22 (this row). Follow-up: PRs 2 + 3 of the strategy-monocle sprint extend the gate to support strategy-driven close (full + partial) with exchange-side execution |
| BUG-045 | 2026-05-03 | claude/fix-silent-dry-run-and-pings-noise (PR #386) | dispatch / live-mode interlock | Liveness watchdog screamed `5 actionable signals fired in the last 1h, but 0 trades landed` for hours while `bybit_2` was `mode: live` in `accounts.yaml` with $177.68 balance and zero open positions. Same shape as BUG-034 (NotImplementedError on live BybitAPI placement) but a different root cause. Surfaced post-PR-#382 once the early-out journal-row contract made bybit_1 + prop_velotrade_1 rejections visible — the *missing* row for bybit_2 became conspicuous | `Coordinator.multi_account_execute(self, pkg, *, dry_run: bool = True, ...)`. `src/runtime/pipeline.py:783` called `coord.multi_account_execute(pkg)` without specifying `dry_run`, so the `True` default won. The dispatch then took the `if not dry_run: build_client` branch (False → no client built), passed `dry_run=True` to `execute_pkg`, and `execute_pkg`'s own per-account `mode` fallback never fired (it only consults `account_cfg["mode"]` when `dry_run is None` — the caller-supplied bool always wins). Net effect: every live signal silently went through dry mode for the entire post-#357 window. This is the BUG-039 anti-pattern recurring — a process-level interlock above the per-account mode field. Per CLAUDE.md § "Autonomous live-trading rule": *"per-account `mode: live | dry_run` in `config/accounts.yaml` is the SINGLE dry/live toggle in the codebase"* — a process-level override violates that contract | this PR — `multi_account_execute(dry_run: Optional[bool] = None)`. Per iteration, `effective_dry` is computed as `dry_run if dry_run is not None else account.dry_run` (where `account.dry_run` was loaded from `cfg["mode"]` by `load_accounts`). Caller override still works for tests / smoke runs (explicit `True` / `False` short-circuits the per-account decision). Client construction + the live-creds error gate on `effective_dry` instead of the caller arg. 4 regression tests cover live + override + dry-RiskBreach paths. Companion fix: truncated `docs/claude/pending-pings.jsonl` (16 stale entries from CP-2026-05-02-19 → CP-2026-05-03-X were re-firing on every VM pull because the drain script's contract `truncation is the responsibility of the follow-up commit` was never honoured) | architecture / live-mode | architectural lesson: same shape as BUG-039 — when a "safety default" sits above the per-account toggle, the toggle becomes ineffective. Recurring family: BUG-024, BUG-026, BUG-031, BUG-035, BUG-038, BUG-039, BUG-045 — each a different layer where `mode: live` was disagreed-with by some convention drift. The cumulative architectural debt is "auditing every place that takes a `dry_run` parameter and confirming the source-of-truth ordering" — possibly worth a dedicated audit-and-collapse sprint. Cross-references: BUG-034 (sibling silent-failure on the live order routing path; same observation surface; PR #284 collapsed BybitAPI's `route_order` interlock), BUG-039 (the canonical single-source-of-truth fix; PR #355 removed the env-var interlocks but didn't catch this one because the parameter default is at the *call* site, not the env-var site), CP-2026-05-03-22 (this row). The CI dry-run guard (`scripts/check_dry_run_in_diff.py`) did not catch this because the regex targets YAML `mode: dry_run` and env-var `DRY_RUN=true`, not Python parameter defaults — possible follow-up: extend the guard to flag `dry_run\s*:\s*bool\s*=\s*True` in dispatch APIs |
| BUG-044 | 2026-05-03 | claude/fix-vwap-trade-logging-7C3FE (PR #382) | dispatch / journal observability | Operator's `/packages` showed 5+ recent VWAP → bybit_2 packages from 2026-05-02 22:56 → 2026-05-03 01:49 with `status='orphaned'` and the matching open-package surface had 10 entries with `linked_trade_id=NULL`. The user paraphrased the symptom as "the vwap strategy sent a trade package but the bybit_2 account didn't log a trade — even with rejection". Distinct from BUG-041 (pre-#357 ghost-rows) and BUG-042 (monitor-loop reconciler): those address rows that were *opened* and went stale. This bug is about packages that were *never paired with any trade row at all*, so there's no rejection counterpart to explain why dispatch didn't land | `Coordinator.multi_account_execute` had three early-out branches that produced a result row with an `error` field but skipped `log_rejection_to_journal`: (a) `skipped_not_assigned` — `pkg.strategy not in account.strategies` (per-account strategy filter, S-029 PR1); (b) `sizing_failed` — `RiskManager.position_size` raised; (c) `below_min_balance` — `sized_qty <= 0`. In all three, `_log_new_order_package` had already inserted a row into `order_packages`, but no row landed in `trades`. So `/packages` rendered the package as "open with no linked trade" with no rejection reason — exactly the operator's observation. The architecture-audit-2026-05-02 P0-2 + the subsequent S-030 work added `log_rejection_to_journal` for the `RiskBreach` and exchange-rejected paths, but the three pre-`evaluate()` early-outs were missed. Compounded the diagnosis because BUG-041 (pre-#357 ghost rows) had the same observation surface (`/packages` showing orphaned/open-no-link rows) — the operator's first instinct was that BUG-041's cleanup notebook didn't catch every row | PR #382 — every early-out branch now calls `log_rejection_to_journal` with `status='rejected'` and the matching reason token (`skipped_not_assigned: …`, `sizing_failed: <ExcType>: <msg>`, `below_min_balance: balance=… USD < min_balance_usd=…`). New `tests/test_multi_account_execute_early_out_logs_refusal.py` (3 classes — one per branch) pins the contract: skipped/failed/under-balance accounts each land a rejection row with the strategy name, account_id, and reason token. Companion fix in `scripts/notify_on_pull.py`: only fire the checkpoint Telegram ping when the diff *added* a `## CP-…` header matching the file's current topmost entry — pre-fix any commit touching `CHECKPOINT_LOG.md` re-pinged the file's current top entry, so feature-PR merges that brought an old branch's checkpoint commit into main, or in-place edits to existing entries, spammed the operator with already-announced content | observability | architectural lesson: the contract for `/packages` is *every open package must pair with a journal-row reason*. Pre-fix three orthogonal early-out branches violated that contract. Recurring shape with BUG-039 (single-source-of-truth) + BUG-024 / BUG-026 (config-file path drift) — the failure mode is "subset of code paths skipped a contract that the rest honoured". Follow-up sprint candidate: a contract-test that asserts `for every status in REFUSAL_STATUSES + 'open': len(refusal_rows) + len(open_packages_with_link) >= len(order_packages)` on a synthetic trade journal — would have caught this on green main. The notify_on_pull dedup is unrelated to dispatch but landed in the same PR because both were operator-flagged in the same message; both ride on the existing observability surface (`/packages` and Telegram pings) |
| BUG-041 | 2026-05-03 | claude/fix-trading-validation-kGwLc (CP-2026-05-03-17 § 6b → CP-2026-05-03-20 P1) | journal / trade lifecycle | Pre-#357 ghost-trade rows: trades inserted into `trade_journal.db::trades` with `status='open'` were never updated to a terminal status when the underlying exchange position was closed (TP / SL / manual flatten). They lingered indefinitely as `status='open'`, surfaced as orphaned rows in `/last5`, `/trades`, and (after PR #360) `/packages`. The orphaned rows also carried `confidence: 0.0` for the same reason BUG-043 documents (unrelated leak path; same observation surface) — the operator surfaced both bugs from the same `/packages` snapshot | Two compounding gaps existed simultaneously: (a) no canonical `_log_trade_to_journal` write-back path covering RiskManager refusals and exchange-side errors, so legacy direct callers under-reported lifecycle events; (b) no monitor-loop reconciler comparing `trades.status='open'` against the exchange's actual open-positions list, so any race between bot-tick and exchange-side close left the DB row stuck. (a) was the *prevention* gap (new ghosts being created); (b) is the *cleanup* gap (existing ghosts going stale forever) and remains the open issue tracked by BUG-042 | PR #357 closed the prevention gap with the canonical `_log_trade_to_journal` covering RiskManager + exchange-rejection paths; PR #367 shipped the orphaned-status backfill notebook (`notebooks/operator/cleanup_ghost_trades.ipynb`) as the one-shot remediation for the historical backlog. The operator ran the cleanup notebook before CP-2026-05-03-20 but the row-count output was lost in the session blunder (`unknown` rows changed). Architectural-evidence: the orphaned rows visible today via `/packages` (CP-2026-05-03-20 § 1 operator paste) confirm the cleanup ran but didn't catch every row, OR new ghosts have accumulated since the run — distinguishing requires BUG-042's automated reconciler. Cleanup row count: **unknown** (operator confirmed run, did not capture the SELECT preview count) | architecture | architectural lesson: trade lifecycle is a two-sided contract — the DB writes the *intent* on placement, but only the exchange knows the *outcome* on close. Without a periodic reconciler the two views silently diverge. PR #357 (prevention) and PR #367 (one-shot cleanup) are necessary but insufficient; the always-on reconciler is BUG-042. Cross-references: PR #357 (prevention), PR #367 (one-shot cleanup notebook), CP-2026-05-03-17 § 6b (where the symptom was first cleanly framed), CP-2026-05-03-18 § 3 P0 (sibling diagnosis through the same `/packages` surface that found BUG-043), CP-2026-05-03-20 P1 (this row), and BUG-042 (the permanent fix sprint, plan in `docs/sprint-plans/bug-042-monitor-loop-reconciler.md`) |
| BUG-043 | 2026-05-03 | claude/fix-trading-validation-kGwLc (CP-2026-05-03-18) | strategy / journal attribution | Operator review of `/packages` and the orphaned-status backfill (PR #367) flagged that EVERY order package logged in `trade_journal.db::order_packages` (and every ghost trade visible via `/last5`) carried `confidence: 0.0`. CP-17 § 6d originally classed this as a cosmetic display issue (P3); the operator correctly reframed it as a **latent live-trading blocker**: adding the (correct) `RiskManager` confidence floor on top of these journal entries would reject every signal, and downstream consumers (`/latest_backtest` delta tracking, hourly attribution, future ML scoring) treat all live signals as zero conviction by construction | The pipeline production path is `build_vwap_signal → _signal_to_order_package → _log_new_order_package → order_packages.confidence`. `_signal_to_order_package` reads `meta.get("confidence") or 0.0` to populate `OrderPackage.confidence`. `src/units/strategies/vwap.py::build_vwap_signal` — the only signal builder for VWAP, the predominantly active strategy in production — never set `confidence` in the returned dict (neither at top level nor inside `meta`). So every VWAP signal landed at the `or 0.0` fallback and the journal silently zeroed every row. The strategy unit's own `order_package()` function had the formula correct (`confidence = min(deviation / ENTRY_STD_THRESHOLD, 1.0)`), but the pipeline never calls `order_package()` — it calls `build_vwap_signal()` directly. DB / coordinator / execute layers were already correct (end-to-end pinned at `confidence=0.7` / `0.8` in `tests/test_s030_pr1_order_packages_log.py`); the only broken seam was layer (a) per CP-2026-05-03-18 § 3 step 5 | this PR — `build_vwap_signal` now computes `confidence = round(min(abs(deviation) / ENTRY_STD_THRESHOLD, 1.0), 4)` (same formula as `order_package()`) and emits it at both top level and inside `meta`. The "no signal" branch also emits `meta.confidence` so the renderer shape is stable. 4 new regression tests in `tests/test_vwap_strategy.py::TestBuildVwapSignal`: actionable buy/sell signals carry non-zero top-level + meta confidence, no-signal branch still emits the field, and an end-to-end pin (signal → `_signal_to_order_package` → `_log_new_order_package` → `SELECT confidence` shows the strategy's value, not 0.0). RiskManager confidence floor explicitly **out of scope** — separate Tier-2 sprint per CP-18 § 3 closing | architecture | architectural lesson: when a "single canonical generator" (`order_package()`) and a "production builder" (`build_vwap_signal()`) compute the same field, drift between the two will silently kill journal data. Recurring shape with BUG-039 (single-source-of-truth) and BUG-024 / BUG-026 (config-file path drift). Follow-up sprint candidate: collapse `build_vwap_signal()` and `order_package()` so both routes share one implementation, and add a contract test that asserts `_signal_to_order_package(signal)` produces an `OrderPackage` with the same `confidence` as `order_package(cfg, candles_df)` for the same input. Cross-references: PR #360 (`/packages` where the symptom first surfaced), PR #367 (orphaned-status backfill where the same 0.0 pattern appeared on every ghost trade), CP-2026-05-03-17 § 6d (originally classed as cosmetic), CP-2026-05-03-18 § 3 P0 (operator's reprioritization). **CP-2026-05-03-20 verification status:** the post-deploy `/packages` snapshot taken in the next session (most-recent open row `updated_at = 2026-05-03T20:37:21+00:00`, predates PR #371's merge at `~2026-05-03T20:52Z`) showed no new package landed in the verification window — likely because BTC stayed under the 2σ VWAP entry threshold (the trader's quiet-market default). Verification is therefore **correct-by-construction via the regression contract**: `tests/test_vwap_strategy.py::TestBuildVwapSignal` (4 tests, including the end-to-end `signal → _signal_to_order_package → _log_new_order_package → SELECT confidence` pin) guarantees any future post-deploy package will record the strategy's real conviction. Live-VM confirmation remains a passive check on the next ≥ 2σ VWAP excursion |
| BUG-040 | 2026-05-03 | claude/fix-telegram-truncate-blockquote | UI / Telegram parse_mode=HTML | `/last5` (and any command whose rendered body exceeded Telegram's 4096-char cap) failed with `Can't parse entities: can't find end tag corresponding to start tag "blockquote"`. The operator saw the message wrapped as `⚠️ Could not render trades: …` because the `cmd_last5` handler caught the Telegram error and re-replied. Live VM, post the BUG-039 / rogue-process / `/packages` trio | `src/units/ui/telegram_format.py::_truncate` was a naive `text[:budget]` slice. Every Telegram message in the codebase routes through `render_html` → `_truncate`. When the rendered body exceeded 4096 chars, the slice could land mid-`<blockquote expandable>...</blockquote>` and lose the closing tag. Telegram's HTML parser then rejected the whole message. `/last5` triggered it whenever 5 trades × ~600 chars + a long `entry_reason` / `notes` field pushed past the cap | this PR — tag-aware truncation in two paths: (1) prefer cutting at a section seam (`</blockquote>\n\n`) so every opened tag has its matching close, no healing needed; (2) fall back to mid-cut + heal — reserve worst-case 17 chars (`</blockquote>` + `</b>`) up-front, roll back to before any incomplete `<...` so we never cut mid-tag, then append the missing close tags. Final length guaranteed `≤ limit` in every branch. 5 new regression tests including `test_truncate_real_world_last5_shape` that reproduces the failure on green main | markdown | bug shape mirrors BUG-009 / BUG-030 / BUG-031 (Markdown rejection on dynamic content) — same lesson, different parser layer. Architectural lesson: any sanitiser that operates on rendered HTML must understand tag balance, not just character count. The pre-existing `test_render_html_truncates_overlong_payloads` was passing because it only checked length + marker, NOT balance; adding a balance assertion to every truncation test would have caught this. Future renderers that produce balanced markup should add a `_is_balanced(html)` helper to their tests as standard practice |
| BUG-039 | 2026-05-03 | claude/single-source-trading-mode | architecture / dry-live toggle proliferation | Operator review of the post-BUG-038 `render_env_from_drive_master.ipynb` flagged that the notebook was offering profile-level dry-run options (`paper`, `colab`, `oracle_paper`, `vwap_btcusd_dry_run`), the env-render script was emitting `MODE` / `DRY_RUN` / `ALLOW_LIVE_TRADING`, `safe_place_order` was gating on those env vars, `validate_startup` required them, and `bybit_connector` logged on them. Multiple layers each carried a dry/live toggle — most surfaced as the "ALLOW_LIVE_TRADING=true is required for live submission" rejection (BUG-031, BUG-035, BUG-038) when they disagreed with whatever was actually deployed | Convention drift accumulated across 18 months of sprints. Each new layer was added *in addition to* the previous toggle rather than *replacing* it. The architectural intent (per the original Autonomous live-trading rule) was always per-account, but the process-level interlock was kept "for safety" — and then strategy-level / profile-level / env-level analogues kept being introduced for assorted edge cases. Result: 5+ places to flip, no single-source-of-truth, recurring "I thought I was live" surprise | this PR — operator directive of 2026-05-03 collapses **every** dry/live toggle to a single source: per-account `mode: live \| dry_run` in `config/accounts.yaml`, applied via `RiskManager.dry_run`, checked inside `RiskManager.evaluate()` returning `reason="account_mode_dry_run"`. Removed: `src/runtime/trading_mode.py`, `safe_place_order`'s ALLOW_LIVE_TRADING / DRY_RUN gates, `validate_startup`'s `MODE` / `DRY_RUN` / `ALLOW_LIVE_TRADING` requirements, `build_settings_from_env`'s `dry_run` / `allow_live_trading` / `mode` keys, the `vwap_btcusd_live` profile + builder in the env-render script, `--allow-live` requirement (now accepted-but-ignored for back-compat), the entire profile picker in the colab render notebook (one canonical render path → `~/ict-trading-bot/.env`), `bybit_connector`'s mode-logging chatter. Tests for the deleted gates were removed (`tests/test_trading_mode.py`, `tests/test_s012_live_mode.py`). The CI dry-run guard (`scripts/check_dry_run_in_diff.py`) now targets `mode: dry_run` lines in `accounts.yaml` diffs, with the legacy env-var patterns retained as "kept for back-compat" warnings | architecture | architectural lesson: when adding a "safety" layer, ask whether it *replaces* an existing toggle or *duplicates* it. Duplicates are the seed of every "I thought I was live" surprise. The recurring shape across BUG-024 / BUG-026 / BUG-031 / BUG-035 / BUG-038 was: convention drift between two layers that disagree on whether they're authoritative. Single-source-of-truth is the only stable architecture |
| BUG-038 | 2026-05-03 | claude/fix-telegram-pipeline-9lvk5 | deploy / .env routing (recurrence of BUG-026) | Per-tick Telegram still showed `Pipeline result: status=failed_validation \| reason=ALLOW_LIVE_TRADING=true is required for live submission` even though `scripts/render_env_from_master.py` emits `ALLOW_LIVE_TRADING=true` for both `live` and `vwap_btcusd_live` profiles. Diagnostic from BUG-035 self-debug message was truncated by user but the failure mode is the same | Convention drift between the colab-rendered env file and the systemd unit. The notebook (`notebooks/setup/render_env_from_drive_master.ipynb`) renders `.env.live` (or `.env.vwap_btcusd_live`); `deploy/ict-trader-live.service` reads `EnvironmentFile=/home/ubuntu/ict-trading-bot/.env` — no suffix. Operator SCPs `.env.live` to the VM but the trader keeps reading the legacy `.env`, which on a fresh VM is empty / missing `ALLOW_LIVE_TRADING`, so the live-mode interlock fires. BUG-026 attempted to fix this in #252 by writing both `.env` and `.env.live` from the rotation notebook, but the **render** notebook (the entry point) was never updated to do the same | this PR — (1) `notebooks/setup/render_env_from_drive_master.ipynb`'s post-render documentation now instructs the operator to copy the rendered file to `~/ict-trading-bot/.env` (not `.env.live`) on the VM. (2) Notebook adds a verification step printing `head -5 /home/ubuntu/ict-trading-bot/.env` (key names only) so the operator can confirm `ALLOW_LIVE_TRADING=true` is actually present after install | config | architectural lesson: same shape as BUG-026 / BUG-024 — multi-file config drift recurs whenever a render path and a consumer path disagree on filenames. Long-term fix is `EnvironmentFile=-/home/ubuntu/ict-trading-bot/.env.live` in the systemd unit (deploy/ change requires PM review) so either name works; this row tracks the documentation-side fix that unblocks the operator without touching deploy/ |
| BUG-037 | 2026-05-03 | claude/fix-telegram-pipeline-9lvk5 | UI unit / path resolution (recurrence after S-032 module move) | `/trades`, `/last5`, hourly accounts summary all reported `No accounts configured` even though `config/accounts.yaml` had three populated accounts and `/balance` (which uses a different lookup path) successfully showed Bybit_2 wallet balance. `/strategies` showed every strategy at `0 signals` despite `data/trades.db` being written by the pipeline | `src/units/ui/data_loaders.py::REPO_ROOT` was computed as `os.path.abspath(os.path.join(_BASE_DIR, "..", ".."))` — only two levels up from the file. The module had been moved from `src/bot/data_loaders.py` (depth 2) to `src/units/ui/data_loaders.py` (depth 3) during the S-032 reorg; the path-up calculation was not updated. Result: `REPO_ROOT` resolved to `<repo>/src` instead of `<repo>`, so `ACCOUNTS_YAML_PATH = <repo>/src/config/accounts.yaml` (does not exist), `SIGNALS_DB = <repo>/src/data/trades.db` (does not exist), and `TRADE_JOURNAL_DB` likewise fell through to a non-existent default. `list_accounts()` swallowed the empty result with `except Exception: return []`, masking the real cause | this PR — bumped the `os.path.join(_BASE_DIR, "..", "..", "..")` to three levels and added a comment naming the S-032 move so the regression is documented at the call site. Also updated the `/trades` empty-message string in `src/bot/telegram_query_bot.py` from "Add .env (legacy) or .env.<id> files." (stale post-S-012) to "Edit config/accounts.yaml and restart the trader." | config | drives the rule: when moving a module that computes paths via `os.path.dirname(__file__) + "../.."`, every move MUST adjust the `..` count and a regression test should pin the resolved path. Recurring shape — same as BUG-024 / BUG-026 (config-file path drift across refactors). Worth a sprint candidate: replace ad-hoc REPO_ROOT calcs with a single `src/utils/paths.py::repo_root()` helper that walks up to the marker file (`.git`/`pyproject.toml`/`requirements.txt`) so the path-up count can never go stale |
| BUG-036 | 2026-05-03 | 2026-05-03-vwap-improvement (training run) | strategy / signal-quality | VWAP strategy backtested unprofitable on 365 d BTCUSDT 5m: Sharpe -0.12, expectancy -0.002 R per trade, max DD -21.2 R, 946 trades. Win-rate looked healthy at 65% but the per-trade R/R at the 1.0σ entry threshold is exactly 1:1 (TP=VWAP, SL=entry±1σ), so 65% win-rate was barely break-even after losers paid full risk and winners only paid half | The default `ENTRY_STD_THRESHOLD=1.0` treats a one-standard-deviation move from VWAP as a tradable mean-reversion setup. A 1σ move is by construction a routine/noisy excursion, not a tail event — so the strategy was firing on noise. Compounded by no HTF / regime / session filter to weed out trades against trend | this PR — raised `ENTRY_STD_THRESHOLD` from 1.0σ to 2.0σ in `src/units/strategies/vwap.py`. Threshold sweep (1.0 / 1.5 / 2.0 / 2.5σ) showed monotonic Sharpe improvement up to 2.0σ then small regression at 2.5σ — clean local maximum. Same window backtest at 2.0σ: Sharpe 1.71, expectancy +0.044 R, max DD -5.2 R, 336 trades | strategy-tuning | architectural lesson: strategy default constants should be set from a backtest, not from a "feels reasonable" first-pass. `ENTRY_STD_THRESHOLD=1.0` had no empirical justification in the original implementation. Filing as a follow-up principle: every numeric threshold in `src/units/strategies/*` should reference a backtest run-id in its docstring. Full writeup in `experiments/2026-05-03-vwap-improvement/RECOMMENDATIONS.md`; PRs #348 (plan) / #349 (results) / #350 (recommendations, merged d52a816) |
| BUG-035 | 2026-05-03 | S-telegram-format | telegram messaging / live-mode interlock surfacing | Operator's per-tick Telegram still showed `Pipeline result: status=failed_validation \| symbol=BTCUSDT \| side=sell \| qty=0.001 \| reason=ALLOW_LIVE_TRADING=true is required for live submission`. Two observable problems: (a) message lacked `strategy=` even though G5 (CP-2026-05-02-09) added it — VM is running pre-G5 code; (b) reason did not name the actual value of `ALLOW_LIVE_TRADING` or whether it came from the settings dict / env / default, so the operator had to journalctl to debug | Pre-G5 deployment + a generic failure reason that didn't surface the resolver inputs. Recurrence likely if the deployed code path bypasses `build_settings_from_env()` (which sets the uppercase aliases) or if the env writes literal `false`/`0`/`disabled` | this PR (claude/standardize-telegram-messages-9pJzz) — (1) `safe_place_order` now resolves `ALLOW_LIVE_TRADING` via three-tier lookup (settings → env → default) and the failure `reason` includes the actual repr'd value AND its source. Same pattern for `DRY_RUN`. (2) Per-tick "Pipeline result" Telegram message is now an HTML-mode envelope (`src/units/ui/telegram_format.py`) with collapsable `<blockquote expandable>` sections: Strategy, Order package (entry/sl/tp/direction or "(not generated)"), Multi-account dispatch (when applicable), and Why & next step (only on failure — echoes reason + remediation hint). Header keeps the canonical `Pipeline result: status=... \| strategy=...` text so journalctl greps stay stable. (3) Hourly summary split into two parallel reports: strategies-focused (`build_hourly_report`, ticks/signals/health) and accounts-focused (`build_accounts_hourly_report`, trades placed/closed + per-account balance & open positions). Both rendered with the unified collapsable formatter. (4) `src/main.py` sends both reports each hour via `send_telegram_direct(parse_mode="HTML")` with `send_scheduled` plain-text fallback. (5) Tests: `tests/test_telegram_format.py` (10 tests), new pipeline-result envelope tests in `tests/test_orders.py`, hourly-report tests updated for the new shape | telegram | architectural lesson: silent per-tick failures need a *self-debugging* message — when the same alert fires every tick for hours, the diagnostic must include enough state for the operator to act without reaching for journalctl. Generic phrasing is a smell. Follow-up sprint candidate: drop the legacy `/hourly` plain-text fallback path once HTML mode is confirmed live in production. |
| BUG-034 | 2026-05-02 | recurring-hardening session 1 | order execution / multi-account dispatch | VWAP signals fired but Bybit never executed any of them. Hourly report showed `vwap: N signals / 0 fills`. Operator's journalctl: `pipeline_order → failed_dispatch: multi_account_execute: BybitAPI live placement requires an injected exchange_client; use execute_pkg() from src.units.accounts.execute for live trading.` The error message itself names the fix | `Coordinator.multi_account_execute` called `account.place_order(pkg, dry_run=False)` → `integrator.route_order` → `BybitAPI.place(dry_run=False)`, which raises `NotImplementedError` for live mode because the legacy `BybitAPI` stub never received an injected exchange client. The canonical live entry point `src.units.accounts.execute.execute_pkg` takes an `exchange_client` and was the right routing target. Compounded by a second silent failure: when live creds were missing `execute_pkg` flipped `is_dry=True` and returned a `dry-…` trade_id, so the operator believed they were live | this PR — (1) `Coordinator.multi_account_execute` now resolves a per-account `bybit_client_for(account_cfg)` (or `binance_conn_for`) and routes through `execute_pkg(pkg, account_cfg, exchange_client=client, qty_override=sized_qty, dry_run=…)`. The `qty_override` parameter (new) preserves the live RiskManager's already-sized qty so the daily-loss-budget state is not bypassed; (2) live-mode missing-creds is now a hard `RuntimeError("missing API credentials …")` instead of a silent dry-run fallback; (3) per-account execution failures (RiskBreach, RuntimeError, exchange SDK exception) now drop a JSON file into `runtime_logs/pending_pings/` via the new `src/runtime/execution_diagnostics.py::enqueue_execution_failure` — the bot's job-queue tick delivers it to the operator within ~5 s with account/strategy/symbol/side/qty/reason; (4) `tests/test_s028_vwap_execute_routing.py` (6 tests) pins the contract end-to-end: dry-run reaches `execute_pkg`, live builds the per-account client, `BybitAPI.place` is never touched, missing creds emit a ping, success paths stay quiet | order-execution | architectural lesson: when an exception's own message names the fix, treat that as a P0 for the next hardening session. The legacy `BybitAPI`/`integrator.route_order` path stayed in the codebase post-S-008 because dry-run runs (most tests) never tripped its `NotImplementedError`. The cleanup item is to drop `BybitAPI`/`BreakoutAPI` entirely once no live caller exercises them — the only remaining caller is `account.place_order` which is itself only used by tests post-fix. Filing as a follow-up sprint candidate |
| BUG-033 | 2026-05-02 | S-026 (G4) | audit log / strategy attribution | Hourly summary's "Last hour" block reported `Signals: 4 fired (unknown x4)` while the same hourly's "Strategies (today)" section correctly listed those signals under their real names (`vwap: 9 signals`). The trade-journal path attributed them correctly; only the audit-log path (`runtime_logs/signal_audit.jsonl::pipeline_result` rows) dropped `meta.strategy_name` | unidentified end-to-end. The G1 (#281) multiplexer fix already covered the leading hypothesis (`dict(signal)` shallow copy + meta-mutation losing `strategy_name`); the remaining leak path needs the operator's VM-side journalctl to nail down. Without VM access in-session, this PR ships the diagnostic instrumentation + a defensive fallback so the bug is bounded | this PR — (1) safe-default fallback in `run_pipeline._strategy` is now `"multiplexed"` instead of `"unknown"` so the missing-label "unknown" bucket can no longer eat real signals; (2) one-shot `logger.warning("audit: actionable signal lacks meta.strategy_name + top-level strategy; resolved=… via fallback. signal_keys=… meta_keys=… settings_has_STRATEGY=… env_has_STRATEGY=…")` fires when an actionable signal still lacks the strong attribution sources — next hourly cycle on the VM tells the operator (via `journalctl`) exactly which path under-attributes; (3) `tests/test_s026_g4_audit_attribution.py` (8 tests) pins the contract end-to-end (vwap-actionable, turtle_soup-actionable, multiplexer preserves attribution, defensive fallback is `"multiplexed"`, env STRATEGY wins, diagnostic warning fires for under-attributed signals only). Existing `test_pipeline_result_message_strategy_unknown_when_meta_missing` updated to expect `multiplexed` | audit-log | architectural lesson: the safety-default for an attribution field should match the **actual** producer (here: `multiplexed`, the production builder name when STRATEGY is unset). `"unknown"` was a generic placeholder that aggregators counted as a real bucket — it created the appearance of a separate failure mode. Once the diagnostic warning identifies the leak source, delete the warning + replace this row with the root-cause fix |
| BUG-032 | 2026-05-02 | S-XXX (telegram-bot UI overhaul) | telegram alert routing | Operator never received hourly summaries since some sprint earlier in 2026-05. Manual `/hourly` did fire `send_scheduled` but the alert never arrived | `src/runtime/notify.py::_send_via_alert_manager_async` called `mgr.send(message)` on `AlertManager`, but that class only exposes `send_alert`. Every send raised `AttributeError`, was caught by `outcomes._send_telegram_or_queue`, and the message was appended to the pending-queue JSONL — silently. Also blocked: `_send_via_alert_manager_async` used `asyncio.run` from inside the bot's running loop, which would have failed even if the method name was right | this PR — replaced the AlertManager dance with a direct stdlib `send_telegram_direct(message, parse_mode=None)` call. Made `parse_mode` configurable on `send_telegram_direct` (default still HTML for back-compat). New `tests/test_notify_send_via_alert_manager.py` (6 tests) covers the contract | telegram | recurring shape: silent-failure paths in async-wrapper code. The previous `try/except logger.warning` in `_send_telegram_or_queue` was protecting upstream callers from crashing but also hiding the never-recovered failure mode. The fix re-raises so `_send_telegram_or_queue` can correctly fall through to the pending-queue drain |
| BUG-031 | 2026-05-02 | S-XXX (telegram-bot UI overhaul) | telegram parse_mode | `/hourly` replied `⚠️ /hourly failed: BadRequest: Can't parse entities: can't find end of the entity starting at byte offset 138`. Same shape as BUG-009 + BUG-030 | `cmd_hourly` success-reply contained `send_via_alert_manager` (3 underscores) and `pending_pings.jsonl` (1 underscore). `parse_mode="Markdown"` interpreted unbalanced underscores as italic delimiters and Telegram returned BadRequest, which the operator-facing handler relayed verbatim | this PR — dropped `parse_mode="Markdown"` from the success-reply; new regression test `TestCmdHourlyReplyMarkdown` asserts no parse_mode is set | markdown | THIRD occurrence of the same shape (BUG-009 #190, BUG-030 #265, this). The pattern is: ANY operator-facing reply that includes Python identifiers / file paths / log messages with underscores must NOT use `parse_mode="Markdown"`. A small lint that grep's for `reply_text(... parse_mode="Markdown"` would catch this at PR time |
| BUG-030 | 2026-05-02 | S-XXX (telegram-bot UI overhaul) | telegram parse_mode | `/last5` replied `⚠️ Could not render trade: Can't parse entities: can't find end of the entity starting at byte offset 621`. Same shape as BUG-009 | `_format_trade_row` rendered DB columns (`notes`, `entry_reason`, `exit_reason`, `setup_type`) inside a `parse_mode="Markdown"` message. Those columns frequently contain `*`, `_`, `[`, ` ` ` characters that the legacy Markdown parser can't disambiguate, so Telegram returned `BadRequest: Can't parse entities` and the renderer swallowed it as the user-facing warning | this PR — dropped `parse_mode="Markdown"` on the `/last5` reply, removed the `*Trade #N*` decoration, added regression tests asserting `_format_trade_row` accepts arbitrary content and that `cmd_last5` no longer attaches `parse_mode='Markdown'` | markdown | second occurrence of the same shape (see BUG-009). Confirms the rule "do not use `parse_mode='Markdown'` on dynamic / DB-sourced content". The `/help`, `/balance`, `/trades` paths still use Markdown on dynamic strings — flagged for follow-up in the G3/architecture audit |
| BUG-029 | 2026-05-01 | operator-onboarding | telegram bot | `/accounts_status` still showed "missing env vars" after a successful key rotation that wrote `.env` and restarted the trader | Notebook only restarted `ict-trader-live.service`. `/accounts_status` runs in `ict-telegram-bot.service` (separate systemd unit), which kept reading its old environment from before the `.env` write | #253 — notebook restarts BOTH services; allow_fail on the bot one for VMs that don't run it | deploy | drives the rule: any state derived from `os.environ` lives in a process; rotating env requires restarting *every* process that reads it. Worth a section in `repo-map.md` listing which surfaces live in which systemd unit |
| BUG-028 | 2026-05-01 | operator-onboarding | telegram parse_mode | Backslash-underscore escapes (`\_`) rendered as visible `\_` in `/accounts_status` output instead of being processed as escaped underscores | Telegram's legacy `parse_mode="Markdown"` does NOT process backslash escapes (only `MarkdownV2` does). The previous fix (BUG-027) used the wrong mechanism | #253 — switched `/accounts_status` to `parse_mode="HTML"`; `<b>` for bold, `<code>` for code, only `& < >` need escaping | markdown | recurring shape — escape mechanisms differ across parse modes; a single helper per mode is clearer than trying to escape for whichever happens to be selected |
| BUG-027 | 2026-05-01 | operator-onboarding | telegram parse_mode | Env var names like `BYBIT_API_KEY_1` in `/accounts_status` rendered as `BYBITAPIKEY1` (underscores eaten as italic markers) | Telegram's legacy Markdown treats `_` as italic. The diagnostic was correct in code; only rendering broke | #252 (incomplete — tried Markdown escaping which doesn't work; superseded by BUG-028 / #253 HTML mode fix) | markdown | first attempt at this fix was ineffective. Prefer HTML mode for any handler whose output contains user-visible identifiers |
| BUG-026 | 2026-05-01 | operator-onboarding | systemd / .env file | `.env.live` was written by `scripts/render_env_from_master.py` and the rotation notebook, but the systemd unit `ict-trader-live.service` reads `EnvironmentFile=/home/.../ict-trading-bot/.env` (no `.env.live`). Result: rendered keys were never picked up by the running service | Convention drift across files: render script + `pipeline.py::load_dotenv(".env.live")` use `.env.live`; systemd unit uses `.env`; nothing reconciles them | #252 — rotation notebook now writes both `.env` and `.env.live` atomically. `deploy/ict-trader-live.service` itself untouched (deploy/ requires PM review) — long-term fix is to add `EnvironmentFile=-/home/.../.env.live` to the unit so either file works | config | architectural concern: 3-way label/path drift across config files (this is the same concern as BUG-024). Drives the rule: when a config-loading code path knows about file X, the systemd unit must also know about file X |
| BUG-025 | 2026-05-01 | operator-onboarding | colab / Drive mount | Notebook reported "SSH key not found" before the Drive auth dialog completed — race between `drive.mount()` returning early on a stale token and the path scan running | Single cell wrapped Drive mount + secrets load + key lookup; `drive.mount()` is supposed to block on auth but in some Colab sessions returns early. No verification step | #251 — split into 3 cells: mount (with `force_remount` retry + `MyDrive` exists check), then locate-key (with `files.upload()` interactive fallback when not found in Drive), then load-secrets | config | recurring shape: trusting that an external SDK call blocks/completes when its docs say it does. Verify post-conditions explicitly |
| BUG-024 | 2026-05-01 | S-023 | accounts.yaml ↔ master ↔ render | Three-way label drift: `accounts.yaml` declared `BYBIT_API_KEY_1` etc., master template had no per-account block, render script wrote `BYBIT_API_KEY` (singular). Result: every account showed "balance unavailable" no matter what was in master file | Lack of a config-drift regression test crossing the three files | #243 — render script reads `accounts.yaml` to drive per-account env-var emission; master template gains `bybit.accounts.<id>` block; new test asserts every accounts.yaml id has a matching template entry | config | drives the proposal: "any time two config files reference each other, write a regression test that walks the cross-references". Worth adding to `docs/claude/testing-policy.md` |
| BUG-023 | 2026-05-01 | S-023 | data_loaders projection | `_load_yaml_accounts` projected accounts.yaml entries through a fixed schema that silently dropped `api_key_env` / `api_secret_env`. Even with PR #243 wiring up the env vars, downstream `bybit_client_for(account)` couldn't see them | Projection-to-fixed-schema pattern that whitelists fields by accident | #244 — projection now preserves `api_key_env`, `api_secret_env`, `type`, `risk` | config | drives the rule: when refactoring config-loading code, prefer non-dropping transforms unless there's a privacy/security reason. Pre-existing test bug in `test_data_loaders.py` (two `_bybit_account` functions with the same name) was masking this — also fixed in #244 |
| BUG-022 | 2026-04-30 | S-020 | Telegram auto-ping | Auto-ping on `CHECKPOINT_LOG.md`-touching commits never fired (CP-15 / #226 confirmed lost), while manual `/ping_test` works end-to-end | `deploy_pull_restart.sh` baselined the ping diff against `PRE_SYNC_HEAD` (this run's local HEAD), so a mid-debug `git reset --hard` outside the timer's window left no record of which commits were already pinged. Next tick saw pre==post and skipped. | this PR — persist `runtime_logs/notify_state.txt`; ping baseline = last-notified head, not last-tick head; add `--force-checkpoint` + `runtime_flags/auto_ping_test.flag` for manual verification | telegram | architectural lesson: any "have we already done X for commit Y?" check must persist across runs, not rely on local in-memory state. Closes the BUG-018 thread for real |
| BUG-021 | 2026-04-30 | S-016 | tests / shared sys.modules state | Sprint015 tests started failing when run in the same pytest invocation as telegram tests because the latter `sys.modules.setdefault("pandas", MagicMock())`-d at file-level | Per-file optional-dep stubs polluted shared session state | #218 — moved to `tests/conftest.py` with conditional `_stub_optional` (only stubs when the real module isn't installed) | tests | drives BUG-010's broader fix |
| BUG-020 | 2026-04-30 | S-016 | Telegram bot UX | `/sprintlet_status` and `/sprintlet_complete` hardcoded `"S-008.5"` (~70 sprints stale) | Hand-edited string in two cmd_* handlers; never sourced from CHECKPOINT_LOG.md | #215 — handlers now parse the topmost CP entry's `Sprint:` field | config | recurring shape: stale-text drift in handler output |
| BUG-019 | 2026-04-30 | S-016 | Telegram /status leak | Per-account block in `/status` displayed the systemd unit name (`ict-trader-live`) which is identical for every account since S-012 (single-process architecture) | Service name was useful pre-S-012 when each strategy had its own unit; never cleaned up after the consolidation | #215 — dropped the service column; strategy header stays | config | drives the rule "remove implementation details from operator-facing output" |
| BUG-018 | 2026-04-30 | S-015 | Telegram ping | Operator not receiving sprint progress pings | TELEGRAM_BOT_TOKEN absent in sandbox env; `notify_session.py` fails on missing pandas; no VM-side trigger on checkpoint commits | #213 — VM-side `scripts/notify_on_pull.py` wired into `deploy_pull_restart.sh`; stdlib + requests only, idempotent on no-advance pulls | telegram | first fix landed S-016 H3, then S-018 (#225) added `EnvironmentFile=`, then S-019 (#227) decoupled via inbox/drain. Final fix in S-020 (BUG-022) — this row is now fully resolved end-to-end |
| BUG-017 | 2026-04-30 | S-015 | data_sources / coinmetrics | github-raw adapter returned 0 rows for 2025 window despite the CSV having 5,765 valid bars | Used `ReferenceRateUSD` column which only populated for the last 7 rows; correct column is `PriceUSD` | #207 | data | column-name assumption — verify schema before parsing |
| BUG-016 | 2026-04-30 | S-015 | CHECKPOINT_LOG conflicts | Final checkpoint PR (#205) hit a CHECKPOINT_LOG merge conflict because the mid-session checkpoint (#203) had landed at the same insertion point | Two append-only-at-top sections opened from different bases → both want line 11; rebase fails | #206 (re-issue with rebase done locally) | git | recurring — see also BUG-006; could be solved by an "append at bottom" log convention or a non-positional ID-keyed log |
| BUG-015 | 2026-04-30 | S-015 | data egress | Sandbox returns 403 for every keyless market-data API; egress allowlisted to pypi + github only | Egress proxy policy — out of repo's control | #207 (worked around with github-raw + coinmetrics) | tls | drives the testing-policy.md note we still need to add |
| BUG-014 | 2026-04-30 | S-015 | VWAP timeframe precedence | Setting `vwap.timeframe: "5m"` in `config/strategies.yaml` would silently no-op if any account's `.env` had `TIMEFRAME=15m` | `vwap_signal_builder` consulted env first, YAML last | #209 | config | recurring shape: env-vs-yaml precedence — see also BUG-009 |
| BUG-013 | 2026-04-30 | S-015 | yfinance noise | Smoke-test report had `Failed to get ticker 'BTC-USD'…HTTP 403…` lines leaking into the markdown | yfinance writes to stderr from inside the Python interpreter; the script captured stdout but not stderr | fixed in #208 by `2>/dev/null` on the runner invocation | data | better fix is logger redirection inside the adapter |
| BUG-012 | 2026-04-30 | S-014 | gitignore — fragments/ subdir | M3 PR #1 lost `web/templates/fragments/*.html` because the repo-wide `*.html` exclusion swallowed them | `!web/templates/*.html` whitelist is non-recursive | #195 (added `!web/templates/**/*.html`) | git | drives the "document the recursive whitelist pattern" item still pending in carry-over |
| BUG-011 | 2026-04-30 | S-014 | gitignore — top-level templates | M1 PR #1 lost `web/templates/*.html` for the same reason | repo-wide `*.html` exclusion (added for coverage reports) hit web templates | #192 (added `!web/templates/*.html`) | git | also see BUG-012 |
| BUG-010 | 2026-04-30 | S-014 | tests / telegram stub | Module-level `_VM_WRITE_BUTTONS = InlineKeyboardMarkup([[…]])` in PR #184 broke the `_tg.InlineKeyboardMarkup = MagicMock` stub used by ~10 existing test files | `MagicMock([[…]])` constructor crashes `_mock_set_magics` because lists are unhashable | #218 — `tests/conftest.py` centralises the lambda-factory stub; 10 legacy test files updated to defer to it | tests | resolved by S-016 H5 |
| BUG-009 | 2026-04-30 | S-014 | /signals Markdown parse | Telegram `/signals` returned no message even though `runtime_logs/signal_audit.jsonl` was growing | Pipeline statuses / reasons (`no_signal`, `failed_validation`) contain underscores; the formatter wrapped them in legacy-Markdown italic / bold delimiters → unbalanced italic → Telegram rejected with `Bad Request: Can't parse entities` → BadRequest swallowed in reply_text | #190 (switched to plain text + emojis) | markdown | recurring shape: legacy Markdown parse — avoid `parse_mode="Markdown"` on dynamic content |
| BUG-008 | 2026-04-30 | S-014.5 | git-sync over-restart | `ict-git-sync.timer` restarted both services every 5 min unconditionally, killing in-flight `/vm` runner cgroup children | `scripts/deploy_pull_restart.sh` had explicit "no-op restart is cheap" logic | #188 (conditional restart on HEAD advance + defer if claude-vm-runner@*.service is active) | deploy | timer-driven side-effects need idempotency |
| BUG-007 | 2026-04-30 | S-014.5 | systemd ProtectHome | `/vm` runner ran (exit 0) but Claude's Bash tool was disabled because `~/.claude/session-env` was unwritable | `ProtectHome=read-only` on `claude-vm-runner@.service`; needed selective `ReadWritePaths` for `~/.claude`, `~/.cache`, `~/.config/claude` | #187 | deploy | systemd hardening interacts with toolchain expectations |
| BUG-006 | 2026-04-30 | S-014.5 | systemd-run polkit hang | Bot's wrapper subprocess hung silently when invoking system-mode units as non-root | `systemd-run` non-root needs polkit auth, which has no tty in the bot's cgroup | #186 (privileged dispatcher + sudoers drop-in) | deploy | privilege-boundary needs an explicit wrapper, not a `sudo` chain |
| BUG-005 | 2026-04-30 | S-014.5 | apscheduler / tzlocal | Bot crash-looped 121 times before VM session restarted it cleanly | `apscheduler 3.6.3` ↔ `tzlocal 5.x` timezone format mismatch — fixed by `apscheduler>=3.10.4` | #219 — pinned `apscheduler>=3.10.4`, `pytz>=2024.1`, `tzlocal>=5.0` in requirements.txt | deploy | resolved by S-016 H6 |
| BUG-004 | 2026-04-30 | S-014.5 | empty Anthropic API credit | Bot couldn't dispatch `/vm` because pay-as-you-go API key had $0 balance | external billing | switched to long-lived OAuth subscription token via `claude setup-token`; `CLAUDE_CODE_OAUTH_TOKEN` in `/etc/ict-trader/claude.env` | auth | also: leaked OAuth token from chat needs revoke (still pending) |
| BUG-003 | 2026-04-30 | S-014 | bybit /balance silently empty | `/balance` for accounts.yaml entries said "balance unavailable" | `account_balance` and `account_open_positions` inlined `_bybit_client(env)` (legacy env_path-only) instead of routing through the api-key-env-aware `bybit_client_for(account)` | S-012 hotfix #3 (predates this log) | config | recurring shape: legacy code path bypasses the post-refactor API |
| BUG-002 | 2026-04-30 | S-014 | strategy/account wiring | Both Bybit accounts had `strategies: [turtle_soup, vwap]`, but bybit_2 wallet is USDT-only and turtle_soup needs BTC base | config drift between intent and YAML | #198 (turtle_soup → bybit_1 only; vwap → bybit_2 only; prop disabled) | config | drives the no-PM-review-needed-for-config rule clarification |
| BUG-001 | 2026-04-30 | S-014 | PR rebase loop | PR #183 (M0 backend) was carried over from previous session and had a CHECKPOINT_LOG conflict with main | append-only-at-top doc + cross-session rebase | resolved at start of S-014 by taking main's superset | git | see BUG-016 — same shape, recurring |

## Standing patterns flagged for architecture planning

If a category accumulates ≥ 3 entries, surface it as a discussion topic in
the next planning sprint. Current candidates:

- **`config` (5 entries — BUG-002, BUG-003, BUG-014, BUG-019, BUG-020)** —
  config / env / YAML precedence + stale-text drift in handler output is
  the most common bug source. Worth designing a single
  "settings resolver" module with documented precedence + unit-tested
  independently of every consumer; plus a convention that operator-facing
  text never embeds environment-dependent constants.
- **`git` (4 entries — BUG-001, BUG-011, BUG-012, BUG-016)** — append-only
  CHECKPOINT_LOG conflicts and gitignore patterns recur. Worth considering
  a non-positional log format (one file per checkpoint, indexed). The
  gitignore-whitelist convention now lives in `docs/claude/git-workflow.md`
  (S-016 H5) but the log-format question is open.
- **`deploy` (4 entries — BUG-005, BUG-006, BUG-007, BUG-008)** — systemd /
  polkit / dependency drift between sandbox and VM is a recurring failure
  mode. Worth treating the VM bootstrap as a tested contract, not a
  sequence of hand-fixed hotfixes. BUG-005 now has a runtime-pinning fix
  (S-016 H6); the deeper "VM-as-contract" architectural item remains.
- **`tests` (2 entries — BUG-010, BUG-021)** — shared `sys.modules` state
  across test files breaks isolation. Resolved this round via
  `tests/conftest.py` centralisation (S-016 H5); flagged here so future
  copy-paste regressions get caught early.

## Resolved during S-016 housekeeping

| Bug | Was | Fix PR |
|---|---|---|
| BUG-005 | apscheduler/tzlocal not pinned in requirements.txt | #219 |
| BUG-010 | Telegram MagicMock stubs broken in ~10 test files | #218 |
| BUG-011, BUG-012 | gitignore recursive whitelist undocumented | #218 |
| BUG-015 | sandbox no-egress note undocumented | #218 |
| BUG-018 | operator not getting Telegram pings on checkpoints | #213 |
| BUG-019 | /status leaked systemd unit name per-account | #215 |
| BUG-020 | sprintlet handlers hardcoded "S-008.5" | #215 |

## Cross-references

- `docs/claude/sprint-planning.md` — sprint planning template that
  references this log.
- `docs/claude/checkpoints/CHECKPOINT_LOG.md` — sprint-level history.
