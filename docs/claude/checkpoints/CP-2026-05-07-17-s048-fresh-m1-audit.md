## CP-2026-05-07-17-s048-fresh-m1-audit — S-048 (fresh re-issue): M1 comms infrastructure deep audit (PARTIAL verdict)

> **Note (2026-05-07):** delivered as a standalone CP file because the
> running `CHECKPOINT_LOG.md` had grown beyond the per-call API push
> budget for this session. A future Janitor sprint should fold this
> entry into the top of `CHECKPOINT_LOG.md` per the standard pattern.
> Until then, this file is the canonical CP-17 record.

- **Session date:** 2026-05-07 (continuation of `CP-2026-05-07-16`).
- **Sprint:** S-048 — M1 comms infrastructure deep audit (M1 reopen). Tier 1 docs-only audit + four P1 follow-ups in the same session per operator directive.
- **Active milestone:** M1 reopened ⚠️ → 🔄 PARTIAL after this audit. Hand-off resumes S-047 T6 per § 8 of `docs/sprints/sprint-048-prompt.md` (no P0 surfaced).
- **Last completed checkpoint:** `CP-2026-05-07-16-s047-T5-reconciler-spot-margin`.
- **Branches:** all S-048 deliverables (and the four P1 follow-ups for the same session) on `claude/update-roadmap-status-ZnLM9`. Supersedes the earlier S-048 attempt on `claude/audit-comm-channels-9UWa0` (PR #463 closed in favour of this PR).
- **Telegram sent:** sprint-start + sprint-complete pings ride on this branch's checkpoint commit.

### Why a fresh re-issue

S-048 was first delivered as PR #463 on 2026-05-07 afternoon. The operator validated the substance post-write but flagged that the body described the workplan-vs-reality conflict as an *implementation gap* when in fact the **workplan is wrong** (ClaudeBot is intentionally one-way; the S-027 two-way request/response system correctly lives on `@bict_trading_bot`; merge decisions happen on GitHub, not via Telegram callbacks). The post-write redlines tacked the corrected reading onto a stale body.

Per operator directive 2026-05-07 evening, this fresh re-issue:

1. Bakes the corrected architecture directly into the audit body (no header-redlines split).
2. Reflects the actual current sprint reality — S-047 T1..T5 + S-049 fast-followup all shipped between PR #463 and now; the active sprint is S-047 T6, not T3.
3. Reduces the P1 list to four real gaps under the corrected reading (was seven before three were dropped as workplan-drift not implementation-drift).

PR #463 is closed in favour of this re-issue. PR #467 (a contradictory S-047 T3 close-checkpoint that re-ordered S-048 to *after* S-047) was closed earlier in this session.

### Deliverables in this session

- **D1** `docs/audits/M1-comms-audit-2026-05-07-fresh.md` — master audit report. Verdict 🔄 PARTIAL, no P0.
- **D2** `docs/audits/M1-comms-audit-followups-fresh.md` — four P1 follow-ups + one P2 hygiene cluster.
- **D3** `docs/claude/milestone-state.md` — M1 row → 🔄 PARTIAL with backlog reference; Active sprint stays at S-047 T6.
- **D4** `ROADMAP.md` — M1 row mirrors D3; queue updated.
- **D5** this checkpoint file.
- **Sprint summary** `docs/sprint-summaries/sprint-048-summary.md`.
- **P1 follow-ups (same-session, operator directive):** P1-A workplan correction, P1-D `/new_session` + `/test` commands, P1-B stuck-request recovery, P1-C auto-hourly snapshot. Each ships in its own commit on the same branch with its own subset of files; see PR description for the full landed-files list.

### Top P1 follow-up summaries (full text in D2)

- **P1-A** Workplan correction — `docs/claude/workplan.md` § "ClaudeBot workflow" rewritten to describe the one-way channel; the merge-review-buttons / required-action-prompts / recovery-alerts language removed or relocated to describe the trader-bot S-027 surface. (Tier 1 docs.)
- **P1-D** `/new_session <sprint_id>` and `/test <strategy>` on the trader bot — write `comms/requests/REQ-…json` artifacts via the existing S-027 surface; M5 inherits the `/test` artifact consumer. (Tier 2.)
- **P1-B** Stuck-request recovery alerts — `CommsPoller.poll_once` fires Telegram alerts before silently transitioning to EXPIRED. (Tier 1.)
- **P1-C** Auto-hourly snapshot timer + service — `deploy/ict-hourly-snapshot.timer` + `.service` fire `scripts/send_hourly_now.py` once per hour. (Tier 2.)

### § 4.4 compliance check

1. ✅ No refuse-to-trade outside the dispatcher — pure docs + comms-side code, no order-path edits.
2. ✅ No per-account refusal flag/branch — no risk-engine code touched.
3. ✅ No operator-run notebook / capture step — operator just runs `systemctl daemon-reload` for the new hourly timer (P1-C); standard install path.
4. ✅ Live-mode invariant passes — `scripts/check_dry_run_in_diff.py` clean over the entire branch.
5. ✅ CI green — see PR check-runs.

### Hand-off

Per sprint-prompt § 8 — no P0 surfaced — next sprint = current active sprint per `milestone-state.md`. As of this checkpoint that's **S-047 T6 (end-to-end live smoke + runbook)**. The four P1 follow-ups have already landed in this same session; they do not need a separate next-session hand-off.

The next session opens against:

1. `CLAUDE.md` (router).
2. This file (CP-17).
3. `docs/claude/milestone-state.md`.
4. `docs/sprint-plans/S-047-bybit2-spot-margin.md` § T6.
