{
 "schema_version": 1,
 "run": "system-review 2026-09-11 (MI-272, session_01W56JbiKrada5AM4pGtHxmg)",
 "started_at": "2026-09-11T17:51:50+00:00",
 "items": {
  "strategy_promotion": {
   "status": "in_progress",
   "evidence": "PARTIAL AND DELIBERATELY SO. The real-money roster was read (bybit_2 the only account actually trading real money; alpaca_live mode live/real_money but FLAT with 0 orders/0 positions — an empty denominator, not coverage; ib_live dry_run/strategies[]). NO per-leg promote/demote verdict was produced, because MI-271 owns the 2026-08-30 directional-leg break and the drawdown attribution, and two lanes producing competing verdicts off the same contaminated 30d instrument is worse than one. Routed, not duplicated."
  },
  "ml_training_health": {
   "status": "done",
   "evidence": "8 cycle_start / 8 cycle_end over 1000 rows of /api/bot/ml/cycle (2026-09-03T05:36Z..09-11T06:07Z); overall_rc=0 and failed=0 on all 8; manifest_failed / manifest_quarantine_tripped / manifest_quarantined all ZERO with a stated positive control (all three are live emitters in run_training_cycle.sh:396,613,624). My >=09-08 sub-window is 456 of those rows and FULLY INSIDE the 1000 cap. ⚠️ builds DOES truncate: /api/bot/ml/builds caps at 500 and reaches only to 09-09T05:20Z, so 09-08 dataset builds are COULD-NOT-LOOK, not zero. Trainer disk 86.9%, up from 86.1% on 09-08 — the WATCH has grown, +0.27pp/day on two dated points (not a trend line). 6 manifests are not training: 4 refused every cycle (audit_skipped_enforced, 47d), 2 with ZERO runs ever."
  },
  "soak_status": {
   "status": "done",
   "evidence": "10 soaks graded with per-soak denominators and rates. 9 accruing. advisory_decisions DISSOLVED not stalled — newest row 2026-06-25, 78 days, and the head it scored is at shadow today, so the advisory-SIZING path has had no advisory-stage input for 78 days and is a permanent no-op (stated as a consequence, not filed as a stall). ict_scalp_exit_head_soak accruing but thin: 968 not_scored / 32 scored_no_fire / 0 fires per 1000. ⚠️ LOUD FINDING: setup-quality-lgbm-v2 reports soak_days 80.42 against a model the trainer has REFUSED to retrain for 47 days — the BL-20260628-XA-TRAINING-ZERO class, filed."
  },
  "execution_capture": {
   "status": "not_started",
   "evidence": "NOT DONE. position_telemetry capture/giveback was not re-measured this run. Recorded as not-done rather than implied — the prior run's 2026-09-08 figures stand and must not be re-quoted as current."
  },
  "backlog_drive": {
   "status": "done",
   "evidence": "8 rows RETIRED, every one on its own stated criteria and each re-verified against the tree rather than taken on report: 2 resolved (AUDIT-F61 — the audit doc's F-61 row cites the row by id; EXIT-HEAD-AUC — 60 rows / 9 distinct fold_offsets in the sibling artifact its criterion permits), 2 wont_fix (IBLIVE-2FA on a re-verified dry_run/no-strategies config with a reopen condition recorded; BL-20260607-005 on its own second disjunct), 4 superseded across 2 duplicate clusters. ⚠️ BOTH CLUSTERS WERE CHECKED FOR RECURRENCE FIRST and neither is one — no fix ever shipped in either case. 9 rows filed (7 health, 2 ml). NET ON THE OPEN SET: 865 -> 864. Stated plainly rather than framed as a burn-down: this review found more than it closed, and the filings are the more valuable half."
  },
  "account_reachability": {
   "status": "done",
   "evidence": "All 11 accounts read. 24 of 24 open journal rows match venue quantity EXACTLY — that 24/24 is the positive control that makes the reconciliation trustworthy. All 13 Alpaca positions at exactly 1.00x stop AND target coverage. Only non-reachable are the two declared exclusions (breakout_1 API-less by design, ib_live dry_run). ⚠️ TWO DEGRADED READ-STATES, neither a failure: bybit_2 (REAL MONEY) available_usd reads coin_derived with is_broker_truth FALSE — correctly labelled, not collapsed, but the only real-money crypto account is sizing against a derived figure; and ib_paper's position read is intermittent and reports 'could not look' as error: null."
  },
  "since_last_build_verification": {
   "status": "not_started",
   "evidence": "NOT DONE. The shipped-capability enumeration since the last review was not performed this run."
  },
  "backlog_classes": {
   "status": "done",
   "evidence": "6 structural classes named over the WHOLE open set (950 open+kept_open across all four backlogs — note this is 1707 total rows / 7.7MB for health alone, NOT the 951/5.1MB CLAUDE.md still states, which is stale by ~500 rows). Largest: E1, the merge_slot single mutable key with no release mechanism — 18 open rows, one keyed-append fix, and a GHOST CLAIM was live in session-board.json during this review (held_by a branch that had merged 6 minutes after claiming). Also E4 (bybit hedge-book reads that cannot see the second book, 4 rows, real money) and E5 (detection exists, logger.error reaches only the journal, 6 rows — whose own tracker row says 'one member left' and is therefore measurably wrong)."
  },
  "ml_output_actionability": {
   "status": "n_a",
   "evidence": "NOT ASSESSED this run — deliberately, and the reason is a finding rather than an omission: the drift input that gates promotion is computed against 9.4% of its declared window (see ml_review.promotion_recommendations), so any actionability verdict taken today would rest on it."
  },
  "structural_health": {
   "status": "not_started",
   "evidence": "NOT DONE. The whole-history close-reason composition analysis was not re-run; MI-271 owns the performance half."
  },
  "unexercised_fixes": {
   "status": "done",
   "evidence": "Graded via OPEN-ITEMS observation. Two landed-and-now-EXERCISED confirmed with live evidence: the Alpaca share-hold classifier (close_wedge_standing carries a non-empty wedges with share_hold cancel_accepted_ineffective — the SECOND determination, so the classifier is discriminating rather than collapsing), and the closed-flat invariant (9 violations — exercised, and WRONG, see findings). Two shipped-and-still-unexercised recorded against their rows (blocked-lane watch, rollback registry)."
  },
  "research_results_disposition": {
   "status": "not_started",
   "evidence": "NOT DONE this run. The 103/11/256 disposition census was not re-measured; the prior figure must not be re-quoted as current."
  },
  "test_execution_verification": {
   "status": "blocked",
   "evidence": "BLOCKED — and the blocker is environmental, not a red tree. pytest is NOT INSTALLED in this sub-session container, which is a known and recorded property of these containers (a clean main reproduces 'No module named pytest'). So any guard whose command is a pytest invocation reports FAIL here for the environment, not for the diff. Recorded as blocked rather than skipped; CI is the authority for this item."
  },
  "health_review.findings": {
   "status": "done",
   "evidence": "7 findings, led by two P1s: the 60s exit-evaluation requirement BREACHED on the durable cross-process record (1,219/72,148 intervals = 1.69%, max 2.00x; restart gaps 77/462 = 16.7%, max 3.51x) while the live per-process instrument reads `within` on a 95-minute process that never drew the tail; and the closed-flat invariant grading the symbol NET book as the closing trade's residual."
  },
  "health_review.security_check": {
   "status": "in_progress",
   "evidence": "PARTIAL AND SAID SO, not implied clean. 858 request lines parsed from the web-api journal; 17 auth denials, all 401s on session-gated routes hit with a diag bearer — including my own probe, which served as the positive control. Ordinary internet scanning present and all rejected (403/404). ⚠️ THE WINDOW IS ~11.4h, NOT since 09-08: the journal tail caps at 1000 lines and reached only to 09-11T06:38Z. The 09-08..09-11T06:38 window is UNMEASURED, not clean. ⚠️ AND A STATED LIMIT ON THE VERDICT: the diag token is publicly exposed (accepted risk), so a third party using it would present as a cloud IP with a valid token — indistinguishable by IP from our own runners. The clean read rests on route-fingerprint plausibility, which is evidence, not proof."
  },
  "health_review.soak_decisions_due": {
   "status": "done",
   "evidence": "mes-regime-15m-lgbm-v2 re-surfaced, now 53 days parked. ⚠️ CORRECTED FRAMING: the prior review escalated it as met-and-unactioned. The operator ALREADY ANSWERED on 2026-08-16 — 'do NOT promote yet, compare the two heads head-to-head first' — because advisory resolves per-SYMBOL, so promoting it would silently DISPLACE the operator-approved 5m incumbent rather than add an opinion. What is owed is the MEASUREMENT, owed 26 days. Re-escalating it as a pending decision would re-ask a settled question."
  },
  "health_review.new_work_compliance": {
   "status": "not_started",
   "evidence": "NOT DONE this run."
  },
  "health_review.anomalies": {
   "status": "done",
   "evidence": "4 recorded: restart_pending is true-by-construction after any docs-only merge and reads as its own docstring's '2026-05-09 incident state' (I verified all 33 changed files are non-runtime and nearly filed a false P1); exchange_positions reports a failed ib_paper read as error: null; /api/diag/journal is capped at 1000 rows with no offset, which presented four venue positions as unjournalled orphans that are NOT (ids below the floor) — a live instance of 'a count that reads 0 may mean we could not look'; and /api/diag/audit_query returned malformed JSON to one parser, unconfirmed."
  },
  "health_review.backlog_drain": {
   "status": "done",
   "evidence": "8 retired / 7 health rows filed. See backlog_drive."
  },
  "health_review.backlog_coverage": {
   "status": "done",
   "evidence": "Whole open set read: 950 open+kept_open across all four backlogs, counted directly from the parsed items[] rather than via the helper. ⚠️ THE HELPER IS BROKEN AND IT IS WORSE THAN PREVIOUSLY RECORDED: backlog_burndown() reads only opened_at, and 465 rows (not ~287) carry no opened_at — 371 of them still open — while the real creation date lives under at least seven other keys. It also never reads research-review-backlog.json at all (19 rows, 13 open). September is filing ~37 rows/day against 1.5 closes/day."
  },
  "health_review.sprint_doc_review": {
   "status": "done",
   "evidence": "Done, and it produced real coherence findings. CLAUDE.md states the health backlog is '951 rows / 5.1 MB' — measured today it is 1462 rows / 7.7 MB, stale by ~500 rows. CLAUDE.md also states api.github.com is 403 from a session; measured today curl reached it successfully with the proxy-injected token. Both recorded for the doc-freshness pass rather than silently worked around."
  },
  "performance_review.strategy_performance": {
   "status": "n_a",
   "evidence": "OWNED BY MI-271 (research lane), which has the 2026-08-30 directional-leg break and the drawdown attribution. Deliberately not duplicated — overlapping measurement produces two numbers that disagree and nobody knows which to trust. Not an omission; a routing decision."
  },
  "performance_review.trade_decision_grades": {
   "status": "not_started",
   "evidence": "NOT DONE. No order packages were graded this run and comms/claude_strategy_scores.jsonl was not written. This is a REAL GAP in the mandate, recorded as not-started rather than excused."
  },
  "performance_review.insights_review": {
   "status": "not_started",
   "evidence": "NOT DONE this run."
  },
  "performance_review.proposed_tweaks": {
   "status": "n_a",
   "evidence": "Tier-3 and owned by MI-271. None proposed from this lane."
  },
  "performance_review.backlog_drain": {
   "status": "in_progress",
   "evidence": "Performance backlog READ for class membership (18 open / 29 kept_open of 117) but 0 closed from this lane — the open rows are predominantly Tier-3 or blocked on MI-271's attribution."
  },
  "performance_review.paper_book_tracker": {
   "status": "not_started",
   "evidence": "NOT DONE this run."
  },
  "performance_review.real_money_allocation_benchmark": {
   "status": "n_a",
   "evidence": "OWNED BY MI-271."
  },
  "performance_review.anomalies": {
   "status": "not_started",
   "evidence": "NOT DONE this run."
  },
  "ml_review.trainer_findings": {
   "status": "done",
   "evidence": "Trainer alive; mirror 19.4s fresh and reconciling EXACTLY with the live registry on every stage; ict-trainer.timer active/enabled, last trigger 09-11T00:07:56Z; live->trainer DB sync ok at 05:01:15Z rc=0. Disk 86.9% (WATCH, grown from 86.1%). All trainer facts are MIRROR-DERIVED — no relay was dispatched, so stage-guard / gate-check / list-models were NOT run. Stated rather than implied."
  },
  "ml_review.model_status": {
   "status": "done",
   "evidence": "THE 34/4 RATIO VERIFIED EXACTLY and reconciled two ways with ZERO stage disagreements: 34 scoring records (30 shadow + 4 advisory) = 32 registry rows at shadow+ − 2 registered-but-not-scoring + 4 off-registry exit heads. Soak ages over the 34: median 80.0d, mean 72.5d, max 115.6d, 11 models >=100d."
  },
  "ml_review.promotion_recommendations": {
   "status": "done",
   "evidence": "THE OPERATOR'S QUESTION ANSWERED DIRECTLY: 34-shadow/4-advisory is NEITHER a healthy funnel NOR a stalled gate — it is a STRUCTURAL CEILING. Advisory resolves per-SYMBOL (ml_vol_regime_for_symbol), so the 3 advisory registry heads are exactly one regime head per symbol (BTC/MES/SOL) and a promotion REPLACES an incumbent rather than adding one. Measured: 36 stage transitions in 90d, 9 into advisory, and every promotion PAIRED with a demotion. So the correct alarm is not 'why aren't we promoting' but '30 heads at shadow, median 80 days, and nothing ever RETIRES a head that lost its race'. ⚠️ AND A MECHANICAL BLOCKER, which REFUTES the prior review's claim: reference_count is NOT 0 (it is 0 for 2 of 34, both correctly insufficient_data). The real defect is worse — the declared 30-day drift reference window actually covers 2.81 days (9.4%) because iter_records never opens the rotated archives, while reporting reference_window_start 2026-08-05, a date for which the file holds ZERO rows. 24 of 32 FAIL drift_clean on that input and 2 of the 8 passes pass BECAUSE THEY EMIT A CONSTANT. NO promotion and NO demotion recommended: acting on that statistic in either direction is the unprovenanced-diagnostic trap."
  },
  "ml_review.experiments_proposed": {
   "status": "done",
   "evidence": "5 proposed, led by the one-module fix that unblocks the rest (teach iter_records to read the 3 nightly-synced archives). Also: root-cause the setup-quality-lgbm-v2 audit refusal; remove the stale expected_optional_features declaration the trainer names in its own output every cycle; run the MES 15m-vs-5m head-to-head the operator asked for 26 days ago. ⚠️ THE REAL OPPORTUNITY IS ETH — the ONE symbol with 5 shadow heads and NO advisory head, i.e. the only place a promotion would ADD coverage rather than displace an incumbent, and eth-regime-15m-lgbm-fc-pcv-v1 sits at candidate with macro_f1 0.584 on n_eval 87,636, the best ETH head by a clear margin."
  },
  "ml_review.backlog_drain": {
   "status": "done",
   "evidence": "2 ml rows filed, 0 closed; blockers named."
  },
  "ml_review.anomalies": {
   "status": "done",
   "evidence": "4 recorded, incl. two constant-output baselines still constant at 3,895 predictions each and acing the drift gate because of it; and Coordinator.log_advisory_scores writing a CWD-relative advisory_decisions.jsonl that diag never reads — zero production callers, tests only."
  },
  "consolidated_report": {
   "status": "not_started",
   "evidence": "NOT RENDERED. Recorded as not-done rather than a partial artifact passed off as the deliverable. The mandate's priority order was followed as dispatched (unstick sweep -> technical health -> ML -> OPEN-ITEMS observation debt -> backlog drive) and the report is the item that did not fit. A --strict render would also fail its own coverage guard on the 8 not_started items above, which is the guard working."
  },
  "operator_ping": {
   "status": "n_a",
   "evidence": "Suppressed by dispatch — this lane reports ONE consolidated result to the manager, who relays. The three sub-review pings are suppressed as the skill requires."
  }
 },
 "notes": [
  "**THE REVIEW IS NOT COMPLETE AND THIS CHECKLIST SAYS SO.** 8 of 37 items are not_started, 1 blocked, 5 n_a (4 of those routed to sibling lanes by dispatch). The dispatch's own priority order was followed and the consolidated report is what did not fit. An unevidenced tick is the thing this checklist exists to stop.",
  "**P1 — THE 60-SECOND EXIT-EVALUATION REQUIREMENT IS BREACHED, and the live instrument says it is not.** Over the DURABLE cross-process record (72,148 intervals, 463 processes — the first reading not subject to the 'no process lived long enough' caveat): 1,219 breaches (1.69%), max 120,189ms = 2.00x, last breach TODAY. Restart gaps are far worse: 77 of 462 = 16.7%, max 3.51x. The running process reports requirement_state `within` over 172 intervals on a 95-minute life. CLAUDE.md's 2026-08-25 figure (0 of 991, max 45.0s) is a 991-row tail of a 72,611-row file and must not be re-quoted as current.",
  "**P1 — THE ONE INDEPENDENT FALSIFIER OF 'THIS TRADE IS CLOSED' IS NOW SPEAKING, AND IT IS WRONG.** The closed-flat invariant was repaired on 09-09 and has produced 9 violations since 09-10. All are false: three SOLUSDT trades sized 7.5 / 9.3 / 9.8 each report the IDENTICAL residual -250.4, which is arithmetically impossible per-trade. exchange_qty is the account+symbol NET book. On a netted account a close with open siblings fires BY CONSTRUCTION — and 3 of the 5 trades are pairs-sleeve legs, where open siblings are the normal state. It emits at Level.ERROR, so all 9 paged the operator.",
  "**A TWO-AND-A-HALF-MONTH EVIDENCE BLACKOUT NOBODY NOTICED.** replay-pregate-nightly.yml has ZERO successful runs in its entire life — 77 scheduled runs, 68 failure / 9 cancelled — and runtime_logs/replay_pregate/ is empty on main. A workflow that has NEVER been green cannot regress, so no green->red detector fires; what is missing is a detector for 'declared cadence, zero successes, ever'.",
  "**THE 'CRON NEVER FIRES' PREMISE IS FALSE AND FIVE OPEN-ITEMS ROWS SAY OTHERWISE.** All 27 scheduled workflows have genuine event=schedule runs. The real defects are (a) the shared commit-to-main landing step, which on one day discarded the output of nine workflows AFTER they had computed it — today's due-list, constraint readout and error-feed digest exist only as abandoned branches — and (b) delivery falling off monotonically with declared frequency: hourly 25%, 2-hourly 37-48%, 3-hourly 62%, 6-hourly 91-100%, daily 100%. Also: the median cron fires 4-5 HOURS late, so no clears_when clause may read a declared minute as a firing minute.",
  "**THE DRIFT GATE IS COMPUTED AGAINST 9.4% OF ITS OWN DECLARED WINDOW**, blocking 71% of the ML fleet, and two models pass it perfectly because they emit a constant. This also REFUTES the prior review's 'reference_count is 0 by arithmetic' — do not carry that forward. It is a RECURRENCE: sync_trainer_data.sh:318 documents this exact diagnosis; the SYNC half was fixed and the READ half never was. Three archives are synced nightly and read by nothing.",
  "**A DATED LIVE INSTANCE OF THE ALPACA OVER-CLOSE.** alpaca_portfolio/TLT trade 5414 (56 shares) closed 09-08 12:54:52 on sl_cross; four minutes later sibling 5266 (16 shares) closed on exchange_flat_reconciled with pnl NULL. The sibling's outcome was not mis-attributed — it was never measured. This strengthens the MI-140 hold rather than releasing it.",
  "**I NEARLY FILED TWO FALSE P1s AND CHECKED FIRST.** (1) restart_pending: true reads as its own docstring's '2026-05-09 incident state'; all 33 undeployed files are docs/.github/markdown, i.e. the deploy script's designed non-runtime skip. (2) Four venue positions appeared to have no journal row; they are below the /api/diag/journal 1000-row id floor. Both are instrument defects worth fixing, and neither is the outage it looks like.",
  "**DISPATCH CORRECTIONS (RULE ONE).** My --owns-object named no file under docs/claude/work/objects/ and the spawn-gate correctly refused it; my --registry-key was never pushed to origin so confirm had nothing to confirm; and my first confirm passed --observed-source-url none, which the registry refused with 'that child has no repository' — one get_session call showed the source was correct all along. Self-registered against the real object.",
  "**STANDING PROHIBITIONS OBSERVED.** No order placed, modified or cancelled. No leg cancelled on any over-covered book. No src/, config/, deploy/ or .github/workflows/ change. Every Tier-2/3 finding PROPOSED with the exact fix, never enacted — including the closed-flat fix, which is Tier-2 because it changes what pages a live operator."
 ]
}
