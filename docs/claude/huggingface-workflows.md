# Hugging Face workflows

> **Doc status:** `unknown` · category `unknown` · last verified `never` · registered in [`docs/DOCUMENT-INDEX.md`](../../docs/DOCUMENT-INDEX.md) · **nobody has verified this document's status — do not act on it as current**

Use Hugging Face for datasets, model artifacts, and optional Spaces.

## Dataset naming

Use names like:

```text
ict-trading-bot-btcusdt-1m
ict-trading-bot-backtest-results
```

## Model naming

Use names like:

```text
ict-trading-bot-rf-breakout-v1
```

## Rule

Claude should create upload/download scripts and notebooks. It should not train large models locally.
