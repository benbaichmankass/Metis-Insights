# M19 T0.4 — Probabilistic (quantile-forecast) features — design + RESULTS

> **Status:** ✅ RAN 2026-07-01 on the trainer VM. **Bottom line: forecast
> features deliver a REAL, base-rate-ROBUST lift to the BTC-15m vol-regime head**
> — a steady **+0.018 to +0.027 macro_f1** (and positive f1_volatile) across all
> three volatile base rates tested, *including* the shipped 0.005 operating point
> where the T0.1 embedding lift had faded to +0.012. Where the embedding lift is
> a base-rate cliff (helpful only when the volatile class is populated), the
> forecast's forward-looking predicted *range* holds up as the class thins — the
> exact thesis this bet was placed on. Still offline/`candidate` — no live-path
> change. Part of the M19 pivot: with the frozen-embedding lift shown to be
> volatility-base-rate-bound (see [`T0.1-embedding-followup-evidence-2026-07-01.md`](T0.1-embedding-followup-evidence-2026-07-01.md)
> § Threshold sensitivity), T0.4 tests a *different* use of the same free TSFM.
> Design: [`ai-model-strategy-roadmap-2026-07-01.md`](ai-model-strategy-roadmap-2026-07-01.md).

## Thesis

T0.1 used `chronos-bolt-tiny` as a frozen **embedder** (representation). T0.4 uses
the **same** frozen model as what it's actually trained to be — a **quantile
forecaster** — and turns its predicted next-bar distribution into a handful of
**scale-free, leakage-safe features** a LightGBM head can consume. Where the
embedding carries a *volatility signature*, the forecast quantiles carry an
explicit, interpretable **forward view**: expected move, expected range, and
skew. The A/B question: do these lift the regime / conviction heads beyond the
hand-engineered features — and, importantly, do they help where the embedding
*didn't* (direction, and the rare-volatile regime)?

## Why this is a distinct bet from T0.1

- **Interpretable, low-dimensional.** 6 columns vs the embedding's 32 — far less
  overfitting surface, and each column *means* something (median return, range,
  skew). That matters on the label-bound heads (conviction) where 32 raw
  embedding dims swamped a ~100-row slice.
- **A forward view, not a state summary.** The forecast's predicted **range**
  (q90−q10) is a *forward* volatility estimate — plausibly more useful to a
  vol-regime head than a backward-looking realized-vol feature, and it does not
  depend on the volatile class being populated the way a learned representation
  does (the T0.1 base-rate cliff).
- **Reuses the exact same infra** — same model, same trainer-side dep, same
  as-of-join / candidate-stage / injectable-lazy-fn pattern. Near-zero new
  surface.

## The feature block (built this session)

`ml/datasets/forecast_features.py` — mirrors `embedding_features.py` exactly
(stdlib-safe import, injectable lazy `chronos_forecast_fn`, pure
`compute_forecast_feature_rows`, neutral-0.0-on-failure, `stride` + as-of carry).
Per strided bar it runs `pipe.predict_quantiles()` over the trailing close window
(past-only), takes the next-bar predicted price quantiles `{q10, q50, q90}`, and
emits 6 scale-free `fc_*` columns in log-return space vs the last close:

| Column | Meaning |
|---|---|
| `fc_ret_med` | median predicted log-return (directional expectation) |
| `fc_range_rel` | (q90−q10)/last_close — **forward volatility proxy** |
| `fc_up_prob` | sign proxy for P(up) (median > 0) |
| `fc_skew` | (q90+q10−2·q50)/(q90−q10) — forecast asymmetry |
| `fc_q10_rel` | q10 predicted log-return (downside) |
| `fc_q90_rel` | q90 predicted log-return (upside) |

Wired into `market_features` via an optional `forecast_path` kwarg (builder
v10→v11; 0.0 when omitted → existing builds unchanged), producer
`scripts/ml/build_forecasts.py`, candidate manifest
`btc-regime-15m-lgbm-fc-v1.yaml`, and a stub-based test suite (no torch needed).

## Results — the A/B (ran 2026-07-01, trainer VM)

**Method.** Built the `fc_*` side-stream on the trainer (`build_forecasts.py`
over BTC-15m `market_raw` v002, context-len 64, stride 4 → the as-of carry fills
the gaps), joined it into `market_features` via `forecast_path=…` at three
volatile base rates, and trained the BTC-15m **vol-regime** head **base (7
hand-engineered features)** vs **base + 6 `fc_*` columns** on the same dataset,
`time_aware_holdout` split. Δ = fc − base. Every arm fixes the dataset and varies
only the `fc_*` columns (the clean-comparison discipline from T0.1). The fc
side-stream is **fully populated** post-fix (`nonzero_fc 175272/175272` — see the
methodology note below on why that mattered).

| vol_threshold | volatile base rate | base macro_f1 | fc macro_f1 | **Δ macro_f1** | base f1_vol | fc f1_vol | **Δ f1_volatile** |
|---|---|---|---|---|---|---|---|
| **0.005** (shipped) | 4.6% | 0.5808 | 0.6037 | **+0.023** | 0.2169 | 0.2503 | **+0.033** |
| 0.004 | 8.4% | 0.5806 | 0.5991 | **+0.018** | 0.2760 | 0.2948 | **+0.019** |
| 0.003 | 16.1% | 0.5761 | 0.6028 | **+0.027** | 0.3759 | 0.3942 | **+0.018** |

**Verdict — LIFT, and it's base-rate-ROBUST.** The forecast features add a steady
**+0.018 to +0.027 macro_f1** and a **positive f1_volatile at every threshold**.
This is the headline contrast with T0.1: the *embedding* lift was a base-rate
cliff (+0.052 at 0.003 → +0.012 at the shipped 0.005 → −0.004 at 0.006 —
[`T0.1-embedding-followup-evidence-2026-07-01.md`](T0.1-embedding-followup-evidence-2026-07-01.md)
§ Threshold sensitivity), so it stopped helping exactly at the production
operating point. The **forecast features do not decay as the volatile class
thins** — at the shipped 0.005 they give **+0.023 macro_f1, ~2× the embedding's
+0.012**, with the biggest f1_volatile gain (+0.033) of the three. That is the
T0.4 thesis confirmed: a *forward* volatility estimate (the predicted q90−q10
range) is a better vol-regime feature than a *backward* learned representation,
and it doesn't depend on the class being populated the way the embedding did.

**Why the forecast helps where the embedding faded.** The embedding is a
mean-pooled *state summary* — it encodes "how the series has been behaving,"
which is only discriminative for the volatile class when that class is
well-populated (lots of examples to separate). The forecast's `fc_range_rel` is
an *explicit forward variance estimate* per bar — it carries signal about the
*next* bar's dispersion regardless of how rare volatile bars are in the label
set. Six interpretable columns vs 32 raw embedding dims also means far less
overfitting surface, which matters most at the sparse 0.005 rate.

### Methodology note — the zero-vector bug (why the first run was void)

The initial A/B was **invalid** and never reported: the producer called
`pipe.predict_quantiles(context=contexts, …)`, but chronos-bolt's first
positional arg is `inputs`, so every forecast raised
`missing 1 required positional argument: 'inputs'` and the block's
neutral-0.0-on-failure guard silently emitted a **zero vector for all 43,823
rows** — the "A/B" was comparing base against base-plus-zeros. Fixed in PR #5335
(pass the batch positionally), the side-stream rebuilt fully non-zero, and the
numbers above are the first *valid* run. The stub-based unit tests can't catch
this because the real-chronos lines are `# pragma: no cover`; a trainer-side
smoke test that runs one real forecast is the logged follow-up.

### Purged-CV confirm + fc+emb combined + ETH (ran 2026-07-01)

Followed the holdout run with the leakage-safe confirmation + two extensions, all
at the shipped **0.005** threshold under **purged walk-forward CV** (n_folds=5,
embargo) on one fixed dataset per symbol (BTC `v520` carries BOTH the forecast and
embedding side-streams; ETH `v521` the forecast side-stream — both fully populated,
`nz_fc`/`nz_emb` = 175272/175272). Δ = arm − base.

**BTC 15m @ 0.005 (4.6% volatile), purged-CV:**

| arm | macro_f1 | f1_volatile | Δ macro_f1 | Δ f1_volatile |
|---|---|---|---|---|
| base | 0.5908 | 0.2417 | — | — |
| **fc** | 0.6031 | 0.2535 | **+0.012** | **+0.012** |
| emb | 0.6029 | 0.2415 | +0.012 | **−0.000 (flat)** |
| fc + emb | 0.6054 | 0.2468 | +0.015 | +0.005 |

**ETH 15m @ 0.005 (9.5% volatile), purged-CV:**

| arm | macro_f1 | f1_volatile | Δ macro_f1 | Δ f1_volatile |
|---|---|---|---|---|
| base | 0.5553 | 0.2926 | — | — |
| **fc** | 0.5840 | 0.3126 | **+0.029** | **+0.020** |

**Three verdicts:**

1. **The fc lift SURVIVES purged-CV — confirmed, leakage-safe.** BTC +0.012
   macro_f1 / +0.012 f1_volatile (the single-holdout +0.023 shrank ~half under the
   stricter split — the expected, healthy direction, not a reversal). **ETH is
   stronger (+0.029 / +0.020)**, so the lift is not BTC-specific — it generalizes
   to a second liquid 15m series, exactly as the T0.1 embedding did.
2. **fc beats emb on the class that matters, and fc+emb is NOT worth the 32 extra
   dims.** At the sparse 0.005 operating point the *embedding* raises overall
   macro_f1 (+0.012, via range-class precision) but its **f1_volatile is flat
   (−0.000)** — the base-rate cliff again: emb stops helping the *volatile* class
   when it's rare. **fc keeps improving the volatile class (+0.012 f1_volatile).**
   Combining them is only weakly additive on macro_f1 (+0.015 vs +0.012 each — they
   overlap heavily) and *dilutes* fc's volatile-class gain (fc+emb f1_volatile
   +0.005 < fc-alone +0.012). **So for the vol-regime head, prefer `fc` alone** —
   the cleaner, lower-dimensional, base-rate-robust signal; the combined arm adds
   surface without a real gain.
3. **Net M19 read:** the frozen-TSFM **forecast** feature (6 interpretable `fc_*`
   cols) is now the strongest offline lever for the volatility-regime family at the
   production operating point — leakage-safe, multi-symbol, and (unlike the
   embedding) it doesn't decay as the volatile class thins. The concrete candidate
   for a future T0.4→shadow path is a BTC-15m (and ETH-15m) vol-regime head with
   the `fc_*` block; the remaining gate is the **live-parity torch/chronos
   dependency cost** (the mirror-publish spike, Track A / Option D), NOT the offline
   lift.

## Next

1. ~~**Confirm under purged walk-forward CV** + ETH + fc+emb~~ ✅ done (above) — fc
   survives leakage-safe validation, generalizes to ETH (stronger), and fc+emb is
   not worth the extra dims.
2. ~~**Direction head**~~ ✅ RAN 2026-07-01 — **CLEAN NEGATIVE (fc does NOT help
   direction).** BTC-15m direction head, v520, purged-CV: base macro_f1 **0.3450**
   / fc **0.3438** (Δ **−0.001**, flat) / emb **0.3490** (Δ **+0.004**, noise —
   matches the T0.1 embedding's +0.004 exactly). The forecast's directional
   columns (`fc_ret_med`/`fc_up_prob`/`fc_skew`) add nothing to direction, same as
   the embedding — **both frozen-TSFM signals' value is vol-regime-specific.**
   Direction on liquid 15m crypto is near-martingale (all three heads ≈ 0.34
   macro_f1 = 3-class chance), and a frozen generic model doesn't crack it; that
   would need task-specific representation learning (T1.2), not a frozen model. An
   informative negative that localizes where the forecast's edge lives. **The
   conviction stacker with `fc` remains open** (label-wall-bound, as T0.3 was).
3. **If pursuing promotion:** scope the live-parity dependency (compute the forecast
   live for train==serve parity) via the mirror-publish path before proposing a
   `fc`-block vol-regime head shadow→advisory. That decision, not the offline lift,
   is the cost.

<details><summary>Superseded pre-confirm "Next" list (kept for record)</summary>

1. **Confirm under purged walk-forward CV** — these are single-holdout numbers
   (as the T0.1 first run was); rerun the 0.005/0.004/0.003 arms under
   `split_purged_walk_forward` + a second symbol (ETH 15m) before treating the
   lift as leakage-safe, exactly as Lever 1 did for the embedding.
2. **Combine fc + embedding** — the two carry *different* structure (forward
   variance vs backward state); test base + fc + emb to see whether the lift is
   additive at 0.005, and whether fc rescues the embedding's base-rate cliff.
3. **Try the direction head + the conviction stacker** — the forecast's
   directional/range features are exactly where the *embedding* fell flat, so
   this is the real test of whether forecast features add orthogonal signal
   beyond the vol-regime head.
4. **If pursuing promotion:** same live-parity dependency as T0.1 (the forecast
   must be computed live) — but the mirror-publish path (Track A, Option D)
   applies identically, so nothing new is needed there.

</details>

## Gates / discipline (unchanged from T0.1)

Offline/`candidate` throughout; shadow promotion is operator-gated and carries
the same live-parity dependency question as T0.1 (the forecast would have to be
computed live) — but the **mirror-publish path** (Track A, Option D) applies
identically, so nothing new is needed there. Every A/B fixes the dataset and
varies only the `fc_*` columns.
</content>
