# Work plan — 2026-08-14 (non-M20 track)

> **Status:** ACTIVE. Built from a full roadmap + work-plan review across all
> three repos plus a live performance review, on operator direction
> ("prioritize, serialize, and get to it autonomously").
>
> **Companions:** [`ROADMAP.md`](../../ROADMAP.md) § "Next — prioritized work
> plan" (this doc refreshes it) · [`ROADMAP_MACRO.md`](../../ROADMAP_MACRO.md).
>
> **Scope boundary:** M20 (Exit Refinement) is running in a concurrent session
> (PR #9224, branch `claude/m20-exit-coverage-matrix-8d3he7`). Everything below
> is deliberately **disjoint** from it. M20's own queue is not re-planned here.

---

## 0. The reframe — read this before the lanes

The instinct from the roadmap alone is "which strategy or model do we improve
next". Measured against the live system on 2026-08-14, that is the wrong
question, and the numbers say so unambiguously.

### 0.1 What the money surface actually is

| | |
|---|---|
| Accounts declared | **11** |
| Accounts that are `mode: live` **and** `account_class: real_money` | **1** — `bybit_2` |
| `bybit_2` equity | **$280.92** |
| `alpaca_live` (real money, **16** strategies) | `dry_run`, equity **$0.10** |
| `ib_live` (real money) | `dry_run`, **0** strategies |
| `breakout_1` (prop, 5 strategies, live) | manual bridge, `api_ok: false` |

**The entire real-money surface is one Bybit account holding $280.92.**
Everything else with real money attached is shelved in `dry_run`.

### 0.2 What that surface earned

`/api/bot/performance`, real-money population, measured 2026-08-14T11:52Z:

| window | trades | win rate | net PnL | profit factor |
|---|--:|--:|--:|--:|
| 30d | 27 | 22.2% | **−$28.90** | 0.46 |
| 7d | 3 | 33.3% | −$1.47 | 0.45 |
| 24h | **0** | — | — | — |

Lifetime real-money `totalPnL` is **−$67.38** at a 26.5% win rate.

**State the population**, per the standing rule: those figures are real-money
only, and `pnlCoverage` on the 30d window is **0.889** (24 MEASURED / 2
ESTIMATED / 1 FABRICATED of 27), so the headline is well-evidenced — unlike the
paper book, whose 7d `pnlCoverage` is **0.119** and whose `totalPnL` of
**−$98,156.51** should not be quoted as a result at all.

One caveat that cuts the other way: 30d `expectancyR` reads **+0.836**, which
looks healthy. It is carried almost entirely by `ict_scalp_5m` at +35.4R over
**4 trades**. At n=4 that is not an edge, it is a sample. Do not build a plan on
it.

### 0.3 The `vwap` line is history, not a live problem

`/api/bot/strategy/attribution` shows `vwap` at **318 closed trades, 23.6% win
rate, −$50.71** — by far the largest single real-money contributor, and ~75% of
all real-money closed trades ever. **It is already dead**: `config/strategies.yaml`
has `enabled: false` + `execution: shadow` (the M7 Tier-3 kill, 2026-06-09).
Verified in the field, not inferred from the changelog.

So excluding `vwap`, the *live* fleet's real-money lifetime is roughly
**−$16.67** across ~62 trades. Small, negative, and — critically — **thin**.

### 0.4 The conclusion the numbers force

The binding constraint on this system making money is **not** strategy quality,
model quality, or exit timing. It is that **almost nothing is live, the one live
thing is $280, and several declared-live legs cannot place an order at all**.

An enormous, genuinely rigorous research apparatus (M20–M36) is producing
findings — most of them honest negatives, which is a credit to the process — and
measuring them against a book that cannot act on them. Meanwhile the health
backlog carries 180 open items, of which several are *"this declared-live leg
places nothing"*.

**Therefore the next work is capability integrity, not more research.** Research
lanes continue, but behind it.

---

## 1. Lane order (serialized)

Work top-down. Each lane names its own done-condition.

### Lane 0 — Live-capability integrity  ·  **P0, in progress**

*Everything declared live that structurally cannot trade, or reports numbers
that are not measurements of the thing they name.*

| # | Item | Evidence | Tier | State |
|---|---|---|---|---|
| 0.1 | IB market-data races the order path on one socket | #9233 live journal; `ib_client.py:370-395` invariant unimplemented for `IBMarketData` | 1 | ✅ **M20's PR #9240 — NOT this branch** (see below) |
| 0.2 | `exit_loop_health` serves a dead process's `fresh` | file `generated_at` 16s *before* process start, live 11:54Z | 1 | ✅ **PR #9244** |
| 0.3 | `alpaca_paper` + `alpaca_portfolio`: `balance()` → `None` while `/v2/account` reads ACTIVE | `BL-20260813-ALPACA-BALANCE-NONE-WHILE-ACCOUNT-READS-ACTIVE` | 1→2 | 🔴 **OPEN — not presenting, NOT fixed** (re-measured 2026-08-25, see below) |
| 0.4 | `ict_scalp_avax_5m` places **nothing** — every order EXCHANGE_REJECTED over Bybit's AVAXUSDT `max_qty` | `BL-20260810-ICTSCALP-AVAX-QTY-EXCEEDS-VENUE-MAX` | 3 (sizing) | ⚠️ **PREMISE STALE — it DOES place** (re-measured 2026-08-25: 10 placed / 23 refused → `partially_refused`, not `signalled_never_placed`). M20 still owns the sizing fix. |
| 0.5 | `ib_paper` MGC uPnL **$119,490** on a `markprice_local` source vs ~$37.80 exchange truth | `BL-20260807-IBPAPER-UPNL-CONTRADICTS-EXCHANGE-TRUTH`; live `/positions` 2026-08-14 | 1 | 🔴 **OPEN — masked by a SECOND defect, not fixed** (re-measured 2026-08-25, see below) |
| 0.6 | `breakout_1` prop status stale — the daily-loss / $4,700 DD guard computes off that snapshot | `/api/bot/prop/status` live | 1 | 🔴 **OPEN, and the cushion is $64.00** (re-measured 2026-08-25, see below) |

**Done-condition:** every `mode: live` account × enabled strategy leg is either
(a) demonstrably able to place an order, or (b) carries a filed row saying why
not — **and a standing check exists**, so the next such leg is *detected* rather
than discovered months later by a session reading a backlog.

That last clause is the real deliverable. Items 0.3–0.6 were all *already filed*
and all still live; filing alone has not been sufficient.

#### Lane 0 re-measured LIVE, 2026-08-25 ~09:30Z (this table was 11 days old)

Every queued row was re-read against the live system rather than quoted. **None
of the three closed.** Two changed shape, and the changes both run in the
direction of *looking better than they are*, which is why re-reading mattered.

**0.3 — `balance()` → `None` is NOT FIXED. It is not presenting, over a
denominator too small to tell the difference.**

State the population. Newest `balance() returned None` row **anywhere** in the
journal is **2026-08-13** — the day the row was filed. Since then, by predicate
(not by a sampled `MAX(entry_reason)`):

| account | balance()-None rows | newest | dispatch attempts since 08-14 |
|---|--:|---|--:|
| `alpaca_paper` | 4 | 2026-08-13 | 17 |
| `alpaca_portfolio` | 1 | 2026-08-13 | 15 |
| `ib_paper` | 5 | 2026-08-13 | 34 |
| `alpaca_live` | 20 | **2026-06-26** | — |
| `bybit_1` / `bybit_2` | **0** | — | — |

So **66 clean attempts over 12 days**. That is not evidence of a fix, for two
reasons. First, **no fix exists**: the only commits touching that path since
08-13 are `ad70907` (the long-only side gate) and `78d0bdd` (an exposure
observation), neither of which goes near `balance()`. Second, the condition
**fires in bursts weeks apart** — 07-01, 07-15, 08-02, 08-11, then four rows on
08-13 across three different accounts in one day — so a 12-day quiet spell is
inside its normal inter-arrival. 66 attempts cannot separate *fixed* from *has
not recurred yet*.

⚠️ **And the healthy balance SNAPSHOT is not evidence either.** The handoff
flagged this and it holds: `/api/bot/accounts/balances` reads the
`balance_snapshots` DB table written by the hourly report. It is a different
code path from the sizing call that produced these rows. A green snapshot beside
a broken `balance()` is exactly the state the row describes.


#### 0.3 follow-up — it CANNOT be closed by waiting, and the reason is structural (measured 2026-08-25T11:25Z)

**Positive control first, because a quiet probe is worth nothing without one.**
All four Alpaca accounts answer right now: `/v2/account` returns
`status: ACTIVE · trading_blocked: false · account_blocked: false` for
`alpaca_live` / `alpaca_paper` / `alpaca_portfolio` / `alpaca_options_paper`
(`/api/diag/broker_account_status`), and the balance snapshot carries
`api_ok: true` with real figures for all four (`alpaca_live` $200.10,
`alpaca_paper` $83,790.68, `alpaca_portfolio` $99,083.60,
`alpaca_options_paper` $100,000.00). So the read path **works**, and the
absence of `balance()`-None rows is genuine quiet rather than a dead probe.

**But quiet is not repair.** Newest `balance()`-None row remains **2026-08-13**;
66 clean attempts across 12 days; historical inter-arrival is **weeks**
(07-01 → 07-15 → 08-02 → 08-11, then four rows across three accounts on 08-13);
and **no fix exists in the code**. Twelve quiet days against a weeks-scale
arrival rate is not evidence of a repair nobody made.

⚠️ **A conflation to avoid, surfaced by this read: `alpaca_live` is
`mode: dry_run`.** Its refusals — 312 in the window, 156 carrying
`dry_run_sizing_skip` — are the **execution gate working**, not a capability
failure, which is exactly the `refusing_by_declaration` bucket #10257 restored to
the offline audit. Any 0.3 evidence drawn from `alpaca_live` refusal counts must
separate the declared-skip rows first, or it will read a switched-off account as
a broken one. The 08-13 occurrence spanned **three** accounts, so 0.3 itself does
not rest on `alpaca_live`.

**So the done-condition is a DETECTOR, not a quiet period** — and the detector
has two known holes, one of which is being fixed by another session right now:

1. `account_reachability_alert` probes **`positions()`, not `balance()`**
   (`BL-20260814-REACHABILITY-PROBES-POSITIONS-NOT-BALANCE`). An account whose
   `positions()` answers while `balance()` returns `None` reads **UP** while
   refusing every signal routed to it — which is precisely 0.3's shape. **Untouched.**
2. ~~`silent_refusal_alert`'s latch cannot clear for an account that stops
   producing rows~~ — ✅ **CLOSED 2026-08-25 by #10259 (`d4bf264`)**, the
   concurrent `/system-review` session's item B. Verified by reading the merged
   code, not by taking the PR description's word: `run_silent_refusal_check`
   gained a **second loop over `state.items()`** — the latched set itself —
   after the `assessed` loop, so an account that latched and then went quiet is
   now re-examined instead of never re-entering. ⚠️ And it does **not** collapse
   the two: a quiet account's latch is cleared as *"the window went quiet"*,
   explicitly distinguished from a recovery, because the normal 🟢 `[OK]`
   ("N orders reached the exchange") would assert something nobody measured.

   **Not touched by me, deliberately** — their branch held that file while I was
   working, and reasoning about my edit against their unmerged state is how
   collisions happen. This is the coordination working rather than a gap.

**Recommended disposition:** 0.3 stays **open**, and its resolution criteria
should be rewritten from *"confirm it no longer occurs"* to *"a detector exists
that will catch the next occurrence"* — because the first can never be satisfied
by observation at this arrival rate, and framing it that way invites a future
session to close it on twelve quiet days.

**Status after 2026-08-25: HALF the detector is now real.** Hole 2 closed by
#10259; **hole 1 is the whole of the concrete remaining work.**

⚠️ **And hole 1 does NOT have an obvious fix — the obvious one was already
rejected.** Adding a `balance()` call to `account_reachability_alert`'s probe
would put a per-account broker round-trip on the live trader tick, which
`CLAUDE.md` names as the shape of *both* June 2026 wedges and which
`silent_refusal_alert` was deliberately built to avoid ("No new exchange
round-trip pattern"). So the next session must not reach for it. Two directions
that do not add a socket call, in preference order:

1. **Make the reachability verdict say what it measured.** The probe answers
   *"does `positions()` answer?"*, and the alert already says "reachable", not
   "healthy" — so the wording is defensible. The gap is that `down_accounts()`,
   which the review skills read, offers no way to express *"reachable but
   possibly unable to size"*. A third state on the existing read-only surface
   costs nothing at the tick.
2. **Let the journal answer it.** A `balance()`-None refusal already leaves a row
   with a distinguishable reason; `dead_leg`/`silent_refusal_alert` read the
   journal with no socket. Recognising that reason as its own cause — the way
   the declared-skip tokens are recognised — would surface the condition through
   machinery that already exists and already runs.

Neither is started. Both are Tier-1-shaped, and (2) composes with the
declared-skip work landed today.


#### 0.4 re-measured — the premise is STALE, and the check found a defect in its own author's work (2026-08-25)

The row says the leg *"places **nothing** — every order EXCHANGE_REJECTED"*. **It
places.** Measured over the live journal (400-row diag pull, window 2026-08-14 →
2026-08-25), bucketed with the shipped `dead_leg` vocabulary:

| leg | buckets | verdict |
|---|---|---|
| `ict_scalp_avax_5m` | 23 refused · **10 placed** | **`partially_refused`** — not `signalled_never_placed` |
| `avax_pullback_2h` | 13 policy_skipped | `refusing_by_declaration` |
| `trend_donchian_avax_4h` | 4 placed | `healthy` |

And the 23 refusals are **two different things**, which the single "every order
EXCHANGE_REJECTED" framing hides: **15** are the venue `max_qty` rejection this
row is actually about, **8** are `intent_noop:flip_suppressed_hold_policy` — the
`FLIP_POLICY=hold` gate working as designed. So the row's own denominator is
overstated. **M20 still owns the sizing fix**; what changes is that this is a
degraded leg, not a dead one, and the severity should be read accordingly.

⚠️ **The measurement also exposed a hole in the standing check shipped in
#10257** — i.e. the check catching its own author. `avax_pullback_2h` is
`execution: shadow`, **declared off**, and it graded `signalled_never_placed`,
the most alarming verdict the family has. Cause: `EXPECTED_DISPATCH_SKIP_REASONS`
recognised the **sizing**-path dry token (`dry_run_sizing_skip`, prefixed by the
coordinator) but not the **dispatch**-path one (`dry_run_no_order_placed`, set by
`execute.py:442` when `_genuinely_dry`). #10257 fixed the CALLER drift and left
the PREDICATE half of the same class. Fixed + pinned;
`BL-20260825-DECLARED-SKIP-SET-MISSES-THE-DISPATCH-PATH-TOKEN`.

**0.5 — the $119,490 fabrication is NOT FIXED. It is masked by a second,
separate defect, and the masking bug is the more interesting finding.**

All three `ib_paper` legs now read `unrealizedPnl: null` / `unavailable`
(MGC 5007, MHG 4796, MES 4350). That is not a repair:

* **The fabrication path is unchanged.** No commit has touched
  `src/runtime/local_pnl.py` or the uPnL block of `routers/dashboard.py` since
  2026-08-01 (`78d0bdd`, unrelated).
* **IB is healthy**, so this is not the documented logged-out-gateway case:
  `/api/diag/ib_state` shows clients 498 and 9619 `connected` with
  `account_data_ready: true`.
* **The mark IS obtainable.** `/api/bot/candles` returns real bars for all three
  symbols **from the same web-api process** — MGC 4652.5, MHG 6.62, MES 7699.25.
* So had the fallback run, it would have produced roughly **+$4,284.00 (MGC)**,
  **+$8,845.00 (MHG)** and **+$4,837.50 (MES)** — ≈ **$17,966.50** across three
  paper legs, from `contract_value_usd` 10 / 2500 / 5.

**Why it does not run — a clientId collision.** `_broker_unrealised_for_trade`
fails first (`/api/diag/exchange_positions?account_id=ib_paper` returns
`positions: null` — *could not look*), so `_local_unrealised_for_trade` runs and
calls `last_mark_price`, which passes **`{}`** as settings.
`market_data._ib_connection_identity` then resolves
`settings → env → exec_client_id + 1`, and `ib_paper.ib_client_id` is **497** —
so with `IB_MD_CLIENT_ID` unset it lands on **498**, the clientId the trader's
own live market-data socket holds (visible in `ib_state`). `candles.py::_settings()`
is unaffected because it hardcodes `"600"` itself.

⚠️ **Nothing in the repo provisions `IB_MD_CLIENT_ID`** — no unit, no env
template, no install script; the sole supplier is that one literal. So the
reservation `candles.py` documents as protecting the trader *"once the fetch
actually connects … the trader's MES market-data calls fail with IB error 326,
starving every MES strategy of candles"* is a **per-caller literal, not a
resolved default**, and every other caller defeats it.

⚠️ **This is stated as the best-supported explanation, not as a measurement.**
The live value of `IB_MD_CLIENT_ID` on the `ict-web-api` unit was **not
readable** — it was absent from `get_env.py::ALLOWED_KEYS`, which is
`BL-20260813-ENV-VARS-SHIP-WITHOUT-A-READ-SURFACE` on a var that governs whether
two processes fight over one socket. **Added to the allowlist in this change**,
so the next session can settle it in one call instead of by inference.


#### 0.5 follow-up — the blocker cleared, the criterion is DISCHARGED (measured 2026-08-25T11:25Z)

When `BL-20260825-REPAIRING-THE-UPNL-MASK-RE-EXPOSES-A-FABRICATION-PATH` was
filed, `/api/diag/exchange_positions` returned `positions: null` for `ib_paper`,
so its first resolution criterion could not be attempted. **It returns all three
legs now**, so the measurement was taken.

**Journal and exchange AGREE on direction and size** — MGC sell/short 51.0, MHG
buy/long 29.0, MES buy/long 15.0. So 0.5 is **not** a position-divergence
problem, which is worth stating because that was the cheap explanation.

| leg | journal basis | exchange basis | delta | notional |
|---|--:|--:|--:|--:|
| MGC | $1,122.00 | $3,006.04 | $1,884.04 | $2,375,937 |
| MHG | $10,186.25 | $9,840.91 | −$345.34 | $481,291 |
| MES | $4,743.75 | $5,034.60 | $290.85 | $577,350 |
| **TOTAL** | **$16,052.00** | **$17,881.55** | **$1,829.55** | **$3,434,578** |

⚠️ **State the population:** marks read once at ~11:2xZ. A point-in-time
reading, not a distribution — the figure moves with the market, and an earlier
reading this session gave ≈$17,966.50 on the journal basis.

**RULED OUT — the multiplier handling is correct.** `local_pnl` computes
`(exit − entry) × qty × contract_value_usd`, negated for a short, resolving
`contract_value_usd` from `config/instruments.yaml` (MGC 10.0 · MHG 2500.0 ·
MES 5.0). Verified by reading the function, not by reproducing the figure. The
obvious candidate for `BL-20260807`'s $119,490 is therefore **eliminated**.

**NEW — the estimate's *entry* term is also approximate, and nothing says so.**
`markprice_local` prices against the **journal's** entry; the account's real
cost basis is the **exchange's**; they disagree on every leg (MGC 4660.9 vs
4664.594 · MHG 6.498 vs 6.5028 · MES 7634.75 vs 7630.872). Basis choice alone
moves the total **$1,829.55 = 11.4%**, and MGC carries $1,884.04 of that because
3.7 points of entry difference is multiplied by 510 oz. The label names the
*mark* as the estimated term and implies the entry is known. Both are estimates.

**Verdict, unchanged but better supported:** restoring the value is still not
the safe default. The feared mechanism is gone; a smaller, measured one replaces
it. Option (a) of the row's resolution criteria should require the **entry
basis** to be declared beside the mark, not merely the mark labelled an estimate.

**A probe error worth recording, since it nearly became a finding.**
`/api/bot/candles?...&limit=1` returned `no_data` for all three IB futures, which
read as *"the asymmetry has collapsed, candles are failing too"*. It had not:
`limit=20` returns 20 bars for the same symbols in the same process. The bad
probe was mine. The asymmetry (candles work, the uPnL fallback does not) **holds**.


**0.6 — OPEN, and materially worse than "stale".** `/api/bot/prop/status`:
snapshot `2026-08-23T16:10:45Z`, **41.26 h** old, `status_freshness: "stale"`
against a 24 h threshold. The number that matters is not the age:

* `distance_to_dd_floor_usd` — **$64.00**. Balance $4,764 against a **$4,700**
  static floor on a $5,000 account. The account is **1.3 % from the
  account-killer**.
* `distance_to_daily_loss_usd` — **`null`**, because `day_pnl_state` is
  `realized_unreported`: the day's realized PnL has not been reported, so the
  $142.92 daily-loss limit currently has **no measured distance**. That is a
  missing input, not a permanent limit — one operator `bal` report restores it.

So the guard is 41 h stale on one axis and unmeasured on the other, while the
measured cushion on the axis that does compute is $64. The freshness plumbing
works correctly — `status_freshness` and `day_pnl_state` both report honestly,
which is exactly what the 2026-08-14 work was for.

⚠️ **CORRECTION — "operator-owed input, not a code defect" was the wrong
conclusion, and wrong in the flattering direction.** This section first closed
on that sentence. The concurrent `/system-review` session reached the same
ground independently about ten minutes earlier and went one step further (board
[#6927 09:31:06Z](https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5408377894);
PR #10256 finding 1). Recorded here rather than re-derived, and **credited**:

**Staleness is not the whole defect. A perfectly fresh snapshot would still
produce a ticket that can kill the account.** Ticket `prop-manual-1a29db54154e`
(2026-08-25T00:15:23Z, 17.305 SOL) was sized at **`risk_usd: 75.00`** against a
live cushion of **$64.00**; the ticket's own *"recompute at 1.5% of your live
balance"* instruction yields **$71.46**, which also overshoots.

The mechanism is the write-only-signal class this repo has a guard family for:

* `prop_reconcile.compute_rule_distance` → `distance_to_dd_floor_usd` has
  **three consumers, all DISPLAY** (`routers/prop.py`,
  `prop/telegram_report_handler.py`, `prop/prop_report.py`).
* **Zero decision consumers.** `src/prop/breakout_ticket.py:133` computes the
  floor and uses it *only* to print rule text at line 175.
* `src/prop/prop_balance.py` is **not** the hole — it refuses correctly on
  `stale`; the emitter simply never consults it, sizing at the nominal $5,000.

**So 0.6's done-condition wants TWO things, not one:** a fresh snapshot **and**
the emitter reading the cushion it already computes. Only the second survives a
refill. The operator report is still owed — but it is the smaller half, and
closing 0.6 on it alone would have left the account-killer intact.

#### Two items moved to M20 (recorded so the handoff is not re-derived)

**0.1 — the IB fix is M20's (#9240), not this branch's.** This plan originally
carried a lock serialising `IBMarketData` against the order path. That was
**insufficient, and M20's review said so correctly**: a lock serialises but does
not *pin*, so thread B can still drive the event loop thread A created — and
since `ib_insync` binds the socket transport to one loop, driving it from another
thread is the documented hang. Mutual exclusion made the two threads take turns
at the unsafe thing. Their single-worker executor + `_on_ib_fetch_thread()` pin
closes it. Mine was dropped wholesale, along with its test (which asserted
`_ib_lock` — an identifier that no longer exists, so it would have *failed*, not
merely gone stale).

**0.4 — the venue-max clamp is M20's too.** They root-caused it:
`risk.py::position_size` resolves `qty_step` and `min_qty` and caps by margin,
but carries **no `max_qty` clamp anywhere**, so the margin pre-flight cap is free
to exceed the venue ceiling and nothing catches it before submit. That is
Tier-3 (sizing) and **not AVAX-specific** — any symbol whose venue `max_qty` sits
below `available × leverage × buffer / price` has the same shape, real money
included. The separation measurement below still stands as independent
confirmation, and it agrees.

#### The standing check — `scripts/ops/dead_leg_audit.py` (built 2026-08-14)

Neither existing check can see this class. `/health-review`'s **strategy
silence** check measures `*_eval` events — a leg that evaluates, signals, and has
every order bounced is *not silent*, it is loudly failing at the last step, and
grades `ok`. `account_reachability_alert` probes `positions()` — the account
answers. So "declared live, signals, places NOTHING" falls between them.

**First run, 14d window over the trainer's synced journal (trainer-diag #9262):
72 legs graded, 32 `signalled_never_placed`.** The verdict is descriptive, not a
defect claim — the refusal *reason* is what separates a broken leg from a correct
refusal, and both are present:

**⚠️ TWO RULES FOR READING THIS OUTPUT, both learned by getting it wrong first:**

1. **`MAX(entry_reason)` is a SAMPLE, not the population.** I reported
   `ib_paper`/`mgc_trend_1h` as "43 rejections, ALL `balance() returned None`"
   on the strength of one sampled reason string. Counting the actual predicate
   gives **5**, not 43 — the other ~38 are
   `intent_noop:flip_suppressed_hold_policy`, which is the hold policy working
   *correctly* on the held 105-contract MGC position. **Always count the
   predicate before quoting a cause.**
2. **A window that straddles a fix makes a fixed bug look current.** Read the
   output with fix dates in hand (next row).

| leg | n | reason | reading |
|---|--:|---|---|
| `bybit_1` / `ict_scalp_avax_5m` | **18** of 22 | `order_qty > max_qty` | **DEFECT, confirmed decisively** — see separation below |
| balance-None class (all accounts, all history) | **48** over ~7wk | `balance() returned None` | **defect** — recurrent, 5 onsets; **not Alpaca-specific** (`ib_paper`, `breakout_1` too) |
| `bybit_2` / `ict_scalp_5m` | 6 | `ErrCode 110007` (available balance) | **already fixed 2026-08-13** — all 6 rows *predate* the fix; untested since |
| `ib_paper` / `mgc_trend_1h` | ~38 of 43 | `flip_suppressed_hold_policy` | **correct** — hold policy doing its job |
| `alpaca_live` / many | 1–8 | `zero_balance: gate_balance=0.00` | **correct** — a $0.10 `dry_run` account. ✅ **Challenged and upheld** — see below |
| `bybit_1` / `avax_pullback_2h`, `fade_breakout_4h` | 12, 10 | `dry_run_no_order_placed` | **correct** — shadow legs |

**The venue-max finding is confirmed by perfect separation.** Every
`ict_scalp_avax_5m` order over 14d, against Bybit's 22,000 AVAXUSDT cap:

- `exchange_rejected`: **n=18, range 24,357.8 – 34,526.3 — all ABOVE the cap**
- placed/closed: **n=4, range 368.4 – 15,414.0 — all BELOW the cap**
- **Zero overlap.**

That rules out the alternatives a boundary case would leave open: not
intermittent venue behaviour, not a misconfigured leg, and not a sizing bug —
the sizer's number is defensible from margin, it is simply never clamped to the
venue ceiling. **The clamp is absent from the decision, not wrong in it.**

M20 reached the same conclusion from the other direction (the sizing code rather
than the outcome distribution), and their read is the actionable one because it
names the missing line. Two independent derivations agreeing is worth recording;
the fix is theirs.

#### The `alpaca_live` `zero_balance` row — challenged, and upheld

Worth writing down because the challenge *failed*, and a plan that only records
its confirmations is not honest about how it was built.

Reading `coordinator.py` for Lane 0.3 turned up that `_default_balance_fetcher`'s
branch 3 returns **`0.0`** on a whole-fetch exception, which surfaces as exactly
the `zero_balance: gate_balance=0.00` string above. That made the "correct — a
$0.10 account" reading look like it might be a misattribution: the same symptom
has two possible causes, and one of them is a bug.

So it was measured rather than flipped (trainer-diag #9293), with a
**discriminator** chosen in advance: a balance-service outage refuses every
routed account inside one tick, so it must **burst** across several `account_id`s
within an hour; an unfunded account **trickles** on one account over many days.

The answer was unambiguous — 120 refusals, `alpaca_live` **alone**, 3–5 per hour
across 16 separate days, with exactly one hour in the whole window touching a
second account. That is the trickle. The original reading stands, the branch-3
hypothesis is **not** what produced these rows, and branch 3 is filed as *latent*
(`BL-20260814-BALANCE-FETCHER-BRANCH3-ZERO-AND-A-LOG-THAT-LIES`) rather than as an
active cause.

One thing the same pull *did* surface: `balance() returned None` is low-volume
(**6 in 30d**), but **4 of the 6 land on 2026-08-13 across three different
accounts** — which is the burst signature, on the branch that refuses loudly.
That belongs to `BL-20260813-ALPACA-BALANCE-NONE-WHILE-ACCOUNT-READS-ACTIVE`.

⚠️ **A third rule for reading this audit, learned the same way as the first two:
the journal you query must have a denominator.** The first three attempts at the
query above returned `(zero rows)` for every question — because the trainer's
canonical resolver points at an **empty** stray journal (0 rows, stale since
2026-08-02) while the real 4,649-row one sits under `data/`. Only a sanity probe
placed *first* stopped that from being reported as "no such refusals exist".
Filed as `BL-20260814-TRAINER-CANONICAL-RESOLVER-POINTS-AT-EMPTY-JOURNAL`, and
`dead_leg_audit.py` now refuses a zero-row journal outright rather than printing
an all-clear.

### Lane 1 — Restore the 60s exit-evaluation guarantee  ·  ✅ **DONE by M20, not by this lane**

The operator's ask ("no live trade goes >60s without evaluation") was met on
2026-08-12 at 30.2s, then lost to the rollback, which put exit evaluation back
inline on a tick measuring **93.8s mean / 107.8s max** (`/api/diag/tick_cost`,
n=3, process 11:46:29Z — small sample, stated as such).

**M20 restored it on 2026-08-14**, and the whole sequence this lane planned is
theirs: their IB thread-pin (#9240) was the precondition, they deployed it, and
they cleared `EXIT_LOOP_DECOUPLE_DISABLED` and restarted. Recorded here rather
than re-derived, so this lane is not re-opened by a later reader.

**The done-condition is satisfied, and by the right evidence.** This lane insisted
the verification was not a formality: an **empty** `offloop_hooks` while the
decouple is deployed would mean the thread-identity segregation is silently not
working and the off-loop cost is being mis-attributed — making a concurrency
change read as a pure optimisation. M20's post-restart reading shows
`offloop_hooks: monitor.strategy_monitor_loop n=5`, i.e. **populated**. That is
the direct proof.

> ⚠️ **The cushion is thinner than the sprint log implies, and this is M20's open
> row, not a finding of mine** (`BL-20260814-EXIT-PASS-SLOWER-THAN-M20`). The pass
> now measures **40.7–41.5s** against the M20 sprint log's documented **22.4s
> mean** — ~1.8×. **That must not be quoted as a regression**: it is n=5 from a
> process up ~3 minutes with cold caches, against an n=55 distribution. The
> honest reading is that the 60s ask is still cleared, but at a **31% margin
> rather than the ~2×** the decouple originally bought. M20 owns the warm
> re-measure and has explicitly scoped it to *after* their follow-up lock lands —
> so this lane must **not** pre-empt it with a cold number of its own.

> ⚠️ Separately: the tick had *also* regressed independently (93.8s mean vs the
> 69.3s recorded post-decouple on 2026-08-12). Not the same issue as the
> rollback, and it wants its own measurement at larger n before anyone acts.

### Lane 2 — Capital & activation decision packet  ·  ✅ **DELIVERED**

📄 **[`CAPITAL-ACTIVATION-PACKET-2026-08-14.md`](./CAPITAL-ACTIVATION-PACKET-2026-08-14.md)**

The question §0 raises and no lane below can answer: **what is the intended
real-money configuration?** `alpaca_live` carries 16 strategies and $0.10.
`ib_live` carries none. `bybit_2` carries $280.

The packet **presents a decision, it does not take one** — account-mode flips and
funding are Tier-3 and squarely the operator's.

**Headline: the blocker is not what this lane expected.** The draft assumed the
paper evidence base was untrustworthy (aggregate paper `pnlCoverage` **0.119**).
Measured per-account with the canonical `classify_pnl` over 293 closed rows, the
0.119 is dragged down almost entirely by **`bybit_1`** — 203 of 293 rows at 0.340
coverage with 57 fabricated — and `bybit_1` is the data-only **soak** account, not
`alpaca_live`'s mirror. The actual mirror, `alpaca_portfolio`, reads **0.750
coverage with ZERO fabricated rows**.

So coverage does not block it. **Sample size does**: `alpaca_portfolio` has **16
closed trades in 30 days**, i.e. roughly one resolved trade per routed strategy,
and a +$2,656.48 sum whose sign a handful of trades could flip. The uncomfortable
implication the packet states outright is that *the mirror may never accumulate
evidence fast enough to justify the account it mirrors.*

The cheapest honest move is **not** flipping `alpaca_live` live — it is **funding
it while leaving `mode: dry_run`**, so its $0.10 stops producing `zero_balance`
refusals that mask every other signal the account could give. Operator-side, no
tier flip, no code.

### Lane 3 — M27 GLD 1h promotion packet  ·  ✅ **DELIVERED**

📄 **[`M27-GLD-1H-PROMOTION-PACKET-2026-08-14.md`](./M27-GLD-1H-PROMOTION-PACKET-2026-08-14.md)**

The strongest uncommitted non-crypto cell in the whole M27 sweep: **k-fold
baseline 4/4 folds, +5.92R net, expectancy +0.4933R/trade** — the only equity/ETF
symbol clearing 4/4, already flagged in the roadmap as "the standout uncommitted
P4 candidate".

**Recommendation: SHADOW-first, not live — and the reason is n, not the edge.**
The figure this lane's own earlier note omitted is the denominator: **18 trades
over ~2 years**, i.e. ~9/year, ~4–5 per fold. "4/4 folds positive" over 4–5 trades
each is a far weaker statement than it sounds, and **+5.92R is the sum of 18
numbers**. The workplan flags exactly this failure mode twice elsewhere
(`ict_scalp_5m` at +35.4R over 4 trades; `alpaca_portfolio` at 16 trades) — a rule
applied only to *unfavourable* results is not a rule.

Two things the packet surfaces that were not in the brief:

- **1h was chosen for DATA, not strategy logic.** Yahoo's ~60-day cap applies to
  5m/15m but not 1h, so 1h was the fix for `PB-20260721-M27-EQUITIES-DATACAP`.
  **Every** existing scalp leg is 5m or 15m — this would be the first above 15m,
  and a scalp firing 9 times a year is not behaving like a scalp. Open question:
  is this a *scalp* edge, or a different setup that happens to work on gold?
- **`gld_pullback_1h` already exists, is live, and is a different strategy.** One
  word apart; conflating them would have made the packet argue for something
  already running.

### Lane 4 — M30 P1: the C2 `--features` selector  ·  ❌ **ALREADY SHIPPED — this lane was a planning error**

Verified in the field, not the prose: **`f4cbc3ba` — "M30 P1: C2 `--features`
common-core selector (unblocks pooled multivariate discovery) (#7718)"**, and the
implementation is live at
`scripts/research/analyze_research_panel.py:913` (`# --- P1: --features
common-core selector for the MULTIVARIATE fit. ---`) with its argparse at
`:1120`. Two other callers already consume it (`sweep_research_panels.py`,
`m20_exit_head_round.py`).

### Lane 5 — M29 P1b: real point-in-time EIA/NG calibration  ·  ❌ **ALREADY SHIPPED — same error**

Also verified against artifacts, not prose:
`comms/macro/sysdyn_gas_dual_scorecard.json` carries
**`target_source: "eia_v2 + fred + open-meteo"`, 418 obs, generated
2026-07-25** — i.e. real point-in-time EIA storage + FRED Henry Hub + open-meteo
HDD, exactly what this lane proposed to go and do. The roadmap states outright
that `EIA_API_KEY` **is** provisioned and that the "register a free key" ask is
stale.

---

### ⚠️ Why Lanes 4 and 5 were both wrong — and it is not a coincidence

**The ROADMAP cells carry stale forward-looking lists alongside later
"shipped"/"corrected" statements about the same items, in the same cell.** Both
lanes came from reading the first and missing the second:

- **M30's cell** lists *"Queued loose-ends: **P1** C2 `--features` selector
  (unblocks pooled multivariate discovery — **do first**)"* — and then, further
  down the very same cell, *"**PIVOT 2026-07-27** … Shipped: **P1 `--features`
  (`f4cbc3b`)** · P2 sweep driver (#7730) · P4 wider capture (#7723) …"*. The
  queued list was never pruned when the pivot shipped its items.
- **M29's** says *"**P1b remaining** = calibrate on real point-in-time
  EIA/weather/NG-price history"*, and elsewhere that the calibration ran on
  `eia_v2` with 864 storage obs and that the operator hand-off is *"**stale — no
  operator hand-off is outstanding here**"*.

This is `field beats comment` at document scale: a "remaining"/"queued" list is a
**claim about** the work, and the commit, the argparse, and the committed
scorecard are the **field**. A planner reading top-down hits the stale claim
first, and — this is the part that makes it costly — the stale claim is
*forward-looking*, so it generates work rather than merely misinforming.

**This is arguably the review's most useful finding**, because it is the one that
recurs: the operator asked for a full roadmap review precisely because it is hard
to tell what is actually left, and the answer is that **the roadmap's own
"remaining" lists cannot be trusted without checking the field.** Two of five
lanes in this very plan were already done.

Filed as `BL-20260814-ROADMAP-QUEUED-LISTS-CONTRADICT-SHIPPED-LINES`.

#### So what IS left in M30? — field-checked, and the answer is "almost nothing"

Rather than leave that as an open question, the whole `Queued loose-ends` list was
checked against the tree:

| item | artifact | status |
|---|---|---|
| P1 `--features` | `f4cbc3ba`; `analyze_research_panel.py:913` + argparse `:1120`; 2 callers | ✅ **shipped** (verified hard) |
| P2 sweep driver | `scripts/research/sweep_research_panels.py` | ✅ exists (roadmap: #7730) |
| P3 hypothesis→backtest | `scripts/research/build_backtest_panel.py` | ⚠️ **ambiguous** — the roadmap calls that file "the C1-for-backtests bridge (#7744)" while listing P3 separately |
| P4 wider capture | `killzone` in `build_backtest_panel.py`, `build_research_panel.py`, `hf_vectorized.py` | ✅ exists (roadmap: #7723) |
| P5 excursions | `excursion` in `analyze_panel_by_cell.py`, `analyze_research_panel.py`, `build_backtest_panel.py` | ✅ exists (roadmap: #7737/#7741) |
| P6 SHAP | — | ❌ **genuinely open** |

**The roadmap's `Shipped:` line is the accurate half; the `Queued loose-ends`
list is stale for P1/P2/P4/P5.** What is actually left is **P6**, plus resolving
whether P3 and the "#7744 bridge" name the same artifact.

##### ✅ Both resolved 2026-08-14 — and neither is worth starting

**P3 and the #7744 bridge are DIFFERENT things.** `build_backtest_panel.py`'s own
docstring opens *"M30 · **C1-for-backtests** — the BACKTEST research-panel
builder"* — it brings backtests **into** the C2 panel. P3 is the opposite
direction: the session prompt defines it as *"**C3 hypothesis→backtest bridge**:
wire a confirmed feature into `scripts/backtest_system.py` / the per-strategy
walk-forward harness so a discovered edge can be routed to the standing gate."*
So P3 is genuinely open.

**But P3 is BLOCKED, and P6 is explicitly low-value:**

- **P3 has nothing to carry.** The ledger states it outright for Studies 1–4:
  *"leads / weak-unstable / null, so there is **nothing to route**"*. A
  hypothesis→backtest bridge with no confirmed hypothesis is a bridge to nowhere.
  It unblocks when a study actually confirms a feature — not before.
- **P6 is, in the session prompt's own words, "a low-priority interpretation
  nicety."**

⚠️ **So the lesson from Lanes 4/5 extends one step: OPEN ≠ WORTH DOING.** The
field check correctly identified P6 as the only unshipped item, and a planner who
stopped there would have gone and built the least valuable thing in the milestone
while the substantive one sat blocked on a precondition nobody had checked.
*Establish that an item is both open **and** unblocked **and** worth its cost
before queueing it* — three checks, not one.

**M30 is therefore parked**, correctly and not by neglect, until a study confirms
a feature worth routing. Capability-integrity work (the health backlog: 158 open
rows, 2 critical / 39 high) outranks it by this plan's own § 0.4 ordering.

⚠️ **A caution that nearly became an error, recorded because it is the same class
as the finding above.** A first pass grepped `shap` and hit three files — which
would have supported "all six shipped". Those hits are **`x.shape[1]`**: a
substring false positive, not SHAP. Token presence is weak evidence for
"implemented"; what settled it was asking whether the token appears in an
`import`, a `def`, or an `add_argument` — for P6 it appears in none. **Do not
grade this list by grep alone**, which is exactly the trap that the roadmap's own
stale list sets for the next reader.

M29's P2–P4 have **not** been field-checked to this standard; `src/sysdyn/identify.py`
exists with 7 functions, which is suggestive for P2 (AI sysID) and nothing more.

---

## 2. Explicitly NOT in this plan

- **M20** — concurrent session owns it.
- **M28 value sleeve** — closed. 21yr OOS, `edge_vs_baseline −0.0047`. Do not
  reopen construction iteration; the input class is mapped.
- **M23 P3+, M18 P2/P3, M21** — parked on honest negatives. Reopening any of
  them needs a *new input*, not another pass at construction.
- **M31 Track B (Schwab)** — blocked on one operator credential hand-off
  (`SCHWAB_APP_KEY`/`SCHWAB_APP_SECRET`). Nothing else to do until it lands.

## 3. Standing cadence

Telegram ping ≥1/hour via `send-ping` while the operator is travelling; every
Tier-2/Tier-3 item surfaces as a decision request rather than being actioned.
