# T1.3 — cross-sectional ranker: next-research-session spawn prompt

> **Status:** READY, not started (operator-parked 2026-07-06 — "saved and ready
> to go"). Paste the prompt below verbatim into a fresh deep-research session
> when picking this thread up. Keep this file in sync with the ROADMAP T1.3
> row and `MB-20260629-ALLOC-RANKER` if the gate or constraints change.

---

Deep-research session — T1.3 cross-sectional ranker (revive M18 P3; the M19/M18 junction).

Start by reading the ROADMAP T1.3 row, `MB-20260629-ALLOC-RANKER` (+ `-COSTCAP`, `-CORR`) in docs/claude/ml-review-backlog.json, docs/research/M18-allocator-backtest-findings-2026-06-29.md (the negative you must beat), and the refreshed soak-clock inventory in docs/sprint-logs/S-VOLGATE-ETHSOL-EVIDENCE-2026-07-06.md § Addendum.

Primary goal: test whether a LEARNED expected-net-R ranker can materially beat the 2026-06-30 baseline of OOS AUC ≈ 0.51 on the cross-strategy candidate set — the one untested lever blocking the whole M18 P2/P3 allocator line. Constraints and gates:

1. **The harness is fixed**: scripts/research/allocator_candidate_dataset.py → allocator_ranker_eval.py → allocator_multisymbol_backtest.py with the **sizing-normalized** control (shared_priority) — the 06-30 finding was that the apparent cross-symbol edge was sizing/concentration, not selection, so any claimed edge must survive that normalization. Walk-forward split, never pooled.
2. **Feature levers to try, in order of expected value**: (a) the per-cell expectancy / regime-cell stack (the trend×vol cell attribution now exists per symbol — BTC #4825, ETH/SOL runtime_logs/volgate_ethsol/attr_*.json on the trainer); (b) the conviction/calibrator stack (ml/calibration + conviction-meta-v1); (c) the T1.2 corpus embedding as a feature block — this is the ONE place the parked SSL encoder might still pay off (task-matched daily/cross-asset decision, per the T1.2 negative's own analysis); (d) decision-time correlation features (MB-20260629-ALLOC-CORR). Check MB-20260629-ALLOC-COSTCAP first — if per-trade cost capture is still a data gap, net-R labels are the blocker to solve or work around before training anything.
3. **Gate before ANY routing plumbing**: OOS AUC materially > 0.51 (pre-register what "materially" means before running — suggest ≥0.55 across all walk-forward folds, no knife-edge), AND the sizing-normalized backtest arm shows selection net ≥ priority net without a maxDD regression. If the ranker fails, record the honest negative in the backlog and stop — do not build P2/P3 on a sub-threshold ranker.
4. Training runs on the trainer VM (free CPU first; a GPU burst only if a deep ranker is justified — ≤$5 within the $10/mo ledger cap, public-data-only on rented pods). All work Tier-1 research; any registry write caps at candidate/shadow; nothing touches routing (that would be M18 P2+, backtest- and operator-gated).

Standing constraints: Tier-1 autonomous; no promotion past shadow without explicit operator approval; trainer git pull works; the trainer-vm-diag relay has a 10-min timeout — nohup long runs and poll. Session end: sprint log + ROADMAP + backlog updates per the sprint-format skill, and refresh the soak-clock inventory table.
