# Work Plan — 2026-08-02

> **Type:** Tier-1 execution plan, built from a verified survey of the whole open
> surface (three review backlogs + the RESEARCH-PROGRAM-2026-07-30 T/S/M/A/R tracks
> + ROADMAP_MACRO + M36). Supersedes the stale handoff framing that listed "A1 +
> M25-P2, both trainer-relay" as the open work — both of those verify as
> done/owned (below). Owns `BL-20260730-RESEARCH-PROGRAM-OPEN-ITEMS` continuation.

## Why this plan exists (the handoff was stale)

A predecessor handoff pointed the session at **A1** (`MB-20260530-001`) and **M25 P2**
(powered-RG4 sweep) as "the two open trainer-relay items." Verification (2026-08-02):

- **M25 P2 is DONE / superseded.** The powered-RG4 sweep deliverable exists
  (`docs/research/M25-rg4-sweep-2026-07-19.md`, per-head PROMOTE/WAIT/NEVER). The
  promotion gate was **reframed 2026-07-19** — RG4/`live_regime_discrimination`
  demoted to advisory-only; promotion evidence is now `gate-check` (live_parity +
  labels_accruing + drift_clean + oos_edge). Its dated re-checks are consumed (SOL
  v2 promoted 08-02) or **claimed on the board by sibling sessions** (BTC fc-pcv-v2
  drift, ~08-03/04). A fresh sweep changes no decision and would collide.
- **A1's pipeline is BUILT and was RUN — for MES only.** S-MLOPT-S7 built the
  `source`/`include_backtest` machinery in both decision families; #8318 ran it
  end-to-end for MES (26 backtest trades → 2 shadow baselines, nightly-pinned
  #8326). The genuine remainder is extending it to the **pooled/crypto** decision
  models (the original n≈78 wall) — see W1.2 + W2.1.

**Also found:** the macro/value/event/credit/seasonality families
(M1/M2/M5/M28/M29/M32/M33/M34) are **conclusively closed-negative** — do not
reopen without a new hypothesis. `ROADMAP.md` is ~4 days stale (stamps 07-28,
changelog stops 07-29). The live frontier is **correctness bugs + unblocking
leverage + the ML data-wall + M36 integration/microstructure**, not new research.

## Execution status (updated 2026-08-02, session `wave-1-sweeps-l2kiin`)

Wave 1 free-runner sweeps executed. Full evidence: [`wave1-sweeps-2026-08-02.md`](wave1-sweeps-2026-08-02.md).

| Item | Status | Result / PR |
|---|---|---|
| **1.7** GLD-1h regime-cell walk-forward | ✅ done | corrected-cost walk-forward (#8410) `short_stable_drag: TRUE`, pooled short −12.89R@36 → the live `trending.gld_pullback_1h {short:off}` cell **HOLDS**. Resolves RESEARCH-PROGRAM operator decision #2: **KEEP**. *(Reproduced the 2026-08-01 #8272 run bit-for-bit — a determinism confirmation, not new.)* |
| **1.4** ETF fee re-grade residual | ✅ done | fee-A/B across 17 residual ETF legs (#8411), all deltas negative; asset-class venue-fee re-read now complete. `PB-20260730-REGIME-EVIDENCE-VENUE-FEE-REGRADE` follow-up #1 closed. |
| **1.3** ADX cut-point sweep (R2) tool | ✅ done + first run | tool + free-runner workflow + label merged (#8412); first sweep on `gld_pullback_1h` (#8413) → `short_stable_drag` robust **16/16** cut-point pairs (cell not fragile to the un-swept 20/25). |
| A1 research-backtest-augment runner | 📋 scoped, build handed off | scope doc merged (#8414) + **roster corrected** (#8417 — real pooled roster is `trend_donchian@1h`/`squeeze_breakout_4h@4h`/`htf_pullback_trend_2h@2h` × BTC/ETH/SOL, NOT the coverage_debt pullbacks). Build = fresh session. |
| 1.1 / 1.2 / 1.5 / 1.6 | ▫️ not this session | still open per the Wave-1 table below. |

### Operator decisions recorded this session (2026-08-02)

- **GLD-1h Track B routing — APPROVED (M36 Track B).** Operator directive: *"track the gold
  from paper to portfolio."* → route `gld_pullback_1h` onto **`alpaca_portfolio`** (the
  paper-money portfolio mirror), carrying the confirmed `trending {short:off}` cell,
  **gated on** `account_compat_matrix --strategy gld_pullback_1h --symbol GLD
  --fee-bps-roundtrip 0` clearing the ROUTE gate for that account's ruleset. The config edit
  is `config/accounts.yaml` (Tier-3 file) but pre-approved — show the exact diff, then ship
  it if the compat-matrix clears; flag it if it doesn't. This is the paper-portfolio-mirror
  step, distinct from the still-pending real-money `alpaca_live` flip (S4, separate).
- **"Run the rest" — APPROVED.** Build the A1 runner (corrected roster), the compat-matrix
  runner, and the R2 dispatches on the other live-celled families
  (`htf_pullback_trend_2h`, `ict_scalp_5m`, crypto donchians). Handed to a fresh session.

## Routing legend

`[code]`/`[doc]` pure repo edit · `[runner]` free GitHub-hosted CPU ($0, parallel,
no VM lane) · `[trainer]` trainer-VM-resident state (FIFO lane) · `[live-read]`
diag/shadow read · `[T3]` operator Tier-3 decision. Route to the cheapest that
suffices (`docs/claude/vm-resource-management.md`): a config-exact backtest needs
**no** VM-resident state and belongs on a runner, never the scarce trainer.

## Wave 0 — inline · no compute · no collision · Tier-1

| # | Item | Backlog id | Route |
|---|---|---|---|
| 0.1 | Fix `trades.created_at` vs `closed_at` mixed timestamp format (string compares silently drop rows across every windowed/sorted consumer) | `BL-20260730-TRADES-TIMESTAMP-FORMAT-MIXED` | [code] |
| 0.2 | Three anti-"falls-through-cracks" CI guards: harness-lever-map coupling · cited-but-unfiled backlog ids · destructive-git-sync preflight | `BL-20260730-HARNESS-LEVER-MAP-COUPLING-GUARD` · `-CITED-BUT-UNFILED-BACKLOG-IDS` · `-DESTRUCTIVE-GIT-SYNC-NO-GUARD` | [code] |
| 0.3 | Doc reconciliations: (a) `MB-20260530-001` status vs S-MLOPT-S7 "closes" claim → verified reality; (b) ROADMAP staleness note to 08-01/02; (c) regen stale natgas econ scorecard (reads pre-#8300 verdict) | see-something-say-something | [doc] |
| 0.4 | Author the `regime-selectivity` skill (no-cosmetic-cell / walk-forward-before-Tier-3 / axis-fidelity rules) | `BL-20260730-REGIME-SELECTIVITY-SKILL` | [doc] (+operator sign-off to adopt) |

## Wave 1 — free runners · parallel · $0 · no VM lane

| # | Item | Backlog id | Tier |
|---|---|---|---|
| 1.1 | **Regime-cell gate integrity**: propagate `--vol-labels` into `regime_cell_walkforward.py` + fix the fold-count verdict flip → re-audit the six *live* 2-D `trend_vol` cells. The required `*_stable_drag` gate cannot run on cells that are order-routing today. | `BL-20260730-WALKFORWARD-NO-VOL-AXIS` · `-WF-FOLDCOUNT-VERDICT-FLIP` · `-2D-VOL-CELLS-UNAUDITABLE` | T1 |
| 1.2 | **A1 CPU half**: config-exact backtests of the live crypto strategies → emit `is_backtest=1` per-trade rows (`record_harness_trades`). Also the M23-P3 label-volume unblock. | `MB-20260530-001` · `BL-20260731-BACKTEST-AUGMENTATION-NEVER-FED` | T1 |
| 1.3 | ADX cut-point sweep (R2), fold-split-validated per family — two unswept global constants gate every regime cell | RESEARCH-PROGRAM R2 | T1 |
| 1.4 | ETF fee re-grade residual (qqq + remaining; gld held) — live OFF cells rest on a ~25× fee over-charge | `PB-20260730-REGIME-EVIDENCE-VENUE-FEE-REGRADE` | T1→T3 |
| 1.5 | Faithful `spy_pullback_1h` re-run (+skip_hours) + walk-forward — largest surviving directional drag, currently `approximate`-only | RESEARCH-PROGRAM S2 residual | T1 |
| 1.6 | `exit_ladder` fleet lever sweep — M20's literal done-condition, un-built fleet-wide | M20 / S3 | T1→T3 |
| 1.7 | GLD-1h regime-cell walk-forward + `account_compat_matrix` — strongest remaining +EV (+5.92R, 4/4 folds); GLD already live on `alpaca_paper`, no new integration | M36 Track B | T1 build |

## Wave 2 — trainer VM · FIFO lane · needs VM-resident state

| # | Item | Backlog id |
|---|---|---|
| 2.1 | **A1 trainer half**: build `include_backtest` datasets from 1.2 + retrain pooled `setup_quality`/`trade_outcomes` + evaluate on a **live-only holdout** (the actual A1 research question) | `MB-20260530-001` |
| 2.2 | Rebuild `setup-candidates-metalabel-xsym-yz-v1` dataset — parkinson/GK/RS/YZ range-vol read dead (self-contained recompute) | `MB-20260726-XSYMYZ-RANGEVOL-DEAD` |
| 2.3 | Harden `ict-promotion-readiness.service` (MemoryMax/SwapMax=0) — the one trainer unit with no cgroup cap, OOMs the sweep | `MB-20260719-PROMOREADY-OOSEDGE-OOM` |

## Wave 3 — live-read first

| # | Item | Backlog id |
|---|---|---|
| 3.1 | Netting partial-close journal integrity: diag-read current phantom rows → propose the fix in `order_monitor.py`/`execute.py`. Investigation T1; **fix is Tier-2** (order-path, operator OK) | `BL-20260801-NETTING-PARTIAL-CLOSE-ROWS-NEVER-REDUCED` |

## Prepare-only — Tier-3 packets (draft, do NOT enact)

GLD-1h real-money flip (post-soak) · any regime cell that clears the fixed 1.1 gate ·
S4 `alpaca_live` dry_run decision · ETF/exit cells surfaced by 1.4/1.6.

## Explicitly excluded

- **Owned by siblings (do not touch):** BTC fc-pcv-v2 drift re-check
  (`full-system-review-zajauh` + `backlog-cont`, ~08-03/04) · ETH class-weight
  recalibration (PR #8345, in flight) · `/dev/null` OCI diagnostic (`ragtvm` #8364).
- **Dead — do not reopen without a new hypothesis:** M28 (D1–D5, #8363) · M29
  (+P2, `park_deeper_investment`) · M32 · M33 · M34 · "M5" vix_term (#8351,
  window-fragile) · M1-events/M2 → M3/M4/M5-events · M36 Track C (base thesis
  exhausted).
- **Blocked on operator/accrual:** M31 options-skew (Schwab credential + ~1qtr) ·
  M24 P3/P4 (broker-truth ledger stale since 07-13) · VPIN/L2 heads (forward
  capture) · conviction P4/P5.

## Execution order

Wave 0 inline (correct, zero-collision) → kick 1.1 + 1.2 to runners (highest
leverage) → remaining Wave 1 as runner capacity allows → Wave 2 behind the VM lane
→ Wave 3 after a diag read. Tier-3 stays prepared packets, pinged to the operator.
Board: merge-slot per PR; `🔒 VM-LANE CLAIM` before any trainer job; loud flag on
any dead run.
