# Research capability index — "can we measure X?"

> **Doc status:** `unknown` · category `evidence` · last verified `never` · registered in [`docs/DOCUMENT-INDEX.md`](../../docs/DOCUMENT-INDEX.md) · **nobody has verified this document's status — do not act on it as current**

**Purpose.** Answer, in one lookup, *what this repo can already measure* — so a research
session never concludes something is unmeasurable when a tool for it exists.

**Why it exists.** On 2026-07-30 a session concluded that `trend_donchian`'s six live
regime cells could **never** be re-measured, because an ML exit head "cannot be replayed
offline." It built a three-option disposition on that, filed a high-severity backlog row,
and escalated it to the operator as a decision. All of it was wrong: the M20 toolchain
(`build_intrabar_exit_panel.py` → `analyze_exit_head.py`) replays exactly that, under
grouped/purged/embargoed walk-forward. The session had looked at one code comment in one
harness — `regime_debt_matrix.py::_UNREPLAYABLE`, whose real scope is *"`backtest_trend.py`
does not model this lever"* — and generalized it to the whole system.

The measured gap behind that: **47 of 51 scripts in `scripts/research/` were mentioned in
no skill**, while `backtesting/SKILL.md` claimed to map *"every real backtest entry point in
the repo."* A session therefore reconstructs the toolbox from `grep` plus code comments —
and code comments are what misled it. Full audit:
[`RESEARCH-INFRA-AUDIT-2026-07-30.md`](./RESEARCH-INFRA-AUDIT-2026-07-30.md).

> **Binding:** before writing "X cannot be measured / is not replayable / needs new
> tooling" in any research output, check this index and grep `scripts/research/`. A tool
> asserting **impossibility** is not authoritative — see the audit § "impossibility claims".

Paths are repo-relative. Each script's own docstring is the detailed contract; this is the
routing layer, not a replacement for reading it.

---

## 1. Strategy P&L over history (net-of-fee)

| Question | Tool |
|---|---|
| How does a Donchian/trend leg perform? | `scripts/backtest_trend.py` — the **one** trend engine (43 flags; its trail freezes the ENTRY bar's ATR, which is what `trend_donchian.monitor()` does) and the one `regime_debt_matrix` runs. There used to be a second, non-live-faithful copy at `scripts/research/backtest_trend.py`; its 15 lever flags were ported here (PR #8633) and it was **retired to a hard-fail shim 2026-08-09**. History: design-doc §5f + `BL-20260808-TREND-HARNESS-FORK-SPLITS-FIDELITY-FROM-EVIDENCE` |
| **Did the 2026-08-30 regime break hit the e35 legs specifically, or the whole directional book?** Three arms — `e35`, a family-matched control and an other-family control — split on `created_at` around a dated break, with every exclusion CENSUSED by name (not_closed / backtest / pnl_null / off_venue / pairs_sleeve_exonerated / fabricated_close / created_at_unparseable) so a shrinking population is never silent. One-sided binomial against the family control's OWN post-break rate, never a 50% null, and it REFUSES rather than dividing by zero on an empty control. Answers ATTRIBUTION only — it cannot say the geometry CAUSED the break | `scripts/research/e35_break_attribution.py` |
| **Is `trend_donchian`'s live trail-decay lever still justified?** Config-exact `(arm_r, tight_mult)` grid + a lever-OFF arm on the CONVERGED engine, IS/OOS + per-year folds; reports whether the lever is INERT (identity vs OFF, and whether `max_mfe_r` even reaches the arm) before ranking cells | `scripts/research/m20_trail_resweep.py` (`BL-20260808-TRAIL-LEVER-TUNED-ON-NON-LIVE-FAITHFUL-TRAIL`) |
| **Why does M20's coverage roll-up over-count `shipped`, and how were the bundled rows split?** One-shot migration that explodes the multi-leg coverage rows into one row per leg, assigning per-leg status ONLY from each ref's explicit wording (silent, or a run that ERRORED/TIMED OUT, becomes `pending` — an un-run cell is not a negative). Asserts row count and leg-uniqueness, and refuses to run against an already-exploded file rather than corrupting it | `scripts/research/m20_explode_coverage_rows.py` (`BL-20260809-COVERAGE-MATRIX-MULTILEG-ROW-ONE-STATUS`) |
| **How many trades does a proposed trail-decay retune actually TOUCH?** The denominator behind the sweep's aggregate: trades that ARM the lever (`mfe_r >= arm_r`) and, of those, which exit differently between two `tight_mult` values — with the per-trade R deltas, so a headline is traceable to the trades that produced it. Three-way verdict (`INERT` / `TOO_THIN` / `MEASURABLE`) so "no evidence" never reads as "no difference". Arms joined on `entry_time`, never zipped positionally | `scripts/research/m20_trail_attribution.py` (`BL-20260808-TRAIL-LEVER-TUNED-ON-NON-LIVE-FAITHFUL-TRAIL`) |
| **A banking cell says `honest_negative` — WHICH kind of negative is it?** Reads a sweep's existing `verdicts.json` and reports what the net_R+maxDD gate discards: **MAR** (`net_R/maxDD`) and **DD/R** (drawdown removed per unit of net_R surrendered), beside `banked_pct` (the rung-fill denominator — a rung that never filled is INERT, not a negative). `honest_negative` covers at least three different objects: a return-for-smoothness trade, a **both-axes loss** (measured on 3 of 4 ict_scalp 5m legs, 2026-08-10), and an inert rung; the gate reports all three identically. **Changes no gate, ships nothing** | `scripts/research/m20_banking_risk_adjusted.py` (`BL-20260810-BANKING-GATE-CANNOT-PASS`; memo §10.6/§11) |
| **Is a second trend engine creeping back in?** Convergence guard — fails when any other `backtest_trend.py` exposes an engine entry point, and names the flags it declares that the canonical engine does not. Carries the retired fork's measured divergence as the record | `scripts/research/trend_harness_divergence.py` (CI: `trend-engine-convergence-guard`) |
| **Did the capital EARN ITS KEEP while it was committed?** — `net_R per position-day` and `net_R per capital-day`, the exit-refinement gate's declared-but-never-implemented tiebreak. A trade reaching TP after 149 bars scores identically to one reaching it in 10 under a net_R+maxDD gate; only this separates them (operator directive 2026-08-10, off a real-money `eth_pullback_2h` held 149 bars on a 2h strategy). Bar length is **measured from the frame's timestamps**, never the `--timeframe` label; a value it could not compute is **None, never 0.0** (an undefined rate must not rank alongside a flat one). `capital_bars` is SIZE-WEIGHTED so a partial-TP release is credited its shorter hold — measuring banking on unweighted hold would score it identically to doing nothing. **The ONE definition** (used by `backtest_ict_scalp.py` + `backtest_pullback.py`; a second copy would make cross-harness comparison meaningless, the `candle_io.py` lesson) | `scripts/capital_efficiency.py` (`BL-20260810-NO-STALL-EXIT-CAPITAL-SITS-IN-DEAD-TRADES`) |
| **Is a Path B base-rate FLOOR supported by the data, or would it be a guess?** Path A's derived allowance (`allowed = base_maxDD x (dNetR / base_netR)`) gets more permissive the WEAKER the base book — `eth_pullback_2h vt_cold10_t2.5` cleared with +43.59R of headroom on a book earning 6.62R against a 16.41R drawdown (rate 0.40). A floor would fix that and re-introduce the free parameter the derivation removed, so this tests whether the rate actually predicts fold-generalisation before anyone sets one. **Three verdicts, never collapsed:** `insufficient_population` (we did not look) · `no_separation` (we looked, it does not predict) · `separation` (floor + effect + p). Scanning K floors is selection over an unstated denominator, so K is printed and a `separation` must clear a **Bonferroni** bar; alpha is surfaced as the one CHOSEN number. Recommends nothing it cannot support | `scripts/research/m20_path_b_floor.py` (operator directive 2026-08-10: *"database decisions and not arbitrary guesses"*) |
| **Where does the sweep evidence GO between runs?** Flattens `verdicts.json` into an accumulating, versioned per-cell corpus. Before this, every sweep's evidence went to an Actions **artifact no session can download** (`CLAUDE.md` § PM-side capabilities) with a top-30 slice in the PR comment — so each run restarted the population from zero and no threshold could be derived from more than one sweep. Legs that produced NOTHING (harness error, skipped) get rows too: they are part of the fleet denominator. Re-extracting a run **supersedes** its rows rather than appending, so a post-fix re-run does not leave the pre-fix vintage in the population | `scripts/research/m20_corpus_extract.py` → `docs/research/m20-sweep-corpus.jsonl` |
| **A research run CONCLUDED something — where does the CONCLUSION live?** The corpora answer *what was measured*; nothing answered *what this run decided, against which pre-registered rule, over what population*. MEASURED 2026-09-22: research workflows overwhelmingly ended at `actions/upload-artifact` plus a `gh issue comment` — an artifact EXPIRES and a comment is not queryable, so a result that cost real runner minutes existed only until it didn't. This is R1 of `RESEARCH-WORKFLOW-ARCHITECTURE-2026-08-27.md` made executable. A record must carry its `research_unit`, the `decision_rule` it was graded against (registered BEFORE the run), the stated population and `n`, the commit sha and run id, and — **never collapsed** — the `verdict` BESIDE a `read_state`: `measured` with `n: 0` is *we looked and found nothing*, while `no_data`/`producer_failed`/`not_attempted` REQUIRE `n: null`, because a fabricated zero asserts a measured empty sample. The store is one file per `(unit, run)` with the **unit id in the PATH**, so a session holding a queue unit finds its answers by name with no index; `.jsonl` so `assert_rows_landed.py` reads it verbatim and `--min-rows` means the per-leg fan-out width. ⚠️ **NOT A SECOND CORPUS** — it POINTS at the measurement rows (`artifact.store` + `artifact.locator`) and never copies them | `scripts/research/research_result.py` (guard: `research-results-guard`; action: `.github/actions/research-result`) → `research/results/<unit>/<run_id>.jsonl` |
| **Did the exit-head build clear its bar, and can I still read that in a month?** Maps `research-exit-head-build.yml`'s own output onto the result contract. ⚠️ **THE DISCRIMINATION BLOCK IS `regression`, NOT `oos_discrimination`** — the first draft used the plausible name, which would have graded EVERY healthy run `no_data`: a wrong record, green, forever. There is a planted control for exactly that key. ⚠️ **`n` is TRADES, never panel ROWS**: the trade count lives on the panel MANIFEST (`trades_used`), while `exit_head.json` carries `n_rows_total` (several rows per trade), so reading the latter as a denominator overstates n by that factor. `clears_bar` MISSING is `indeterminate`, never `fail` — a plumbing gap must not manufacture a negative finding | `scripts/research/exit_head_result.py` (`--self-test`: 2 positives + 8 planted defects) |
| **Which legs did this lever sweep actually cover, and did any cell beat the shipped geometry?** One result record PER DISPATCHED LEG, so `--min-rows` can assert the fan-out width and a run that silently dropped four of twelve legs fails instead of grading green. The population is the DISPATCHED leg list, not the observed one: a leg that died before writing a row still gets a `no_data` record, because deriving the population from the rows makes a dropped leg indistinguishable from one nobody asked for. `no_action_warranted` (cells swept, none cleared the gate) is `read_state: measured` with a real `n` — *we looked* — and is never collapsed into the `no_data` of a leg that produced nothing. ⚠️ **The rows are NOT on `main`**: a default-branch dispatch retargets the corpus to `claude/m20-sweep-corpus`, which never merges, and the record's `artifact_locator` says so rather than implying otherwise | `scripts/research/m20_sweep_result.py` (`--self-test`, plus verified against 1,379 real corpus rows) |
| **Where did the e35 bracket-sweep's measured cells GO?** Until 2026-08-23: nowhere a session could reach. The sweep measured **19 legs x 199 cells = 3,781** and ended at `actions/upload-artifact`, while a PM-side session has **no artifact download** (root `CLAUDE.md`) and `m20-sweep-corpus.jsonl` carries only LEVER cells — measured at 1,379 rows, 331 donchian, and **zero** naming a stop. So re-interrogating any cell (a dispersion test, a different gate, `wins_effective` beside `wins`) meant re-running a multi-hour sweep from candles, which a session did on 2026-08-23. This flattens `report.json` into one durable per-cell row and `e35-bracket-sweep.yml`'s new `corpus` job commits it. ⚠️ **A SEPARATE FILE FROM THE LEVER CORPUS, DELIBERATELY** — a lever cell answers *does this exit lever help* and carries `lever`; a bracket cell answers *does this (tp, stop, timeout) geometry help* and carries `axis` plus the three geometry values. Merged, a bare row count is wrong for both, and this repo has already paid for two sweeps sharing a filename with different schemas. Every row also stamps `corpus: e35_bracket` so a copied row still says what it is. **Re-extracting a run SUPERSEDES its rows** on `(leg, cell, tp_cap_pct, split_mode, split_target_oos)` so a post-fix re-run cannot leave the pre-fix vintage beside it. **An ungated cell keeps `gate_verdict: null`** — never confused with a cell that was gated and passed. A **foreign `report.json` is refused loudly** (markers taken from the reader, not guessed), an **empty scan is a refusal not a clean no-op**, an unparseable corpus row **refuses the rewrite** rather than dropping it, and the write is **read back and verified** rather than trusted | `scripts/research/e35_corpus_extract.py` (`BL-20260823-E35-SWEEP-EVIDENCE-HAS-NO-DURABLE-PATH`) -> `docs/research/e35-bracket-corpus.jsonl`
| **Is a gate FAIL a return-for-drawdown trade, a loss on both axes, or an inert cell?** The e35 bracket gate reports all three as `is_oos_fail`, and 77/77 donchian cells failed it. `m20_banking_risk_adjusted.py` already separates them (MAR, and drawdown bought per unit of return) but reads only the LEVER sweep's `verdicts.json`. This adapter reshapes an e35 run into that shape so the CANONICAL arithmetic is imported rather than re-implemented — a second definition of MAR is exactly the `_regime_score_semantics` hazard (*two probes re-derived it independently and both got it wrong the same day*). ⚠️ **Per-half ABSOLUTES come from `results.jsonl`, verdict STRINGS from `report.json`** — `report.json`'s gate block keeps only deltas, and a delta cannot produce a ratio of levels. ⚠️ **Only the gate SHORTLIST has halves** (measured: 7 of 199 per leg); a cell the sweep never split is *we did not look*, so it is counted in `cells_without_halves`, never emitted with a null half. ⚠️ **The NET column lands in the gated slot** (`net_total_r` -> `total_r`), because that is what `fleet.beats` gates on and what the tool prints as netR; e35's gross `total_r` there would be a label substitution. ⚠️ **`dd_per_r` is null for a return-GAINING cell by construction** — the tool computes it in the banking direction only, so read the ratio the other way up rather than reading the null as no trade-off | `scripts/research/e35_verdicts_adapter.py` -> `scripts/research/m20_banking_risk_adjusted.py` (`docs/research/donchian-stop-gate-is-drawdown-bound-2026-08-23.md`)
| **Two corpora disagree — which row is the real measurement?** Unions one M20 sweep corpus into another, resolving each shared `measurement_key` by **`sweep_generated_at`** rather than by which side is being merged in. That distinction is the whole point: the extractor's own merge lets the incoming side supersede unconditionally, which is right for artefacts→corpus and BACKWARDS corpus→corpus — measured 2026-08-17, 904 shared keys with 19 differing, and on all 19 `main` was the newer and more complete side, so a side-wins union re-drops the schema `BL-20260816-CORPUS-CONFLICT-REDERIVE-RUNS-THE-STALE-BRANCH-EXTRACTOR` exists to protect. Where the timestamp cannot order two rows it breaks the tie only on a strict superset of keys and otherwise **refuses (exit 2)** rather than picking a side. Called by the sweep workflow's conflict re-derive so the corpus branch stops shedding default-branch rows on every run | `scripts/research/m20_corpus_union.py` → `docs/research/m20-sweep-corpus.jsonl` |
| **A corpus row is missing a field — was it NOT MEASURED, or measured and DROPPED IN TRANSIT?** Those are opposite claims and the M20 corpus cannot presently settle them, because whether a run's extractor COULD emit a field is a property of the COMMIT it was dispatched with and **the corpus records no dispatched sha** — `run_id` is deliberately the sweep's own `generated_at` TIMESTAMP (`m20_corpus_extract.run_id_for`, correct for its own purpose), so a row cannot be joined to the workflow run whose `head_sha` would answer it. This grades every row against **two** candidate boundaries — the earliest `sweep_generated_at` among rows that DO carry the field, and when the field reached the default branch — and where they disagree it answers `absent_between_boundaries`, ***we cannot establish it***, rather than picking whichever boundary happened to be available (the unprovenanced-diagnostic **sub-class B** shape). ⚠️ **It caught its own author's error by 20x**: a hand probe against the default-branch boundary alone called **21** rows degraded; measured over `main`'s 1379 rows the split is present 452 / predates 906 / **after both 1** / **between 20** / undateable 0. A shallow clone cannot read the default-branch boundary at all, and that reads as `unreadable` with the census declaring itself ONE-BOUNDARY — so the absence of `between` is never mistaken for agreement. **Writes nothing and re-grades nothing**: stamping 'schema-degraded' on rows whose cause is unestablished would record a claim nobody measured | `scripts/research/m20_corpus_schema_census.py` (`BL-20260912-THE-SWEEP-CORPUS-RECORDS-NO-DISPATCHED-SHA-SO-A-MISSING-FIELD-CANNOT-BE-TOLD-FROM-ONE-THE-EXTRACTOR-COULD-NOT-EMIT`) → reads `docs/research/m20-sweep-corpus.jsonl` |
| **A corpus row says `tp_cap_pct: null` — was that run UNCAPPED, or did it just not record the field?** The two are opposite claims and the corpus cannot tell them apart, because `tp_cap_pct` is in `measurement_key`: a mislabelled row keys as a separate geometry and never supersedes its true counterpart. Diagnosed for the 140 affected rows by **dating the code from each run's head SHA, never from when the change reached `main`** — the runs were `event: push`, so `inputs` was empty and `TP_CAP_PCT` fell to the workflow default `0.099`; the two bracketing runs differ only in whether their sweep script wrote the field (`d76643b9` no, `e6e519d1` yes, ~8 minutes apart on the feature branch while `main` saw it a day later). Fills **only** the cap: `min_oos_trades_floor` stays null because null is the TRUTH for a run that predates the floor, and the relabel therefore collapses nothing | `scripts/research/m20_corpus_relabel_tp_cap.py` (dry-run by default; refuses rather than guessing when the corpus does not match its stated preconditions) |
| **A cell reports `wf_summary: 5/6` — did the lever WIN five folds, or sit out three of them?** The walk-forward gate returns `ok` for a fold in which the lever changed **nothing** (`d_net_r == 0.0` AND `d_max_dd == 0.0`), so a no-op counts as a win and a bare `N/M` is not quotable. Needs **no re-sweep**: every wf-carrying corpus row already ships `wf_folds` with the per-fold deltas, so this has been measurable from committed data all along and the only thing missing was a reader. Measured 2026-08-17 over 96 newest-run `(leg, lever, cell)`: **75 of 386 `ok` folds are inert (19.4%)**, reaching SHIPPED levers on the real-money `bybit_2` leg — `trend_donchian_xrp_4h`/`decay_arm2R_t2.5` records `5/6` and is **2/6 effective**. Prints `effective` **beside** the recorded figure, never instead of it (overwriting would destroy the evidence that the two differ); requires BOTH deltas at zero, and treats an **absent** delta as not-inert since absent is *not recorded*, not *recorded as no change*. **Re-grades nothing, writes nothing** | `scripts/research/m20_wf_effective.py` (`BL-20260817-FLEET-SWEEP-WF-COUNTS-INERT-FOLDS-AS-WINS`) |
| **The harness says a lever arms at 4.5R — can the LIVE book even get there, and did it?** M31 P4 backtest<->live MFE parity. **Two checks, separate because they need different denominators.** **Check A (ceiling)** is per-ROW and gradeable at **n=1**: a live trade's `peak_r` must not exceed its own `cap_r`, the venue TP ceiling that ends the trade. Measured 2026-08-17 over the whole live table: **14/14 `within_cap`, 0 breaches** — `xrp_pullback_2h` closest at **87.1%** of its own ceiling, `ada_pullback_2h` 73.3%, the other 12 below 61%. **Check B (distribution)** compares harness `mfe_r` against live `peak_r` per leg and **ABSTAINS** until it has FINAL (post-close) live MFE at n — today **n=1 fleet-wide** (14 telemetry rows = 13 open + 1 closed, lifecycle join certified `filter_state: applied`, `total: 27`), and the committed corpus carries **no per-trade `mfe_r`** either, so both halves are missing. Refuses three things: it never grades a row whose lifecycle is unknown (a closed row is byte-shaped like an open one — `PB-20260817-TELEMETRY-HAS-NO-TERMINAL-SNAPSHOT`, **closed 2026-08-17**), never compares against an **uncapped** harness book (`tp_cap_pct <= 0` has no take-profit path at all, so `mfe_r` runs past the ceiling by construction), and never treats live `peak_r` as final (it is a **lower bound** truncated at the last exit pass, stamped on every record). Seven never-collapsed `parity_state` values so an ungraded leg never reads as a passing one; `--self-test` asserts the probe FLAGS planted breaches before any quiet result is trusted. **Takes its harness half two ways (2026-08-17): `--harness-emit` raw sweep rows, or `--harness-dist` a COMMITTED per-leg distribution — mutually exclusive, because two harness sources at once would make the report's provenance a function of argument order.** With a committed artifact the **`tp_cap_pct` gate becomes PER-LEG** rather than one global flag (an artifact can hold legs swept under different settings, and one uncapped leg must neither condemn nor be excused by its neighbours), and `harness_source`/`harness_symbol`/`harness_timeframe` travel into every record so a 4h SOL distribution is distinguishable from a 1m BTC one | `scripts/research/m31_mfe_parity.py` (M31 P4; CI runs the self-test only — running the parity check itself would green on `live_no_final_rows`) |
| **Check B wants a harness `mfe_r` distribution — where is it?** It was **nowhere**: `backtest_trend.py` computes `Trade.mfe_r` and writes it to the `--emit-trades` JSONL, but that file is committed nowhere and `m20-sweep-corpus.jsonl` holds CELL-level aggregates only — a key census over all **1,376** corpus rows finds **zero** keys containing `mfe`. So P4's harness half had no standing artifact and had to be regenerated per comparison, and a session waiting only on live soak depth would have found it still missing when the depth arrived (`PB-20260817-NO-COMMITTED-PER-TRADE-HARNESS-MFE`). Aggregates emit rows into a small committed per-leg record — percentiles + `n`, never per-trade volume. ⚠️ **REFUSES to write an UNCAPPED sweep** (`--tp-cap-pct <= 0`): with no take-profit path `mfe_r` runs past the venue ceiling by construction, so that is a different book, and committing it under the name Check B reads would be worse than the honest absence it replaces. **Also requires `--symbol` + `--timeframe`**, which is what lets the consumer refuse a mismatch — the repo's only committed candles are `data/backtest_candles.csv` (**BTCUSDT 1-MINUTE**, ATR ≈ 0.101%, where the 9.9% cap lands at ~37R against live legs at cap_R **2.13–5.83**), so a fixture-derived distribution is an order of magnitude out. Percentiles are **imported** from `m31_mfe_parity._pct`, never re-derived — two definitions of p80, one writing and one reading, would drift silently and both look right in isolation | `scripts/research/m31_harness_mfe_dist.py` (M31 P4/P5 precond. 2; `--self-test` carries a **positive control** — a capped sweep with identity IS written — before its three refusals, so they cannot pass by refusing everything) |
| **What exit geometry does a MANUALLY-BRIDGED prop leg support?** A prop ticket's exit is a static broker bracket (`src/prop/breakout_ticket.py:18`), so a trail-managed evidence record describes a geometry the account never runs. This runs the leg's own entry argv (`regime_debt_matrix.build_harness_cmd`) through `backtest_trend.py` with the exit flags swapped for bracket-only geometries, re-walks each trade's candles for bars-to-MFE / bars-to-level (asserting the re-walked MFE equals the harness `mfe_r`, the positive control), and scores the prop ruleset (daily loss, P(breach within N) from a cushion). The candidate set and decision rule are hard-coded to what E65 pre-registered; change the memo first. Percentiles imported from `m31_mfe_parity._pct` | `scripts/research/prop_exit_evidence.py` (manual-only; E65, `docs/research/prop-exit-evidence-2026-09-24.md`) |
| **Two ETH legs passed the exit-head E1 gate — is that the SYMBOL, or the BOOK SIZE?** Gives the ETH hypothesis a denominator instead of two positives (operator decision (a), 2026-08-14) and inverts it: over the 20 of 36 resolved `exit_head_ml` cells whose ref states an `n_oos`, `is ETH` classifies the verdict at 80.0% while a single `n_oos >= 350` split reaches 90.0%, and holding book size fixed the symbol adds nothing (large-book stratum: ETH 2 of 2, **non-ETH 4 of 5**). What is left of the effect is ONE cell. The point is about the GATE: E1 needs two-thirds of folds on the right side, per-fold noise falls as trades-per-fold rises, so a gate 90% predicted by book size is substantially a **power test** — and a thin-book `honest_negative` is nearer *underpowered* than *the head does not help*, which the status vocabulary cannot say. Prints its population, its mixed geometries, and that the 350 split is fitted on the same sample (so 90.0% is an upper bound), and reports the one internally-clean live-parity stratum separately | `scripts/research/m20_exit_head_denominator.py` (`BL-20260814-EXIT-HEAD-E1-GATE-IS-90-PERCENT-PREDICTED-BY-OOS-BOOK-SIZE-NOT-SYMBOL`) |
| **Would making a declared take-profit FIRE actually have made money — and how much?** Sizes the prize before an exit sprint is spent, which is what stopped the 2026-08-21 review shipping a claim it had not earned. Per closed trade: did price REACH `take_profit_1` during the hold (1h bars from `/api/bot/candles`, the same connector the strategies trade on), and what would that TP have paid on the same qty? ⚠️ **THE HEADLINE IS A NET OF TWO OPPOSITE EFFECTS and neither half is quotable alone** — measured 2026-08-21 over 323 closes (2026-07-23→08-21): **52 (16%) touched TP; 29 exited WORSE (+$20,137 recoverable) and 23 exited BETTER (−$13,899 forfeited — winners that ran), NET +$6,237**, of which **$1.81 is real money** and $6,235 paper. By family: scalp +5,562 (41 of 109 touched), trend/donchian +522, pullback +153, **pairs 0 of 121** (their 2×/0.5× placeholder brackets are never reached, which is its own finding). 25 of the 29 give-backs exited `reconciler_filled`. So the mechanism is real and positive but at **1.45:1, over 16% of trades, essentially all paper** — a case for a measured reversible change, NOT for the 'highest-leverage item in the exit program' it was almost called. Reach rate is a LOWER BOUND (1h cannot see intrabar); fees ignored and a fill AT the TP assumed, both of which bias the prize UP | `scripts/research/tp_recovery_counterfactual.py` (`BL-20260821-SCALPS-HELD-10-TO-100X-THEIR-DESIGN-HORIZON`; operator decision 2026-08-21: *diagnose deeper before building*) |
| **How long does a live trade actually wait between exit evaluations, and would decoupling the monitor fix it?** The per-hook tick split (`/api/diag/tick_cost`) answers both. Measured 2026-08-10 over 18 ticks: the tick costs **104s mean / 125s max** against the operator's 60s ask, split ~50/50 between signal generation (51.7%) and the order monitor (46.8%), with `attributed_pct` 98.5 so every other hook is ~1.5% COMBINED. The decouple is therefore **necessary AND barely sufficient** — the monitor's own 52.8s peak becomes the cadence floor, 13% under the target on an 18-tick sample — so it must ship with a budget on the monitor's own runtime, whose VALUE needs a soak rather than this reading | `/api/diag/tick_cost` → `docs/research/M20-exit-monitor-decouple-evidence-2026-08-10.md` |
| Load OHLCV (CSV / Parquet / **JSONL**, `ts`→`timestamp`) for a research script | `scripts/candle_io.py` — `load_candles` / `resample_ohlcv`, lifted verbatim from the retired engine and now **the one reader**: `scripts/backtest_trend.py::_load_candles` delegates to it, so JSONL (what `build_continuous_contract.py` writes) works again (`BL-20260809-TWO-CANDLE-READERS-DIVERGE-ON-JSONL`) |
| A pullback leg? | `scripts/backtest_pullback.py` |
| A TTM squeeze (BB-inside-KC) leg? | `scripts/backtest_squeeze.py` |
| A fade/failed-breakout leg? | `scripts/backtest_fade.py` |
| ict_scalp? | `scripts/backtest_ict_scalp.py` — ⚠️ **gross only, no fee model** (`BL-20260610-M15-1`) |
| vwap? | `src/backtest/run_backtest_vwap.py` |
| The whole multi-strategy system on one clock? | `scripts/backtest_system.py` |
| **The whole multi-strategy system on N BOOKS, with a SWAPPABLE election key?** The one-book harness above models a single shared book and always uses whatever ranking key is compiled in, which is why the trade-prioritisation A/B could not be EXPRESSED and sat unmeasured from 2026-08-31. This replays many books over one tick stream — ONE `gate_intents` per tick, N `elect_from_gated` calls, which is the caller that split exists for — under `--arbitration per_account|global`. Under `global` a book that does not carry the winning strategy **STANDS ASIDE and it is COUNTED**, which is the `BL-20260827-PROP-ONLY-TWIN-WINS-THE-GLOBAL-SYMBOL-SLOT-AND-STARVES-ITS-PAPER-SIBLING` shape made visible instead of rendering as silence. ⚠️ It is a SECOND engine and the drift risk is closed by an EXECUTABLE assertion, not a promise: at N=1 with the full roster it reproduces `run_system_backtest` **trade-for-trade**, and that test fails rather than going vacuous if its fixture stops producing trades | `scripts/research/nbook_portfolio.py` (→ `docs/research/trade-prioritisation-ranking-key-AB-2026-09-08.md`) |
| **Which RANKING KEY should decide when two trades compete — and does the order matter at all?** Confidence has been the live primary key on REAL MONEY since 2026-08-31 (PR #10544) on a CORRECTNESS argument, never an outcome result. Four declared arms: `confidence_first` (the SHIPPED key — `src.runtime.intents._election_sort_key` **imported by identity**, never a copy, so the A/B cannot end up measuring a drifted duplicate), `priority_first`, `recent_pnl_first`, and `random_tiebreak` — **the CONTROL, and the floor the others must clear**. Installed by patching the module attribute for one replay and restoring it unconditionally, so nothing in `src/` changes; the tier-3 track-record term is repointed at the REPLAY's own closes because the live one reads `trade_journal.db` (look-ahead AND a machine dependency). **Answered 2026-09-08: NO** — at power the shipped key is not distinguishable from `priority_first` (folds 2-2, p=0.583) or `recent_pnl_first` (folds 2-2, p=0.937), and the only effect that survives is STABILITY (the control flips 17x as often). ⚠️ Every expectancy in every stratum is NEGATIVE, which bounds what any key can be worth on that population | `scripts/research/ranking_keys.py` + `.github/workflows/ranking-key-ab.yml` (→ `docs/research/trade-prioritisation-ranking-key-AB-2026-09-08.md`) |
| **Did a ranking arm actually BEAT the control, or just win a fold majority while losing?** The gate for the arms above: **pooled net R AND a majority of EFFECTIVE folds, never either alone**, with inertness IMPORTED from `m20_wf_effective.is_inert` rather than restated — because `be_floor_r=1.5` won a strict majority of exercised folds under all three panel members while LOSING 35R, with 30-39% of its folds inert. Its self-test plants exactly that shape and requires a refusal. Four verdicts, never collapsed: `preferred` / `not_preferred` / `inert` (the arm changed NOTHING vs the control — not the same claim as 'no better') / `insufficient_population` (**we could not grade**). Declares its Bonferroni correction (alpha/3) BEFORE any pairwise read, and states that its p is a LOWER bound because backtest trade sequences are autocorrelated. REFUSES to grade arms whose fold panel or cost basis differ | `scripts/research/rank_arm_report.py` |
| Does our backtest actually reproduce the LIVE trade distribution (is a leg's backtest TRUSTWORTHY)? | `scripts/research/backtest_fidelity_calibrate.py` — the backtest↔live agreement gate (win-rate + KS on realized-R over measured-provenance live trades), verdict `calibrated`/`drifts`/`insufficient-live`; the earned-trust linchpin of the Faithful-Backtest Platform (`docs/research/FAITHFUL-BACKTEST-PLATFORM-DESIGN-2026-08-04.md`) |
| **Can I TRUST `expectancyR` before I quote it — and how much of it is one handful of rows?** Reproduces `/api/bot/performance`'s own headline fields (n / wins / totalPnl / totalR) on the population that endpoint actually uses — `demo=False` means **real money only**, plus the three documented exclusions — and only then decomposes the R by `src/runtime/r_provenance.py::classify_r` state. **The reproduction is the point:** without it the decomposition describes a lookalike population rather than the instrument, and this repo already has a figure whose sign flips on the filter choice. Measured 2026-09-06 on the live journal: the 30d real-money window reproduced exactly (n=39, wins=15, totalPnl −3.6266, totalR +38.2891) and **12 rows — 30.8% — carried 117% of its R**, so removing them flipped `expectancyR` from the published **+0.9818 to −0.242**, agreeing with `profitFactor` 0.95. Prints a `|R|>threshold` **control** and says plainly when the separation is NOT clean, so a quiet result can never be read as a passing one. Mechanism it exists to expose: `r_multiple` `abs()`-es a stop stored on the wrong side of entry, and `trades.stop_loss` holds the **final** stop while R is defined against **entry-time** risk. Read it before quoting any R-based promotion verdict | `scripts/research/r_contamination_audit.py` (`--trades`/`--packages` take the `rows` arrays from `/api/bot/db/table/{trades,order_packages}`; evidence `docs/research/real-money-0for13-attribution-2026-09-06.md`) |
| Does adding funding+slippage move a leg's backtest↔live agreement toward the gate (P1 cost attribution)? | `scripts/research/backtest_fidelity_cost_ab.py` — execution-realism A/B: derives fee-only vs fee+slippage+funding arms from one cost-on emit (identical trade set), calibrates each against the measured-provenance live journal, reports the KS/win-rate delta + a direction-stratified cost-on agreement (`docs/research/FAITHFUL-BACKTEST-PLATFORM-DESIGN-2026-08-04.md` § 3.B/§ 5a) |
| **Is the harness's slippage term the right size? What is realized slippage per venue from real fills, and which Stage-0 verdicts flip at it?** Joins `exchange_fills` (by `broker_order_id`) to `order_packages.entry` for the entry side, and MEASURED-provenance `sl`/`tp` exits to the package stop/TP level for the exit side. Reports n, mean, median, p75/p90 and a bootstrap CI per account; paper accounts are kept separate. Then re-prices every `comms/strategy_evidence` record linearly from its per-trade `cost_slippage_r`, after a positive control that reproduces `net_r_oos` at the recorded bps. Measured 2026-09-24: Bybit perps real (`bybit_2`) +0.87 bps round-trip, CI [-0.91, +2.95] (entry n=87, exit n=42); equities and futures unmeasurable (checked: scripts/research/realized_slippage.py). **Changes no default** | `scripts/research/realized_slippage.py` (checklist D3) → `comms/research/d3_realized_slippage/` |
| **For Alpaca equities and IBKR futures: how much slippage can be measured now, what blocks the rest, and when does each venue reach a usable n?** Reuses D3's `measure()` unchanged. Reports entry-side mean with a 95% bootstrap CI per account, paper and live separately. Adds a sensitivity that drops entries in the first 5 minutes of the US session, because for `_1d` legs those measure the overnight gap. Adds a census of sl/tp-family closes by exit provenance, and an exact-Poisson accrual forecast to n=20/30. The forecast uses the live roster's legs' `alpaca_paper` history. Measured 2026-09-24: alpaca_live entry n=3 (insufficient); 0 of 39 Alpaca sl/tp-family exits MEASURED (checked: scripts/research/e62_slippage_accrual.py). **Changes no default** | `scripts/research/e62_slippage_accrual.py` (checklist E62) → `comms/research/e62_equity_futures_slippage/` |
| Time-series momentum / MA-cross (new-idea harness)? | `scripts/research/research_momentum.py` |
| A roll-adjusted continuous futures series to test on? | `scripts/research/build_continuous_contract.py` |
| Convert raw `market_raw` jsonl candles to harness CSV? | `scripts/research/market_raw_to_csv.py` |

## 2. Regime conditioning — when is a leg strong vs weak?

| Question | Tool |
|---|---|
| Net-R per (trend regime × direction) from **any** harness's emitted trades? | `scripts/research/regime_tag_emitted.py` |
| The same, split by the **2-D `(trend, vol)` cell the live router gates on**? | `scripts/research/regime_tag_emitted.py --vol-labels` |
| **Would this grade survive a different candle feed?** (the #8144 defect) | `scripts/research/regime_tag_emitted.py` — `boundary_exposure`, emitted on every run |
| The direct two-feed test of that | `scripts/research/regime_tag_emitted.py --sensitivity-data <second-feed>` |
| **Per-bar `calm`/`volatile` labels** — replay the live ML vol axis offline | `scripts/research/ml_vol_label_replay.py` |
| Does that replay actually reproduce the LIVE label? | `scripts/research/ml_vol_label_replay.py verify` |
| The same, driven per-strategy off live config, with a fidelity label? | `scripts/research/regime_debt_matrix.py` |
| Is ONE (regime, direction) cell **temporally stable** (the #7915 gate)? | `scripts/research/regime_cell_walkforward.py` |
| Is a cell's verdict **robust to the ADX attribution cut-points** (live 20/25) or does it flip? (R2 — the two un-swept global constants every cell keys on) | `scripts/research/regime_adx_cutpoint_sweep.py` |
| Regime × strategy matrix (routing groundwork)? | `scripts/research/regime_matrix.py` |
| Conditional discovery **within** a regime cell over a panel? | `scripts/research/analyze_panel_by_cell.py` |

⚠️ **Fidelity is the load-bearing field** on §2 rows. `faithful` = the harness models every
lever the live config declares; `approximate` = it does not, and the omitted levers are
named. **Never author or retire a live cell off an `approximate` row.** A lever the harness
cannot model is not necessarily unmeasurable — check §3 before concluding that.

⚠️ **The vol axis is a SECOND fidelity question, and it is about POPULATION, not levers**
(`BL-20260730-2D-VOL-CELLS-UNAUDITABLE`). Six live cells are 2-D `trend_vol` cells. Grading
such a strategy on the 1-D trend axis alone POOLS vol states the live gate already refuses,
so the verdict measures a population live does not trade — it can be complete, green, and
still wrong-signed (a near-miss Tier-3 proposal on `squeeze_breakout_4h` was withheld for
exactly this on 2026-07-30). Pass `--vol-labels` for any strategy carrying a `trend_vol`
cell; `regime_tag_emitted` declares `vol_axis: absent` when you don't, so the gap is always
visible in the artifact rather than assumed away.

⚠️ **A THIRD fidelity question, and it is about the INSTRUMENT itself**
(`BL-20260731-REGIME-ATTRIBUTION-FEED-SENSITIVE`). Per-regime net-R is bucketed by HARD
cutoffs (chop <20 · transitional 20-25 · trending ≥25) applied to a noisy rolling indicator,
against a heavy-tailed per-trade R distribution — so a few large winners sitting near a
cutoff carry tens of R across it on sub-1% input differences. **Measured (#8137): the SAME
357 trades re-tagged against a second, equally-valid BTCUSDT 1h feed moved one bucket by
24.92R (~31% of lifetime net-R)** while the two feeds agreed on trade outcomes to 1.05R and
on regime base rates to 0.5pp. The instrument moved; the market did not. `regime_tag_emitted`
now reports `boundary_exposure` on every run (no second feed needed) and
`feed_sensitivity_checked` so an unchecked grade can never read as a cross-checked one.
**A bucket whose sign flips across feeds cannot source a Tier-3 proposal in either
direction, at any n.** Read each bucket's exposure against its `structural_floor_pct`:
`transitional` is only 5 ADX wide, so its ~100% is a tautology, not an alarm.

⚠️ **The vol label comes from the ADVISORY ML HEAD, never from `vol_detector`.** Under
`REGIME_ML_VERDICT_MODE=use` (live for BTC since 2026-06-28) `intents._decision_vol_regime`
resolves the vol axis from the advisory head's `P(volatile)` keyed **per symbol**. The
frozen-edge `vol_detector` is a *different* label whose own docstring records that the
authored cells "LOSE money under the frozen label" — splitting a harness on it is not a
cheaper approximation, it is a second, opposite mismatch that looks like a fix.

## 3. Exits — including ML exit heads (the 2026-07-30 blind spot)

| Question | Tool |
|---|---|
| Where do exits give back R? (MFE/MAE excursions) | `scripts/research/build_exit_panel.py` |
| **Does this leg have an exit MECHANISM at all, or only a safeguard?** (E0 of the exit-mechanism process) Per `(strategy, symbol)`: every `exit_reason` sorted into four never-collapsed classes — `bracket` (a price level fixed at ENTRY) · `clock` (a timer fixed at entry, kept separate because the literature treats a timer as a different instrument) · `path` (decided by post-entry observation — **the only class that constitutes a mechanism**) · `unclassified` — plus the two practitioner exit-quality metrics that existed nowhere in this repo: **MFE capture rate** (`net_r / mfe_r`; XRP 4163 captured 41.7% of its peak against a 65–80% healthy band) and **MAE-to-stop**. ⚠️ **The vocabulary is DISCOVERED, not assumed** — harnesses disagree on spelling (`stop`/`sl`, `tp`/`take_profit`/`target`), so a leg whose unclassified share exceeds the bound grades `ungradeable` rather than getting a confident verdict over a vocabulary the module does not understand; the regression control asserts 50% `stop` + 50% `quantum_exit` does NOT read as 100% bracket, with a positive control of the same shape that does grade. `mfe_r <= 0` makes capture **UNDEFINED, never 0.0** (folding those in would drag every median toward zero with trades that had nothing to capture); a missing `mae_r` field is a declared coverage gap (`unmeasured_no_mae_field`), never a measured zero. **Descriptive only** — a bad capture rate does not show an improvable exit; that is E2. Live-journal companion measurement: 781 of 1226 closes (63.7%) carry a reason the producer declares it could not classify (`docs/research/e0-exit-census-live-2026-08-20.md`) | `scripts/research/exit_census.py` (`docs/design/exit-mechanism-construction-PROCESS.md` § E0) |
| **Did the STRATEGY's own exit fire, or did something else book the row?** — and, before that, **what IS the real-money population?** Attributes every real-money closed trade to the mechanism that actually closed it, read from RAW `trades.exit_reason` rather than from the published `closeReason` (whose `other` bucket holds FIFTEEN distinct mechanisms and mixes genuine strategy exits — `vwap_cross`, `sl_cross`, `tp_cross`, `exit_head` — with operator flattens, backfills and orphan artifacts, so reading it as 'not a strategy exit' is wrong in BOTH directions). Emits THREE never-collapsed populations because the number moves between them: **LIST** (what `/api/bot/trades/closed` returns) → **DECISION SET** (minus the canonical KPI exclusions in `src/web/api/_clean_trades.py`) → **CONVICTABLE** (strategy-exit ∩ `pnlProvenance == 'measured'` — the only rows that can convict a leg). Buckets are `a_strategy_exit` / `b_not_strategy_exit` / **`unknown_mechanism`** — an unrecognised `exit_reason` lands in the third rather than being folded silently into either real one. Also reports the tail run of consecutive non-wins with an **explicit IID FLOOR** on how ordinary that run is (⚠️ a floor, NOT a p-value — these trades are not IID). ⚠️ **It deliberately does NOT send `include_paper`**: the default already excludes paper, so the flag NARROWS the real-money set (measured: 447 → 15) and a session that passed it reported a 5-day window as the population. Asserts `filter_state == 'applied'` on every Data-Explorer page before trusting `total`, and cross-checks page arithmetic and id uniqueness. Live measurement: `docs/research/real-money-close-attribution-2026-09-08.md` | `scripts/research/real_money_close_attribution.py` (`--self-test` = 20 assertions, offline) |
| **Why has a mechanism that is BUILT and WIRED never once fired?** — the reachability question, asked from LIVE state rather than from reading the code. Establishes the gate chain for the partial-close producer (`_apply_partial_close`), whose only input is a `close_qty_pct` < 1 verdict emitted at exactly one site in `src/`. Answers it as **clause (b): reachable in principle, unreachable as configured** — the sole emitter is routed to **0 of 11 accounts** on the live `/api/bot/config` and additionally pinned `execution: shadow`, so its input population is empty by construction rather than failing a threshold. ⚠️ **Every zero it reports carries a positive control of the SAME SHAPE** and the run REFUSES (exit 2, `ProbeUnsound`) rather than printing a zero whose control came back quiet — and it asserts `filter_state == 'applied'` on every count, because an ignored filter returns the unfiltered `total` and reads identically to a universal match. Also prints the excursion margin against the TP1 rung, **split by `peak_provenance` and never folded**, with the `-1e18` sentinel excluded AND counted rather than silently dropped (a mean over the raw column is fabricated — this script hit exactly that on its first pass). ⚠️ The margin is a labelled FLEET PROXY: the strategy in question has zero telemetry rows of its own. Live measurement: `docs/research/partial-close-producer-never-fired-2026-09-09.md` | `scripts/research/partial_close_reachability.py` (`--self-test` = 14 assertions, offline) |
| **Does this leg carry an EXPECTATION at entry — and could it, under the venue clamp?** Fleet census over `config/strategies.yaml` answering the operator's 2026-08-23 directive (*"brackets ALWAYS represent our prediction of where the trade should end"*). `target_expectation.py` grades ONE trade from a config dict it is handed; this is the population view, and it exists because two numbers that ought to agree do not: counting `tp_r >= 50` **in the YAML** gives **24 of 45** enabled+live legs, while **34 of 45** BEHAVE as sentinels. The gap is 10 `pullback` legs that declare no target key at all and inherit `tp_r = 50.0` from `htf_pullback_trend_2h.py:67` — invisible to a `grep`, and confirmed from the other side by soak rows reporting `target_source_key: tp_r` on legs whose YAML has neither key. ⚠️ **Declared and effective are reported SEPARATELY and never summed**: a leg that writes `tp_r: 50.0` chose the sentinel, a leg that writes nothing inherited it — same runtime behaviour, different remedy, which is exactly the distinction `STATE_NO_TARGET_KEY` exists to preserve (and which is **unreachable in production**, since the monitor passes the MERGED config — `BL-20260823-NO-TARGET-KEY-STATE-UNREACHABLE-IN-PRODUCTION`). Also reports **reachability**: `cap_r = 0.099 * entry / (atr_stop_mult * ATR)`, so the reachable target is **inversely proportional to the stop** — widening a stop LOWERS the target you may place — which is why a target chosen alone gets clamped and why the only e35 cell to survive dispersion was JOINT. A sentinel is never graded reachable (the question is moot); the cap_r column is a **REFERENCE at a stated ATR/entry, not a measurement** — live `cap_r` must come from the soak. **Config-only: no socket, no journal, grades no live trade** | `scripts/research/bracket_expectation_census.py` (`docs/research/bracket-expectation-construction-2026-08-23.md`; `docs/design/exit-mechanism-construction-PROCESS.md` § E3.6)
| **What would an `adx_min` floor actually REFUSE?** Settles a chosen-vs-derived question with a measurement. 12 of 16 enabled+live `pullback` legs declare no `adx_min`, and because `htf_pullback_trend_2h.py:76` defaults it to `None` while the gate at `:297` runs only when declared, **those legs have no ADX filter at entry at all** — so "declare `adx_min` to fix `thesis_unknown`" is an ENTRY-BEHAVIOUR change, not observability (`BL-20260823-TWELVE-PULLBACK-LEGS-HAVE-NO-ENTRY-REGIME-CONDITION`). Measures ADX **at the bars where entries actually occurred** (entries from the harness's own emitted trades, never a reimplementation of the entry logic; a distribution over ALL bars answers a different question) and reports, per candidate floor, the fraction of historical entries it would have refused. Runs the harness with **`--adx-min 0`**, which computes ADX and rejects nothing — `adx_active = adx_min is not None`, so a floor is required to make it compute at all, and `adx_val < 0` never fires — giving the UNFILTERED entry set that is the correct denominator. `_adx` is **imported** from `backtest_pullback`, the verbatim copy of the live strategy's, so the live predicate and the harness cannot drift apart. Five states, never collapsed: `measured` · `insufficient_n` (a refusal rate over 3 trades is not a rate) · `no_entries` (we looked) · **`no_data`** (no candles — *we could not look*, the opposite claim) · `harness_error`. ⚠️ **MEASURED 2026-08-23: the family's own declared values would delete most of the strategy** — `adx_min` 25/28/30 (the four 2h CRYPTO legs' values) refuse a mean of **65%/74%/80%** of historical entries on the six measured 1d equity/bond/metal legs, range 53–86%; the only floor refusing 0% is 10, which sits 0.14 below the lowest observed entry (10.14) and is therefore **fitted to the sample**. Conclusion: do NOT declare a floor — the predicate is wrong, not the value, and these legs need a trend-STRUCTURE thesis. Coverage is partial and stated: 6 measured (n 17–24, 123 entries), 4 `insufficient_n` (the venue serves ~1 month of hourly), 2 `no_data` (IBKR futures) | `scripts/research/adx_entry_distribution.py` (`docs/research/bracket-expectation-construction-2026-08-23.md` § 7.5)
| **Per-bar in-trade** panel — one row per bar while a position is open | `scripts/research/build_intrabar_exit_panel.py` |
| **Does any feature in the panel carry information about the outcome — measured against a control that can actually fire?** (E2 of the exit-mechanism process; **the step that had never been run before 2026-08-20**) Per feature, the out-of-fold **Spearman** association with the chosen outcome on each fold's test rows, aggregated as the **absolute mean of the SIGNED per-fold values** so a sign-flipping feature cancels toward zero instead of being rewarded by a mean-of-absolutes. Uses `_grouped_purged_folds` **imported** from `analyze_exit_head` — and deliberately NOT that module's `_univariate_fdr`, which is called on the pooled row set with no folds, purge, embargo or grouping and is therefore the leaky version of this same test (`BL-20260820-UNIVARIATE-FDR-IS-POOLED-AND-UNPURGED`). ⚠️ **The shuffled-label null is at the `trade_id` BLOCK level, never the row level** — rows inside a trade share an overlapping label window, and a row-level shuffle gives a null tight enough that a trade-structured feature clears it almost automatically; the inflation requires BOTH series to be trade-structured, which a self-test asserts on a path-shaped probe (an i.i.d. probe does not show it, and the module's first rationale was wrong about this until its own test caught it). Two decision rules, both reported: the pre-registered **Westfall–Young max-statistic FWER** threshold, plus a scale-free **min-p** companion, because a raw max-|stat| bar is set by the wide-null path features and is conservative for the narrow-null peer-return ones — a self-test builds that exact regime and requires min-p to rescue a real narrow-null signal max-statistic misses. **Every run injects a positive control (must fire) and a negative control (must stay silent); if either misbehaves the run returns `harness_invalid` and NO negative from it is admissible**, with a planted-failure test proving that gate can fire. Below the declared power floors it returns `unmeasured`, **never** a negative — an underpowered null is not a result. ⚠️ **`--target` matters more than it looks**: `forward_r` is measured FROM ENTRY and shares its baseline with `feat_upnl_r`, so score `advantage_r` (= `forward_r − upnl_r − cost_r`) beside it — measured 2026-08-20, `feat_upnl_r` goes 0.5753 → 0.0062 between them, a factor of 93, and the verdict flips from `informative_features_found` to `no_feature_beats_control` (`docs/research/e2-feature-information-2026-08-20.md`) | `scripts/research/e2_feature_information.py` (`docs/design/exit-mechanism-construction-PROCESS.md` § E2) |
| **Does an E2 verdict depend on the LABEL HORIZON?** Sweep the vertical barrier and hold everything else fixed — measured 2026-08-20, `label_hold` goes `no_feature_beats_control` at 12/24 bars to `informative_features_found` at 48/96 on two legs, so **state the horizon or do not quote the verdict**. ⚠️ Pin `--expected-hold-bars` (else `bars_in_trade_frac`'s denominator tracks the barrier and the FEATURE SET moves too) and set `--embargo-bars` = horizon (`docs/research/e2-horizon-arm-2026-08-20.md`) | `.github/workflows/research-e2-horizon-arm.yml` (dispatch-only; free GH runner + public Binance archive, **not** the trainer VM) |
| **Is E2's shuffled-label null actually VALID on this panel — and does that depend on the panel?** Pushes a **bank of K independent pure-noise columns** through E2's *imported* fold/block/shuffle machinery and reports the empirical rate at which they clear the pointwise bar. Exists because E2 injects **one** negative control, so a run yields one Bernoulli(α) draw that cannot tell 5% bad luck from a broken null — the ambiguity behind `BL-20260820-E2-NEGATIVE-CONTROL-GATED-POINTWISE-NOT-FWER`. A rate is a measurement; one draw is not. Also carries a **length-matched** comparison null with no cycling or truncation to distort, and a block-length profile (`cv_length`, `mobile_fraction`) so the suspected mechanism's severity is a covariate rather than an assertion. ⚠️ Measured 2026-08-20 on SOL h48: clear-rate **0.040** vs α 0.05, mean permutation *p* **0.517** (a valid null gives 0.5) — the SOL null is CALIBRATED, and the four discards were a shared control column, not a panel defect (`docs/research/e2-gate-decision-and-sol-clustering-2026-08-20.md`) | `scripts/research/e2_null_calibration.py` |
| **Is a `label_hold` association EDGE, or a restatement of the triple barrier?** Decomposes a feature's association into the BETWEEN-strata part (how it shifts *which* barrier gets hit — mechanical) and the WITHIN-stratum part, plus the entropy share the barrier alone explains. ⚠️ **The stratified number is a DIAGNOSTIC, never a lever licence** — `touch` is the barrier the trade LATER reached, so conditioning on it conditions on the future. Measured 2026-08-20: the barrier explains **13.9% → 27.0% → 46.6%** of `label_hold` across h=12/24/48 on one leg, and the pooled sign **REVERSES inside every stratum** on exactly the features E2 reported clearing FWER — so E2's long-horizon hits are composition (`docs/research/e3-barrier-geometry-2026-08-20.md`) | `scripts/research/e3_barrier_decomposition.py` (`docs/design/exit-mechanism-construction-PROCESS.md` § E3) |
| **Do decision-time exit levers beat holding, singly and JOINTLY?** (E3) Replays levers over `upnl_r` / `running_mae_r` / `dist_to_stop_atr` on the existing trade set, selecting in-sample on anchored strictly-forward folds and reporting OOS, with the process's added-degrees-of-freedom falsifier stated explicitly. ⚠️ **NOT the M20 gate** — entries are held fixed so the per-exit fee CANCELS; it is cost-NEUTRAL, not net-of-cost, and cannot see the turnover an earlier exit creates. **A negative here is strong** (it failed under flattering assumptions); **a positive is provisional** and must go through `m20_fleet_exit_sweep.py`. ⚠️ **Horizon-invariant by construction** (verified to float equality): the horizon moves only the LABEL, a lever acts on the TRADE. `--cost-r` is an ASYMMETRIC break-even probe (lever arm only), not a fee model. Measured 2026-08-20: the joint grid bought **+0.000 R** over 16.3× the cells on XRP, and the one positive cell breaks even below the repo's own fee constant (`docs/research/e3-joint-lever-screen-2026-08-20.md`) | `scripts/research/e3_joint_lever_sweep.py` |
| **Replay an ML exit head offline** and score it vs the fixed SL/TP exit | `scripts/research/analyze_exit_head.py` |
| **CAN THE M20 EXIT SWEEP DETECT A POSITIVE AT ALL?** (MI-145, the positive control on the instrument itself) Plants a cell whose sign is known BEFORE the run — `net_r' = max(net_r, 0)`, provably dominant on BOTH terms of `beats()` because every trade weakly improves AND every R is ≥ 0 so drawdown is exactly 0 — and grades it with the sweep's OWN `run_cell`/`beats`/`walkforward`, imported unmodified. Two null arms bound it: a wrapper-faithfulness arm and an INERT-lever arm that must move the book by exactly 0.0. ⚠️ **A FLOOR ON THE INSTRUMENT, NEVER A CEILING** — a passing control shows the plumbing is sound, NOT that the gate can resolve a realistic effect size; for that read the corpus's own smallest passing cell. ⚠️ **An empty base book is reported `could_not_test`, never a negative** — `beats()` returns False on two zero-trade books with `reason: tie_no_improvement`, which is byte-identical to a measured negative. MEASURED 2026-09-06 on `trend_donchian` BTCUSDT 1h (n=366): PASS, IS +236.06R / OOS +39.79R, walk-forward 6/6, `inert_wins` 0 (`docs/research/exit-sweep-positive-control-2026-09-06.md`) | `scripts/research/exit_sweep_positive_control.py` |
| **(control instrument, not a question)** The harness wrapper the row above plants its transform through — runs the real backtest harness as a subprocess and re-derives `net_total_r`/`max_drawdown_r` with `_summarize`'s own arithmetic. ⚠️ **NOT A LEVER. Never ship it, never sweep it as a candidate, never enter it in the coverage matrix** — its `loss_free` transform retroactively deletes every losing trade and is physically impossible; that is the point. Invoked by `exit_sweep_positive_control.py` and by nothing else | `scripts/research/_control_oracle_harness.py` |
| **At entry, which bracket is a trade more likely to hit first — and is the take-profit even a decision?** (E3.5) Buckets every trade by `tp_R = cap/(risk/entry)`, the entry-time distance to the venue-capped take-profit, and reports the realised first-passage outcome + net R per bucket. ⚠️ **`trail_stop` is counted as `other`, never as `stop`** — a stop that MOVED is a managed exit, and booking it as `stop` attributes a managed exit to the entry-time bracket. ⚠️⚠️ **THIS AXIS CANNOT SEPARATE "bad bracket geometry" from "loses in high vol", and the tool refuses to try**: `tp_R` and `ATR/close` are the SAME variable up to `cap/atr_stop_mult`, asserted against the data by `collinearity_check` (three-state `confirmed`/`violated`/`unchecked`) rather than argued from the formula — `confirmed` 19/19, worst deviation 2.78e-17. Separating them needs `atr_stop_mult` to VARY, which is `e35_bracket_geometry_sweep.py`. Measured 2026-08-20 (6,428 trades, 19 legs, net of fees): **16 of 19 legs declare `tp_r: 50.0`** so the placed TP is the exchange's 9.9% rejection threshold; target distance varies **6.5×–38.9× WITHIN every leg** (medians cross-checked **18/18 EXACT** against the harnesses' own `tp_r_effective_median`); P(TP first) is monotone in `tp_R` while **P(stop) is flat**; and **76.2% of fleet net R comes from the 23.1% of trades whose target is >5R away** — trades the bracket cannot close (`docs/research/e35-bracket-is-not-a-decision-2026-08-20.md`) | `scripts/research/e35_barrier_race.py` (`docs/design/exit-mechanism-construction-PROCESS.md` §§ E3.5-E3.6) |
| **Is the BRACKET GEOMETRY itself worth anything — `tp_r` × `atr_stop_mult` × `timeout_bars`?** (E3.5) The one exit parameter set never swept (`BL-20260820-TP-LEVEL-IS-THE-ONE-EXIT-PARAMETER-NEVER-SWEPT`), run as a new DIMENSION through the existing m20 machinery — `base_args`, `resolve_split`, `beats` (Path A), `is_path_b_candidate`, `walkforward` are all IMPORTED from `m20_fleet_exit_sweep`, never restated, so a verdict here is the verdict that module would give. **Read `net_r_spread` BEFORE any argmax**: it is what the whole bracket dimension is worth on that leg, and an argmax over a flat surface is noise. ⚠️ **NET OF FEES IS LOAD-BEARING HERE IN A WAY IT WAS NOT FOR LEVER REPLAYS** — a lower target changes the trade population and raises turnover, so a fee-free basis flatters exactly the cells most likely to pass. ⚠️ **A stop-axis `net_R` is unreadable without `leverage_multiple`**: moving `atr_stop_mult` moves R itself, and since `position_size` sizes by RISK a tighter stop buys its R with MORE notional per unit risk budget. That is REPORTED, never gated (a ceiling with no measured distribution is what `gross-exposure-governance-DESIGN.md` §§ 6-7 refuses) — an earlier version REFUSED stop cells on a net_R-vs-notional sign disagreement and was **backwards**, refusing the cells that were better in R *and* cheaper in leverage | `scripts/research/e35_bracket_geometry_sweep.py` |
| **Does a DECLARED bracket target actually exit the trade, or does the 9.9% venue clamp get there first?** (E3.6) Reads the existing `e35-bracket-corpus.jsonl` and attaches TWO axes to every joint (`tp_r` + `stop_mult`) cell — kept apart because they are different claims with different evidence. **TRUNCATION** is DERIVED: `tp_r` vs a `cap_r` rescaled from the § 6.5 measured per-leg median via `bracket_expectation_census.cap_r_for` (one definition of the inverse-proportionality, never restated). Because that `cap_r` is a MEDIAN over the leg's trades and ATR varies per trade, `truncated` means *clamped on more than half the trades*, NEVER *never reached*. **COSMETIC** is OBSERVED and needs no such qualifier: byte-identical net_R **and** max_dd to the same stop with no target declared — the declaration provably changed nothing. ⚠️ The one-way implication (every cosmetic cell is also truncated) is ASSERTED against the data, not assumed, and a violation is a refusal about the cap basis rather than a footnote — it holds 40/40 on cells with a cap basis, with 9 further cells **unverifiable** (no measured `cap_r` for their leg) and counted as such, never as holding. ⚠️ Reporting *N cells pass the gate* is worthless without a **positive control** that the verdict occurs anywhere: a Path-A filter on this corpus returned 0 across all four legs, and Path A passes **0 of 2204 rows corpus-wide** — the tool refuses to print a zero-pass result it could not have matched. Measured 2026-08-24: **49 of 308** gradeable cells are cosmetic (**25.0%** on the four clamp-dominated 4h legs); **200 reachable cells across those legs, 3 ever graded**; ⚠️ **CORRECTED 2026-08-24 (same day): the claim that the live `atr_stop_mult: 2.5` is "absent from the joint grid entirely" was FALSE, and it was this tool's own filter that made it look true.** `e35_bracket_geometry_sweep` builds its stop axis as `(None,) + STOP_MULT_GRID`, where `None` means *run at the harness base* — the live 2.5 — and `cell_tag` omits the `sm` token for those cells (tags like `tp1.5_to24`). `classify` then did `if stop_mult is None: continue`, dropping **390 of 2204** corpus rows: every cell at the live stop with a varied target. An empty result read as a clean negative is sub-class **C**, and it ran in the dangerous direction — the cosmetic measurement was computed over a population that excluded the stop the fleet actually trades. Fixed: `None` now RESOLVES to `BASE_STOP_MULT`, carrying `stop_basis` (`explicit` / `base_implicit`) so a swept 2.5 could never be conflated with an implicit one. **Population 1540 -> 1928 cells.** The two axes move differently and are reported separately: on TRUNCATION the 388 newly-visible cells split 167 reachable / 153 truncated / 68 no-cap-basis, so **153 truncated cells at the live stop were previously invisible**; on COSMETIC they are **all `no_baseline`, so the 49/308 headline is UNCHANGED** — stated plainly rather than left to imply the fix moved it. That second blindness is STRUCTURAL, not a filter bug: a cosmetic verdict needs a same-leg no-target book at the same stop, and the corpus holds **zero** rows with `tp/sm/timeout` all None, because that grid point is a provable no-op the sweep records (`inert_equals_base`) and never runs. ~~Unlocking it costs ~one run per leg, NOT a grid re-sweep.~~ ⚠️ **CORRECTED again 2026-08-24: it cost ZERO runs — the book was already in the corpus.** `e35_bracket_geometry_sweep.surface` runs the config-exact base on EVERY sweep (`fleet.run_cell(harness, base)`), which IS the no-target book at the live stop; it lands in the leg's `base` block rather than `cells[]`, and `e35_corpus_extract` iterates `cells[]` only — while copying it onto every row as `base_net_total_r`/`base_max_drawdown_r` (**2204/2204 rows carry both**, one distinct base pair per leg). A missing ROW was read as a missing RUN — *measured-and-never-emitted*, the written-and-never-read class one level up. `classify` now falls back to it via a third rung, and `baseline_basis` (`row` / `base_block` / `absent`) says which book answered so they are never quoted as one number. ⚠️ **The unlock is 80 cells, not 388** — 308 carry a TIMEOUT OVERRIDE and stay `no_baseline` deliberately (identity to a differently-timed book is not a statement about the TARGET; pinned by a test). Result, strata apart: `row` 308 cells / **49 cosmetic**; `base_block` 80 / **10 cosmetic**. **The 49/308 headline is UNCHANGED** — 10/80 is a NEW stratum, not a movement. `no_baseline` 1620 -> 1540. The implication holds on all 10, zero violations. **Corpus-only: no harness run, no socket, no order path** | `scripts/research/bracket_reachability_audit.py` (`docs/research/bracket-target-reachability-2026-08-24.md`) |
| **Is a leg's declared `tp_r` reachable under the venue ceiling, at DECLARE time rather than by audit months later?** The warn-only half of the reachability work (operator decision 2026-08-24: *warn, do not refuse*). ⚠️ It **cannot compute `cap_r` from YAML** — `cap_r = TP_VENUE_CAP_PCT * entry / (atr_stop_mult * ATR)` needs ATR/entry, a MARKET quantity absent at config-load time — so it compares against caps that were actually MEASURED and, where none exists, reports the ATR/entry a leg would NEED as a **condition, never a verdict**. Five states, never collapsed: `above_cap_measured` (measured for THIS leg) · `above_cap_sibling` (a `_prop` variant inherits its sibling's ceiling — cap_r depends on entry/ATR/stop, none account-specific, so the inference is sound but is LABELLED, not merged into measured) · `within_cap` · `sentinel` (`tp_r >= 50` is not a target) · `no_cap_basis` (**we did not look** — never "fine"). Sibling matching never crosses timeframes (`trend_donchian_sol` 3.22 vs `_sol_4h` 1.44 — a fuzzy match would hand one leg the other's ceiling). Its MECHANISM is gated (self-test failure or unreadable config exits 1); its FINDINGS never fail CI. Measured 2026-08-24: of 55 legs, **30 declare the sentinel and only 4 declare a real target — 2 of those 4 are already cosmetic** (`trend_donchian_sol_prop` 6.0 vs 3.22, `trend_donchian_eth_prop` 6.0 vs 4.08). ⚠️ Both are PROP legs and the clamp is nonetheless real: it is applied in the strategy signal builder (`trend_donchian.py:388`) **before the account is known**, so a cap named for Bybit's ErrCode 10001 also binds tickets placed elsewhere. **Config-read only: no harness run, no socket, no order path** | `scripts/research/target_reachability_report.py` |
| **Which pullback legs can a `pullback_frac` cross-leg test actually COVER, and over how much history?** The criterion-first half of the operator's 2026-08-24 cross-leg decision — *the criterion goes first* (donchian § 6.0b: a shortlist chosen before its criterion is a shortlist chosen by the argmax). `pullback_frac` is declared by **19 enabled legs at TWO values** (0.5 x11, 0.618 x8), so the question is whether either GENERALISES or each is fit to its own leg — a cross-leg claim, whose first invalidator is a population that quietly excludes legs. **12 of the 19 had NO free candle lane at all until 2026-08-24.** ⚠️ **SPAN IS PART OF THE POPULATION.** yfinance serves `1d` uncapped but REFUSES a >730 d `1h` request outright (measured, proof run 32734360738), so blending a decade-long daily leg with a 2-year hourly one into one *"N legs agree"* claim is an unstated-denominator error. Four states, never collapsed: `crypto_archive` (full) · `yfinance/full` (1d) · `yfinance/capped` (1h, 730 d — INCLUDED but stratified, never blended) · `NOT_SERVED` (**we cannot test this leg**, which is not *"it disagreed"*). An unknown timeframe is never reported as uncapped. Measured 2026-08-24: **15 full-history legs (9 x 0.5, 6 x 0.618), 4 capped (2/2), 0 unserved** — eight of the 15 are runnable ONLY because of that day's candle-feed work. ~~⚠️ **The sweep itself does not exist yet**: `m21_entry_sweep` is donchian entry-filters, not this.~~ **BUILT 2026-08-24** — `pullback_frac_cross_leg_sweep.py`, next row. **Config-read only: no harness run, no socket, no order path** | `scripts/research/pullback_frac_cross_leg_scope.py` |
| **Does ONE `pullback_frac` GENERALISE across the fleet, or is each value fit to its own leg?** The sweep half of the operator's 2026-08-24 cross-leg decision, at FULL approved scope: all 19 enabled legs. Population comes from the scope tool and every resolver (`classify`, `resolve_data`, `base_args`, `run_cell`, `FAMILY_HARNESS`) is IMPORTED from `m20_fleet_exit_sweep`, so this cannot disagree with the fleet sweep about what a leg's base is — verified that `base_args` already threads the leg's CONFIGURED `--pullback-frac`, so a base is config-exact on this axis. **Not a new engine**: `scripts/backtest_pullback.py` already accepts the flag. Grid `{0.33, 0.5, 0.618, 0.75}` — every point has a reason (0.33 is the `htf_pullback_trend_2h` unit's own default; 0.5 and 0.618 are the live config values; 0.75 is a SHAPE probe, present because two points cannot distinguish *"0.618 is better"* from *"the surface is flat and noise picked one"*). ⚠️ **THE CRITERION IS A CONSTANT IN THE FILE, FIXED BEFORE ANY RESULT EXISTS** (`VERDICT_RULE`, `MIN_LEGS_FOR_A_CROSS_LEG_CLAIM=5`): a value GENERALISES only if it is argmax on **>50%** of its stratum (exactly 50% is SPLIT, not a win), and a stratum below the floor is **UNDERPOWERED** and gets no verdict at all. ⚠️ **THE TWO STRATA ARE NEVER BLENDED** — 15 full-history vs 4 capped-at-730d legs do not share a denominator; `summarise` emits no all-legs roll-up and there is no flag to make one. ⚠️ **Read the SPREAD before any argmax** (`flat` per leg, median/max per stratum): a flat surface means the axis has little to give and the argmax is noise. ⚠️ **NET OF FEES IS LOAD-BEARING** — `pullback_frac` is an ENTRY gate, so moving it changes the trade POPULATION and turnover and the per-exit fee does NOT cancel between arms. A grid point equal to the leg's own config value is `inert_equals_base` — recorded, never run. An ungradeable leg (<2 graded points, or a harness error) is recorded as UNGRADED and **never counted as a disagreement**. ⚠️ Its shard planner is **CONFIG-gated, not data-gated**, unlike `e35_shard_plan.py`, which cannot plan on a fresh CI checkout (leg CSVs are gitignored, so it returns `0 job(s) … data_missing=43` and exits 1 — measured 2026-08-24; `e35-bracket-sweep.yml` has never run). 39 self-test checks. **Observe-only, Tier-1: no config write, no socket, no order path** — changing `pullback_frac` is Tier-3 | `scripts/research/pullback_frac_cross_leg_sweep.py` (`.github/workflows/pullback-frac-cross-leg-sweep.yml`) |
| **What does the prop EV/survival gate say at a `tp_r` the config does not declare?** The measurement half of the operator's 2026-08-24 *"measure first, then decide"* on the Tier-3 `_TP_SENTINEL_CAP_PCT` venue-scope question. ⚠️ **It exists because the question was previously UNASKABLE without making the change being evaluated**: `account_compat_matrix.run()` passed `overrides={}` hardcoded, so the engine path could only score a strategy at its `config/strategies.yaml` values — measuring a candidate `tp_r` required editing a Tier-3 file first. ⚠️ **The proposal's option C is NOT documentation-only, and that was VERIFIED not assumed**: the effective target is `min(cap_r, tp_r)` with `cap_r = 0.099*entry/risk` — a percent-of-entry against a multiple-of-risk — so **no `tp_r` reproduces the clamp**, and lowering it to 3.22/4.08 tightens the real target on **half the trades by the definition of the median** (3.22 and 4.08 ARE the medians), on legs whose live promotion rests on an EV gate measured at 6.0, on an account where a breach is TERMINAL. ⚠️ **The 6.0 arm in the SAME run is the control — never trainer relay #8975's +$483/+$883**, which came from a different data vintage and window; comparing across them compares two populations. The grid `{6.0,5.0,4.0,3.0,2.5}` and the three-part accept criterion are fixed in the workflow header BEFORE any result (criterion-first). ⚠️ **A 5m base is required, not 1h** — `run_system_backtest` resamples `base5m` to both the strategy frame and the clock, so 1h would make both no-ops and silently drop the intra-bar exit resolution a `tp_r` change moves. A candle file under 100k rows is REFUSED, and each arm asserts its override reached the payload (a result labelled with an arm it did not run under is the class this exists to avoid). **Observe-only, Tier-1: no config write, no order path** — the flip itself is Tier-3 and the operator's | `scripts/prop/account_compat_matrix.py --override` (`.github/workflows/prop-tp-r-gate.yml`) |
| **Shard the bracket sweep ONE LEG PER CI JOB** so a multi-hour serial pass stops being killable by a container recycle. Emits the GitHub Actions matrix that `.github/workflows/e35-bracket-sweep.yml` consumes, resolving legs through the sweep's OWN `plan_legs` so the planner and the sweep cannot disagree about which legs exist. ⚠️ **An empty matrix EXITS NON-ZERO and writes nothing** — GitHub reports a matrix that expands to zero jobs as SUCCESS, so a green run that tested nothing would be indistinguishable from one where every leg passed. ⚠️ **An unmapped timeframe RAISES rather than defaulting**: a wrong interval code does not error anywhere downstream, it silently fetches the wrong bars and every verdict then measures a population nobody asked for (the hazard `m20-exit-lever-sweep.yml` already names for its own data source, and which `research-panel-build.yml`'s `*) BYBIT_INT="${rp_timeframe}"` fallback still has). Refused legs are reported with a named reason and counted separately from legs the sweep never offered — two different problems, two different fixes | `scripts/research/e35_shard_plan.py` |
| Is an ML-supplemented exit even feasible for a family? | `scripts/research/m20_ml_exit_probe.py` |
| A/B the declared exit levers over full history | `scripts/research/m20_exit_sweep.py` · fleet-wide: `m20_fleet_exit_sweep.py` |
| Trainer-side exit analysis | `scripts/research/m20_exit_analysis.py` |
| **What can end a WINNING trade on this leg, and what arms it** (the two gates: the unit implements the call site AND the YAML declares the key) | `scripts/research/m20_u1_winner_close_inventory.py` |
| **Has the market's favourable:adverse travel changed?** (MFE/MAE in ATR units over fixed hours-from-entry windows — provenance-free, geometry-invariant, and immune to the break-even-ratchet denominator defect; `--validate` refuses a clean bill unless the known 2026-08-30 move is reproduced) | `scripts/research/m20_u5_excursion_regime.py` |
| **Is a winner's R inflated by the break-even ratchet?** (identify the ratcheted rows, recover the ENTRY risk from the frozen tp, re-measure — with a control that fails the run) | `scripts/research/m20_u2b_ratchet_risk_basis.py` |
| **Which mechanism actually ENDED each winning trade** (provenance-graded, with the `reconciler_filled` label recovery and its sensitivity curve) | `scripts/research/m20_u2_winner_close_attribution.py` |
| Would a **regime-flip** exit have helped? | `scripts/research/m20_regime_flip_replay.py` · fleet: `m20_flip_replay_sweep.py` |
| **Is there ROOM for a partial-TP ladder at all, before anyone builds a ladder producer?** Measures CAP UTILISATION — what share of trades actually reach the live 9.9%-of-entry take-profit ceiling — and how often candidate rungs are reached below it. Exists because the classic bank-then-run thesis needs somewhere to run, and **27 of the 40 capped live legs declare `tp_r: 50.0`**, a sentinel the venue cap always overrides, so the remainder's ceiling is fixed whether a rung banked or not. `cap_r` is computed PER TRADE (`0.099*entry/risk`) because the cap is a share of price while R is a share of risk — a leg-level 'cap in R' would be a fiction. MFE is read through `exit_capture.mfe_r_of` (the scalp harnesses nest `mfe_r` under `meta`; a re-derived read reported 0-of-1102). Three verdicts, never collapsed — `cap_binds` (the CEILING is the constraint, so the real question is fixed-TP vs a trailing final) / `no_room` / `ladder_opportunity` — plus `insufficient_trades`, which reports NO share rather than one over a thin denominator. Answers a question a producer CANNOT: a producer run first would emit ~46 cells reading 'the ladder did not work' when they mean 'there was never room' | `scripts/research/m20_ladder_headroom.py` → `runtime_logs/m20_ladder_headroom/<date>/headroom.json` |
| **Is a peak worth BANKING, or does the trade run from here? Base rate, conditioned on cap-proximity.** Answers the question every giveback/trailing lever presupposes and none of them measures: given a trade has reached X% of its venue TP cap, what fraction go on to finish HIGHER than that peak? A lever that banks at 75% of cap is only right if trades reaching 75% mostly stall there. Thresholds are swept (0.25 -> 0.90) rather than fixed, because a single cut-point cannot distinguish 'the banking thesis is wrong' from 'this particular threshold is wrong'. Verdicts are only `refuted` / `candidate` -- deliberately NO `pass`: a favourable base rate is a reason to sweep a lever, never evidence the lever works, and a verdict named `pass` would be quoted as though it were. MEASURED 2026-08-18: pooled REFUTED at all 8 thresholds (n=891); BTC is the lone candidate at all 8, retaining 0.278-0.632 of its peaks where every other leg retains ~1.0, i.e. BTC round-trips its peaks and the others do not | `scripts/research/peak_banking_basis.py` |
| **Is a lever's verdict STABLE, or an artifact of where the IS/OOS boundary fell?** Partitions one `--emit-trades` run at a band of split targets and reports the verdict distribution + a `split_sensitive` flag. Exists because the fleet sweep emits ONE binary verdict from ONE split with no dispersion measure, so downstream a PASS and a coin-flip are indistinguishable — measured 2026-08-19 on `sol_pullback_2h`, holding corpus and commit fixed and moving only `--split-target-oos` 50->35 swung dOOS **5.14x** (+3.7428 -> +0.7277) and flipped the pre-registered rule PASS->FAIL, with `gb1R_afterMFE1R` flipping the same way. ONE harness run, not N: the walk-forward folds are split-INVARIANT (calendar-year based, byte-identical across both runs), so only the IS/OOS gate moves. The gate is IMPORTED from `m20_fleet_exit_sweep.beats`, never copied. ⚠️ The metrics ARE re-derived here — unavoidable, since partitioning at a new boundary aggregates a subset the harness never did — so `verify_against_harness` reproduces the full population against the harness's own figures and `analyse` **REFUSES to report** on a mismatch rather than publishing a band built on an unvalidated aggregation | `scripts/research/m20_split_dispersion.py` |
| Drive one (family, tf) exit-head round end-to-end | `scripts/research/m20_exit_head_round.py` |
| **Where ARE all the fold-dispersion arms, and how do I get them into one file?** Walks the trainer's `runtime_logs/m20_exit_head/` tree and emits one consolidated record. Exists because the study's headline was **22% machine-readable** — 6 legs committed against a screened denominator of 27, the rest prose-only. Stamps the `fold_offset` the rows cannot state for themselves (`BL-20260815-EVIDENCE-ROWS-DO-NOT-RECORD-FOLD-OFFSET`) from each arm's `round_report.json`, keeping `offset_source` and a separate `dir_offset` so the two can be cross-checked instead of one standing in for the other; an unreadable report yields a **null offset, never a defaulted 0**. Refuses to write an empty record (an empty file that exists reads as a measured negative). RUNS ON THE TRAINER; transfer the output **compressed and hash-checked** — a plain-text emit was silently truncated at 137 of 234 rows | `scripts/research/m20_consolidate_dispersion_arms.py` → `docs/research/m20-fold-dispersion-arms-consolidated.jsonl` |
| **How much does an E1 verdict depend on WHERE the fold boundaries fall — and what is the mover rate?** Derives the fold-dispersion headline from the consolidated arms record instead of prose (the doc's `per_leg` denominator had already gone stale at 2 where the data says 3, and a power calculation was built on it). Prints **both** dedup rules because they differ by 7.4 points — any-screen 9/27 = 33.3% vs every-screen 7/27 = 25.9% — and naming only one silently picks a side; states the denominator including the 14 single-arm pairs EXCLUDED (a leg measured at one offset cannot move); and names the legs whose **mover verdict itself** disagrees across screens (2 of 22), so the rate is read as one draw of a statistic that has its own dispersion | `scripts/research/m20_dispersion_rate.py` → reads `docs/research/m20-fold-dispersion-arms-consolidated.jsonl`; pinned against the doc by `tests/test_dispersion_rate_matches_the_doc.py` |
| **A committed corpus is missing a field the producer now stamps — how do I backfill it WITHOUT inventing values?** Adds `fold_offset` + `fold_offset_basis` to the 33 rows of `m20-exit-head-rounds.jsonl` written before the driver stamped it. The offset is **ESTABLISHED, not defaulted**: `--fold-offset` landed in `43820a32` @ 2026-08-14T23:49:18Z and every row predates it, so each was produced by a driver whose argparse would have *rejected* the flag — the unshifted partition is therefore derived. 28 rows dated from the round-dir timestamp; the 5 naming only a relay were dated by **reading the GitHub API**, not inferred from issue numbering. A row that cannot be dated, or one dated *after* the flag, records `null` + `unavailable` — never 0, because "we did not record it" and "it was the control arm" are different facts and collapsing them is the defect that opened the row. Idempotent (a row already carrying the key is untouched, so a measured value can never be overwritten by an inferred one) and refuses to rewrite an empty file. Read it as the worked example for the general shape: **backfill only what the code's own history proves, and give the inference its own provenance field** | `scripts/research/m20_backfill_corpus_fold_offset.py` → rewrites `docs/research/m20-exit-head-rounds.jsonl` (`--write`; dry-run by default) |
| **The corpus contradicts a matrix cell's status — how do I acknowledge it without overstating the contradiction?** Drafts the `ref` segment `matrix-corpus-agreement` demands, for every unacknowledged disagreement. Exists because the first two were hand-written at n=2 and the next sweep produced ten more within the hour; a hand-written ref drops the CAVEAT, which is the part you have to go looking for. **Imports the guard and uses ITS `find_disagreements`** rather than a second predicate free to drift from the one CI fails on. Computes three caveats per row and omits each when unwarranted (boilerplate teaches readers to skip caveats): **inert walk-forward folds** — a fold where the lever never fired still counts `ok`, so `slv_pullback_1d`'s 5/6 is 2 real wins against 1 real loss with 3 inert; **Path B is not Path A** — reported with `is_oos_pass` + the OOS gate reason, since +0.001 net_R is arithmetically nonzero and economically nothing; and **the per-leg derived split**, so a fresh row and the evidence it contradicts are different partitions, not a rerun. Writes `ref` prose ONLY — never `status`, never `tp_geometry`, never a shipping decision (that call is Tier-3 and the operator's). Dry-run by default, asserted on bytes | `scripts/research/m20_ack_corpus_disagreements.py` → appends to `docs/research/exit-refinement-coverage.json`; pinned by `tests/test_m20_ack_drafter.py` |
| **What IS the M20 coverage number, and how many cells still block the milestone?** The one place the headline is derived — three sessions hand-counted three different figures (319/311/304) for an unchanged file. Reports all three cuts and names each; `--done-condition` lists the blocking cells (**61**, not 57 — the headline counts `blocked` as closed and the done-condition does not); `--check` is the CI guard. **Also answers "is an `exit_head_ml` cell blocked by ARITHMETIC or waiting on data?"** (added 2026-08-15): reads each blocked cell's `lifetime_trades` field and derives `u` by mirroring `train_exit_head.fold_blocks`'s own `range(b, N-b+1, b)` — so the u≥2 bound (N≥150) and the one-fold bound (N≥100) are computed, not quoted. Measured 11 of 11 cannot form a SINGLE fold. Exists because those counts lived only in ref PROSE, which let two incompatible arithmetics sit in one lever column: three futures cells reasoned from the LEVER sweep's `MIN_OOS_TRADES` + date split and named an "earlier split" route this lever has no date split to take (`BL-20260815-EXIT-HEAD-MATRIX-REFS-USE-THE-LEVER-SWEEPS-ARITHMETIC`). A cell missing the field is reported **ungraded**, never skipped | `scripts/research/m20_coverage_rollup.py` |
| **Can a closed trade's exit price be RECONSTRUCTED when the broker fill was never recovered — and how wrong is it?** Hides the known fill on measured rows, rebuilds the exit from 1m klines with the harness's SL-first rule, and reports error in bps against broker truth + per-venue candle coverage | `scripts/research/exit_reconstruction_validator.py` · v2 (decision-time bracket, BE replay, time-consistency stratification): `scripts/research/exit_reconstruction_validator_v2.py` |
| Runner (off-VM) | `.github/workflows/research-exit-head-build.yml` — label `research-exit-head-request` |

**`analyze_exit_head.py` is the answer to "can an ML exit head be backtested?" — yes.** It
trains the take/skip head *and simulates its decisions per trade* (first bar the head says
exit, the trade realizes its mark-to-market R there, net of an exit fee) against the
baseline fixed exit, under CV **grouped by `trade_id`, purged, embargoed, and
uniqueness-weighted**. That is stricter than the plain harnesses in §1.
| **What DENOMINATOR is behind a coverage-matrix verdict — and does the cell state one at all?** Promotes the M20 matrix's base trade counts from free-text `ref` PROSE into structured `base_is`/`base_oos` fields, and flags every RESOLVED verdict whose ref states no count with `denominator_unstated`. Measured 2026-08-27: **110** cells carry a stated count; **271 of 380** resolved verdicts (**71.3%**) state none — the matrix's headline is computed over cells most of which carry no denominator anywhere a reader or a tool can find. ⚠️ **It re-grades NOTHING and touches no `status`.** Base size does not separate the verdicts fleet-wide (`honest_negative` median OOS **33**, `shipped` **33**, `passed_unshipped` **36**), so the population is uniformly thin and a per-cell `underpowered` status would flip almost the whole matrix — including the shipped cells — and mean nothing; the power caveat is recorded ONCE at artifact level in `known_caveats._headline`. ⚠️ **`base_oos` is an UPPER BOUND on the denominator that matters** — the trades the LEVER MODIFIED are unrecorded (`exit-refinement/SKILL.md`: *"per-cell fire counts are not recorded; that gap is open"*) — so a comfortable base is not a powered cell. ⚠️ **Extracts only fixed unambiguous FORMS, never an interpretation**: a regex for "exactly zero folds" matched **60** cells and reading them showed every mention was a comparison to a *different* cell, a *superseded* verdict, or a note that inert folds are *already excluded* — auto-grading would have mis-flipped 60. Idempotent; `--check` is registered in `run_guards.py::artifact-caveat-guard` so the extraction cannot rot back to prose | `scripts/research/m20_coverage_base_counts.py` (`docs/research/RESEARCH-INFRA-AUDIT-2026-08-27.md` § 2) |
| **Are our take-profits PREDICTIONS about where the trade ends, or a venue artefact?** E3.6's own falsifier, which `exit-lever-wiring-audit-2026-09-06.md` § Q1 measured as having **no instrument, no artifact and no cell**. Two views with OPPOSITE flaws, never pooled: `--exits` grades where trades actually ended against the target they declared (large n, but **censored by the current geometry** — those trades exited at a trail, so a realised exit is a floor on how far the move went; good for FALSIFYING a target, bad for SETTING one), and `--mfe` grades how far each trade ever GOT from `position_telemetry.peak_r` (the right basis for setting one, but small n and `peak_r_is_lower_bound: True` on every row). **Percent-of-entry, never R** — the R denominator is measurably contaminated (`trades.stop_loss` is the FINAL trailed stop; MI-144 measured 104 of 1287 rows carrying 96.6% of `totalR`), and the venue clamp is itself a percent of entry, so this is the basis on which prediction-vs-artefact is directly decidable. Four grades never collapsed — a missing INPUT (`unreadable`) is not a missing DECLARATION (`no_target_declared`), and an empty population yields `None`, never a rate of `0.0` | `scripts/research/bracket_calibration_report.py` + `src/runtime/bracket_calibration.py` (MI-148; `BL-20260901-E35-TP-R-CAN-BE-INERT-BEHIND-THE-VENUE-CLAMP-AND-THE-BINDING-RATE-IS-UNMEASURED`) → `docs/research/bracket-calibration-2026-09-06.md` |
| **Is a backtest MFE quantile a statement about the FLEET, or only about the harness's book?** MI-151 § 5 makes every per-leg target conditional on this and could not clear it, having measured the two populations disagreeing by **~2.5×** on the exact quantity a target would be set from (backtest BTC p90 **3.87%** at n=3,194 vs MI-148's live **9.70%** at n=63). Grades each enabled+live leg into **three states that are never collapsed** — `fidelity_ok` / `fidelity_failed` / **`insufficient_n` (*we could not look*)** — and states **BOTH n's on every row**, including abstaining ones, because a ratio quoted without both denominators is exactly what hid the 3,194-vs-63. **Result 2026-09-07: 44 of 44 enabled+live legs `insufficient_n`** (max per-leg live n = **8** against an abstain floor of **30**; `n_backtest = 0` on all 44). ⚠️ **REFUSES to join across TIMEFRAME** — the only backtest corpus is `trend_donchian` at **15m** while every live donchian leg runs **1h or 4h**, and measured inside the live crypto book alone p90 goes **2.16% (1h) → 7.07% (2h) → 9.77% (4h)**, a 4.5× span on horizon with no harness involved. So the ~2.5× is a **horizon-composition artifact**, not a fidelity failure — which removes it as evidence of infidelity without establishing fidelity. Inherits `m31_mfe_parity`'s three refusals and **imports** `quantile` from `bracket_calibration` so two definitions of p90 cannot drift; it is that parity moved off **R** onto **percent-of-entry**, not a second opinion. Settles the MI-148/MI-151 basis question in writing: **measure in percent-of-entry, express the knob as `tp_r`** — with the stated defect that *no `tp_r` reproduces the clamp*, so the config schema cannot express the quantity that was measured | `scripts/research/exit_location_fidelity.py` (MI-155; `--selftest` asserts a thin-live/huge-backtest leg ABSTAINS rather than inheriting the backtest's confidence, before any verdict is trusted) → `docs/research/exit-location-fidelity-2026-09-07.md` |
| **How far do a leg's trades ACTUALLY go, at an n a target could be set from — and what would a candidate `tp_r` actually produce?** MI-148's live `--mfe` view is the right basis and is starved (n=1–8, `estimated`, a declared LOWER BOUND on every row); this asks the same question of the OFFLINE harness, where n is 112–1,004 per leg and the R denominator is clean because the emit carries the ENTRY stop rather than `trades.stop_loss`'s final trailed one. **Two arms, never pooled**: `uncapped` (no take-profit path — the target-SETTING basis) and `capped` (`--tp-cap-pct 0.099` — the live-comparable one, truncated at each trade's own `cap_r` by construction). ⚠️ **Reports `P(mfe_r >= min(cap_r, x))`, never `P(mfe_r >= x)`** — a declared `tp_r` does not set the target (`tp_venue_cap.py`: *no `tp_r` reproduces the clamp*), so the naive form silently assumes the declared value reaches the wire. An exact COUNT, not a third percentile convention. ⚠️ **All trades, NOT winners-only** — deliberately not a duplicate of `m20_fleet_exit_sweep.winner_mfe_p80`, which restricts to `net_r > 0` because it calibrates a TRAIL arm; a take-profit is hit by any trade whose excursion reaches it, including every trade that went +2R then reversed into a stop, and those are exactly the trades a target would change. **Validated by a positive control it could have failed**: predicted effective reach vs the capped run's OBSERVED `take_profit` share agrees within **3.2 pp on 19 of 19 legs** (mean −0.3 pp), across two arms with different trade counts. **Result 2026-09-18: no per-leg `tp_r` is proposable and that is the finding** — the clamp already sets a per-leg target spanning **1.74R–7.20R (4.1×)** that fires on **5.1%–44.2%** of trades, above it a declared `tp_r` is inert (13 of 19 legs differ <1 pp between 4R and 10R), and all 6 legs declaring a real `tp_r` have it overridden on **70–97%** of trades. Four never-collapsed states — `ok` / `no_data` / **`not_capped_capable`** (harness lacks `--tp-cap-pct`, read from its SOURCE, so the 7 `ict_scalp_*` legs + `fvg_range_15m` are never miscounted as a FEED limit) / `harness_failed`. ⚠️ **`not_capped_capable` NO LONGER FIRES FOR THOSE 8 LEGS AND THE DIAGNOSIS BEHIND IT WAS HALF WRONG — see MI-312 below** (2026-09-18): those legs' live units apply no venue clamp at all, so a capped arm would be a COUNTERFACTUAL and the harness DEFAULT was already their live parity; the arm genuinely missing was the target-SETTING one. Both harnesses now carry `--tp-cap-pct` **and** `--no-tp`, and this script's `run_leg` gates the capped arm on the FAMILY (new state `capped_arm_not_applicable`) so it cannot label a scalp counterfactual live-comparable. Also extends MI-155's backtest reference **3 → 18 legs**, moving 13 legs off `no_backtest_corpus_for_leg` | `scripts/research/mi307_offline_mfe.py` (MI-307; `--selftest` carries 14 checks of which 4 are negative controls, incl. that a tight clamp makes effective reach EXCEED the naive one) → `docs/research/offline-mfe-distribution-2026-09-18.md` |
| **How far do the `ict_scalp` / `fvg_range` legs ACTUALLY go — the family whose target is the only exit lever with attributed mass (MI-278 U2) — and does their live target predict its own hit rate?** The 8 legs MI-307 could not grade. ⚠️ **It could not grade them for the OPPOSITE reason to the one recorded**: their live units apply NO venue clamp (`TP_VENUE_CAP_PCT` is applied in exactly four unit modules and nowhere downstream; `tp_venue_cap.CLAMPING_FAMILIES` excludes `scalp` and `fvg`), so the harness DEFAULT — which exits at the live unit's own `tp` — was already the live-comparable book, which is the state `m20_fleet_exit_sweep.tp_geometry_for` calls `live_parity_uncapped`. What was missing was the **target-SETTING** arm: no way to switch the target off, so MFE stayed truncated at it. **Two arms**: `live` (the default) and `no_tp` (`--no-tp`). ⚠️ **Adding `--tp-cap-pct` alone would have manufactured a passing control** — measured on BTCUSDT 5m (n=45) a 0.099 cap is byte-identical to the default, inert on 45 of 45, because a 1.5R target sits at a p50 of **0.95% of entry** against a 9.9% clamp; a control against that arm compares a book to itself. Reuses MI-307's reach expression `P(mfe_r >= min(cap_r, x))` unchanged, with `x` read PER TRADE from each row's own `unit_tp` (scalp declares `tp_at_r`, fvg targets a range boundary, so no fleet-wide `x` exists) and the clamp term simply never binding — reported as `clamp_would_bind_rate` rather than assumed. ⚠️ **Its control is WEAKER than MI-307's and says so**: MI-307's two arms had different trade counts, whereas here a shared `next_eligible_idx` can leave the arms with the same entries — `entry_overlap` is reported per leg so a reader meets that limit instead of inheriting a claim of parity. Target basis is per-trade DATA (`scripts/target_basis.py`, five never-collapsed states), never inferred from what the caller passed | `scripts/research/mi312_scalp_target_arms.py` (MI-312; `--selftest` carries 16 checks of which 4 are negative controls, incl. that the two arms must DIFFER) → `docs/research/scalp-family-target-arms-2026-09-18.md` |
| **Given MI-307's and MI-312's distributions, WHERE is a per-leg target decision actually owed — and where is "change nothing" the answer?** The DECISION packet, not a third measurement: it commissions no sweep and re-runs neither instrument, because MI-307's memo records that re-measuring is the failure mode `OI-20260906` exists to stop. Joins `config/strategies.yaml`, **`config/accounts.yaml` (the routing — which book an order reaches, and whether it is real money)**, both MFE artifacts, the e35 corpus and the coverage matrix into one row per leg over **all 55 legs**, with a DERIVED disposition so the verdict cannot disagree with the evidence columns beside it. ⚠️ **Resolves the clamp on the UNIT MODULE (`pipeline.monitor_unit_for` + `tp_venue_cap.CLAMPING_UNIT_MODULES`), never on the family string** — a family-only test under-claims and misses `fade_breakout_4h` outright, which `tp_venue_cap`'s own docstring warns of and which this unit reproduced. ⚠️ **`no_change` is a first-class verdict and is never pooled with `not_measured`**: a leg whose ~180 sweep cells produced nothing that generalises has its declared value standing BY EVIDENCE, which is a different and harder fact than a leg nobody looked at. **Result 2026-09-18: owed on 8 / already in config on 10 / refused by evidence on 31 / unknown on 6.** Three findings neither source memo could reach alone — **44 of 55 legs do not set their own target** (the 0.099 clamp does) and **26 of those touch NO Bybit account, 5 of them routing REAL MONEY through `alpaca_live`**; **the e35 corpus covers 41 legs, not the 19 MI-307 read** (7,343 real-`tp_r` cells), of which exactly **11 cells on 10 legs** survive walk-forward free of a timeout component and of inert folds — and **on all 10 the surviving cell's `tp_r` is already what config declares**, so no unshipped per-leg target is hiding there; and the open decision is **8 legs wide, none placing a real-money order today**. ⚠️ **Reads the FOLDS, never `wf_wins`** (`BL-20260817`: an inert fold scores as a win — 2 cells here report 5/6 with 3 folds inert), and counts `wf_pass` apart from `path_b_wf_pass`, whose 6 cells all FAIL their own gate on `maxdd_worse` | `scripts/research/mi317_target_geometry_packet.py` (MI-317; `--selftest` carries 39 checks of which 7 are negative controls, incl. that a scalp leg is never reported clamped and that `no_change`-by-evidence always has cells behind it) → `docs/research/per-leg-target-geometry-packet-2026-09-18.md` |
| **Can an arm-vs-arm control on the non-clamping (`ict_scalp`/`fvg`) family be INDEPENDENT at all — or is MI-312's near-tautological control a property of the FAMILY rather than of its design?** MI-312's residual, answered as a BOUND over its committed artifact rather than a new run, so it commissions nothing. ⚠️ **The load-bearing input is `entry_overlap` at the MAXIMUM perturbation** — `no_tp`, the target deleted outright — because no target change can perturb more than that. Exit time is monotone in target distance (`exit(T) = min(first touch of T, other)`), so every candidate target's exit is SANDWICHED between the live arm's and `no_tp`'s, and entry divergence is bounded by a divergence already measured. **Result 2026-09-18: `arm_vs_arm_cannot_be_independent` on 8 of 8 legs** — `entry_overlap` min 0.9900 / max 1.0000 / mean 0.9955 over 7,342 joined pairs, i.e. an **independent-content ceiling of 1.00%** against MI-312's observed 0.29% disagreement (the two are consistent, and that the ceiling exceeds the observation is the check). **So the obvious stronger design — predict the hit-rate at a target the live arm never ran, then run that arm — is refuted BEFORE it is built**: that arm is sandwiched between the two already measured and would share ≥99% of entries. ⚠️ **Step 4 is a BOUNDING ARGUMENT, not a proof of set-overlap monotonicity, and the memo says so** — what needs no caveat is the measured extreme case. ⚠️ **Does NOT transfer to MI-307's 19 clamping-family legs**, which are trailing strategies whose arms genuinely differ (`sol_pullback_2h`: 200 vs 277 trades) — which is why its 3.2 pp control means more than MI-312's 0.71 pp. Names the two controls that COULD fail (exit-mix, hold-time — quantities `mfe_r` underdetermines) and declines to run them, because both need emits that are gitignored | `scripts/research/mi318_scalp_control_bound.py` (MI-318; `--selftest` carries 15 checks of which 4 are negative controls, incl. that the opposite verdict is reachable and that no verdict turns on the chosen threshold) → `docs/research/scalp-control-design-2026-09-18.md` |
| **Is `fvg_range_15m`'s harness a VERBATIM PORT of the live entry block, or a reimplementation?** Two contradictory claims, neither measured: the live unit's own docstring (`fvg_range_15m.py:228`) says verbatim port; the MI-319 dispatch says it reimplements entry and computes its own target. Graded by MEASUREMENT, and the two claims graded SEPARATELY because the docstring is scoped to the entry block while the harness computes the target outside it. **Result 2026-09-18, ENTRY: the docstring holds and `reimplements entry` is refuted** — 18 shared rejection-disjuncts of 19 harness / 20 live with all three residuals classified (one harness loop-bookkeeping, two live argument validations) and ZERO unclassified; behaviourally 14 of 14 accepts matched (entry/target/confidence identical, stop to 4.3e-09) and zero one-sided accepts over 622 graded bars, in both window modes. **TARGET: the dispatch is correct** — harness default `mid` vs live `far`, corroborating MI-312's already-filed `base_args` row from the opposite direction. **And a third finding neither claim anticipated:** the unit declares 51 candles sufficient while its own three-deep `ewm(adjust=False)` ADX gate disagrees with the validated one on **21.9% of bars (136 of 622)** at that window and on **0 of 423** at the 250 the builder actually passes — the contract is wrong, latent rather than live, and the structural shape holds on `fade_breakout_4h` too (50 declared vs 200 fetched). ⚠️ **Does NOT clear shipped parameters** (0 accepts on this population = `no_accepts_in_population`, which is not agreement), does not compare the exit/monitor half, and is one symbol over seven days | `scripts/research/mi319_fvg_parity.py` (MI-319; `--selftest` carries 32 checks of which 11 are negative controls, incl. that a split-vs-combined gate is not a divergence and that an unexplained residual is never excused by the classifier) → `docs/research/fvg-parity-2026-09-18.md` |
| **`backtest_ict_scalp.py` CALLS the live `order_package` rather than porting it — so does it hand that function the same INPUTS the live builder does?** The successor to MI-320, which established the call and said in terms that this moves the question rather than answering it. Two seams, opposite answers. **WINDOW: closed, and measured rather than reasoned** — the same live `order_package` called on every bar at the harness's 50-bar slide and the live builder's 200-bar fetch gives **46 accepts each, 0 differing geometry, 0 one-sided, over 9,879 graded bars**. ⚠️ **MI-319's 21.9% gate-disagreement figure must NOT be carried here**: `fvg_range_15m` diverged because its ADX is a three-deep `ewm(adjust=False)` recursion, while `ict_scalp`'s `_add_atr` is a finite `rolling().mean()` — a reason to EXPECT agreement, not evidence of it, hence the measurement. ⚠️ The 46 is load-bearing: a zero-accept population grades `no_accepts_in_population`, which is **not** agreement. **CONFIG: open on exactly one leg of eight (all `execution: live`)** — the harness hardcodes the `ict_scalp_5m` block while 7 legs run through a variant builder, and `--strategy-name` relabels every emitted row without rerouting it. `ict_scalp_xrp_5m`'s `off_cells` grades **`not_expressible`** (the live builder SKIPS an entry on it, the harness never does, and no flag exists) and its `vol_spec` **`asymmetric_gate`** (`--vol-spec-json` exists and reaches no skip). **The live builder's own docstring names the consequence: *"XRP's pass was conditional on this gate (ungated 2/4 folds fails; gated 4/4 passes)"* — so this harness can only produce the arm that FAILED.** ⚠️ **Its reusable move is joining two measurements instead of tuning one predicate**: *"does this name reach a control-flow decision in the harness"* returns TRUE for `vol_spec` (it guards the regime-STAMPING routine, which occurs 12 times and skips nothing), so the asymmetry appears only when BOTH sides are asked the same narrower question — does the key guard a `continue`/`break`/`return`/`raise` — and the answers are joined. ⚠️ Does **not** re-run any backtest, and does **not** establish that any published number for that leg is wrong: where its evidence came from was not traced | `scripts/research/mi321_ict_scalp_seam.py` (MI-321; `--selftest` carries 26 checks of which 11 are negative controls, incl. that an ambiguous CLI-flag prefix is refused rather than guessed and that an unreadable side yields `could_not_look` rather than a verdict) → `docs/research/ict-scalp-seam-2026-09-18.md` |
| **Which pre-live backtest harnesses actually RUN the live entry decision, which REIMPLEMENT it, and which only CLAIM to — and how many `execution: live` legs sit behind each answer?** The successor to MI-319, which adjudicated ONE harness/unit pair and found the entry a verbatim port while the target contract was wrong — so adjudication changes the answer, and nobody had asked how many other pairs are in that state. ⚠️ **Two premises a reader would start from are wrong in both directions**: the load-bearing port claim lives in the live UNIT and runs the opposite way (the unit claims to port the harness), and not every harness reimplements anything — **2 of 13 call the live `order_package` outright**, one of them (`backtest_system.py`) through `importlib`, which a grep for the static import form reports as a reimplementation. The classifier is therefore AST-driven and asserted against that inversion, and against a harness defining its own `def order_package` — a reimplementation wearing the name. **Result 2026-09-18 over 13 harnesses / 16 units / 55 legs (45 `execution: live`, 0 unresolved via `pipeline.monitor_unit_for`): of the 45 live legs, 8 are gated by a harness that RAN the live decision, 20 by a claimed-but-never-adjudicated reimplementation, and 17 by a correspondence that asserts nothing at all — and THE ONE PAIR EVER ADJUDICATED GATES ZERO LIVE LEGS.** Exactly 3 of 16 units carry the formulaic port claim, and the extractor's positive control was NEEDED (an earlier draft required a backtick and missed `fvg_range_15m`, i.e. missed the adjudicated pair). ⚠️ **Its most reusable move is refusing to launder a difference**: residuals get six classes — `renamed_counterpart` only on an AST-shape match with a CONSISTENT name map, `implemented_upstream` only against a positive AND negative search control, `harness_only_lever` only when the argparse default is off AND no leg **of that unit** declares it (a fleet-wide version of that second clause grades `trend_donchian`'s inert ADX gate live, because pullback legs declare `adx_min`) — and a name overlap is reported as a HINT, never as a class. `trend_donchian` (20 live legs) grades **`idiom_mismatch`**, a REFUSAL that must not be read as parity or as divergence; `fade_breakout_4h`'s one genuine residual is the degenerate-target guard, explained by the live unit's far-sentinel target, which is MI-319's target finding recurring. ⚠️ **Does NOT compare behaviour** (structural only — a classified residual says what a difference IS, never that two would place the same trade), does not clear any parameter, and re-runs no sweep | `scripts/research/mi320_harness_provenance.py` (MI-320; `--selftest` carries 41 checks of which 14 are negative controls, incl. that an inconsistent name map is refused rather than called a rename, and that a locally-defined `order_package` never reads as running the live one) → `docs/research/harness-provenance-2026-09-18.md` |
| **Which strategy legs are STARVED — losing their symbol because another account took the winner — how often, and does the per-account arbitration fan-out cover them?** Answers the operator decision DEC-20260910-MI-29-MEASURE-THE-STARVED-LEGS-FIRST, measurement only. ⚠️ **Its load-bearing move is refusing to merge `starved` with `no_winner`** — `arbitration_fanout.py` records that doing so overstated this exact finding **6.5×** once already, and the first pass of this measurement repeated the error, reporting an `execution: live` leg 'losing 64% of its contests' when all 27 events were `no_winner`, i.e. it lost to nobody. Pooling inflates the finding **4.3× / 4.2×** by epoch. ⚠️ **And it splits the soak on `fanout_schema`**, because the write condition is `notable` and the 2026-09-12 projection repair changed what qualifies — so the file's inclusion rule differs either side of it under one filename. **Result 2026-09-18 over the COMPLETE soak (771 rows, 2026-08-30 → 2026-09-18): starvation is four legs and one account** — `breakout_1` on 88 of 90 gradings, holding `eth_pullback_prop_2h` (shadow), `trend_donchian_eth_prop` and `trend_donchian_sol_prop` (both live), and **no non-prop `execution: live` leg is starved more than twice in either epoch**. Those same two live prop legs are the **only two** live legs fleet-wide with order packages and zero fills in the 27.4-day journal window. **The fan-out does not cover them:** it genuinely dispatches (89 packages carry `meta.arbitration_fanout_round`, all `bybit_1`, from 2026-09-13) but **0 of 89 dispatched a leg other than the tick's global winner**, and `breakout_1` reads `not_allowlisted` on all 88 starved gradings. ⚠️ **Also establishes that a row in `trades` is not a fill** — 365 of 999 in-window rows are journalled refusals and 27 are orphan adoptions, so a per-leg row count reports `mgc_trend_1h` (zero fills) as having 51 trades | `scripts/research/mi29_starved_legs.py` (MI-29; `--selftest` carries 26 checks of which 10 are negative controls, incl. that `elected=None` can never grade as a loss and that a journalled refusal can never grade as a fill) → `docs/research/starved-legs-2026-09-18.md` |
| **Per account, which order decisions did the OLD routing drop that per-account election routes — replayed over the LIVE soak, not simulated?** E35 (2026-09-25). Every soak row since 2026-08-31 carries `rounds_planned`, the output of `plan_per_account_election` over that tick's once-gated set — and E35 makes exactly that output the dispatch, so the replay reads the new routing off the live process rather than re-deriving it. Compares three routings per `(row, account)`: `global` (only accounts holding the one global winner), `armed` (what the fan-out actually handed the dispatcher: v3 `rounds_applied`, else global; a v2 `rounds_applied` is cosmetic and graded as global) and `new`. Five never-collapsed outcomes — `same` / **`new_only`** (the fix) / **`old_only`** (a subtraction; MUST be 0) / `changed` / `neither` — plus a count of any account named in two rounds (a double-place). ⚠️ **The population is SOAK ROWS, not ticks**: the writer drops a tick on which every candidate-holding account held the global winner, and on those ticks all three routings coincide. **Result 2026-09-25 over the last 1000 rows (2026-09-03T12:56Z → 2026-09-25T06:20Z): old_only = 0 and double-routes = 0 in every routing; vs `global`, 110 account-gradings newly routed (breakout_1 100, bybit_2 5, bybit_portfolio 5); vs `armed`, 226 before the 2026-09-22T13:30Z widening and 0 after** — so post-widening the routing DECISION already matched, and what E35 changes on the live path is the per-round gates and the round meta | `scripts/research/e35_per_account_election_replay.py` (E35; `--since`/`--until` split the epochs) |
| **Does anything on the live fleet move the stop UP to bank R as it accrues — and what does that cost in excursion?** The banking half of M20's exit thesis, the counterpart to MI-146→158's target half. **Settles first whether `src/runtime/exit_plan.py` is on the live exit path — it is NOT** (both callers write to `order_packages` columns / a JSONL soak and nothing reads either back; `trail_mult` is the positive control that makes the silence meaningful), so a banking verdict read off its levers would describe a shadow artifact. Classifies each enabled+live leg by the mechanism its `monitor()` actually runs: **36 chandelier-ratchet** (monotone, genuinely banks R) vs **8 one-shot break-even** (`sl < entry` guard ⇒ fires once and **structurally cannot accrue R**). Because all three ratchet units share identical geometry and freeze the entry ATR, the excursion cost is EXACT arithmetic with the ATR cancelling — **`R_TO_BREAKEVEN = trail_mult / atr_stop_mult`** — directly comparable to `position_telemetry`'s `peak_r`. **Result 2026-09-07: median `R_TO_BREAKEVEN` 2.00R against a median live peak of 0.84R; the stop reached break-even on 14 of 89 (15.7%) and banked +1R on 4 (4.5%)** — every figure a LOWER BOUND, since `peak_r_is_lower_bound` is true on 171/171. Also reports **`be_at_r` is UNREACHABLE over the 44**, not merely undeclared (all 8 one-shot legs are `ict_scalp`, which never reads it), and carries a **telemetry-hook-gap detector** that re-derives the hooked unit set from source: **9 of 44 legs emit no peak-R telemetry at all**, so MI-155's 13 zero-n legs split **9 *we did not look* / 4 real absence**. Grades the POOLED corpus separately from per-leg (n=89 vs the repo's floor of 30 ⇒ **sufficient**, where every per-leg n is not) and its counterfactual **refuses to call itself net** — the telemetry carries peak and terminal, never the path, so forgone continuation is unmeasurable from it (checked: scripts/research/banking_mechanism_audit.py — its `counterfactual()` reads the served schema and reports `is_net: False`) | `scripts/research/banking_mechanism_audit.py` (MI-163; its tests pin the no-live-reader finding and the hooked-unit set so neither can rot silently) → `docs/research/banking-half-2026-09-07.md` |
| **Does moving the stop to break-even once a trade has run actually PAY, net of what it gives up?** The path-aware counterpart to `banking_mechanism_audit.py`, and the run that closes its stated gap. That audit measured a GROSS benefit table (peaking +16.87R at +0.75R) and refused to propose a value, because live telemetry carries `peak_r`/`open_r`/`giveback_r` and **no intra-trade path**, so the FORGONE CONTINUATION — what a trade that retraced to X and then resumed would have paid — is invisible to it. A bar-replay harness supplies exactly that: the floor stops the trade at entry, the harness records that exit, and the continuation is never earned, so **the cost is measured by construction rather than estimated**. Adds `be_floor_r` to ALL THREE ratchet harnesses (trend / pullback / squeeze — the three units share one trail geometry, so a lever meaning different things in each would make the pooled number incomparable) as a `max()`-ed floor that can only TIGHTEN, **default 0.0 and byte-identical**, then sweeps {0.5, 0.75, 1.0, 1.5} against the ungated control on each leg's OWN declared config params. ⚠️ **Folds are equal-CALENDAR, not equal-COUNT by trade order** — five arms produce different trade counts, so a common partition is a precondition for a delta — over a FIXED panel {3,4,5} (`BL-20260730-WF-FOLDCOUNT-VERDICT-FLIP`), with inertness IMPORTED from `m20_wf_effective.is_inert` rather than restated. **Result 2026-09-07: NO arm clears the gate.** Pooled over 28 graded legs / 4090 control trades, every floor LOSES net R and the loss is monotone in the floor — −145.16R at 0.5 · −128.13R at 0.75 · −99.08R at 1.0 · −35.04R at 1.5 against a +671.58R control — so the cost term exceeds the benefit at every value tested and **the proposed Tier-3 diff is none**. ⚠️ **Its most reusable finding is about the GATE, not the lever:** `be_floor_r=1.5` wins a strict majority of exercised folds under ALL THREE panel members while losing 35R, because 30–39% of its folds are inert — `BL-20260817-FLEET-SWEEP-WF-COUNTS-INERT-FOLDS-AS-WINS` reproducing on live-fleet parameters, and the reason the gate requires pooled-net AND fold-majority. ⚠️ **Backtest population, NEVER poolable with MI-163's 89 live rows** | `scripts/research/be_floor_sweep.py` (MI-165) → `docs/research/be-floor-sweep-2026-09-07.md` + `docs/research/data/be-floor-sweep-2026-09-07.json` |
| **WHERE should a trade be expected to exit — can that be PREDICTED from decision-time information, and is the prediction calibrated?** ML-2, named in `exit-mechanism-construction-PROCESS.md` § E-ML. Regresses the **conditional q-quantile** of the favourable excursion, because a take-profit is a level reached with some probability rather than a forecast of the mean — which makes E3.6's falsifier the training objective instead of an afterthought. ⚠️ **GRADED ON TWO BARS THAT DO NOT SUBSTITUTE**, because the first is vacuous alone: the unconditional empirical quantile is **calibrated BY CONSTRUCTION**, so a model that ignores every feature passes E3.6's falsifier exactly. Bar 2 is out-of-sample pinball loss against that baseline, and it is **gated by a shuffled-label null run PER QUANTILE** — a conditional fit beats the raw empirical quantile by +0.006..+0.027 even on data with NO signal (estimator efficiency, not market information), so `beats_baseline` alone is not evidence and `beats_baseline_within_null` is a refusal. The instrument is validated as a **RATE, never one draw** (TPR 1.00, FPR 0.00 over 6 seeds): its own first selftest graded a single draw and reported a **complete false positive**, the exact failure `e2_null_calibration.py` exists to prevent. Features are **decision-time and exogenous by construction** (§ 0.2's root cause: all 11 of the usual features are endogenous), asserted by set arithmetic in the tests rather than left to review | `scripts/research/ml2_bracket_train_eval.py` + `src/research/bracket_quantile.py` (MI-151) → `docs/research/ml2-predictive-bracket-2026-09-06.md` |
| **What does a leg's favourable-excursion distribution look like at PROPER n — the MFE numbers MI-148 gated its clause 1 on and could not compute?** Converts harness `--emit-trades` rows into the ML-2 corpus on the **percent-of-entry** basis MI-148's grader speaks, and emits the per-leg MFE quantile table beside `reach_venue_cap`. ⚠️ **The live telemetry gives 1–8 rows per leg**, which is why MI-148 refused per-leg target values; the offline harness gives 166k–184k 15m bars per symbol. ⚠️ **The backtest corpus is CLEANER than live for this one question** — the R-denominator contamination MI-148 cites is a property of the live journal (`trades.stop_loss` is the final trailed stop), not of R, so here both bases are exact and both are emitted. ⚠️ **One row shape cannot be converted and is SILENT**: under the M20 bank lever `gross_r` is a blend of two fills, the emit carries no flag for it, so the builder takes `--bank-frac-asserted` and REFUSES (`blended_unrecoverable`) rather than inverting; MFE survives because it is a path statistic computed before the blend. ⚠️ **This table is the model's COMPETITOR, not its input** — if ML-2 does not beat it out of sample, it is the answer and the model is surplus | `scripts/research/ml2_bracket_corpus.py` (MI-151) → `docs/research/ml2-predictive-bracket-2026-09-06.md` |
| **Did the VENUE book the close a `netting_attributed` journal row records — and for how much?** Asks `/v5/position/closed-pnl` via `/api/diag/bybit_raw_closed_pnl`, deliberately **not** the position path, because `OI-20260908`'s mechanism IS a misread of that path (a zero-size hedge-book sibling returns `_flat` for a symbol that is not flat) so adjudicating there would prove nothing. ⚠️ **The matcher is graded before it is trusted and its control CHANGED THE ANSWER**: rows are matched on side+qty, which under one-way netting is a live coincidence hazard, so every match is scored against `avg_entry_price` — a field the matcher does not key on — and over the 50 closed rows in the 2026-09-12 journal tail that gives **25 corroborated / 8 weak / 5 refuted** of 38 qty matches. Journal-minus-venue is **−1,955.86 over all 38 and +4,416.65 over the 25**, i.e. **the aggregate reverses sign on match quality**, most of the swing carried by one refuted match whose best candidate sits 5.8 days away. Decomposes the surviving gap **exactly** into a price term (+2,040.68, the real estimation error) and a **fee** term (+2,375.97, 53.8% — a basis difference, since the journal figure equals its own gross price arithmetic on 25 of 25 while the venue's is net). **Results: the closes are REAL on 25 of 25 corroborated rows; 5 of 25 disagree with the venue on the SIGN of the PnL; and 3 were booked by the venue 61–173 min AFTER the journal closed the row against a median of −0.3 min — one of them trade 5568, the exact row `OI-20260908` names**, giving that item venue-side timestamps. Makes **no network call of its own** (prints the `diag_fetch` plan, since Bybit caps a closed-pnl window at 7 days); `--self-test` asserts 8 controls including that a refuted match is refused and `could_not_look` never reads as `venue_no_rows` | `scripts/research/netting_close_venue_adjudication.py` (MI-278 U9; `BL-20260912-THE-NETTING-ATTRIBUTED-PATH-CARRIES-THE-LARGEST-SHARE-OF-THE-WINNER-COLLAPSE-AND-IS-100-PERCENT-ESTIMATED` option (b)) → `docs/research/m20-u9-netting-close-venue-adjudication-2026-09-12.md` |
| **Can the `netting_attributed` rows the venue adjudication could NOT match be adjudicated at all?** Grades the remainder MI-278 U9 left — 10 `venue_closed_different_qty` and 2 `venue_no_rows_in_window` — by asking whether a journal row corresponds to a SUM of same-side venue closes rather than to one, which is what netting-by-slices would produce. **IMPORTS U9's population loader, venue loader, adjudicator and `QTY_TOL`** so the two files cannot disagree about what 'same size' means. ⚠️ **Its headline is a REFUSAL, and the refusal is the finding: subset-sum is UNDER-DETERMINED.** It sweeps ±60/360/1440-minute windows deliberately rather than choosing one, because the sweep IS the ambiguity measurement — trade 4930 resolves to `2429.2+8.4+11.5` ($199.09) at one window and `2429.2+5.2+14.9` ($180.53) at another, and admits **74** distinct venue-PnL answers in total. A row with more than one distinct sum is graded `ambiguous_multiple_subsets` and NEVER matched; `n_alternatives` and the PnL spread ship beside it. **Do not fix the window** — that picks one arbitrary answer and hides the spread. Five states never collapsed: `sum_matched_unique` · `ambiguous_multiple_subsets` · `no_subset_sums` (searched, none found) · `venue_silent` (**asked and the venue had nothing**, which is not the same as searched-and-missed) · `not_searched`. Bounded search (≤4 terms, ≤14-row pool) because a 5-term sum over a netted book is numerology. **Result: `sum_matched_unique` = 0 of 12** — 8 no-sum, 2 venue-silent, 2 ambiguous — so option (b) of the backlog row is UNREACHABLE for the remainder and the row rests on option (a). The largest unadjudicated figure is trade 5451, `bybit_portfolio`/BTCUSDT, journal −$2,250.34, venue silent. One journal pull plus 27 `diag_fetch` venue windows (no relay needed); `--self-test` asserts 12 controls with no network | `scripts/research/netting_unmatched_remainder.py` (MI-278 U16; `BL-20260912-THE-NETTING-ATTRIBUTED-PATH-CARRIES-THE-LARGEST-SHARE-OF-THE-WINNER-COLLAPSE-AND-IS-100-PERCENT-ESTIMATED`) → `docs/research/m20-u16-netting-unmatched-remainder-2026-09-12.md` |
| **Does `/api/bot/performance` publish an `expectancyR` that agrees in sign with its own `totalPnl` and `profitFactor`?** Grades the LIVE endpoint response per block (`real_money` / `demo` / `paper` / `paperPortfolio`) against `PB-20260906-...`'s clause (1). ⚠️ **It READS the endpoint rather than reproducing it, and that is forced by the data**: `/api/diag/journal` hard-caps at 1000 rows whatever limit is asked (measured at 3000 and 5000), which no longer spans 30 days, so the row's original journal-side reproducer yields n=38 against the endpoint's 41 — whereas the payload already carries its own `rProvenance` and `rBasis`, making the grading exact by construction and unable to drift from the endpoint's predicates. **Three sign states, never collapsed** — `agree` / `disagree` / **`ungradeable`**, the last covering a zero or absent term, because *expectancyR is 0.0* and *we could not compare* are different facts. Surfaces the **R BASIS** (`declaredInitial` / `storedStop` / `refusedWrongSide`) beside the verdict, so a fixer learns before writing code which of the row's candidate fixes are already in. Its optional fan-out term carries **three reachability states** and **refuses to report an inflation computed over a shorter window than the block it describes** as that block's own — `partially_reachable` never renders as `measured`. **Results 2026-09-12: real money DISAGREES (−14.5552 / 0.8251 / +0.1113), the three paper blocks agree but carry 14.0% contaminated rows, `declaredInitial=41 storedStop=0` on real money and `storedStop=319` on paper, and real-money fan-out is exactly 1.000× — for a contingent routing reason.** `--self-test` asserts 15 controls | `scripts/research/expectancyr_correction_matrix.py` (MI-278 U13; `PB-20260906-R-CONTAMINATION-QUANTIFIED-AND-THE-HEADLINE-SIGN-IS-WRONG`) → `docs/research/m20-u13-expectancyr-two-defects-2026-09-12.md` |
| **Which surfaces compute a rate over `trades` rows for a question about PRICE, and by how much is n inflated?** Finds candidates mechanically (reads `trades` AND forms a rate) and joins them to a **DECLARED registry** classifying each `price_path` / `account_level` / `mixed` / `no_rate_over_journal_trades` / `ungradeable`. ⚠️ **A registry rather than a guard, on the backlog row's own reasoning and tested rather than assumed** — the row count is the RIGHT denominator for account questions and wrong only for price ones, nothing in the source distinguishes `wins/len(rows)` from `refused/len(rows)`, and the mechanical half over-collects so badly it matched itself. A candidate absent from the registry grades **`undeclared` — the finding, never a pass**. **Decomposes the inflation into TWO terms that a naive package-dedupe silently merges**: `cross_account` (one package, several accounts, one price path — the fan-out) and `intra_account` (one package, ONE account, several rows — a partial close or re-adoption, which is *not* an extra view of the same price path); a row with **no** package id is reported, never counted as its own package, and an ungradeable population returns `None` with its own state rather than 1.0. Also reports inflation **per cut**, because the row's finding is that it is UNEQUAL between arms. **Results over 36 live-journal surfaces and a 1000-row journal tail: 21 publish a price-path rate on a row denominator (3 of them live SPA routes), only 2 already dedupe by package, overall 1.264× / cross-account 1.238× / intra-account 1.021×, and ~1.00× within any single account.** Reads the repo and one journal pull; makes no network call of its own; `--self-test` asserts 19 controls | `scripts/research/fanout_denominator_survey.py` (MI-278 U12; `BL-20260911-A-RATE-PER-TRADE-ROW-COUNTS-ACCOUNT-FANOUT-AS-INDEPENDENT-AND-INFLATES-N-UNEQUALLY`) → `docs/research/m20-u12-fanout-denominator-surfaces-2026-09-12.md` |
| **Does a published price-behaviour rate survive a PACKAGE denominator instead of a ROW one — and by how much?** The successor to the survey above: that one NAMED the 21 surfaces, this one PAYS the clause on the two that matter most. Re-derives any cell both ways side by side, importing `population` / `group_of` / `era_of` / `adjudicate_exit` / `wilson` / `fisher_2x2` **unmodified** so a difference is the denominator and never the population. **Three package states, never collapsed**: `unanimous` · **`disagreement` — REPORTED AND NEVER ARBITRATED** (MI-278 U15 measured that choosing which fan-out row to believe swings a headline 4.3–28.2pp, so picking one is a choice wearing the clothes of a fact; such a package is in neither numerator nor denominator) · `ungradeable`. An ungradeable row beside a gradeable one does **not** manufacture a disagreement — *we could not look* is not a competing opinion — and a row with no `order_package_id` is counted as an orphan, never given a synthetic id that would restore the row denominator for exactly the rows we cannot place. **Results 2026-09-12: the `tightened_to_2_from_2.5` cell's Fisher p goes ~0 → 0.021, the conclusion survives and its strength does not; the dose inversion survives (0.714 vs 0.250); the PRE cell holds 2 of 9 self-contradicting packages; and MI-278 U18's own headline goes p 0.0486 → 0.1984 while its sign-survival criterion flips to PASS by +0.0044 — `criteria_agree` exists so that disagreement cannot be quoted one-sidedly.** `--self-test` asserts 29 controls | `scripts/research/package_denominator_rederive.py` (MI-278 U19; `BL-20260911-A-RATE-PER-TRADE-ROW-COUNTS-ACCOUNT-FANOUT-AS-INDEPENDENT-AND-INFLATES-N-UNEQUALLY`) → `docs/research/m20-u19-package-denominator-rederivation-2026-09-12.md` |
| **Was an order's size a RISK decision or a margin CEILING — and can the journal even tell?** Grades every row carrying `notes.margin_basis` into **six ratio BANDS** rather than testing equality, because `PB-20260822`'s own wording (*equals to FULL FLOAT PRECISION*) is a trap: the shipped size is **QUANTISED to the venue step**, so an `==` probe returns **zero across the whole journal** and a reader concludes the effect stopped. **`zero_size` is its own band** — the sizer refusing is not a conservatively-sized order, and pooling them lets 154 refusals read as prudence. **And it measures whether the record exists at all**: `visibility_state` separates `recorded` from **`absent_on_placed`** — the finding — from `absent_on_refused`, because `margin_basis` has exactly one writer, `execute.log_rejection_to_journal`, a refusal-only path, so a PLACED order can never carry it while `PB-20260822`'s criterion is about exactly those. The negative ships with its **positive control** (placed rows carrying ANY notes) so an absence cannot be missing data, and the independent `dump_capped` hazard is counted **separately** rather than folded in. The opposite failure — a size ABOVE its own cap — is reported even when empty, as a measured zero. **Results 2026-09-13 over 1000 journal rows: 208 carry a record and 32 (15.4%) sit at ratio ≥0.999 of `max_qty_by_margin`, 30 of them `ict_scalp_avax_5m`/`bybit_1` — 15× the row's original n=2 — while 623 placed rows all carry notes and NONE carries the stamp, and 138 of them are already truncated.** Reads a saved journal pull; no network call, no code path touched. `--self-test` asserts 24 controls; 26 pytest controls run the same contract in CI | `scripts/research/margin_ceiling_visibility.py` (MI-278 U37; `PB-20260822-AVAX-SCALP-SIZED-OFF-MARGIN-NOT-RISK`) → `docs/research/m20-u37-margin-ceiling-visibility-2026-09-13.md` |
| **Is a journal row's `position_size` an OBSERVED quantity, or one ASSIGNED by netting attribution — and can the `exit_reason` filter every analysis uses even SEE the difference?** Establishes at the field that `order_monitor._netting_rows_to_attribute` computes `take = min(qty, remaining)` from `excess = journal_qty - backed`, a subtraction of two AGGREGATES distributed by a heuristic, with no venue fill anywhere. **And finds what the candidate did not: `_netting_apply_close`'s PARTIAL branch rewrites `position_size` and leaves the row OPEN**, so it closes later as `sl` / `sl_cross` / `reconciler_filled` — invisible to the `exit_reason='netting_attributed'` scope MI-277 A6, U9 and U16 all use. **Four `qty_basis` states, never collapsed**, of which **`evidence_may_be_shed`** carries the design: the `notes` stamp goes through `dump_capped(notes, 500)` and neither key is in `json_notes._DEFAULT_PROTECTED`, so a truncated unstamped row is *we could not look*, never *not attributed*. **Grades against TWO INDEPENDENT SOURCES** — the journal stamp and `--soak`, the attribution soak, which is written before the DB is touched and is not subject to the cap, so it rescues a shed stamp AND supplies the CUMULATIVE take the overwritten stamp cannot. An `annotate` soak row mutated nothing and is never counted; a soak row with no `trade_id` is dropped rather than keyed on `None`. **Results 2026-09-13 over 765 bybit rows: the filter matches 50, the stamp census is 52, the filter misses 2, and 111 are ungradeable from the journal — while 4 of the soak's 29 applied trades carry NO stamp at all. Six invisible rows across all three e35 arms; the journal alone sees two.** Also measures, with a positive control, that `src/runtime/provenance.py` has **no quantity axis** (`position_size` 0, `quantity` 0, `size` 0 against `exit_price_source` 12) and surfaces the rows it grades `measured` on an assigned quantity. `--self-test` asserts 38 controls; 29 pytest controls run the same contract in CI | `scripts/research/netting_position_size_basis.py` (MI-278 U33; `BL-20260912-THE-NETTING-ATTRIBUTED-PATH-CARRIES-THE-LARGEST-SHARE-OF-THE-WINNER-COLLAPSE-AND-IS-100-PERCENT-ESTIMATED`) → `docs/research/m20-u33-netting-position-size-basis-2026-09-13.md` |
| **Did the netting path's contribution CHANGE after the two hedge-book fixes?** The re-measure `BL-20260912-THE-NETTING-ATTRIBUTED-PATH-CARRIES-THE-LARGEST-SHARE-OF-THE-WINNER-COLLAPSE-AND-IS-100-PERCENT-ESTIMATED`'s option (a) waits on, now that `OI-20260908`'s root cause is landed and deployed. **FOUR eras, not three** — a SECOND fix to the same chain (PR #11903, the `(symbol, position_idx)` dedupe) merged 2026-09-12T19:47:52Z inside the naive post-fix window, so a pooled result attributes to NEITHER and the pooled arm is LABELLED as such. ⚠️ **The scope decides the verdict and it is imported, not re-derived**: on `exit_reason` alone (the unsound filter U33 refuted) the pooled contrast reads p=0.0375, SIGNIFICANT; on the corrected stamp-AND-soak union it reads p=0.0681, not — the filter misses 7 of 33 applied soak rows UNEVENLY across eras. It reports every contrast it computed plus the Bonferroni line, because a single p selected from five is not a p. ⚠️ **R is imported from `winner_size_collapse_2026_09_12.risk_usd`**: `trades` carries NO `r` column (0 of 1000 measured), and this instrument's first draft read one and graded both arms `unmeasurable` — a FALSE negative. Never-collapsed: era `unknown` for an undateable row · three scope sources reported apart · soak `covered`/`partial`/`uncovered`/`unknown` (a union scope inherits its weakest source) · `graded`/`no_risk_basis`/`risk_reader_unavailable` · verdict `fall_persists`/`fall_vanished`/**`insufficient_n`**/**`unmeasurable`** (an EMPTY arm is not a vanished fall). On the live window it returns `insufficient_n` at 4 post-fix graded winners against a required 11, and converts the wait into DATES (~2026-09-28 share, ~2026-10-01 winner-R) explicitly labelled planning figures. 54 self-test controls; 14 planted defects, 14 caught, one only after the suite was strengthened because no fixture reached the branch | `scripts/research/netting_postfix_remeasure.py` (MI-278 U52) → `docs/research/m20-u52-the-netting-remeasure-is-runnable-and-under-powered-2026-09-17.md` |
| **When `expectancyR` disagrees in sign with `totalPnl`, is that a DEFECT or is it risk normalisation working?** Adjudicates `PB-20260906`'s two statements separately, because only one survives. **The mechanism is testable directly and is not a matter of opinion**: the endpoint publishes `rBasis`, which counts WHICH risk each published R was divided by, so `storedStop: 0` means no published R touched the trailed stop and the named collapse cannot be responsible — **whatever `rProvenance.contaminated` says**, because that field grades the STORED STOP and not the basis used, a distinction the route's own comment draws and this module is the reader for. **The criterion does not survive**: R is pnl over the trade's own risk, so a book whose winners are sized smaller than its losers MUST show positive mean R and negative dollars, and requiring the two to agree would require R to stop being risk-normalised. Reports **three orthogonal facts, never collapsed** — `sign_state` · `mechanism_state` (`can_operate` / `cannot_operate` / **`not_gradeable`** when `rBasis` is absent, which is *we could not look*) · `explanation` (`risk_size_heterogeneity` / `partial` / **`unexplained`** — it does NOT manufacture a cause) — and a **live mechanism outranks a benign explanation**, so an available real cause is never displaced. Separation is **threshold-free**: it asks only whether the winner and loser dollars-per-R ranges OVERLAP. A leg whose PnL and R have opposite signs has NO dollars-per-R and is counted, never given a negative ratio that would read as a risk. **Results 2026-09-13 on the live 30d real-money payload (41 trades, 6 legs): signs disagree (+0.1113 vs −14.5552, profitFactor 0.8251), `rBasis` is 41/41 `declaredInitial` with `storedStop: 0` on every leg so the mechanism CANNOT operate, and the two profitable legs carry 1.480 and 2.961 dollars per R against the three losing legs' 3.793, 4.274 and 4.577 — total separation, gap 0.832, spread 3.09×.** Reads a saved payload; makes no network call and touches no route. `--self-test` asserts 23 controls; 23 pytest controls run the same contract in CI | `scripts/research/r_sign_split_basis.py` (MI-278 U36; `PB-20260906-R-CONTAMINATION-QUANTIFIED-AND-THE-HEADLINE-SIGN-IS-WRONG`) → `docs/research/m20-u36-r-sign-is-risk-size-2026-09-13.md` |
| **Is an `order_package_id` a FAN-OUT key, and could a tail pull have TRUNCATED the package?** Two questions asked together because the second cannot be bounded without the first. **A package holding two rows for the SAME account cannot be one cross-account fan-out**, and that test runs FIRST and needs no timestamp — a repeat is provable with no clock, so testing simultaneity first would throw away a fact we hold. Five `fanout_state`s, never collapsed (`simultaneous_fanout` · `account_repeated` · `distinct_accounts_wide_spread` · `ungradeable_no_account` · `ungradeable_no_timestamp`). ⚠️ **CONTIGUITY IS NOT THE TEST** and a session reaching for it is wrong on a quarter of the class: of the 16 reused ids measured, 12 are id-non-contiguous but **4 are contiguous** and one spans 105 ids. The truncation half is a **derivation, not a re-run**: `/api/diag/journal` is `ORDER BY id DESC LIMIT n` with no offset, so a missing member is BELOW the minimum, and two measured preconditions — **density** (no interior gap) and **monotonicity** (id order agrees with `created_at` order) — bound how far below the edge a sibling can reach. `basis_state` REFUSES when either fails, because a bound derived from a violated assumption reads as an answer and is worse than none. Four `reach_state`s: `beyond_observed_reach` (**named for an OBSERVED maximum, never `complete`**) · `within_observed_reach` · **`unbounded_class`** (the reuse class, whose spread is limited only by the window — neither safe nor at risk) · `ungradeable_reach`. A missing bound is `None`, **never `0.0`**, which would declare every package safe. Single-row packages ARE graded — a truncated fan-out's remnant is a single row. `--cross-check-u15` imports U15's own arm builder and U22's own band unmodified and varies only whether the reuse class is in the cells, behind a **positive control** that returns `no_overlap_cannot_verify` rather than a clean zero. **Results 2026-09-13 on one live 1000-row pull (793 packages, 110 multi-row): 16 (14.5%) repeat an account; the two classes separate at max 5.918 s vs min 3 907 s, a 662× gap with nothing between; 1 package of 793 (0.13%) is within reach of the tail edge; and all 10 reuse units that reach U15's cells land in `control_post`, carrying 6 of its 7 disagreements — band 3.8..29.6pp → 6.1..23.7pp, sign surviving both.** Proposes no exclusion and refuses to. `--self-test` asserts 68 controls; 33 pytest controls run the same contract in CI | `scripts/research/package_fanout_integrity.py` (MI-278 U38; `BL-20260912-A-1000-ROW-TAIL-PULL-CHANGES-A-FANOUT-PACKAGES-MEMBERSHIP-WITHIN-HOURS-SO-A-PACKAGE-LEVEL-RATE-OVER-A-TAIL-IS-UNSTABLE-BY-CONSTRUCTION`) → `docs/research/m20-u38-order-package-id-is-not-a-fanout-key-2026-09-13.md` |
| **Is the arbitration FALLBACK still below the legs it was chosen to lose to — and where is an uncovered leg routed?** A CI guard, not a one-shot: `_UNKNOWN_STRATEGY_PRIORITY` is documented as *"picked deliberately below the in-scope strategies"* and the distribution has since moved under it, so **omission does not merely fail to be safe — it WINS the arbitration**. Direction verified in code (`_election_sort_key` carries `-effective_priority()`; the aggregator sorts ASCENDING and takes `ordered[0]`), never assumed. **R1** is the row's own *transferable half* — the fallback **STRICTLY** below `min(DEFAULT_PRIORITIES.values())`, with a control proving `<=` is caught. **R2** is per-leg coverage with the absent legs **NAMED, never only counted**. **R3** is the enforcing rule: a WORSENING against a committed seed, including **a seeded leg MOVING ONTO A REAL-MONEY ACCOUNT with no new leg appearing** — exactly what the Option-A roster change did. ⚠️ **R1/R2 are REPORTED and NOT enforced, deliberately**: both are violated today, the remedy is **Tier-3**, and failing here would red every PR in the repo, which is how a guard gets disabled instead of fixed; `--strict` arms them once the operator's change lands and there is no flag to unset. ⚠️ **Seeding is NOT a disposition** — the seeded legs are still uncovered. `improved` is reported, does not fail, and is the signal to RE-SEED, since a baseline wider than reality stops catching the next regression. Parses `DEFAULT_PRIORITIES` via **both `ast.Assign` and `ast.AnnAssign`** — it is declared `Dict[str, int] = {...}`, so an Assign-only walk returns an empty map that reads as *no legs are mapped*. **Results 2026-09-13: fallback 10, 50 mapped, min 0, 45 strictly below; 5 of 44 live legs uncovered; and `alpaca_live` (`real_money`, `mode: live`) routes two of them.** Reads `intents.py`, `strategies.yaml` and `accounts.yaml` and writes none of them. `--self-test` asserts 37 controls; 25 pytest controls run the same contract in CI | `scripts/ci/check_priority_fallback_distribution.py` (MI-278 U40; `BL-20260909-UNKNOWN-STRATEGY-PRIORITY-NOW-BEATS-45-OF-50-DECLARED-LEGS-AND-THE-CONTENTION-IS-LIVE`) → `docs/research/m20-u40-priority-fallback-inverts-its-own-purpose-2026-09-13.md` |
| **Does an account's DECLARED liveness agree with what the executor actually DID — and if it books dry, which gate did it?** Reports the **CURRENT ERA**, never the window census: an account's refusal cause changes, and grading the modal cause is how a row gets filed two weeks after its cause stopped binding (measured: `alpaca_live`'s cause changed FOUR times, and the sizing cause the 2026-09-09 row names last fired 2026-08-28). Six `gate_state`s, never collapsed, of which **`declared_live_books_dry`** is the finding and `no_rows_observed` / `ungradeable_no_is_dry` are *we could not look* — an unknown declared state is never read as dry, and an absent `notes.is_dry` is never read as a live dispatch. Six `dry_attribution` states whose terminal value is **`unattributed_dry`** — *the account gate and the strategy gate were BOTH permissive and the client-unavailable path is excluded BY THE REASON STRING the executor writes* — a refusal to name a cause, not a fallback bucket. ⚠️ **Attribution is scoped to the CURRENT ERA and that is load-bearing**: the gate flags a caller passes describe today's config, and `alpaca_live` was `mode: dry_run` until 2026-08-27, so a window-wide attribution answers about rows a declared gate fully explains; the window-wide value is reported BESIDE it, never instead of it. Four `tradeability` states supply the distinction `BL-20260909-ALPACA-LIVE-CANNOT-SIZE-A-SINGLE-SHARE-SO-ITS-OPEN-ITEM-IS-UNREACHABLE-NOT-PENDING` asks for — **`structurally_unable` REQUIRES one cause across every graded row**, because several causes is a tuning question and one deterministic cause is a standing condition. A refusal quoting no balance reports `None`, **never `0.0`**, which would invent the very condition being investigated. **Results 2026-09-13 on 33 live `alpaca_live` rows: `declared_live_books_dry` · `unattributed_dry` · four real-money rows since 2026-09-01 (newest 2026-09-11) carrying `is_dry: true`, with both declared gates verified permissive against git history at every timestamp.** Reads a saved journal pull; makes no network call and touches no gate. `--self-test` asserts 56 controls; 26 pytest controls run the same contract in CI | `scripts/research/declared_vs_effective_dry.py` (MI-278 U39; `BL-20260909-ALPACA-LIVE-CANNOT-SIZE-A-SINGLE-SHARE-SO-ITS-OPEN-ITEM-IS-UNREACHABLE-NOT-PENDING`) → `docs/research/m20-u39-alpaca-live-books-dry-on-a-live-account-2026-09-13.md` |
| **Does the BLEED RECORD's own rate survive a PACKAGE denominator — and which READING of it is safe?** The successor to the row above, on the surface that matters most: `bleed_attribution_2026_09_11.py` is the script behind the −$38,851.81 figure the loud `OI-20260911` rests on, and it reads `order_package_id` **zero** times (positive control: `account_id` twice). Verified off the SCRIPT rather than off prose, it also does **not** scope by account — no `--account` arg, `account_id` touched only inside a per-account *breakdown* printed beside arms computed over the whole population — **so its arms are POOLED and the pooled arms are the exposed ones.** ⚠️ **THE DELIVERABLE IS A PARTITION, NOT A CORRECTION, AND `pnl_sum` IS DELIBERATELY NOT RE-DERIVED**: it is an ACCOUNT fact, every fanned-out row is a real position that really lost that money, and a package denominator would **understate money actually lost** — a session revising the headline DOWNWARD would commit the backlog row's error in the opposite direction, which is the likelier mistake because a package denominator sounds uniformly more careful. Four basis states, `undeclared` being the finding, and completeness is checked by **calling** `summarise` rather than reading it, so a key added tomorrow surfaces. Two orthogonal verdict axes that are never collapsed — `significance` (`both_significant`/`lost`/`gained`/`neither_significant`) beside `strength` (`weaker`/`stronger`/`unchanged`) — because a single string labelled an arm that was never significant as having LOST significance, on live data. **Results 2026-09-13 (1000-row tail, ids 5731→4732, 292 in population): POOLED 1.3709× inflation, 17 of 213 packages self-contradicting, and the e35 pre/post stop Fisher goes p=8e-05 → 0.05703 — it CROSSES α and is the only arm that changes, while the mechanism is NOT unequal inflation (1.444× vs 1.471×) but ~⅓ fewer observations plus post stop-outs concentrated in fanned packages (15 rows → 8 packages, 1.875×). Scoped to `bybit_1` the inflation is 1.0581× and e35 is BYTE-IDENTICAL on both denominators, because FAN-OUT IS CROSS-ACCOUNT — so a per-account reading is immune and the whole exposure sits in the pooled arms.** `--self-test` asserts 25 controls (38 more in `tests/`) | `scripts/research/bleed_record_package_denominator.py` (MI-278 U31; `BL-20260911-A-RATE-PER-TRADE-ROW-COUNTS-ACCOUNT-FANOUT-AS-INDEPENDENT-AND-INFLATES-N-UNEQUALLY`) → `docs/research/m20-u31-bleed-record-package-denominator-2026-09-13.md` |
| **When a statistic takes TWO terms from row counts, WHICH one moves the answer?** The successor to the re-derivation above, on a subject that re-derivation cannot answer: `e35_break_attribution`'s one-sided binomial `P(X<=k | Bin(n,p))` draws `k,n` from the E35 arm's POST rows AND its null `p` from the family-matched CONTROL arm's own rows/rows rate, so fan-out reaches the sample size and the null hypothesis by different paths and a single rows→packages swap cannot attribute the change. Computes **four cells** — every combination of (n from rows | packages) × (p from rows | packages) — importing `population` / `arm_of` / `DEPLOY` from the subject **unmodified**, so a difference is the denominator and never the population. The attribution is **threshold-free**: it classifies which substitutions carry the p-value across ALPHA and carries no tuned constant. **Seven driver states**, of which **`offsetting` is the one that pays for the design** — both pure bases agree while a MIXED cell crosses, i.e. the two substitutions push OPPOSITE ways and cancel, so the published figure is stable by coincidence rather than robustness; collapsing it into `no_crossing` would report a cancellation as a confirmation. Grades **wins**, so it deliberately does NOT reuse U19's `package_verdict` (which adjudicates EXIT verdicts from the price path): a P&L sign is an account fact and can legitimately differ across a package's rows. Also re-derives the arm **deltas** a reader actually quotes, with `sign_flip` as its own stability state. A package straddling the deploy boundary is named and placed in no era; a degenerate null rate (0 or 1) is flagged vacuous rather than refused. **Results 2026-09-13, pooled `bybit_*`, 204 rows / 155 packages (1.3161×, 27 packages spanning >1 account): the four cells span 0.0243–0.1779 and one crosses α while both pure bases read 0.0586 and 0.1010 — `offsetting`; the e35 delta is robust (−55.0 → −57.6pp) but the family-matched control's FLIPS SIGN (+0.8 → −8.3pp) on 3 pre packages; and per-account every cell collapses to one number at inflation ~1.0, so pooling IS the exposure.** `--self-test` asserts 52 controls; 36 pytest controls run the same contract in CI | `scripts/research/e35_binomial_package_denominator.py` (MI-278 U32; `BL-20260911-A-RATE-PER-TRADE-ROW-COUNTS-ACCOUNT-FANOUT-AS-INDEPENDENT-AND-INFLATES-N-UNEQUALLY`) → `docs/research/m20-u32-e35-binomial-package-denominator-2026-09-13.md` |
| **Did the market change, or did e35? — MAE/MFE in ATR units on the NON-e35 CONTROL arm, before vs after 2026-08-30.** Supplies the arm `stop_width_counterfactual_2026_09_11.py` never had: its `build_units` filters `group_of == "e35"` and then drops anything `stop_change()` does not recognise, so a control leg is excluded twice over by construction. **IMPORTS `excursion_regime` and `fixed_window_excursion` unmodified rather than reimplementing them** — a second implementation of "MAE in ATR units" would be free to disagree with the one already in the record, and the comparison is worth nothing unless both arms are measured by the same code. ⚠️ **Refuses the obvious comparison and says why in the output:** the arms differ in holding period by an order of magnitude (`ict_scalp_*` 5m/15m vs `trend_donchian_*` 2h/4h) and `excursion_regime`'s window IS the holding period, so own-lifetime excursions are stamped `cross_arm_comparable: false`; only the **fixed-window-from-entry** basis is differenced, and a **symbol-matched** view ships beside the pooled one because the arms do not trade the same symbol mix. **The builder positive control is the admissibility gate:** `--group e35` rebuilds the TREATED arm through this file's own builder and compares package-by-package against MI-275's recorded output — **0 disagreements over all 31 shared packages**, after failing three times and surfacing three instrument findings (`extreme()`'s bare `else` returning max-high for any unrecognised side, so MAE came back exactly equal to MFE in all eight cells with no error; `excursion_regime` windowing on `rows[0]`'s close where 5 of 35 packages have a sibling close spread over an hour, widest 30.9h, and `rows[0]` is input order; and MI-275's OKX basis grading **`unfit`** on the scalp arm, 5 of 126 at up to 120.5bp against a 25bp tolerance, having been validated on trend legs only). Unit is the **order package**, never the trade row. States are never collapsed — `graded` / `no_candle_source` / `no_entry_frozen_atr` / `no_package` / `unknown_era` / `no_direction`, and an ungraded unit is counted and named rather than dropped. **Results: MAE grew in BOTH arms (post/pre 1.09–1.45×) while MFE collapsed only in the treated one (48h: 0.195× vs 0.945×), surviving symbol matching on 4 of 5 shared symbols with BTCUSDT the exception at 0.36× in both** — which acquits e35 of the winner-size collapse, since a window measured forward from entry ignores the exit. One journal pull plus OKX 1m via MI-275's fetcher; `--self-test` asserts 13 controls with no network | `scripts/research/excursion_control_arm.py` (MI-278 U5; `OI-20260911-THE-DIRECTIONAL-LEGS-BROKE-ON-2026-08-30-AND-THE-CAUSE-IS-UNATTRIBUTED`, `BL-20260911-MFE-OVER-MAE-IN-ATR-UNITS-IS-A-LEADING-INDICATOR-NOBODY-COMPUTES`) → `docs/research/m20-u5-the-non-e35-control-arm-2026-09-12.md` |
| **Was the stop that ended it the DECLARED one, on BOTH arms — and what is that worth to a published before/after rate?** Splits adjudicated stop-outs into `stop_is_entry_declared` / `stop_amended_tighter` / `stop_amended_wider` / `ungradeable_no_final_stop` / `ungradeable_no_declared_stop`, per ORDER PACKAGE, reading the declared level from `order_packages.exit_plan.stop.price` and **never from `pkg.sl`, which may already carry a trailed value** — falling back to it would grade an amended stop as the declared one, which is the defect. ⚠️ **The four-state rule lives INLINE in `CF.build_units` and cannot be imported, so the copy is asserted rather than trusted**: `--verify-against-cf` re-derives MI-275's own recorded verdicts through this file's function and a disagreement is a REFUSAL, while an empty intersection reports `no_overlap_cannot_verify` and never a pass. **Reports TWO rates per cell, never one** — the naive rate over all stop-outs and the rate over declared-stop ones — because the row it discharges forbids dropping the trailed ones silently. **Results: e35 pre 2 stop-outs (2 declared / 0 amended), post 8 (6 / 2); untouched_control pre 31 (22 / 9), post 41 (33 / 8) — every amendment TIGHTER, zero ungradeable, zero skipped. Trailing is NOT an e35 phenomenon and only the control arm could show it (control amends 29% of pre-era stop-outs against the treated arm's 0 of 2).** ⚠️ **It corrects a number published the same day**: MI-278 U5's stop-rate DiD of +21.2pp becomes **+7.6pp** declared-only on U5's own row selection, and across that choice AND the fan-out `rows[0]` ambiguity the headline ranges **4.3–28.2pp on identical data**, the entire swing arriving through the CONTROL arm while the treated arm reads 35.9pp under every selection. Sign positive in all six combinations; magnitude a third to a half of what was published, on an e35 pre cell of TWO. One journal pull, no network; `--self-test` asserts 22 controls | `scripts/research/stop_integrity_both_arms.py` (MI-278 U15; `BL-20260911-A-TRAILED-STOP-OUT-IS-ATTRIBUTED-TO-THE-DECLARED-GEOMETRY-THAT-DID-NOT-END-IT`) → `docs/research/m20-u15-stop-integrity-both-arms-2026-09-12.md` |
| **Is the 2026-08-30 break verdict's STOP-OUT term robust — to PROVENANCE, to SAMPLING, and to a REPULL?** The audit of the one scalar `m20_u41_break_attribution_verdict.verdict()` reads but does not compute: it branches on `stop_rose = stop_did_pp > 0` while `stop_did_pp` arrives as a hand-typed `--stop-did-pp 11.6` CLI float from `stop_integrity_both_arms`, so **the SIGN of an externally supplied number is the whole difference between `both_contribute` and `market_regime`** — between an e35 revert being on the table and not. **Imports** `bleed_attribution_2026_09_11` (population / era / arm / `adjudicate_exit`), `stop_integrity_both_arms` (`build`, the declared-vs-amended split) and **`src.runtime.provenance`** unmodified; it re-derives no verdict and does not re-run #12205. ⚠️ **Classifies the row the adjudication actually GRADED** (`trade_ids[0]`, already `closed_at`-sorted), not the package — siblings can carry different exit provenance and the term is computed from the graded row. **Three never-collapsed rate states** (`measured` / `insufficient_n` / **`empty`** — *no unit reached this cell*, whose rate is `None` and never 0.0) and **two declared floors, cell ≥ 8 AND stop-outs ≥ 5**, because the binding quantity is the NUMERATOR: a test pins that 4/44 clears the cell floor and is still ungradeable. **Fragility is reported BESIDE the term, never folded into it** — the cell floor binds first in `verdict_term`, so a knife-edge finding would otherwise be swallowed by `cannot_discriminate`, which is the collapse this file is about. **Results 2026-09-17, trades ids 4887–5886, e35 8 pre / 20 post vs control 47 pre / 103 post, 0 skipped: the fabrication hypothesis is REFUTED — ZERO fabricated rows in all four cells (the 2026-07-30 anchoring fix and the 2026-08-24 `recorded_exit_price` reclassification removed the class), though the coverage asymmetry is real at 12.5% vs 70.9% and is measured-vs-ESTIMATED; the declared-only DiD is +5.31pp and **ONE** extra stop-out in the treated pre cell flips its sign; that cell holds 2 stop-outs BOTH from `ada_pullback_2h`/ADAUSDT, Wilson CI (7.15, 59.07), which CONTAINS the control's pre rate; **all 13 e35 per-leg cells grade `insufficient_n`**, so the row's per-leg demand is not answerable; and the term is unstable under REPULL — 13.2 → 11.57 → 5.31pp as the e35 pre cell fell 18 → 8 packages under the 1000-row journal clamp.** Computes the Wilson interval U15 declined to, on the grounds that an interval visibly spanning everything is evidence where the assertion was not. `--self-test` asserts 31 controls incl. 3 planted defects; 19 pytest controls RUN in CI | `scripts/research/mi301_stop_term_robustness.py` (MI-301; `OI-20260911-THE-DIRECTIONAL-LEGS-BROKE-ON-2026-08-30-AND-THE-CAUSE-IS-UNATTRIBUTED`, `BL-20260917-THE-BREAK-VERDICT-CONSUMES-A-HAND-TYPED-STOP-DID-SCALAR-WHOSE-POPULATION-AND-PROVENANCE-ITS-OWN-ARTIFACT-NEVER-STATE`) → `docs/research/mi301-stop-term-robustness-2026-09-17.md` |
| **What IS a fan-out package's exit, when its sibling rows do not close together and do not agree?** Declares the answer once so no analysis decides it by input order: **the unit is the PACKAGE and no row is selected.** `package_exit_verdict` grades every sibling through the canonical `adjudicate_exit` and returns `unanimous` / `disagreement` / `ungradeable`, never collapsed — a **disagreement is REPORTED, never arbitrated**, because picking a winner among contradicting siblings is a choice wearing the clothes of a fact. A rate then comes back as a BAND (`rate_band`, `did_band`) counting every disagreement both ways. ⚠️ **`did_band` is a real FOUR-cell difference-in-differences** — an earlier draft computed `treated − control` and called it a DiD, the sub-class-A defect committed inside the module written to refuse it; controls 37/38 exist because planting that defect initially tripped NOTHING (every fixture had both `pre` cells at zero, where the two formulas coincide). ⚠️ **The band bounds disagreements ONLY, never the `ungradeable` exclusion**, whose count ships beside it. `row_selection_spread` reproduces the three row-selection rules **only** so the measured spread stays checkable, and `spread_verdict` states in its own output that three selections agreeing **closes nothing** (they agree whenever no fan-out package has divergent closes — a property of that population, not of the code); `earliest`/`latest` on rows with no `closed_at` return **`unorderable`**, never a silent fallback to population order, which would reintroduce the defect. **`--restate-u15` re-states MI-278 U15's DiD importing U15's OWN arm splitter unmodified: band +3.8pp to +29.6pp, sign survives, containing all three published points (17.9 / 21.2 / 28.2pp) and wider than their spread; 12 self-contradicting packages, 10 of them in the CONTROL arm; and U15's own code no longer reproduces U15's own table (`control_pre` 30 today vs 31 published).** One journal pull, no network; `--self-test` asserts 54 controls and the harness is shown able to fail against five planted defects | `scripts/research/fanout_exit_unit.py` (MI-278 U21; `BL-20260912-WHICH-FANOUT-ROW-IS-ROWS-0-MOVES-THE-STOP-OUT-ADJUDICATION-AND-SWINGS-THE-HEADLINE-DID-BY-10-POINTS`) → `docs/research/m20-u21-fanout-exit-unit-2026-09-12.md` |
| **Do the analyses that compute fan-out package rates actually SHARE one definition of a package's exit?** They do now. `stop_width_counterfactual_2026_09_11.py::_per_package_rates` held a verbatim second copy of the unanimous/disagreement/ungradeable rule and now calls `fanout_exit_unit.package_exit_verdict`; `stop_integrity_both_arms.py::build` carries `package_verdict`/`package_exit` per unit and `census` a `pkg_stop_rate_low`/`_high` band plus `pkg_disagreement`, **beside** the published row-selected rates rather than instead of them, so the `closed_at` sort reads as the CHOICE it is. ⚠️ **The classification is shared; the POLICY is not, deliberately** — what a package's exit IS belongs to the declaration, what a rate does with a contradiction belongs to the caller, and the two callers legitimately differ. **Additivity is PROVEN, not asserted**: pre- and post-patch modules loaded side by side over one 1000-row pull and compared key-by-key — counterfactual byte-identical (0 added / 0 changed / 0 removed), integrity module 356 added / 0 changed / 0 removed. ⚠️ **Byte-identical means the duplicate had not yet drifted — a FUTURE hazard removed, not a PRESENT disagreement fixed.** The new bands reproduce U21's restatement through a different code path, and show the published `31` in U15's control_pre is exactly that cell's band-high, so it needs no data change to explain | `scripts/research/stop_width_counterfactual_2026_09_11.py` + `scripts/research/stop_integrity_both_arms.py` (MI-278 U22; `BL-20260912-WHICH-FANOUT-ROW-IS-ROWS-0-MOVES-THE-STOP-OUT-ADJUDICATION-AND-SWINGS-THE-HEADLINE-DID-BY-10-POINTS`) → `docs/research/m20-u22-wire-fanout-exit-unit-2026-09-12.md` |
| **Which LABEL STATE is a close actually in — did the exit classifier run, decline, or never reach it at all?** Four never-collapsed states from `notes.exit_reason_source`: `attested` / `unresolved` (ran, LOOKED, declined) / `refused` (declined because the price was fabricated) / **`unattested` (no key — `order_monitor` calls this 'a 100% signature that none had ever reached the classifier')**. ⚠️ **`parse_notes` exists because the provenance is NOT columns** — `trades` returns 41 and none is `exit_price_source`; reading the schema dump and concluding the provenance is gone is the misread this function documents. The proposed-field probe reports a zero **only beside a positive control** and returns `not_gradeable_no_control_field_present` otherwise, so an absent field can never be reported off a broken reader. **Run on the M20 window it splits the row's single 'unattributable' count in two: `reconciler_filled` 29/29 `unresolved` against `netting_attributed` 7/7 `unattested`, which need different fixes; and it shows the proposed `stop_order_type` / `cancel_type` absent on all 362 closed rows against controls at 357/325/186.** ⚠️ Control 3b reads `EXIT_LABEL_REFUSED_UNMEASURED` textually out of `src/runtime/provenance.py`, because planting a rename tripped NOTHING while the docstring promised it would — the constant is duplicated as a literal here so the module runs without importing the runtime, and that is only safe if a rename BREAKS here. `--self-test` asserts 24 controls; five planted defects, two of which exposed real gaps | `scripts/research/exit_label_state_census.py` (MI-278 U24; `BL-20260912-FOR-55-PERCENT-OF-WINNERS-THAT-MISSED-TARGET-THE-MECHANISM-THAT-STOPPED-THE-RUN-IS-NOT-IN-THE-JOURNAL`) → `docs/research/m20-u24-exit-label-state-census-2026-09-12.md` |
| **Does `/api/bot/performance` publish an `expectancyR` that agrees in sign with its own PnL?** The runnable form of `PB-20260906-R-CONTAMINATION-QUANTIFIED-AND-THE-HEADLINE-SIGN-IS-WRONG`'s criterion (1), which previously required re-deriving the whole journal decomposition by hand. Four never-collapsed states — `agrees` / `disagrees` / `not_gradeable_zero` (a term is exactly 0.0, so there is no sign to compare) / `not_gradeable_absent` (*we could not look*). Reports every term it used, plus `rCoverage` and the **contaminated SHARE** beside the raw count. ⚠️ **`agreement_is_not_evidence_of_a_fix` ships on every graded window** — a window can agree because its contamination is diluted, or because it has none, while the aggregate still counts contaminated R the broken way. **`count_predicts_the_flip` is an explicit control and returns `None`, never `False`, when there is nothing to compare.** **Run live 2026-09-12 it finds the defect STILL PRESENT on `30d` (−14.5552 PnL against +0.1113 expectancyR, `rCoverage` 1.0, 26.8 per cent contaminated) while `all` agrees on MORE contaminated rows (3.3 per cent share) — so the flip tracks share, not count.** `--self-test` asserts 21 controls; five planted defects, all five caught | `scripts/research/performance_sign_agreement.py` (MI-278 U25; `PB-20260906-R-CONTAMINATION-QUANTIFIED-AND-THE-HEADLINE-SIGN-IS-WRONG`, `PB-20260821-R-AND-DOLLARS-DISAGREE-IN-SIGN`) → `docs/research/m20-u25-performance-sign-agreement-2026-09-12.md` |
| **What was this memo's table computed FROM, and if a re-run disagrees, WHY?** Gives a journal-derived memo a declarable two-part fingerprint of its input: `rowset_digest` (order-INDEPENDENT, over a **MULTISET** — deduplicating would let a pull that lost a duplicate row hash identically to one that did not, silently and in the reassuring direction) and `order_digest` (the id SEQUENCE). **Five compare states, never collapsed**: `reproduces` · `reordered` (hypothesis (a) of the row it discharges) · `content_changed` (hypothesis (b)) **whose changed rows are NAMED only when BOTH sides carry per-row digests** — ⚠️ **CORRECTED 2026-09-17 by MI-278 U51, and the uncorrected wording was false in two opposite directions.** `render_stanza` emitted no per-row digests, so a comparison against a MEMO — the only one a later session can make — derived its answer from an absent map: against a live pull it reported the ENTIRE population as `rows_added`; against another memo (measured on the real U32/U38 pair, a genuine re-stamp) it reported `rows_changed/added/removed` all `[]` beside `rows_not_localisable: 0`. `compare()` now returns `None` with a four-state `localisation` verdict, `rows_basis` says whether an answer came from `row_digests` or from `rowset_digest_equality` (which entails the empty lists on `reproduces`/`reordered` with no per-row map at all), and `render_stanza(include_row_digests=True)` — opt-in, with the prefix length DECLARED and the observed side trimmed to it — buys the naming back · `undecidable_field_sets_differ` (**a digest over a different field set answers a different question, so a renamed column lands here and is never reported as a data change**) · **`no_declared_input`** — *we could not look*, and the state **every** memo in the repo was in. ⚠️ **`shared_order_preserved` exists because `also_reordered` is CONFOUNDED WITH POPULATION CHANGE, and the confound was found by running on live data rather than by reading the code** — a tail pull that gained and lost rows moves `order_digest` even when every survivor kept its place, and the first live run reported `also_reordered: True` on exactly that, which reads as support for hypothesis (a) and is not. ⚠️ **It does NOT attribute the historical 30-vs-31 and says so in its own docstring** — U15's pull was not retained; this makes the NEXT difference attributable, which is the row's own second resolution path. **Census over 337 memos at `origin/main` b209768c2: `declares_input` 0 · `journal_cited_no_declaration` 66 · `not_detected` 271 — and `not_detected` is NOT called *not journal-derived*, because the probe reads prose and a memo that used a pull without saying so is indistinguishable from one that never touched the journal.** **⚠️ AND IT FOUND A THIRD CAUSE THE ROW DOES NOT NAME: on two real pulls 7.4h apart, 0 of 998 shared rows changed and their relative order was identical, yet `pkg-2f8c3a901ae84be1` went 3-row → 2-row because row 4729 fell off the `limit=1000` tail — nothing re-stamped, nothing reordered, the WINDOW moved.** `group_field` is off by default and grades `not_declared` rather than *no group changed*. One journal pull, no network of its own beyond it; `--self-test` asserts 64 controls and the harness is shown able to fail against fifteen planted defects, two of which had to be re-armed because the PLANT was ineffective | `scripts/research/memo_input_provenance.py` (MI-278 U26; `BL-20260912-U15S-OWN-CODE-NO-LONGER-REPRODUCES-U15S-OWN-PUBLISHED-TABLE-ONE-CONTROL-PACKAGE-FLIPPED-AND-NOBODY-WOULD-HAVE-KNOWN`, `BL-20260912-A-1000-ROW-TAIL-PULL-CHANGES-A-FANOUT-PACKAGES-MEMBERSHIP-WITHIN-HOURS-SO-A-PACKAGE-LEVEL-RATE-OVER-A-TAIL-IS-UNSTABLE-BY-CONSTRUCTION`) → `docs/research/m20-u26-memo-input-provenance-2026-09-12.md` |
| **Has this backlog row already been answered — and does the pooled answer survive its own stratification?** Grades a row's resolution criteria ONE AT A TIME against the artifacts, in **four never-collapsed states**: `met` · **`met_by_another_route`** (satisfied, but not the way the row specified — recorded rather than smoothed, because a reader following the row to the named file will not find it there) · `unmet` · **`ungradeable`** (*the artifact could not be read* — never `met`, and never `unmet` either, and it BLOCKS the close). ⚠️ **It exists because the study the row demands had already run**: `trade-prioritisation-research-DESIGN.yaml` carries `status: answered / 2026-09-08`, so building the harness again would have been `RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED`. **Results: all four criteria met (one by another route — the arm is on `nbook_portfolio.py`, not the `backtest_system.py` the row names), so the row closes on its own text (*'a null result closes this row'*).** ⚠️ **AND THE POOLED HEADLINE HIDES WHAT CRITERION (3) EXISTS TO EXPOSE**: `confidence_first` is −0.278R pooled but **−0.1361R over the 864 trades confidence DECIDED and −0.739R over the 266 that fell through**, so **81% of the arm's whole loss sits in the 24% it could not decide** — flagged in the output as a WITHIN-ARM composition note and never as an effect, since the strata are different contests. ⚠️ **It also re-measures the tie rate on DISJOINT slices and the obvious explanation is REFUTED**: 190/370 = 0.5135 on the published window against **90/115 = 0.7826 since**, while the saturating family's SHARE of observations FELL 0.482 → 0.267 and its OWN 1.0 rate ROSE 0.420 → 0.677 — fewer saturating legs contending, saturating harder. **The 1.0s attribute to 7 of 26 strategies, six of them `trend_donchian` legs or `mgc_trend_1h`**, which makes the open clamp-vs-default-vs-real question a question about ONE builder. A positive control reproduces the published 0.501 as 0.5135 and the 1.1pp gap is reported, not smoothed. One soak read, no network of its own; `--self-test` asserts 22 controls and the harness is shown able to fail against seven planted defects | `scripts/research/prioritisation_row_discharge.py` (MI-278 U28; `BL-20260831-TRADE-PRIORITISATION-IS-UNPROVEN-CONFIDENCE-IS-A-START-NOT-A-RESULT`, `BL-20260831-CONFIDENCE-SATURATES-AT-ONE-SO-HALF-OF-ARBITRATIONS-CANNOT-BE-DECIDED-ON-IT`) → `docs/research/m20-u28-prioritisation-answered-and-the-ties-got-worse-2026-09-13.md` |
| **Does an existing walk-forward evidence base actually LICENSE the decision it is about to be quoted for?** Grades whether a factor can be held apart from its strata before anyone reads a rate off it. Four never-collapsed separability states — `separable` / `confounded` / `single_level` / `not_present` — plus three dose states, so *'we cannot compare these'* never renders as *'we compared them and found nothing'*. Reports the candidate rate **with its denominator**, and the data-volume confound (`usable_folds` / `n_oos` by verdict) beside it. **Run on the M20 arm set (246 arms, 33 legs, 3 families) it REFUSES: `tp_geometry` is perfectly confounded with `family` and `block_unit`, no family carries both geometries, every arm shares one `tp_cap_pct` of 0.099, and the 75%-vs-30% candidate-rate gap is the same 28 scalp arms wearing a geometry label.** ⚠️ **A refusal always carries its reason, and control 3 is the positive control that makes the refusal meaningful** — adding one arm inside an existing family flips the verdict to `separable` and NAMES the rescuing stratum, so the probe is not one that can only say no. `--self-test` asserts 19 controls and the harness is shown able to fail against four planted defects | `scripts/research/m20_target_geometry_evidence_audit.py` (MI-278 U23) → `docs/research/m20-u23-tier3-proposal-target-geometry-2026-09-12.md` |
| **When the journal cannot say what ended a trade, can the VENUE name the mechanism?** Joins a journal close to `/api/diag/bybit_raw_order_history` through the `order_id` that `/api/diag/bybit_raw_closed_pnl` carries — two endpoints, because neither answers alone: the closed-PnL feed gives the realised close and the order id but **no order type**, and the order-history feed gives `stop_order_type` / `cancel_type` / `order_type` / `order_status` / `trigger_price` but cannot be reached without that id. ⚠️ **The trade→close matcher is IMPORTED from `netting_close_venue_adjudication` (MI-278 U9), never rebuilt** — two copies of "is this the same close?" would be free to drift, and that module already records that grading matches on `avg_entry_price` **reverses the sign** of its own aggregate. **Four states, never collapsed**: `named` · `plain_order` (present, empty `stop_order_type` — an ordinary close, a real answer) · `absent_from_order_history` (*we could not look*) · `no_corroborated_match` (the join, not the venue, is what failed). **Results over the 17 winners in MI-278 U2's three no-mechanism classes (all `bybit_1` paper, closes 2026-08-27→2026-09-09): the venue answers 11 of 17 (64.7%) — 10 `named`, 1 `plain_order`, `absent_from_order_history` ZERO** (so the order history is never the bottleneck; all 6 failures are at the closed-PnL join), **7 of the 11 are stop fires**, and **5 of the 11 are LOSSES at the venue while the journal calls them winners (journal +$358.94 vs venue −$2,095.68)**. Reports `cancel_type: UNKNOWN` as CORRECT rather than missing when the row is `Filled`. Makes **no network call of its own** (prints the `diag_fetch` plan — Bybit caps a closed-pnl window at 7 days); `--self-test` asserts 10 controls | `scripts/research/venue_mechanism_recovery.py` (MI-278 U10; the prior question behind `BL-20260912-FOR-55-PERCENT-OF-WINNERS-THAT-MISSED-TARGET-THE-MECHANISM-THAT-STOPPED-THE-RUN-IS-NOT-IN-THE-JOURNAL`) → `docs/research/m20-u10-venue-mechanism-recovery-2026-09-12.md` |
| **Is this leg structurally capable of producing a live closed-trade observation — and does an untreated within-family control for a shipped geometry experiment exist?** Censuses every leg's `execution` and every account it routes to against that account's `mode` and `account_class`, then searches for same-family legs the experiment did NOT touch. ⚠️ **The treated set is diffed out of the SHIP COMMIT itself, never read off the inline YAML comments** — the commit is the event and a comment is a claim about it; and **family comes from the e35 sweep's own corpus**, so the module and the sweep cannot disagree about what a family is. **Six reportability states, never collapsed**: `can_report` · `blocked_shadow` (cannot open a position however routed — graded FIRST, so the binding cause is the one named) · `blocked_prop_only` (fills land in `prop_tickets`/`prop_fills`, isolated from `trades` by design) · `blocked_dry_run_only` · `blocked_unrouted` · `unknown_execution` (*we could not look*). A leg with no family grades `family_unknown`, never *no control exists*. **`--power` grades what a dose-response against a NAMED control could detect** (minimum detectable difference, two-sided Fisher), reporting `None` — *nothing is detectable at any effect size* — rather than a large number, and counting unattributable exit labels instead of silently dropping them. It **refuses** a control that cannot report and **refuses** a cross-family control arm, because a within-family control only controls its own family. **Results over the 55 legs at `origin/main` 01aa8d367: 42 can report; both of e35's declared controls are `blocked_prop_only`; `trend_donchian_eth` (ETHUSDT 1h, held at 2.5, 8 closes after the deploy) is the reporting twin of the declared control; and the test is blocked not by n but by 16 of 23 treated closes not recording what ended them.** Reads config and one journal pull; makes no network call of its own; `--self-test` asserts 18 controls | `scripts/research/e35_control_reachability.py` (MI-278 U11; `BL-20260911-BOTH-OF-THE-E35-EXPERIMENTS-DESIGNED-CONTROLS-HAVE-ZERO-OBSERVATIONS`) → `docs/research/m20-u11-e35-controls-that-can-report-2026-09-12.md` |
| **Can the e35 dose-response be GRADED once the venue is asked for the mechanism the journal did not record — and if not, what is the ceiling on the retrospective route?** The direct successor to the row above: U11 established the control reports, this asks whether the OUTCOME can. Builds both arms (treated diffed out of ship commit `892c9a2c8` itself, never the inline comments; control re-verified HELD at `atr_stop_mult: 2.5` at three refs, because a control that moved is not a control), then sends every close the journal could not name to the venue and folds the two witnesses into one verdict. **Five adjudication states, never collapsed**: `journal_named` · `venue_named` · `venue_plain_order` (the venue ANSWERED: an ordinary order, not a bracket leg) · `unnameable_venue_silent` (corroborated but absent from order history) · `unnameable_no_match` (*no corroborated venue row to join from*) — and a self-test control DRIVES all five, so a state that can never be reached is caught rather than declared. ⚠️ **`_is_stop` returns `None` for an unnameable close, never `False`** — folding it into `False` is the substitution that makes an unnameable population read as evidence against the mechanism. ⚠️ **The headline is a SENSITIVITY BAND, and the module refuses to print the p-value without it**: a rate over the gradeable subset is a rate over a population selected by whether anyone could name the close, which is correlated with the outcome. The verdict is `gradeable` only when the band excludes the null under sign-survival — deliberately the weakest reasonable test, inventing no alpha. **The venue is asked through U10's `recover()` and U9's `load_venue`, IMPORTED UNMODIFIED**, so there is one answer to *what does the venue call this close*. **Results on the 2026-09-12T20:0xZ pull (27 treated / 12 control closes, 28 of 28 venue windows, 0 errors): recovery returns 7 of 21 (33%) — 4 of 17 treated against 3 of 4 control — so treated goes 10 → 14 of 27 gradeable and 13 stay unnameable; the subset reads 0.786 vs 0.364 at p=0.0486, and the band [0.407, 0.889] vs [0.333, 0.417] OVERLAPS with the worst case reversing the sign at p=1.0. Verdict `not_gradeable_missingness_dominates`.** `--fetch-plan` emits the diag lines; `--self-test` asserts 45 controls | `scripts/research/e35_dose_response.py` (MI-278 U18; `BL-20260911-BOTH-OF-THE-E35-EXPERIMENTS-DESIGNED-CONTROLS-HAVE-ZERO-OBSERVATIONS`) → `docs/research/m20-u18-e35-dose-response-still-not-gradeable-2026-09-12.md` |

## 4. Entries

| Question | Tool |
|---|---|
| Where do a leg's entries lose? | `scripts/research/m21_entry_baseline.py` |
| Fleet entry-filter sweep | `scripts/research/m21_entry_sweep.py` |
| Fleet P_win entry-head round | `scripts/research/m21_entry_head_round.py` |
| Component-level edge attribution | `scripts/research/component_edge_report.py` |

## 5. Panels + standing discovery (the large-N substrate)

| Question | Tool |
|---|---|
| Analysis-ready research panel | `scripts/research/build_research_panel.py` |
| Large-N panel from **backtests** | `scripts/research/build_backtest_panel.py` |
| Analyze a panel (standing discovery toolkit) | `scripts/research/analyze_research_panel.py` |
| Sweep panels per strategy / asset class | `scripts/research/sweep_research_panels.py` |
| Runner (off-VM) | `.github/workflows/research-panel-build.yml` |
| **Augment the pooled decision models** — config-exact harness backtests → `is_backtest=1` rows in one `backtest_trades.db` (A1 W1.2; roster verified vs `setup-candidates-metalabel-p2pool-v1.yaml`) | `scripts/ml/backtest_augment_runner.py` (runner: `.github/workflows/research-backtest-augment.yml`) |
| Emit ONE config-exact harness `(strategy, symbol)` as a per-trade JSONL (the shared fetch→`--emit-trades` primitive behind the augment runner + the GLD compat gate) | `scripts/research/regime_debt_matrix.py::emit_trades_for` |
| **How FAR BACK can we actually GET the candles?** Measures Dukascopy's depth per instrument behind the blocked non-crypto legs. Its sibling `dukascopy-coverage-probe.yml` answered EXISTENCE and `docs/research/dukascopy-coverage-adjudication-2026-08-24.md` adjudicated the mapping; both closed on the caveat recorded in `ROADMAP.md` — *"Existence is not span, and span is the actual question."* The consumers ask for DEPTH: `e35-bracket-sweep.yml` fetches `days: 1830` per leg, and yfinance REFUSES a >730 d 1h request outright (proof run 32734360738). ⚠️ **FOUR states, never collapsed** — `bars` / `empty` (we looked; the venue has nothing) / `error` (**we did not look**, never folded into `empty`) / `unknown_instrument` (a MAPPING bug, not a depth fact); `earliest_bars_anchor` always ships beside `anchors_errored` and any error downgrades the verdict to `lower_bound_some_anchors_errored`, so the number cannot be quoted without its caveat. ⚠️ It does NOT decide substitutability — every row carries the adjudicated `relation`, and `QLD`/`TQQQ` are absent because the adjudication REFUSED a proxy (daily leverage reset ⇒ path ≠ N× underlying). Feeds `BL-20260824-E35-SWEEP-FEED-IS-BINANCE-ONLY-FOR-A-MOSTLY-NON-CRYPTO-MATRIX`. 11 self-test checks, injected fake fetcher, no venue call | `scripts/research/dukascopy_span_probe.py` (`.github/workflows/dukascopy-span-probe.yml`) |

## 6. Portfolio / allocation / conflict

| Question | Tool |
|---|---|
| Cross-symbol allocator backtest | `scripts/research/allocator_multisymbol_backtest.py` |
| Per-candidate (features → forward net-R) dataset | `scripts/research/allocator_candidate_dataset.py` |
| Walk-forward ranker quality | `scripts/research/allocator_ranker_eval.py` |
| Cost of directional conflict between legs | `scripts/research/m26_p0_conflict_bleed.py` |
| Net-R re-grade scorecard | `scripts/research/net_r_regrade.py` |
| **What conflicts does the OPEN BOOK contain, read as a book rather than trade by trade?** Five kinds, counted separately and never blended: `opposing_same_symbol`, `self_opposing_strategy` (ONE strategy holding both directions -- the netting guard keys on (account, strategy, symbol) and cannot see this), `mirror_stop_divergence` (legs of one signal at an identical entry now carrying different stops), `correlated_opposition`, `nominal_stop`. Correlation is **supplied and measured, never assumed** -- unmeasured pairs are reported by name as `unmeasured, NOT uncorrelated` rather than folded into 'no conflict'. Exists because every exit lever is a univariate cut on ONE trade's path and none can ask 'is this the best use of this capital'. MEASURED 2026-08-18 on 35 live positions: 19 findings, none visible on any prior surface | `scripts/ops/portfolio_conflict_audit.py` (module: `src/runtime/portfolio_conflicts.py`) |
| **How many `(account, symbol)` pairs hold >=2 SIMULTANEOUSLY-OPEN journal rows, and has a whole-symbol close ever over-closed a sibling?** The precondition for `BL-20260907-ALPACA-CLOSE-OF-ONE-TRADE-LIQUIDATES-ITS-SIBLINGS`, counted rather than assumed, plus the journal-open vs `exchange_positions` diff MI-167 asked pass 3 to build. Every negative carries its positive control (0 Alpaca close-with-open-sibling events against 187 on Bybit; 0 unbacked Alpaca rows against 3 venue positions with no journal row). READ-ONLY -- it touches no close path. ⚠️ Its population is a CAP, not a window: `GET /api/diag/journal` has no `offset`/`since` and clamps to 1000 rows, so no figure it prints is all-time. MEASURED 2026-09-07: 1 of 12 pairs is Alpaca, 0 over-close events | `scripts/research/mi171_alpaca_overclose_population.py` (verdict: `docs/research/alpaca-over-close-population-2026-09-07.md`) |
| **Did a CONFIG CHANGE break a book, or did the market?** The e35/2026-08-30 attribution. Splits closed trades into geometry groups and eras and reports stop-out rate, win rate and PnL for each, with 95% Wilson intervals and Fisher exact tests including the FALSIFIER (were the groups already different BEFORE the change?). Three instrument repairs are built in because without them the answer is wrong: it splits on `created_at` (a leg carries the geometry it was OPENED under, never the close date); it ADJUDICATES exit location from each row's own declared levels rather than trusting `exit_reason` (`reconciler_filled` is the frozen label of `BL-20260822` and was 40.5% of the population, 42 of 145 of which HAD reached a bracket) and it runs a POSITIVE CONTROL on that adjudicator before using it; and it builds the control from legs declaring NEITHER `atr_stop_mult` NOR `tp_r`, i.e. structurally immune rather than merely un-edited -- which is what caught the 2026-08-29 B4 geometry change sitting inside what every prior framing called its control. States are never collapsed (`ungradeable_no_price` / `_no_levels` are *we did not look*, never `neither`). ⚠️ Its population is a CAP, not a window: `GET /api/diag/journal` clamps to 1000 rows with no `offset`, so a trade opened before the oldest id but closed inside the window is ABSENT. MEASURED 2026-09-11 | `scripts/research/bleed_attribution_2026_09_11.py` (verdict: `docs/research/bleed-attribution-2026-09-11.md`) |
| **Would the OTHER stop width have been hit? The counterfactual behind a revert decision.** Answers, for a bracket-geometry change, whether the pre-change stop would have ended the same trades -- which is the question a stop-out RATE cannot answer. TWO ARMS, and the second is the strong one: FORWARD (post-change stop-outs, would the WIDER stop have been hit?) needs price action after the real exit and so needs a HORIZON, reported at 24h/72h/168h/to-data-end rather than at one invented cap; REVERSE (pre-change trades, would the TIGHTER stop have been touched inside the trade's OWN observed lifetime?) **needs no horizon at all**, because the trade bounds its own window, and its damage cell is directly decision-relevant -- a past WINNER the new stop would have killed. ⚠️ **THE RECORDED TELEMETRY CANNOT ANSWER THIS AND NEITHER CAN ANY IN-TRADE INSTRUMENT:** `position_telemetry` holds `peak_r` (maximum FAVOURABLE excursion) and no maximum ADVERSE excursion, and is an upsert, so there is no path -- and the question needs the adverse path AFTER the position closed. So it re-simulates from 1m candles. Bybit and Binance are geo-blocked from the research container (the VENUE refusing on geography, HTTP 403/451, not the sandbox proxy), so the basis is OKX `*-USDT-SWAP` -- a perpetual, matching Bybit linear -- with a disk cache per (instrument, bar, UTC day). THREE POSITIVE CONTROLS RUN BEFORE THE ARMS and are reported whether they pass or fail: basis (does the journal's entry sit inside the 1m bar?), REPRODUCTION (can it reproduce the stop-out that actually happened?) and a false-positive bound. ⚠️ REPRODUCTION IS WHAT MAKES IT TRUSTWORTHY AND IT EARNED ITS KEEP: it caught that **the declared stop is anchored to the PACKAGE entry, not the trade's FILL**, so the script scales the entry-frozen `exit_plan.stop` DISTANCE by the multiplier ratio instead of recomputing `atr * mult` -- preserving whatever anchor, clamp and rounding the bot used. It also separates stop-outs at the ENTRY-DECLARED stop from those at a stop a lever had already TIGHTENED (reverting an entry multiplier cannot recover the latter), measures adverse/favourable excursion in ATR UNITS over FIXED windows from entry so the regime read carries no truncation confound, and counts per ORDER PACKAGE rather than per trade row, because account fan-out shares one price path. MEASURED 2026-09-11: 5 of 5 forward, 1 of 14 winners reverse | `scripts/research/stop_width_counterfactual_2026_09_11.py` (verdict: `docs/research/stop-width-counterfactual-2026-09-11.md`) |
| **WHY did the average win fall? Decomposing a PnL move into size, exit distance, composition and regime — and grading whether the move is MEASURED at all.** Uses the exact identity `mean(pnl over winners) = mean(risk_usd) * mean(R) + cov(risk_usd, R)`, valid because `risk.py::_size_unbounded` sizes every trade to `balance * risk_pct / |entry - sl|`, and attributes the CHANGE across those terms with the interaction reported **separately** rather than distributed by an unstated convention. ⚠️ **THE RISK BASIS MUST BE `order_packages.sl`, NEVER `trades.stop_loss`** — the latter is the FINAL TRAILED stop and it sits in a DENOMINATOR here, so one trailed winner moved a mean R from 7.3 to 102.6; a prior session withdrew a finding to the same trap (`BL-20260908-SL-CROSS-LABELS-NINE-CLOSES-THAT-EXITED-IN-PROFIT`). ⚠️ **A WINNERS-ONLY READ OF RISK IS A SELECTION EFFECT** and points the OPPOSITE way to the full book — always report both. Ships its own honesty terms: a provenance split of winners with a standardisation to the other era's coverage mix (so a coverage shift cannot be mistaken for a performance move), a shift-share over legs that separates a MIX change from a LEG change, a venue-cap pile-up test, an exit-lever test, and PRICE-FREE metrics (hold time, win rate) that no exit-booking defect can manufacture. `contract_value == 1` is asserted per account rather than assumed. Use it whenever a PnL or expectancy move needs attributing; it is NOT an entry-side tool and produces no entry evidence. | `scripts/research/winner_size_collapse_2026_09_12.py` |
| **Was this order's size a RISK decision, or the MARGIN CEILING — and can we say so PER ORDER without a new stamp?** Proves it equity-free: the risk sizer demands `equity x risk_pct x entry / risk_per_unit` and the margin pre-flight cap allows `basis_usd x leverage x buffer`, and since EVERY basis the sizer may use (`venue_available`, `equity_unadjusted`, `free_balance`) is at most equity, the equity term CANCELS — `risk_pct x entry / (risk_per_unit x leverage x buffer) >= 1` proves the cap binds AT ANY BALANCE. Equivalently the cap binds below `10000 x risk_pct / (leverage x buffer)` bp = **55.56 bp** at 1.5%/3x/0.9. ⚠️ **SUFFICIENT, NOT NECESSARY, and `provableCoverage` is shipped WITH the instrument so the bound rate is never quoted alone** — the inequality is slack by `equity / basis_usd`, so `not_provable` is ***we did not look***, never *risk-derived*. ⚠️ **It reads `risk_per_unit` through `r_provenance.declared_initial_risk`, never `|entry - sl|`: `order_packages.sl` IS amended too** — measured over 677 packages, 543 unamended / 123 trailed toward entry / **11 trailed PAST entry onto the profit side** (11 of 11, cross-checked by direction), so one order in five is mis-measured by the distance. ⚠️ **A SECOND, ORTHOGONAL AXIS (`qty_provenance`, five states) keeps three non-sizer populations out of the verdict** — `intent_reduce` (the multiplexer's reduction qty), `adopted_orphan` (a venue-read size) and the **pairs sleeve** (an isolated order path that never calls `RiskManager.position_size`); on the live pull they are 320 of 707 rows. That axis is what WITHDREW this lane's own paused claim that inverting `position_size x |entry - sl| / risk_pct` recovers decision-time equity to ~0.5%: the agreeing row was a PAIRS leg with a sentinel stop at 2x entry, and the agreement was numerology. ⚠️ **Futures are REFUSED, not graded** (`position_size` skips the crypto margin cap there). Prints a RECONCILIATION of the per-strategy table against its own census, which is how its author's first cut was caught reporting 62 bound orders against a census of 43. MEASURED 2026-09-17 on `bybit_1` (n=274 gradeable): 43 provably bound, provableCoverage 0.2445, control SOUND with **zero false positives** on the 32 stamped rows — and the split is a FAMILY property, `ict_scalp_*` 42 of 43 against 1 for the other 16 legs | `scripts/research/margin_bound_sizing_basis.py` (MI-278 U45; `PB-20260822-AVAX-SCALP-SIZED-OFF-MARGIN-NOT-RISK`) -> `docs/research/m20-u45-the-size-is-the-ceiling-and-the-stop-is-why-2026-09-17.md` |
| **Can the rows in an R aggregate register an R AT ALL -- a question `rCoverage` cannot ask?** `rCoverage` answers *does this row have a risk denominator*; it does not answer *is that denominator a risk distance*, and on this fleet the two come apart. MEASURED 2026-09-17 over 1000 order_packages: the PAIRS distribution of `|entry - sl| / entry` runs 0.5000 -> 1.0000 (median 1.0000, 162 of 323 at exactly 1.0) while every other leg runs 0.0001 -> 0.1440 (median 0.0107) -- **the two do not overlap**, and 0 of 323 pairs packages carry a `risk_per_unit`. A stop at 50-100% of price is a SENTINEL, so its R is a return on NOTIONAL. **On the live paper block that is 312 of 631 rows (49.4% of the R population) contributing a mean +0.00008 R and 0.4% of the block's |PnL|, while rCoverage reads 1.0000** -- and because expectancyR divides by the row COUNT the reported +0.01504 is **1.97x understated** against the +0.02968 the admissible rows produce. [!] THIS IS THE MIRROR OF PB-20260821's DEFECT AND POINTS THE OTHER WAY: that row feared a denominator collapsing so R EXPLODES (its worst case 206.920 R/trade); a sentinel makes R VANISH, which is quiet, looks healthy, and can never trip a bound that only limits R from above. **Three axes, never collapsed** -- `basis` (5 states, read from the endpoint's own `rBasis`, never re-derived) x `scale` (`risk_level` / `notional_sentinel` / `unknown`) x `admissible` (only when both are known). [!] **WITHOUT `--packages` IT PUBLISHES NO ADMISSIBLE SHARE AT ALL** and says `SCALE NOT GRADED -- we did not look`: deriving one from `rBasis` alone would repeat the exact collapse it exists to expose. A MIXED leg grades `unknown` and is NAMED rather than rounded; an ungraded leg counts toward neither bucket. The restated aggregate prints BESIDE the reported one, never instead, and a per-leg-vs-block reconciliation prints on every run. The 0.25 sentinel bar is a CHOSEN value sitting in the measured empty gap between the two distributions, and is a flag. Also answers PB-20260821's clause 1 with a STATED bound: over 57 leg-rows across all four blocks the largest |expectancyR| anywhere is **2.3586**, against filed outliers of 62.6 / 63.183 / 73.697 / 206.920 -- clause 1 PASSES, because `rBasis.declaredInitial` is now 100% on every non-pairs leg. It proposes NO endpoint change: what an R aggregate should exclude is Tier-3 | `scripts/research/r_denominator_admissibility.py` (MI-278 U46; `PB-20260821-R-AND-DOLLARS-DISAGREE-IN-SIGN`) -> `docs/research/m20-u46-half-the-paper-r-population-cannot-register-an-r-2026-09-17.md` |
| **Why does realised per-trade RISK disperse across legs on ONE account with ONE declared `risk_pct`?** Closes the question by ALGEBRA rather than hunting: `risk_usd = position_size x risk_per_unit = balance x risk_pct` EXACTLY on an unclamped row, and a leg's dollars-per-R is a risk-weighted mean of `risk_usd` -- so on a single-`risk_pct` account the dispersion is either a CLAMP or the BALANCE, and the clamps are a FINITE list readable inside `position_size`. **Four verdicts, never collapsed**: `unreachable` (the mechanism CANNOT run here, established by reading config or code -- stronger than any correlation) x `refuted` x `contributes` x **`untestable` = *we could not look*, which is neither a pass nor a refutation**. [!] **NARROWING THE FIELD IS NOT AN ATTRIBUTION** and the `verdict` string says so: a run where every candidate is unreachable/refuted/untestable returns `no_candidate_survives -- nothing is attributed`, never silence that reads as a result. MEASURED 2026-09-17 on `bybit_2` (n=33 closed rows, 80.5% of the endpoint's 41, realised risk spanning 4.37x): **four candidates UNREACHABLE on a config read** (one declared risk_pct for all 6 legs; `confidence_sizing` off; bybit/linear so no whole-unit round-up; `policy_declared: False` on all 91 exposure-soak rows), **four REFUTED** (lot floor -- 33 of 33 sized rows are an EXACT lot multiple so flooring lost nothing; daily-loss budget r=+0.018; margin ceiling r=-0.073; free balance / concurrent positions r=+0.346 WITH THE WRONG SIGN), **one CONTRIBUTES** (netting rewriting `position_size` post-entry, 4 of 33, including the two most under-risked rows). [!] **THE EXHIBIT THAT LOOKED LIKE THE ANSWER AND WAS NOT:** per-leg median realised risk orders PERFECTLY by lot count (2, 3, 4, 8, 585, 996 lots -> 1.68, 2.98, 3.61, 3.69, 4.06, 4.58), six of six monotone -- and `floor(L)/L` is 1.000 on all 33 rows, so the lot floor lost nothing. A session that stopped at the table would have shipped a wrong cause behind a beautiful figure. [!] **WHAT SURVIVES IS UNREADABLE**: the residual IS the decision-time `balance_usdt`, recorded in `account_context_snapshots.equity` and absent from `diag.py::_JOURNAL_TABLES` (HTTP 400) -- so U45's one-line Tier-2 proposal is load-bearing for a SECOND high-severity row. Proposes no resize; per-leg risk is Tier-3 | `scripts/research/risk_dispersion_attribution.py` (MI-278 U47; `BL-20260913-EVERY-PROFITABLE-LEG-IS-SIZED-SMALLER-PER-UNIT-OF-RISK-THAN-EVERY-LOSING-ONE-ON-REAL-MONEY`) -> `docs/research/m20-u47-the-risk-dispersion-is-not-any-clamp-a-session-can-read-2026-09-17.md` |
| **Can a labelled axis VARY inside the unit it is compared across -- i.e. is the prescribed remedy for a confound even runnable?** Grades REACHABILITY rather than association: for each family, which `tp_geometry` labels are attainable at ANY cap, and does `--tp-cap-pct` actually REACH the harness. [!] IT IMPORTS AND CALLS THE REAL PRODUCERS -- `m20_fleet_exit_sweep.tp_geometry_for` (the declared single owner of the stamp) and `base_args`, whose argv is READ to determine flag delivery -- and REFUSES to grade if either cannot be imported, because a local re-derivation of their rule is precisely the drift it exists to detect. MEASURED 2026-09-17 over the complete 246-arm corpus: `tp_geometry` is COMPUTED from family membership in `CLAMPING_FAMILIES`={donchian,fade,pullback,squeeze} by the SAME predicate that gates the cap (`if tp_cap_pct > 0.0 and fam in LIVE_TP_CAPPED_FAMILIES`), so a clamping family attains {live_parity_capped, NO_TAKE_PROFIT} and NEVER live_parity_uncapped while scalp is uncapped at EVERY cap -- **clause A of the row's own resolution_criteria is `unsatisfiable_by_construction`, so that row can never close as written**. [!] **THE UNREACHABILITY IS PROVEN, NOT SAMPLED, AND THE PROOF ASSERTS ITSELF**: the cap enters the label ONLY through `cap > 0`, a sign test, so {0.0, any positive} is EXHAUSTIVE -- `cap_enters_only_as_a_sign_test()` checks that over 12 magnitudes spanning 1e-9..1e9 (40 random positives in the self-test) and a future size-dependent label FAILS it and downgrades every `unreachable_by_construction` to `unknown`. Read `sign_test.holds` before quoting any verdict. [!] It also settles a caveat the row left open: declared-vs-DELIVERED is **218 delivered / 28 recorded-but-never-delivered**, the 28 being the scalp arms whose `tp_cap_pct: 0.099` `base_args` dropped -- so a dose sweep on a non-clamping family is inert by construction. [!] And it bounds the attribution from inside: within the single `live_parity_capped` level, donchian 43.3% vs pullback 24.5% = an **18.8pp** family spread against a 44.7pp between-label spread, **within/between 0.42**; a one-family level reports spread `None`, never 0.0. Four never-collapsed pair states x three dose states, and an absent corpus publishes NO census rather than a clean one. Use it whenever a remedy is being prescribed for a confound -- it answers *can that arm be built* before anyone spends the run. Proposes nothing; target geometry is Tier-3 | `scripts/research/tp_geometry_reachability.py` (MI-278 U48; `BL-20260912-THE-M20-WALKFORWARD-ARM-SET-CANNOT-GRADE-TARGET-GEOMETRY-BECAUSE-TP-GEOMETRY-IS-PERFECTLY-CONFOUNDED-WITH-FAMILY-AND-BLOCK-UNIT`) -> `docs/research/m20-u48-the-geometry-contrast-cannot-be-built-2026-09-17.md` |
| Measured crypto correlation matrix (daily log returns, 90d + 365d, with provenance and a re-measure warning -- it is NOT stationary) | `comms/research/crypto_correlation_2026-08-18.json` |

## 7. Pairs / market-neutral (M22)

| Question | Tool |
|---|---|
| Is a pair still cointegrated? | `scripts/research/cointegration_stability.py` |
| Scan the universe for pairs | `scripts/research/pairs_universe_scan.py` |
| $-and-lots realism | `scripts/research/pairs_dollar_lots.py` |
| Perp-funding drag | `scripts/research/pair_funding_drag.py` |
| Maker-fee economics | `scripts/research/maker_economics.py` |

## 8. Macro / value / events

See [`macro-research`](../../.claude/skills/macro-research/SKILL.md) — it owns this half
(`scripts/macro/`, `src/units/strategies/macro_thesis/`, `comms/macro/`, the macro workflow
cluster). Binding invariants there: off-VM compute, point-in-time / no lookahead,
verify-the-source-before-you-build.

## 9. Robustness / significance gates (run these BEFORE proposing a live change)

| Question | Tool |
|---|---|
| Temporal stability of a directional edge (walk-forward) | `scripts/research/direction_walkforward.py` |
| Rank stability walk-forward | `scripts/research/rank_walkforward.py` |
| Fee/slippage sensitivity + 3-fold robustness | `scripts/research/validate_robustness.py` |
| Monthly net-R correlation / diversification vs the live fleet | `scripts/research/validate_corr.py` |
| Significance + robustness of sweep survivors | `scripts/research/ws_a_s3_significance.py` |
| Fee/commission headroom | `scripts/research/ws_a_s3b_fee_breakeven.py` |
| **Per-account** routing compatibility (mandatory before live routing) | `scripts/prop/account_compat_matrix.py` |
| Would the R4 research→results gate PASS/BLOCK/ABSTAIN a live leg on **measured** (not fabricated) PnL? (observe-only, P0) | `scripts/research/research_results_gate_report.py` (logic: `src/runtime/research_results_gate.py`) |
| **Was this leg's verdict measured under an exit production does not have?** Compares the harness's bar-count force-close (`timeout_bars`, default 200 trend/pullback, 48 squeeze — **no live unit for those families implements one**) against a relaxed arm at identical geometry, per leg. Grades `clean` (default provably inert ⇒ the base arm IS live-parity), `contaminated`, or `no_power` — never `clean` off silence alone | `scripts/research/timeout_binding_audit.py` (`BL-20260829-HARNESS-FORCE-CLOSES-TREND-PULLBACK-TRADES-ON-BAR-COUNT-AND-LIVE-NEVER-DOES`; write-up `docs/research/timeout-bars-harness-vs-live-2026-08-29.md`) |
| **Did a corpus re-sweep actually reproduce, and is the control I am checking even satisfiable?** Diffs two COMMITTED revisions of the e35 corpus per cell. ⚠️ Reports the naive identity check AND the fold-level reading that explains it, because the sweep fetches a TRAILING window (`days=1830` ending at run time) so BOTH data edges move and *numerical identity across days is unsatisfiable by construction* — a bare "N cells differ" is a false alarm. Also splits the base-vs-`to400` residual by the leg's PRIOR grade, since after the base-arm pin that comparison asks a different question and a long-hold leg differs correctly | `scripts/research/e35_resweep_verdict_diff.py` (write-up `docs/research/e35-resweep-verdict-diff-2026-08-29.md`) |
| **Is the coverage matrix's `bracket_geometry` verdict still what the CURRENT corpus says — and did a lever we already SHIPPED actually deliver?** Re-reads every matrix cell against the newest corpus run: stale refs (three-state — a ref with no date, or a leg with no rows, is UNDECIDABLE, never 'current'), `passed_unshipped` cells whose winner carries a `to<N>` axis no live unit can deliver, and legs whose direction REVERSED. ⚠️ `--b4-outcome` exists because the alarming reading is the wrong one: a `shipped` leg shows ZERO passing cells BY DESIGN (the base is config-exact, so the winner cannot beat itself) and the check that distinguishes that from a regression is whether the BASE rose by the claimed improvement — measured 3 of 8 exact to 4dp, all 8 within 0.89-1.03x | `scripts/research/e35_matrix_recheck.py` (write-up `docs/research/e35-matrix-recheck-2026-08-29.md`) |
| **Does this leg's KILL/DEMOTE case rest on rows anyone can stand behind?** Grades a leg's HEADLINE dollar loss against `totalPnlMeasured`, which sums MEASURED+ESTIMATED only (`performance.py:490`) — so a large headline loss whose admissible part is a GAIN is caught before it becomes a kill argument. Eight never-collapsed verdicts; `no_admissible_rows` is *we could not look* and is deliberately NOT folded into `loss_inverts` (which would assert the loss is unreal), and `absent_from_block` is *0 closes* and never a 0.0 reading. Prints a POSITIVE CONTROL — how many other strategies each block returned — so an absent leg cannot be confused with a dead reader. ⚠️ It grades PROVENANCE, never edge, and proposes NO disposition: retiring a leg is Tier-3 | `scripts/research/kill_case_admissibility.py` (write-up `docs/research/m20-u42-the-kill-case-does-not-rest-on-measured-rows-2026-09-13.md`) |
| **Did a shipped geometry change cause a PnL break, or did the market move under it?** Splits packages into the treated arm and TWO controls at a dated event, and combines two independent terms: the stop-out-rate difference-in-differences (supplied) and the MFE:MAE excursion, which is **geometry-free by construction** so the treated change cannot move it. Returns `e35_geometry` / `market_regime` / `both_contribute` / `neither_explains` / `cannot_discriminate` — the control's excursion is the deciding term. ⚠️ Because MFE:MAE is unbounded above (MAE→0), an arm mean can be carried by two packages, so each term is graded on **sign stability**: where the delta of means and the delta of medians disagree the term is `sign_unstable` and returns `cannot_discriminate` rather than a number — it can never be spent as evidence the control did NOT move. ⚠️ Candles come from Binance Vision, so equity/ETF/CME legs contribute 0 excursion rows and the excursion half is crypto-only. ⚠️ **The stop-out DiD is SUPPLIED, not computed here, and `verdict()` branches on nothing but its sign** — MI-301 measured that sign as knife-edge (one additional stop-out in the treated pre cell flips it) and the pre-era baseline as eroding under the 1000-row journal clamp, so treat a `both_contribute` from this instrument as resting on an external scalar: `docs/research/mi301-stop-term-robustness-2026-09-17.md` | `scripts/research/m20_u41_break_attribution_verdict.py` (write-up `docs/research/m20-u41-e35-break-attribution-verdict-2026-09-13.md`) |

## 10. Sweep orchestrators

`sweep_wave1_families.py` · `sweep_wave2_momentum.py` · `ws_a_futures_sweep.py` ·
`ws_a_s2_retune.py` · `chop_scalp_study.py` · `session_gating.py` ·
`hf_vectorized.py` + `hf_solo_sim.py` (HF candidates, research-only).

---

## 11. Running the research itself — the standing queue (R4 + R5)

| question | tool |
|---|---|
| **Which queued experiment may run, and where should it run?** The two decisions the queue exists to make, kept as PURE FUNCTIONS so the policy is arguable in tests rather than against a live dispatch. Grades a job's declared `power` block into five states that are never collapsed — `cleared` / `underpowered` / `undeclared` / `unverifiable` / `not_applicable` — and routes it by its DECLARED resource needs into `runner` / `trainer` / `gpu` / `unroutable`. ⚠️ **`not_applicable` is NOT `cleared`**: a `kind: deterministic` job faced no power bar, a cleared one faced it and met it, and tallying them together would report "N jobs cleared the gate" over a population where some never took the test. ⚠️ **`unroutable` is a REFUSAL, never a fallback to the runner** — a job declaring both a GPU and trainer-resident data names no destination that exists, and one declaring more memory than any destination has is refused rather than sent to be OOM-killed on the 6 GB trainer. ⚠️ **The `required_n` floor is the NORMAL approximation and assumes iid draws**; backtest trades are autocorrelated and walk-forward folds overlap, so the true requirement is HIGHER, never lower — clearing it is necessary, not sufficient, and it adjusts nothing for multiplicity. An untabulated alpha/power RAISES rather than interpolating a quantile nobody chose | `scripts/research/research_queue.py` (library; the queue lives in `research/queue/*.yaml`, one file per job) |
| **Where do the compat-matrix verdicts GO?** Until 2026-08-30: nowhere a session could reach. `gld-compat-matrix.yml` ended at `actions/upload-artifact` and a PM-side session has **no artifact download** (root `CLAUDE.md`), so every per-account verdict it had ever computed died with its run — one of the eighteen in `BL-20260827-EIGHTEEN-EVIDENCE-WORKFLOWS-UPLOAD-AND-LAND-NOTHING`. That became BLOCKING when the research-queue cron was armed: `RQ-20260827-001` routes to this workflow and declares `lands.store` with `min_rows: 1`, and it is the FIRST job the schedule fires — so arming without this would have produced, on day one, exactly the job-exits-0-having-landed-nothing failure R2 names. Flattens each `compat_*.json` into one row per account. ⚠️ **The producer writes `account`; this emits `account_id`** — renamed once at the corpus boundary because that is the queue job's declared `assert_field` and how every other store in the repo keys an account; `account_compat_matrix.py` is UNCHANGED, so do not expect to find `account_id` in it. ⚠️ **Every row carries `run_generated_at` and the run stamp is PRINTED**, because the store is cumulative and an assertion keyed on a field the store already holds is satisfied by history and can never fail — the e35 vacuity fixed in #10487, which reported `landed - 6624 rows, exit 0` for a run whose rows were entirely unmerged. An **empty scan is a refusal (exit 2)**, not a clean no-op, and the write is **read back** before success is claimed | `scripts/research/gld_compat_extract.py` -> `docs/research/gld-compat-matrix-verdicts.jsonl` (wired into `.github/workflows/gld-compat-matrix.yml` via `commit-to-main` + `assert_rows_landed.py`) |
| **Did anyone READ a landed result, and what did they decide?** The stage R1-R6 never had: R2 makes landing part of the run and the architecture stops there. Measured 2026-08-30, `grep -c 'corpus\|research/queue'` over the system-review / health-review / performance-review / ml-review / research-driver SKILL.md files returns **0 for all five** — the research pipeline is invisible to every review we run. ⚠️ **THIS IS NOT WHAT `provenance-consumer-guard` ASKS.** Eight scripts already read these corpora, so a consumer EXISTS; this asks the question one level over, which no guard asks — **was a consumer RUN, on THIS batch, and did a decision come out of it.** A tool that could have read a result is not a record that anyone did. The unit is `(corpus, run_stamp, leg)`: a row is too fine to decide anything about (8,321 in e35) and a corpus too coarse to decide anything at all; the leg is what a decision is actually taken about. ⚠️ **A SEPARATE STORE, DELIBERATELY** — stamping `dispositioned_at` onto a corpus row would make an append-only MEASUREMENT record mutable, and a re-run superseding by `measurement_key` would carry an old disposition onto a number nobody read. Five states, never collapsed, and **`superseded_unread` is the load-bearing one**: 196 of 288 historical units are superseded by construction, so folding it into `unread` would report 288 findings on day one and be ignored by week two — the desensitized-alarm P1. `corpus_unreadable` is *we could not look*, never *nothing unread*. A disposition must state a REASON, and the vacuous-reason vocabulary is **imported** from `render_system_report.py::_NON_REASONS` rather than re-typed (`backlog_drive` was already hardened against exactly this: 75 recorded touches saying *no new evidence, carried forward* while the guard stayed green and the backlog grew +129 net). First live survey: **92 unread / 196 superseded / 0 dispositioned** | `scripts/research/research_disposition.py` -> `docs/research/research-disposition-ledger.jsonl` (read by `/performance-review`'s `research_results_disposition` coverage key) |
| **What is due right now, and fire it.** Reads the queue, applies the two gates above, and dispatches via `gh workflow run`. ⚠️ **DRY RUN IS THE DEFAULT** — `--fire` is opt-in, and the daily cron passes nothing, so a scheduled run reports and spends nothing. Exits **2** when it could not READ the queue, which is deliberately distinct from an empty one (exit 0 with an explicit "EMPTY, not a read failure" line), and **1** on an invalid job — a malformed entry is reported, never skipped, because a dispatcher that drops what it cannot parse reports "nothing to do" for a queue that holds work. Stamps `last_dispatched_at` only on a SUCCESSFUL fire (stamping a failure would mark the job run and drop it for a full cadence period) and warns loudly when the stamp itself fails, since an unstamped job re-fires. ⚠️ **GPU routes spend real money** — measured 2026-08-27 from `comms/gpu_spend_ledger.json`: 7 RunPod runs in 2026-07, lifetime $0.2164, largest single run $0.0987, against a $10/month cap. `--max-gpu-dispatches-per-run` is the third gate, because the ledger cap is *monthly* and cannot bound a loop inside one run | `scripts/research/dispatch_queue.py` -> `.github/workflows/research-queue-dispatch.yml`, contract in `research/queue/README.md` |

---

## Maintaining this index

It is only useful while it is complete, and a stale index that *looks* complete is worse
than none — that is the failure it was written to fix. So:

- **Adding a script to `scripts/research/`** → add a row here in the same PR.
- **Enforced** by the `research-capability-index` check in
  `scripts/ops/check_research_index.py` (CI), which fails when a script in
  `scripts/research/` appears in neither this file nor its declared-exempt list. The
  exemption list requires a reason, so "not indexed" is always a visible choice.
- **Do not** claim completeness in prose anywhere else. Point here instead. The
  `backtesting` skill's old "every real backtest entry point in the repo" line is exactly
  the sentence that stopped a session from looking further.
