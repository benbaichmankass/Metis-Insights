# Exit-lever wiring audit — is the bracket/active-management thesis in the roadmap, built, and applied?

**Status: IN PROGRESS (skeleton committed early so the analysis cannot be lost to a
push denial at the end — `BL`-class incident on a prior session).**

Work object: `docs/claude/work/objects/WO-20260906-ARE-THE-BRACKET-AND-ACTIVE-MANAGEMENT-MECHANISMS.yaml`
Registry key: `pending-20260906T105301Z-2` · MI-146.

**PROPOSE-ONLY.** Strategy logic and exit geometry are Tier-3. Nothing here is applied.

## The thesis being audited (operator, 2026-09-06)

> "the brackets need to be the prediction of where we think the price is gonna go.
> That's what it's based on, and trades need to be ending at the brackets. Either we
> were wrong and it hit the stop loss or it was right and it hit the take profit. But
> none of those are, like, formalities. ... the stop loss is our risk manager to make
> sure that things get stopped out at the right level, but we also need to keep on
> moving it up to be banking our R as it's coming in."

## Sections (to be filled)

1. Q1 — is it in the roadmap properly?
2. Q2 — is it built? (per-lever runtime-consumer census)
3. Q3 — is it applied where we claim? (live `/api/bot/config` read)
4. The per-lever table separating **never wired** from **wired and honestly failed**
5. Recommendation — what to build first
