id: WO-20260906-ALPACA-LIVE-S-ROUTED-REAL-MONEY-LEG
type: question
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: >-
  alpaca_live's routed real-money leg cannot place an order: both gates read live, every signal journals dry_run_no_order_placed
stage: EVIDENCE
lifecycle: done
owner: null
opened_at: 2026-09-06
closed_at: '2026-09-11'
review_trigger: >-
  Re-read at the next cycle. A captured idea is PARKED, not queued — it becomes
  work only when it is given an owner and moved out of `dormant`.
done_condition: >-
  The reason the order is refused is NAMED and traced to the code path that refuses it, with the population stated (how many signals, over what window, all with the same reason or not). Then EITHER a fix is proposed at its correct tier, OR it is established that the refusal is CORRECT and something else is mis-declared -- ALPACA_CASH_SETTLEMENT_MODE=apply is armed on this exact account and is a live candidate, as is the side filter and the whole-share clamp. !! Do not assume it is a bug: 'declared live and correctly refusing' is a real outcome and would move the defect to the declaration.

why: >-
  Found by the 2026-09-06 observation sweep (session_01UaZ9bo) while re-observing OI-20260831-ALPACA-LIVE-FIRST-REAL-MONEY-LEG-ROUTED-BUT-HAS-NEVER-TRADED. tlt_pullback_1h was routed to alpaca_live as its first real-money leg since 2026-07-15 (Tier-3, operator-approved 2026-08-31), and EVERY signal since journals dry_run_no_order_placed while BOTH declared execution gates read live. A leg that is declared live and structurally cannot trade is its own class (MI-38). It also means that OPEN-ITEMS row can never clear until this is fixed.

source: >-
  operator (conversational)

blocked_on: []
blocked_on_basis: >-
  NOT_ASSESSED. ⚠️ This empty list is NOT the claim that nothing blocks this
  object — nobody has looked. Write a TRUE edge before moving it out of
  `dormant`; an invented edge is read by the constraint computation as a real
  blocker, and a false blocker is worse than a missing one.

captured_by: scripts/ops/capture_idea.py
sweep_2026_09_11: |-
  COHORT RULE 3 ("non-migrant, individually read") of the MI-243 dormant sweep, 2026-09-11.
    THE RULE, stated before it was applied: a dormant object that is NOT a Phase-C migrant (47 of the
    622 dormant rows at `main` 4e9255acb) gets an INDIVIDUAL read, because it has no shared provenance
    to grade as a group. Each was graded into exactly one of: `done` where there is CHECKABLE completion
    evidence (4 rows); `ready` where it is live, operator-directed work with a clear done_condition and
    no completion evidence (42 rows); or LEFT `dormant` where it could not be graded (1 row).
    ⚠️ THE ASYMMETRY IS DELIBERATE: `ready` is the default and `done` needs evidence, because a row
    wrongly queued is corrected by whoever picks it up, while a row wrongly closed is not looked at again.
    ⚠️ `ready` HERE MEANS QUEUED, NOT STARTED, AND NOT ASSESSED FOR BLOCKERS. No `blocked_on` edge was
    invented for any of these rows — an invented edge is read by the constraint computation as a true
    blocker, and a false blocker is worse than a missing one.
  ---
  GRADED `done` — the completion evidence:
    ⚠️ CLOSED AS A DUPLICATE — THE WORK IS **NOT** COMPLETE. It lives on
    `WO-20260907-ALPACA-LIVE-S-ROUTED-REAL-MONEY-LEG`, which is the row the dispatch named and the row
    carrying the typed `blocked_on` edge. The duplication is not this sweep's inference: PR #11279's own
    body names it — "Duplicate work object: WO-20260906- and WO-20260907-ALPACA-LIVE-S-ROUTED-REAL-MONEY-LEG
    are two objects for the same work ... the duplicate was left rather than merged unasked."
    ⚠️ `done` is the WRONG WORD for this and is used only because the `lifecycle` vocabulary has no
    terminal value meaning superseded/duplicate/abandoned. That gap is filed by this sweep as its own
    finding; if a value is added, this row should be re-pointed at it.
