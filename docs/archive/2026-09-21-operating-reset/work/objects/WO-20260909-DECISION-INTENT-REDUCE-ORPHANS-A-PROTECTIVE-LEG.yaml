# Hand-authored by the manager (session_01HrmZ1RRNM4UnEUaFdrPEjj) on 2026-09-09 to put an
# audit finding to the operator as an answerable question, and to record the answer given.
# Every figure below is TRANSCRIBED from docs/audits/full-system-audit-2026-09-09.md
# (landed on main as 566c0ff4c). No measurement was re-run by the manager.
id: WO-20260909-DECISION-INTENT-REDUCE-ORPHANS-A-PROTECTIVE-LEG
type: question
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: >-
  intent_reduce leaves a stop up to 101.8x the row it protects - operator answered: resize the
  leg AND add the per-row invariant
stage: DECISION
lifecycle: waiting
owner: claude
opened_at: '2026-09-09'
closed_at: null

review_trigger: >-
  Re-read when a live intent-reduce is seen on a Bybit partial-tpsl account. !! THE BUILD IS NO
  LONGER WHAT REMAINS AND THIS FIELD SAID IT WAS UNTIL 2026-09-09T23:45Z: it read "The DECISION
  is discharged; what remains is a Tier-2 BUILD on the order path", which was true when the
  manager wrote it and is false now - #11604 merged as 7b8b6b51d. What remains is an
  OBSERVATION, and picking this up does not mean writing code. Do not re-measure the finding -
  the venue read is in the audit at F-36 - and do not re-run the build.

done_condition: >-
  BOTH: (a) apply_intent_reduce_partial_close resizes the reducing row's OWN tracked protective
  leg, verified against a venue read after a real intent-reduce; and (b)
  INV-PROTECT-LEG-MATCHES-ROW exists in system_invariants.py asserting each open trade's venue
  leg qty equals THAT row's position_size. !! (b) IS STRICTLY STRONGER THAN THE EXISTING
  SYMBOL-LEVEL INV-PROTECT-OVERCOVER, which passes whenever siblings happen to sum correctly -
  do not treat the existing one as covering this.

blocked_on:
  - kind: data_accrual
    ref: >-
      a real intent-reduce firing on a Bybit account under BYBIT_TPSL_MODE=partial, after the
      7b8b6b51d deploy reaches the running trader - and the shrunk row's tracked leg then read
      back off /api/diag/bybit_open_orders at that row's NEW position_size
    since: '2026-09-09'
    note: >-
      THE BUILD IS DELIVERED AND HALF THE DONE-CONDITION IS MET; THIS EDGE IS THE OTHER HALF AND
      IT IS AN EVENT NOBODY CAN SCHEDULE. Clause (b) - INV-PROTECT-LEG-MATCHES-ROW existing in
      system_invariants.py - is DONE: merged as 7b8b6b51d (#11604, squash, human merge click
      2026-09-09T23:39:57Z), verified present on main by reading the blob rather than the commit
      message (11 references), self-test 39/39 with the load-bearing control that it FAILS the
      live F-36 fixture while INV-JOURNAL-EXCHANGE PASSES on the same fixture. Clause (a) -
      the resize "verified against a venue read after a real intent-reduce" - is NOT met and
      cannot be met by this lane: it needs a reduce to FIRE, and the class incidence is 21 of
      1000 journal rows, so the wait may be days. !! DO NOT MANUFACTURE THE EVENT. Placing or
      amending an order to test this is exactly what the Tier-2 approval withholds, and the
      currently-oversized legs must not be "remediated" by cancelling them
      (BL-20260820-OVERCOVER-REMEDIATION-CANCELLED-THE-JOURNAL-MATCHING-LEG). !! AND THE CHEAP
      HALF IS NOT THE OBSERVATION: the intent_reduce_executed audit row carries
      notes.intent_reduce_allocations[].leg_resize with resized / failed / no_tracked_id /
      not_applicable / not_attempted. A `resized` there says THE VENUE ACCEPTED THE CALL, which
      is necessary and is NOT clause (a) - the defect class this fixes is a system believing its
      own summariser, so the leg's qty must be read off the venue. A `failed` is the MORE
      VALUABLE finding: it most likely means Bybit refuses qty on an amend of a Partial-tpsl
      conditional leg, i.e. the one premise taken from the V5 API contract rather than from a
      call is false and the fix does not work. Full criteria:
      OI-20260909-INTENT-REDUCE-LEG-RESIZE-SHIPPED-AND-NO-LEG-HAS-BEEN-OBSERVED-RESIZED.
blocked_on_basis: >-
  RE-ASSESSED 2026-09-09T23:45Z by the building lane (session_0178Pn128KK71u6nCzLeVH6w) after
  #11604 merged. !! THE PRIOR BASIS IS SUPERSEDED BY A CHANGE OF FACT, NOT A DISAGREEMENT, AND
  ITS TEXT MUST NOT BE RE-QUOTED: it read "Nothing blocks the BUILD ... IT IS NOT SCHEDULED: the
  WIP ceiling stood at 8 of 8 in_flight, so no lane could be spawned". A lane WAS spawned, the
  build is DELIVERED and merged, so both halves of that sentence are now false and a session
  reasoning from them would conclude this is unstarted work. What blocks it now is a LIVE EVENT,
  which is a different kind of blocker from a decision gate and is why the edge above is
  external_event rather than operator_decision. !! THE EDGE IS A TRUE ONE, WRITTEN BY A SESSION
  THAT BUILT THIS ROW AND UNDERSTANDS IT - not a bulk pass over unassessed rows, which the work
  store's README records as an active harm because the constraint computation reads an invented
  edge as a real blocker and a false blocker is worse than a missing one. Measured while writing
  it: 65 of 707 objects carried an ASSESSED basis (9.2%), against the declared 50% floor that
  keeps scripts/ops/constraint_readout.py at `insufficient_basis`. This is one edge, so it does
  not move that verdict and is not claimed to. !! NOTHING IS OWED TO THIS ROW BY A HUMAN: the
  operator's Tier-2 approval is discharged and MUST NOT be re-sought
  (WO's own answer block, chosen: resize_and_invariant, 2026-09-09).

captured_by: manager session_01HrmZ1RRNM4UnEUaFdrPEjj (routing the landed full-system audit)

decision_requests:
  - id: DEC-20260909-INTENT-REDUCE-ORPHAN-LEG
    question: >-
      apply_intent_reduce_partial_close shrinks a trade's row but never resizes that row's own
      venue stop - leaving a reduce-only stop up to 101.8x the row it protects. 21 of 1000
      journal rows, including 3 on real-money bybit_2. It is the root cause of 64 of the 112
      rows in the operator ERROR feed. Tier-2.
    urgency: high
    asked_on: '2026-09-09'
    context: >-
      Audit F-36. Live at 2026-09-09T16:47Z on bybit_1/ADAUSDT: trade 5479 position_size 79227.0
      with its stop at 79227.0 (correct); trade 5417 position_size 628.0 with its tracked stop
      resting at 63943.0 - 101.82x. Journal 79227.0 + 628.0 = 79855.0 reconciles EXACTLY with
      the exchange position; resting stops total 143170.0 = 179.3% over-covered. 63943 - 63315 =
      628, i.e. exactly 5417's current size, which is the arithmetic identifying the cause.
      First over- cover page fired 41 seconds after the reduce. A grep over the 133-line
      function finds ZERO references to sl_order_id, tp_order_id, modify_open_order,
      set_trading_stop or amend. The leg was still 63,943.0 after a trailing amend on 2026-09-08
      - the amend followed the tracked id and faithfully preserved the wrong size. Population:
      24 (account, symbol) groups in the live open book, 23 exactly sized, 1 broken;
      bybit_1/BNBUSDT had no reduce and is exactly sized - the negative control. !! TODAY'S
      STANDING INSTANCE IS PAPER-CLASS. The mechanism is account-agnostic and has fired 3x on
      real-money bybit_2; those rows have since closed, so there is no standing real-money
      instance right now.
    options:
      - key: resize_and_invariant
        label: Resize the leg, add the invariant
        implication: >-
          Fixes the cause and adds the detector that would catch a recurrence. Tier-2 - it
          touches the order path. Also removes most of the ERROR-feed noise.
      - key: invariant_first
        label: Add the invariant first, fix after a soak
        implication: >-
          Safer sequencing; the oversized legs and the pages persist meanwhile.
      - key: detector_only
        label: Detector only, never resize
        implication: >-
          Measures and pages correctly, leaves the legs. The over-cover is reduce-only so it
          cannot flip a position, but a stop 101x its row can cut a live sibling.
    allows_free_text: true
    answer:
      answered_by: operator
      answered_on: '2026-09-09'
      channel: >-
        Claude Code chat (AskUserQuestion option picker), answered directly to the manager
        session session_01HrmZ1RRNM4UnEUaFdrPEjj at 2026-09-09T21:1xZ. NOT via the transit log,
        so commit_work_decisions.py has nothing to replay and this answer is written BY HAND —
        the README's 'or a session doing the same thing by hand' path. !! THE OPERATOR ASKED
        'give me any decisions that are waiting for me as pop ups'. The 2026-09-08 precedent
        recorded in WO-20260908-DECISION-BE-FLOOR-R-ARM-NOTHING is that this phrasing means
        RENDER THEM IN THE CHAT, and that a manager who built Telegram delivery instead made the
        operator ask twice. That precedent was read first and followed. The Telegram channel was
        separately VERIFIED healthy the same minute (sweep receipt 21:01:23Z:
        destination=claude, poll_state=polled_with_handler,
        token_from=TELEGRAM_CLAUDE_BOT_SECRET; write gate open) — it was not the blocker, and
        these six were simply never declared as decision_requests at all.
      chosen: resize_and_invariant
      free_text: >-
        Operator chose the full fix on 2026-09-09: resize the reducing row's own tracked leg AND
        ship INV-PROTECT-LEG-MATCHES-ROW. This is the Tier-2 approval for an ORDER-PATH change.
        !! IT DOES NOT AUTHORISE CANCELLING ANY CURRENTLY-RESTING LEG BY HAND. The standing
        prohibition holds: do not 'remediate' an over-covered book by cancelling legs - the one
        prior attempt at that cancelled the leg that MATCHED the journal (
        BL-20260820-OVERCOVER-REMEDIATION-CANCELLED-THE-JOURNAL-MATCHING-LEG). The approval is
        for the code path that resizes on the NEXT reduce, plus the detector.
    asked_by:
      session_id: session_01HrmZ1RRNM4UnEUaFdrPEjj
      recorded_at: '2026-09-09T21:15:00Z'
      note: >-
        Manager session. One of the six the full-system audit filed and did not take.
sweep_2026_09_11: |-
  COHORT RULE 4 ("accrual is not an external event") of the MI-243 dormant sweep, 2026-09-11.
    THE RULE, stated before it was applied: an edge whose `ref` names DATA OR AN OBSERVATION
    ACCUMULATING ON THE FLEET, which no person can schedule and which needs nobody to act, is
    `kind: data_accrual` — a value ALREADY DECLARED in the work store's own vocabulary
    (`docs/claude/work/README.md`; `scripts/ops/constraint_readout.py:234`) and, MEASURED over all
    710 objects on 2026-09-11, used by ZERO of them. An edge that names a PR merging, an operator
    deciding, or a session doing something stays `external_event`. Only the `kind:` value changed;
    the `ref`, the `since` and the note are untouched, so no new claim is introduced.
    ⚠️ WHY THIS MATTERS: `external_event` was carrying BOTH facts, so "nothing is happening" and
    "something is accruing that nobody is watching" rendered identically — the collapsed-state
    defect this repo has a guard family for, one level below `lifecycle`.
    ⚠️ AND STATE WHAT IT DOES NOT YET BUY: nothing BRANCHES on `data_accrual` today. It is already
    correctly excluded from `constraint_readout.operator_block` (which collects ONLY
    `operator_decision`), so a soak-bound row is not counted as blocked on a person — but that is
    true of `external_event` too. The re-point makes the RECORD true; making it VISIBLE is a
    renderer change on the SPA Work page, which already receives `blockedOn[].kind` from
    `/api/bot/work`. No `lifecycle` widening is required and none was made.
  ---
  RE-POINTED its single edge from `external_event` to `data_accrual`. Justified from the edge's OWN
  note rather than from this sweep's reading of the title: it says "an event NOBODY CAN SCHEDULE",
  "DO NOT MANUFACTURE THE EVENT", and that at 21 of 1000 journal rows "the wait may be days".
  That is data accruing, by the author's own description.
