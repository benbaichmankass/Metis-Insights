id: WO-20260907-APPLY-THE-OPERATOR-APPROVED-REACHABLE-TAKE-PROFIT
type: question
parent_intent: IN-20260903-TRADING-SYSTEM-HEALTH
title: >-
  APPLY the operator-approved reachable-take-profit change, and ship the tp_intent reader in the same cycle
stage: DEPLOYMENT
lifecycle: ready
owner: null
opened_at: 2026-09-07
closed_at: 2026-09-07
review_trigger: >-
  Re-read at the next cycle. A captured idea is PARKED, not queued — it becomes
  work only when it is given an owner and moved out of `dormant`.
done_condition: >-
  config/strategies.yaml carries (a) tp_r 4.0 on gld_pullback_1h with its comment block intact, and (b) a tp_intent declaration on all 24 remaining legs exactly as MI-156 proposed - 22 mode:none and 2 mode:unexamined, never collapsed into each other. AND a real CONSUMER reads tp_intent and BRANCHES on its mode, so provenance-consumer-guard passes on a key that is written and read rather than written and ignored. The 9 sub-population-B legs get their inherited 50.0 sentinel written out explicitly, and it is VERIFIED runtime-identical rather than asserted to be. Post-merge, the live config is read back off the fleet and shown to match what was merged.

why: >-
  Operator Tier-3 approval 2026-09-07, verbatim: 'ok on the gld tp4 and the 24 declarations'. MI-156 (merged d50543a1) established that on 24 of 25 legs the honest repair is a DECLARATION rather than a number, because their only gate-passing TP cells are entangled with a bar-count timeout live cannot express - an absence of evidence, not an oversight. The proposal's own stated condition is that tp_intent must not land without a reader; a written-and-never-read provenance key is precisely what exit_price_source was, and it produced a phantom -6358 dollar exit leak nobody could find.

source: >-
  manager session_01HrmZ1RRNM4UnEUaFdrPEjj, carrying the operator's approval of PR #11225

blocked_on: []
blocked_on_basis: >-
  NOT_ASSESSED. ⚠️ This empty list is NOT the claim that nothing blocks this
  object — nobody has looked. Write a TRUE edge before moving it out of
  `dormant`; an invented edge is read by the constraint computation as a real
  blocker, and a false blocker is worse than a missing one.

captured_by: scripts/ops/capture_idea.py

outcome: >-
  DONE, and the post-merge live read-back is COMPLETE - the half MI-158
  correctly declined to claim. Applied as #11232 (3f88bfff); read back off the
  live fleet by the manager at 2026-09-07T11:52Z.
  DEPLOYED: /api/diag/version reads git_sha = git_sha_on_disk = 296c9392 with
  restart_pending false, so the running process carries the merge.
  OBSERVED on /api/bot/config (as_of 2026-09-07T11:52:13Z): gld_pullback_1h has
  tp_r 4.0 and tp_intent {mode r_multiple, basis e35_cell_tp4, calibrated
  false}, enabled true, execution live. Across the 44 enabled+live legs the
  tp_intent split reads 22 none + 2 unexamined + 1 r_multiple = 25 declared,
  with 19 legs carrying no tp_intent at all. That 19 is CORRECT rather than a
  gap: those are the legs that already had a reachable target, and 19 + gld = 20
  real, which reconciles with MI-158's own post-change count of 24 declared / 0
  inherited / 20 real. The arithmetic is a genuine cross-check, not a read-back
  of what was expected.
  ⚠️ WHAT THIS DOES NOT ESTABLISH: the geometry is deployed and readable, NOT
  exercised. No trade has yet exited at the new bracket, and 4.0R remains
  REACHABLE, NOT CALIBRATED - MI-155 grades this leg insufficient_n (n_live 3
  against a floor of 30) and 2 of its 6 walk-forward folds lost. Do not quote
  4.0R as an evidenced target on the strength of this observation.
sweep_2026_09_11: |-
  COHORT RULE 3 ("non-migrant, individually read") of the MI-243 dormant sweep, 2026-09-11.
    THE RULE, stated before it was applied: a dormant object that is NOT a Phase-C migrant (47 of the
    622 dormant rows at `main` 4e9255acb) gets an INDIVIDUAL read, because it has no shared provenance
    to grade as a group. Each was graded into exactly one of: `done` where there is CHECKABLE completion
    evidence (4 rows); `ready` where it is live, operator-directed work with a clear done_condition and
    no completion evidence (42 rows); or LEFT `dormant` where it could not be graded (1 row).
    ⚠️ THE ASYMMETRY IS DELIBERATE: `ready` is the default and `done` needs evidence, because a row
    wrongly queued is corrected by whoever picks it up, while a row wrongly closed is not looked at again.
    ⚠️ `ready` HERE MEANS QUEUED, NOT STARTED, AND NOT ASSESSED FOR BLOCKERS. No `blocked_on` edge was
    invented for any of these rows — an invented edge is read by the constraint computation as a true
    blocker, and a false blocker is worse than a missing one.
  ---
  GRADED `ready` (QUEUED). Live, operator-directed work with a clear done_condition and no
    evidence of completion found by this sweep. It was moved out of `dormant` because `dormant`
    means "carried, not started, NOT queued" (this store's own migration note), and that is a
    false description of work the operator asked for and still wants.
