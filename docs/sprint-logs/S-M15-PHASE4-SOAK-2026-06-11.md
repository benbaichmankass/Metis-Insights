# Sprint Log: S-M15-PHASE4-SOAK (part 1 — activation)

## Date Range
- Start: 2026-06-11 (operator: "all keys added")
- End: 2026-06-11 (activation complete; soak ongoing)

## Objective
- Propagate the OANDA practice + Alpaca paper demo creds to the live VM,
  deploy the M15 code, and verify the gold shadow strategy evaluates
  real OANDA data — all behind the existing gates.

## Tier
- Tier 2 (credential propagation + deploy + service restart), operator
  ack in chat 2026-06-11. CI/tooling fixes Tier 1.

## Work Completed
- **Two real infra bugs found + fixed en route** (the "canonical" sync
  workflow had NEVER completed end-to-end):
  1. `sync-vm-secrets.yml` was an **invalid workflow** since the
     2026-06-10 Tradovate removal (empty `env:` block) — every trigger
     silently dead; synthetic failed runs on each push (#3330).
  2. The **SendEnv transport never delivered values**: the VM's sshd
     `AcceptEnv` allowlist only carries the legacy rotate names
     (`NEW_API_KEY`/`NEW_API_SECRET`); generic names were dropped at the
     SSH boundary (run 27333260838, exit 2 on the required-var check).
     Replaced with **stdin transport** (NAME=VALUE pairs over the
     encrypted channel into the script's new `--stdin` mode; same
     no-logs/no-ps contract; no sshd mutation on the money box) (#3332).
  - Plus the **issue-label dispatch path** for the workflow itself
    (#3328, label `sync-vm-secrets-request`) — the hosted GitHub MCP
    cannot fire workflow_dispatch (403 verified).
- **Sync attempt 3 succeeded** (issue #3333, exit 0): all four demo
  creds in the live VM `.env`; optional-presence audit listed all four.
- **pull-and-deploy verified** (issue #3334, exit 0): VM at origin/main,
  `ict-trader-live.service` active.

## Validation Performed
- Post-deploy journal (in #3334's comment): `xauusd_trend_1h` evaluated
  a live OANDA gold 1h bar — `close=4104.925 within channel
  [4023.87, 4186.42]` — proving token auth, XAUUSD→OANDA routing,
  candle normalization, and the Donchian evaluation end-to-end. The
  multiplexer correctly stayed flat (no breakout). Normal 60 s tick.
- Gates verified intact: `oanda_practice` mode dry_run, strategy
  execution shadow, demo true. Alpaca creds present but unconsumed
  (no strategies routed — by design).
- Gaps: OANDA balance probe + Dukascopy-vs-OANDA candle fidelity
  cross-check still to run during the soak; FX weekend gate fires
  first this Friday 21:00 UTC (watch for `fx_market_closed` rows).

## Contradictions or Drift Found
- CLAUDE.md § PM-side capabilities says the MCP has "no run-log read,
  no workflow_dispatch": **half stale** — `get_job_logs` + `actions_list`
  now work (used throughout this sprint); `actions_run_trigger` still
  403s. Fixed in this commit's CLAUDE.md touch.
- `sync-vm-secrets` was documented as the canonical, working
  propagation path; it had never run end-to-end. Now it has.

## Risks and Follow-Ups
- Soak monitoring: confirm shadow order-package rows accumulate on the
  first gold breakout; weekend-gate behaviour Fri/Sun.
- Tier-3 next: gold shadow→live-on-practice flip (after soak + operator
  OK); daily-ETF strategy-assignment PR for alpaca_paper.
- Live trading later = new keys per broker + explicit OANDA_ENV/
  ALPACA_ENV flips + set-account-mode (operator-gated), per the memo.

## Wrap-Up Check
- [x] Verified against live runtime output, not PR intent.
- [x] Roadmap updated; contradictions recorded; unknowns stated.


---

## Part 2 — GO-LIVE ON PRACTICE (same day)

Operator approved "go live on practice" in chat. Executed:
- #3336 (Tier-3, merged): `xauusd_trend_1h` execution shadow → live +
  the three ETF legs (`spy_trend_long_1d`, `qqq_trend_long_1d`,
  `gld_pullback_1d`) at execution: live on `alpaca_paper`; roster 13→16;
  builders carry the `us_equity` session gate; 78/78 tests green.
- #3337: deployed; all services active.
- #3338/#3339: `set-account-mode` → live for both accounts (paper money
  on both hosts); trader restarted cleanly each time.
- #3340: repo mode-mirror + roadmap + backlog BL-20260611-M15-1
  (runtime_status live-map missing the two new accounts).

**Verified live**: gold evaluating real OANDA 1h bars each tick; the
`us_equity` gate correctly held the ETF legs closed pre-13:30 UTC.

**Soak watch items** (handed to the follow-up session — see
`docs/research/session-handoff-m15-soak-2026-06-11.md`): first practice
fill on each broker (verify broker-side SL/TP + journal row + dashboard
display + is_demo stamping), the FX weekend gate's first live window
(Fri 21:00 UTC), balance probes appearing in balance_snapshots, and the
Dukascopy-vs-venue candle fidelity cross-check.


---

## Part 3 — Soak session (WS-A/B/C) + first alt-sleeve adoption (same day)

Operator brief: `docs/research/session-handoff-m15-soak-2026-06-11.md`.
PRs #3360, #3372, #3378 (all merged + deployed same day, each on explicit
operator approval in chat).

### WS-A — soak verification (report: `docs/research/m15-soak-report-2026-06-11.md`)
- Fleet healthy end-to-end; gold evaluating live OANDA 1h bars per tick.
- **Candle fidelity PASS on both venues** vs the Phase-0 Dukascopy data:
  XAU_USD median |Δclose| 0.73 bps (482 bars); SPY/QQQ/GLD 0.7–1.1 bps.
- **Real bug found + fixed pre-open (#3360, deployed #3370):** Alpaca
  `/bars` defaults `start` to the current day, so `get_ohlcv` returned at
  most today's bars — all three alpaca_paper daily legs would have
  silently no-opped from their first session tick. Explicit
  timeframe-aware lookback + regression test; **verified at the first
  13:30 UTC tick** (SPY evaluated a real Donchian-30 channel, #3376).
- Backlog: BL-20260611-M15-1 resolved (live-map settled);
  BL-20260611-M15-2 opened (balance_snapshots.json stale since
  2026-05-25; new accounts never recorded — balance probe blind).
- Still open: first practice fills (verify is_demo + broker-side SL/TP),
  FX weekend gate first window Fri 21:00 UTC.

### WS-B — SPY/QQQ intraday validation (report: `docs/research/m15-ws-b-spy-qqq-walkforward-2026-06-11.md`)
- K-fold anchored walk-forward (M8 KFold, 5 OOS folds, net of fee) on
  the corrected RTH data: **all four cells FAIL the every-fold gate**
  (SPY ict_scalp the near-miss: 4/5 folds, +8.6R at double fee).
  **No shadow PR; QQQ stays research** — per the operator's gate.

### WS-C — Bybit alt sweep (report: `docs/research/m15-ws-c-alt-sweep-2026-06-11.md`)
- 7 alts × 7 families, net 7.5 bps, train/OOS: **trend 4h generalizes
  (5/7 robust)**, pullback 2h on ETH/ADA/SOL/XRP, ict_scalp on
  ADA/AVAX/ETH/LINK; fade/squeeze/fvg_range dead on alts. (Fetch
  tooling hardened en route: Bybit retCode 10006 rate-limit retry +
  pagination pacing — first pull lost 8/14 datasets.)
- K-fold follow-up: every cell fails the strict 5-fold gate; under the
  **3-fold discipline (the live trend_donchian precedent) ETH pullback
  passes cleanly** (+22.2/+12.2/+23.6, +58.1R total, +53.2R at 15 bps)
  and trend_4h passes on AVAX/ETH/SOL/XRP. **Operator endorsed the
  3-fold gate and approved ETH pullback ONLY.**
- **`eth_pullback_2h` MERGED (#3378) + DEPLOYED (#3403) + VERIFIED
  loaded** (status #3408: roster 17 configured / 15 running, ETHUSDT in
  the tick loop) — the demo book's first non-BTC symbol, paper money,
  execution: live per the paper-accounts-execute policy. The four
  3-fold-passing trend_4h cells stay research-validated, NOT adopted.

### Validation discipline notes
- Fold reports bucket a single full-series emit by entry_time (valid for
  fixed params; no per-fold warm-up artifacts) —
  `scripts/ops/m15_ws_b_fold_report.py`.
- ict_scalp ADA failed 2× fee hard (−38R) — the 5m family's slippage
  fragility on alts is confirmed; it stays research-only everywhere.

## Wrap-Up Check (part 3)
- [x] Verified against live runtime output (status #3408, journal #3376), not PR intent.
- [x] Tier-3 changes merged only on explicit operator approval (gate decision in chat).
- [x] Reports committed; backlog updated; unknowns (first fills, weekend gate) stated.
