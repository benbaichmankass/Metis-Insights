# Sprint Log: S-M15-PHASE0-2026-06-10

## Date Range
- Start: 2026-06-10 (operator kickoff in chat, post-merge of memo PR #3273)
- End: 2026-06-10

## Objective
- Primary goal: execute M15 Phase 0 — the backtest evidence pass over the
  candidate markets (FX majors + gold, US ETFs intraday + daily) that
  decides which platform (OANDA / Alpaca) gets wired first in Phase 2.
- Secondary goals: ship Phase 1 (data-only connectors + market-hours
  gate) and Phase 2 prep (placeholder secrets) so the operator's return
  queue is accounts/keys/approvals only.

## Tier
- Tier 1 (research scripts, sweeps on trainer, docs, backlog) +
  **Tier 2 prepared-not-merged** (draft PR #3303 touches
  `src/runtime/market_data.py` + adds runtime connector modules — inert
  for all current configs, awaiting operator OK).
- Justification: no live-path behaviour changes merged; trainer VM is
  autonomous territory (trainer-vm-mode.md); all live-trading promotion
  remains Tier-3 and is only *proposed* in the results doc.

## Starting Context
- Active roadmap items: M15 (registered same day, PR #3273).
- Prior sprint reference: memo `docs/research/market-alternatives-2026-06-10.md`
  (operator decisions §0: intraday-first, both platforms sequenced, US
  setup, IBKR legs stay).
- Known risks at start: no trading calendar in the runtime; harness
  params tuned to BTC vol; ict_scalp harness has no fee model.

## Repo State Checked
- Branch: `claude/happy-heisenberg-p3vt9j` (work) over `main` @ 8833bed.
- Deployment state: untouched (no live VM mutations this sprint).
- Canonical docs reviewed: CLAUDE.md, CLAUDE-RULES (hierarchy), skills
  `backtesting` + `git-actions` (skill-first), SPRINT-LOG template.

## Files and Systems Inspected
- Code: `scripts/backtest_{trend,pullback,ict_scalp,fvg_range}.py`
  (flag surfaces verified against code — found ict_scalp's missing fee
  model/windowing), `src/runtime/market_data.py`, `src/exchange/*`,
  `config/instruments.yaml`, `src/core/profile_loader.py`.
- Workflows: `trainer-vm-diag.yml` (cmd parsing), `init-actions-secrets.yml`.
- Trainer VM: state, venv, dukascopy lib, data cache (relay issues
  #3286 → #3323).

## Work Completed
- **Data**: `scripts/ops/fetch_dukascopy_ohlcv.py` (generic Dukascopy
  fetcher; `--rth-only` with month-based DST windows — a fixed-window
  bug was caught and fixed mid-sprint, commit 7dccf4d) +
  `m15_phase0_fetch.sh`. 9 datasets fetched on the trainer (FX 15m ×3
  since 2019, QQQ/SPY 5m RTH since 2019, dailies ×4 since 2010).
- **Sweep pass 1** (`m15_phase0_sweep.sh`): 46 runs, net-of-fee 2 bps,
  train/OOS split 2025-01-01 (dailies 2022-01-01).
- **Sweep pass 2** (`m15_phase0_rerun_ict.sh` + `m15_net_ict_scalp.py` +
  additive emit fields in `backtest_ict_scalp.py`): corrected-RTH equity
  legs + exact per-trade net-of-fee for every ict_scalp leg.
- **Results doc**: `docs/research/m15-phase0-results-2026-06-10.md`
  (method, full matrix, NET table, findings, Phase 2 recommendation).
- **Phase 1 (draft PR #3303)**: `src/exchange/alpaca_connector.py`,
  `src/exchange/oanda_connector.py` (REST-only, data-only),
  `market_data.py` registration, `src/runtime/market_hours.py` (inert),
  10 unit tests (mocked HTTP).
- **Phase 2 prep**: placeholder Actions secrets via issue #3302.
- ROADMAP M15 row updated to Phase-0-complete status.

## Validation Performed
- Tests run: `tests/test_m15_connectors_market_hours.py` 10/10 green
  locally; ruff clean; net-of-fee post-processor math verified against a
  hand-computed fixture.
- Sweep outputs: per-run JSONs + SUMMARY.md collected from the trainer
  via relay (#3308, #3310, #3323); pass-2 NET table cross-checked
  against pass-1 gross (fee deltas in the expected 0.03–0.15R/trade
  band).
- Gaps not yet verified: CI on draft PR #3303 (was in progress at
  sprint close — webhook-subscribed); Dukascopy CFD series vs real
  OANDA/Alpaca feeds (Phase 1 connectors enable that comparison once
  keys exist); no k-fold walk-forward yet (screening pass only).

## Documentation Updated
- Roadmap updates: M15 row (Phase 0 complete, operator queue).
- Subsystem doc updates: results doc (new); memo §6 already pointed here.
- Historical docs marked superseded: none this sprint.

## Contradictions or Drift Found
- `backtesting` skill claims every harness is net-of-fee —
  `backtest_ict_scalp.py` has no fee model and no `--start/--end`.
  Logged as **BL-20260610-M15-1** (fix skill text ± harness).
- My own `--rth-only` first cut dropped the DST cash-open hour — fixed
  in-sprint (7dccf4d), equity legs re-run on corrected data.

## Risks and Follow-Ups
- Remaining technical risks: CFD-vs-venue data mismatch (validate against
  OANDA/Alpaca candles in Phase 1 once keys arrive); 1.5y OOS window;
  single-split screening (promotion requires M8-style k-fold + fee
  headroom); trainer worktree `/home/ubuntu/m15-phase0` holds results —
  mirror or copy before any reprovision.
- Remaining product decisions (Tier 3): adopt "OANDA first, Alpaca
  second"; which gold strategies go to practice-account shadow first.
- Blockers: operator-only — Alpaca paper + OANDA practice accounts (US
  setup) + paste 4 placeholder secret values (#3302); operator OK on
  draft PR #3303 (Tier 2).

## Deferred Items
- FX-scaled `fvg_range` parameterization (0-trade cells on majors).
- QQQ intraday: shadow data collection only.
- Copper daily: inconsistent — keep watching, don't wire.
- M15 program-branch convention decision (research scripts currently on
  the session branch).

## Next Recommended Sprint
- Suggested next sprint: **S-M15-PHASE2-OANDA** — `new-broker` checklist
  for OANDA practice (account package, integrator, executor with
  attached TP/SL, `accounts.yaml::oanda_practice`, weekend gate wiring),
  gated on keys + #3303 approval.
- Why next: Phase 0 verdict (gold multi-family edge) + Phase 1 connector
  already shipped — execution wiring is the critical path.
- Required verification before starting: secrets present; #3303 merged;
  OANDA practice candles cross-checked against the Dukascopy series.

## Wrap-Up Check
- [x] Code was inspected directly, not inferred only from summaries.
- [x] Documentation was reviewed and updated as part of the sprint.
- [x] No pipeline stage touched (TRADE-PIPELINE.md unchanged — connectors inert).
- [x] Roadmap status was checked.
- [x] Contradictions were recorded.
- [x] Remaining unknowns were stated clearly.
