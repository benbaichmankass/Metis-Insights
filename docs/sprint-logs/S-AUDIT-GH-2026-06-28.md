# S-AUDIT-GH — M17 full-codebase sweep (G/H) + stale-PR/issue closeout + #3910 tail

## Date Range
- **Start:** 2026-06-28
- **End:** 2026-06-29
- **Session:** `session_01LGsKNEjcTeujEGVACSPESK` (M17 lead/coordinator)

## Objective
- **Primary:** complete the operator's "review every single line" mandate (S-AUDIT-F/G/H), close every loose end (stale PRs + open issues), and resolve the unmerged-#3910 tail.
- **Secondary:** wire the stranded real-money `slv`/`gdx` daily-pullback strategies; spin up a parallel security session for the external-intrusion probes.

## Tier
- **Mixed.** Tier-1 (docs/dead-code/ops-script DB-path fixes, issue/PR triage); one **Tier-3** shipped with explicit operator approval (#4969 risk-sizing refuse) and one Tier-3 real-money wiring (#4964 slv/gdx) approved 2026-06-28.

## Starting Context
- Active roadmap: **M17 Full-System Audit & Hardening** (sprint breakdown S-AUDIT-A..F). Flagship (Alpaca balance) RESOLVED in S-AUDIT-C; governance shipped S-AUDIT-D.
- Prior: the A/B/C/D/E sweeps + the operator-approved merge tail (#4951/#4952/#4955/#4957).
- Known risk: multiple parallel audit sessions → merge-racing (mitigated by the S-AUDIT-D session-board single-slot protocol).

## Repo State Checked
- Branch `main`; swept against HEAD through `744ac178` (the #4969 merge).
- Deployment: live trader auto-deploys `main` via `ict-git-sync`; confirmed post-merge (below).
- Canonical docs reviewed: `CLAUDE.md`, `docs/CLAUDE-RULES-CANONICAL.md`, `docs/ARCHITECTURE-CANONICAL.md`, `ROADMAP.md`.

## Files and Systems Inspected
- `src/units/accounts/risk.py` (`position_size`, `_size_unbounded`) — the sizing path.
- `src/units/accounts/clients.py` (`account_closed_pnl_for_trade`, `_bybit_closed_pnl_lookup`) + `src/utils/closed_at.py` / `src/web/api/_closed_at.py` — #3910 Items 1 & 2 verification.
- `src/runtime/strategy_signal_builders.py` + `src/runtime/intent_multiplexer.py` — slv/gdx wiring.
- `config/instruments.yaml` (BTCUSDT 0.001 / ETHUSDT 0.01 lots), `config/accounts.yaml` (bybit_2).
- `scripts/backtest_system.py` (`_risk_qty`) + the research harnesses — walk-forward applicability.
- Diag relays (`/api/diag/exchange_positions`, `/api/diag/journal?table=trades`, `/api/diag/status`).

## Work Completed
**G-sweep fixes (background fan-out → consolidated PRs):**
- #4958 — `ml/registry/model_registry.py` `promote()`/`promote_stage()` no longer wipe preserved run history.
- #4959 — 13 `scripts/ops/*` DB-mutators routed through the canonical `trade_journal_db_path()` resolver.
- #4960 — G housecleaning (safety/doc-drift fixes + dead-code).
- #4961 — tailscale integration + `ml/predictors/external.py` (`ExternalPredictor`) removed (operator-confirmed irrelevant).
- #4962 — `scripts/init_db.py` + `scripts/daily_heartbeat.py` canonical DB-path; `canonical-db-resolver` guard widened to `("src","ml","scripts")`.
- #4963 — dead `src/core/signals.py` removed; `AccountProfile.from_dict` mode default kept `dry_run` (the read-only-view-safe default); runtime doc-drift fixes.

**slv/gdx real-money wiring (S-AUDIT-H1, Tier-3 operator-approved):**
- #4964 — `slv_pullback_1d` + `gdx_pullback_1d` signal builders (mirrored from the proven `tlt`/`gld` US-ETF daily builders; `htf_pullback_trend_2h` unit, `is_market_open('us_equity')` gate), registered in `intent_multiplexer._default_intent_builders()` + the registration list; `tests/test_slv_gdx_pullback_wiring.py`. They were declared `enabled`/`execution: live` routed to `alpaca_live` (real money) with **no builder** — the MES `MULTI_SYMBOL` stranded-capability class. CI fix: the disabled-gate test must patch `src.units.strategies.load_strategy_config` (source module), not the builder namespace (local import). **Merged + deployed** (loaded on the trader).

**#3910 tail (verify-before-merge):**
- Verified all four items against `main`: **Item 1** (paper/demo PnL) and **Item 2** (closed_at epoch-ms) already FIXED on `main` (so the stale #3910 branch must NOT be merged — it would regress them); **Item 4** is data, not a bug; **Item 3** (bybit_2 min-lot-bump over-risk) was the only live item.
- #4969 — operator-approved **refuse**: `position_size` passes `min_qty=0.0` to `_size_unbounded` (no bump) and refuses a sub-min-lot crypto/fx size (`return 0.0`); futures whole-contract + equity round-up paths preserved. Reworked 3 tests + added `tests/test_sub_min_lot_refuse_3910.py`. **Merged (`744ac178`) + deployed.**
- #4979 — logged `BL-20260628-CRYPTO-INSTRUMENT-MIN-FLOOR` (the instrument-aware platform-floor refinement, ETH 0.01 gap) deliberately scoped out of the surgical real-money PR.

**Closeout:** #4660 (zero-qty obs) rebuilt+merged; #3841 (signals cutover) investigated → **closed not-worth-rebuilding** (durable dual-write half already live; reader-swap marginal; rebuild would revert since-landed fixes); 34 stale issues closed incl. 2 external-intrusion probes (danleejames23) → parallel **security session** spun up (intrusion-hardening PRs #4965/#4967/#4970/#4971/#4972 + dashboard #129; backlog BL-20260628-SEC-HARDENING-FOLLOWUPS).

## Validation Performed
- **Tests:** 168 sizing/risk tests + 6 futures-whole-contract tests pass locally for #4969; `tests/test_sub_min_lot_refuse_3910.py` (refuse + over-risk-avoided + normal-SL no-over-refuse guard + exact-min-lot boundary). CI green on #4964, #4969, #4979 (14/14 checks on #4969). Guards clean: `env_gate_in_diff`, `silent_empty_in_diff`, `canonical_db_resolver`.
- **Walk-forward — N/A by construction (documented honestly):** no backtest harness models the exchange min-lot floor (`backtest_system.py::_risk_qty` = `bal×rpct/stop_dist`, no `max(min_qty,…)`; research harnesses are R-normalized), so bump-vs-refuse is byte-identical in any backtest. The change is therefore provably inert for all normal-balance sizing (zero strategy-perf regression risk).
- **Real-world evidence (diag relay, in lieu of walk-forward):** bybit_2 holds a live `ETHUSDT` 0.01 position (a **legitimate** floored size, raw ~0.015→0.01, NOT a bump); a closed `BTCUSDT htf_pullback_trend_2h` trade was a **confirmed min-lot BUMP** (`position_size` 0.001 on a ~$998 SL, balance back-solved < ~$100) that **lost −$1.05** — exactly the over-risk #4969 refuses; `ict_scalp_5m` sized 0.004 (normal, untouched). Grid sweep: refuse fires only on sub-min-lot sizes, where the old bump risked 2–20× the budget.
- **Deploy verified:** `/api/diag/status` after the #4969 merge — `bot_uptime_s: 26`, `last_tick_utc` current, heartbeat `running`; `slv_pullback_1d`/`gdx_pullback_1d` present in loaded strategies; bybit_2 + alpaca_live live; VM healthy (cpu 10% / mem 9% / disk 27%).

### Gaps not yet verified
- bybit_2 exact equity is **inferred** to ~$51–100 from the sizing (the qualitative bump-vs-legit verdict holds across that band; not pinned to an exact figure).
- `git_sha` on the live web-api read back `"unknown"` (known soft-failure of the resolver) — deploy confirmed via uptime + loaded-strategies instead.
- The #4969 refuse path is now live but no sub-min-lot refusal has yet been observed in production (it fires only on wide-SL/small-balance signals).

## Documentation Updated
- ROADMAP.md — M17 breakdown: added S-AUDIT-G + S-AUDIT-H rows (this PR).
- This sprint log (this PR).
- `docs/claude/health-review-backlog.json` — `BL-20260628-CRYPTO-INSTRUMENT-MIN-FLOOR` (#4979).
- `tests/test_slv_gdx_pullback_wiring.py`, `tests/test_sub_min_lot_refuse_3910.py` (new).

## Contradictions or Drift Found
- None new across the canonical set (the `canonical-doc-coherence` checker passes; no canonical doc described the now-removed crypto min-lot bump). The earlier H3-F5 "AccountProfile mode default" reclassification (won't-fix) was resolved in #4963.

## Risks and Follow-Ups
- **Follow-up (logged):** instrument-aware platform-min floor — refuse on the venue's real lot (ETH 0.01) rather than the account-default 0.001; the clean rebuild path is in the closed #3910 diff. Tier-3. `BL-20260628-CRYPTO-INSTRUMENT-MIN-FLOOR`.
- **Security:** the Tier-2/operator-only intrusion-hardening items are tracked in `BL-20260628-SEC-HARDENING-FOLLOWUPS` (owned by the security session).

## Deferred Items
- None from this thread; all loose ends (stale PRs #4660/#3841/#3910, 34 issues) resolved.

## Next Recommended Sprint
- A future `/system-review` to confirm the #4969 refuse fires correctly on a real sub-min-lot bybit_2 signal (the one production-observation gap above), and to drain `BL-20260628-CRYPTO-INSTRUMENT-MIN-FLOOR` if the ETH-lot refinement is wanted.

## Wrap-Up Check
- [x] Code inspected directly (risk.py, clients.py, builders, instruments.yaml — file:line cited above).
- [x] Docs reviewed/updated (ROADMAP M17 row + this log + backlog).
- [x] TRADE-PIPELINE: no pipeline *stage* changed (a sizing refusal within the existing per-trade gate; no new order path).
- [x] Roadmap checked + updated.
- [x] Contradictions recorded (none new; coherence checker passes).
- [x] Unknowns stated (Gaps not yet verified).
- [x] Doc-freshness run at close.
