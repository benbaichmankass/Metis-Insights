# S-M19-FC-SHADOW-T12-P1 — T0.4 fc head → live shadow + T1.2 SSL-encoder P0/P1

## Date Range
- **Start:** 2026-07-03
- **End:** 2026-07-03

## Objective
- **Primary:** graduate the M19 **T0.4 forecast (`fc_*`) head** to a real **live shadow soak** — the first fc-conditioned model observing on the money box — after the T1.1 deep-sequence clean negative pointed the program at (a) shipping the strongest offline lever and (b) breadth.
- **Secondary:** push **Track 2 (T1.2 "reads-everything" SSL encoder)** from design → the leakage-safe corpus panel (P0) → the masked-reconstruction encoder code (P1), teeing up the ledger-gated GPU-burst pretrain (P1b) for an operator go-signal.

## Tier
- **Tier 1** for all landed changes. The fc-serve producer/reader + P1 encoder are trainer-side / offline-only observe paths (no `src/runtime` order-path, `config/`, or live unit touched). The **candidate→shadow promotion** is the operator-**pre-authorized** autonomous step (observe-only; shadow never influences an order — the shadow→advisory money gate remains Tier-3/operator).

## Starting Context
- **Active roadmap:** M19 § T0.4 (fc, the strongest + base-rate-robust offline lever, at candidate) and § T1.2 (the label-free SSL encoder frontier). T1.1 closed a **clean negative** (TCN below the tree — the 2nd price-only-representation negative → breadth is the untested lever).
- **Prior sprint:** [`S-M19-T1.1-DEEP-SEQUENCE-2026-07-02`](S-M19-T1.1-DEEP-SEQUENCE-2026-07-02.md).
- **Operator grants (standing):** ≤ $5 autonomous GPU spend within the $10/mo ledger cap; candidate→shadow promotion autonomous (observe-only); public-code/public-data-only on any rented pod.
- **Known risk:** a wired-but-blind shadow head if the producer isn't scheduled (stale fc artifact → `forecast_live` returns None, fail-permissive) — addressed by scheduling the producer BEFORE promoting.

## Repo State Checked
- Branch/commit: worked from `origin/main`; PRs merged in order **#5476/#5477** (fc-serve producer + live reader, prior in-session) → **#5475** (corpus widen) → **#5480** (T1.2 design) → **#5478** (corpus_panel) → **#5484** (producer timer) → **#5487** (P1 SSL encoder). Final `main` @ `33aa3af` (#5487).
- Deployment: live trader `ict-bot-arm` (141.145.193.91) auto-deploys `main` via `ict-git-sync`; trainer `ict-trainer-vm` (158.178.209.121) pulled via the `trainer-vm-diag` relay.
- Canonical docs reviewed: `CLAUDE.md` (dashboard REST-API + env-gate rules), `ROADMAP.md` M19 §, the T1.2 design doc.

## Files and Systems Inspected
- `ml/shadow/factory.py` (`_resolve_default_registry_root` → live root = `$DATA_DIR/runtime_logs/trainer_mirror/models`), `src/runtime/regime_bar_scoring.py` (`_resolve_regime_predictors` cache keyed on the shadow-id tuple → hot-reloads on a promotion), `src/runtime/forecast_live.py`, `scripts/ml/publish_live_forecasts.py`, `scripts/ops/publish_trainer_mirror.sh` (forecasts rsync block), `scripts/ops/install_trainer_publish_units.sh`, `deploy/training-vm-cloud-init.yaml`, `ml/cli.py` (`promote-stage`, `list-models`), `ml/trainers/onnx_export.py` + `base.py`, `ml/trainers/torch_sequence.py` (T1.1 precedent), `ml/datasets/{corpus_store,embedding_features,families/corpus_panel}.py`.
- Services/timers: `ict-trainer-publish.timer` (2-min mirror), **new** `ict-trainer-forecast.timer` (15-min fc producer). GitHub Actions: `trainer-vm-diag`, `vm-diag-snapshot` relays.

## Work Completed
1. **fc-serve producer scheduled on the trainer (#5484).** New `scripts/ops/run_forecast_producer.sh` (body of `ict-trainer-forecast.service`) + `install_trainer_publish_units.sh` and `training-vm-cloud-init.yaml` install `ict-trainer-forecast.{service,timer}` (15-min cadence, `Nice=10`). Produces `runtime_logs/trainer_mirror/forecasts/<SYMBOL>.json`; the existing 2-min publish mirrors them to the live VM.
2. **Promoted the fc head candidate→shadow.** `python -m ml promote-stage btc-regime-15m-lgbm-fc-pcv-v1 --new-stage shadow` (registry stage_history entry at 11:47:38 UTC); publish mirrored the flip + model bundle to the live VM.
3. **Track 2 — T1.2 toward the encoder:** widened the keyless-FRED corpus with FX majors + gold/natgas (**#5475**); merged the **T1.2 SSL-encoder DESIGN** (**#5480**); merged the leakage-safe **`corpus_panel`** date×series matrix family (**#5478**); built + merged **P1 — the masked-reconstruction SSL encoder** (**#5487**): `ml/trainers/ssl_corpus_encoder.py` (MAE trainer, fold-frozen standardizer baked into the exported graph, encoder-trunk-only ONNX via the reused fail-closed parity gate, torch lazy), `ml/predictors/ssl_corpus_encoder.py` (onnxruntime CPU embedding predictor), `ml/datasets/corpus_embedding_features.py` (offline side-stream block, one-day-lag as-of join, neutral-on-failure), 14 no-torch contract tests + a torch/onnx end-to-end test.

## Validation Performed
- **Producer runs on the trainer** (relay #5481): `forecast_available: True`; wrote valid `fc_row` artifacts for BTCUSDT + ETHUSDT (all 6 `fc_*` cols, fresh `as_of_ts`).
- **Registry state** (relay #5483): `btc-regime-15m-lgbm-fc-pcv-v1` at `candidate` (macro_f1 0.60 / purged 5-fold / n_eval 87.6k) with its bundle on disk — a valid promotion target.
- **Timer live** (relay #5485): `ict-trainer-forecast.timer` enabled + active; the install-time immediate run exited 0 and wrote fresh artifacts (CPU ~7s).
- **Promotion applied** (relay #5486): `target_deployment_stage: shadow`; publish `published → 141.145.193.91`.
- **Soak accrues on live** (relay #5489): `/api/diag/shadow_stats` for the fc head — **4 shadow predictions**, first 12:01:18 (~14 min post-promotion, first 15m cycle), scores 0.51–0.95 (non-degenerate).
- **fc-conditioning confirmed** (relay #5490): the fc head's `shadow_predictions.jsonl` `feature_row` carries **populated** `fc_*` values (`fc_range_rel: 0.00453`, `fc_skew: -0.291`, `fc_q10_rel: -0.00317`, …) — `forecast_live` → `regime_bar_scoring` → the shadow head end-to-end, scoring on real forecast features.
- **P1 code:** `ruff` clean on all 5 files; **14 no-torch contract tests pass** (windowing leakage-safety, mask convention, fold-frozen standardizer = serve-uses-train-stats, anti-fabrication `missing→0.0`, predictor degrade, block as-of/one-day-lag); `import ml.trainers.ssl_corpus_encoder` succeeds with torch absent (lazy-torch proven). Contracts against T1.1 verified by reading (`onnx_export.export_and_verify` return shape, `Trainer`/`Predictor` ABCs).
- **GPU spend:** **$0 this session** — the ledger was untouched (P1 is code + no-torch validation; the burst is P1b, gated).

### Gaps not yet verified
- The **torch/ONNX end-to-end + fail-closed parity** path of the SSL encoder runs on a torch-present host (trainer/burst), not this sandbox — it is exercised for the first time by the P1b burst.
- The fc soak has only its first ~4 predictions — the **track-record accrual** (drift/edge vs the incumbent) is a multi-day observation, not verified here.

## Documentation Updated
- **ROADMAP.md** M19 §: appended live-status to **T0.4** (fc head → shadow, producer scheduled, soak verified), the **Data** row (corpus widened + `corpus_panel` matrix), and **T1.2** (design + P0 + P1 landed; P1b teed up).
- This sprint log.

## Contradictions or Drift Found
- **Design-doc drift (pre-existing, noted):** `T1.2-ssl-encoder-DESIGN.md` §7 marked "P0 — corpus_panel … Landed" while the family PR (#5478) had not yet merged when P1 was built. Now consistent (both landed). No fix needed beyond this note.

## Risks and Follow-Ups
- **Infra hygiene (logged for backlog):** the PM-session `DIAG_BASE_URL` env points at the **terminated** x86 micro `158.178.210.252`, not the live VM `141.145.193.91` — so the direct diag path is dead and live reads must go through the `vm-diag-snapshot` relay. A cloud-env config fix (not a repo change).
- **Session crons are unreliable** — a `*/15` re-check cron vanished between turns; the soak verification was closed via manual relay re-checks instead. Do not depend on `CronCreate` for load-bearing wakeups.

## Deferred Items
- **T1.2 P1b prerequisite (free code):** a reconstruction evaluator + candidate manifest so the encoder is trainable via the `ml` CLI / burst (the current `ml/experiments/runner.py` always scores a label; the SSL encoder emits an embedding). `fit()` already returns `reconstruction_loss`/`val_loss`/`parity_max_abs_diff` inline.
- **T1.2 P1b (spend):** the ledger-gated GPU-burst pretrain — **awaiting operator go-signal**.
- **T1.2 P2:** wire `corpus_embedding_path` into `market_features` (mirrors `embedding_path`) + `scripts/ml/build_corpus_embeddings.py`.
- **Corpus width:** the stooq/yfinance breadth adapter (sector ETFs, crypto breadth).

## Next Recommended Sprint
- **Build T1.2 P1b's free half** (reconstruction evaluator + candidate manifest), then on the operator go-signal fire the ledger-gated burst to pretrain the encoder at `candidate`, then P2 A/B (does the `corpus_emb_*` block beat BOTH the no-embedding baseline AND the frozen-Chronos T0.1 embedding?). Required verification: the burst's fail-closed ONNX parity gate passes; the A/B runs under purged walk-forward.

## Wrap-Up Check
- [x] Code inspected directly (factory/regime-scorer hot-reload path, onnx_export/ABC contracts, the 5 P1 files read in full before landing).
- [x] Docs reviewed + updated (ROADMAP M19 rows + this log).
- [x] TRADE-PIPELINE: unchanged (no pipeline stage changed — observe-only shadow path).
- [x] Roadmap checked + updated.
- [x] Contradictions recorded (design-doc P0 drift; DIAG_BASE_URL staleness).
- [x] Unknowns stated (torch/ONNX parity runs on the burst; soak track-record is multi-day).
