# Sprint Log: S-M15-PHASE3-AND-2B-2026-06-11

## Date Range
- Start: 2026-06-11 (operator: "merge and continue", post-#3324)
- End: 2026-06-11

## Objective
- Primary goal: Phase 3 — assign the Phase-0 verdict's lead strategy
  (`xauusd_trend_1h`) to `oanda_practice` in shadow, with the FX
  weekend gate. Phase 2b — wire the second platform (Alpaca) end to
  end so the whole milestone converges on credentials only.

## Tier
- Tier 3 throughout (strategies.yaml / accounts.yaml / order path) —
  each phase shipped as a draft PR and merged only on explicit operator
  approval in chat (#3325: "ok merge and continue"; #3326: "approved").

## Repo State Checked
- Branch `claude/happy-heisenberg-p3vt9j` over `main` (post-#3324).
- Read in full before changing: `mes_trend_long_1d` YAML entry +
  builder + multiplexer/intents registration (the clone template),
  `backtest_trend.py` defaults (the sweep-validated params),
  dry-run-guard marker contract (`scripts/check_dry_run_in_diff.py`),
  roster contract tests.

## Work Completed
- **Phase 3 (#3325, MERGED)**: `xauusd_trend_1h_signal_builder`
  (bidirectional trend_donchian reuse; FX weekend gate → side=none,
  `fx_market_closed`; fail-permissive), multiplexer + intents
  registration, strategies.yaml entry pinned to the sweep params
  (donchian 20 / ATR 14 / stop 2.5× / trail 3.0× / min_confidence 0.0
  — deliberately not the BTC 0.60 floor), `XAUUSD` instrument profile
  (oanda routing), `oanda_practice` routing, dashboard description,
  7 wiring tests, roster contract tests extended 12→13.
- **Phase 2b (#3326, MERGED)**: `alpaca_client.py` (bracket MARKET
  orders — entry + TP limit + SL stop atomic, whole-share floor, 2dp;
  equity; positions; 404-idempotent close; paper host default),
  `alpaca_client_for`, `AlpacaAPI` + map entry, executor branch,
  coordinator dispatch, inert `alpaca_paper` account (dry_run + marker,
  strategies [], demo, SPY/QQQ/GLD), master-template block, 11 tests,
  runbook `docs/runbooks/alpaca-integration.md`, ARCHITECTURE Step 6.

## Validation Performed
- Local: 84 tests green across alpaca/oanda/xauusd wiring + executor +
  multi-account + drift-guard suites. CI: 11/11 green on both PRs
  (5,538+ tests), including dry-run-guard with the literal
  `shadow-guard: allow` marker.
- CI failures hit and fixed in-sprint: missing shadow-guard marker on
  the new `execution: shadow` line; 4 roster contract tests pinned to
  the 12-name roster (extended to 13, mirroring mes/mgc/mhg).
- Gaps not yet verified: everything credential-gated — OANDA/Alpaca
  smoke tests, candle fidelity cross-checks, the shadow soak itself.

## Documentation Updated
- ROADMAP M15 row (Phases 2+3 merged, 2b built→merged, operator queue).
- New runbook (alpaca-integration.md); ARCHITECTURE Step 6 (both brokers).

## Contradictions or Drift Found
- None new. (Roster tests by design require extension per strategy —
  expected contract, not drift.)

## Risks and Follow-Ups
- **VM deploy deliberately deferred** until OANDA creds land: deployed
  now, the XAUUSD candle fetch would 401 every tick (builder raises on
  None candles → per-tick error noise). One deploy after `sync-vm-secrets`
  covers code + creds together.
- Sizing notes for the smoke tests: XAU integer-oz floor (~$2.3k
  notional min) and SPY whole-share bracket floor (~$600) vs the 0.5%
  risk_pct on small paper balances — verify margin headroom.
- Remaining product decisions (Tier 3): shadow → live-on-practice flips
  per leg after smoke tests; eventual daily-ETF strategy-assignment PR
  for alpaca_paper.

## Blockers
- Operator-only: OANDA practice + Alpaca paper account creds into the
  #3302 placeholder slots, then a ping ("I'll add credentials soon" —
  acknowledged 2026-06-11).

## Next Recommended Sprint
- **S-M15-PHASE4-SOAK** (fires on the creds ping): sync-vm-secrets →
  pull-and-deploy → both smoke tests via the diag relay → candle
  fidelity cross-checks → soak monitoring; then the daily-ETF
  assignment PR.

## Wrap-Up Check
- [x] Code was inspected directly, not inferred only from summaries.
- [x] Documentation was reviewed and updated as part of the sprint.
- [x] Pipeline stage touched (executor) — ARCHITECTURE updated.
- [x] Roadmap status was checked.
- [x] Contradictions were recorded.
- [x] Remaining unknowns were stated clearly.
