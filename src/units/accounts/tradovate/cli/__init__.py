"""CLI utilities for operating the Tradovate integration from a shell.

Every script defaults to demo and supports ``--env demo|live`` to
override the resolved environment without exporting ``TRADOVATE_ENV``.
"""
