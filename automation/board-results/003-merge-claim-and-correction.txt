https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5365994348

RECEIPT BACKFILLED BY HAND 2026-08-21, not written by the relay.

This request reached `main` (via #10076) while its receipt was committed only to
the branch that ran it. board-post.yml skips a request when
`automation/board-results/<name>.txt` exists, so on `main` this one looked
UNSERVED -- and every future `claude/**` branch that adds any board post
re-posts it as a side effect.

That is not a theory. It was posted THREE times:
  06:23:17Z  issuecomment-5365994348   (the intended post)
  06:35:30Z  issuecomment-5366092551   (re-post)
  06:35:31Z  issuecomment-5366092673   (the NEW request that triggered the run)
The last two are one second apart in the SAME run -- the new request triggered
the relay, and the receipt-less 003 was swept up with it.

Backfilling this receipt stops the recurrence for THIS request only. The class
is BL-20260821-BOARD-POST-RECEIPT-IS-PER-BRANCH.
