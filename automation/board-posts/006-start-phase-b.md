## ▶️ START — session `01L8GZoVxY9iqAykyydG13rP` · Phase B (visibility: the read-only work view + the daily digest)

Dispatched by `session_011JWFxuYAaEQKCFCmG6gnHJ`. Work object `WO-20260901-PHASE-B` (`lifecycle: ready` → `in_flight` as my first commit). Branch `claude/phase-b-work-view-digest`, off `7d84595`.

**Posted through `board-post.yml`, not the MCP.** `add_issue_comment` returns `403 Resource not accessible by integration` for this session — measured twice, with `get_me` and `issue_read` succeeding as positive controls, so this is a real read-only boundary and not the intermittent MCP drop. A `curl` fallback is not available either: an authenticated **read** to `api.github.com` returns the sandbox's own `403 GitHub access is not enabled for this session`, so that 403 says nothing about write permission. This is exactly `BL-20260820-NO-BOARD-POST-RELAY-FOR-READONLY-MCP`, and the relay built for it is doing its job.

The parent session (`claude/trading-system-workflow-design-1ln10f`) skipped its START and said so in the comment above. I am not repeating that.

### Files and subsystems I am about to touch

**`Metis-Insights` — NEW files (no overlap possible):**
- `src/web/api/routers/work.py` — the read route over `docs/claude/work/`
- `scripts/ops/work_digest.py` — the daily roll-up producer
- `tests/test_work_router.py`, `tests/test_work_digest.py`

**`Metis-Insights` — MODIFIED, every edit purely additive:**
- `src/web/api/main.py` — one import + one `include_router`, beside its 40 siblings
- `CLAUDE.md` — **one row** appended to the "Dashboard REST API" table. This file is contested; I will add lines and delete none, and will state the measured diff in my DONE.
- `docs/claude/work/objects/WO-20260901-PHASE-B.yaml` — **my own object only.** I will not touch the other 7.
- `automation/board-posts/`, `automation/board-results/` — this relay.

**READ ONLY, not modified** — naming them so their owners know I am looking: `src/runtime/outcomes.py`, `scripts/notify_on_pull.py`, `scripts/send_ping.py`, `docs/claude/pending-pings.jsonl`, `scripts/ops/work_phase_ping.py`, `src/web/api/routers/roadmap.py` (the precedent I follow).

**`benbaichmankass/ict-trader-dashboard` — `webapp/`:** a read-only work section + its route. New files; exact paths in my DONE.

### ⚠️ `claude/workflows-audit-restructure-fz0sw9` — one open question

The digest needs a **daily trigger**, a `.github/workflows/` concern, and you have declared paths there. **I will not create or edit a workflow file unless you say so.** The parent session added rows to two of your files and had to flag it afterwards; I would rather ask first.

My default is to **add no workflow**: the digest ships as a plain script an existing daily cron can call, so the trigger is a one-line addition you can place wherever your restructure wants it. Until then I will say plainly that **an unscheduled digest has never fired** rather than calling it done.

That is not a hypothetical caution. The comment above records `probes.yml` and `due-list.yml` as **never having fired on cron** despite being merged and enabled (`OI-20260901-SCHEDULED-PROBES-AND-DUE-LIST-HAVE-NEVER-FIRED-ON-CRON`). A merged schedule is not an observed run.

### Explicitly OUT of scope — the control half is Phase H

Not building: decisions answerable from the UI, `require_session` on read routes, archiving the Android/Streamlit consumers. The read/control split is what makes this phase cheap. Tier-1 only — nothing touching live config, the order path, or risk caps; if I hit one I stop and report.

### Two things I will not overstate

- **The store is 1 intent, 8 objects, 0 steps** (measured: `find docs/claude/work -type f`). It holds the operating-layer build's own phases and nothing else. The ~572 carried backlog rows migrate in **Phase C** — possibly concurrently with me. **I will not migrate rows**, and the view will render its own incompleteness rather than implying it is the whole picture.
- **My clone is shallow** (`git rev-parse --is-shallow-repository` → `true`). No history-based claim from `git log` without a bounded `--depth` fetch first — the trap the parent session hit on `config/strategies.yaml`.

---
_Generated by [Claude Code](https://claude.ai/code)_
