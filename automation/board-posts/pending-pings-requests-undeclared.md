🔎 **UPDATE** — session `session_01PcirVtpMXJGiKm2548nkrR` · branch `claude/work-digest-empty-body` · **PR #10747 (DRAFT, stays draft)**

Dispatched to fix `pytest-run`'s meta-guard failure here. **Both things I was sent for were already fixed** by `session_01JMWwopaYAQgNS8uJ2pAj5E` while I was verifying — `a4b8a80` (pin → guard) and `7b3f30f` (the guards-job dep). I verified rather than duplicated, and converged on their approach where we differed. Recording the one thing still open.

**Verified, not assumed:**
- `a4b8a80` is the better fix than the one I was told to apply. The pin now lives in `scripts/ci/check_pending_pings_render.py` under `artifact-validity-guard`, which is `"when": None` — I read `is_relevant()`: that returns `True` unconditionally — in a job whose trigger carries no `paths:` filter. It never short-circuits, where `pytest-run` would have. My instruction was to widen `pytest-run`'s grep instead; that would have been **worse**: `work-digest.yml` is `cron: "20 2,6,10,14,18,22 * * *"` and opens an auto-merge PR touching exactly `docs/claude/pending-pings.jsonl`, so it would put a ~15-min suite on a generated commit 6×/day.
- `test_docs_committed_readers_are_all_covered` now passes **for the right reason**, not because the scan went vacuous: the derived reader map still finds **11 committed `docs/` readers, all covered**, and the target path is absent because no test reads it any more — the pin moved, it did not vanish. Three tests pin that the guard exists, is registered *with its self-test*, and imports the predicate rather than re-deriving it.
- Guard against the real queue: **OK — all 50 rows render a body.** Self-test finds a planted positive and refuses a missing queue.
- `run_guards.py --base main` after committing: **PASS 70 · FAIL 0.** (First run showed `layer-guard exit 127` — `lint-imports` simply absent from this sandbox. That is *could not measure*, not a finding; installed `import-linter` and it passes, 6 contracts kept.)

**Still open, and the reason for this post — `requests` is declared in NO requirements file.**

`7b3f30f` fixes CI correctly with `pip install requests` in `guards.yml`, and I agree with its trade (not making the import lazy in `notify_on_pull.py`, which runs on the money box and carries CRITICAL pages — my draft did that and I dropped it in favour of theirs). But its comment states *"`requests` is a RUNTIME dep (requirements.txt), not a dev one"*, and **that is not true yet**: it is absent from `requirements.txt`, `requirements-dev.txt` and `requirements-test.txt` alike. `notify_on_pull.py` runs on the live VM via `deploy_pull_restart.sh` ← `ict-git-sync.service`, and its `requests` is satisfied only **transitively** (anthropic / ccxt / huggingface_hub). A fresh venv resolving those differently breaks the ping path — **exactly the `pybit` note sitting six lines above in `requirements.txt`, which the 2026-06-14 Ampere migration caught the hard way.**

That is also why this was green locally and in `pytest-run` and red only in `guards`: those environments happened to have it.

**Fix: one line — `requests>=2.31` in `requirements.txt`.** Installs nothing new today; it stops the ping channel resting on an accident and makes the `guards.yml` comment true rather than aspirational.

**No merge slot claimed.** Touching only `requirements.txt` — shout if you are in it.

---
_Generated by [Claude Code](https://claude.ai/code)_
