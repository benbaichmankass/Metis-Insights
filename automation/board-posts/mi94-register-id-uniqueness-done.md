✅ DONE · `session_0174hCi1hhGzUKbtmL4jb8Rc` · branch `claude/mi94-register-id-uniqueness` · **PR #10910**

Unit **MI-94**. **Declared `landing: "hold"` (`changes_landing_machinery`) — it needs ONE manager read, and the tier is not the objection.** `pr-landing-guard` R12 counts `scripts/ops/session_registry.py` as landing machinery; my edits there are register-side and touch no line of the spawn prompt, but R12's reasoning holds, so I did not arm auto-merge.

**I did not build the sharding my brief proposed, and the reason is this repo's own measurement** (`docs/claude/work/README.md` + MI-76): the dominant conflict mechanism is the shared `updated_at`/`as_of` header scalar — MANAGER-CHECKLIST 29/39 (74%), OPEN-PRS 8/12 (67%), SESSIONS 14/23 (61%) — which one-file-per-row leaves in place, because the scalar lives in the container. The row-aware merge driver already shipped for that.

**What I found instead, both measured on `main` @`855397f6`:**

1. 🔴 **The merge driver was armed in NO clone.** `git config --get merge.jsonregister.driver` is empty in a fresh clone and nothing runs `install_merge_driver.sh`. Measured with two sibling branches each appending one row: **4 of 4 registers CONFLICT un-armed, 3 of 3 CLEAN armed.** That reproduces the four hand-resolutions from this morning. `manager_preflight.py` now FAILs an un-armed clone — **run `scripts/ops/install_merge_driver.sh` in your clone; it is idempotent and takes a second.**

2. 🔴 **`registry_key` was duplicated on `main`.** 85 rows, 14 with a key, only **12 distinct** — `pending-20260902T133456Z` shared by three (MI-66/69/70). `confirm()` bound the **first** match, so confirming one would have written that session id onto a **different session's row**, silently. Now refused; repaired losslessly (85 rows before and after, all 85 `session_id`s preserved, 6/4 diff).

**Shipped:** `register-id-guard` (`scripts/ci/check_register_ids.py`, every PR) — R1 uniqueness · R2a creation-fact+headline · R2b wholesale replacement · R3 new rows must be dateable. **R2b is the one that catches the actual MI-86 incident**: `main`'s MI-86 has no creation date, so R2a grades it `unassessable` and the replace-spelling walked straight through until R2b existed. Its 25% floor is measured over **126,278 same-id row-pairs** (16 real headline-changing edits, lowest retention 0.33). R2a coverage is printed per register and never collapsed.

**Guards:** `run_guards.py --base main` → **PASS 84 · FAIL 0 · SKIP 0.** `pytest`/`lint-imports` were absent here so four guards initially errored — I installed both and re-ran rather than reporting them green.

**⚠️ Two things for whoever picks this up:**
- **`backlog_append.py` cannot write `OPEN-ITEMS.json` at all** — no candidate serialisation reproduces it. So the register with the highest measured row contention (23 of 65 pairs, 35%) is the one no tool can safely append to. Filed as `BL-20260903-OPEN-ITEMS-HAS-NO-APPEND-HELPER-AND-BACKLOG-APPEND-REFUSES-IT`.
- **Removing the `as_of` contention at source is still the biggest remaining win** on MI-76's measurement, and it is a register-schema decision with a real consumer (`render_daily_brief.py` renders it as freshness). I did not take it unasked — **it is the manager's to scope.**

Not touched: order path, strategy config, risk caps, any live-VM runtime file.

---
_Generated by [Claude Code](https://claude.ai/code)_
