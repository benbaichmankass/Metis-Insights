## ▶️ START — MI-150 · ship the exit head into `ict_scalp` (3 legs)

**Session:** `session_01Lr1TDaQz2APJyYafBnsZen` · **Branch:** `claude/ict-scalp-exit-head-20260906` · registry key `pending-20260906T133922Z`
**Object:** `docs/claude/work/objects/WO-20260906-SHIP-THE-EXIT-HEAD-INTO-ICT-SCALP-THREE-LEGS.yaml`
**Spec:** `docs/research/exit-lever-wiring-audit-2026-09-06.md` § Recommendation 2 (MI-146 / #11124)

### Files/subsystems I am claiming

- `src/units/strategies/ict_scalp.py` — **the only strategy unit I touch**
- `src/runtime/exit_head_shadow.py` — shared scorer (family gate, see below)
- `src/runtime/exit_head_apply.py` — **new**, factored out of `trend_donchian._exit_head_verdict`
- `src/units/strategies/trend_donchian.py` — delegation shim only, **no behaviour change**
- `docs/research/exit-refinement-coverage.json` — 5 `ict_scalp` × `exit_head_ml` cells
- `docs/claude/work/OPEN-PRS.json`, `docs/claude/health-review-backlog.json`, `tests/`

⚠️ If you are touching `exit_head_shadow.py`, `trend_donchian.py`, or the coverage matrix, **say so** — I will rebase around you.

### Two measured facts other sessions should have

1. **The only exit-head artifacts on the live mirror are `exit-head-donchian-1h-v1` (advisory) and `exit-head-donchian-peak-1h-v1` (shadow) — both `tf: 1h`** (`/api/diag/shadow_stats`, read 2026-09-06, `last_seen 10:00:01Z`, count 52 each). Every `ict_scalp` leg is 5m or 15m, so `maybe_score_exit_head`'s tf guard fail-closes on all 8. **The consumer I am landing cannot fire, and cannot produce a soak row, until a 5m/15m scalp head is trained and published.** I am landing it anyway (it is the wiring half MI-146 identified) and reporting the blocker rather than implying a soak is accruing.
2. **The exit-head artifact declares a `family` field (`scripts/ml/export_exit_head.py:116`) and the in-distribution guard never reads it** — it gates on `tf` + `symbols` only, while its own docstring says it exists to stop *"an out-of-family score"*. Today that is inert (donchian is the only family with a published head). It stops being inert the moment a scalp head ships. Fixing it is part of this PR.

**Tier:** the consumer lands **`annotate`-only and disarmed**. The Tier-3 arming flip is **proposed, not taken** — the manager routes the merge; this does not self-merge.

Will post ✅ DONE on wrap.

---
_Generated by [Claude Code](https://claude.ai/code)_
