🔁 **Follow-up on Phase D** (previous post: `007-phase-d-constraint-readout.md`) — **PR #10680**, head `800e941`.

**Two corrections to my own work since that post, both caught before merge:**

**1 · `guards` went red in CI and it was mine — two `ruff` F541 errors** (f-prefix on constant table-header strings) in `scripts/ops/constraint_readout.py`. ⚠️ **Worth flagging as a trap, not just a typo:** my local `run_guards.py` reported **PASS** while explicitly printing *"NOT SELECTED because the work is UNCOMMITTED (18)"* — and `ruff-lint` was one of those 18. **A local green on uncommitted work has not checked what CI checks.** Read that warning; I read past it. Fixed and verified with `ruff check .` → clean, plus a `--all` run: **PASS 64 / FAIL 3**, all three failures missing sandbox tooling (`pytest` ×2, `lint-imports`), not defects.

**2 · A whole-file reformat of `OPEN-ITEMS.json`, reverted.** A naive `json.dumps(indent=2, ensure_ascii=False)` round-trip produced **589 insertions / 567 deletions** for a one-row add — the `BL-20260820-BACKLOG-APPEND-REFORMATS-AND-REATTRIBUTES` defect. ⚠️ **Cause, for anyone else about to write this file: its escaping is MIXED** — literal em dashes near the top, `⚠️` escapes further down — so **no single serialisation reproduces it** and `detect_format` correctly refuses rather than writing. `backlog_append.py` therefore cannot help you here. I reverted and inserted the row as **text** at the array boundary, then asserted the file still parses, that the id list is exactly the old one plus mine, and that every other item and top-level key is byte-identical. Result: **21 insertions, 0 deletions.**

**Register updated:** `OI-20260901-CONSTRAINT-READOUT-SHIPPED-AND-IT-REFUSES-NOBODY-HAS-ACTED-ON-THE-REFUSAL` (loud, monitoring, 3d, `verified_at: null`). What is unproven is **not** that the readout is wrong — it is that a permanently-refusing readout gets skimmed past. Its `clears_when` **explicitly refuses a bulk edge-writing pass**, because that would reproduce the false-blocker defect Phase C correctly avoided.

**Status:** local verification complete; **the CI verdict on the fix is still PENDING** — `guards` and `pytest-collect` have been `in_progress` ~17 min against ~2 min for the prior run on the same branch, which looks like runner queueing rather than the change. **I have not seen it green and am not claiming it.** Whoever picks this up: check #10680's checks before merging, and note the PR is a **draft**.
