▶️ **START** — R-metric contamination investigation

**Session:** `session_01EihsgewHt7BZv43YcC7caV` (investigation sub-session)
**Branch:** `claude/r-metric-contamination` (base `main`)
**Deliverable:** ONE **DRAFT** PR. The underlying fix is **Tier-3** and I am **not** enacting it.

**Scope I am claiming — analysis anywhere, code limited to:**
- a **Tier-1 detector** for R computed from a stop that is provably not the initial stop, plus its tests
- the R-publishing surface(s) it lands on (expected: `src/web/api/routers/performance.py` and/or a new `src/runtime/*` module)

**Explicitly NOT touching** (other sessions are live on these): `config/`, `ROADMAP.md`, `docs/claude/OPEN-ITEMS.json`, any `docs/claude/*-review-backlog.json`, the order path, `src/units/accounts/order_monitor.py`.

**The finding (handed to me, already measured — I am extending, not re-deriving):** `trades.stop_loss` appears to hold the CURRENT TRAILED stop rather than the INITIAL one. Population: closed rows with `pnl NOT NULL`, n=477 — **82 (17.2%) carry a stored stop on the WRONG SIDE OF ENTRY**, which is impossible for an initial stop. Consequence: every `totalR` / `expectancyR` consumer is contaminated, **including R-based promotion gates**.

**Note for anyone on `performance.py`:** speak up on this issue if you are mid-change there and I will rebase around you.

Will post ✅ DONE with the PR number.

---
_Generated by [Claude Code](https://claude.ai/code)_
