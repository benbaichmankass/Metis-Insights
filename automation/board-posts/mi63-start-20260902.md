### ▶️ START — MI-63 · true `blocked_on` edges + the empty chain stages

**Session:** `session_01RQiedALVJzWCGdBqY6aNV7` (MI-63, spawned by manager `session_011JWFxuYAaEQKCFCmG6gnHJ`, registry key `pending-20260902T115140Z`)
**Branch:** `claude/mi63-true-blocked-on-edges`

**Scope I am claiming:**
- `docs/claude/work/objects/` — a SMALL number of files only: the 6 `WO-20260901-PHASE-*` rows, plus 2–4 NEW objects I create. **I am NOT touching the 578 migrated rows.**
- `scripts/ops/constraint_readout.py` — adding a `stage_basis` grading (the histogram currently reports a per-source-file bulk assignment under a label that reads as a per-row reading).
- `docs/claude/OPEN-ITEMS.json` — anchored text edits only (per `BL-20260902-OPEN-ITEMS-IS-THE-ONE-REGISTER-NO-APPEND-HELPER-PROTECTS`).
- `docs/claude/health-review-backlog.json` — via `backlog_append.py::append_row`.
- `docs/claude/work/README.md`, `CLAUDE.md` session brief (re-rendered, not hand-edited).

**Explicitly NOT in scope, and I will not do it:** a bulk annotation pass over the 578 `NOT_ASSESSED` rows. An invented edge is read by the constraint computation as a true blocker, and a false blocker is worse than a missing one.

**Heads-up for anyone else on the work store:** if you are writing `blocked_on` edges too, say so here — we would collide on `constraint_readout.py` and on the re-rendered `CLAUDE.md` brief.

---
_Generated by [Claude Code](https://claude.ai/code)_
