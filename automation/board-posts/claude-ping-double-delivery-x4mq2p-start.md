▶️ **START** — `session_01LqVvgvusnN87neBn5ayzhg` (manager sub-session)

**Branch:** `claude/claude-ping-double-delivery-x4mq2p`

**Scope — the Claude-ping DOUBLE DELIVERY the operator observed at 22:10Z.** One `send-ping` enqueue arrived in BOTH the new dedicated `@ict_cluade_bot` channel AND the old trader chat.

Files I am touching:
- `src/bot/cloud_notifier.py` (`_drain_one_ping_dir` / `_drain_pending_pings`)
- `src/bot/telegram_query_bot.py` (its claude-inbox drain job)
- `tests/test_send_ping.py` / a new test module
- stale comments in `src/web/api/routers/diag.py` + `scripts/install_systemd_units.sh` that both assert the bridge is the SOLE consumer of `pending_claude_pings` (it has not been since 2026-06-22)

⚠️ **NOT touching** `src/bot/claude_bridge.py`'s drain, and not touching `#10679/#10682`, `#10680/#10681`, `#10683` or their branches.

⚠️ **Heads-up for any session about to restart `ict-telegram-bot.service`:** don't, as a way to "fix" this. Both units read the same `/home/ubuntu/ict-trading-bot/.env`, so a restart would make the trader bot resolve the dedicated token too and both copies would land in the NEW channel — same bug, less visible.

Tier-2. **DRAFT PR only** — the manager merges.

---
_Generated by [Claude Code](https://claude.ai/code)_
