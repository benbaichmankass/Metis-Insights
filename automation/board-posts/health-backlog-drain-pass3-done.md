### ✅ DONE — health-review backlog DRAIN, pass 3 · [PR #10741](https://github.com/benbaichmankass/Metis-Insights/pull/10741) (DRAFT, all 4 checks GREEN)

**Left as a DRAFT for the manager. Not merged, auto-merge not enabled.** Head `c93ff7ab`, base `main`.

`pytest-collect` ✅ · `pytest-run` ✅ (**14m39s — a real run, not the docs-only short-circuit**) · `guards` ✅ · `repo-inventory` ✅

#### Burn-down (denominator 1,100 rows / 524 unresolved at base `b3772e3a`)
**2 CLOSED · 1 REFUSED · 2 FILED · net unresolved 524 → 524 (unchanged).** Saying that plainly rather than leading with the class retirement.

#### The class
Two rows shared one **mechanically-verifiable** exit — drive `scripts/check_diagnostic_provenance.py --all` to zero. **52 → 0**, all triaged per site, never mass-annotated. Split at the start: D/inert 26 · B/implicit-input 13 · C/unquantified 8 · A/semantic-substitution 5.

The residue had sat at **exactly 52 for 26 days across five review passes**, so two things were done beyond the drain:
1. **The `# inert:` override was cheaper to lie to than to satisfy** — a bare presence-only regex, the `new-table-wiring-guard` failure this repo has already paid for, and one this guard's own test-module docstring already said must not exist. It must now **name the parameter**. Tightening it immediately exposed **11 markers across 4 files** that named nothing while the tree reported OK.
2. **The zero is now HELD** — the guard carries an **ungated whole-tree `--all` step** (the `api-tier-policy-guard` pattern). It could not have existed yesterday; it would have failed every PR on day one.

#### Two things the next session should not have to rediscover

⚠️ **`scripts/check_writer_conformance.py` was a CI guard printing `clean` with no denominator** — a diff with nothing scannable rendered byte-identical to one fully scanned and clean. Fixed; observed live as `clean over 243 added line(s)`. Worth a look at any other guard whose summary line carries no count.

⚠️ **A PR opened by `pr-opener.yml` gets ZERO check runs and stays that way** — GitHub does not fire workflows for a PR opened by `github-actions[bot]` with `GITHUB_TOKEN`. `mergeable_state` reads `blocked`, **not** `dirty`, so it is not the merge-conflict case the docs warn about, and waiting does not help. One ordinary commit pushed to the head branch arms CI. This is a *different* trap from the documented results-commit one, and it costs a full CI cycle to discover.

#### Files released — nothing of mine is in flight
`scripts/{ml,research,ops,macro,reports}/` (diagnostic sites) · `ml/datasets/families/market_features.py` · `ml/evaluators/{classification,classification_auc,reconstruction}.py` · `scripts/check_diagnostic_provenance.py` + `tests/test_diagnostic_provenance_guard.py` · `scripts/ci/run_guards.py` · `docs/claude/health-review-backlog.json` · one `CLAUDE.md` paragraph.

**Deliberately NOT touched:** the ml / performance / research backlogs (drains are live on those), `docs/claude/OPEN-ITEMS.json`, `config/`, `ROADMAP.md`, and every order-path and Tier-3 file. No live-fleet read was made or needed — the whole class is static code, which also means **nothing in this PR is evidence about the running trader**.

Full population, measurements, positive controls, the refusal's reasoning, and an OBSERVED-vs-INFERRED caveats section are in the PR body.

---
_Generated by [Claude Code](https://claude.ai/code)_
