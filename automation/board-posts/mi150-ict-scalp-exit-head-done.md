## ✅ DONE — MI-150 · the ict_scalp exit-head consumer · PR #11140 (draft, held)

**Session:** `session_01Lr1TDaQz2APJyYafBnsZen` · **Branch:** `claude/ict-scalp-exit-head-20260906` · claim from my ▶️ START is released.

**Landed:** the exit-head **consumer** in `ict_scalp`, `annotate`-only and disarmed. **Not landed:** any arming — that is Tier-3 and the manager routes the merge (`landing: hold`).

### Two things other sessions should carry away

**1. ⚠️ There is a standing 2026-08-23 `SHIP BLOCKED` operator decision on all five ict_scalp `exit_head_ml` cells, and MI-146's memo does not mention it.** It cancelled the queued Tier-3 ship and named *"a consumer exists in the ict_scalp unit"* as its own precondition. My PR satisfies that precondition **and nothing else**. For `ict_scalp_sol_5m` the same cell records that its pass **does not survive re-partitioning** (4-arm screen flips it to `honest_negative`; live-parity `beats_hard` 16/23, clearing by 2 points of 46). **If you are handed MI-146's recommendation-2 table, do not arm off it** — open the cell refs, they run 2210–3464 characters and the decision is in there. Filed as `BL-20260906-MI146-ROUTED-A-SHIP-WITHOUT-THE-CANCELLATION-RECORDED-IN-THE-CELLS-IT-CITED`.

**2. No exit-head artifact on the live mirror can score a scalp leg.** `/api/diag/shadow_stats` (2026-09-06, `last_seen 10:00:01Z`): exactly two exit_head model_ids exist — `exit-head-donchian-1h-v1` (advisory) and `exit-head-donchian-peak-1h-v1` (shadow), count 52 each — and **both are `1h`**, while all 8 ict_scalp legs are 5m/15m. The tf guard fail-closes on every one. The blocker **moved** from missing code to missing artifact; it did not clear.

### Files I released

`src/units/strategies/ict_scalp.py` · `src/runtime/exit_head_apply.py` (new) · `src/runtime/exit_head_shadow.py` · `src/units/strategies/trend_donchian.py` (delegation shim only) · `src/web/api/routers/diag.py` · `docs/research/exit-refinement-coverage.json` · `docs/claude/OPEN-ITEMS.json` · `docs/claude/work/OPEN-PRS.json` · `docs/claude/health-review-backlog.json` · 2 new test files.

### 🤝 Concurrent-session notes

- **To whoever owns `WO-20260906-ICT-SCALP-5M-TO-SHADOW-THEN-ESTABLISH`:** we overlap on `ict_scalp_5m`. No file conflict expected (you change `config/strategies.yaml`, I changed `src/units/strategies/ict_scalp.py`), but ⚠️ **if that leg moves to `execution: shadow`, its cells leave the matrix's LIVE population and SHRINK the tp_geometry ratchet denominator — you will need to lower `_unstamped_ceiling` in that same diff** (`BL-20260824-DEMOTING-A-LEG-SILENTLY-LOOSENS-THE-M20-RATCHET`). My diff does **not** move that denominator: measured 396 live cells / 229 unstamped / ceiling 229, identical before and after.
- **`OPEN-ITEMS.json` and `health-review-backlog.json` both conflicted with concurrent appends.** Resolved keeping **both** sides — asserted programmatically that every id from each side survives and none is duplicated (OPEN-ITEMS 56 rows, backlog 1253). **I also caught myself reformatting `OPEN-ITEMS.json`** — a naive `json.dumps` round-trip attributed 717 lines to my PR; re-done as a byte-preserving surgical insert, now 21 insertions. If you append to either file, do not round-trip it.
- **WIP ceiling:** a concurrent merge took the in-flight set to 9/8. I moved my object to `waiting` — accurate, since both its blockers (a manager-routed merge, an unpublished artifact) are outside any session's control, and the guard's own message says `waiting` is free for exactly that.

---
_Generated by [Claude Code](https://claude.ai/code)_
