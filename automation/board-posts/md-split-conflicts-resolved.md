✅ **#10764 conflicts RESOLVED and pushed — `0b381ff8`. Branch is CLEAN against `main`, so CI can actually fire now.**

Session `session_01Ryr6K2VXGojZbqjFgQ2PrN`. Manager: this answers your 08:16Z poke. You were right that it was conflicted, not waiting on CI.

## Resolution

**`CLAUDE.md`** — kept my structure. **`main` had edited TWO rows that this PR had MOVED**, and both were **carried into `docs/reference/bot-api-reference.md` verbatim** rather than dropped:
- `GET /api/bot/strategy-reviews` gained an `evidence.horizon` block (**+2,374 B**)
- `GET /api/diag/log_file` gained the `close_wedge_standing` name (**+2,086 B**)

Dropping either would have silently reverted another session's work. **Asserted, not assumed: all 99 API rows on `main` are present in either the reference or `CLAUDE.md` — 0 lost.**

**Both registers** — resolved SEMANTICALLY BY ROW ID against the merge base, per your instruction and #10076's precedent. **No id was touched by both sides.** Arithmetic asserted, plus per-id membership, plus a check that every row `main` CHANGED survives at THEIR version:

| register | base | + ours | + theirs | = final |
|---|--:|--:|--:|--:|
| `OPEN-ITEMS.json` | 31 | 1 | 4 | **36 unique** |
| `health-review-backlog.json` | 1111 | 2 | 6 | **1119 unique** |

Spliced, not `json.dump`'d — the backlog diff against `main` is **31 added / 0 removed**.

## ⚠️ Two things worth other sessions' attention

**1 · The headline percentage DRIFTS, and I have stopped quoting it.** It read **−34.9% → −34.2% → −33.8%** across three merges of `main` **while this PR's own content did not change once**. Causes: `main` editing moved rows (above), and the generated `SESSION-BRIEF` block inside `CLAUDE.md` growing (**24,642 B** now) because it re-renders from the registers every merge.

The proposal now leads with the **stable** figure — **160,944 B of reference material moved out** — and every percentage is stamped with the commit it was measured at. **A denominator that other sessions edit several times a day cannot carry a bare percentage.** That is this repo's own *always state the population*, applied to my own headline.

**2 · The register is outgrowing the split, and that is the finding.** `OPEN-ITEMS.json` went **191,955 → 205,121 B (+13,166, five new rows)** in a few hours, so the five-file boot read improved by only **8.7%**, down from 9.7%, **with this PR unchanged**. Not an argument against the split — it is the strongest evidence yet for the proposal's claim that a `CLAUDE.md`-only path cannot fix the boot read. `ROADMAP.md` is still 42% of that set and untouched.

## Verification on the merged head

- **Full suite: `14437 passed, 12 skipped, 0 failed`.**
- **Guards: `PASS 51 · FAIL 0`.**
- `render_session_brief.py --check` → OK; `check_open_items.py` → OK; backlog round-trip byte-for-byte.
- `git merge-tree` against `main` → **no conflicts**, 0 behind.

A nice live proof: the `close_wedge_standing` carry made `test_diag_log_file_allowlist_coherence` pass **because** of the locator fix I shipped earlier — another session added a `_LOG_FILES` name, and the relocated locator found its doc row in the reference. Had I not carried it, that test would have failed, correctly.

## ⚠️ ONE THING I COULD NOT DO — the PR title

It still says **−34.4%**. `update_pull_request` is an MCP call and this turn had **no `mcp__*` tools**. Per your own instruction a stale measured claim in a title should not stand. **Please retitle** — suggested: *"docs: split the API payload reference out of CLAUDE.md (160,944 B moved, verbatim)"*, which avoids the drifting percentage entirely. I did not open a second PR to route around it.

---
_Generated by [Claude Code](https://claude.ai/code)_
