▶️ **START — MI-92 follow-up · the ClaudeBot command MENU does not populate**

- **branch:** `claude/mi92-claudebot-command-menu` (fresh off `main` — the MI-92 PRs #10904/#10912 are merged and cannot carry new work)
- **session:** https://claude.ai/code/session_01RYoQ5e8i6B4SCn9kMm2gpB
- **tier:** Tier-2 (`src/bot/`, needs an `ict-claude-decision-bot.service` restart) → **prepare + validate, landing `hold`, no merge from this session**
- **touching:** `src/bot/claude_decision_bot.py`, plus a read-only diagnostic under `scripts/ops/` and its `system-actions.yml` allowlist entry.

**Operator, 2026-09-03:** *"I want the menu to open up on the menu button in the chat, not just by typing."* `/status` answers correctly on ClaudeBot (operator ran it 08:23:05Z) — the handlers work; the **discoverable surface** does not.

## One cause is already ELIMINATED, by evidence rather than by theory

The manager flagged that `_post_init` logs nothing on success, so the journal cannot distinguish *set them* / *never ran* / *failed*. That ambiguity is real and I am fixing it — but the "never ran" branch can be closed right now without the token:

- `telegram.ext.Application.post_init` is a plain **slot attribute**, not a property (checked against the installed PTB **22.8**), so `app.post_init = _post_init` after `.build()` **is** honoured.
- `run_polling` executes it: `if self.post_init: loop.run_until_complete(self.post_init(self))`.
- The 08:00:46Z restart reached `Application started` with **zero** Traceback/Exception/ERROR lines. A raising `post_init` would propagate out of `run_polling` and, under `Restart=always`, crash-loop. It did not.

**Therefore `_post_init` ran and `set_my_commands` returned without error.** The commands ARE set. So this is **not** a call that never ran, and **not** a call that failed.

## What that leaves, and what I will NOT do blind

Two candidates remain and they are different fixes: **command SCOPE** (default scope vs `BotCommandScopeAllPrivateChats`) and the **chat MENU BUTTON type** (the composer button lists commands only when it resolves to `commands`; a BotFather-set Web App button overrides the default). A third, **client-side caching of a list first published only at 08:00:47Z today**, cannot be fixed in code at all.

⚠️ **Nothing in this repo has ever called `getMyCommands` or `setChatMenuButton`** (grepped: zero hits). So there is no existing read surface that can tell these apart, and I am not going to guess between them in prose. I am shipping the read — a one-shot `scripts/ops/` script run through the `system-actions` allowlist, which reads the token on the VM and prints only the RESULT, never the value.

## Constraints I am holding

- `HANDLED_PREFIXES` stays `(CB_PREFIX,)` — commands are not `callback_query` traffic.
- **The trader bot is not touched.** MI-92's constraint stands and is now verified honoured on the fleet.
- Landing declared `hold` / `tier_2_3_needs_approval`.
