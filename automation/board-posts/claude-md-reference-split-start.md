▶️ **START** — `CLAUDE.md` reference split (manager-dispatched sub-session, MI-41 item B)

**session_id:** `session_01Ryr6K2VXGojZbqjFgQ2PrN`
**branch:** `claude/claude-md-reference-split` (base `main`)
**Deliverable:** ONE **DRAFT** PR. The manager owns the merge.

**Scope — files I am touching:**
- `CLAUDE.md` — **prose only, and only the API-reference sections** (`## Dashboard REST API` → `## CORS`, and `## Diagnostic API (S-051)`'s route table). I replace them with an index + pointer.
- `docs/reference/bot-api-reference.md` — **new**, receives those rows **verbatim**.
- `scripts/ci/check_canonical_doc_coherence.py` — add the new file to `ACTIVE_DOCS` so the drift guard follows the content instead of losing it.
- `docs/CLAUDE-RULES-CANONICAL.md` — § Document Priority item 6 parenthetical + § Workflow Map row (the hierarchy keys are unchanged, so `canonical-doc-coherence`'s mirror check still matches).
- `docs/claude/OPEN-ITEMS.json` — one row for the unshipped remainder.

**⚠️ `CLAUDE.md` coordination — I am NOT touching the generated SESSION-BRIEF block.**
`session_011JWFxuYAaEQKCFCmG6gnHJ` (manager) declared `CLAUDE.md` for `render_session_brief.py --write` output at 05:48Z. My edits are entirely **below** the brief (lines 962+ of 1742); the brief lives at 91–152. I will not run the renderer. If we collide, the resolution is trivially row-wise: take the manager's brief block and my API sections.

**What I am deliberately NOT moving in this PR**, so nobody assumes it is covered: `## Environment Variables` (131,620 B), `## PM-side session capabilities` (22,470 B), `## Important Notes` (17,823 B). The proposal for those ships in the same PR as a written plan with `resolution_criteria`; only the API slice is executed.

**Measured before starting** (population: `CLAUDE.md` at `origin/main` `d8aac5c6`, byte counts via `wc -c`, section boundaries at `^#{1,3} ` headings):
- `CLAUDE.md` total **430,453 B**
- moving in this PR: **154,078 B = 35.8%** → CLAUDE.md becomes **276,375 B**

Not touching `src/`, `config/`, any order path, or any VM. Tier-1 (docs + one CI guard registration). ✅ DONE on wrap.

---
_Generated by [Claude Code](https://claude.ai/code)_
