### 📋 MI-280 (ENGINEERING lane) — manager report
`session_01BjTp5RYwedmpFfkEtkQo1j`

**CHECKLIST**

| unit | PR | landing | state |
|---|---|---|---|
| U1 green-but-dirty | #11862 | self, armed | green, awaiting `pytest-run` |
| U2 armed-push race | #11873 | self, armed | green, awaiting `pytest-run` |
| U3 scope-overlap parser | #11866 | self, armed | green, awaiting `pytest-run` |
| U4 register field revert | #11878 | self, armed | **clean + green — landing** |
| U5 automerge arming gate | #11889 | — | ✅ **MERGED** (human click) |
| U5b per-branch merge slot | #11891 | — | ✅ **MERGED** (human click) |
| U6 stuck branch | #11895 | self, armed | green, awaiting `pytest-run` |
| U7 due-list token | #11897 | self, armed | green, awaiting `pytest-run` |
| session-end row | #11898 | hold | goes green when #11895 lands |
| U8 sunset-pass route | #11900 | self, armed | still owes #11902's file |
| U10 burn-down keys | #11904 | self, armed | green, awaiting `pytest-run` |
| U11 re-serialization | #11907 | self, armed | green, awaiting `pytest-run` |
| U12 last-verified carry | #11909 | self, armed | green, awaiting `pytest-run` |
| U13 could-not-run | #11911 | self, armed | green, awaiting `pytest-run` |
| U14 slot payload | #11914 | self, armed | green, awaiting `pytest-run` |
| **U15** R13 contention measured | #11918 | self, armed | green |
| **U16** pytest skip attribution | #11923 | self, armed | green |
| **U17** R13 clause (2) discharged | #11926 | self, armed | green |
| **U18** commit-to-main re-derive | #11930 | **hold** | ⚠️ **needs a human merge** |

**RECENTLY DONE**

**The treadmill is over, and it is measured rather than asserted.** Both PRs I flagged as needing a human click landed, and #11891 was the fix. Same census script, same method, before and after:

| | n | conflicted |
|---|---|---|
| before | 17 | **10** (10/10 on `session-board.json`) |
| after | 20 | **2**, neither an armed per-branch claimant |

**All 13 armed branches carrying a per-branch claim merge clean — 13 of 13.** The control is inside the same census: `mi280-constraint-readout-cause`, still on the shared field, **still conflicts** in the same run against the same main. The old mechanism fails beside the new one, so this is not "main went quiet".

That discharges clause (2) of `BL-20260909-R13-FORCES-EVERY-ARMING-BRANCH-ONTO-ONE-SINGLE-HOLDER-KEY-...` (it asked for *two* branches armed concurrently; there were thirteen). Clause (1) was already paid. **Row resolved in #11926** — which also states what is *not* shown: merges against main, not two branches landing back-to-back.

**Also established and worth keeping:** binding `session-board.json` to `jsonregister` is a **measured no-op** (17 branches, 10 conflicts in *both* arms), so the cheap `.gitattributes` one-liner is foreclosed. And the cost while it was open: main's median gap **9.4 min** (n=102) against `pytest-run` at **19.5 min when it actually runs the suite** (n=23 of 60; the other 37 are path-filtered and do nothing, which drags the naive median to 2.2 min). Main advanced ~2.1× per CI cycle, and resolving *is* a push, so the clock restarted. No fixed point.

**NEXT / WHAT I NEED**

⚠️ **#11930 needs a human merge click** — `landing: hold` / `changes_landing_machinery`, R12, same as the two you just cleared. It fixes a workflow that has concluded `success` **zero times in its entire life**: `commit-to-main`'s stale-branch refresh merges main in and re-triggers the checks, but **never re-derives**, so a producer whose output is derived from the registers pushes the same stale file one sha later. Cause reproduced first (#11901) — a renderer/guard disagreement was *refuted*, the moving-base staleness *reproduced* with the exact verdict. New input is empty by default, so all twelve other callers are byte-for-byte unchanged. **It does not close the row**, and the PR says so: CI does not run that workflow, so the criterion is still an observed `event=schedule` success.

Then: #11900 owes #11902's `sunset-pass.yml`; then the next open `high`/`critical` row in `scripts/ci/**`, `scripts/ops/**`, `.github/workflows/**`.

**Against myself, this stretch:** I repeated my own `git checkout --` mistake and destroyed an uncommitted edit for the second time today (restored). Two "escaped" mutants turned out to be my harness failing to apply the patch — a plant that does not apply is indistinguishable from one that escapes, so the plant now asserts its own pattern count. And my test suite for #11930 found a real gap in my own implementation: the out-of-scope check used `git diff`, which cannot see an untracked file, so a re-derive that *creates* one reported a clean tree.
