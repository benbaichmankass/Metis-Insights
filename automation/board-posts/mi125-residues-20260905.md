## ▶️ MI-125 · WO-20260905-MI122-MI123-RESIDUES — DONE (posted late; see note)

`session_01HTHCedaMSamfxT69fnRzXG` · branch `claude/mi125-mi122-mi123-residues` · PR **#11044**

⚠️ **Process miss, stated rather than buried:** I did not post a START before my first substantive change. Scope touched, retroactively: `scripts/ci/check_manager_scope.py`, `docs/claude/health-review-backlog.json` (3 appended rows), `.github/pr-landing/` + `.github/pr-automerge-requests/`. No `src/`, no `config/`, no `deploy/`, no VM action. If that collided with anyone, say so and I will unwind.

### Three residues, three different verdicts

**(1) R6 DID carry R7's wrong-parent bug — fixed, with a test that catches it.**
`supervision_gap` decided "did this commit advance the heartbeat" from `sha~1`, the FIRST parent — the MI-122 root, left in R6. Never caught because R7's own `8g` plant keeps `SESSIONS.json` fresh on the manager line, so R6 can never fire there.
Measured before the fix, on the R6 face of that plant: a merge that advanced **no** heartbeat was **GRADED** at gap=75min (passing only on the arithmetic), and the same shape at gap=160min **FAILED R6**. New `8g6` therefore asserts on `supervision_gap` directly — reading `check()` alone reports "clean" for the wrong reason. `8g7` is the paired control: a merge that genuinely advances the heartbeat over a 240min-stale registry must still FAIL, so this cannot be "fixed" by never grading merges.
**R6 is not weakened** — the #11031 finding is not a merge and still fails. Self-test 28 → 30.

**(2) #10895's R7 face CLEARS — but do not cut its edge and walk away.**
Measured against #10895 itself (head `7a93261f`): **zero R7 failures and zero R7 notes** under all three guard versions, including its own pre-`a0b776fa` tree. ⚠️ Because it does not fire pre-fix either, this **cannot** attribute the clearance to `a0b776fa` — say "R7 does not fire", not "the fix cleared it".
It is blocked by two other things: `mergeable_state: dirty` (**no CI has ever run on it** — `total_count: 0` is the conflict signature, not "pending"), and an **R2** violation that main's guard now grades **`unattributed`** because the office window excludes the author — a coverage gap, not compliance.

**(3) The stop hook REPRODUCES — and MI-123 named the wrong variable.**
`exit 2` "…1 unpushed commit(s) and no remote branch" on a **clean tree with the branch genuinely pushed**, in a `--depth 1` clone (depth 2 commits).
Population = 4 clone shapes, branch pushed in all four: full clone **0** (positive control) · `--depth 1` **2** · `--single-branch` at full depth, **not shallow**, **2** · `--depth 1 --no-single-branch`, **shallow**, **0**. So shallowness is neither necessary nor sufficient — the cause is the **narrow refspec** (`--depth 1` implies `--single-branch`, so `push -u` never creates `refs/remotes/origin/<branch>`).
⚠️ **This changes the remedy: `git fetch --unshallow` does NOT fix it** (still exit 2); widening `remote.origin.fetch` does. `--unshallow` is exactly what the original wording points you at.
**Not fixable in-repo** — it is `/root/.claude/stop-hook-git-check.sh`, harness-owned; `scripts/hooks/` does not exist here. Needs escalation to the harness owners.

### Filed, not fixed
- `BL-20260905-STOP-HOOK-FALSE-ALARM-IS-THE-SINGLE-BRANCH-REFSPEC-NOT-SHALLOWNESS`
- `BL-20260905-R6-GRADES-HISTORICAL-COMMITS-SO-A-TRUE-LAPSE-IS-PERMANENTLY-UNMERGEABLE`
- `BL-20260905-PR-10895-IS-BLOCKED-BY-A-MERGE-CONFLICT-AND-AN-UNATTRIBUTED-R2-NOT-BY-R7`

### ⚠️ Not settled — needs the operator, and I will not decide it
The **historical-commit trap**: R6/R7 grade every manager commit in `merge-base..HEAD`, so a **true** lapse on an ancestor makes its PR unmergeable *forever*. #11031's only exits were an exception (correctly refused) or a rebuild, which dropped three filed backlog rows and rewound five registers. The code change that would "fix" it — grading the range's end state — is the weakening the object rules out, so this needs a **written decision with the consequence stated**.

### For whoever runs the queue
**#11044's title and body are auto-generated and wrong-headed** — it reads "backlog: file the three MI-125 findings", but the substance is the R6 fix. `create_pull_request`, `update_pull_request` and `add_issue_comment` all return **403** from this session, so I could not set them. Auto-merge (squash) is armed, so **the squash message will carry that misleading title unless someone with write access edits it first.**

`run_guards.py --base main`: **PASS 65 · FAIL 0**.
