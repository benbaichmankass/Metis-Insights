## 🔒 MERGE SLOT CLAIM — #10857 (per-merge ping, MI-83)

**Session:** `session_01AYPxs3aDHwv3XBLRF4oK15` (night manager, **holds the lease** — claimed 21:55:05Z, pushed)
**Claiming:** #10857 `notify: ping the operator when a PR merges to main`
**Then releasing the slot.** One PR at a time, re-verified between each.

### Why this one first

It is the operator's most-repeated ask of the day — raised three times, and nothing else was merged on it. Tier-1: `scripts/notify_on_pull.py` + tests + one `OPEN-ITEMS.json` row. No order path, no strategy config, no risk cap.

### Checks at claim time (head `6f56ace8`)

`pytest-run` **success** (21:59:00Z, 15m43s) · `guards` **success** · `pytest-collect` **success** · `repo-inventory` **success** · `audit` **in_progress**

Merging only once `audit` completes green. **Not merging on 4-of-5.**

### ⚠️ What merging this does NOT establish

**Shipped is not observed.** No merge ping has been seen arriving. This lands the producer; the delivery path is a separate open defect — `enqueue()` writes a local file and returns, so a "delivered" hash attests a **file write**, not a Telegram send (`BL-20260902-PING-DELIVERY-IS-MARKED-ON-A-LOCAL-FILE-WRITE-SO-DELIVERED-IS-NOT-EVIDENCE`). Clearing `OI-20260902-PER-MERGE-PING-SHIPPED-AND-NO-MERGE-HAS-BEEN-SEEN-PINGING` needs this merged, pulled by `ict-git-sync`, and a *subsequent* merge seen arriving.

**#10859 stays untouched** — Tier-2, held on the operator's one OK.
