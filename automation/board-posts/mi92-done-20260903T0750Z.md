✅ **DONE — MI-92 · review #10793 on its merits**

- **branch:** `claude/mi92-operator-commands-off-trader-bot` · **draft PR:** #10904
- **session:** https://claude.ai/code/session_01RYoQ5e8i6B4SCn9kMm2gpB
- **object:** `WO-20260903-REVIEW-10793-ON-ITS-MERITS-THE-ONE` (`pending-20260903T070814Z`)
- **tier:** Tier-2 · landing `hold` · **nothing merged, nothing deployed, no env touched**

## Disposition

**The constraint stands, it is violated right now, and the fix is a move rather than a removal.**

#10793's own title is *"HELD, must not reach the trader bot."* It was auto-merged 2026-09-02 with no human decision, and `/status` + `/decisions` have been registered on the **trader** bot ever since — the code logging a WARNING naming its own violation as it did it.

⚠️ **Re-verified LIVE before anything was changed — this is not a re-quote of the 2026-09-02 audit.** Read via `scripts/ops/diag_fetch.sh` (`served by https://ict-bot.duckdns.org`, the direct path) at **2026-09-03T07:11Z**: VM `git_sha` = `git_sha_on_disk` = `053b8d45`, `restart_pending false`; `ict-telegram-bot.service` **active**, `ict-claude-decision-bot.service` **active**. **Population: 400 journal lines spanning 07:03:38Z→07:11:08Z**, containing **exactly 1 restart (07:07:08Z)** and **exactly 2 `operator_commands` lines**, both at it — the `answerable_elsewhere` WARNING and `registered /status, /decisions`. So the violation was live ~4 minutes before the branch was cut.

## The fix

`install_operator_commands` had **exactly one caller in the repo** (`telegram_query_bot.py:788`), so ending the violation and keeping the feature is one call site moving — onto `claude_decision_bot.py` (`ict-claude-decision-bot.service`), which already polls `TELEGRAM_CLAUDE_BOT_SECRET` and already owns the `wdec` tap via the same `handle_decision_callback`. That is the bot the operator asked for:

> *"that's supposed to be showing up in Cloudbot. Right? Not on the trader one, the decisions."*

It also retires `answerable_elsewhere` **by construction** — on ClaudeBot the answerable route *is* the polled token, so a `/decisions` button is received by the process that sent it. The `set_my_commands` menu moved with the handlers.

**Deliberately unchanged:** the trader bot's `wdec` callback branch (the declared fallback — removing it would ship dead buttons that look healthy), `HANDLED_PREFIXES` (commands are not `callback_query` traffic), and `install_operator_commands`'s register-in-all-three-states logic.

## Validation

- `tests/test_operator_commands.py` **20 passed** (was 17) — two wiring assertions **re-pointed, not weakened**, plus three new: the trader bot neither imports nor calls the installer (AST, so it fails on the import too); `polled_token` is the same expression handed to `Application.builder()`; the `wdec` fallback survives.
- 5 affected test files: **203 passed, 4 skipped**.
- `scripts/ci/run_guards.py --base main` → **PASS 61 · FAIL 0 · SKIP 22**. ⚠️ The first run read PASS 60 · FAIL 1 and **that failure was not a finding**: `layer-guard` exited **127** — `lint-imports` was absent from the container, i.e. the guard *did not run*. Named rather than reported green; `import-linter` was then installed and it reports **6 contracts kept, 0 broken**.
- `pr-landing-guard` → `OK — state=declared_needs_approval`, independently grading all three touched `src/bot/` paths Tier-2.
- **CI:** armed and running at time of posting — `repo-inventory` ✅, `pytest-collect` ✅, `guards` + `pytest-run` still in progress on head `8ffc4f31`. ⚠️ The PR first read `total_count: 0` / `mergeable_state: blocked` — the documented `pr-opener` trap (bot-authored results commit; `GITHUB_TOKEN` pushes don't trigger workflows), **not** a conflict (which reads `dirty`). An ordinary commit armed it.
- ⚠️ **A green harness proves none of the done-condition.** Nothing here sent a Telegram message. Registered as `OI-20260903-OPERATOR-COMMANDS-MOVED-OFF-THE-TRADER-BOT-PREPARED-AND-HELD` (`loud: true`), which keeps *stopped on the trader* and *started on ClaudeBot* as two separate observations.

## ⛔ Needs the operator

One OK to merge, **then two restarts — `ict-telegram-bot.service` AND `ict-claude-decision-bot.service`**. Between them the commands exist on neither bot; merge is near-deploy here (#10793 restarted its unit 2s after merging) but the two units are not guaranteed to bounce together, and that window is the one cost of this change.

**The alternative disposition is legitimate:** the operator may **withdraw** the constraint and keep the commands on the trader bot. That closes the object equally — it just has to be *recorded as a decision* rather than left standing as a violated constraint.

## Notes

- **Scope kept:** only #10793. The other nine unauthorised merges were not touched.
- `create_pull_request`, `add_issue_comment` and `update_pull_request` all returned real 403s from this session — PR/issue writes are read-only here, so this rode `pr-opener.yml` and `board-post.yml`. The PR body therefore still carries the first guard run's honest `PASS 60 · FAIL 1 (layer-guard did not run)` wording; the clean re-run above supersedes it.
- **Third recurrence of a known trap, filed nowhere new because it is already filed twice:** appending to `OPEN-ITEMS.json` with a naive `json.dumps` round-trip reformats the whole file (measured here: no candidate serialisation reproduces the original — `items` elements sit at 1-space indent while their keys sit at 3). `backlog_append.py` exists for exactly this and covers only `*-backlog.json`. Already open as `BL-20260901-OPEN-ITEMS-CANNOT-BE-ROUND-TRIPPED-AND-HAS-NO-APPEND-HELPER` and `BL-20260902-OPEN-ITEMS-IS-THE-ONE-REGISTER-NO-APPEND-HELPER-PROTECTS`; a third row would be a duplicate, so this is a datapoint on those, not a new filing. Worked around with a surgical textual insert (**26 insertions, 1 deletion**).
