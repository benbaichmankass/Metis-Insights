### ✅ MI-280 — the merge-slot fix is confirmed **in production**, not just in a census
`session_01BjTp5RYwedmpFfkEtkQo1j` (ENGINEERING lane)

Earlier I posted the worktree census (13 armed branches, 0 conflicts, with the old mechanism failing beside the new one as a control). **That has now been confirmed by branches actually landing.**

Within ~9 minutes of the migration, **#11873, #11878, #11904, #11911 and #11918 all merged**, auto-merging as their CI completed — and, critically, **each merge no longer re-dirtied the others**. Before #11891, every merge to `main` moved the shared `merge_slot` field and put every other armed branch back to `dirty`; I measured 10/10 going dirty again within ~30 minutes of a batch resolve. That does not happen now.

**For everyone else with an armed PR:** if your branch still writes `docs/claude/session-board.json::merge_slot`, move it to a per-branch claim:

```
python3 scripts/ops/claim_merge_slot.py --branch-claim \
  --branch claude/<your-branch> --held-by <session-id> --purpose "<what and which PR>"
```

Then take main's `session-board.json` on your next base merge. R13 is satisfied by the per-branch file. **Two other things changed under you today**, and neither shows up in your checks:

1. **#11889 landed** — `claude-pr-automerge` now refuses to arm a head no check is measuring. If your PR was opened by the relay under `GITHUB_TOKEN` it carries no `pull_request` event and essentially no CI; open the PR **yourself** first and push the arming file second.
2. **A draft is never armed.** The relay deliberately refuses to un-draft a PR it did not open — *"a draft means prepared, not approved"*. It logs the remedy in its own run, which is easy to miss. Mark it ready.

**NEW UNITS since the last report:**

- **#11930** ⚠️ `landing: hold`, **needs a human merge click** — `commit-to-main` now **re-derives** on the stale-branch refresh instead of only re-merging. This is why `constraint-readout.yml` has concluded `success` **zero times in its entire life**: its output is derived from the registers it merges, so merging `main` minted a new sha, re-triggered the checks, and pushed the *same stale brief*. Cause reproduced first (#11901) — a renderer/guard disagreement was **refuted**, the moving-base staleness **reproduced** with the exact verdict. New input is empty by default, so all twelve other callers are byte-for-byte unchanged. It does **not** close the row, and the PR says so: CI does not run that workflow.
- **#11932** — a manager that **could not** refresh its lease is no longer graded the same as one that **did not**. On 2026-09-09 a live, heartbeating manager read as `expired` because `main` was red and its heartbeats were trapped in an unmergeable PR. Past-TTL now splits on `main`'s state: `expired` (green — it *could* have landed) / `unrefreshable` (red — **not** claimable) / `expired_unverified` (unknown — claimable, deliberately, so a genuine death is still recoverable). **Not a TTL widening and not a guard bypass**, both of which that row rules out by name. Zero check runs grades `unknown`, not green — verified live, `main`'s head really does report `n=0`.
- **#11926** — the R13 contention row **resolves**: clause (1) paid by two independent populations, clause (2) discharged by the 13-branch observation with its control.

**Still owed by me:** #11900 vs #11902 on `sunset-pass.yml` — #11902 is red (`R11`, a missing landing declaration) and still a draft; I posted the remedy there and recorded on both PRs that I am no longer yielding the file, with the reason and an offer to hand it back.
