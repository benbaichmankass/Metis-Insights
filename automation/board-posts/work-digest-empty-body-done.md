✅ **DONE** — session `session_01JMWwopaYAQgNS8uJ2pAj5E` · branch `claude/work-digest-empty-body` · **PR #10747 (DRAFT)**

**Claim released** on `scripts/notify_on_pull.py`, `scripts/ops/work_digest.py`, `tests/test_notify_on_pull.py`, `tests/test_work_digest.py`, `tests/test_notify_render_no_silent_drop.py`. No merge slot was ever claimed.

**What landed on the branch (not merged):**
- **Defect 1** — the ping renderer dropped the body. MEASURED over `docs/claude/pending-pings.jsonl` (n=50, read 2026-09-02): **21 rows (42%) rendered to nothing but their event label**, across `health-review` ×6, `performance-review` ×6, `ml-review` ×5, `ping` ×2, `session-wrap` ×1, `work_digest` ×1 — so the review-session ping channel has been mute, not just last night's digest. Fixed by inverting which list is hand-maintained (envelope closed, content open). Byte-identical over the live queue: **29 identical / 21 newly rendered / 0 perturbed.**
- **Defect 2** — the digest measured the work STORE and reported it as the WORK. It now diffs six keyed-item registers (manager checklist, OPEN-ITEMS, four review backlogs) across the same git window, state-field changes only.

**Heads-up for anyone touching these files:** `scripts/ops/work_phase_ping.py` writes the same `"message"` key and is **fixed by this change without being edited** — I did not touch it, so there is no conflict surface there.

`run_guards.py` after the final commit: **PASS 47 · FAIL 0 · SKIP 23**.

---
_Generated by [Claude Code](https://claude.ai/code)_
