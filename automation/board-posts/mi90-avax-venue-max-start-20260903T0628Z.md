## ▶️ START — MI-90 AVAX venue-max: own the order path orphaned by a dead session

- **branch**: `claude/mi90-avax-venue-max-resolution` (scratch for relays: `claude/mi90-board-scratch`)
- **session**: `session_01Q1HYBVoThQdKTWSYQHpm6Y`
- **object**: `WO-20260903-AVAX-VENUE-MAX-OWN-THE-ORDER-PATH` (registry `pending-20260903T061749Z`)
- **intent**: `IN-20260903-TRADING-SYSTEM-HEALTH`
- **spawn-priority exception**: approved by the operator for this ONE object (`docs/claude/work/spawn-priority-exception.yaml`). Cycle priority remains `CY-20260903-MANAGER-CONTROL`; this does not widen.

### Files / subsystems I am touching

Taking ownership of **PR #10780** (`[RECOVERED — DO NOT MERGE]`, +499/-45 across 10 files):

- `src/units/accounts/qty_legalize.py`, `src/units/accounts/precision.py`, `src/units/accounts/execute.py` — **order path**
- `src/core/instrument_profile.py`, `src/core/profile_loader.py`, `config/instruments.yaml`
- `tests/test_qty_legalize_venue_max{,_resolution}.py`, `scripts/ci/check_collapsed_states.py`, `docs/claude/health-review-backlog.json`

No VM action. No account-mode / risk-cap / strategy-config change.

### Tier

Order path is **Tier-2/3**: I **propose only**. Draft PR, no self-merge. #10780 gets closed as part of the disposition either way — a preservation branch is not a disposition.

### Status so far (evidence, not intent)

- Root cause **verified in code**: `AVAXUSDT` is absent from `precision._STATIC_LOT_RULE` (which holds BTC/ETH/SOL only), so on a live-lookup miss it falls to the offline `InstrumentProfile`, which on `main` has no `max_qty` → `venue_max=None` → the clamp is keyed on `venue_max is not None` → **skipped**.
- Reproduced through the **real resolution path** (no monkeypatch): on `main` the new test yields `LegalizedQty(qty=22995.1, source='instrument_profile', venue_max=None, clamped=False, qty_str='22995.1')` — byte-identical to the rejected wire payload.
- **Real venue read** (live VM journal, `ict-trader-live.service`, 2026-09-02T09:06:11Z): `order_qty:2299510000000 > max_qty:2200000000000 (ErrCode: 10001)` → `/1e8` = **22,000**. The venue itself states the ceiling, so `config/instruments.yaml max_qty: 22000` is **measured**, not guessed.

Leaning toward carrying the recovered diff forward as a Tier-gated draft rather than discarding it. Full evidence and the caveats in the PR.

---
_Generated by [Claude Code](https://claude.ai/code/session_01Q1HYBVoThQdKTWSYQHpm6Y)_
