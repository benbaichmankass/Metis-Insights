▶️ **START / ✅ DONE (posted together — the relay was the only route)** — MI-157 · session `session_01MLKjDPG9n1MHBByYEAEgjc`, work session under manager `session_01HrmZ1RRNM4UnEUaFdrPEjj`

⚠️ **Why this is late:** `add_issue_comment` returned `403 Resource not accessible by integration` on this board, so the START could not be posted before acting. Falling back to `board-post.yml` (on a **separate** branch, so its result commit does not bury the PR's checks — the trap `pr-opener.yml`'s header documents only for itself). Scope was single-session, read-only until the final commit, and touched no file another session is likely to hold.

**Scope touched:** `data/ohlcv/**` · `data/ict_validate_manifest.csv` (read only) · `scripts/candle_io.py` · `scripts/candle_variance.py` (new) · `scripts/ci/check_candle_fixture_variance.py` (new) · `scripts/ci/run_guards.py` · `bin/backtest_ict.py` · `tests/test_backtest_ict_cli.py` · `tests/test_candle_fixture_non_degeneracy.py` (new) · `docs/claude/work/objects/` · two backlogs.
**NOT touched:** `config/`, `src/runtime/`, any order path. **No strategy backtest was run or re-interpreted.**

---

### The census — population stated, census not sample

Population: **all 8 committed `.csv` under `data/`**, established by `git ls-files` over the whole repo, plus a wider sweep for `.parquet`/`.feather`/`.pkl`/`.h5`/`.npy` and for any file named like candles/ohlcv/bars/klines, which found nothing further. Positive control: the same probe returns `data/ohlcv/btc_5m_2026.csv`, so its silence elsewhere is measured, not assumed.

**7 candle files checked · 5 flat · 2 real** (+1 manifest, no `close` column).

| file | rows | distinct close | non-zero returns | verdict |
|---|---|---|---|---|
| `data/backtest_candles.csv` | 5000 | 4873 | 4999/4999 | real |
| `data/btc_1m_sample.csv` | 10080 | 9688 | 9962/10079 | real |
| `data/ohlcv/btc_5m_2026.csv` | 300 | **1** (95000.0) | **0/299** | **FLAT** |
| `data/ohlcv/eth_5m_2026.csv` | 300 | **1** (3200.0) | **0/299** | **FLAT** |
| `data/ohlcv/qqq_15m_2026.csv` | 300 | **1** (490.0) | **0/299** | **FLAT** |
| `data/ohlcv/spy_15m_2026.csv` | 300 | **1** (580.0) | **0/299** | **FLAT** |
| `data/ohlcv/spy_5m_2026.csv` | 300 | **1** (580.0) | **0/299** | **FLAT** |

⚠️ `open`, `high` and `low` are **also** 1-distinct in all five: the whole OHLC bar is constant, not just the close. MI-151 reported the close; the bar is worse.

### ⚠️ Correction to the dispatch, in the reassuring direction

The brief pointed at `BACKTEST_DATA_PATH` ("23 files per CLAUDE.md") as the consumer set. **Measured: 21 files read that variable and every one defaults it to `data/backtest_candles.csv`, which is REAL. That fleet never reaches `data/ohlcv/` at all.** Acting on the brief's premise would have audited 21 files that were never at risk and missed the one that was.

The only route is `data/ict_validate_manifest.csv` → `bin/backtest_ict.py` and the notebook (whose Cell 4 *fetches over* those paths first). The fifth file, `spy_5m_2026.csv`, is named by **nothing** — an orphan from the manifest's 5m→15m SPY upgrade.

**The consumer that actually consumed the flatness:** `tests/test_backtest_ict_cli.py::test_ict_validate_manifest_runs_all_pairs` ran the full ICT backtester across all four and asserted success. Green, in CI, for months, over a flat line.

### Repair — all five DELETED, not replaced

`.gitignore:75` already declared `data/ohlcv/*.csv` ignored; they were force-added past the repo's own stated intent. The one designed workflow fetches over them. And only 2 of 5 were refetchable from this sandbox (`data.binance.vision` 200; Bybit 403, Yahoo 429) — a mixed real/flat population is the original defect with better numbers. `data/ohlcv/README.md` now makes the empty path declare itself.

The **silence** was the defect, so deletion is the smaller half: both candle readers now refuse a degenerate series at the read, importing one grader (`scripts/candle_variance.py`) rather than re-deriving it. Three states, never collapsed: `non_degenerate` / `degenerate` / `not_gradeable` (*we could not look* — never a pass).

### Detector (RECURRENCE)

`candle-fixture-variance-guard`, registered **ungated** — the class is defined by a file's content, not its location. Proven both ways as tests: fails on a 300-row flat file, passes on `data/backtest_candles.csv`. This finding **already had a written record** — `BL-20260820` measured the same five files 18 days earlier and they were still flat. A record is not a detector.

### What this calls into question — less than feared

Both research artefacts that touched these files **caught them and routed around them**: MI-151 used the trainer corpus; the 2026-08-02 ETH recalibration says outright they "were **not** used". No research verdict is known to be corrupted. One provenance gap **filed, not investigated**: `BL-20260907-S006-M7-GO-NO-GO-VERDICT-MAY-REST-ON-FLAT-FIXTURES`.

### ⚠️ Landing: HELD, not self-landed — deliberately

The dispatch said Tier-1 self-land. `pr-landing-guard` R5 refuses to certify `data/ohlcv/*.csv` and `bin/backtest_ict.py`, and **that refusal is correct, so it is honoured rather than engineered around.** Widening `TIER1_SURFACE` inside the PR it would let through is the "guard cheaper to lie to than to satisfy" antipattern — and especially wrong for `data/**`: a committed corpus **is** the instrument every backtest is graded against, which is this change's own subject. A session that can swap the corpus under every research verdict with no human read is the failure class MI-157 exists to close.

**So this needs one line of review.** 87 of 88 guards pass (the one failure is `lint-imports: command not found`, absent from this container). 35 tests pass across the two touched test files.
