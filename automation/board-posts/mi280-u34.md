✅ **U34 DONE** — `claude/mi280-u34-corpus-passed-negatives` → **#12043** (open, armed)
- `scripts/research/m20_coverage_rollup.py` — new `negative_cells_the_corpus_passed()`; rides the `rollup()` dict so `--json` carries it, and PRINTS on every default run
- `tests/test_rollup_corpus_passed_negatives.py` — new, 7 controls
- `docs/claude/health-review-backlog.json` — `BL-20260906-MATRIX-STATUS-COLLAPSES-A-PASSING-SWEEP-CELL-INTO-HONEST-NEGATIVE-RECURRENCE-AT-35` → `resolved` by its criterion (b)

**Measured 2026-09-12, population stated:** `corpus-passed negatives 26 of 259`. The row's **35 reproduces exactly** on its own join (all matrix rows, no floors); 26 applies the guard's own admissibility floors + per-CELL supersession (35→30) and then LIVE legs only (30→26). It changes no cell status and proposes no disposition — that is Tier-3 and the row scopes it out.

**8 planted defects, all caught.** One earlier plant ESCAPED — `"Tier-3" in out` over the WHOLE render was vacuous because the phrase occurs elsewhere on the page — now scoped to the section, and the escape is recorded in the test docstring rather than quietly fixed.

Guards on committed state: `landing=0 backlog=0 fieldloss=0`; `ruff --select E4,E7,E9,F` clean (local ruff is 0.16.7, outside the repo's `<0.16` pin, so its expanded default ruleset is not CI's); `check_trainer_heavy_lock` self-test + list OK for the `scripts/research/` path.

**Standing scope question, still unanswered and still not blocking:** `scripts/research/**` is in neither the allowed nor the forbidden list of my dispatch. I am treating it as in scope — Tier-1 research tooling, no runtime caller, no order path. Correct me and I will stop.

Lane: **#11989 merged.** Open + armed: #12021, #12028, #12036, #12041, #12043.

---
_Generated by [Claude Code](https://claude.ai/code)_
