✅ **DONE** — R-metric contamination investigation → **DRAFT PR [#10748](https://github.com/benbaichmankass/Metis-Insights/pull/10748)**

**Session:** `session_01EihsgewHt7BZv43YcC7caV` · branch `claude/r-metric-contamination` · claim from my START comment is **released**.

**Files I touched** (nothing else, and nothing another session was on): `src/runtime/r_provenance.py` (new) · `src/web/api/routers/performance.py` (additive) · `scripts/ci/check_collapsed_states.py` (one contract registered) · `tests/test_r_provenance.py` + `tests/test_performance_r_provenance.py` (new) · `automation/` probes. **Did NOT touch** `config/`, `ROADMAP.md`, `OPEN-ITEMS.json`, any `*-review-backlog.json`, the order path, or `order_monitor.py`.

**Mechanism confirmed at the writer:** `order_monitor._apply_update:1349-1357` mirrors every confirmed trailing amend onto `trades.stop_loss`, so that column holds the CURRENT stop, not the initial one — and `_clean_trades.r_multiple` derives the R denominator from it. `order_packages.sl` is overwritten too, so it is not an alternative.

**Recoverability (this decides whether the fix is forward-only):** `order_packages.meta.risk_per_unit` IS an independent signal-time record that `_apply_update` cannot reach. **682 of 1346 rows (50.7%) carry it**, so the fix is NOT forward-only — but `vwap` (318 real-money rows) and the whole pairs sleeve (283) have none and stay permanently ungradeable.

**Two things that will save another session time:**
1. **The naive wrong-side count (226) OVERSTATES contamination by ~2×.** 108 of those rows are `setup_type='intent_reduce'`, whose `trades.direction` is deliberately opposite to `order_packages.direction` — the whole bracket reads mirrored, which is not a trailed stop. After separating them: **118 contaminated · 156 confirmed · 1051 unverified · 21 no-basis**, n=1346.
2. **Neither automated gate reads R.** The M7 strategy-review packet grades on `expectancy` in DOLLARS (`strategy_review_packet.py:587-589, 983-984, 1030-1071`) and the R4 results gate keys on `pnlCoverage`/`totalPnlMeasured` (zero R references). The contamination reaches decisions through **review sessions and promotion packets a human reads**, plus `net_r_label.py` (an independent second site). Real, but a different severity from "an automated promotion gate is reading a corrupt number".

**⚠️ Heads-up for anyone on `/api/bot/performance`:** it applies the reconciler / superseded / reset-flat exclusions but **not** `exclude_intent_reduce_predicate`, so those 108 mirrored rows are inside the R and PnL populations. Flagged in the PR, not fixed — it is a call I did not make.

**Guards:** `PASS 50 · FAIL 0 · SKIP 20`, run after the final commit. 67 new tests, both-direction controls, mutation-verified.

The Tier-3 fix is **proposed, not enacted** — PR §5 carries the exact 5-step diff and the one question for the operator. Manager owns the merge.

---
_Generated by [Claude Code](https://claude.ai/code)_
