### 📋 MI-280 (ENGINEERING lane) — manager report
`session_01BjTp5RYwedmpFfkEtkQo1j`

**CHECKLIST**

| | unit | PR | state |
|---|---|---|---|
| ✅ | U1 green-but-dirty surfaced | #11862 | **merged** |
| ✅ | U2 armed-push detector | #11873 | **merged** |
| ✅ | U2 prevention (arming gate) | #11889 | **merged** (human click) |
| ✅ | U4 register field revert | #11878 | **merged** |
| ✅ | U5 slot-claim grading | #11881 | **merged** |
| ✅ | U5b per-branch merge slot | #11891 | **merged** (human click) |
| ✅ | U9 constraint-readout cause | #11901 | **merged** |
| ✅ | U10 burn-down creation keys | #11904 | **merged** |
| ✅ | U13 `could_not_run` | #11911 | **merged** |
| ✅ | U15 R13 contention measured | #11918 | **merged** |
| ⏳ | U3 scope-overlap parser | #11866 | armed, CI |
| ⏳ | U6 stuck branch | #11895 | armed, CI |
| ⏳ | U7 due-list token | #11897 | armed, CI |
| ⏳ | U8 sunset-pass route | #11900 | armed, CI |
| ⏳ | U11 re-serialization | #11907 | armed, CI |
| ⏳ | U12 last-verified carry | #11909 | armed, CI |
| ⏳ | U14 slot payload | #11914 | armed, CI |
| ⏳ | U16 pytest skip attribution | #11923 | armed, CI |
| 🔒 | session-end row | #11898 | `hold` — green once #11895 lands |
| 🔒 | U17 R13 clause (2) discharged | #11926 | not armed (see below) |
| 🔒 | U18 commit-to-main re-derive | #11930 | **hold — needs a human merge** |
| 🔒 | U19 lease `unrefreshable` | #11932 | not armed (see below) |
| 🔒 | U20 arming-relay crash fix | #11936 | **hold — needs a human merge** |

**RECENTLY DONE**

**The merge treadmill is closed, measured, and confirmed in production.** #11891 landed; I migrated the lane; the same census script that read **10 of 17 conflicting** now reads **0 of 13 armed branches conflicting**, with the one branch still on the shared field failing beside them as a control. Ten units then landed — and a full re-sync of twelve branches just now produced **eleven clean merges and one ordinary content conflict**, where every single one used to conflict on `session-board.json`.

**What I need from you — two human merge clicks**, both `landing: hold` under R12 (a landing-machinery diff cannot self-land, by design):

- **#11936** — the arming relay has been crashing since #11889 merged at 09:56:31Z: `Cannot find module '@actions/github'`, unhandled, across four lanes. ⚠️ **I first called this an outage and that was wrong** — already-armed PRs keep landing (six of mine did); only *new* arming fails. Corrected on the board and in the PR. The fix does **not** guess at the right require: it tries the documented hatch, then a bare require, then degrades to the path the step already documents (checks do not attach, step 3 refuses to arm) with a loud warning — because I cannot execute `github-script` here and will not ship an untested guess into the relay that lands every PR. It also fixes the sharper half: `openerKind` now keys on the **client built**, not the **token held**, so a fallback can no longer claim `pat` and arm a head nothing is measuring.
- **#11930** — `commit-to-main` now **re-derives** on the stale-branch refresh instead of only re-merging. This is why `constraint-readout.yml` has concluded `success` **zero times in its entire life**. Cause reproduced first (#11901): a renderer/guard disagreement **refuted**, moving-base staleness **reproduced** with the exact verdict. Empty by default, so all twelve other callers are byte-for-byte unchanged.

**#11926 and #11932 are collateral of the arming break** — armed after 09:56, so they never got auto-merge. They need a click or a re-arm once #11936 lands. Re-pushing now just re-runs the crash.

**NEXT:** #11900 vs #11902 on `sunset-pass.yml` (#11902 is red on `R11` and still a draft; I posted the remedy there and recorded on both PRs that I am no longer yielding, with an offer to hand it back). Then the next open `high`/`critical` row in `scripts/ci/**`, `scripts/ops/**`, `.github/workflows/**`.

**Against myself, this stretch** — five, all recorded rather than quietly fixed:
1. Wrote off three reds as "stale heads" without reading the logs. Three real, unrelated causes; two were not lint nits.
2. Inferred a module was skipping in CI from "14 tests, 14 skipped" — **unsound**, those runs carried a 7-test version of the file.
3. Shipped a summariser that collapsed every module into `tests`, because my fixture set a `file=` attribute real pytest does not emit. Its self-test now *runs pytest*.
4. Repeated my own `git checkout --` mistake and destroyed an uncommitted edit for the second time today.
5. Called the arming break an outage on evidence that was already on `main` disproving it.

Two "escaped" mutants also turned out to be my harness failing to apply the patch — a plant that does not apply is indistinguishable from one that escapes, so plants now assert their own pattern count.
