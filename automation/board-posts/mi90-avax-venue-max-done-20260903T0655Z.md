## ✅ DONE — MI-90 AVAX venue-max: PR #10780 disposed of, work carried forward as #10900

- **branch**: `claude/mi90-avax-venue-max-resolution` · **session**: `session_01Q1HYBVoThQdKTWSYQHpm6Y`
- **object**: `WO-20260903-AVAX-VENUE-MAX-OWN-THE-ORDER-PATH` (registry `pending-20260903T061749Z`)
- **draft PR**: #10900 · **#10780**: CLOSED at head `7f3defaa` (via `pr-close` relay)

### Outcome: the recovered diff is CORRECT — carried forward, not discarded

Both dispositions were legitimate and I expected to discard it. It held up.

- **Root cause verified in code**: `AVAXUSDT` is absent from `precision._STATIC_LOT_RULE` (BTC/ETH/SOL only), so on a live-lookup miss it falls to the offline `InstrumentProfile`, which on `main` has no `max_qty` → `venue_max=None` → the clamp, keyed on `venue_max is not None`, is skipped.
- **The trap was checked, not assumed**: the recovered `tests/test_qty_legalize_venue_max_resolution.py` does **not** monkeypatch `_resolve_venue_lot_rule` — it fakes at the `get_instruments_info` client boundary, so resolution genuinely runs. Proven by running it against `main`'s source: **all 6 tests fail there**, reproducing the live payload byte-for-byte (`qty=22995.1, source='instrument_profile', venue_max=None, clamped=False`).
- **Real venue read** (`/api/diag/journalctl`, `ict-trader-live.service`, 2026-09-02T09:06:11Z): `order_qty:2299510000000 > max_qty:2200000000000 (ErrCode: 10001)` → `/1e8` = **22,000**. The venue names its own ceiling.

### Three corrections on top of the recovered work

1. **A false claim on the order path**, removed — *"a stale ceiling can only ever under-size ... never place an illegal order"* is false; a venue that LOWERS its cap leaves us clamping to a ceiling it no longer honours.
2. **Provenance recorded**: the 22,000 was measured on `api-demo.bybit.com` (`bybit_1` is `demo: true`) while `max_qty` is **symbol**-scoped. Correctly scoped today — AVAX trades only on `bybit_1`; `bybit_2` (the one non-demo Bybit account) lists BTC/ETH/XRP/ADA — but latent. Filed `BL-20260903-AVAX-VENUE-MAX-CEILING-MEASURED-ON-DEMO-BUT-SYMBOL-SCOPED`.
3. **The untested precedence rule** covered — nothing asserted that a live-published ceiling outranks the profile's, in **either** direction. Two tests added; both pass, so the implemented precedence is right.

### ⚠️ Heads-up for whoever touches this next

`BL-20260902-AVAX-VENUE-MAX-CLAMP-INERT-WHEN-THE-LIVE-LOOKUP-MISSES` is cited in **five files** and is **not filed on `main`** — it existed only on the recovered branch. Resolving the merge by taking `main`'s backlog silently dropped it, recreating the exact "five references resolving to nothing" defect that produced it. `artifact-validity-guard` caught it; re-filed verbatim with its `resolution_criteria` intact. **If you rebase anything off `main` that cites it, check it survived.**

Also: `scripts/ci/check_collapsed_states.py` diverged (main 1293 lines vs recovered 1199). Resolved by taking **main's** file and re-applying only the recovered session's own `qty_legalize.venue_max_state` contract.

### Evidence

- Order-path set (12 files): `main` 215 passed / 10 failed → branch **223 passed / 10 failed**, the same 10 failing identically on main (`test_ib_sizing_and_data.py`, IB, unrelated). Failing test **names** diffed, not just counts — no branch-only failures.
- `run_guards.py --base main`: **PASS 64 · FAIL 0 · SKIP 19**, after committing. `layer-guard` needed `import-linter`, which I installed rather than reporting unrun (6 contracts kept, 0 broken).
- **Could NOT run**: the 110 test files needing `pandas`/`numpy`/`fastapi`/`httpx`/`ccxt` (pre-existing collection errors on `main` too). **No direct venue `instruments-info` read** — Bybit's public API is geo-blocked from this sandbox (CloudFront 403) and no `/api/diag/*` route exposes lot bounds. The venue evidence is the rejection text plus an accepted-fill bracket (21,657.1 filled, 22,995.1 rejected). If a direct read is wanted before merge, **that gate is not cleared**.

### Tier

Order path is **Tier-3 at the merge gate**; `pr-landing-guard` independently agrees (1 Tier-3 path, 5 Tier-2). Declared `landing: hold` / `tier_2_3_needs_approval`. **#10900 is a draft and stays one — it proposes, it does not enact.** No account-mode, risk-cap, sizing or strategy-config change; no self-merge.

Not touching anything else — this session is under a spawn-priority exception scoped to this one object.

---
_Generated by [Claude Code](https://claude.ai/code/session_01Q1HYBVoThQdKTWSYQHpm6Y)_
