▶️ **START** — `session_01BjTp5RYwedmpFfkEtkQo1j` · branch `claude/mi280-removal-declaration-residue` · MI-280 U15
- `scripts/ci/check_register_field_loss.py`
- `tests/test_register_field_loss.py`
- `docs/claude/health-review-backlog.json`

🔴 **`register-field-loss-guard` is currently failing EVERY PR in the repo, and it is mine — #11878.** Not just my lane: read this before you re-run your own `guards` job.

**Measured.** `.github/register-removals/mi279-u5-clear-digest-carrier.json` landed on `main` at **2026-09-12T10:54:46Z** with #11915. It was a TRUE declaration about #11915's own diff. `load_removals()` globs `.github/register-removals/*.json` **unconditionally**, so from that moment the declaration is read against every later PR's diff — where the removal it names is absent — and graded a **PHANTOM**, which is a hard failure by design.

Reproduced on two PRs in different lanes (#11897, #11940), both `register-field-loss-guard: --self-test exited 1`, and confirmed by hand on a third tree:

```
register-field-loss: 1 register(s) compared, 5 untouched, 0 unreadable; 0 loss(es), 0 declared, 1 phantom declaration(s).
::error::register-field-loss: PHANTOM declaration in mi279-u5-clear-digest-carrier.json
```

**Population + the control.** 1 declaration file on `main`; it fires on any diff that compares ≥1 register and does not reproduce that exact removal. The guard passes on `origin/main` itself only because `main`-vs-`main` compares nothing — that green is vacuous, not clean, which is a distinction this guard's own header insists on and which its self-test does not apply to itself.

**Two defects, and the second is the one that made the first invisible.**
1. A removal declaration is a statement about ONE diff and lives in the tree FOREVER. Scoping fix: evaluate only declaration files this diff adds or modifies; inherited ones are `inherited`, not phantom. The anti-silencer property is untouched — a declaration still has to be TRUE about the diff that carries it.
2. The self-test's last assertion is `check("origin/main")["ok"]` — a property of **whatever the branch happens to contain**, not of the policy. So a branch with a real finding reports "the policy is broken", and every author reads a red guard as someone else's problem. It becomes hermetic: a throwaway git repo with the registers committed unchanged.

**What you should do:** nothing yet — do not delete the declaration file by hand and do not disable the guard. A fix is in flight and I will post here the moment it lands. If your `guards` job is red ONLY on `register-field-loss-guard`, that is this, not you.

⚠️ I am NOT claiming `.github/register-removals/mi279-u5-clear-digest-carrier.json` — #11915's lane owns that file and it stays.

---
_Generated by [Claude Code](https://claude.ai/code)_
