▶️ **START** — `session_01TM8XjkvVpvMxH4EcLtTsvP` (worker; manager `session_01HrmZ1RRNM4UnEUaFdrPEjj`)

**MI-229 — collapse the two strategy-builder registries into one (audit F-28, operator-approved `collapse`).**
Work object: `docs/claude/work/objects/WO-20260909-DECISION-THE-ROLLBACK-REGISTRY-IS-AN-OUTAGE.yaml`
Branch: `claude/mi229-collapse-builder-registry`

**What I am about to touch — steer clear of these:**
- `src/runtime/pipeline.py` — **deleting `_STRATEGY_BUILDERS`** (the second registry); `multiplexed_signal_builder` + `monitor_unit_for` resolve through `intent_multiplexer._resolve_builders()`.
- `src/runtime/intent_multiplexer.py` — docstring only (it documents the legacy dict that is going away).
- Tests that reference the deleted name: `tests/test_runtime_flags.py`, `tests/test_outcomes_integration.py`, `tests/test_s026_g4_audit_attribution.py`, `tests/test_s012_pipeline.py`, `tests/test_ltmgmt_p5_contract_ci.py`, `tests/test_strategy_monitor_unit_resolution.py`, `tests/test_unit_c_prop_exit_variants.py`.
- NEW: `tests/test_strategy_builder_registry_single_home.py` (the re-split detector).
- Docs: `.claude/skills/new-strategy/SKILL.md`, `docs/strategies/supported_strategies.md` (both currently instruct authors to register in the registry being deleted — that instruction IS the drift mechanism).
- Records: the WO yaml, `docs/claude/session-board.json`.

**Deliberately NOT touching** (MI-227 is live in both): `src/units/accounts/execute.py`, `scripts/ops/system_invariants.py`.

Tier-2 (signal path), operator-approved. **No order is placed, modified or cancelled. No env var is flipped on any VM.** Landing: PR opened **ready**, `landing: hold` — the manager or the operator merges. Auto-merge will NOT be armed, so I claim no merge slot.

---
_Generated by [Claude Code](https://claude.ai/code)_
