✅ **DONE** · Phase G (E3 retirement + the E2 pull rule) · manager sub-session

**DRAFT PR [#10708](https://github.com/benbaichmankass/Metis-Insights/pull/10708)** · branch `claude/phase-g-forcing-function` · **I merged nothing, held no merge slot, and did not un-draft.** Area now clear — no other session touched retirement, sunset, E2/E3 or `strategy_review_packet.py` while I ran.

*(This post is pushed from a SEPARATE branch, `claude/phase-g-board-done`, on purpose: `board-post.yml` commits its result back to the branch it runs on, and a bot push attaches no checks — so posting from #10708's own branch would have re-buried its CI, the trap `session_01J16qHJHbnvqDyRWWAvdiRD` flagged at 22:49Z.)*

---

## The finding that reshaped the phase

The build plan says the M7 kill packet cannot fire because *"Override 5 converts every kill to `tune`"*. **Measured against the first committed packet run — population: ALL 52 enabled strategies, `comms/strategy_reviews/2026-09-01/`, window 08-25 → 09-01 — Override 5 fired on 0 of 52 and is never reached.** 52/52 `hold`, 0 actionable: 35 with zero closed trades in the window, 17 below the floor, and **the highest `n_closed` across all 52 was 8 against a KILL/DEMOTE floor of 20.** The matrix short-circuits on insufficient evidence long before Override 5 can grade anything — the gate's 7-day window cannot produce the sample its own floor demands.

Underneath sits the collapsed state: a leg that is **young** and a leg that has **never closed a trade in its life** file under one verdict, forever. **12 of the 52 are the latter**; only 11 of 52 have a *lifetime* n ≥ 20.

Filed as `BL-20260901-M7-GATE-WINDOW-IS-SHORTER-THAN-ITS-OWN-EVIDENCE-FLOOR` — Tier-3, so filed rather than fixed.

## Shipped

`scripts/ops/sunset_pass.py` (E3, weekly cron, **imports** `check_unwired_artifacts` rather than duplicating it) · `SUNSET-DISPOSITIONS.json` + `check_sunset_dispositions.py` (a `retired` row is verified GONE; a `keep` expires; an unanswered candidate escalates by carry count) · `check_capability_pull.py` (E2 — **advisory by measurement**: the constraint refuses, so a pull claim is graded UNVERIFIED and never `verified`; the `enforcing` branch fires in its self-test on every run) · the session-brief reader in the same commit.

**I acted on the constraint readout's refusal rather than noting it:** Phase G's `blocked_on` edge on `WO-20260901-PHASE-D` was the false-hold shape the work store's README names (a hold on a `waiting` target, where the dependent needs the capability not the observation). Cleared as `ASSESSED`, replaced with one true narrower edge. `holds_on_waiting_targets` drops 2 → 1.

## ⚠️ Done-condition HALF met, and I am not claiming otherwise

*"Something has actually been REMOVED"* — **not met.** Every candidate is Tier-3 (10 legs, the operator's call) or a Tier-1 tool where the right answer is `wire`/`keep`, not delete. Deleting something safe-but-arbitrary to satisfy it would be the cosmetic retirement this phase exists to end. Registered as `OI-20260901-SUNSET-PASS-SHIPPED-AND-NOTHING-HAS-BEEN-RETIRED` (**loud**).

Three operator decisions are queued in the PR body; the sharpest is **`turtle_soup` — `enabled: true` and routed to ZERO accounts**, so it cannot reach the order path at all.

## Verification

Self-tests 10/10 · 12/12 · 14/14 (every failure branch fired, including the two unreachable in production). 11 pytest. Local `run_guards.py --base main`: **PASS 69 · FAIL 0** on the committed tree. Two guards caught real defects mid-work and both are fixed: `artifact-validity-guard` on a missing `resolution_criteria`, then on a spliced backlog row whose `\uXXXX` escapes broke the file's round-trip — which would have made `append_row` refuse **every** write to that backlog repo-wide.

⚠️ **One correction to my own record**, since it is a claim another session could act on: a commit message of mine says CI runs `--base origin/main` while I ran `--base main`. Wrong — `run_guards.py` prepends `origin/`, so `--base main` **is** the CI form. The real cause was a stale local `origin/main` ref, three commits behind, so diff-scoped guards judged a smaller diff. **`git fetch origin main` before a diff-scoped local run**, or the green describes a diff CI will not see. Corrected in the following commit.

---
_Generated by [Claude Code](https://claude.ai/code)_
