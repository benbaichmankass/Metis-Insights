### 📋 MI-280 (ENGINEERING lane) — manager report at a unit boundary
`session_01BjTp5RYwedmpFfkEtkQo1j`

**CHECKLIST**

| unit | PR | state | note |
|---|---|---|---|
| U1 green-but-dirty | #11862 | armed, dirty | slot conflict |
| U2 armed-push race | #11873 | armed, CI running | resolved + pushed |
| U3 scope-overlap parser | #11866 | armed, unknown | — |
| U4 register field revert | #11878 | armed, CI running | resolved + pushed |
| U5 automerge arming gate | #11889 | **clean, green, NEEDS HUMAN MERGE** | R12 |
| U5b per-branch merge slot | #11891 | **clean, green, NEEDS HUMAN MERGE** | R12 — *the fix for the treadmill* |
| U6 stuck branch | #11895 | armed | ruff fixed, pushed |
| U7 due-list token | #11897 | armed | dead predicate fixed, pushed |
| session-end row | #11898 | `hold` | green the moment #11895 lands |
| U8 sunset-pass route | #11900 | armed | still owes #11902's file |
| U10 burn-down keys | #11904 | armed | — |
| U11 re-serialization | #11907 | armed | — |
| U12 last-verified carry | #11909 | armed | inert-param fixed, pushed |
| U13 could-not-run | #11911 | armed | — |
| U14 slot payload | #11914 | armed | board post reverted off it |
| **NEW** R13 contention measurement | **#11918** | armed, opened by hand | below |

**RECENTLY DONE**

Three reds I had wrongly written off as "stale heads" turned out to be three real, unrelated failures. **I retracted that on the board** — I explained away a red without reading it, for the second time today. Two were not lint nits:

- **#11897** — the unused `remainder` *was* the predicate the comment describes; nothing read it, so the documented mechanism never ran. It could not have worked either (it stripped every renderer mention, so the "if nothing is left" test was vacuously true). Behaviour unchanged, measured: n=3 collecting steps over all workflows, before/after byte-identical.
- **#11909** — `diagnostic-provenance-guard` caught a genuine inert parameter in the change that introduced it, **and the tidy-up it invites is the bug**: `assess` sets `last_verified` to `never` for unassessed docs, so "use the parameter" would stamp a real date on a document nobody assessed. Removed. Behaviour identical over all **1055** registered documents. 6 planted defects, 6 caught.
- **#11895** — genuinely style; only AST delta is `+1 Import`, imported-name set identical at n=13.

**NEW UNIT — #11918**, and it is the one worth your attention:

I measured the conflict treadmill properly and **refuted the obvious fix**. Population: every open `claude/mi280-*` branch merged against `origin/main`, driver armed, n=17. Armed branches **13 → 10 conflicted**; unarmed **4 → 0**. `wrote merge_slot?` tracks `armed?` at 13/13 and 4/4, because R13 requires it. **100% of conflicts on `session-board.json`.**

Positive control so the negative has a denominator: `health-review-backlog.json` (driver-**bound**) is contested on **11** branches and conflicts **0** times; `session-board.json` (unbound) contested on **10**, conflicts **10**. Contested *more*, conflicts *never*.

⚠️ **And the cheap fix is a measured no-op.** Binding `session-board.json` to `jsonregister` in `.gitattributes`: **ARM A 10 conflicts, ARM B 10 conflicts — identical.** There is no merge to perform; `merge_slot` is a single-valued mutual-exclusion claim and a row-aware driver correctly refuses to invent a winner.

**WHAT I NEED FROM YOU — two human merge clicks, and they are the bottleneck.**

**#11891 and #11889 are both `clean`, green, Tier-1, and blocked ONLY by R12**, which forbids a landing-machinery diff from self-landing. I cannot merge them and must not try — R12 is the machinery protecting itself from me.

**#11891 is the fix for the treadmill** (per-branch claim files, so two branches never write the same lines). Until it lands, every armed PR in this lane pays the conflict tax, and the tax is self-inflicted: R6 mandates arming as the one landing route, R13 forces every armed branch onto one shared field, and R13's own message says it *"does NOT serialize"* — full cost of serialization, none of the benefit.

**NEXT:** #11900 still owes #11902's `sunset-pass.yml`; then the next open `high`/`critical` health row touching `scripts/ci/**`, `scripts/ops/**` or `.github/workflows/**`. Known remaining: the scope-overlap parser row (U3), and `BL-20260911-REPLAY-PREGATE-NIGHTLY-HAS-ZERO-SUCCESSFUL-RUNS` (critical).

**Against myself:** the scope-overlap audit on #11911 flagged my own board `START` as unattributed. It is right — my STARTs name the session id in a bullet, not where the extractor looks. That is my own U3 item pointing at my own posts, and I will not "fix" it by relaxing the #10729 control, per that row's `resolution_criteria`.
