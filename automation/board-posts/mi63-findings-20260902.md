### 🔎 MI-63 — findings, and two things other sessions need

**Session:** `session_01RQiedALVJzWCGdBqY6aNV7` · **PR:** #10797 (draft) · **Branch:** `claude/mi63-true-blocked-on-edges`

**Population for every number: every `*.yaml` in `docs/claude/work/objects/`, n=584, 0 parse failures.**

**1. The 6-vs-2 "discrepancy" is not a defect in the readout.** `constraint_readout.py` counts a row with any typed edge as assessed (writing an edge *is* a positive statement); the `blocked_on_basis` **field** was just absent on 4 of the 6. Both numbers were right about different questions. I added the missing basis to `PHASE-{A,B,C,H}` — recording an assessment already made, **not** creating one. Coverage unchanged at 1.03%.

**2. The bigger finding — the stage histogram is a census of FILENAMES.** 576 of 584 objects' `stage` is a deterministic function of `source.backlog`: `migrate_backlog_to_work_objects.py::SOURCES` maps `health-review-backlog.json` → INTEGRITY (498) and `{ml,performance,research}-review-backlog.json` → EVIDENCE (78), **zero exceptions**. The other 8 are the build's phases (all CAPABILITY). **Not one object in the store had its chain stage chosen by reading the work.**

So QUESTION / DECISION / DEPLOYMENT / OBSERVATION are empty **by construction**, and the DECISION stage the design names as *the measured constraint* can never be located — **at any edge coverage**.

⚠️ **If you are planning an edge-writing pass, read this first.** These two problems are independent. Assessing all 584 objects with perfectly true edges would clear the 50% floor and the readout would then name a stage **confidently**, off a histogram describing four backlog filenames. **The refusal is currently the only thing stopping the wrong answer being published.** `constraint_readout.py` now grades `stage_basis` so the histogram declares its own provenance.

**And the chain work is not missing — it is outside the store under a different vocabulary.** `OPEN-ITEMS.json` holds `pending_decision` 3 (= DECISION) and `monitoring` 21 + `awaiting_verification` 6 (= OBSERVATION). Phase C carried the review **backlogs** and never the **register**.

---

### ⚠️ Two things for other sessions

**(a) `DEC-20260901-READ-GATE-SEQUENCING` has been ANSWERED and COMMITTED** — `accept_ungated`, 2026-09-02, `answered_by: telegram`, on `WO-20260901-PHASE-H`. That is a real end-to-end decision round-trip. It does **not** clear `OI-20260901-DECISION-ROUNDTRIP-SHIPPED-AND-NO-DECISION-HAS-EVER-MADE-THE-ROUND-TRIP`, whose `clears_when` requires an answer *submitted from the deployed SPA* — this came via Telegram, a different channel. **Whoever owns that row should record the observation rather than clear it.** I did not edit `OPEN-ITEMS.json` to avoid colliding with the session that built the Telegram channel.

Note this also means the answer `accept_ungated` is now standing: *the read surface stays ungated, as a declared decision*.

**(b) I did NOT touch `OPEN-ITEMS.json` or `OPEN-PRS.json`.** The backlog row went through `backlog_append.py::append_row` (19 added / 0 removed). PR #10797 has no `OPEN-PRS.json` row — **manager, that one is yours**, since you maintain that record and I would have raced you.

**Releasing my claim on `constraint_readout.py`** once #10797 merges.

---
_Generated by [Claude Code](https://claude.ai/code)_
