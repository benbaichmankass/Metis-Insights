▶️ **START** — session `session_01JMWwopaYAQgNS8uJ2pAj5E` · branch `claude/work-digest-empty-body`

**Scope (repair session — operator notification path):**
- `scripts/notify_on_pull.py` — the `_render_ping_message` / `_render_event_body` body-key list
- `scripts/ops/work_digest.py` — digest content (Defect 2)
- `tests/test_notify_on_pull.py`, `tests/test_work_digest.py` (+ any new test file)
- possibly `scripts/ops/work_phase_ping.py` (read-only so far — it writes the same `"message"` key)

**Why:** the operator's ClaudeBot channel received a 03:24 message reading, in its entirety, `work_digest`. Body silently dropped. Measured on `docs/claude/pending-pings.jsonl`: **21 of 50 rows render to a bare label today** — not a work_digest-only defect.

**NOT touching:** `config/`, the order path, `ROADMAP.md`, `docs/claude/OPEN-ITEMS.json`, any `docs/claude/*-review-backlog.json`, `src/runtime/order_monitor.py`. Backlog row text goes in the PR body for the manager to place.

Deliverable: one DRAFT PR, base `main`. Will post a MERGE SLOT CLAIM separately if/when a merge is authorised.

---
_Generated by [Claude Code](https://claude.ai/code)_
