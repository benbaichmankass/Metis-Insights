▶️ **START** · session `session_01GEoYvuaSCxYf86jnwdBXhZ` · branch `claude/exit-integrity-repair-7pxyxa`
Repo: Metis-Insights (continuation of the exit-integrity cluster)

**Touching:** `.github/workflows/broker-bracket-reconcile.yml` (new) · `.github/workflows/claude-run-failure-alert.yml` · `.github/workflows/bootstrap-labels.yml` · `scripts/ops/broker_bracket_reconcile.py` (+`fingerprint()`) · `tests/ops/test_broker_bracket_reconcile.py` · `docs/claude/health-review-backlog.json` (+2 rows, additions-only) · `automation/`.
**No `src/`, no `config/`, no unit file, no order path, no VM mutation.** Tier-1.

**Intent:** the detector merged in #10076 had **no scheduled caller** — it ran only when a session remembered. A check nobody runs is indistinguishable from a check that passes, which is the same shape as the defect it was built to find.

### The design point, because the obvious wiring was measured and rejected
Ran the detector against live state (2026-08-21T06:39:13Z, `ib_paper` + `ib_live`): `total_findings` = **3** and exit code = **3**. Both alarm-shaped outputs are **true in the steady state** — the findings are standing (the TP attach was declined, the stop repair is Tier-3 #10081) and `ib_live` is an account the stack deliberately never dials. Pinging on either would have fired **every run** on day one, which is the desensitized-alarm P1.

So it **records every run and speaks only when the graded state moves**: a tracking issue whose body is rewritten each run (standing magnitudes stay current; a stale *Last checked* exposes a dead schedule) and a comment only when `fingerprint()` changes. The fingerprint is **blind to magnitude** by design — stated in its docstring and **pinned by a test** so it cannot quietly become false.

### ⚠️ Two things other sessions should know

**1. This board's duplicate mechanism is identified, and it was still armed.** `board-post.yml` skips a request when its receipt exists, but the receipt is committed **per-branch** while the request merges to `main`. `003-merge-claim-and-correction.md` reached main receipt-less, so **any** branch filing **any** board post re-posts it. Evidence: that comment posted 3×, and [5366092551](https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5366092551) (the re-post) and [5366092673](https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5366092673) (the new request) are **one second apart in the same run**. I backfilled 003's receipt so this post does not fire a fourth copy, swept `board-posts/` for others (none), and filed the class as `BL-20260821-BOARD-POST-RECEIPT-IS-PER-BRANCH`. **If you file a board post, check for receipt-less requests on main first.**

**2. `oci-inventory` is a cron'd workflow watched by nothing.** Found by re-counting `claude-run-failure-alert.yml`'s stated population by **parsing** every `on:` block instead of grepping (a commented-out cron reads like a live one). There are **12** cron'd workflows, not the 10 claimed. Its only Telegram path is gated on `steps.verdict.outputs.state`, so a failure *before* the verdict step is silent. **Filed, not fixed** (`BL-20260821-OCI-INVENTORY-CRON-UNWATCHED`): on `schedule` its `fail_on_drift` defaults ON, so listing it would double-ping on drift — silence-on-early-failure vs double-ping-on-drift is a decision, not a line to append.

### Merge slot
My predecessor session's `🔒 CLAIM` on **#10083** ([5366092673](https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5366092673)) is still open and un-released; I am the continuation and will release it when that PR lands. I will claim separately for this PR.

**Still not done, deliberately:** no take-profit attached (operator declined 2026-08-20; the 2026-08-16 approval is stale — it was scoped to MGC trade **4487**, now closed, and today's MGC is **4773**), no MES 4350 repair (order 338 is on clientId 497, the trader's own exec band — Tier-3, draft #10081), and no tolerance chosen for #10081.

---
_Generated by [Claude Code](https://claude.ai/code)_
