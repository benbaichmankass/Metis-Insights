⚠️ **CORRECTION** · session `session_014myC5S5VacHNuzzBR8dGBC` — retracting a claim I made two comments ago, [5365976641](https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5365976641).

## What I said, and why it is false

I wrote: *"I am **dispatching the three required checks** on that ref"* — about **PR #10082**, which sits with auto-merge armed and 1 of 5 checks.

**I cannot dispatch anything. This session has no Actions-dispatch tool at all.** `CLAUDE.md` § "PM-side session capabilities" records `run_workflow` as working since the 2026-08 MCP update, and I wrote the sentence from that line **without checking my own tool surface**. Two `ToolSearch` probes return nothing, and there is no issue-driven workflow-dispatch relay in `.github/workflows/` either (checked, not assumed). So #10082 is exactly as blocked as it was, and my comment claimed a remedy was in flight when nothing was.

**The precedent-over-verification shape, on a capability claim.** `CLAUDE.md` is a *claim about* what a session can do; the tool list is the field. Field beats comment — and I have now made that same error twice in this cluster, having earlier trusted `place_protective`'s docstring over its body. That is the very thing PR #10083 (below) exists to fix in the prose.

## What is actually true about #10082

`claude/session-wrap-0820`, from `sysrev-0816`. Head `daace9af`, **1 of 5 checks** — only `open-and-automerge`; `guards`, `pytest-collect`, `pytest-run`, `repo-inventory` never attached. Auto-merge is armed and **will never fire**.

Both known causes ARE excluded, and that part stands: the head was pushed by session credentials (not `github-actions[bot]`, so the `GITHUB_TOKEN` recursion rule does not apply), its message carries no skip directive, and none of the four required workflows is path-filtered. That leaves the dropped-`pull_request`-delivery class, `BL-20260730-PR-CI-NOT-ATTACHING`.

**The remedy needs someone who can either dispatch or push.** All three required checks carry a `workflow_dispatch` for precisely this; one ordinary commit on the branch would also do it. I am doing **neither** — it is another session's branch, and adopting it is not mine to decide. **Flagging, not fixing, and now saying so accurately.**

## Meanwhile: PR #10083 is open — `place_protective` docstring, Tier-1

One docstring in `src/units/accounts/ib_client.py`, **no executable lines**. It said the method *"places only the reverse-side protective legs"*; it also **pre-cancels**, and a session that read it and stopped there concluded no safe in-place repair existed — the wrong conclusion that sent a Tier-3 question to the operator.

Its own CI hit the pathology it documents: the relay's `pr-opener: results` commit by `github-actions[bot]` landed as the head, giving zero checks. **Verified by reading the head, not assumed** — then armed with one ordinary commit (`1e4a6b8a`). I will not read a zero-check state as green.

Still **not** doing: no take-profit attached (operator declined 2026-08-20; the 2026-08-16 approval is stale — it was scoped to MGC trade **4487**, now closed), no MES 4350 repair (clientId 497, Tier-3, draft **#10081**).

---
_Generated by [Claude Code](https://claude.ai/code)_
