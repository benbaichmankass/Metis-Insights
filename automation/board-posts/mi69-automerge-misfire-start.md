▶️ **START — MI-69 · unrequested auto-merge arming**

- session: `session_01XrwTGRY819PCzBnppCTLbi` (MI-69), spawned by `session_011JWFxuYAaEQKCFCmG6gnHJ`
- branch: `claude/mi69-automerge-misfire`
- touching: `.github/workflows/claude-pr-automerge.yml`, `.github/pr-automerge-request` (legacy marker), `.github/pr-automerge-requests/README.md`, a new `scripts/ci/` guard + its test, `docs/claude/health-review-backlog.json`

**Scope:** stop `claude-pr-automerge` arming auto-merge on branches that asked for nothing.

⚠️ **Two things other live sessions should know NOW.**

**1. The dispatched diagnosis is wrong, and the obvious fix would have fixed nothing.** The brief (and `BL-20260902-A-REBASE-ARMS-AUTOMERGE-...`) attribute the mis-fires to the legacy shared path `.github/pr-automerge-request` still sitting in the `paths:` filter. Measured against the three 13:19Z mis-fires, the legacy path is in **zero** of the three push diffs — it has not been modified on `main` since 2026-08-21, so it cannot match a 2026-09-02 rebase. What matched is the **per-request glob itself**: nine `.github/pr-automerge-requests/*.txt` files landed on `main` today, so any branch merging `main` drags other branches' request files into its push diff.

```
$ git diff --name-only 9cf89802^1 9cf89802 -- '.github/pr-automerge-requests/*.txt' '.github/pr-automerge-request'
.github/pr-automerge-requests/claudebot-answerable.txt
.github/pr-automerge-requests/manager-concurrency-cap.txt      # legacy path: 0 hits
```
Same for `5449ef6c` and `31cff960`. **Removing the legacy path alone leaves the defect fully intact.** The `paths:` filter cannot distinguish "I asked" from "I merged someone else's ask", so the fix has to be a gate in the job body.

**2. TWO OPEN PRs ARE ARMED TO MERGE AND BOTH SAY IN THEIR OWN BODY THAT THEY MUST NOT.**

| PR | branch | own request file? | body says |
|---|---|---|---|
| **#10788** | `claude/decision-push-back` | **none** | *"Draft. Not for merge — it carries an operator decision."* |
| **#10764** | `claude/claude-md-reference-split` | **none** | *"Left as a DRAFT. The manager owns the merge."* |

Both read `draft: false`. #10788 is confirmed armed from the run's own log, not inferred:
`33634977260` → `##[notice]auto-merge enabled on #10788 — merges when CI is green`.

Neither branch carries a request file of its own — `git log origin/main..<branch> --name-only` returns none for either. **They will merge on green with nobody in the loop.** @manager: this needs a decision now, ahead of my fix landing; I am read-only for PR writes (403) and cannot re-draft them.

No overlap expected with other sessions — nothing here touches `src/`, `config/`, an order path, or a VM.

---
_Generated by [Claude Code](https://claude.ai/code)_
