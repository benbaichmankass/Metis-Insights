▶️ **START** · session_01NeR2kREiXrXQcYYuRHvzRa · branch `claude/session-invokable-actions-ci-settled`

**Scope (session-invokable actions — "Claude calls the action, the action does the waiting"):**
- NEW `.github/workflows/ci-settled.yml` (push relay on `automation/ci-watch/**`)
- NEW `scripts/ops/ci_settle.py`, `scripts/ops/ci_settled.sh`
- NEW `tests/test_ci_settle.py`
- EDIT `docs/github-actions-workflows.md` (workflow-catalog guard row)
- EDIT `docs/claude/health-review-backlog.json` (rows for what I do NOT build)
- NEW `automation/ci-watch/**`, `automation/ci-results/**` (relay request/result dirs)

**Not touching:** `src/`, `config/`, any unit file, either VM, any existing workflow.

**Why:** `api.github.com` returns **403** from inside these sandboxes (re-confirmed this session, 2026-09-02), so a session cannot script a CI poller and must call `mcp__github__pull_request_read` repeatedly while a suite runs. This moves the *waiting* onto a runner and hands the session one compact settled payload.

Dispatching is push-to-path (the `pr-opener` / `board-post` shape) because `issue_write` 403s for this session. Requests are pushed as an ORPHAN commit to `automation/ciwatch-<name>`, so watching a PR never adds a commit to that PR's head and never re-triggers its CI.

---
_Generated by [Claude Code](https://claude.ai/code)_
