✅ **DONE — MI-89, the manager view**

- **session:** `session_01Kyq3Zpgh6DoujFGAMkyJ9o` · **branch:** `claude/mi89-manager-view`
- **draft PR:** #10896 · **checklist item:** MI-89 · cycle `CY-20260903-MANAGER-CONTROL`

All three deliverables landed. Full evidence in the PR body; the three things a manager should read first:

**1. The dispatch's preflight diagnosis needed correcting, and I have the evidence both ways.** `manager_preflight.py --self-test` **passes** on a full clone of `main` @`bdbf090c` (62 assertions, exit 0). I reproduced the reported failure **verbatim** in a `--depth=50` clone. The assertion was `count_autonomous_actions(2020) > 100` — a claim about **clone depth**, not about the behaviour under test. In that same shallow clone the tool was *correct* (50 of 50 reachable counted; 0 for the far-future cutoff). **The test was wrong, not the tool.** Replaced with an equality against the count git reports — strictly stronger: a planted silent under-count of 3 **passes** the old `> 100` and **fails** the new assertion. Verified at depth 1, depth 50 and full.

**2. `scripts/ops/manager_view.py` reproduced the cycle's finding by an independent route.** Population: all 82 `SESSIONS.json` rows, of which 24 assert live work, graded against a live `list_sessions(mine=true, limit=40)` read at 06:15Z — **24 of 24 CONTRADICTED**. It stores nothing (no fourth register), derives every column at read time, and mirrors `handoff_check.py`'s `ready`/`not_ready`/`unknown` shape rather than inventing a register. Supporting number for *why* deriving beats storing: only **34 of 82** rows carry a `state` field at all — for 48 rows the stored register has no opinion to be stale.

**3. ⚠️ A FINDING FOR THE MANAGER, not for me to fix — and my own tool found it on its first live run.** **MI-89's registry row does not exist on `main`.** Registry key `pending-20260903T054243Z` appears in **0 of 82** rows of `SESSIONS.json`, and no row carries this session's id. So the exact failure `session_registry.py` exists to prevent (3 of 6 on 09-01, 6 of 9 on 09-02) **recurred for this session**. I have deliberately NOT written the row myself: `SESSIONS.json` is a monolithic register that re-conflicts on sibling PRs (measured 61% header contention), and the row needs the manager's own `spawned_by`/`why`. Please `session_registry.py confirm --registry-key pending-20260903T054243Z --session-id session_01Kyq3Zpgh6DoujFGAMkyJ9o`.

**Guards:** `run_guards.py --base main` → **78 PASS · 4 FAIL**, and all 4 are container-environment rather than repo defects — 3 invoke `python3 -m pytest` which is not importable here (run against the real `pytest` binary: **144/144 pass**), and `layer-guard` exits 127 because `lint-imports` is not installed. **I could not run that one; that is `unknown`, not green.** Three guard failures *were* mine and are fixed — the guards caught all three, including one where my new workflow step's exit-status token collided by substring with an invariant `check_pr_queue_watch.py` asserts. I renamed rather than loosening the assertion.

**Not proven, stated plainly:** no queue-watch receipt exists yet, so the new guard grades `never_ran` (correct — it arms on first run). **The Routine's prompt must be updated to pass `--write-receipt`; a Routine prompt cannot be changed from the repo, so that is a manager action.** And `manager_state_watch.py` has never run in CI — merged is not deployed and deployed is not run.

Handing over. — MI-89
