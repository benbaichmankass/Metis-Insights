▶️ **START** · Phase F / C3 (decision preparation) · session `session_01MDjxAnncsh71UiRAWtYHkH` · branch `claude/phase-f-c3-decision-prep`

**Board tail read first:** page 125 @ `perPage=15` returned **8 items — a short page, which is the proof of end** (`BL-20260817-BOARD-TAIL-READ-CANNOT-ASSERT-IT-REACHED-THE-END`). Latest was the #10672 merge-claim audit at 20:46:57Z. No live session holds this area.

Posting via this relay because `add_issue_comment` returns **403 "Resource not accessible by integration"** — persistent across two attempts, so it is the read-only-MCP case `board-post.yml` exists for, not the transient reconnect blip.

## ⚠️ Correcting my own brief: most of C3 is ALREADY SHIPPED, and I am not rebuilding it

I was pointed at the build plan's finding that `strategy_review_packet.py` *"emits a real action badge … and writes to a gitignored path with no cron"*, with the repair given as *"a cron and a committed path"*. **RULE ONE — I verified rather than taking the doc's word, and two of the three parts already exist**, landed by PR #10649 (merged 2026-09-01T11:58Z) and repaired by #10653:

| Part | State | Evidence |
|---|---|---|
| A schedule that runs it | ✅ **exists** | `.github/workflows/strategy-review-packets.yml`, `cron: "40 4 * * *"` + `workflow_dispatch` + issue-driven |
| A committed path | ✅ **exists** | `comms/strategy_reviews/2026-09-01/` on main — 105 files; `INDEX.json` `graded=52 · actionable=0 · by_action={"hold": 52}` |
| A read surface | ❌ **missing — this is my scope** | grep-verified below |

Rebuilding the first two would be `RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED`, whose cheap preventer is exactly the existence check I ran. **Flagging it because the plan text is now stale in the dangerous direction** — it invites the next session to duplicate a shipped mechanism. I will correct the Phase F paragraph in the same PR.

## The gap that is real, with evidence

`grep -rn "comms/strategy_reviews"` over `*.py`/`*.ts`/`*.svelte`/`*.yml` returns **the writer and the docs, and no reader**. The committed decision record has **zero consumers**. The only route, `GET /api/bot/strategies/{name}/review`, reads `runtime_logs/strategy_reviews/` — `.gitignore:29`, VM-local, lost on re-provision — and is **per-strategy only**, so "what did the fleet grade today and what does it propose" cannot be asked without opening 52 files by hand.

A record that is written and never read is the shape `provenance-consumer-guard` exists to catch, and it is the C3 failure one level up: the packet becomes durable and still reaches no decision.

## ⚠️ On the cron — DEPLOYED, NOT OBSERVED

The workflow landed at **11:58Z today** and its cron is **04:40 UTC**, so **its first scheduled opportunity is 2026-09-02T04:40Z and has not arrived yet.** That is a third state, distinct from both "fired and worked" and "fired and failed", and I will not let it read as either. Both existing runs (#10652, #10656) were dispatch-driven, not `schedule`. `OI-20260901-SCHEDULED-PROBES-AND-DUE-LIST-HAVE-NEVER-FIRED-ON-CRON` is a live counter-example in this repo, so correct cron syntax is not evidence of firing.

I cannot read run history to check: `api.github.com` returns **403** in this sandbox (as CLAUDE.md documents) and no Actions MCP tool is in this session's toolset. Stating that as a limit rather than inferring a clean negative.

## Files I will touch — Tier-1, read-path only

`src/web/api/routers/strategy_review.py` · its test · `CLAUDE.md` API table · the stale Phase F paragraph in `docs/design/operating-layer-build-plan-DESIGN.md`.

**Nothing in `config/strategies.yaml`, no risk caps, no order path.** Anything Tier-3 goes in the PR body, not the tree. Not taking C4 — deferred with a reason I am not routing around.

---
_Generated by [Claude Code](https://claude.ai/code)_
