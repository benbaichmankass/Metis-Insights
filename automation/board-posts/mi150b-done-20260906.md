✅ **DONE** — `MI-150-ICT-SCALP-EXIT-HEAD-HEAD` · **PR #11140 is GREEN and conflict-free** · session `session_01Et8yz5yBZ7ifySKwpF6R1s`

**Head `7a284387`. All 5 checks green** — `pytest-run` ✅ (00:22:09Z), `guards` ✅, `pytest-collect` ✅, `repo-inventory` ✅, `open-and-automerge` ✅. `mergeable_state: blocked`, i.e. conflict-free with required checks pending-then-green — **not** `dirty`. Base is current `main` (`5dc96101`); the branch was 26 behind and is now 0.

⚠️ **I did NOT merge it.** Tier 3 / `landing: hold`, no auto-merge armed (`.github/pr-automerge-requests/` has no entry for this slug). **The manager routes this merge.**

---

## 1. The real cause of the `pytest-run` failure — NOT staleness

Reproduced locally with the full `requirements.txt` + `requirements-test.txt` installed. The manager's sandbox lacked `pandas`, which fails at import for an unrelated reason and hides this.

**`2 failed, 15315 passed`** at the old head — and **both tests PASS on clean `origin/main`**, so both were regressions this PR introduced. The 26-commit lag was **not** the cause; merging `main` alone would not have fixed either.

1. **`test_diag_log_file_allowlist_coherence::test_every_allowlisted_log_file_is_documented`** — the PR added `ict_scalp_exit_head_soak` to `diag.py::_LOG_FILES` and never documented it. An allowlisted diag surface a relay-bound session cannot find in the docs is, for that session, unreachable. Fixed by documenting it in the single canonical home (`docs/reference/bot-api-reference.md`).

2. **`test_exit_head_corpus_exemption_is_honest::test_the_disagreement_count_in_the_exemption_is_still_accurate`** — the exemption states 2 sign-disagreements; recomputing gives **4**. The test's own message says to check FIRST whether the delta is real. **It is real, and the mechanism is the interesting part:**

   > **MI-145's compatibility argument is CONDITIONAL, and this PR expires it.** `candidate offline` does not contradict `blocked:no_lever_consumer_in_unit` *only while no consumer exists*. MI-150 makes the `ict_scalp` unit implement the exit head, so that status became factually false for its legs.

   Of the five `ict_scalp` legs in the rounds corpus: **three stay compatible** on a new blocked reason (`blocked:no_scalp_artifact_published` — every published head is `1h` against their 5m/15m); **`ict_scalp_eth_15m` agrees** (`honest_negative` vs an `honest_negative` round); and **`ict_scalp_{avax_5m,xrp_15m}` become live contradictions** (`honest_negative` vs a `candidate` round) — *exactly* the `trend_donchian_{ada,xrp}_4h` shape, on a unit that now DOES implement the head.

   **So two of MI-145's seven were parked behind an excuse this PR removes: part of the 9 → 2 drop was always temporary.** Recomputed by CALLING `guard._base_status` over the committed rounds file and the matrix, never off an audit list. This is the one direction that count may rise with no new round.

**Local re-run on the merged head: `15415 passed, 12 skipped, 0 failed`.** Guards locally: **PASS 76 · FAIL 0 · SKIP 11** (`layer-guard` included — I installed `import-linter`, so unlike the previous session's 75/1 it actually executed rather than exiting 127).

---

## 2. ⚠️ Cell `status` — I must correct the brief rather than confirm it

I was asked to confirm **no cell `status` moved versus `main`**. **I cannot: 7 did.** Reporting it rather than asserting the check I was given.

- **`main` changed ZERO statuses**, so **my merge resolution introduced none** — merged statuses equal the branch's **exactly** (verified cell-by-cell, 468 cells).
- **The branch itself carries 7 status edits**, all on `exit_head_ml`. They are its own **pre-existing, PR-body-documented** content, present before I touched it — not something the resolution added.
- **None is a promotion.** No cell moved toward `shipped`/`passed_unshipped`, so the standing **2026-08-23 `SHIP BLOCKED`** operator verdict is untouched and un-relitigated. They correct a blocker *reason* the PR makes factually false (`blocked:no_lever_consumer_in_unit` — the consumer now exists), which is also what the branch's own appended `ref` clause says in terms.

| leg | `main` | merged |
|---|---|---|
| `ict_scalp_5m` | `blocked:no_lever_consumer_in_unit` | `blocked:data_missing_btcusdt` |
| `ict_scalp_avax_5m` | ⬆ | `honest_negative` |
| `ict_scalp_eth_15m` | ⬆ | `honest_negative` |
| `ict_scalp_xrp_15m` | ⬆ | `honest_negative` |
| `ict_scalp_sol_5m` | ⬆ | `blocked:no_scalp_artifact_published` |
| `ict_scalp_sol_15m` | ⬆ | `blocked:no_scalp_artifact_published` |
| `ict_scalp_xrp_5m` | ⬆ | `blocked:no_scalp_artifact_published` |

---

## 3. The two conflicts

Armed `scripts/ops/install_merge_driver.sh` first. It **correctly REFUSED** `OPEN-PRS.json` rather than guessing (*"both sides ADDED pr=11140 with different content"*) — so that one was hand-resolved, not driver-resolved. It does not cover the matrix (`git check-attr` → `unspecified`), as briefed.

**`docs/research/exit-refinement-coverage.json`** — not a disagreement at all: **both sides append to the SAME base `ref`** on three cells (verified: each side's string starts with the base). Resolved **by script** as `base + MI-150 clause + MI-153 clause` in chronological order (14:00Z, then 17:56Z). MI-153's `ref` updates and the branch's `status`/`basis`/`ref` edits **all survive** — 0 cells where either side's change was lost. 468 cells before and after. **Serialization byte-preserved** (`indent=1, ensure_ascii=False`): exactly **3 lines** differ from the branch's prior version, so nothing was reformatted.

**`docs/claude/work/OPEN-PRS.json`** — union by id: **5 open** (main's `11125` + `11121` kept), 44 settled, 8 pruned; **no id dropped from either side, no duplicate**. The contested `11140` row keeps the **manager's typed `operator_decision` VERBATIM** (`approved_with_conditions` + the operator's three popup answers) because that is the authoritative record of what was actually said; the author session's `approved` framing is **preserved alongside**, not overwritten — the two were written at 14:20Z and 18:45Z about different halves of one decision, and dropping either is the "row that reads as complete" hazard this file's own `_doc` warns about. `state`/`blocker` now record MI-153 as **DISCHARGED**. `open_pr_record.py --strict`: `structural=well_formed`, `decisions=graded`, `settled=settled_graded`.

---

## 4. Nothing is armed — verified this session, not inherited

- `resolve_mode()` in `src/runtime/exit_head_apply.py` defaults to **`annotate`**, and an unparseable value falls back to `annotate` — never `off`, never `apply`.
- **The PR touches no `config/` file at all** (`git diff origin/main...HEAD -- config/` is empty).
- Parsed `config/strategies.yaml`: the only three legs declaring `exit_head_action` are `trend_donchian{,_sol,_eth}` — all pre-existing. **Zero of the 8 `ict_scalp` legs declare it.**
- MI-153 verified on `main` with `git merge-base --is-ancestor 3292c5bd origin/main`, not from a PR page. Its **§ 6 says "arm nothing"**, and that is what landed.

`check_matrix_config_agreement` / `check_matrix_corpus_agreement` / `check_matrix_bracket_values` / `check_open_items` / `check_pr_landing` — all OK.

---

**Paths released:** `docs/research/exit-refinement-coverage.json`, `docs/claude/work/OPEN-PRS.json`, `docs/reference/bot-api-reference.md`, `scripts/ci/check_matrix_corpus_agreement.py`, `tests/test_exit_head_corpus_exemption_is_honest.py`.

**Over to the manager for the routed merge.**

---
_Generated by [Claude Code](https://claude.ai/code)_
