✅ **DONE — MI-69 · unrequested auto-merge arming** → **[#10810](https://github.com/benbaichmankass/Metis-Insights/pull/10810)** (DRAFT — manager merges)

## ⚠️ Read this first: the dispatched diagnosis was wrong

MI-69 was sent to remove the legacy shared path `.github/pr-automerge-request` from the `paths:` filter. **Measured on all three 13:19Z mis-fires, that path is in ZERO of the push diffs** — it had not been modified on `main` since 2026-08-21, so it could not match a 2026-09-02 rebase. What matched is the **per-request glob**, because nine `*.txt` request files landed on `main` today and a merge of `main` drags them into the push diff.

**The prescribed fix would have fixed none of the three — while looking like a fix and closing the rows.** `BL-20260902-THE-PER-REQUEST-GLOB-IS-ITSELF-DRAGGED-IN-BY-A-MERGE-OF-MAIN` records the correction.

The transferable bit: the mechanism was inferred from a plausible reading of the workflow and never checked against the diff. `git diff --name-only <sha>^1 <sha>` inverts the conclusion in one line. MI-62's row was the more honest of the two — it marked its mechanism **UNVERIFIED**; the confidently-stated one is what sent a session at the wrong target.

## The fix

**A `paths:` filter cannot be the gate** — it cannot tell "I asked" from "I merged someone else's ask". This was the *third* narrowing of that filter, and the first two both recurred. The gate now lives in the job body: the request file must be **named for this branch** and **added-or-modified relative to `main`**. Plus: the relay **no longer calls `markPullRequestReadyForReview` at all**.

Legacy file **deleted** (verified: zero of 14 open `claude/**` branches touch it). Held by `scripts/ci/check_automerge_trigger.py`, registered **ungated** — a diff-scoped guard only fires when someone edits the relay, and nobody was editing the relay when it armed three PRs.

**Negative control, both halves.** `tests/test_automerge_request_gate.py` runs the *shipped* script under node. Against the pre-fix workflow: `5 failed, 2 passed` — the four negatives fail, the two positives pass. Against this PR: `7 passed`. The negatives failing before is what makes them instruments; the positives passing in both is what shows the trigger isn't merely broken.

## 🚨 @manager — STILL ARMED, and I cannot disarm them

The fix stops **new** mis-arming; it cannot retract auto-merge already enabled.

- **#10788** `claude/decision-push-back` — `draft: false`, no request file. Body: *"Draft. Not for merge — it carries an operator decision."* Confirmed armed from run `33634977260`: `auto-merge enabled on #10788 — merges when CI is green`. It carries an **unanswered operator decision** on minting an account-wide `CLAUDE_CODE_OAUTH_REFRESH_TOKEN`.
- **#10764** `claude/claude-md-reference-split` — `draft: false`, no request file. Body: *"Left as a DRAFT. The manager owns the merge."*

Branch protection is not a backstop: it gates on **checks**, and the checks are green. `disable_pr_auto_merge` / `update_pull_request` are 403 from this session. Filed **critical** as `BL-20260902-TWO-OPEN-PRS-ARE-ARMED-TO-MERGE-WHILE-THEIR-OWN-BODIES-SAY-DO-NOT-MERGE`.

Guards **PASS 72 · FAIL 0**. `layer-guard`'s 127 was `lint-imports` missing from the container — installed and run, 6 contracts kept.

---
_Generated by [Claude Code](https://claude.ai/code)_
