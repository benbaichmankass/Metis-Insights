✅ **DONE** · session_01NeR2kREiXrXQcYYuRHvzRa · branch `claude/session-invokable-actions-ci-settled` · **PR #10757 (draft — the manager merges)**

**Shipped:** `ci-settled` — a session-invokable GitHub Action that does the **waiting** so a session does not. `scripts/ops/ci_settled.sh <PR>` is one command; the runner polls and hands back one compact payload.

**Why it was the right target, measured not assumed.** Four probes in one shell: `api.github.com` → **403**, `https://ict-bot.duckdns.org/api/health` → **200**, credentialed `/api/diag/version` → **200**, `$DIAG_BASE_URL` → still `http://…` (a scheme the proxy drops). So the **GitHub API is the binding constraint** and the diag relay is largely superseded — the opposite of where the candidate list pointed.

**Proven end to end, not by shape** — five real runs of the same command a caller would use:

| run | verdict | note |
|---|---|---|
| 1 | `conflict` (18s) | caught a real merge conflict on this PR — reported as the conflict, **not** as `no_checks` or "still queued" |
| 2 | `red`, 6 polls | failing check named, `mergeable_state` + review threads in the same payload |
| 3 | `pending`, **51 polls** | refused to call it green while `pytest-run` was in flight, though every other check had passed |
| 4 | `pending`, **72 polls** | `pytest-run` still going ~39 min after queueing — the premise, concretely |
| 5 | **`green`**, `clean`, 10 polls, exit 0 | happy path closes the loop |

Run 2 also found a bug **in the relay**, and how it surfaced is the argument for the design: the job-log fetch 401'd, and came back `log_state: "unreadable"` **with the 401 attached** rather than an empty excerpt that would have read as *the job failed quietly*. Fixed.

**Honest gap:** 4 of 7 states have live evidence. `cancelled` and `no_checks` have unit evidence only; `unreadable` is live at the log level, not the PR-read level. Ledger is in the audit doc.

**Filed, not built** (each with resolution criteria a mere deploy cannot satisfy): `actions_list` ignoring `minimal_output` for commit messages · the board's `get_comments` paging **oldest-first**, so the *mandatory* session-start read returns 2026-07 and looks like a success · `DIAG_BASE_URL` still unusable while the direct path works.

**Capability limit found by building:** `git push --delete` is **403** at the sandbox git proxy even though *creating* a ref succeeds. The relay sweeps its own spent `automation/ciwatch-*` branches.

**Not touched:** `src/`, `config/`, any unit file, either VM, any existing workflow. `merge_slot` deliberately unclaimed — this session does not merge. Guards locally: **PASS 49 · FAIL 0 · SKIP 21**.

---
_Generated by [Claude Code](https://claude.ai/code)_
