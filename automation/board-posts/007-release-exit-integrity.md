🔓 **RELEASE** · session `session_014myC5S5VacHNuzzBR8dGBC` — exit-integrity cluster, merge slot free.

All four claimed PRs are merged. The slot claimed in [comment 5365617904](https://github.com/benbaichmankass/Metis-Insights/issues/6927#issuecomment-5365617904) is released.

| PR | merge commit | contents |
|---|---|---|
| **#10076** | `2be01dea` | `scripts/ops/broker_bracket_reconcile.py` + 28 tests · `board-post.yml` · backlog |
| **#10077** | `a62c6962` | `tests/ops/test_attach_ib_target.py` |
| **#10078** | `70196ac5` | one backlog row |
| **#10079** | `9752348b` | `pr-opener.yml` fix + the new `pr-close.yml` relay |

**Closed without merging:** #9924, #9919 (their live content landed as #10077 / #10078 respectively — the PRs themselves were stale branches).
**Still open and NOT mine:** #10068 (draft, untouched by this session).
**Left open deliberately:** **#10081** — a Tier-3 *proposal* (`docs/design/ib-protective-price-reconciler-PROPOSAL.md`), draft, **no code implemented**, awaiting an operator decision. Nothing in it is merged or running.

---

## ⚠️ Three things the next session must not misread

**1. `MES 4350` is still protected 69 ticks from its declared stop.** The detector flags it; nothing repaired it. **Do NOT fire a one-off cancel.** Order 338 sits on **clientId 497** — the trader's own exec band — and `cancel_ib_order.py` refuses `<9000` precisely because connecting on that id evicts the trader's live IB session. The safe repair is **one `modify_protective` call on the trader's own client**; a place-then-cancel would create disjoint OCA groups, i.e. over-cover then naked short. No urgency: paper account, uPnL +$2,287.50, price ~7665 vs a stop ~131 pts away.

**2. Correction I owe this board, again: `place_protective` does NOT "only place, never cancel".** I read its docstring, which omits the pre-cancel, and stopped there — then concluded from it that *no safe repair existed*, which is what sent a Tier-3 question to the operator. `_locked_place_protective` **does** pre-cancel resting protective legs (OCA-scoped when given, symbol-wide otherwise, `BL-20260624-MHG-FLIP`). Field beats comment; the docstring is the thing that is wrong.

**3. The detector has no scheduled caller.** `scripts/ops/broker_bracket_reconcile.py` is merged and runs only when someone remembers to run it. Exit 3 = *could not look*, which is not a pass.

_Generated by [Claude Code](https://claude.ai/code)_
