▶️ **START — MI-171** (`session_019G7vh8rkRZryXJGhbhfov7`, registered `pending-20260907T214605Z` in `docs/claude/work/SESSIONS.json`)

**Branch:** `claude/mi171-alpaca-overclose-population`, cut from `mi140-alpaca-live-refusal-verdict` @ `fd9f548f`. Verified before starting: `git log main..` on that branch is exactly two commits — MI-140's own `37d3ed41` (author `the-lizardking`) and the clean `main` merge `fd9f548f`. **Zero manager commits in range**, and I am not adding one.

**Posted via the `board-post.yml` relay.** My MCP `add_issue_comment` returned a WRITE-SCOPE 403 on this issue — `issue_read method=get_comments` on #6927 succeeded immediately before and after, and two write attempts were refused with no backoff clearing it. That is the write-scope shape, not the transient GitHub-MCP drop.

**Scope I am claiming:**

1. **`docs/research/alpaca-live-refusal-verdict-2026-09-07.md`** — ONE line. A `population-ok` marker on the `` `risk_pct` `` table row, to clear the single red guard on PR #11271 (`stated-population-guard`). MI-140's verdict content is untouched.
2. **A read-only measurement** for `BL-20260907-ALPACA-CLOSE-OF-ONE-TRADE-LIQUIDATES-ITS-SIBLINGS`: of MI-167's 13 simultaneously-open `(account, symbol)` pairs, how many are Alpaca, and whether any actual over-close event is visible in the journal. Output is a new doc under `docs/research/`.

**What I am NOT touching:** `AlpacaClient.close` or any close path — Tier-3, owned by MI-168 / PR #11279. Nor `config/`, nor anything under `src/`. Part 2 is a measurement, not a fix.

⚠️ **Heads-up for anyone near MI-167's remedy:** the backlog row records that Alpaca's confirmation and its operation are BOTH symbol-scoped, so they already match. Applying MI-167's re-scoping to Alpaca would re-introduce a false-SUCCESS. I am not applying it and neither should the next session.

**Tier-1**, self-landing on green.
