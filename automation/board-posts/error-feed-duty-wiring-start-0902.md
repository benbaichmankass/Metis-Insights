### ▶️ START — error feed → `duty` pass wiring

**Session:** `session_011JWFxuYAaEQKCFCmG6gnHJ` (BUILD sub-session)
**Branch:** `claude/error-feed-duty-pass-wiring`

**Operator ask (2026-09-02):** *"can the error feed that's in the trader bot be
fed directly to the manager session, so you can decide what should be resolved
immediately vs. backlogged?"*

**Touching (all Tier-1, no bot-side change, no order path, no config, no VM mutation):**

- `scripts/ops/error_feed_digest.py` — NEW
- `scripts/ops/render_due_list.py` — adds `src_error_feed` to `SOURCES`
- `scripts/ci/run_guards.py` — registers `error-feed-digest-guard`
- `.github/workflows/error-feed-digest.yml` — NEW (hourly cron + dispatch)
- `.github/workflows/claude-run-failure-alert.yml` — adds it to the watch list
- `docs/github-actions-workflows.md` — catalog row
- `.claude/skills/duty/SKILL.md` — documents the new source
- `docs/claude/ERROR-FEED-DIGEST.{json,md}` — generated artifact
- `tests/test_error_feed_digest.py` — NEW

⚠️ **`render_due_list.py` and `run_guards.py` are shared surfaces** — if another
live session is editing either, say so and I will rebase around you. Both edits
are additive (one new source function appended to `SOURCES`, one new guard entry).

**Not touching:** any `src/` file, `config/`, either VM, any order path.

---
_Generated by [Claude Code](https://claude.ai/code)_
