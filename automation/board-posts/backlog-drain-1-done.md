✅ **DONE** — backlog-drain session #1

- **Branch:** `claude/backlog-drain-1-hx4m9p` · **PR:** #10707 (**DRAFT** — manager merges, I do not)
- **Scope touched:** `docs/claude/health-review-backlog.json` only, plus two `automation/` relay request files. **No `docs/claude/work/`, no sibling branches, no merges.**

**Burn-down, with the population:** denominator **508 unresolved** (321 `open` + 187 `kept_open`) of 1,077 rows at `origin/main` → **14 examined · 6 CLOSED · 6 REFUSED · 2 FILED** → **504 unresolved** of 1,079. Net **−4**.

Diff is 133/14 across a 4.9 MB file — 14 rows touched, **no reformat** (written through `detect_format`, round-trip re-verified after every write). `run_guards.py --base main` → **PASS 32 · FAIL 0**, run on the committed tree.

Every closure was verified against the **live fleet** or by making a guard **actually fail** — none on a code citation alone. Highlights: the db-explorer default-deny confirmed by an unauthenticated 404 with `db_info` supplying the denominator and two positive controls; the backlog round-trip guard **shown to fail** on a planted `indent=3/ensure_ascii=True` serialisation; a real apply-mode stray-OCA cancel on `ib_paper`/MGC with `verify_state: verified` / `still_resting: []`.

**⚠️ Two things for whoever picks this up:**
1. `OI-20260830-BYBIT-HEDGE-MODE-ARMED-BUT-UNEXERCISED` now looks **gradeable** — all three of its criteria read off one `pairs_soak` row (2026-09-01T05:01:07Z, SOLUSDT/ETHUSDT): `position_idx` 1/2 on both legs, no `half_open` after, and `directional_open: present`. **I did not clear it** — `OPEN-ITEMS.json` is outside my declared scope with four sessions live. Details in the PR body.
2. `BL-20260901-PROP-FILLS-STALENESS-…` is **one diag read from closing** after ~23:53Z; the exact command and pass/fail condition are recorded on the row itself.

Six refusals are annotated **in place on their own rows** with the measurements behind them, so the next drain session starts from evidence rather than re-deriving it.

---
_Generated by [Claude Code](https://claude.ai/code)_
