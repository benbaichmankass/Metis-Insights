🔄 **CORRECTION + final numbers** — backlog-drain session #1 (supersedes my `✅ DONE` above)

My DONE comment said **6 closed**. It is **7**: `BL-20260901-PROP-FILLS-STALENESS-FILTERS-ON-A-MUTABLE-REPORTED-AT-SO-A-CORRECTION-MANUFACTURES-A-FINDING` closed after the DONE post, once its cadence window elapsed. Refusals are therefore **5**, not 6.

**Final burn-down** — denominator **508 unresolved** of 1,077 rows at `988747c5`: **14 examined · 7 CLOSED · 5 REFUSED · 2 FILED**. Branch contribution **net −5**. Head reads 511/1,087, and the arithmetic closes exactly: `508 − 7 + 2 + 8 (sibling filings, all unresolved) = 511`.

⚠️ **I also corrected myself in the PR body**: an earlier revision claimed that sum did *not* reconcile and blamed two "already-terminal" sibling rows. That was an assertion I had not computed. Measured: 10 new rows = 2 mine + 8 siblings, 8 of 8 unresolved.

**CI is fully green** on the head — all 6 checks success. PR #10707 remains a **DRAFT**; the manager merges.

**⚠️ TWO `OPEN-ITEMS.json` ROWS NOW LOOK CLEARABLE on this session's evidence, and I cleared neither** (that file is outside my declared scope with four sessions live — flagging rather than acting):
- `OI-20260901-PROP-FILLS-STALE-BANNER-IS-A-KNOWN-FALSE-POSITIVE-UNTIL-THE-FIX-MERGES` — its `clears_when` asks for merge → restart on the merged sha → one cadence window → banner absent from a live read. All four now hold: process start 23:20:44Z on `cc1d71eb` (`git merge-base --is-ancestor 8b53aa4d cc1d71eb` TRUE), `__last_check__` advanced 22:49:09Z → 23:49:42Z, `findings: {}` cleared *itself* with no state-file surgery, and no `prop_fills_stale` banner — with four banners of other kinds rendering as the positive control.
- `OI-20260830-BYBIT-HEDGE-MODE-ARMED-BUT-UNEXERCISED` — all three criteria read off one `pairs_soak` row (2026-09-01T05:01:07Z), including the `directional_open: present` clause that row calls "the whole criterion".

Both are `loud`-class facts a morning session should not have to re-derive; the full evidence is in the PR body and on the backlog rows themselves.

---
_Generated by [Claude Code](https://claude.ai/code)_
