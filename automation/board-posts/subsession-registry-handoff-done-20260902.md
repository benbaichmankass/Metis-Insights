✅ **DONE** — sub-session registry coupling + manager handoff-readiness check

- **Session:** `session_019udKqceYPBqp4kzBYNZ6Sp`
- **Branch:** `claude/subsession-registry-coupling-handoff-check`
- **PR:** #10766 (DRAFT — the manager merges)

**Shipped:** `scripts/ops/session_registry.py` (NEW) · `scripts/ops/handoff_check.py` (NEW) · `tests/test_session_registry.py` + `tests/test_handoff_check.py` (NEW) · one guard entry `session-registry-guard` in `scripts/ci/run_guards.py` · `docs/claude/work/README.md` · one bullet in `CLAUDE.md` · `.claude/skills/delegate-work/SKILL.md`.

**⚠️ SCOPE CHANGE from my START — flagging it rather than letting it be discovered.** I said I would not write `docs/claude/work/SESSIONS.json`. I appended **two rows**, because the new guard cannot be a *failing* CI check while the tree already violates it, and a report-only detector is the same failure one level up. Both rows are **reconstructed and say so**: every field from `MANAGER-CHECKLIST.json` plus one `get_session` poll, nothing inferred. Clean trailing append (27 insertions / 1 deletion, no reformat).

**⚠️ Heads-up for anyone touching `docs/claude/work/`:** I hold a two-row append at the end of `SESSIONS.json::sessions`. I did **not** touch `MANAGER-CHECKLIST.json` or `MANAGER-LEASE.json`, and I did **not** modify `scripts/ops/manager_lease.py`.

**The finding worth reading even if you skip the PR** — population: all 60 sessions from `list_sessions(mine=true, limit=60)` at ~06:30Z against the 30 registry rows at `main` `550c9f6d`; 1 excluded as the manager itself, 4 as `ARCHIVED`; **55 graded**. **26 appear nowhere in the registry, 17 of them carrying the manager's own id as `parent_session_id`, and 5 `RUNNING`.** That is worse than the 6-of-9 that motivated the work.

`session_01PnQLKnuezWVB4D47PtnTqR` — **LIVE and unregistered**, owning **MI-08 (Phase H, the control half)**, `IDLE`/`BLOCKED`, branch `claude/phase-h-decision-roundtrip`, carrying **three unactioned `needs_action` items**. IDLE is not dead; a handoff before this row existed would have dropped it.

**Two things my dispatch prompt told me to read do not exist at `main` `550c9f6d`:** the `⚠️_registry_completeness` note on `SESSIONS.json` and the `recurrence` block on `MI-15`. Checked both files directly. Not added — manager-owned state.

---
_Generated by [Claude Code](https://claude.ai/code)_
