🔒 **MERGE SLOT CLAIM** · session `session_014myC5S5VacHNuzzBR8dGBC` · **PR #10079** (`claude/pr-relay-close-and-ciskip`)

**Slot read:** free — no claim outstanding since my own 🔓 at 21:5xZ. **Open PRs checked:** #10068 (draft, not mine, untouched) · #10081 (my Tier-3 proposal, deliberately staying draft).

**Tier-1** — two GitHub Actions workflows plus the executed close-request records. **No `src/`, no `config/`, no unit file, no order path, no VM.** File sets disjoint from #10068.

**All four checks green** on head `bdb74a50`: `guards` · `pytest-collect` · `pytest-run` · `repo-inventory`.

Merging because leaving it open would be the exact pattern this session was sent to clear — a working fix parked in a PR. It fixes CI-stranding for **every** session that uses `pr-opener.yml`, so it is worth more on `main` than in a branch.

⚠️ `BL-20260820-PROJECT-HOOKS-INERT-ON-WEB`: the `PreToolUse` merge guard does not fire here, so a merge going through is **not** evidence the protocol ran. This claim is by hand, and so is the release.

---

🔓 **RELEASE** to follow · then ✅ **DONE** — this session is wrapping and handing off.

**Left standing for whoever picks it up, honestly labelled:**

- **#10081 (Tier-3, draft)** — the IB protective-price reconciler. Proposal only; **no code implemented**, deliberately. Needs an operator decision, then implementation with the six specified tests. Best started **fresh** — this session has run long, and the canonical rules note verification degrades late in long sessions, which is consistent with three of my four self-reported errors landing in the last stretch.
- **MES 4350 is still protected 69 ticks from its declared stop** (`ib_paper`, ~131 pts away, no urgency). Filed as `BL-20260820-OVERCOVER-REMEDIATION-CANCELLED-THE-JOURNAL-MATCHING-LEG`. **Do not fire a one-off cancel**: order 338 is on clientId **497**, the trader's own execution band, and `cancel_ib_order.py` refuses `< 9000` because connecting as it evicts the trader's live IB session.
- **`EXIT-EVAL-INTERVAL-AT-60S-REQUIREMENT` is escalated** — 114/398 intervals breach (**28.6%**), p90 **71.2 s**, max **83.7 s**, across 3 processes. It was filed as "1.1 s of margin". Re-read it before quoting the old figure.
- **`BL-20260816-IB-PROTECTIVE-STOPS-NEVER-SET-OUTSIDERTH`** — untouched, and it *interacts* with #10081: a re-arm through that path inherits whatever `outsideRth=False` means for a GTC futures stop, which this repo holds no evidence on.
- **The new detector has no scheduled caller.** `scripts/ops/broker_bracket_reconcile.py` is on `main` and runs when someone remembers to. That is the written-and-never-read shape `provenance-consumer-guard` exists for, and it is filed as an explicit criterion rather than left implied.

---
_Generated by [Claude Code](https://claude.ai/code)_
