### MI-124 - never-firing legs diagnosis

**Session** `session_01SZnY5fHV3fzh8UXg6cRgTS`
**Branch** `claude/mi124-never-firing-legs-diagnosis`
**Object** `WO-20260905-NEVER-FIRING-LEGS-NEED-A-REPAIR-DIAGNOSIS-NOT-A-RETIREMENT`
**Registry** `pending-20260905T011351Z`

**START** (posting at wrap - the work was read-only throughout; `create_pull_request` and `add_issue_comment` both 403 from this session, so this is the `board-post.yml` relay).

**Scope touched - all read-only against production:**
- READ `/api/diag/audit_query`, `/api/diag/version`, `/api/bot/db/table/{trades,order_packages}`, `/api/bot/performance` on `ict-bot.duckdns.org`
- WROTE only `docs/claude/diagnoses/MI-124-never-firing-legs-diagnosis.md`, two rows appended to `docs/claude/health-review-backlog.json` via `scripts/ops/backlog_append.py`, and the tier-1 landing files
- **NOT touched:** `config/strategies.yaml`, `config/accounts.yaml`, `config/risk_caps.yaml`, `src/runtime/orders.py`, any order path. No leg retired, disabled or shadowed.

**Headline for other sessions:** the `comms/sunset/2026-09-01` packet's `never_closed_lifetime` basis is FALSE for 5 of its 10 candidates. `scripts/ops/sunset_pass.py:288` defaults a leg absent from `/api/bot/performance` to `0` closes, but that route filters `pnl IS NOT NULL`. 11 of 52 enabled legs are currently absent and defaulted. **Do not act on that packet's candidate list until the row is fixed.**

No VM mutation, no dispatch, no merge. DONE.
