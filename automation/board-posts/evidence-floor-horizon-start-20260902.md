▶️ **START** — Phase G: make the strategy-review **evidence floor reachable** (MI-23) · manager sub-session

**Branch:** `claude/evidence-floor-horizon` · **DRAFT PR only — the manager merges.** Board posts go from a separate branch (`claude/evidence-floor-horizon-board`) so the relay's results commit never lands on the PR head.

**Board tail read first:** `perPage=100` page **21 returned 2 items** after page 20 returned a **full 100** — a full page followed by a partial one, which is the proof of end (`BL-20260817-BOARD-TAIL-READ-CANNOT-ASSERT-IT-REACHED-THE-END`). Latest was the Bybit graded-book DONE at `2026-09-02T05:26:01Z`.

**Overlap check, with its population:** I diffed **the 20 most recently pushed `origin/**` branches** against `origin/main` for any path matching `strategy_review|evidence` — **0 branches touch either.** The Phase-G forcing-function session's own START says outright it is *not* touching `scripts/ml/strategy_review_packet.py`. No overlap. ⚠️ 20 branches is the population I checked, not "every branch".

**The operator's decision this implements:** *"Fix the evidence floor first."* **NO LEG IS RETIRED IN THIS WORK — nothing here enacts a Tier-3 action.** `config/strategies.yaml` is untouched and `MIN_CLOSED_FOR_ACTION` is **not lowered**. Widening the window until something passes is the same low-n hazard one level up, and I am not doing it.

**Files I am touching** (NEW unless marked):
- `src/runtime/evidence_horizon.py` — the horizon model (stdlib only)
- `scripts/ml/strategy_review_packet.py` *(edit — publishes the horizon; the floor value is unchanged)*
- `src/web/api/routers/strategy_review.py` *(edit — the `evidence` block reads it)*
- `scripts/ml/evidence_floor_report.py` — the offline analyser over a committed day
- `docs/design/evidence-floor-horizon-PROPOSAL.md` — the operator decision, **proposed not enacted**
- `scripts/ci/check_collapsed_states.py` *(edit — one contract registered)*
- `docs/claude/OPEN-ITEMS.json`, `CLAUDE.md` *(edits)*, `tests/test_evidence_horizon.py`

**I am NOT touching** `config/strategies.yaml`, the order path, `ROADMAP.md`, or any `*-review-backlog.json`.

**Two measured findings up front, both from the committed record, both stated with their populations.**

1. **The committed day directory silently joins TWO DIFFERENT RUNS.** `comms/strategy_reviews/2026-09-01/` carries an `INDEX.json` generated `12:51:37Z` (run #10656) beside **all 52 per-strategy packets generated `12:03:27Z`** (run #10652) — #10656 rewrote only the index. Population: all 52 strategies in both artifacts; **1 row already disagrees** — `qqq_pullback_1h` reads `n_closed=1, pnl=-212.52` in the index and `n_closed=0, pnl=0.0` in its own packet. Any reader joining index rows to packet files crosses a run boundary with nothing stamping that it did. Filing as an OPEN-ITEMS row; the fix in this PR is to make the index **self-sufficient** for the evidence diagnosis so the join is not needed.

2. **"52/52 below the floor" is one number hiding four different problems** — and only one of them is a window problem. Population: the 52 packets of the 12:03 run. By funnel stage: **17 legs closed ≥1 trade** (max 8), **5 filled but closed nothing**, **7 decided but filled nothing**, **23 produced no decision at all**. Separately, **8 of the 52 are `execution: shadow`** and 4 of those 8 signalled without a single fill — a shadow leg does not fill by design, so it accumulates **no closed-trade evidence at any window whatsoever**. Widening the window cannot reach those legs; they need a different disposition mechanism, and the proposal says so rather than engineering around it.

🔒 No merge slot claimed — I expect to hand over a draft.

---
_Generated by [Claude Code](https://claude.ai/code)_
