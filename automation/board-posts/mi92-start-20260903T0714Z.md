▶️ **START — MI-92 · review #10793 on its merits**

- **branch:** `claude/mi92-operator-commands-off-trader-bot`
- **session:** https://claude.ai/code/session_01RYoQ5e8i6B4SCn9kMm2gpB
- **object:** `WO-20260903-REVIEW-10793-ON-ITS-MERITS-THE-ONE` (`pending-20260903T070814Z`)
- **tier:** Tier-2 (`src/bot/`, needs an `ict-telegram-bot.service` restart) → **prepare + validate only, landing `hold`**
- posted via `board-post.yml`: `add_issue_comment` returned a real 403 `Resource not accessible by integration` from this session.

**The live-state check came first, and the violation is NOT stale.** Read directly (`scripts/ops/diag_fetch.sh`, served by `https://ict-bot.duckdns.org`) at 2026-09-03T07:11Z:

- `/api/diag/version` → `git_sha` = `git_sha_on_disk` = `053b8d45`, `restart_pending: false`
- `/api/diag/services` → `ict-telegram-bot.service` **active**, `ict-claude-decision-bot.service` **active**
- `/api/diag/journalctl?unit=ict-telegram-bot.service` — **population: 400 lines spanning 07:03:38Z→07:11:08Z today**, containing **1 service restart (07:07:08Z)** and **exactly 2 `operator_commands` lines**, both emitted at that restart:

```
07:07:08Z WARNING src.bot.operator_commands - operator_commands: the answerable
          route (TELEGRAM_CLAUDE_BOT_SECRET) is NOT the bot this process polls
          — /status and /decisions are registered here …
07:07:08Z INFO    operator_commands: registered /status, /decisions
          (route_state=answerable_elsewhere)
```

So `/status` and `/decisions` were registered on the **trader** bot ~4 minutes before this post — against #10793's own title, *"HELD, must not reach the trader bot."* This is today's observation, not a re-quote of the 2026-09-02 audit.

**Direction — separate the feature from the wrong bot, do not rip it out.** `install_operator_commands` has exactly one caller in the repo: `telegram_query_bot.py:788`, the trader bot. The minimal fix moves that single call onto `claude_decision_bot.py` (`ict-claude-decision-bot.service` — already **active**, already polling `TELEGRAM_CLAUDE_BOT_SECRET`, already owning the `wdec` tap via the same `handle_decision_callback`). That ends the violation *and* puts the commands on the bot the operator actually asked for:

> *"that's supposed to be showing up in Cloudbot. Right? Not on the trader one, the decisions."*

It also retires the `answerable_elsewhere` warning by construction — on ClaudeBot the answerable route **is** the polled bot.

Draft PR to follow, landing `hold`. **No merge from this session.**
