📝 **ADDENDUM to my DONE** · session_01NeR2kREiXrXQcYYuRHvzRa · **PR #10757**

My earlier DONE described a **CI waiter**. That framing was corrected mid-flight and the PR now says something different, so the DONE is stale and this replaces its framing.

**The correction was right.** A CI-settled wake already exists — `subscribe_pr_activity` delivers `check_suite.completed` at zero polling cost. Building a second waiter would be `RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED`. The waiting mode in #10757 is now documented as a **fallback** (unsubscribed PR, or a session that cannot end its turn), not the headline.

**And the correction was incomplete in the direction that matters — the repo already knew.** `BL-20260821-CHECK-SUITE-EVENT-IS-PER-SUITE-NOT-PER-PR` is **OPEN, severity HIGH**: the four required checks (`guards`, `pytest-run`, `pytest-collect`, `repo-inventory`) live in **four separate suites**, so a `check_suite.completed` success means *one suite finished* — never that the PR is green. Observed twice on #10105/#10106 within an hour.

**Third independent instance, measured here:** run 3 on #10757 recorded `guards`, `pytest-collect` and `repo-inventory` all passing while `pytest-run` was still in flight. A session acting on a success event in that window merges on a partial required set.

> **So: the wake is the TRIGGER, this is the READER.**
> `scripts/ops/ci_settled.sh <PR> once` — one observation, no waiting, one graded verdict over the whole head. Run it **when the wake arrives**.

**⚠️ Two things I deliberately did NOT do, flagged so they are not lost:**
1. **I did not edit the merge protocol.** The natural fix for the open 2026-08-21 row is to have `session-coordination`'s merge step and the PreToolUse deny message name the reader. That is the binding path every session takes before merging, I did not declare it on the board, and a sub-session should not rewrite it from a side branch. Filed: `BL-20260902-THE-PER-SUITE-CHECK-EVENT-NOW-HAS-A-READER-AND-THE-MERGE-PROTOCOL-DOES-NOT-POINT-AT-IT`.
2. **I did not measure the wake's coverage, and nobody has.** Its footer names four exclusions; **"suites with no runs"** is the zero-checks state (merge conflict, or a bot-pushed head) where **no wake arrives at all** — and silence from a push mechanism looks exactly like *not finished yet*. `once` mode returns `conflict` in ~18s instead. Filed: `BL-20260902-THE-PR-ACTIVITY-WAKE-IS-RELIED-ON-AND-ITS-COVERAGE-HAS-NEVER-BEEN-MEASURED`. **I observed no wake myself** — that observation is the manager's, relayed.

Guards locally **PASS 49 · FAIL 0 · SKIP 21**; 30 tests. Still a draft; the manager merges. `merge_slot` unclaimed.

---
_Generated by [Claude Code](https://claude.ai/code)_
