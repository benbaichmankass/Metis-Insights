{
  "tier": 3,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "Tier-3 mandate text; operator approves the merge in the manager session (session_01Ljhs6sFAdWHdMDhJpL5aBP). This lane opened the PR and does NOT merge it, arm auto-merge or deploy. NOTE ON THIS FIELD: the operator's dispatch asked for that sentence as `hold_reason`; `hold_reason` is a CLOSED vocabulary in scripts/ci/check_pr_landing.py::HOLD_REASONS (R7), so the typed value is `tier_2_3_needs_approval` -- \"Tier-2/Tier-3 work; say what approval is being sought and from whom\" -- and the requested sentence is carried verbatim here, where the prose belongs. Nothing was widened. WHAT THE APPROVER IS APPROVING: replacement text for MD-PROMOTE-S1-S2's `blocked_until`, decided by the operator 2026-09-25 (\"Adopt text + fix the 24\"). The mandate STAYS BLOCKED -- `blocked_until` remains truthy and scripts/ops/mandate_resolver.py still returns REFUSE / R-MANDATE-BLOCKED (verified on this branch, exit 1). No roster, no account mode, no risk cap and no execution gate is touched, and no mandate is armed, granted or widened by this diff.",
  "why": "One field plus its explanatory comment block in config/mandates.yaml: MD-PROMOTE-S1-S2's `blocked_until` stops naming checklist row D1 (which reads `dropped`, killed 2026-09-22 as a duplicate of R1, so the block as written could never clear) and instead names two CHECKABLE artifacts -- pipeline item PI-20260924-JCVFRWEA-0001's `state`, and checklist row E62's own done-when observation. Text taken verbatim from docs/research/d1-cost-stack-verification-2026-09-25.md section 4 (merged #12928), with its `TODAY:` count corrected from 24-of-27 to the read-time measurement of 22-of-27 (E39 / #12919 regenerated two more perp records after the memo's stated population). Verified by a YAML load of both trees that MD-PROMOTE-S1-S2.blocked_until is the ONLY semantic change in the file: every other mandate field, the mandate order, and `proposed: []` are byte-identical. Tier-3 declared by merge gate -- config/mandates.yaml is config/** (TIER2_PATHS) by path, but this is standing-authorization text for the mandate that routes REAL MONEY, and CLAUDE.md is explicit that clearing or editing an arming switch is the operator's act, never a lane's. The stricter tier wins."
}
