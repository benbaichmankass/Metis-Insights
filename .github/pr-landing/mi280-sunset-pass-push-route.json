{
  "tier": 1,
  "landing": "self",
  "why": "MI-280 unit U8, taken as the next open high-severity health-backlog row touching .github/workflows/**: BL-20260911-SUNSET-PASS-PUSHES-STRAIGHT-AT-PROTECTED-MAIN-SO-ITS-ONLY-SCHEDULED-RUN-COULD-NOT-LAND-AND-IT-WILL-FAIL-IDENTICALLY-EVERY-MONDAY. Named on the coordination board BEFORE any file was touched. Tier-1: .github/workflows/**, tests/** and docs/ only — no src/, no config/, no deploy/, no order path. R12 does not apply; checked by importing check_pr_landing.py and printing all 11 LANDING_MACHINERY entries, since this session published a false absence from a truncated grep earlier today. THE DEFECT, VERIFIED STILL LIVE ON MAIN BEFORE TAKING IT: sunset-pass.yml line 108 was `git push origin HEAD:${GITHUB_REF_NAME}` — a direct push at protected main. Its ONE scheduled run (34110131942, event=schedule, cron `10 5 * * 1`, 5.0h late) died with `! [remote rejected] HEAD -> main (protected branch hook declined)` twice. The four-attempt retry loop could not help: every attempt hits the same hook. Structural, so it recurs every Monday. PORTED to .github/actions/commit-to-main, the remedy session-reaper.yml and replay-pregate-nightly.yml both already took. I CHECKED THE THREE PORTING TRAPS FIRST, because the replay-pregate row warns in terms that porting blind 'trades one loss for another'. (1) CLEAR: comms/sunset/ is not gitignored, so the `git add` without -f that bit MI-279 on runtime_logs/ does not apply. (2) REAL: the workflow checked out with a bare actions/checkout@v4, and commit-to-main's own line 12 states the PAT on the CHECKOUT as a requirement — its pushes are a bare `git push origin`, so under GITHUB_TOKEN the landing PR never gets the required checks and auto-merge waits forever. A port without that would have swapped a loud weekly failure for a silent permanent one. (3) REAL, and it bounds what I claim: BL-20260911-THE-SHARED-COMMIT-TO-MAIN-LANDING-STEP-IS-THE-LARGEST-SINGLE-CAUSE-OF-DISCARDED-AUTOMATION-OUTPUT measured all 27 scheduled workflows and found the shared landing step, not the cron, is what loses work. So this moves sunset-pass from NEVER LANDS onto a path that SOMETIMES lands. That is an improvement and NOT a fix of the landing layer, and the workflow says so at the step. It is acceptable HERE because the sunset artifact is RE-DERIVABLE — computed from current config plus lifetime data, so a missed Monday costs a recomputation, not evidence — and would NOT be acceptable for a forward-only stream like replay-pregate's. !! I GOT verify-merged WRONG FIRST AND AN EXISTING TEST REFUSED IT. My initial port left it at the default, arguing a weekly artifact does not justify a 30-minute merge poll. test_every_caller_verifies_its_rows_merged failed, and it is right: without it the step exits 0 when the PR OPENS, so a push that never merges reads as SUCCESS — the green-over-nothing failure this very row exists to end. Field beats comment; the test is the rule. Corrected to true, with timeout-minutes: 45 sized as work + the action's 30m wait + slack rather than merely clearing the floor. AND THE PORT FOUND TWO LIVE DEFECTS IN OTHER WORKFLOWS. The suite did NOT catch the missing checkout PAT — every other invariant in it is about the `uses:` step and this one is about a step three above — so I added test_every_caller_checks_out_with_a_pat, and it failed immediately on POPULATION (n=27) 2 of 27: e35-bracket-sweep.yml:corpus and strategy-review-packets.yml:packets. BOTH already passed the PAT to the ACTION, which authenticates gh only and reads at a glance as if handled. Both fixed here; filed as BL-20260912-TWO-OF-27-COMMIT-TO-MAIN-CALLERS-CHECK-OUT-WITH-THE-DEFAULT-TOKEN-SO-THEIR-LANDING-PR-NEVER-GETS-CHECKS. !! ONE NEAR-MISS WORTH THE WARNING: my first fix to e35-bracket-sweep appended a SECOND `with:` key to a step that already had one. yaml.safe_load ACCEPTED it — duplicate keys, last wins — so the edit validated clean while the token was silently discarded, and only the test caught it. Validate a YAML edit by READING BACK the key you meant to set, never by the file merely parsing. VERIFICATION: 11 pytest cases pass (9 pre-existing unmodified, 2 added); the new test goes RED on a planted defect and quiet on restore; both new tests refuse an EMPTY POPULATION rather than passing over nothing. !! THIS DOES NOT CLEAR THE ROW AND I DO NOT CLAIM IT DOES. Its criterion is a SCHEDULED run completing with its artifact on origin/main, and the cron is weekly — Mondays — so the next opportunity is 2026-09-14. Nothing is observable today. The row stays open with the port recorded as addressed-but-unproven. !! ALSO FLAGGED, NOT FIXED: the row notes the CLAUDE.md SESSION BRIEF advertises '10 retirement candidate(s)' from a pass whose only scheduled run never landed. That block is GENERATED, so hand-editing it is what doc-freshness forbids; it self-corrects once a pass lands. Raised on the board so the operator knows the brief is presenting stale candidates as current until then."
}
