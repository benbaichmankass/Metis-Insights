{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, and NO CODE PATH IS ALTERED: two docstrings, one new test file, one backlog row closed. The whole deliverable is a DECISION recorded in prose plus tests that pin it. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps. NOT IN LANDING_MACHINERY, checked rather than assumed: the files are scripts/ops/check_backlog_refs.py and scripts/ops/check_backlog_criteria.py, neither of which is landing machinery (that is check_pr_landing.py and claim_merge_slot.py), so R12 does not apply. WHAT IT DECIDES: check_backlog_refs.py reads its base at the TIP and is the last of the five tip-readers the base-vs-tip census counts to be graded either way — the row's measured gap was that the string 'check_backlog_refs' appeared ZERO times in the per-script audit. Classified RIGHT BY DESIGN: _refs_anywhere_at is a fallback for a path ABSENT at the base and asks 'is this id cited anywhere in the tree I am merging INTO?', and the tree being merged into IS the tip. MEASURED on origin/main: 1,856 ids cited at the tip, 86 dangling, and of the ids cited ONLY in the last 6/20/60 commits — the entire population where tip and fork point can disagree — ZERO are dangling at every window, so the choice has changed no finding. THE DIRECTION IS THE ARGUMENT: a fork-point read would blame a diff for a dangling id a CONCURRENT branch introduced, which is false blame and is the class that red every PR in this repo on 2026-09-16/17. The accepted reverse error is STATED in the docstring, not hidden, and routes to BL-20260730-CITED-BUT-UNFILED-BACKLOG-IDS. The row's own criteria say an honest negative CLOSES it and needs no code change, which is why this lands as a closure rather than a fix. CONTROLS: 8 tests, four planted defects each verified RED — including the silent swap to a merge base, which the tests refuse by name so a decision reversal cannot pass as a tidy-up. The tests deliberately do NOT assert the tip is correct; they assert the decision was MADE and is REACHABLE from the file a reader has open, which is the row's criterion."
}
