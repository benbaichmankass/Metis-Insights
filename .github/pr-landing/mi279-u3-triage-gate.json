{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain U3 — a BUILD plus a closure. Four files: scripts/reports/render_system_report.py (a REPORT RENDERER; nothing in src/ imports it and it cannot affect the trader), comms/schema/system_report_response.template.json (the report schema authors fill in), tests/test_review_coverage_triage_gate.py (NEW, 7 deterministic cases, no network), and docs/claude/health-review-backlog.json (one row open -> resolved, one new row filed, both via backlog_append). No src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHAT IT CLOSES: BL-20260814-STRICT-GUARD-DOES-NOT-ENFORCE-THE-TRIAGE-GATE, open since 2026-08-14. The /system-review skill calls the backlog drive a HARD COMPLETION GATE and names --strict as the mechanical backstop, and --strict passed a report declaring count_untriaged of 201/39/26 because it never read the field. Both of the row's criteria are built: the gate fails a declared shortfall naming the domain, and --allow-partial-triage is an explicit opt-out that stamps a PARTIAL REVIEW banner into the rendered HTML. The banner rides the PAYLOAD rather than a render flag so every caller of write_report() prints it, and it precedes the <h1> so it cannot be scrolled past. Mutation-checked: 4 of 7 new tests fail with the renderer change stashed, 7 pass restored, and the 3 that pass either way are the controls. The three sibling review-coverage suites still pass, 52 tests. THE ROOT CAUSE WAS ONE LEVEL DOWN AND IS FIXED IN THE SAME CHANGE: count_untriaged was named four times in the skill and ZERO times in the template, so --strict could hardly check a field the schema never asked for; the three counters are added to the template. AND ONE HALF IS DELIBERATELY NOT BUILT AND IS FILED RATHER THAN HIDDEN: an ABSENT count still passes, because every report rendered from the old template omits it and failing on absence would have redded every review run on day one — the same argument CLAUDE.md records for the diagnostic-provenance guard. That gap is BL-20260912-THE-TRIAGE-GATE-CANNOT-TELL-AN-UNSTATED-COUNT-FROM-A-ZERO-ONE-UNTIL-REPORTS-CARRY-THE-FIELD and is PINNED by a test so it cannot close by accident."
}
