{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U28, RESEARCH lane. Tier-1 throughout: one new module under scripts/research/**, one memo under docs/research/**, one hand-inserted generated stub row in docs/DOCUMENT-INDEX.md, one row in the hand-maintained RESEARCH-CAPABILITY-INDEX, and two backlog edits. NOTHING under src/, config/, deploy/, tests/ or .github/workflows/; no order path, no strategy or risk parameter, no unit file. The module makes no network call of its own -- it reads committed artifacts plus one soak file passed on the command line -- and is marked `wiring: manual-only` because it grades a CLOSED question against DATED artifacts. IT CLOSES A ROW, AND THE AUTHORITY FOR THAT IS THE ROW ITSELF: BL-20260831-TRADE-PRIORITISATION-IS-UNPROVEN-CONFIDENCE-IS-A-START-NOT-A-RESULT says '\u26a0\ufe0f A NULL RESULT CLOSES THIS ROW -- it is an answer, not a failure to be re-run until something wins', and the study it demands ran on 2026-09-08 (docs/research/trade-prioritisation-research-DESIGN.yaml, status: answered). All four criteria were graded against the artifacts one at a time, and the one deviation is recorded as `met_by_another_route` rather than smoothed to `met`. Re-running the study would have been RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED. IT PROPOSES NO FIX for the saturation row it advances: that row forbids proposing before attributing, criterion (2) is unpaid, and re-scoring confidence is Tier-3 because it is the primary live routing key. Only criterion (1), the attribution, is paid here, plus criterion (4)'s re-measure. Verified before pushing: --self-test asserts 22 controls with 0 failures, and seven defects were planted against the new code, all seven caught. Every headline carries its population, the published 0.501 baseline is reproduced as a positive control at 0.5135 and the 1.1pp gap is reported rather than smoothed, and the within-arm stratified read is flagged in the module's own output as a composition note and never as an effect."
}
