{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5. Registers only: one appended `observation` plus `verified_at` on OI-20260909-THE-ROLLBACK-REGISTRY-IS-COLLAPSED-AND-THE-ROLLBACK-HAS-NEVER-BEEN-EXERCISED (whose verified_at was `never`), and the session-brief re-render a register change obliges. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY THE ROW STAYS OPEN: its clears_when needs either a tick OBSERVED under MULTI_STRATEGY_INTENT_LAYER=false -- a Tier-2 act this lane holds no approval for and explicitly does not propose -- or a recorded operator decision to retire the rollback. Nothing here is either. WHAT IS ADDED IS TWO PRE-EMPTED FALSE ALARMS, both of which I walked into: (1) the row's own parity test reports `1 skipped` rc=5 in this container because the whole module sits behind pytest.importorskip('pandas'), which is a SANDBOX artifact -- pytest-run.yml installs requirements.txt and that pins pandas>=2.0.0, so it runs in CI; I nearly filed 'the safety evidence never executes'. (2) the 45-vs-44 discrepancy between this row and CLAUDE.md is DEFINITIONAL -- measured, 45 carry execution:live and 44 of those are also enabled, the difference being xauusd_trend_1h -- and the test filters on execution ONLY, so its 45 is a deliberate superset rather than a stale denominator. Both are facts that would otherwise cost the next session a false finding."
}
