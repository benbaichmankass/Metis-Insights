{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U51, RESEARCH lane. Tier-1 by R5's surface test: an extension to an existing instrument under scripts/research/, one new file under tests/, one new memo under docs/research/, one additive backlog `updates` entry, one DOCUMENT-INDEX row, and one in-place correction to a RESEARCH-CAPABILITY-INDEX claim this change proves false. NO src/, NO config/, NO deploy/, NO workflow, no order path, no journal pull and no network -- every number is computed from files in this repo. || NO NEW INSTRUMENT: the fix belongs in scripts/research/memo_input_provenance.py, which is the module whose promise was false; a second one grading the same question would be RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED. || WHAT IT DELIVERS. The row BL-20260912-U15S-OWN-CODE-NO-LONGER-REPRODUCES-U15S-OWN-PUBLISHED-TABLE-ONE-CONTROL-PACKAGE-FLIPPED-AND-NOBODY-WOULD-HAVE-KNOWN has two exits; the first (attribute the historical 30-vs-31) is closed because U15's pull is gone, and the second is 'memos gain a retained/hashed input SO THE NEXT SUCH DIFFERENCE IS ATTRIBUTABLE'. U26 shipped the hashed input. MEASURED, it could not attribute, and it printed a confident wrong answer instead: render_stanza emits no per-row digests, so compare() derived rows_changed/added/removed from a map a parsed stanza never carries. Against a live pull that reported the ENTIRE population as rows_added; against another memo -- measured on the REAL U32/U38 pair, same ids 5731->4732, order_digest identical, rowset_digest different, i.e. a genuine re-stamp -- it reported all three lists as [] beside rows_not_localisable: 0. The two shapes point at OPPOSITE causes, so the module was mis-assigning between the three causes it exists to separate. || WHY THE HARNESS WAS GREEN, stated rather than glossed: all 64 controls compared two LIVE fingerprint dicts, which always carry row_digests, and the single control that reached the stanza path checked the IDENTITY case where the lists are empty either way. A near-miss, not an absence. || THE FIX IS TWO HALVES. (1) STOP ANSWERING: a four-state `localisation` verdict, never collapsed, with None -- never [] and never a derived list -- for the three row lists and for rows_not_localisable; the two one-sided states stay apart because the remedy differs. reproduces and reordered still report [] and MUST, because there the empty lists are entailed by rowset_digest equality; `rows_basis` records which basis produced an answer, and refusing one the digests already give is a planted defect that is caught. (2) MAKE IT POSSIBLE: render_stanza(include_row_digests=True), opt-in, with the prefix length DECLARED in the stanza and the observed side trimmed to it. row_digests is deliberately NOT added to REQUIRED_STANZA_KEYS -- requiring it would grade all five existing declarations incomplete and un-declare the only adoption there is. || ADOPTION MEASURED WITH ITS DENOMINATOR: 357 memos in docs/research, 5 declare an input, 73 cite a journal and declare nothing, 279 not detected -- and not_detected is NOT a clean negative, it is a five-token prose match. 0 of the 5 can name a changed row. || THE ROW IS NOT CLOSED AND NOTHING CLAIMS IT IS: the historical 30-vs-31 stays unattributable, and the five existing memos cannot be made attributable retroactively -- re-rendering them would fingerprint TODAY's journal under an old heading, which is worse than an honest neither_side_carries_row_digests. || VALIDATED BEFORE PUSH on committed state: 80 self-test controls (up from 64), 19 pytest controls, 12 planted defects ALL caught by a named control in BOTH suites, ruff clean, and the full guard set run on committed state."
}
