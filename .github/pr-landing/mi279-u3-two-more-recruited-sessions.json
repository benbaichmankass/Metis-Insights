{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain U3. Docs only: ONE appended `detail` on the existing open row BL-20260912-A-LANDED-FIX-LEAVES-ITS-BACKLOG-ROW-OPEN-SO-THE-ROW-RECRUITS-THE-NEXT-SESSION-INTO-REBUILDING-IT, via backlog_append.update_row (byte-local, status unchanged at `open`, row count unchanged). No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY AN APPEND AND NOT A NEW ROW: the duplicate probe returned this row for both instances, and it already carries the diagnosis and the criteria; what it lacked is more members. WHAT IS ADDED: instance (3), a TRUE member and the cleanest yet — BL-20260823-BACKLOG-TRIAGE-SEES-47-PERCENT-OF-THE-LIVE-ROWS had ALL FOUR of its criteria landed, in code whose own docstring names that row as its reason, and the row was still open with no mention of any of it; I read it, concluded it was an unbuilt Tier-1 build, and began scoping that build, stopping only on an ad-hoc `ls scripts/ops/ | grep backlog` existence check. And instance (4), deliberately filed as a NAMED VARIANT rather than a member: BL-20260822-DIAG-FETCH-404-BLAMES-PATH-FORM-NOT-WRONG-SURFACE had NO fix landed, so this row's fixed-in-fact/open-on-paper class does not cover it — what recurred was the DIAGNOSIS, re-derived from scratch 21 days later and caught only by a duplicate probe returning the row at 0.85 overlap. The variant's remedy is different (a probe at INVESTIGATION time, where backlog_append already covers FILING time), which is exactly why pooling them would blur the criteria. THE ROW STAYS OPEN and its criteria are untouched; the append strengthens the 'NOT satisfied by a memo' clause with the observation that all four instances were saved by an ad-hoc check the session happened to run — four for four on luck, across two sessions."
}
