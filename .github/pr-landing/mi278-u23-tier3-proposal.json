{
  "tier": 1,
  "why": "A Tier-1 DOCUMENT that PROPOSES a Tier-3 decision — the two are different and only the document ships here. Paths: one new scripts/research/ audit, one new docs/research/ memo, one row in each of two docs/ registers, one new docs/claude/ backlog row, and one progress entry on the docs/claude/work/ object. All inside TIER1_SURFACE. No config/, no src/, no order path, no strategy parameter, nothing enacted on the fleet — and the proposal's own content is to change NO parameter. It reaches the operator with a refusal plus two named research arms, which is a thing they can accept or reject, and it states plainly that it does NOT satisfy the object's done_condition (which wants a lever with IS/OOS + walk-forward behind it, where the finding is that the walk-forward cannot support the lever), so the object stays in_flight rather than being closed on this. It also withdraws a claim this session repeated in several board posts and PR bodies. Verified on committed state: the audit's 19 self-test controls pass and the harness was shown able to FAIL against four separately planted defects, including a positive control proving the refusal is a property of the data rather than of a probe that can only say no.",
  "landing": "self"
}
