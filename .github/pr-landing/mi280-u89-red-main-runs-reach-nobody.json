{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 developer tooling: scripts/ops/render_due_list.py, one new test file, one register row closed. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps. R12 does not apply -- checked against LANDING_MACHINERY rather than assumed: render_due_list.py is not in that list. WHAT IT DOES: closes BL-20260913-A-DUPLICATE-ROW-ID-REACHED-MAIN-AND-TURNED-REGISTER-ID-GUARD-RED-FOR-EVERY-BACKLOG-TOUCHING-PR-IN-THE-REPO, criterion 1 by CORRECTING its premise and criterion 2 by measurement; the row says those are opposite findings and to say which, and both are stated. CRITERION 2 -- MACHINERY EXONERATED, the path is a HAND RESOLUTION: the register merge driver invoked directly on a both-sides-add-the-same-id case returns 7 rows and ZERO duplicates; its contract REFUSES a divergent same-id add; unarmed, git's text merge CONFLICTS rather than duplicating. Two of my first three probes were VACUOUS -- git's ordinary text merge resolved them and the driver never ran. CRITERION 1 -- THE CADENCE ALREADY EXISTED and I did not build a 29th cron: guards.yml carries push: branches:[main] (1370 runs) and register-id-guard is when:None. MEASURED that main at 2a6c1b24d carried the duplicate and check_register_ids.py exits 1 there (R1 needs no --base), so the branch was RED-CAPABLE. THE REAL GAP is that src_red_crons queries ?event=schedule only -- its own docstring says 'a nightly nobody is waiting on' -- so a failed push-on-main run was read by NO source. Adds src_red_main_runs over event=push&branch=main, latest COMPLETED run per workflow, real failures only, loud on DUE.md. IT EXCLUDES `cancelled` WITH A MEASURED BASIS (3 of the 15 most recent main runs concluded cancelled; the push concurrency group is identical for every commit with cancel-in-progress), and that is safe because main is LINEAR so a cancelled run is a delay, not lost coverage. IT DOES NOT MAKE DETECTION FASTER and does not claim to -- DUE.md renders daily while a PR author trips over a duplicate in minutes. What changes is ATTRIBUTION, which is the cost the row was filed about. NOT OBSERVED: historical red push-on-main runs were not enumerated, so 'that run went red rather than being cancelled' is inferred; the routing gap is structural and holds either way. CONTROLS: 17 tests and six planted defects each observed reddening its own -- including an in-progress run MASKING an older failure, which my first battery missed entirely (all 16 stayed green on that plant because a null conclusion is already absorbed). diagnostic-provenance-guard separately caught an unmarked inert `today` parameter and was right. Row integrity: ids 1586 -> 1586, 0 lost, 0 added, exactly 1 row content-changed, 0 duplicate ids."
}
