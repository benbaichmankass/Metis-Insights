{
  "tier": 1,
  "landing": "self",
  "why": "MI-280. Resolves BL-20260909-R13-FORCES-EVERY-ARMING-BRANCH-ONTO-ONE-SINGLE-HOLDER-KEY-SO-CONCURRENT-PRS-DIRTY-EACH-OTHER-BY-CONSTRUCTION by discharging its clause (2). Tier-1, ONE file: docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no code, no workflows. THE CRITERION IS AN OBSERVATION, NOT A TEST, and it was met as an observation: 'shown by ARMING TWO BRANCHES CONCURRENTLY and observing no conflict, not by a green unit test'. Thirteen were armed concurrently. MEASURED with the same script and method as the before-reading so the two are comparable. POPULATION: every open claude/mi280-* branch merged against origin/main in a scratch worktree, merge.jsonregister armed. BEFORE n=17, 10 conflicted, 10 of 10 on session-board.json. AFTER n=20, 2 conflicted and neither is an armed per-branch claimant; all 13 armed per-branch claimants merge clean. THE CONTROL IS INSIDE THE SAME CENSUS and is what makes this more than a quiet week: mi280-constraint-readout-cause still carries the SHARED field and still conflicts, same run, same main. The old mechanism fails beside the new one. The second conflict is on claim_merge_slot.py, an ordinary content conflict with the file #11891 changed, not a claim collision. WHAT IT DOES NOT SHOW IS STATED IN THE ROW AND THE PR: it observes merges against main, which is the criterion; it does not show two branches LANDING back-to-back without a base merge, which is ordinary and is not this row's defect. THE EDIT IS INSERTION-ONLY APART FROM THE DELIBERATE STATUS FLIP, verified rather than asserted: the serialisation was reproduced byte-identically before writing, so the other 1494 rows cannot reformat, and the item count is 1495 before and after."
}
