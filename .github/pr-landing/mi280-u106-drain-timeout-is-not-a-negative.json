{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 tooling: one exception handler in scripts/ops/backlog_drain_candidates.py, one new test file, one row appended to docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no workflows. NOT in LANDING_MACHINERY, so R12 does not apply. WHAT IT FIXES: `_added_after` has THREE values and the call site keeps `[s for s in signals if s['ok'] is not None]`, so None is dropped as undecidable while False COUNTS as a decided 'nothing shipped'. The function's _SHALLOW branch and its when.ok branch both return None BECAUSE a definite negative nobody established is worse than an admitted gap -- and then the `except Exception` handler, for the STRICTLY WORSE case of git not answering at all (a 25s timeout, git missing, the subprocess dying), returned False. A timeout did not make a row unshortlistable; it silently ARGUED AGAINST it, and a narrowed candidate list reads identically to a clean one. One line. [!] THE NON-EXCEPTION PATH WAS ALREADY SAFE and is deliberately untouched: a non-zero git exit that does not raise leaves stdout empty and _as_day('') already yields None. CONTROLS: 9 new tests, four planted defects, all four caught. The load-bearing one is the OVER-WIDENING plant -- making every answer None passes a None-only suite while making the drain tool shortlist nothing and still look healthy, the opposite failure -- so `False` is asserted REACHABLE by a test where git answers with a date older than the row. The fixture forces _SHALLOW = False because on a shallow clone (what CI and every sandbox session actually has) the function returns None before any git call and every assertion would be green without the subprocess being reached. POPULATION STATED HONESTLY: an AST sweep returns 11 OR 12 shell-out functions lacking a returncode check depending on whether a nested helper is counted separately, out of ~109, so no single raw number is quoted as a defect count. The stable classification is 7 self-test scaffolding, _is_shallow (try/except returns the safe value), recombination_sweep._run (callers grade it twice), run_guards.run_guard (a display-only diff), check_backlog_refs._git (PR #12379), and THIS one -- the single genuine residual. A negative result, not a sweep. VERIFIED ON COMMITTED STATE: full guard set PASS 85 / FAIL 1 (pr-landing only, which this record is the second push for), register-id-guard OK, artifact-validity EXIT 0, backlog round-trip byte-for-byte on all four registers, ruff clean, 15 tests green after merging current main. [!] ONE CORRECTION MADE BEFORE PUSHING, recorded rather than hidden: the code comment first cited U105's backlog id, which #12379 files and main does not carry until it lands, so this file introduced a reference resolving to nothing and artifact-validity-guard failed. Same cross-PR ordering trap that held #12375 and #12376 red, hit a third time today. It now cites PR #12379 by number, which cannot dangle."
}