{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U49, RESEARCH lane. Tier-1 by R5's surface test: one new memo under docs/research/, one additive backlog `updates` entry through the row-aware driver, two progress entries and one decision_request on a work object under docs/claude/work/, and one DOCUMENT-INDEX row. NO new script, NO src/, NO config/, NO deploy/, NO workflow, no order path. src/utils/json_notes.py and src/runtime/order_monitor.py were READ and are quoted; neither is modified. || NO NEW INSTRUMENT WAS WRITTEN, DELIBERATELY. The row it works names scripts/research/netting_position_size_basis.py --soak as its reference implementation, and that script already grades exactly this with a four-state two-source vocabulary. Writing a second one would be RC-BUILT-A-MECHANISM-THAT-ALREADY-EXISTED. This unit is a USE, which is what the row's clause (1) asks for. || WHAT IT DELIVERS: clause (1) cleared by applying the corrected scoping rule -- notes stamp AND soak, never exit_reason alone -- to MI-278 U47's netting finding, the lane's immediately preceding unit. The audit CONFIRMS U47 rather than overturning it: on bybit_2 n=33 the stamp sees 4, the soak adds zero, soak-only is zero, so U47's `contributes` verdict stands. That is reported as a confirming result and explicitly NOT as evidence the defect is absent -- all five shed rows are bybit_1 and bybit_portfolio. || FLEET CENSUS on the same pull (783 bybit rows graded, 217 off-venue): exit_reason sees 42, the stamp census is 44, and the soak reveals 5 more whose stamp was shed entirely -- 7 invisible in total against the 2 a journal-only audit sees, a 3.5x under-count, with 104 further rows grading `evidence_may_be_shed`, which is we-could-not-look rather than a clean negative. Trade 5744 is new relative to the row's 2026-09-13 reading and is recorded as a WINDOW effect, not growth: that reading covered ids 5731-4732 and 5744 did not exist inside it. The row's own warning that a changed count is not progress is honoured. || CLAUSE (2) IS TIER-2 AND WAS NOT APPLIED. It is put to the operator as DEC-20260917-NETTING-ATTRIBUTION-EVIDENCE-IS-DROPPABLE with three real options, and the argument for the recommended one is json_notes.py's OWN comment ('EVERY PROVENANCE MARKER BELONGS HERE') applied to a key that meets its stated criterion verbatim -- a fixed vocabulary of leg_gone/fifo compared for equality. The tuple's comments already record three prior instances of this class; this is the fourth. || VALIDATED BEFORE PUSH on committed state: the reference implementation's own self-test and pytest suite re-run green, ruff clean, and the guard set run on committed state."
}
