{
  "tier": 1,
  "landing": "self",
  "why": "Manager pass, PR #11440. Tier-1 by the canonical doc's own list and by path: FIVE register files, every one under docs/claude/work/** (MANAGER-LEASE, SESSIONS, MANAGER-CHECKLIST, MERGE-QUEUE, and one work-object YAML), plus the two landing-route files for this branch. NO src/, NO tests/, NO scripts/, NO .github/workflows/, NO config/, NO deploy/, no order path, no strategy config, no risk caps, no live-VM runtime file. Nothing promoted, demoted, retired or re-parameterised. check_manager_scope.py --base origin/main reads `clean` with R6/R7/R8 ok and 1 commit graded, all inside the management surface — run in its own command after committing, never chained to the thing it gates. THE ONE JUDGEMENT CALL, stated rather than buried: the work-object write adds a structured `answer` block to three decision_requests, transcribing an operator quote ALREADY IN THAT FILE's review_trigger. That is 'recording an operator decision into docs/claude/work/**', which check_manager_scope names as management rather than an item — it decides nothing new. It repairs three instances and resolves nothing about the class, and the backlog row for the class stays open."
}
