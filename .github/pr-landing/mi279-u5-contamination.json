{
  "tier": 1,
  "landing": "self",
  "why": "U5 of MI-279: two DUE monitoring rows checked and recorded as CONTAMINATED rather than passed. Two files, both Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs/claude/OPEN-ITEMS.json and the GENERATED SESSION-BRIEF block in CLAUDE.md, re-rendered with render_session_brief.py --write. No src/, no config/, no deploy/, no workflow, no scripts, no order path, no VM mutation. R12 does not apply; neither file is in LANDING_MACHINERY. NEITHER ROW CLEARS and neither is claimed to -- both exist to test whether a session genuinely read a mechanism, and both say a session pointed there by its prompt cannot clear them. My dispatch named docs/claude/OPEN-ITEMS.json by path and set a unit reading 'U5 - monitoring rows that are DUE; observe and write what you saw', so I learned the due-row duty from the prompt and not from the brief; and it named the backlog-drain skill outright, so I cannot be the clean role-pack observation either. What is recorded beyond the contamination is a STRUCTURAL reason the session-brief row has stayed open since 2026-08-26: an ops or backlog dispatch naturally names the register and the duty, so the population that could clear it is 'sessions dispatched for something else that nonetheless report a due monitoring item', which is small by construction -- with a cheap remedy named (dispatch one lane without naming the register) and explicitly left as a manager's call rather than proposed. The role-pack row records that the pack was read and CHANGED what the session did, with two concrete instances, while stating that the specific thing that row tests -- learning about the work store, cycle priority or WIP ceiling from the pack -- did not happen. It also records, for contrast, that the brief WAS read and acted on for a different row (#11920) and warns that #11920 must not be cited as clearing the session-brief row."
}
