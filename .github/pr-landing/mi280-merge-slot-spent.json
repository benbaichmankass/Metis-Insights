{
  "tier": 1,
  "landing": "self",
  "why": "MI-280 unit U5. Adds scripts/ops/merge_slot_state.py and ~50 lines in scripts/ops/claim_merge_slot.py so the tool every claimant already runs GRADES the incumbent merge-slot claim before overwriting it, plus a test file and three backlog dispositions. Tier-1 by the canonical doc's own list: scripts/ops/**, tests/** and docs/ only - no src/, no config/, no deploy/, no order path, no strategy config, no risk cap, no live-VM runtime file. R12 DOES NOT APPLY AND THAT WAS CHECKED RATHER THAN ASSUMED: check_pr_landing.py::LANDING_MACHINERY lists scripts/ci/check_pr_landing.py, scripts/ci/check_automerge_trigger.py, scripts/ops/session_registry.py, the three relay workflows, .github/pr-landing/*.json and the commit-to-main action - claim_merge_slot.py is NOT among them, and the new module is a READER that writes nothing and can neither open, arm, nor merge a pull request. THE DEFECT: R13 refuses to let a branch arm without a claim, so the claim half is a mechanism, while the release half is prose carried by nothing - measured over the last 25 commits touching session-board.json, ZERO ever wrote a cleared slot - and commit-to-main rewrites the claim on every automated commit across 27 workflows, so the field goes stale mechanically at the rate main moves. THE FIX IS DERIVED, NEVER WRITTEN: four never-collapsed states (unclaimed / spent / live / undecidable) computed from merge_slot.branch, which every claim already carries, so nothing new is written at claim time and it works on every claim already in the history. OBSERVED LIVE at 2026-09-12T06:2xZ on the real slot: SPENT, branch gone from origin, Displace freely - on its first run. THE LOAD-BEARING REFUSAL is that branch PRESENCE is not liveness: measured the same minute, 4 of 4 sampled lane branches merged four days earlier were still on origin, so presence grades undecidable and never live. IT NEVER BLOCKS THE WRITE - refusing to overwrite would invent a lock R13's own docstring says the field does not provide - and a crash in the report cannot take down the claim, which is pinned by a test because R13 fails an armed branch that does not hold the slot. VERIFICATION: 9 planted defects, 9 caught by both probes, positive control quiet on restore; 24 self-test and 24 pytest cases; one mutation was WEAK on its first run and was sharpened rather than counted."
}
