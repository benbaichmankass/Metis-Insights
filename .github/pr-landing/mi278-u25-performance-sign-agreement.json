{
  "tier": 1,
  "why": "Tier-1 research only: one NEW scripts/research/ module, one new docs/research/ memo, one row in each of two docs/ registers, and three docs/claude/ backlog appends. Every path is inside TIER1_SURFACE. No src/, no config/, no scripts/ outside scripts/research/, no workflows, no order path. The only VM contact is an UNAUTHENTICATED GET on /api/bot/performance — a read, nothing written to the fleet, no VM action dispatched. It turns PB-20260906's criterion (1) into something runnable and reports that the defect is STILL LIVE six days after filing, with the correction that the flip is driven by contamination SHARE rather than count — the long windows carry more contaminated rows and agree in sign, so that row's count-based framing points a reader at the safest windows. It closes neither R-sign row (the fix is Tier-2 in src/web/api/routers/performance.py) and does not test their criterion (2), and says so. One backlog append is unrelated housekeeping with real preventive value: it records on the netting row that its option (b) is EXHAUSTED by MI-278 U9 and U16, because its criteria still advertise a path U16 proved unreachable and I was about to re-run it. Verified on committed state: 21 self-test controls pass and five separately planted defects were all caught.",
  "landing": "self"
}
