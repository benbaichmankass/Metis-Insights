{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane. Adds scripts/ci/check_workflow_push_target.py (new guard, 12 self-tests), registers it in scripts/ci/run_guards.py, and files one backlog row. Tier-1 by the canonical doc's own list: scripts/ci/** and .github/** are on the Tier-1 surface (CI / tooling). No src/, no config/, no deploy/, no VM, no order path, no risk caps, and NO WORKFLOW FILE IS MODIFIED -- the guard only READS .github/workflows. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY IT SUPERSEDES MY OWN DRAFT #11902: that branch also fixed .github/workflows/sunset-pass.yml, and #11900 (ce6f16b83) landed that fix days earlier -- re-landing it would have been a duplicate of another lane's work, so only the durable half is here and #11902 is closed. WHY AN UNGATED WHOLE-TREE GUARD IS SAFE HERE: the class is already drained to zero (always_default 0, ungradeable 0 over 142 workflow files, measured on this branch), which #11900 did and this guard did not; an ungated guard landed over a live finding fails every PR on day one and gets switched off. THE 4 conditional_default ROWS ARE REPORTED AND NEVER FAIL, deliberately: those workflows' ordinary path is a claude/** branch where the push is correct, and failing correct code is how a guard loses its reviewers. Full suite on the committed tree: PASS 100 / FAIL 1 / SKIP 0, the one failure being this guard's own pr-landing state=undeclared before the arming push."
}
