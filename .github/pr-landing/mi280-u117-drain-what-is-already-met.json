{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, REGISTER-ONLY: docs/claude/health-review-backlog.json and nothing else. No src/, no config/, no deploy/, no workflows, no scripts, no tests. NOT in LANDING_MACHINERY. WHAT IT DOES: closes THREE rows whose resolution_criteria were already fully met, and files the TWO findings that produced. Population stated: the 5 rows carrying filed_by=session_01BjTp5RYwedmpFfkEtkQo1j, i.e. every row this session has filed; all 5 read open. Each graded against its OWN criteria by RUNNING the checks on main at 3078f1b61, not reading them -- the register-id census (#12386, 5 derived sites counted by git grep -c, 6 tests), the check_backlog_refs could-not-look split (#12379, and the live invocation --base no-such-ref-zzz exits 2 and prints no OK, 9 tests), and the drain-candidates timeout (#12384, 9 tests). 24 tests total, and each row's NON-OPTIONAL clauses were checked individually rather than as a block: the positive controls, check_backlog_refs's module-wide returncode invariant, and the _SHALLOW=False forcing without which every drain assertion is green on a shallow clone with the subprocess never reached. TWO rows are CORRECTLY open and are deliberately untouched -- the squash-duplication row (remedy unbuilt) and the dirty-tree-notice row (fix is #12400, unlanded). FINDING 1: closing is a separate act from fixing and nothing requires it; 3 of 5. It is a NAMED class here already (#12344's 'effectively done, never machine-readably closed'). [!] The obvious auto-close is the WRONG fix and the row says so: most criteria in this register demand an observation a merge does not supply, and the two correctly-open rows are exactly that case and would BOTH have been wrongly closed. A prompt to RE-GRADE is wanted, never an auto-close. FINDING 2: backlog_drain_candidates grades 971 of 1014 open rows not_checkable (95.8%). NOT a tool defect -- its two signals need the CRITERION to name a path or diag log name, and a criterion written as a behaviour names neither; all three rows closed here are in the 971. [!] THE OBVIOUS EXTENSION WAS MEASURED AND IS NOT BUILT, recorded so nobody builds it: over the 782 open rows in the four review backlogs, 30.4% name a backticked token but only 9.5% name a real def/class name (24,533 extracted by AST) and only 6.0% name one AND no path; 58.4% name nothing checkable. A naive backtick signal would chase state names -- unknown, dirty, success, hold, generated_at -- and manufacture false likely_met shortlists, the exact harm the tool's own docstring warns about. [!] TWO DENOMINATORS, NOT INTERCHANGEABLE: 4.2% is over the 1014 rows the TOOL scans; the other figures are over the 782 open rows in the four registers. VERIFIED ON COMMITTED STATE: backlog round-trip byte-for-byte on all four registers, register-id-guard OK, claim-basis 0 findings, artifact-validity EXIT 0, stated-population OK. All edits through update_row/append_row, never by hand; resolved_at stamped by the writer. [!] TWO CORRECTIONS MADE BEFORE PUSHING, recorded rather than hidden: I renamed both new rows while drafting and left three `resolution` fields citing the OLD ids -- two dangling references, caught by running check_backlog_refs on COMMITTED state rather than by CI; and the first commit message used -m with double quotes so three backticked example tokens were eaten by shell substitution, amended with a quoted heredoc. [!] IT CLEARS NEITHER NEW ROW: closing these three is the instance, not the class, and a bigger census is explicitly not progress on either. PREDICTED against current main with #12411's own checker before pushing: merge=clean, tree=clean, 10 register(s) read, no duplicate ids."
}
