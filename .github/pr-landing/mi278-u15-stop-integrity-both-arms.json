{
  "tier": 1,
  "landing": "self",
  "why": "One NEW scripts/research/ module plus docs; every changed path is inside TIER1_SURFACE (scripts/research/**, docs/**, .github/**). No src/, no config/, no .github/workflows/, no order path, no strategy parameter, nothing enacted on the fleet. MI-278 U15, object WO-20260912-M20-ACTIVE-TRADE-MANAGEMENT-HOLD-WINNERS-LONGER. WHAT IT DOES: BL-20260911-A-TRAILED-STOP-OUT-IS-ATTRIBUTED-TO-THE-DECLARED-GEOMETRY-THAT-DID-NOT-END-IT requires that ANY analysis attributing a stop-out to declared bracket geometry separate exits at the entry-declared stop from exits at an amended one and STATE the split, and forbids dropping the trailed ones silently because the count is load-bearing. The analysis that most needed it is MINE: MI-278 U5 (#12031) published a stop-rate difference-in-differences of +21.2pp forty minutes earlier, graded with adjudicate_exit, which reads trades.stop_loss -- a field a trailing amend overwrites in place. THE SPLIT, both arms, per ORDER PACKAGE, declared level read from order_packages.exit_plan.stop.price and NEVER from pkg.sl (which may already carry a trailed value, so falling back to it would grade an amended stop as the declared one -- the defect itself): e35 pre 2 stop-outs (2 declared / 0 amended), post 8 (6 / 2); untouched_control pre 31 (22 / 9), post 41 (33 / 8). Every amendment TIGHTER, zero wider, zero ungradeable, zero packages skipped in either arm. TRAILING IS NOT AN e35 PHENOMENON AND ONLY THE CONTROL ARM COULD SHOW IT: the control amends 29% of its pre-era stop-outs and 20% of its post-era ones, comparable to and in the pre era far above the treated arm; MI-275's census is correct and e35-scoped, where 2 of 7 reads as an e35 characteristic. IT CORRECTS ITS OWN AUTHOR'S PUBLISHED NUMBER: on U5's own row selection the declared-only DiD is +7.6pp, so the split removes about two-thirds -- the treated delta falls 35.9 -> 24.2pp while the control's RISES 7.7 -> 11.0pp. A SECOND UNDECLARED DEGREE OF FREEDOM was found while measuring the first and is filed as its own high row: which fan-out row is rows[0] moves the ADJUDICATION, not just the excursion window U5 identified, so the DiD reads 21.2 / 28.2 / 17.9pp (all stop-outs) and 7.6 / 13.2 / 4.3pp (declared only) across population order, earliest close and latest close -- a range of 4.3-28.2pp on IDENTICAL DATA. The treated arm is insensitive at 35.9pp under all three and the ENTIRE swing arrives through the CONTROL, which is why single-arm studies never saw it. That row deliberately does NOT rule which selection is correct, because picking one needs an argument about what a fan-out package's exit IS that nobody has made. WHAT SURVIVES: the sign is positive in all six combinations, so an e35-specific stop-side effect is real; its magnitude is a third to a half of what was published, on an e35 pre cell of TWO stop-outs, and no p-value is offered because one computed on n=2 would be decoration. U5's EXCURSION conclusion is UNTOUCHED -- a fixed window measured forward from entry does not depend on stop adjudication. IT DOES NOT CLEAR THE ROW IT DISCHARGES: that criterion says 'any analysis', a standing obligation on future work, so the row is updated and NOT resolved. VERIFICATION on COMMITTED state: the four-state rule is inline in CF.build_units and cannot be imported, so the forced second copy is ASSERTED -- --verify-against-cf re-derives MI-275's own recorded verdicts through this file's function, 7 shared and 0 disagreements, and an empty intersection reports no_overlap_cannot_verify rather than a pass; --self-test asserts 22 controls; run_guards.py --event pull_request --base main; check_backlog_refs.py --base origin/main OK after catching one truncated id and one I had INVENTED -- an MI188B-shaped id that was never filed, deliberately not quoted here as a bare id because the ref guard cannot tell an invented id from a real one and would flag this file for naming it; the row I actually meant is BL-20260908-A-TRAILING-AMEND-HAS-NO-DURABLE-RECORD-SO-ITS-RATE-IS-UNMEASURABLE; check_claim_basis.py; check_document_index.py; check_research_index.py 133 of 133 routed; ruff clean."
}
