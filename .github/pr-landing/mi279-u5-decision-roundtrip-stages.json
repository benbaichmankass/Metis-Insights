{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5. Registers only: one appended `observation` plus `verified_at`/`last_checked` on OI-20260901-DECISION-ROUNDTRIP-SHIPPED-AND-NO-DECISION-HAS-EVER-MADE-THE-ROUND-TRIP, and the session-brief re-render a register change obliges. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY THE ROW STAYS OPEN DESPITE TWO OF ITS CLAIMS BEING FALSE: its clears_when requires an answer SUBMITTED FROM THE DEPLOYED SPA, observed in_transit and re-read as committed. The one answer carrying a machine signature reads answered_by: telegram -- the wdec tap, not the SPA -- so stage (c) is untouched and I did not watch the transition on the live route. The row's own instruction is to say WHICH stages were cleared rather than to clear it on the easiest one, and that is what the observation does: stage (d) cleared on 61 scheduled successful runs of work-decision-commit.yml, and the ZERO/ZERO baseline refuted at 23 of 179 objects with the denominator change (584 -> 179 via DEC-20260911 retire_mirrors) stated rather than a ratio quoted. WHY IT IS AN OBSERVATION AND NOT A FIX: nothing here is broken in code -- the finding is that the row's description of the fleet has gone stale in the direction that understates progress, which is corrected by measuring, not by changing anything."
}
