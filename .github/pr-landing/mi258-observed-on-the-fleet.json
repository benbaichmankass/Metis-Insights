{
  "tier": 1,
  "landing": "self",
  "why": "Registers only — one file, docs/claude/work/MANAGER-CHECKLIST.json, recording an OBSERVATION that was taken rather than a change that was made. MI-258's bot half (#11752, merged 4e9255acb) is now DEPLOYED and READ OFF THE LIVE ROUTE, and this writes that down where it survives: a board comment does not outlive a register, and the repo's own doctrine is that a fix deployed but not proven on the fleet must be carried by a row. THE DEPLOY WAS WATCHED: /api/diag/version polled every 45s showed on-disk at 6c825266e with restart_pending True for four polls, then git_sha == git_sha_on_disk == c908fcc9a with restart_pending False at 03:34:51Z -- c908fcc9a is NOT the merge sha but CONTAINS it by ancestry, which is the test, and the negative-then-positive pair is what makes this an observation rather than an inference from the merge. GET /api/bot/work/decisions then served bySubjectState {live 1, undeclared 25, unknown 0, gone 1}, matching the pre-merge local measurement exactly, with the defect row grading subject_gone / actionable false, its basis stated, and sorting LAST rather than being dropped. The row ALSO records what the observation does NOT establish -- subject_unknown is 0 so that branch is deployed and unexercised; mootUnanswered == mootAwaitingOperator because no moot question is in transit, which is why that arithmetic defect was invisible to the data; and the operator's screen is unchanged until ict-trader-dashboard#215 merges, because a deploy is not a render. Tier-1 by the canonical doc's own list (docs, registers, observability). NO src/, NO tests, NO config/**, NO order path, NO risk caps, NO deploy/ or unit file; every changed path is inside TIER1_SURFACE, so R5 can vouch for it and R14 would REFUSE an `unvouchable_paths` hold here."
}
