{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5 (the DUE monitoring rows). Docs only: an APPENDED `observation` plus `verified_at` on OI-20260901-OPERATING-LAYER-BUILD-IS-IN-FLIGHT-AND-CARRIED-ONLY-BY-THIS-ROW in docs/claude/OPEN-ITEMS.json, byte-local splice read back through a duplicate-key-detecting parse asserting exactly one row and only those two fields changed AND that the prior observation text survives verbatim; 84 items before and after. Plus the mechanical re-render of CLAUDE.md's generated SESSION-BRIEF block. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. THE ROW WAS ELEVEN DAYS OVERDUE ON A ONE-DAY CADENCE. WHAT WAS OBSERVED: clause 1 is MET — WO-20260901-PHASE-B reads lifecycle done, closed 2026-09-08, and the work view is SERVING, verified live rather than read off the object (GET /api/bot/work/checklist returns HTTP 200, 1,101,596 bytes, readState read, 285 items with a status summary). THE ROW DOES NOT CLEAR: clause 2 needs a COLD session observed reading the view, and I am not cold — I am reading this row because it is DUE in the brief, which is the exact contamination the row names. ALSO RECORDED: the route's freshness block rendered commitAgeHours 12.984 with treeState synced, i.e. the operator's live Workflow page was serving a thirteen-hour-old checklist AND SAYING SO — the anti-staleness property CLAUDE.md designed for that route, exercised rather than merely deployed. That is explicitly NOT filed as a route defect (the route is correct; what is stale is the manager's last push) and explicitly does NOT clear BL-20260903-MANAGER-CHECKLIST-GOES-STALE-SILENTLY-AND-STATUS-REPORTS-IT-AS-CURRENT, whose criterion is about /status, a different surface. AND A CADENCE FINDING: check_every_days 1 on a clears_when that requires an observation nobody can schedule makes this row DUE every day forever, which is the desensitised-alarm shape. Two honest remedies are named and NEITHER IS TAKEN, because both are decisions rather than edits."
}
