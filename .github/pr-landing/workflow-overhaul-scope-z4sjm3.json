{
  "tier": 1,
  "landing": "hold",
  "why": "Manager state and governance records only: the lease heartbeat, docs/claude/work/ registry + work objects + intents, the spawn-priority exception, CYCLE-PRIORITY.json and its rendered CLAUDE.md brief. No runtime, no order path, no strategy config, no risk caps, no live-VM file. Tier-1 by the canonical doc's own examples -- AND THE TIER IS NOT WHY THIS IS HELD. It was `self` until 2026-09-03T12:5xZ and is deliberately demoted: this branch is the MANAGER's, and an armed manager branch merging on its own green is what jammed the queue this morning. MEASURED: 26 merges to main in one day, 19 of them (73%) touching a shared register, 5 of them this branch's and 2 of those bare lease heartbeats -- merges whose entire content is a timestamp. #10918 was dirtied three times and #10923 conflicted instantly. The arming file .github/pr-automerge-requests/workflow-overhaul-scope-z4sjm3.txt was therefore deleted, and pr-landing-guard R6 correctly failed the branch for declaring `self` while unarmed -- a readiness declaration that lands nothing is exactly the drift the guard exists to catch, and the honest fix is to change the DECLARATION to match the behaviour, not to re-arm. The manager merges this branch by hand, last, when the queue is empty.",
  "hold_reason": "depends_on_unmerged_pr",
  "hold_text": "⚠️ THE TOKEN IS IMPERFECT AND DISCLOSED, rather than force-fitted quietly. THE REAL REASON IS QUEUE DISCIPLINE: this is the MANAGER's branch, and an armed manager branch merging on its own green is what jammed the queue on 2026-09-03 -- measured that day, 26 merges to main, 19 of them (73%) touching a shared register, 5 of them this branch's and 2 of those bare lease heartbeats, i.e. merges whose entire content is a timestamp; #10918 was dirtied three times and #10923 conflicted instantly. The arming file was therefore deleted, and R6 correctly failed the branch for declaring `self` while unarmed. THE CLOSED VOCABULARY HAS NO TERM FOR THAT, AND I DID NOT INVENT ONE. `changes_landing_machinery` was tried first and R8 REFUSED it, correctly: this branch's only landing-machinery path is its own declaration file, which the guard deliberately excludes so the excuse cannot be free for everybody. `depends_on_unmerged_pr` is the nearest TRUE term and it is checkable: this branch carries MERGE-QUEUE.json and MANAGER-CHECKLIST.json rows that describe #10920 as position 1 IN FLIGHT, so landing them before #10920 lands would commit a register that is stale on arrival. That is a real dependency, and it is narrower than the reason above. FILED AS MI-104: the vocabulary is missing a `queue_serialisation` term, and forcing an existing one is precisely the 'cheaper to lie to than to satisfy' failure the guard's own R8 comment names. The manager merges this branch BY HAND, last, once the queue is empty.",
  "depends_on": "#10920"
}
