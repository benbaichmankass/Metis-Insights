{
  "tier": 1,
  "landing": "self",
  "why": "docs only — a single backlog register file (docs/claude/health-review-backlog.json). No src/, config/, deploy/, workflow or order path. No row is closed; all three defects are Tier-2/3 and are PROPOSED, not enacted. The audit re-run was read-only (GET) and changed nothing."
}
