{
  "tier": 1,
  "landing": "self",
  "why": "Advances BL-20260810-EXIT-LEVER-SPACE-UNDER-ENUMERATED (open 33 days) by RECORDING and CHECKING the flag->column map its first criterion asks for, and the count it produces differs from both previous hand answers — which is the finding, not a discrepancy to reconcile away.\n\nTHE THREE HAND ANSWERS DISAGREED BECAUSE EACH USED A DIFFERENT POPULATION WITHOUT SAYING SO. 2026-08-10: '21 exit-relevant flags vs 8 columns, 3 with no column' over six harnesses. 2026-09-02: '3 gaps' over TWO (backtest_trend.py + backtest_pullback.py). This pass: FOUR, over the matrix's own five. The 2026-09-02 answer is right for its population and understates the matrix's, because trend+pullback is 42 of the matrix's 52 legs — it could not see a gap arising from the scalp (8), squeeze (1) or fvg (1) legs, and three of the four gaps are exactly there.\n\nTHE HARNESS SET IS DERIVED, NOT DECLARED: whatever the matrix's 52 rows route to through m20_fleet_exit_sweep.classify — donchian 23, pullback 19, scalp 8, squeeze 1, fvg 1. Across those five: 75 distinct flags, 26 exit-relevant candidates by a STATED vocabulary rule, and the remaining 49 REPORTED AS NOT EXAMINED — we did not look, never a clean bill.\n\nTHREE STATES, NEVER COLLAPSED: 19 mapped_to_column · 3 recorded_na (with reasons) · 4 needs_column. Collapsing needs_column into recorded_na is how a real gap disappears by wording, so the checker keeps them apart and deliberately does NOT fail on needs_column — the criterion asks for a RECORDED verdict and 'this needs a column' is one; failing on it would pressure the next session into re-labelling a gap to get green, which is the guard-cheaper-to-lie-to-than-to-satisfy shape this repo already paid for with new-table-wiring-guard.\n\nTHE n/a ARE REASONED, NOT WAVED THROUGH. --exit-grain is a measurement-fidelity knob. --atr-period is recorded as ARGUABLE with the condition that would flip it. --timeout-bars is n/a on THE MATRIX'S OWN RECORDED POSITION rather than my judgement — 33 of its 52 rows carry a caveat of the form 'a NEGATIVE measured on a harness that force-closed at timeout_bars while live has no time stop'. ⚠️ That CORRECTS the row's opening claim that --timeout-bars 'has never been swept': sweeping it would measure a rule production cannot express, and the entry says in terms that it FLIPS to needs_column if BL-20260810-NO-STALL-EXIT-CAPITAL-SITS-IN-DEAD-TRADES ships a live time stop.\n\nTHE FOUR GAPS EACH CARRY THEIR EVIDENCE, and every zero-occurrence claim was MEASURED against the matrix JSON rather than assumed: --rr-floor (swept over 57 leg-cell rows and invisible in the matrix; a column must say BACKTEST-ONLY per BL-20260818-RR-FLOOR-IS-BACKTEST-ONLY-AND-CANNOT-BE-DECLARED), --be-floor-r (MI-165 break-even FLOOR on three harnesses, a one-directional ratchet max()-ed against the chandelier candidate so distinct from trail_geometry; `be_floor` appears ZERO times in the matrix), --sim-breakeven (ict_scalp only and it SIMULATES A LIVE MECHANISM, monitor_breakeven_sl — which is exactly what makes it a gap rather than an n/a; ZERO occurrences while 8 of 52 legs are scalp), --be-arm-on-touch (plausibly a SUB-KNOB of --sim-breakeven per its own help, recorded as needs_column so that decision is made explicitly rather than by omission).\n\nIT IS CHECKED, NOT MERELY WRITTEN: scripts/ops/exit_lever_map.py re-enumerates by AST and FAILS on an uncovered candidate, a missing reason, an off-vocabulary state, or a column that does not exist. MUTATION-TESTED IN THREE DIRECTIONS — drop a flag, blank a reason, name a fake column — all three fail and the clean tree passes. 9 planted controls in --self-test. Registered as `exit-lever-map-guard` with a scoped `when` (a new flag can only arrive by editing a harness, the map or the matrix).\n\n⚠️ THE ROW IS NOT CLOSED, deliberately. Four flags still have no column, and adding one is a schema change to a 765 KB artifact that `matrix-corpus-agreement` and `matrix-config-agreement` both read — ONE decision for the manager, not four edits by a drain session. The criterion is rewritten to say exactly that, with the closing test being `scripts/ops/exit_lever_map.py` reporting 0 in needs_column, and with an explicit warning not to reach zero by re-labelling.\n\nTIER: every path is in TIER1_SURFACE — docs/research/**, scripts/ops/**, scripts/ci/**, docs/claude/**. No src/, no config, no harness behaviour changed: the checker READS the harnesses and writes nothing."
}
