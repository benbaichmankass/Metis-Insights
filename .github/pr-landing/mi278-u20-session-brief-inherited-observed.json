{
  "tier": 1,
  "why": "Tier-1 register only — ONE content file, docs/claude/OPEN-ITEMS.json, inside TIER1_SURFACE (`docs/**`). It records that a STANDING OPEN ITEM's outstanding half is now met, with the evidence read out of a real CI job log rather than reasoned about: run 34717642689, job 103617640153, line 1972 reads `session-brief: verdict=inherited (base=origin/main)`. No code, no config, no order path, nothing enacted. ⚠️ CLAUDE.md is NOT touched by this branch and an earlier draft of this field said it moved by one line — that was true of the pre-merge commit and is false now: main re-rendered the brief itself, the merge took main's copy, and `render_session_brief.py --check --base origin/main` reads `OK — CLAUDE.md matches the registers` with the diff at two files. Field beats comment; the diff is authoritative. ⚠️ THE ROW IS RETAINED RATHER THAN DELETED, following the OI-20260907-TELEMETRY-HOOK precedent in the same register, so the evidence is not lost between sessions; a review prunes it. And the one thing the clearing does NOT establish is stated in the row itself: the observation came through the GitHub job-logs REST endpoint and not through scripts/ops/probe_actions_log.py, so it says nothing about whether the declared probe works.",
  "landing": "self"
}
