{
  "tier": 1,
  "why": "Tier-1 registers only — two files, both inside TIER1_SURFACE (`docs/**` and `*.md`). It records that a STANDING OPEN ITEM's outstanding half is now met, with the evidence read out of a real CI job log rather than reasoned about: run 34717642689, job 103617640153, line 1972 reads `session-brief: verdict=inherited (base=origin/main)`. No code, no config, no order path, nothing enacted. CLAUDE.md moves by exactly ONE line and only because this change touches a register, which is the session-brief guard's own stated remedy (`Whoever next touches the registers should run render_session_brief.py --write`); that line is the wall-clock staleness counter going 33h -> 35h, i.e. the clock-driven staleness the cleared row is itself about. Everything outside the generated SESSION-BRIEF block is untouched. ⚠️ THE ROW IS RETAINED RATHER THAN DELETED, following the OI-20260907-TELEMETRY-HOOK precedent in the same register, so the evidence is not lost between sessions; a review prunes it. And the one thing the clearing does NOT establish is stated in the row itself: the observation came through the GitHub job-logs REST endpoint and not through scripts/ops/probe_actions_log.py, so it says nothing about whether the declared probe works.",
  "landing": "self"
}
