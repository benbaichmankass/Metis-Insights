{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "unvouchable_paths",
  "why": "Lane E64: docs/tooling only, no src/ runtime code, no config/, no order path, no deploy path. Re-points docs/claude/work/PIPELINE.jsonl from a single append-only file to a directory of one immutable JSON file per record (scripts/ops/pipeline.py, scripts/ops/migrate_pipeline_to_dir.py) plus the four CI guard scripts that hand-parsed the old file directly (scripts/check_claim_basis.py, scripts/ci/check_soak_registered.py, scripts/ci/check_operator_owed.py, scripts/ops/check_allow_degraded.py), scripts/ops/render_schedule.py's own self-test fixture, and their tests. NOT self-landing because pr-landing-guard's R5 refuses: scripts/check_claim_basis.py sits at the top-level scripts/ directory, which TIER1_SURFACE deliberately excludes (its own comment: 'every other loose script at scripts/ top level stays outside Tier-1', MI-242). This PR must modify that file (it hand-parsed the old flat file directly), and moving it into scripts/ci/ to fix the classification would touch the #1 canonical doc and several sprint-log references beyond this lane's narrow scope. A human read is one line; widening TIER1_SURFACE or relocating the file is a separate decision for the operator/manager, not this lane's to make.",
  "hold_text": "R5 refuses self-land solely because scripts/check_claim_basis.py (top-level scripts/, modified by this PR) is outside TIER1_SURFACE. Everything else in the diff is inside it. Read once and merge, or ask for the file to move into scripts/ci/ first."
}
