{
  "tier": 1,
  "landing": "hold",
  "why": "MI-312, research tooling and docs only. The diff is two offline backtest harnesses (scripts/backtest_ict_scalp.py, scripts/backtest_fvg_range.py), a new shared definition module (scripts/target_basis.py), a new research driver (scripts/research/mi312_scalp_target_arms.py), a correction to an existing research driver (scripts/research/mi307_offline_mfe.py), and docs/research/**. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy params, no risk caps, no live-VM runtime file. Tier-1 by the canonical doc's own list: 'tooling, observability / read paths, non-live refactors'. The harnesses are offline research tooling and are NOT imported by the trader -- verified by grep rather than assumed: every src/ reference to either file is a docstring or comment, zero import statements. THE ONE THING WORTH A REVIEWER'S EYE, and it is why this field is longer than a data commit's: both live units are documented VERBATIM PORTS of these harnesses (src/units/strategies/fvg_range_15m.py:228 'a VERBATIM port of scripts/backtest_fvg_range.py::run_backtest's', src/units/strategies/ict_scalp.py:736 'Matches the harness semantics EXACTLY'), so a change to harness entry or exit semantics would silently break that correspondence in a way no test here would catch. Both new flags are therefore strictly ADDITIVE and DEFAULT-OFF, and the default arm is VERIFIED byte-identical rather than argued: the unpatched harness from HEAD and the patched one were run on the same BTCUSDT 5m slice (2025-01-01..2025-04-01) and produced an identical summary, 45 of 45 identical rows, zero changed top-level fields and zero changed meta values -- only two new provenance keys added. The fvg change additionally keeps its degenerate-target guard evaluated on the UNIT target so --no-tp cannot change which trades exist, only how they end. The correction to mi307_offline_mfe.py is included here deliberately rather than split: adding the flag to those harnesses is precisely what would have made that script pass a cap to a non-clamping family and label the result live-comparable, so shipping the flag without the gate would introduce the mislabel this unit exists to prevent.",
  "hold_reason": "unvouchable_paths",
  "hold_text": "TIER IS 1 AND THE HOLD IS NOT A TIER CLAIM -- the guard's own docstring draws that distinction and this declaration honours it rather than arguing with it. R5 cannot vouch for scripts/backtest_ict_scalp.py, scripts/backtest_fvg_range.py or scripts/target_basis.py: TIER1_SURFACE allowlists scripts/{ci,ops,research}/** and these sit at the top level of scripts/, which the allowlist does not recognise. That is fail-closed by design and is the correct outcome, so this asks for a human click instead of widening TIER1_SURFACE to admit its own diff -- which would be the cheaper-to-lie-to-than-to-satisfy failure this repo already paid for on new-table-wiring-guard's presence-only marker. The paths are Tier-1 by the canonical doc's list ('tooling ... non-live refactors') and are offline research harnesses the trader never imports (verified by grep: every src/ reference to either is a docstring, zero imports), but 'the guard cannot certify it' and 'it is dangerous' are different statements and only the first is being made. WHAT A REVIEWER SHOULD ACTUALLY CHECK, in one place: both live units are documented VERBATIM PORTS of these two harnesses, so a change to entry or exit semantics would break that correspondence silently. Both new flags are additive and default-off, and the default arm is VERIFIED byte-identical rather than asserted -- the unpatched harness from HEAD and the patched one were run on the same BTCUSDT 5m slice (2025-01-01..2025-04-01) and produced an identical summary, 45 of 45 identical rows, zero changed top-level fields and zero changed meta values, with only two new provenance keys added. The fvg change keeps its degenerate-target guard on the UNIT target so --no-tp changes how a trade ENDS, never which trades exist."
}
