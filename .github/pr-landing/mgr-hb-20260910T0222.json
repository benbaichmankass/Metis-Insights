{
  "tier": 1,
  "landing": "self",
  "why": "Manager state and governance records only: the manager lease heartbeat, the sub-session registry SESSIONS.json, two health-review-backlog edits (one new row filed through backlog_append.py::append_row, one instance appended to an already-open row), and the merge-slot claim in session-board.json written through scripts/ops/claim_merge_slot.py rather than by hand. No runtime, no order path, no strategy config, no risk caps, no live-VM file; no test, guard or workflow is modified. Filing a backlog row is explicitly the act of NOT taking the item."
}
