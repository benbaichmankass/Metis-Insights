{
  "tier": 1,
  "landing": "self",
  "why": "MI-317, the per-leg target-geometry decision packet. PROPOSE ONLY: the diff adds two research artifacts (docs/research/per-leg-target-geometry-packet-2026-09-18.md and its committed JSON), one new research script (scripts/research/mi317_target_geometry_packet.py, 39 selftests of which 7 are negative controls), one work object, one appended row in docs/DOCUMENT-INDEX.md and two appended rows in docs/claude/health-review-backlog.json filed through scripts/ops/backlog_append.py. Tier-1 by the canonical doc's own list and verifiable against the diff: NO config/ (every tp_r and tp_at_r named in the memo is Tier-3 and is put to the operator rather than applied), no src/, no tests/, no .github/workflows/, no deploy/, no order path, no strategy params, no risk caps, no live-VM runtime file. The new script is READ-ONLY -- it opens config/strategies.yaml, config/accounts.yaml and three committed artifacts, and writes exactly one JSON under docs/research/; it has no network call, no DB write and no exchange client. It is not wired into any workflow, cron or runtime path, so landing it changes nothing that executes. The only file outside docs/ and scripts/research/ is this landing declaration plus the arming file and merge-slot claim the self-land route itself requires."
}
