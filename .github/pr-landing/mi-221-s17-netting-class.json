{
  "tier": 1,
  "landing": "self",
  "why": "Docs and one backlog row. Corrects a STALE sentence in § 16 IN PLACE and appends § 17, plus one row appended to docs/claude/health-review-backlog.json via scripts/ops/backlog_append.py, plus the merge-slot field. No src/, no config/, no deploy/, no .github/workflows/, no tests, no order path — Tier-1 by the canonical doc's own list. THE § 16 CORRECTION: that section said the closed-PnL instrument was 'held at landing: hold pending a merge click', which went stale WITHIN THE HOUR — #11560 merged at 15:53Z as 0ff2afba (operator-approved Tier-2) and DEPLOYED. What is true instead is worse and is recorded rather than quietly swapped in: the route was deployed and BROKEN, its first live call at 16:06:02Z returning could_not_look with 'TypeError: run_account_read() got an unexpected keyword argument symbol'. The question it was built to answer is still unanswered, but for a different reason than the sentence claimed, and 'waiting on a merge' vs 'shipped and raising on every request' are not the same state. § 17 RECORDS THE OPERATOR'S SIBLING-COVERAGE HYPOTHESIS, measured: REFUTED for trade 5471 (zero other bybit_2 ETHUSDT rows open at any point during its life; nearest prior close 5403 was 1d 0:27:47 BEFORE it opened; 5471 carries both leg ids) with the population stated as a WINDOW (ids 4611..5610) rather than the whole table. AND IT NARROWS THE CLASS RATHER THAN OVER-CLAIMING IT: two rows closed netting_attributed and 4886's sibling 4808 closed 45.1 seconds later — verified from the journal directly, not relayed — BUT BOTH netting_attributed ROWS CARRY THEIR OWN sl_order_id, so netting-SHARING is demonstrated while 'a row with no legs of its own being MASKED by a sibling' is NOT. The three NULL-leg-id ETH rows were checked rather than assumed and are all intent_reduce_executed with created_at == closed_at to the second, so their NULLs are correct and they are not candidates. Filed as its own backlog row on its own evidence and explicitly NOT as an explanation of 5471, with resolution_criteria demanding a per-ROW leg-residency read and a positive control, and stating that any change to the coverage basis itself is Tier-2. § 17 keeps BOTH remaining branches live — (A) the legs were cancelled while live, and (B) the naked row is a position our journal does not record at all — and says explicitly that (A) must not be collapsed to merely because it is tractable. No order was placed, modified or cancelled on any account; every venue call was a GET. DOCUMENT-INDEX.md untouched (no new file), so document_index.py --write was not run and remains prohibited."
}
