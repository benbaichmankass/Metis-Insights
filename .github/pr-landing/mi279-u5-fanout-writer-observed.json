{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5 (the DUE monitoring rows). Docs only: one `observation` + one `verified_at` on OI-20260912-FANOUT-APPLY-PATH-REPAIRED-AND-STILL-HAS-NEVER-DISPATCHED in docs/claude/OPEN-ITEMS.json (2 insertions / 2 deletions, byte-local splice read back through a duplicate-key-detecting parse that asserts exactly one row and exactly two fields changed; 84 items before and after), plus the mechanical re-render of CLAUDE.md's generated SESSION-BRIEF block, which drops that row out of the DUE list 7 -> 6 because it has now been observed. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHAT WAS OBSERVED: clause (1) of the row is MET -- six live soak rows grade apply_state 'dispatchable' with rounds_applied carrying side/entry/sl/tp, against a trader whose running sha was verified by git merge-base to contain the fix. THE ROW DOES NOT CLEAR, and the observation says so: clause (2) is unmet and its DENOMINATOR IS ZERO, because the newest order package and the newest trade both predate the first dispatchable round by three and a half hours, so there has been nothing to attribute. WHY THIS IS WORTH LANDING RATHER THAN LEFT IN A SESSION: it records two readings that would otherwise cost the next session its search. (a) 196 of 300 order packages match the substring 'arbitration_fanout', and every one of them is the tick's PLAN annotation; the exact key clause (2) names, meta.arbitration_fanout_round, is carried by zero of the 300 -- so a loose grep reads as dispatch attribution and is not. (b) All six dispatchable rounds are for bybit_1 with holds_winner true and a strategy identical to the global winner, and the only non-winner account reads apply_scope not_allowlisted, so on the current allowlist the fan-out can only re-dispatch what the global path already dispatches and clause (2) is unreachable rather than merely unobserved. That is a TIER-2 allowlist question and is explicitly NOT proposed here."
}
