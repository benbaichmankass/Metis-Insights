{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 backlog-only write — ONE file, docs/claude/health-review-backlog.json, and ONE row changed, through the mandated backlog_append.update_row. No src/, no config/, no scripts/, no workflow, no unit file. It closes BL-20260813-HARNESS-SYMBOL-IS-A-LABEL-DATA-DEFAULTS-TO-BTC, which #12072 was titled to close and left at kept_open because that PR declared landing: hold and could not self-land — flipping the status on a held branch would have asserted a landing that had not happened. The merge (c068b7448) is what made the close observable, and the row's own two criterion commands were RUN at that commit rather than read off the diff: backtest_trend.py --symbol SOLUSDT --timeframe 1h and account_compat_matrix.py --symbol SOLUSDT --strategy trend_donchian both exit 2 and refuse, the BTC fixture span appears 0 times across BOTH streams of the first, and btc_5m.parquet appears 0 times in the second's stdout. Positive control included, because a refusal that refuses everything is not a fix: with no --symbol and no --data the harness still exits 0 and prints the identical legacy BTCUSDT result (144 trades, net_r -82.6483) plus one [legacy_default] provenance line on stderr, so the 251 existing --data call sites are untouched. This closes the ROW and explicitly NOT the CLASS — 15 further sites remain, 14 of them sharing one fixture, carried by BL-20260912-FIFTEEN-MORE-HARNESSES-LET-SYMBOL-BE-A-LABEL-OVER-A-DEFAULTED-DATA-FILE-AND-FOURTEEN-SHARE-ONE-FIXTURE, which stays open. The write was verified by READ-BACK rather than by the writer's return value, because this lane has already put a malformed update_row value onto this live register once: 1621 rows before and after, 0 duplicate ids, LOST 0, UNEXPECTED 0, exactly 1 row changed, the two prior updates byte-identical, 0 fields lost, and resolved_at stamped by _close_stamp on a genuine kept_open -> resolved transition rather than back-filled."
}
