{
  "tier": 1,
  "landing": "self",
  "why": "MI-280 unit U2, the DETECTOR half. Adds scripts/ci/check_armed_branch_push.py (a pure grading policy, no network) and .github/workflows/armed-branch-push-watch.yml, which on every push to a claude/** branch reads that branch's own pull request and says whether the pushed commit can still land. Tier-1 by the canonical doc's own list: scripts/ci/**, .github/workflows/**, tests/** and docs/ only - no src/, no config/, no deploy/, no order path, no strategy config, no risk cap, no live-VM runtime file. R12 DOES NOT APPLY AND THAT WAS CHECKED RATHER THAN ASSUMED: neither new file appears in check_pr_landing.py::LANDING_MACHINERY, and the detector is not a landing route - it can neither open, arm, nor merge a pull request, and its only write is a log line. THE PREVENTION HALF IS DELIBERATELY NOT HERE: opening the PR under a PAT so CI attaches on the first head, and arming only after a required check exists, both change .github/workflows/claude-pr-automerge.yml, which IS in LANDING_MACHINERY, so that work is a separate PR declaring landing: hold and routed to the manager. Splitting them is the rule working, not a way around it. THE DEFECT, measured five times: claude-pr-automerge arms native auto-merge and from that moment the branch is closed to new content, but git push reports success either way and the arming state is invisible at push time. On #11846 (2026-09-12) auto-merge squashed head 63e55b8ef at 01:03:28Z and commit 49653310e was pushed roughly 30 seconds LATER to a branch whose PR had already merged; the dropped commit carried the SESSIONS.json row for a sub-session that was ALREADY RUNNING, so a live lane existed on the fleet with no record on main for nine minutes. VERIFICATION: 8 planted defects, 8 caught by BOTH probes, positive control quiet on restore; the FIRST run caught 7 of 8 and the miss - a prefix comparison standing in for full-sha equality - is recorded rather than hidden, with the control rebuilt on two shas that share an 8-character prefix. 30 self-test cases and 21 pytest cases, including CLI cases because CI never runs this workflow. The workflow's shell was exercised, not only lint-checked. It is registered in docs/github-actions-workflows.md in this same diff, so workflow-catalog passes."
}
