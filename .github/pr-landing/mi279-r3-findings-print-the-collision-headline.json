{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling: scripts/ci/check_register_ids.py, one NEW test file, and one row APPENDED to docs/claude/health-review-backlog.json through backlog_append.py::append_row (18 insertions / 0 deletions). No src/, no config/, no deploy/, no workflows, no scripts/ops. check_register_ids.py is NOT in LANDING_MACHINERY, so R12 does not apply. WHAT IT FIXES, REPRODUCED RATHER THAN CONSTRUCTED: against main @123f6c192 -- i.e. AFTER #12399 made main green -- a temp git repo holding two DISTINCT ids with nothing colliding produced BOTH 'OPEN-ITEMS.json::items[id]: 2 rows, unique' in the census AND '::error::A register id collided ... the merge silently deletes the row that was already there' over a finding that reads 'NEW row records no creation date'. main() printed ONE ::error:: headline over a problems list pooling R1, R2 and R3; R3 is not a collision. UNPROVENANCED DIAGNOSTIC OUTPUT sub-class A. [!] THE HARM IS THE REMEDY, NOT THE WORDING: the headline says 'mint a fresh id' and the correct R3 fix is 'add the creation field', so a reader who follows it mints an id nothing asked for -- on the same morning the board was warning that this guard's remedy line is ALSO wrong for a byte-identical duplicate. [!] #12398's CENSUS FIX MADE THIS SHARPER, NOT SAFER: before it the census also printed the literal 'unique' whatever check_uniqueness returned, so a reader could dismiss both lines as one confused guard; the census is now derived and correct, so the run states a TRUE 'unique' beside a FALSE 'collided' and the honest line lends the dishonest one its credibility. [!] THE ARGUMENT WAS ALREADY IN THIS FILE AND R3 WAS NEVER BROUGHT UNDER IT: check()'s docstring explains that unparseable is a separate return value precisely so 'the two causes travel separately and get their own headlines', and that paragraph OPENED by describing the pooling as settled background. It is corrected, not left as the record. Third instance of the class in this one file after _rows's '0 rows, unique' and #12398's literal verdict. [!] ONE CHECK CAME BACK THE OPPOSITE WAY AND NARROWED THE FIX: I expected all three rules to be mis-headlined. MEASURED, R2 IS CORRECTLY DESCRIBED -- its own message ends 'That is the signature of a NEW item filed under an id that was already taken', so 'collided' fits R2 exactly. Only R3 comes out; R1 and R2 SHARE the headline and a test pins that against a later tidy-up on symmetry grounds. CONTROLS: 8 new --self-test cases (43 total) covering group_by_headline purely, plus 9 end-to-end pytest cases that build a REAL git base so the printing path a CI run reaches is exercised. FOUR PLANTED DEFECTS, BOTH SUITES, ALL CAUGHT: (A) R3 routed back to the collision headline = the pre-fix behaviour, 3 FAIL / 3 FAIL; (B) the collision headline deleted = THE POSITIVE CONTROL, 2 FAIL / 2 FAIL, load-bearing because without it deleting COLLIDED_HEADLINE outright would satisfy every negative assertion while destroying the rule the guard exists for; (C) a rule that found nothing still prints its headline, 5 FAIL / 4 FAIL; (D) a positional cross-reference reintroduced, 1 FAIL. An UNTAGGED rule is a hard KeyError, never a silent fallback -- a new rule quietly inheriting the collision headline is exactly how R3 got there. [!] A DEFECT IN MY OWN FIRST DRAFT, RECORDED RATHER THAN QUIETLY FIXED: my R3 headline read 'the OPPOSITE of the collision one BELOW', wrong in both directions a run can take -- HEADLINE_ORDER prints the collision block FIRST when both trip, and on the R3-only run this split exists for there is no collision block at all. A pointer to a neighbouring block that may not exist is the same sub-class A defect one hop down, written while fixing it; found by reading the four arms' OUTPUT rather than the string, and plant D stops it returning. FILED NOT FIXED, one concern per PR: BL-20260917-REGISTER-ID-GUARD-PRINTS-R2B-COVERAGE-100-PERCENT-... -- R2a continues on a hit so that row never reaches R2b, yet the census prints 'R2b (replacement) 1/1 (100%), 0 replaced'. Severity low and stated honestly: it can only occur on a run ALREADY failing on R2a and the row is named in that same run's error block, so nothing is hidden. similar_ok=True was passed after READING all 8 candidates; the nearest (0.57) is about R2a covering 0% of OPEN-PRS.json because its ROWS carry no dates -- a data gap, not a reporting over-claim -- and it is linked in related. VERIFIED ON COMMITTED STATE, no pipes: check_register_ids --self-test EXIT=0 (43 PASS), --base origin/main EXIT=0, pytest 9 passed, ruff clean, backlog_append --check-live 'OK -- 4 live backlog(s) reproduce byte-for-byte', 1606 -> 1607 rows / 1607 unique."
}
