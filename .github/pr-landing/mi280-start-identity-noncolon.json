{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling: scripts/ci/check_scope_overlap.py + one backlog row. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no workflows, no live-VM runtime file. NOT landing machinery — check_scope_overlap.py is absent from check_pr_landing.py's LANDING_MACHINERY — so R12 does not apply and `self` is honest. MEASURED over the most recent 488 coordination-board comments: 76 look like a START and TEN parse to no identity at all (13.2%), across THREE different sessions. The cause is that _IDENTITY_LABEL_RE required a COLON, so six of the ten — which name session AND branch on the line right under the header — were refused for punctuation alone. The cost is not a missed overlap: the audit fails toward REPORTING, so the result was a permanent FALSE line on every affected PR naming a START that was perfectly attributable, which is the desensitised-alarm class. The colon is now optional when the label is followed by a backticked value; re-measured on the SAME population, 10 -> 2. The #10729 control is untouched — the line must still BEGIN with the label, so a prose mention and a mid-sentence one still yield nothing, asserted by three negative cases. DECLARED RESIDUAL, deliberately not fixed: the remaining 2 open with a bare backticked id and no label, and accepting that would refuse nothing that #10729's control exists to refuse. VERIFIED: 4/4 plants caught with the positive control quiet, 94 self-test controls fire, 51/51 tests pass, and the live board re-measured over the identical population. One plant had to become a TWO-edit plant: the line anchor is doubly enforced (`^` and `.match()`), either alone holds, so a single-edit plant is a silent no-op that reads as an escape — now commented at the call site so the redundancy is not tidied away."
}
