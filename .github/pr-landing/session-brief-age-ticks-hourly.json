{
  "tier": 1,
  "landing": "self",
  "why": "One row appended to docs/claude/health-review-backlog.json recording that the generated CLAUDE.md SESSION-BRIEF embeds a wall-clock hour counter, so session-brief-guard re-reds any open register-touching PR as the clock ticks. MEASURED while driving PR #12205 back to green: it was green at head 47b610e6 and red at 03e00938 with the only intervening change a bare `Merge main`; reproduced locally (PASS 89 / FAIL 1, session-brief-guard sole failure, verdict introduced_registers_changed); and the prescribed remedy produced a ONE-LINE diff whose content is `143h OLD` -> `144h OLD` against a FIXED measured timestamp of 2026-09-11T10:39:09Z, which the arithmetic confirms is now-minus-measured floored to hours. The row states plainly that the hourly PERIOD is inferred from that arithmetic rather than observed twice, and that scheduled jobs do not hit it because they re-render inside the same commit. Tier-1 by the canonical list (docs/claude/**, inside TIER1_SURFACE). ONE file plus this declaration. NO src/, NO config/, NO scripts/, NO workflow, NO tests. Nothing in it can place, modify or refuse an order, and it changes no behaviour of any kind. Filed rather than fixed deliberately: the remedy is a judgement about what the brief should display (age band vs bare timestamp vs exempting the line), not a defect with one right answer, so the row lays out the three options and leaves the call to whoever owns the brief."
}
