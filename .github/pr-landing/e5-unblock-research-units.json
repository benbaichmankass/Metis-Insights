{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "unvouchable_paths",
  "hold_text": "Tier-1 research bookkeeping the guard cannot certify. The whole diff is three research/queue/*.yaml units flipped from `status: blocked` to `queued`, plus the workflow_dispatch input each unit's `run.inputs` now declares. `research/queue/**` is outside scripts/ci/check_pr_landing.py::TIER1_SURFACE (the list carries scripts/research/** but no research/**), so R5 refuses a self-land on it. That refusal is CORRECT as written — the allowlist polarity is deliberate and the one precedent for widening it (MI-242, scripts/backtest_*.py) went through an operator decision record — so this PR holds rather than the guard being widened by whoever was inconvenienced. Filed for the operator as PI-20260922-E5-RESEARCH-QUEUE-IS-OUTSIDE-TIER1-SURFACE-SO-NO-LANE-CAN-SELF-LAND-A-QUEUE-FLIP. WHAT A REVIEWER IS BEING ASKED TO CONFIRM: that the blocker each unit named has actually cleared. It has — PR #12739 makes m20-exit-lever-sweep.yml and research-exit-head-build.yml declare `research_unit` and `power_state`, which is verbatim what all three `clears_when` clauses ask for. Depends on #12739; merge that first.",
  "why": "Tier-1 research bookkeeping: three research/queue/*.yaml units flipped from `status: blocked` to `queued` because the blocker each one named has cleared, plus the `research_unit` input each unit's `run.inputs` now declares. No src/, no config/, no deploy/, no order path, no risk caps, no live-VM file — the diff changes what the research dispatcher will FIRE, never what the trader does. It holds rather than self-landing only because research/queue/** is outside TIER1_SURFACE (see hold_text), not because the work needs an approval tier above 1."
}
