{
  "tier": 1,
  "landing": "self",
  "why": "Advances BL-20260901-COORDINATION-BOARD-WRITES-403-FROM-THIS-SESSION-WHILE-READS-SUCCEED by completing its documentation half in the ONE surface that was missed, and records honestly that the observation half is NOT supplied here. Tier-1 throughout: .claude/skills/**, docs/claude/**.\n\nTHE CRITERION NAMES TWO SURFACES AND BOTH WERE ALREADY MET — measured with the row's own probe shape, counts stated: CLAUDE.md board-post 5 / pr-opener 5 at lines 1434-1516, i.e. in the PM-side capabilities section immediately after the claude-pr-automerge 403 workaround, which is the 'beside the 403 discussion' the criterion asks for; docs/claude/coordination-board.md 5 / 5 against 11 mentions of 403. ⚠️ AND .claude/skills/session-coordination/SKILL.md had board-post **0** and pr-opener **0** while mentioning 403 TWICE. That is not a miss against the criterion's letter — it names only the other two — but the skill is what a session MUST read under skill-first lookup, and the sibling row BL-20260819 already treats it as a surface a session in exactly this position reads. The criterion is extended to name it, with the reason recorded.\n\nWHAT THE SKILL NOW CARRIES, at its own 403 paragraph rather than in a separate list: both relays with their request AND result paths; the distinction between the WRITE-SCOPE 403 and the transient MCP drop documented one line above it (retry with backoff clears one and never the other, and neither gh nor curl to api.github.com helps); the fresh-filename-per-PR rule for pr-opener, whose result file is the idempotency key; and the trap BOTH relays share — their result commit is pushed by github-actions[bot], GitHub does not trigger workflows for GITHUB_TOKEN pushes, so a PR can show ZERO checks and read as green-adjacent when it is blocked. Read mergeable_state to tell `blocked` (no checks fired) from `dirty` (merge conflict).\n\n⚠️ I CANNOT SUPPLY THE OBSERVATION HALF AND AM NOT PRETENDING TO. The criterion asks to be 'VERIFIED BY A SESSION THAT HITS THE 403 AND REACHES THE BOARD WITHOUT READING .github/workflows/, and says so'. I did not hit it: MEASURED 2026-09-12, add_issue_comment on the board succeeded 5 of 5 from this PM-side web session. A session that never needed the relay cannot testify that the relay is findable, and the criterion says in its own terms that a grep confirming the words are present is a deploy, not an observation. The row stays OPEN on that half, and the PR title says so.\n\n⚠️ THE 5-of-5 IS ITSELF A FINDING ABOUT THE ROW'S SCOPE. Its title says 'WRITES 403 FROM THIS SESSION', and write access is evidently NOT uniform across sessions. So a session that does NOT hit the 403 must not conclude the relay is unnecessary for the one that does — which is precisely why the pointer belongs in a binding skill rather than in a session's memory."
}
