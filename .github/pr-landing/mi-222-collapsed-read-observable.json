{
  "tier": 2,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "why": "TIER-2, and the tier is set by ONE fact: this diff edits src/units/accounts/clients.py, whose account_open_positions return value feeds _exchange_position_set (order_monitor.py:2884), which keys on (symbol, normalised_side), which gates the CLOSE decision at order_monitor.py:4348 on real money. Nothing about the change being observability-only lowers that -- the file is on the live order path and the tier follows the path, not the intent.\n\nWHAT THE CHANGE IS. Three venue answers -- genuinely flat / a zero-size row was returned / no row was returned at all -- arrived at every caller as one silent value, with no counter, no log line and no position_idx; and a FAILED per-symbol cross-check was indistinguishable from a clean empty one. They are now counted and named, per read, with query_state in {rows_returned, no_rows, could_not_look}. The dedupe and the size gate are UNCHANGED: the rows that were dropped before are still dropped.\n\nWHAT MAKES IT SAFE, ASSERTED RATHER THAN ARGUED. The reader's return value must stay byte-identical, and tests/test_accounts_clients_position_read_observability.py::TestReturnValueIsByteIdentical pins it directly -- same venue payloads, same returned list, key by key -- plus a test that a failing soak write leaves the return value intact, because this sits on the tick path. 13 tests pass. If the set of positions any caller receives changed by one element this would be the Tier-3 change the operator did not approve.\n\nSCOPE OF APPROVAL, AND ITS PROVENANCE STATED HONESTLY. The operator chose observable-first over fixing the behaviour, relayed to this lane by the manager (session_01HrmZ1RRNM4UnEUaFdrPEjj) 2026-09-09: approved to make the collapsed read OBSERVABLE, explicitly NOT approved to change what any caller sees. I could not verify that approval from any repo surface -- MANAGER-CHECKLIST.json on main carries no MI-222 entry -- so it rests on the manager's relay, which is why this declares hold rather than self-landing on the Tier-1 half. The safety property does not depend on the provenance: the byte-identical invariant is asserted by test and is checkable by any reviewer.\n\nALSO IN THE DIFF, and each is load-bearing rather than scope creep: the diag log_file allowlist entry for the new soak (a soak that is written and cannot be read is the VALIDATION_LOG_PATH write-only-log failure, and this file is the ONLY evidence that could ever justify the held-back Tier-3 change -- it ships in the SAME commit as the writer per this repo's own rule); and the OPEN-ITEMS row registering the soak, which soak-registered-guard requires and which states what READY means in DATA (dropped_symbol_dedupe_count>=1) rather than in elapsed days.\n\nNOT REGISTERED with check_collapsed_states.py's CONTRACTS table, and said plainly rather than satisfied: that guard requires a consumer that BRANCHES on the states, and there is none BY DESIGN, because the approved scope forbids changing what any caller receives. Registering would mean inventing a decorative branch, which is the failure that guard exists to catch. Register it when a real consumer exists.\n\nGUARDS: PASS 68 - FAIL 1 - SKIP 23, the single failure being pr-landing-guard in its documented pre-declaration state, which this file clears. Two guards caught real defects in my own work on the way: json-notes-cap-guard (I had json.dumps(...)[:400], which cuts mid-token into invalid JSON -- now dump_capped) and soak-registered-guard (my soak was registered nowhere and would have accrued to nobody). No order was placed, modified or cancelled on any account.",
  "hold_text": "TIER-2 ON A LIVE REAL-MONEY ORDER-PATH MODULE -- it needs the operator's OK to MERGE, and a relayed approval to BUILD is not that. The operator approved the observable-first APPROACH (relayed by the manager, session_01HrmZ1RRNM4UnEUaFdrPEjj, 2026-09-09); what this hold waits on is the merge decision on this specific diff, which per the tier table is 'one operator OK in chat' before shipping.\n\nI am flagging one thing rather than letting it pass silently: I could NOT verify the operator's approval from any repo surface. MANAGER-CHECKLIST.json on origin/main carries no MI-222 entry (the manager's record is still in their own unmerged PR #11547), so the approval reaches me only through a scheduled-trigger relay. That is the established coordination channel here and I am not treating it as suspect -- but a Tier-2 diff on the module that gates a real-money close should not self-land on an approval its own repo cannot show, so the human merging it should confirm the scope is what they granted: OBSERVABILITY ONLY, caller-visible behaviour unchanged.\n\nWHAT TO CHECK IF YOU REVIEW THIS: the invariant is the whole safety argument, and it is asserted, not argued -- TestReturnValueIsByteIdentical runs the real function over the same venue payloads and pins the exact returned list. If that test is green the change cannot have altered what any caller receives.\n\nAuto-merge is NOT armed and no merge_slot is claimed. The Tier-3 behaviour change -- actually fixing the dedupe so a live hedge book is not dropped -- is deliberately NOT in this diff and is not mine to propose: the manager carries it to the operator once the soak shows which of the three states production is in."
}