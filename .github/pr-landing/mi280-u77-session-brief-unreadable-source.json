{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI/ops tooling, one file: scripts/ops/render_session_brief.py. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no live-VM runtime file, and no register is written -- CLAUDE.md is deliberately NOT regenerated, because the clean-case render is byte-identical to before and that is asserted by diff in the PR. R12 does not apply: the file is not in LANDING_MACHINERY, verified against that list rather than assumed. It stops a JSON parse failure in a source register collapsing into 'nothing to report' on the one surface CLAUDE.md itself calls the only channel that reaches a session before it acts. MEASURED on origin/main @7f832e8c0: with THIS IS NOT JSON inserted into docs/claude/OPEN-ITEMS.json the brief rendered 0 monitoring rows instead of 20, exited 0, and printed 'No monitoring item is due ... an empty list here means nothing is past its cadence, not that the renderer failed' -- a sentence that is itself the falsehood, over rows including loud:true real-money items. Three states, never collapsed; the banner is rendered ABOVE everything it invalidates; the monitoring section says NOT LISTED rather than the reassurance. Proven both ways: OLD exit 0 with the false line vs NEW exit 2 naming the file, clean render byte-identical, and three planted defects each red their own control including the OVER-REPORTING direction where an absent or empty-but-valid register must NOT be called unreadable."
}
