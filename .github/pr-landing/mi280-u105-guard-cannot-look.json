{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling: scripts/ops/check_backlog_refs.py, one new test file, one repaired test class, and ONE row appended to docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no .github/workflows/. NOT in LANDING_MACHINERY, so R12 does not apply. WHAT IT FIXES: `_git` ran git and returned .stdout, discarding returncode, so a --base that does not resolve produced an EMPTY diff, zero introduced references, and the message 'OK - every tracking id this change introduces resolves to a filed backlog row.' MEASURED: the same commit graded twice against byte-identical base trees (both rev-parse to 2c2da8644) gives exit 1 naming a real dangling fragment for `--base origin/histbase` and exit 0 OK for `--base hb12245`, a remote-tracking ref spelled without its origin/ prefix. [!] THIS GUARD'S VERDICT IS QUOTED IN PR BODIES AND LANDING RECORDS AS EVIDENCE that a change introduces no dangling reference, which is why severity is high: a guard that cannot read its input and says OK is worse than no guard, because it is read as proof. [!] THE FILE ALREADY KNEW AND THAT IS THE FINDING: `_refs_in_file_at` returns None for 'the file did not exist' rather than set(), citing the Collapsed-states rule by name, and `_refs_anywhere_at` RAISES above rc 1 noting that reading a git failure as 'nothing was cited at base' would 'silently restore the blindness this function removes'. Both are FALLBACK reads. `_git`, which computes the diff itself, did what they refuse. FOUND BY MY OWN POSITIVE CONTROL FAILING while building #12378's evidence table: four PRs graded clean on the prefixless spelling, and only the known-positive control coming back clean too gave the typo away. THE FIX: `_git` raises GitRead carrying git's stderr; main() exits 2 - not 0 and not 1 - so 'could not look' stays legible apart from 'looked and found dangling refs', the same split the --all path already makes for an unreadable register. [!] AN EXISTING TEST WAS GREEN THROUGH THIS: test_the_gating_mode_WARNS_and_keeps_its_verdict passed `--base no-such-ref-000` into a directory that is not a repo and asserted 0-or-1. Its FIXTURE is repaired (a real repo via _as_repo_citing), not its assertion, plus a control proving the gate can still fail and a test pinning the defect. Re-planting the original defect reddens that repaired test, which is the proof the repair was real. CONTROLS: 9 new tests + 2 added to the repaired class; four planted defects, all four caught. One test's first draft passed VACUOUSLY in the reassuring direction (a local branch of the name existed, so the 'prefixless' ref resolved) and that is recorded rather than quietly fixed - the case is now constructed with its premise asserted before the behaviour. BLAST RADIUS MEASURED, NOT ASSUMED: exactly one module imports this one (scripts/ops/check_allow_degraded.py) and it imports REF and filed_ids, not _git or refs_in_added_lines; filed_ids was checked by AST to shell out to nothing, and the script was RUN and reports OK. The --all path is untouched and still exits 0. Full guard set on committed state: PASS 85 / FAIL 1, the one failure being pr-landing-guard, which this record is the second push for. Register integrity: append_row byte-exact, 14 insertions, 0 deletions, 0 lines reformatted, 1597 -> 1598 rows, --check-live clean."
}