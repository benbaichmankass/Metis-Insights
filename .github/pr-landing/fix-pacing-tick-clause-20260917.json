{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 register write, manager scope — recording an operator decision into docs/claude/work/** is explicitly management. ONE file, WO-20260916-OPERATOR-DIRECTION.yaml, and nothing else: no src/, no scripts/, no workflows, no config. DEC-20260916-PACING-RULES clause (3) said the manager heartbeat is every 2h while check_manager_scope.py R7 fails a heartbeat more than one lease TTL (90 min) after the previous one — the row flagged that contradiction itself and the operator resolved it on 2026-09-17 by tightening the ticks to 90 min with R7 unchanged, after which the row still said 2h. This corrects clause (3), preserves the 09-16 wording verbatim in its own key with `chosen` left unedited so the answer is superseded rather than retracted, records DEC-20260917-MANAGER-TICK-CADENCE as its own decision naming exactly what it supersedes (clause 3 only) and what it does not touch (the 8-12 unit cap, the ban on open-ended drain mandates, and one-lane-at-a-time which was offered and declined), and rewrites the R7 warning as RESOLVED including that the manager recommended widening R7 and was OVERRULED so the losing recommendation is not re-proposed. R7 itself is NOT widened and no guard is changed."
}
