{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "unvouchable_paths",
  "why": "R5 correctly refuses this PR: `scripts/check_claim_basis.py` sits outside TIER1_SURFACE (which covers scripts/ci/**, scripts/ops/**, scripts/research/** and scripts/reports/**, but not scripts/*.py at the top level), so the guard cannot certify it and self-landing is refused rather than granted by default. That is the guard working, not an obstacle to route around -- VERIFIED by declaring `landing: \"self\"` first and reading the refusal, rather than assuming it. NARROWING WAS CONSIDERED AND REJECTED: `check_claim_basis.STATUS_ENUM` is mirrored into docs/CLAUDE-RULES-CANONICAL.md by `canonical-doc-coherence`, and `scripts/ops/work_digest.py` derived its backlog-terminal set from the same constant, so the guard change, the doc change and the decoupling are ONE fact and splitting them makes two of the three PRs red. Everything else in the diff is scripts/ci/, tests/, docs/ and .github/.",
  "hold_text": "Touches `scripts/check_claim_basis.py`, a CI guard outside TIER1_SURFACE, so R5 cannot vouch for it and correctly refuses the self-land. What to read there: the guard read four ARCHIVED review backlogs and scanned zero files, while docs/CLAUDE-RULES-CANONICAL.md named it as what enforces the status enum; it is re-pointed at docs/claude/work/MANAGER-CHECKLIST.json (the `state` field, whole-file) and docs/claude/work/PIPELINE.jsonl, and it now ships a --self-test with 11 planted controls, including a violation it must FAIL on. Splitting it out was considered and rejected -- canonical-doc-coherence mirrors its STATUS_ENUM into the #1 doc and work_digest.py derived from the same constant, so they are the same fact. The arming file and the merge-slot claim are withdrawn, so nothing auto-merges."
}
