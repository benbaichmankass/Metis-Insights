{
  "tier": 3,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "why": "TIER-3 INTENT ARBITRATION, AND THIS PR ENACTS NONE OF IT. The diff is evidence and registers only: one research memo under docs/research/, one test file under tests/, one row in docs/DOCUMENT-INDEX.md, one row in docs/claude/work/MANAGER-CHECKLIST.json, one appended row in docs/claude/work/PIPELINE.jsonl, and this declaration. NO src/, NO config/, NO deploy/, NO scripts/, NO .github/workflows/. No set-env was run, config/accounts.yaml and config/strategies.yaml were not touched, and nothing about what the live trader does changes when this merges. All production access was read-only: one GitHub issue-comment read (#12708) and two unauthenticated HTTPS probes that returned 404/401. THE FINDING IT CARRIES IS WHY THE HOLD MATTERS: arming ARBITRATION_FANOUT_ACCOUNTS for bybit_1 alone makes routing STRICTLY NARROWER than leaving it unarmed, because pipeline.py dispatches the fan-out as `if _rounds: <per-round> else: <global>` and scope_round_to_accounts intersects each round's accounts with the allowlist -- so bybit_2 (real money) and bybit_portfolio are DELETED from rounds they had already won. Measured on the live soak row 2026-09-21T20:30:08Z ETHUSDT and reproduced against the real source in the added test (8 passing). The three remedies -- widen the allowlist, fix the scope in aggregate_intents, or set ARBITRATION_FANOUT_MODE=annotate -- are PROPOSED with exact parameters and a before/after table, and are for the operator to choose and the manager to land. The test asserts TODAY'S DEFECTIVE BEHAVIOUR on purpose, labelled as such, so the eventual fix has to flip it consciously rather than silently.",
  "hold_text": "E33 lane dispatch, verbatim: \"Tier 3. You do not land this. Produce the exact diff or the exact set-env parameters, with the before/after and the measurement behind it. The operator approves; the manager lands.\" Also: \"Do NOT subscribe to your own PR and do not idle watching it. Push, open a draft PR with landing: hold / hold_reason: tier_2_3_needs_approval, update row E33, STOP.\"  [!] HOLD DISCHARGED 2026-09-22 BY THE MANAGER, and recorded here rather than merged silently past a `hold`. The operator was offered the three remedies and chose (a) \"Widen the allowlist now\". The manager then ran it as a Tier-3 production change through `system-actions`, READING FIRST: get-env run 35718127262 (process 'bybit_1', declared 'bybit_1', agreeing, no pending_restart — which resolved the lane's open caveat that the live allowlist might hold Alpaca accounts it could not see; it did not), set-env run 35718253767, read-back run 35718365883, then extended to all 11 accounts on the operator's \"yes, we need to add it for alpaca too\" — run 35718739585, read-back 35718808115, process == declared, Post-restart ict-trader-live.service state: active. So the Tier-3 act this PR declined to perform has been performed, approved, and verified on the running process, and what remains in this diff is exactly what it always was: evidence, tests and registers. DEPLOYED, NOT OBSERVED — row E33 is `landed_unproven` and names the two observations that close it."
}
