{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "operator_asked_to_hold",
  "hold_text": "⚠️ THE TOKEN IS IMPERFECT AND DISCLOSED AS SUCH, the same way MI-95's was: the instruction is from the MANAGER SESSION running the merge queue, not the operator directly, and the closed vocabulary has no token for 'a manager asked'. Quoted from the MI-102 dispatch: 'YOU ARE NOT AT THE FRONT OF THE MERGE QUEUE. MI-92's #10916 is position 1 now, #10920 position 2. So BUILD, COMMIT and PUSH -- but do NOT rebase repeatedly against a moving main and do NOT expect to merge until I promote you.' Holding is therefore not a quality judgement on this PR; it is this PR's own subject matter applied to itself. The queue says position 3, so it waits. R12's condition was CHECKED and is NOT met: the diff touches scripts/ci/check_manager_scope.py and CLAUDE.md, neither of which is on check_pr_landing.py's LANDING_MACHINERY list.",
  "why": "Tier-1 by the canonical doc's own list (CI scripts, docs, *.md). Canonizes the three manager duties the operator ruled on 2026-09-03. Adds R7 (check-in cadence) and R8 (merge-queue invariant) to the existing manager-scope guard, seeds docs/claude/work/MERGE-QUEUE.json -- which was cited as the state of record and existed on no branch and in no commit -- and records duty 3 as a DELIBERATE non-mechanization with its reasoning. Self-test grows 13 -> 19 cases, every new rule with a plant AND a control. Two findings filed rather than fixed. No src/, no order path, no strategy config, no risk caps, no deploy/, no live-VM runtime file; merging adds two CI rules and one register file and changes no runtime behaviour."
}
