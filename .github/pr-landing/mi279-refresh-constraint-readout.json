{
  "tier": 1,
  "landing": "self",
  "why": "Re-runs scripts/ops/constraint_readout.py --write and lands its two GENERATED outputs (docs/claude/READOUT.md, docs/claude/CONSTRAINT.json) plus the GENERATED CLAUDE.md session brief and one OPEN-ITEMS observation. Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs and registers only. No src/, no config/, no deploy/, no workflow, no scripts, no order path, no VM mutation -- the script itself is UNCHANGED and was only executed. R12 does not apply; none of these files is in LANDING_MACHINERY. WHY THIS IS WORTH A PR AT ALL: the session brief is the one surface that reaches a session before its first tool call, and it has been rendering a readout generated 2026-09-08 that says 49 of 682 objects (7.2%) carry an ASSESSED blocked_on basis. Re-measured over the same population definition -- docs/claude/work/objects/*.yaml, verified against constraint_readout.py::_OBJECTS in the source rather than assumed -- it is 85 of 179 = 47.5%, because a disposition sweep cut the object count. So every session has been reading a number wrong by roughly six times, and drawing the opposite conclusion from the true one: the verdict is still insufficient_basis, but the store is 2.5 points from the 50% floor rather than 42.8. CLAUDE.md's own text instructs this: the readout 'is a dated snapshot, not a live read -- re-run the script rather than trusting its age'. --self-test passes ALL PASS before the write. THE ROW IT ACTS ON IS NOT CLEARED and the observation says so: OI-20260901-CONSTRAINT-READOUT-... wants a session to WRITE a true blocked_on edge, and I deliberately wrote NONE, because the objects my measurements bear on already carry correct edges and inventing one to cross the floor is the exact harm that row names. The observation records that the temptation is now material and leaves it as a warning rather than an invitation."
}
