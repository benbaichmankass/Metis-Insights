{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U3 (class-first work on the high-severity rows). Docs only: ONE appended `detail` on the existing open row BL-20260902-FIVE-MONOLITHIC-JSON-REGISTERS-RECONFLICT-EVERY-SIBLING-PR-ON-EVERY-MERGE, written through backlog_append.update_row (1 insertion / 1 deletion, byte-local). No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY AN UPDATE AND NOT A NEW ROW: the duplicate probe returned this row at 0.73 overlap and it is the same finding, so per CLAUDE.md the duplicate is dropped and the existing row is updated instead. WHAT THE UPDATE ADDS, none of which the row had: (1) its criterion (b) -- a row-id merge driver -- is now BUILT AND ARMED and I used it ten times today, so a reader could reasonably close the row on it; the update records why that would be wrong, since a merge driver is client-side and GitHub does not run it, so the PR still grades dirty and (b)'s own wording, 'two appends never conflict textually', remains false where it counts. (2) The churn is converted from an anecdote into a rate with a denominator: 59 inter-commit gaps on origin/main touching that register over 5.7h, median 5.0 min, against a MEASURED 2m45s full-green CI cycle taken from PR #12044's six check runs -- so 42% of gaps are shorter than one CI cycle. (3) It states the shape honestly as a race won about half the time rather than a deadlock, because calling it a deadlock invites an over-engineered fix, and it names the observed split that the rate predicts: five PRs merged first try this afternoon while three needed three resolve rounds each. (4) It records that the cost is the CI RESTART rather than the resolve, so resolving faster cannot help -- the same arithmetic that already defeated the shared merge_slot field, whose per-branch remedy does nothing here. The row stays OPEN and its real remedy is unchanged: one file per row, as docs/claude/work/objects/ already does."
}
