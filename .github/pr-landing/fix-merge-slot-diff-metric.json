{
  "tier": 1,
  "landing": "self",
  "why": "MAIN IS RED AND THIS IS THE FIX. `tests/test_claim_merge_slot.py` fails on clean `origin/main` (2 of 11), so `pytest-run` — a required check — fails on every open PR that does not itself rewrite the merge slot. ⚠️ THE GUARD IS FAILING ON CORRECT BEHAVIOUR, which is why this is a repair and not a loosening. Its size assertion measured `sum(1 for x, y in zip(before, after) if x != y) + abs(len(a)-len(b))` — a POSITIONAL comparison of line i to line i — so a claim with a different LINE COUNT from the one it replaces shifts every following line and reads as a whole-file rewrite. MEASURED on origin/main: the outgoing slot carried 6 keys (a manager hand-added `found_stale` and `concurrent_sibling` on #11624), `claim_merge_slot.py` writes 4, the board went 94 -> 91 lines, and the metric reported 87 changed against a budget of 6 — while `test_real_board_only_the_slot_changes` passed, proving the bytes on BOTH sides of the slot were preserved, and the real git diff was 11 lines. BISECTED rather than guessed: ce02075b0 (my branch pre-merge) 11 passed · 4230ef10b (main then) 2 FAILED · 6efbcb2a4 (a later main) 11 passed · origin/main now 2 FAILED. It flips with whatever shape the live board happens to hold, which is why it went unnoticed. THE FIX: measure a real added+removed diff (difflib) instead of a positional one, and bound it by THE TWO SLOTS' OWN SIZE rather than a fixed 6 — the outgoing slot's size is not ours to choose, which is precisely what broke the fixed budget. Same repair in the script's own `--self-test`, which carries the identical check. ⚠️ THE TEETH ARE PROVEN, NOT ASSERTED — a metric loosened to stop failing on correct behaviour is worthless if it also stops failing on what it exists to prevent. Three new planted controls re-serialise the real board (indent=1, indent=4, indent=2+sort_keys) and each must BREACH the budget: measured 175, 175 and 107 changed against 15. And with `splice()` itself planted to re-serialise the whole file, 5 of 14 tests fail. ⚠️ `indent=2, sort_keys=False` is deliberately NOT a control and the reason is stated in the test rather than hidden: the board is already stored in exactly that form, so re-serialising it reproduces the file BYTE-FOR-BYTE outside the slot — not a reformat this guard misses, but not a reformat at all. ⚠️ AND THE REGRESSION IS PINNED TO A SYNTHETIC BOARD, NOT THE LIVE ONE, because the live fixture destroys its own evidence: the moment any session claims the slot it holds a 4-key claim, the line delta is zero, and the bug becomes unreproducible. A parametrized test builds a board whose outgoing slot carries 0/1/2/5 hand-added keys; restoring the old positional metric fails it. Its fixture asserts `json.loads(src)` before proving anything, and that assertion earned its place immediately by catching my own malformed builder. OUT OF MI-229's BRIEF and said so plainly: my unit was the F-28 registry collapse (#11615, merged). This was found because a stale `check_run.completed` webhook named an intermediate head of my own PR, and chasing it rather than dismissing it as superseded surfaced a red `main`. Tier-1 by path — one test file and one script under scripts/ops/. No src/, no config/, no workflows, no order path; nothing is skipped, disabled or quarantined."
}
