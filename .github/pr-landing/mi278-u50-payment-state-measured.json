{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U50, RESEARCH lane. Tier-1 by R5's surface test: an extension to an existing instrument under scripts/research/, one new file under tests/, one new memo under docs/research/, one additive backlog `updates` entry plus an in-place correction to that row's stale pointer through the row-aware driver, and one DOCUMENT-INDEX row. NO src/, NO config/, NO deploy/, NO workflow, no order path. scripts/ops/dead_leg_audit.py and the two re-derived surfaces were READ and are quoted; none is modified. || NO NEW INSTRUMENT: the payment axis was added to scripts/research/fanout_denominator_survey.py, which is the row's own inventory and where the axis belongs. || WHAT IT DELIVERS -- two corrections to the row's own prose, both measured. (1) Its closing sentence says e35_break_attribution.py 'is the next and is NOT done'; U32 paid it on 2026-09-13, AFTER that sentence was written, so acting on it would rebuild an instrument that already exists. The machine-readable registry was right throughout and the hand-written sentence went stale -- field beats comment, applied to the row's own progress tracking. (2) 'Paid' is the wrong word for two of the three payments: MEASURED, bleed_attribution_2026_09_11.py and e35_break_attribution.py carry ZERO package tokens in any form and were never modified, so a reader who RUNS them still gets the row-denominator rate with no pointer to the companion. That matters because U31's own finding was that the e35 Fisher crosses alpha under a package denominator (8e-05 -> 0.05703) and the surface still prints the significant one. || A FALSE FINDING WAS NEARLY FILED AND IS RECORDED RATHER THAN QUIETLY FIXED: a narrow probe on the order_package_id COLUMN grades dead_leg_audit.py a false declaration, while the file counts packages via the order_packages TABLE. The declaration was right and the probe was wrong. The widened probe has zero false positives and the narrow form is now a NAMED regression control in both suites. || AND AN OVERCLAIM IN MY OWN FIRST DRAFT WAS CAUGHT AND CORRECTED: it named the positive state `paid_in_place` and graded 6 of 21 surfaces with it, contradicting the module's own documented asymmetry -- a whole-file token match is necessary and NOT sufficient. Renamed package_notion_present_rate_unverified, with a test asserting the old name cannot return. || CLAUSE (1) IS NOT CLEARED and nothing claims it is; three of the 21 exposed surfaces are live SPA routes and remain Tier-2. || VALIDATED BEFORE PUSH on committed state: 12 new self-test controls (survey self-test OK), 14 pytest controls, 4 planted defects all caught by a named control, ruff clean, and the guard set run on committed state."
}
