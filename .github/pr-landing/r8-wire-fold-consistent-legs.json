{
  "tier": 3,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "why": "TIER-3 REAL-MONEY ROSTER ADDITION, TWO LEGS. config/accounts.yaml: bybit_2 and bybit_portfolio 3 -> 5, identical and same-ordered, plus AVAXUSDT added to both symbol lists (without it avax_pullback_2h would sit on the roster and never trade). config/strategies.yaml: avax_pullback_2h execution shadow -> live. Operator approved 'wire 2 and investigate the other 2' after the demotion history was surfaced. ada_pullback_2h is clean (4/4 folds, +21.93R, n=44). avax_pullback_2h was demoted 2026-07-22 BUT that demotion stated its own re-promotion condition -- 'a 4-6wk shadow re-eval is expected' -- which is met: 2 months, +12.6729R over n=45, 3/4 folds. So it is a discharge, not an override. fade_breakout_4h and htf_pullback_trend_2h are NOT wired and remain execution: shadow, held for row E39.",
  "hold_text": "⚠️ BLOCKED BY A DEFECT IN THE GUARD ITSELF, NOT BY THIS CHANGE. scripts/ci/check_roster_promotion_evidence.py exits 1 on avax_pullback_2h with `identity FAIL: config_fingerprint ... STALE` while C1/C2/C3/C4 all PASS. MEASURED: the record's digest matches the COMMITTED config exactly, and the ONLY field this PR changed on that leg is `execution: shadow -> live` -- which alone moves the digest, because config_fingerprint digests the whole config block including the execution gate. THEREFORE EVERY PROMOTION INVALIDATES ITS OWN EVIDENCE, and B1 has permitted none since it landed 2026-09-21. Row E40 carries the fix (fingerprint the strategy parameters, not the gate) and the two wrong fixes not to take. THIS PR SHOULD NOT MERGE UNTIL E40 LANDS -- not because the wiring is wrong, but because merging it red would either require overriding a required check or would land a leg whose guard says its evidence is stale.  [!] HOLD DISCHARGED 2026-09-22, both halves, and recorded here rather than merged silently past. (1) THE APPROVAL: the operator approved the wiring — 'wire 2 and investigate the other 2' — and then 'Yes push it' on the E40 fix specifically. (2) THE DEFECT: E40 is now IN THIS PR, so the guard it describes no longer refuses it. config_fingerprint digests the strategy PARAMETERS, excluding _GATE_FIELDS = {execution, enabled}, in both the producer and the guard; the 52 records were migrated in a way that PRESERVES the staleness signal (a record whose params had genuinely drifted keeps its old digest and still reads STALE). Verified: the guard now reports `clean (2 promotions)` against the COMMITTED tree with both legs `armed ... [roster_add]`; --self-test PASS including 'P6 stale config_fingerprint fails identity' and 'P6 ...while all four clauses still PASS (so identity is load-bearing)', so the failure branch stays proven; the two implementations agree over all 55 config blocks; and controls confirm a gate flip does NOT move the digest while a moved atr_stop_mult DOES. (3) Full guard suite on this branch: PASS 64 / FAIL 0. The three guards that tripped on the first green-CI attempt were all genuinely this change's — a leg going live moves it into three derived denominators — and each was fixed by recording what moved, with the tp_geometry ratchet raised 229 -> 235 ONLY after measuring that all six added cells are avax_pullback_2h's own."
}
