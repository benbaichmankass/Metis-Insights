{
  "tier": 1,
  "landing": "self",
  "why": "Docs + registers ONLY, three files: the generated CLAUDE.md SESSION-BRIEF re-render, one OPEN-ITEMS.json row's `observation` + `verified_at`, and one work object's `blocked_on` (removing an edge that the merge of #11817 discharged, so it is no longer true). No src/, no config/, no tests/, no scripts/, no workflow, no order path, no strategy, no risk cap, no runtime. Tier-1 by the canonical doc's own list.",
  "landing_note": "THE PREVIOUS DECLARATION ON THIS PATH IS SUPERSEDED, NOT CONTRADICTED, AND THE DISTINCTION MATTERS. It read `landing: hold` / `hold_reason: changes_landing_machinery` and was CORRECT for PR #11817, which rewrote scripts/ci/check_pr_landing.py (+691/-10) and created docs/claude/work/approvals/. #11817 MERGED at 2026-09-12T06:55:51Z (squash 2cd829991, merged_by benbaichmankass) under exactly that hold -- a human read the rule itself, which is the invariant, not a workaround. This branch was then restarted from origin/main per the standing merged-PR rule (a merged PR is finished and must not be reused), so its diff no longer touches any landing machinery and the hold no longer describes it. pr-landing-guard R8 caught the stale claim -- `claims changes_landing_machinery, but no changed path is landing machinery` -- and the guard was RIGHT; this file is the fix, not a weakening of it. Verified against the recognised list rather than by eye: none of CLAUDE.md, docs/claude/OPEN-ITEMS.json or docs/claude/work/objects/*.yaml is landing machinery, and docs/claude/work/approvals/** (which IS) is untouched -- zero approval records exist on main, re-measured with `git ls-tree -r --name-only origin/main -- docs/claude/work/approvals/`, which returns README.md alone."
}
