{
  "tier": 1,
  "why": "Tier-1 by the canonical doc's own list: the entire diff is CI guard logic under scripts/ci/ (check_pr_landing.py, check_manager_scope.py) plus this branch's own landing declaration and its merge-slot claim. There is no src/, config/, deploy/, ml/ or workflow change, no order path, no strategy parameter, no risk cap and no live-VM runtime file — nothing that can reach a trade. Tier is not what holds this PR; R12 is, because the diff edits the landing machinery and must be read once by a human before it lands.",
  "landing": "hold",
  "hold_reason": "changes_landing_machinery",
  "hold_text": "This PR edits scripts/ci/check_pr_landing.py — the landing machinery — so R12 forbids it from self-landing, and R12 is right: the change and its own approval would be the same act, and a mistake in it would disarm the check that would have caught the mistake. It therefore needs ONE human merge click, and the guard is not being weakened, marked or excepted to avoid that. ⚠️ THE COST IS REAL AND MUST NOT BE UNDERSTATED: `main` is RED right now (`python3 scripts/ci/check_board_coherence.py --all` exits 1 on clean `main` at a0ec22ce), so the `guards` job fails on EVERY open PR in the repo until this merges, and nothing can land itself out of that state — by construction, because the repair touches the landing machinery. This PR is the highest-priority merge in the repo. The diff is Tier-1 by the canonical doc's own list — CI guard logic under scripts/ci/ plus this branch's own landing declaration and merge-slot claim. No src/, config/, deploy/, ml/, workflow, order path, strategy params, risk caps, or live-VM runtime file. It fixes two defects, one PR, deliberately: they are the same guard family and splitting them leaves main red longer. (1) board-coherence R3 fails because #11334 added the rule and #11344 added a hardcoded retired board number at check_pr_landing.py:542; each was green alone, red together. The number now resolves through scripts/ci/board_pointer.py and the remedy still names where to post. (2) R13 and R2 were mutually unsatisfiable for the manager; a merge-slot claim naming the commit's OWN branch is now graded as a landing act, decided per-commit by content rather than by adding the path to MANAGER_SURFACE. ⚠️ The `merge_slot` claim in docs/claude/session-board.json is KEPT even though a held PR does not need one (R13 grades only armed branches): a merge really is coming and the claim is the durable record of it. Whoever merges this should clear it afterwards."
}
