{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U44, RESEARCH lane. ONE backlog row appended to docs/claude/health-review-backlog.json via backlog_append.py::append_row (19 lines added, 0 removed). NO code, NO src/, NO config/, no runtime path, no .gitattributes change. Tier-1: docs/** is inside TIER1_SURFACE and filing a backlog row is explicitly permitted with no cap. IT IS FILED TO PREVENT A CHANGE, NOT TO REQUEST ONE, and that is why it self-lands: the safest possible outcome of this unit is that the row exists before anybody reaches for the one-liner it refutes. THE MOTIVATING COST IS MEASURED: one work object -- docs/claude/work/objects/WO-20260912-M20-ACTIVE-TRADE-MANAGEMENT-HOLD-WINNERS-LONGER.yaml -- caused roughly TWELVE conflict resolutions in this session across ten sibling branches, because every unit appends its own key to one shared `progress:` block, so any merge to main moves that block and every open sibling conflicts on adjacent lines. THE OBVIOUS FIX PASSES THE OBVIOUS TEST. With `docs/claude/work/objects/*.yaml merge=union` set, merging origin/main into a genuinely conflicted branch returned rc=0, left no markers, parsed as YAML, and yielded all eleven progress keys with no duplicates. AND IT IS UNSAFE. MEASURED on a purpose-built repository covering the case the first test does not exercise -- both sides ALSO editing a scalar. Base `lifecycle: in_flight`; one side sets `done` and appends U2, the other sets `waiting` and appends U3. Git exits 0 and reports a CLEAN merge; the file carries DUPLICATE `lifecycle`, `owner` and `progress` keys, which yaml.safe_load accepts; the reader takes the LAST, so `lifecycle` silently reads `done`; and `progress` reads ['U1','U2'] -- the other side's U3 entry is SILENTLY LOST despite its text being physically present in the file. The union destroys exactly what it exists to preserve while reporting success, and the loss is invisible in the DIFF too because a reviewer sees both entries there. That is the BL-20260814-HAND-RESOLVED-BACKLOG-MERGE-SILENTLY-REVERTED-SIX-ITEMS-INCLUDING-A-RESOLUTION class. THE GENERAL LESSON IS RECORDED RATHER THAN JUST THE INSTANCE: the real-file test only exercised the APPEND-ONLY case, which is the case union handles. A positive result on the benign input is not evidence about the dangerous one, and it took a constructed adversarial fixture to find the defect -- this session nearly shipped the one-liner on the strength of the first green. THE CRITERIA CANNOT BE SATISFIED BY THE THING BEING PREVENTED: the row closes on either a work-object driver that REFUSES a same-scalar edit (with a planted control reproducing the case above and asserting the merge FAILS rather than exiting 0), or a RECORDED decision to keep plain three-way merging and accept the conflicts -- and it states explicitly that adding `merge=union` does NOT close it, nor does the conflicts becoming rarer as this workstream finishes, since the mechanism is one shared append block that returns with the next multi-unit workstream. NO DANGEROUS LOCAL STATE IS LEFT BEHIND: the `.git/info/attributes` entry set for the experiment was removed and `git check-attr merge` re-read as `unspecified` on the real file -- verified, not assumed, because a stray union attribute in a working clone would silently do to a future merge exactly what this row describes. VERIFICATION: full guard set on COMMITTED state -- PASS 68 / FAIL 1, the one being pr-landing, which is the deliberate second push. check_backlog_refs, check_backlog_criteria and check_claim_basis all clean."
}
