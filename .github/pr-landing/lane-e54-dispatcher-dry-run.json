{
  "tier": 1,
  "landing": "self",
  "why": "E54 (checklist row E54, ceiling $30) was dispatched to read one real dry-run report against the live research queue and establish whether the R5 dispatcher's three spend gates hold before it is armed. MEASURED this session: the dispatcher is NOT unarmed -- the operator personally armed the schedule branch on 2026-08-30 (commit 8183a387d) and it has fired for real on every 06:20Z cron since (runs #20-#34, all event=schedule; 5 queue units carry a real last_dispatched_at stamp). Today's dry run (byte-identical queue to the live cron's in-progress run #34) shows 3 of 11 live units would_dispatch, all CPU-only (route_state=runner) to e35-bracket-sweep.yml, which lands via commit-to-main + assert_rows_landed.py. The ledger preflight gate was verified directly with a real fixture ledger (positive refusal + negative-control clear, $0 spent); the per-run cap is proven by the repo's own existing test. So the task's premise (an arming change to prepare-and-hold) does not apply -- there is no code/workflow change to make. The only remaining work is correcting ROADMAP.md's stale M40 paragraph (which still reads 'deliberately NEVER FIRED') and filing the finding + the one honest residual gap (the GPU path itself -- 0 of 18 queue units need GPU, so GPU_BURST_ARMED's live value has never been read by any session) durably. This diff is docs/pipeline only: ROADMAP.md (*.md, TIER1_SURFACE) is a dated append-style correction to the M40 cell, matching that cell's own established convention; docs/claude/work/PIPELINE.jsonl (docs/**, TIER1_SURFACE) gains two new append-only rows (PI-20260924-5V6UVR9J-0001 done, -0002 queued/observation) written via scripts/ops/pipeline.py's own append() validator, `--check` exits 0. No src/, config/, deploy/, or workflow file is touched -- none of the five HOLD_REASONS is true of this PR (it changes no landing machinery, depends on no unmerged PR, is not awaiting an observation that hasn't landed yet, no operator asked for a hold, and it is not Tier-2/3 work), so declaring hold here would be the false hold_reason CLAUDE.md and MI-226 both warn against."
}
