{
  "tier": 1,
  "landing": "self",
  "why": "One appended row in docs/claude/health-review-backlog.json. No src/, no config/, no tests, no workflow, no order path -- Tier-1 by docs/CLAUDE-RULES-CANONICAL.md § Permission Tiers, which says such work needs no human OK to merge. The diff is `11 added / 0 deleted` on that one file (plus this landing machinery), appended through scripts/ops/backlog_append.py so the file's exact serialisation round-trips and no other row is reformatted or re-attributed. THE BRANCH NAME IS NOW MISLEADING AND IS KEPT DELIBERATELY: it was opened to deduplicate a repeated register id that had reddened register-id-guard on every open PR, and that deletion was landed first by another session on PR #12381 (squash e8b15e138). Re-measured on origin/main 4454148f9 the register reads 1600 rows with ZERO duplicated ids and register-id-guard exits 0, so this branch's original diff would now delete a legitimate row; it was reset onto main and abandoned rather than landed. Renaming the branch would orphan PR #12383 and its comment record, which is the more expensive confusion. What remains is the finding the episode surfaced and which #12381 did not file: the guard prints 'Mint a fresh id for the newer row' for EVERY id repeat, and for two IDENTICAL rows that remedy would manufacture a second backlog row asserting a finding that already exists -- wrong in the direction that ADDS noise to the register it protects. The guard is NOT changed here: main should stay green, and branching the message on row equality is its own unit."
}
