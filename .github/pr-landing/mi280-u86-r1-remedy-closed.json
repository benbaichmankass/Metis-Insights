{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, DOCS-ONLY: one file, docs/claude/health-review-backlog.json -- one row closed and one row filed. No code at all. No src/, no config/, no deploy/, no scripts/, no workflows, no order path, no strategy config, no risk caps. R12 does not apply: nothing in LANDING_MACHINERY is touched. WHAT IT DOES: closes BL-20260912-DOCUMENT-INDEX-GUARD-R1-PRESCRIBES-A-65-FILE-WRITE-TO-FIX-A-ONE-ROW-REGISTRATION-THE-SAME-TRAP-ITS-OWN-DOCSTRING-ALREADY-NAMES-FOR-THE-DATE on its criterion as written, MEASURED rather than taken from the row's own last update: a scratch worktree at origin/main, ONE new document added so R1 fires, then R1's remedy followed exactly. R1 now names ONE line (the generator's own stub) and inserting it reached `document-index: OK` on a total diff of 2 files / 7 insertions -- the new document and one index row, nothing the change did not otherwise touch. An intermediate red on R3 UNSTAMPED was a DIFFERENT rule firing correctly on the probe document's missing header, not the remedy failing. THE 65-FILE FIGURE IS NOT REPRODUCIBLE TODAY FOR A REASON UNRELATED TO R1 -- R6's residue was drained to zero, and it will regrow -- so the closure does not rest on the number shrinking. It rests on R1 no longer PRESCRIBING `--write`: it names the one line and mentions the command second with an instruction to read its diff, which is a different prescription rather than a documented hazard (the thing the criteria rule out). AND THE ROW'S SECOND FINDING, which its own criteria do not cover, is filed separately rather than buried: BL-20260913-SESSIONS-HAND-WRITE-A-VERIFICATION-BASIS-INTO-THE-DOCUMENT-INDEX-THAT-THE-GENERATOR-DOES-NOT-OWN-AND-SILENTLY-RESETS-ON-THE-NEXT-WRITE. MEASURED 35 -> 0 -> 1 (a session re-added one after the reset, so the convention is live and the loss recurring); DEMONSTRATED that a --write probe on a clean main rewrites exactly the file carrying that surviving basis; and MEASURED that STATUS_EXPLICIT, the declared home, appears in ZERO of the five surfaces a session would look at, with a positive control confirming the probe can find the string elsewhere. The new row REFUSES the obvious fix in advance, because making --write carry such a basis reverses a decision the parent row already recorded. Row integrity: ids 1582 -> 1583, 0 lost, 1 added, exactly 1 existing row content-changed, 0 duplicate ids."
}
