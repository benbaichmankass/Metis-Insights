{
  "tier": 1,
  "landing": "self",
  "why": "Manager registers only: MANAGER-LEASE.json (heartbeat 13:26:03Z, expires 14:56:03Z), SESSIONS.json (supervision written for both lanes after a live get_session read), MANAGER-CHECKLIST.json (MI-267 and MI-268 dispositioned, merge queue refreshed). No src/, no config/, no deploy/, no scripts/, no .github/workflows/, no order path -- every path is inside TIER1_SURFACE. BOTH LANES DELIVERED AND BOTH ARE DISPOSITIONED HONESTLY, WHICH MEANS DIFFERENTLY. MI-268 is `done` -- genuinely done, which is unusual on this board: its deliverable was a REPORT, the report is merged (#11799) and readable on main, so no later observation is owed and `done` is not the collapse of done-into-landed_unproven this repo keeps paying for. MI-267 is `landed_unproven` -- ict-trader-dashboard#216 merged (f6480f6c4) and it establishes exactly ONE of three facts: the client can now SEND a bearer. The host still cannot MINT one, measured by the lane against the LIVE host with a credential-free probe (correct email, deliberately wrong password -> 500 auth_unavailable), and the tab therefore does not render. A green CI run on #216 establishes none of the three and the PR says so itself. THE REVIEW'S VERDICT IS RECORDED AND THE MANAGER STILL DOES NOT ACT ON IT: MI-268 found ONE BLOCKING bug in #11767 -- _latch_key omits the blocker STATE, so a blocker first graded could_not_look never pages when it clears, which is the PR's OWN motivating case -- plus two should-fix and one note, and concluded one short fix round rather than a redesign. #11767 stays unmerged by the manager under R12; whether the fix round goes out is an OPERATOR decision being put to them, not taken here. TWO THINGS WORTH THE NEXT READER'S TIME. The MI-268 reviewer RAN the guard self-tests as instructed (15 planted defects still fail, 6 positive controls hold) and then marked its own green result `necessary and not sufficient`, because the guard is not modified by the diff so an identical result on main is the expected outcome -- a weaker review would have quoted that green as the verification. And the MI-267 lane found a failure mode nobody had named: attaching Authorization makes the requests NON-SIMPLE, so the browser now sends a CORS preflight it did not send before, which would have shipped green and still not worked; it probed the live host and confirmed the preflight passes. A MANAGER CATCH, RECORDED BECAUSE IT IS THE STANDING RULE WORKING: the MI-267 lane reported `#216 green, mergeable` -- both true -- and did not say it was a DRAFT, which never auto-merges. Caught by reading the PR rather than the summary; un-drafted and merged by hand. All three lane slots are now free and the next dispatch is deliberately a DECISION rather than a pick."
}
