{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "unvouchable_paths",
  "why": "R5 correctly refuses this PR: `scripts/check_claim_basis.py` sits outside TIER1_SURFACE (which covers scripts/ci/**, scripts/ops/**, scripts/research/**, scripts/reports/** but not scripts/*.py at the top level), so the guard cannot certify it and self-landing is refused rather than granted by default. That is the guard working, not an obstacle to route around. The change to that file is TWO added lines widening BACKLOGS to include docs/claude/research-review-backlog.json -- the fourth backlog scripts/ops/work_digest.py already reads and this guard did not scan. Measured before adding it: 19 rows, statuses `open` x13 and `resolved` x6, all already in the enum, so it widens COVERAGE and changes no verdict today. NARROWING WAS CONSIDERED AND REJECTED: the coverage control (`test_every_backlog_the_digest_reads_is_enum_guarded`) asserts the digest's backlog set is a subset of the guard's, so splitting the widening into its own PR makes the test fail on this one -- the two are the same fact. A human read of two lines is the smaller cost. Everything else in the diff is squarely Tier-1: docs/, tests/, scripts/ci/, scripts/ops/, and two backlog rows.",
  "hold_text": "Touches scripts/check_claim_basis.py, a CI guard that sits outside TIER1_SURFACE (which covers scripts/ci/**, scripts/ops/**, scripts/research/** and scripts/reports/**, but not scripts/*.py at the top level), so R5 cannot vouch for it and correctly refuses the self-land. The change there is TWO ADDED LINES: docs/claude/research-review-backlog.json joins BACKLOGS, the fourth backlog scripts/ops/work_digest.py already reads and this guard did not scan. Measured before adding it -- 19 rows, `open` x13 and `resolved` x6, all already in the enum -- so it widens COVERAGE and changes no verdict today. Please read those two lines and merge; everything else in the diff is docs/, tests/, scripts/ci/, scripts/ops/ and two backlog rows. Narrowing the PR was considered and rejected: the coverage control asserts the digest's backlog set is a subset of the guard's, so splitting the widening out makes that test fail on this PR -- they are the same fact. The arming file and the merge-slot claim are withdrawn, so nothing auto-merges."
}
