{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, NO BEHAVIOUR CHANGE: three docstrings (scripts/ci/check_register_ids.py, scripts/ops/work_digest.py, scripts/ops/work_phase_ping.py) plus one register file. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps. R12 does not apply -- checked against LANDING_MACHINERY rather than assumed: none of these is in that list. WHAT IT DOES: works BL-20260912-SEVEN-OF-TEN-BASE-READING-GUARDS-COMPARE-AGAINST-THE-TIP-SO-THEY-BLAME-A-DIFF-FOR-WHAT-THE-BASE-DID against its SECOND criterion branch -- 'or carries a stated reason why the TIP is the right reference for it'. THE FINDING: check_register_ids.py's docstring read 'R2 -- identity immutability (diff-scoped against the MERGE BASE)' while _load_at_base does `git show {base}:{path}` on the ref as given -- the TIP. The CODE IS RIGHT and the documentation asserted the opposite; the per-script audit names that guard under 'RIGHT BY DESIGN, DO NOT FIX THESE' because the fork point would MISS a collision with a row main gained after the branch was cut. A session trusting the docstring would have corrected working code into a real defect, and the warning lived in a DIFFERENT FILE. Each tip-reader now carries its reason locally. SIX OF SEVEN now meet the criterion, so the row is NARROWED, not closed. The seventh, scripts/check_claim_basis.py, is outside this lane's allowed paths (scripts/ root) -- the same reason PR #12046 is held. AND THE ROW'S POPULATION IS STALE: base_resolution_census.py reports TWELVE scripts and FIVE tip-readers against the title's 'seven of ten'. The extra member, scripts/ops/check_backlog_refs.py, is covered ZERO times by the audit and is FILED SEPARATELY rather than decided here because it cuts both ways -- MEASURED 3 ids exempted over 6 commits of main, which is under-enforcement on one reading and the avoidance of false blame for a concurrent branch's id on the other. NOTHING MOVED BUT THE PROSE: base_resolution_census.py classifies all five tip-readers identically before and after, check_register_ids.py still exits 0, both --help paths run, ruff passes. Row integrity: ids 1585 -> 1586, 0 lost, 1 added, exactly 1 row content-changed, 0 duplicate ids."
}
