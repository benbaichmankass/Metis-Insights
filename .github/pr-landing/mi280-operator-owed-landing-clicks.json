{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, one row appended to docs/claude/operator-owed-register.json. No code, no order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no workflows. Not landing machinery, so R12 does not apply. Files the landing-machinery CLICK QUEUE in the register whose own header says it holds 'work waiting on a human', as distinct from the backlogs, which hold defects. Three Tier-1 PRs (#11936 #11930 #11958) are prepared, evidenced and green-or-in-CI, and R12 refuses to let any of them self-land — correctly, because the mechanism that opens and arms PRs must not land a change to itself. The board was used first, four times; it is transient. owner_class is `judgement` rather than `defaulted_to_human` because R12 exists precisely to put a person in the loop, so the click IS the confirmation the rule requires, and cannot_automate_reason states the consequence of wiring it: exempting landing-machinery diffs from R12 means letting the arming relay land changes to the arming relay. That classification was checked BEFORE filing, not after, because it decides the escalation shape: `judgement` is graded on AGE (3 days at severity high), not on register carry, so this row cannot red-line every PR in the repo after two register commits — which would be the self-reinforcing repo-wide red this lane has spent the day removing. check_operator_owed: OK, 1 open item, none carried past the limit of 2."
}
