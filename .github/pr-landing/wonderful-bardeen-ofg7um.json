{
  "tier": 1,
  "landing": "self",
  "why": "Manager surface only, and every part of it is management rather than an item: two lease heartbeats (17:51Z, 19:17Z); two APPENDED backlog rows, filing being the act of NOT taking the item; three stopped lane objects moved in_flight -> waiting with their existing blocked_on_basis PRESERVED and a new assessment APPENDED; three new lane objects; three spawn_pending registry rows written BEFORE the spawn and then confirmed against what get_session actually reported; three MANAGER-CHECKLIST rows and the two operator decisions of 2026-09-17; and the generated CLAUDE.md SESSION-BRIEF re-render the registers oblige. No src/, no config/, no deploy/, no tests/, no scripts/, no workflow, no order path. CORRECTED IN THIS PUSH: an earlier version of this field claimed the 2026-09-17 session receipt was committed to docs/claude/receipts/. IT IS NOT, AND IT WAS REMOVED -- manager-scope R2 refuses that path as manager surface (authoring it is the ops lane's item) and artifact-validity refuses the artifact's own shortened backlog ids. Both refusals were correct; the artifact went to the lane in its spawn prompt instead. A declaration that misdescribes its own diff is worse than a terse one, because a guard reads it."
}
