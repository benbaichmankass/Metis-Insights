{
  "tier": 1,
  "landing": "self",
  "why": "One observation recorded into docs/claude/OPEN-ITEMS.json -- the session-end duty CLAUDE.md names, written as a line-level edit (2 lines changed, no reserialisation) because the register is hand-formatted and no json.dumps configuration reproduces it byte-for-byte. No src/, no config/, no deploy/, no tests/, no scripts/, no workflow, no order path. Nothing is cleared or deleted: the row is left in place with its clears_when marked MET so the evidence is reviewable before an irreversible removal."
}
