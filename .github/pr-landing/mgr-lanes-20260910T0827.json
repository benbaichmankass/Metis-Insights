{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "depends_on_unmerged_pr",
  "hold_text": "⚠️ THIS IS NOT WAITING ON AN APPROVAL — it is Tier-1 register content and the hold is purely queue mechanics. R13 requires an ARMED self-landing branch to hold the single-holder merge_slot in docs/claude/session-board.json, and that slot is currently held by PR #11669 (MI-201, session_01UxsiSgFa9dKwBFw5RTbLGA, claimed 09:15:00Z) — an armed self-landing PR that needs it. Claiming it here would take it from a PR that is using it, so this branch CANNOT arm until #11669 merges and releases the slot, which is what depends_on_unmerged_pr names. ⚠️ The contention is not only that PR: this branch was dirtied on that ONE field THREE times, most recently 09:01:17Z by work-digest run 34456533719 claiming it through .github/actions/commit-to-main, costing two rebases and one voided ~18-minute CI run. So rather than re-claim and race an hourly automation for it, the branch RELEASES its claim — docs/claude/session-board.json is now byte-identical to origin/main and this PR no longer touches it — and the manager merges it by hand once green. The arming file is removed in the same commit because R10 fails a hold PR that arms the route; that removal is what makes the declaration honest rather than a label. ⚠️ The slot contention itself is a KNOWN OPEN DEFECT with its own row and is NOT fixed here.",
  "why": "Tier-1: one register file, docs/claude/work/MANAGER-CHECKLIST.json, plus docs/claude/work/SESSIONS.json (the supervision record). No src/, tests/, scripts/, config/, deploy/ or .github/workflows/ — nothing here can reach a runtime, an order path or a strategy. Contents are management acts: two filed rows (filing is explicitly the act of NOT taking an item) and a lane tag on fourteen rows, recording an operator direction. The CLAUDE.md and role-pack half of that direction is deliberately NOT in this diff — it is a WORKER path for manager-scope-guard, and it is routed to a session as MI-244."
}
