{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling. scripts/ci/check_scope_overlap.py (the scope-overlap AUDIT, which is non-blocking and gates nothing by its own design), one new test file, and one backlog row updated. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps. NOT IN LANDING_MACHINERY, and that was checked rather than assumed: this module is the audit that COMMENTS on overlap; check_pr_landing.py is the landing machinery, and this diff only IMPORTS from it (branch_slug, LANDING_DIR, AUTOMERGE_DIR, BRANCH_SLOT_DIR) without changing a byte of it. R12 therefore does not apply and this may self-land. WHAT IT DOES: a declaration that matches a changed file ONLY as a directory prefix no longer reports a collision when that file is this branch's own single-writer file under .github/pr-landing, .github/pr-automerge-requests or .github/merge-slots — directories whose files are named after one branch and which were SPLIT out of shared files precisely to make collision impossible. Measured on #11896: 7 of 11 reported rows were structurally impossible. THE TRUE POSITIVE IS PRESERVED THREE WAYS and each is asserted: another branch's slug file stays reported; an EXACT declaration of this branch's own slug file stays reported (only the prefix match is suppressed); an unknown branch is never an exemption. README.md in such a directory is explicitly not per-branch. The suppression is COUNTED in every assess() return including the refusal paths and is stated in render()'s footer, so over-reach is auditable. CONTROLS: 17 tests plus 18 new cases in the module's own _self_test (93 -> 111), five planted defects each verified RED — including the always-True direction, which would swallow real collisions and fails 7 tests and the self-test. All 51 pre-existing tests pass. The self-test cases exist BECAUSE run_guards.py runs --self-test while pytest-run short-circuits. The backlog row is deliberately left OPEN: its criterion is a LIVE audit comment, which no green suite can produce."
}
