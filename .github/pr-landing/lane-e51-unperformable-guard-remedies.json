{
  "tier": 1,
  "landing": "self",
  "why": "CI guard remedy-text fix plus one PIPELINE.jsonl filing: scripts/ci/check_pr_landing.py (R13's error message and slot_claim_state's detail, plus a new self-test case), docs/claude/work/PIPELINE.jsonl (append-only, two new lines), and .github/pr-landing/README.md (its own note about this exact bug, corrected to point at the fix). No src/, no config/, no deploy/, no order path, no strategy logic -- Tier-1 by the canonical doc's own list (CI/tooling/docs). Self-tests and pytest pass locally (scripts/ci/check_pr_landing.py --self-test: 9 positive controls, 34 planted defects; tests/test_pr_landing_tier2_approval.py and tests/test_check_allow_degraded.py: 48 passed)."
}
