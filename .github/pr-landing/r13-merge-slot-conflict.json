{
  "tier": 1,
  "landing": "self",
  "why": "One backlog row appended through scripts/ops/backlog_append.py::append_row (which round-trips the file's exact serialisation), plus this branch's own landing declaration and merge-slot claim. Verified as a diff rather than asserted: 13 lines added, 0 deleted, exactly one new id, zero removed. FILING ONLY -- R13 itself is NOT touched, deliberately: scripts/ci/check_pr_landing.py is on the LANDING_MACHINERY list, #11817 is open against that same machinery and held under R12, and changing a landing rule while its own review PR is in flight is how the two diverge. Tier-1 by the canonical doc's own examples (docs + registers). No src/, config/, deploy/, .github/workflows/, scripts/ or tests."
}
