{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling: scripts/ci/check_canonical_doc_coherence.py, one new test file, one backlog row closed. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps. NOT IN LANDING_MACHINERY (that is check_pr_landing.py and claim_merge_slot.py), so R12 does not apply. WHAT IT DECIDES: the row asks that somebody DECIDE the conflict-marker scan's repo-wide scope rather than leave it unstated. Decided as a DECLARED PATH SET WITH A VERIFIED OVERRIDE — every prose document a session may read: ACTIVE_DOCS + ROADMAP.md + every SKILL.md and command + all of docs/**/*.md, deduplicated, 46 -> 1,089 files. WHAT DECIDED IT: the instruction hierarchy's own LEVEL 4. Levels 1-3, 5 and 6 were covered and docs/sprint-logs/ (309 files) was not, so 'the current sprint log' could carry both sides of a contested edit and pass every guard in the repo. MEASURED BEFORE EXTENDING, because the row says the FALSE POSITIVE is the hard half: 0 of 1,052 tracked docs flagged; 10 files mention a marker in prose and NONE at line start in tracked content (the one repo-wide hit is a git-ignored .pyc); 0.14s for 14.7 MB. WHERE IT STOPS AND WHY, verified rather than asserted: workflows, config and scripts are NOT added because a marker there SELF-REPORTS — planting one yields ScannerError (YAML), SyntaxError (Python), JSONDecodeError (JSON). Markdown is the one format that parses fine while corrupt. THE OVERRIDE IS VERIFIED IN BOTH DIRECTIONS: a marker on an undeclared line in an annotated file still fails, and a declared line carrying no marker is itself a finding. It ships EMPTY, the measured state, so nothing is excused today. CONTROLS: 15 tests, five plants, three red immediately. TWO ABSORBED AND BOTH RECORDED rather than dropped — un-anchoring the regex alone is inert because the call also anchors (with both applied, three tests go red), and the unreadable-branch plant WAS genuinely absorbed because my test drove the predicate one layer below the reporter; a new test drives an undecodable file through the reporter and the re-plant reddens."
}
