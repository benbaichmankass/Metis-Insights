{
  "tier": 1,
  "landing": "self",
  "why": "Record correction only \u2014 two files under docs/claude/, no src/, no tests, no config, no order path, no workflow, no landing machinery. Tier-1 by the canonical doc's own examples (docs + retrieving and analyzing state). It corrects records THIS unit wrote and that its own merge made stale: the OPEN-ITEMS row still read 'WRITTEN, GREEN AND UNMERGED' after #11279 merged as 12b5b84, which is stale in the dangerous direction (a session would read the change as still awaiting approval and could re-propose it, or believe the partial-fill decision is still owed when the operator named `strict`). The work object still carried an operator_decision blocked_on edge that is now discharged, and a satisfied edge is read by the constraint computation as a live blocker \u2014 CLAUDE.md is explicit that a false blocker is worse than a missing one. Neither row is CLEARED: the fleet observation and the MI-140/MI-180 coupling both remain open, and the clears_when now says explicitly that #11279 merging does NOT clear the coupling part. Self-landing because Tier-1 needs no operator OK and leaving a knowingly-false record on main while waiting for one is the worse outcome."
}
