{
  "tier": 1,
  "landing": "self",
  "why": "Manager stand-down at end of session. Two files, both squarely on the manager surface check_manager_scope.py vouches for (docs/claude/work/**): (1) MANAGER-LEASE.json goes to state `released` via scripts/ops/manager_lease.py release -- NOT a hand-edit, and NOT a heartbeat, so R6 does not apply (a release drops heartbeat_at entirely; R6 grades only a commit that ADVANCES it); (2) docs/claude/work/receipts/session_01GHpyXT2qssLJsrvXBQRkQF.json, this session's receipt, generated by scripts/ops/session_receipt.py with its author-completed sections filled. No src/, no tests/, no scripts/, no config/, no deploy/, no .github/workflows/, no order path, no strategy params, no risk caps -- nothing here changes what the live trader does. WHY RELEASE RATHER THAN HEARTBEAT: both PRs of this session (#12580, #12581) are merged and every row it queued (MI-327..MI-334) is DEFERRED on the operator's stated budget constraint until the week of 2026-09-22, so there is no supervision behind this lease. Extending it would be exactly the condition R6 names -- a manager holding a lease longer than its own supervision record is holding a lease it is not using. A release is also strictly better for the 3 BLOCKED and 6 REVIEW_READY lanes this session's live reconcile surfaced: `released` is CLAIMABLE immediately, where lapsing would lock the next manager out for the rest of the 90-minute TTL. THE RECEIPT HONOURS THE OPERATOR'S OWN ANSWER GIVEN TODAY (`Manager session, on demand and at the end of session ... should be part of sprint log`), and it is deliberately only HALF of it: docs/sprint-logs/** is NOT on MANAGER_SURFACE, so a manager commit touching it fails R2. The sprint-log hop is a session's job and is queued as MI-334. Landing this promptly is the point -- a release nobody can see keeps the next manager locked out until the TTL expires, which the tool itself warns about."
}
