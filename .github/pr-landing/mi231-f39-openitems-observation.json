{
  "tier": 1,
  "landing": "self",
  "why": "Registers, generated docs, and ONE tooling self-test repair -- all inside TIER1_SURFACE (docs/**, scripts/ops/**). No src/, no config/, no deploy/, no workflows, so nothing here can reach a runtime, an order path or a strategy. (1) docs/claude/OPEN-ITEMS.json: one row's observation + verified_at, recording that F-39 clause (1) is MET while (2) and (3) are NOT, with the prior observation preserved verbatim and both facts asserted programmatically. (2) docs/claude/operator-owed-register.json: one new row, OO-20260910-F39-REATTACH-EXACT-DIFF-YES-NO, so a pending human decision does not go invisible for three days as a side effect of the monitoring row being correctly re-affirmed; it is filed into a due-list SOURCE and confirmed to surface (operator_owed state=read rows=1). (3) docs/claude/session-board.json: the R13 merge-slot claim, taken over a holder verified TERMINAL rather than assumed. (4) CLAUDE.md: REGENERATED by render_session_brief.py --write, never hand-edited. (5) scripts/ops/constraint_readout.py: its self-test asserted count == 0 against the LIVE operator-owed register, so the first legitimately-open item filed to it turned the suite red -- a register that exists to carry open items must not have a test that fails when one is carried. The input is now PLANTED, matching the discipline that file's own section 5 already states, the INTENT is unchanged, and a POSITIVE CONTROL is added that the live-file form could never assert. MUTATION-TESTED both ways: counting terminal rows as open is still caught, and hardcoding count to 0 is now caught where previously it would have passed. THE RE-ATTACH IS STILL NOT APPLIED: no database written, no order placed, modified or cancelled on any account."
}
