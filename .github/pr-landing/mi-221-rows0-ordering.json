{
  "tier": 1,
  "landing": "self",
  "why": "Docs and registers only. Three files: \u00a7 13 appended to the MI-221 evidence doc under docs/claude/work/ (57 lines), a ONE-LINE surgical splice into docs/claude/OPEN-ITEMS.json, and the merge-slot field in docs/claude/session-board.json (4 insertions / 4 deletions, written by scripts/ops/claim_merge_slot.py). No src/, no config/, no deploy/, no .github/workflows/, no tests, no order path \u2014 Tier-1 by the canonical doc's own list. CONTENT: a MEASUREMENT of what order_monitor.py:8821's `rows[0]` actually returns, taken with the instrument merged in #11548. Population stated: 14 symbol-scoped get_positions queries across all 3 Bybit accounts, 11 two-row, of which 2 are timestamp TIES and are excluded rather than counted as support \u2014 so 9 discriminating observations. Finding: the venue's row order is NOT positionIdx-ascending (bybit_1/ETHUSDT returned idx2 first) and is consistent with updated_time DESC, so rows[0] is the most-recently-updated book, not the live one. THE BUG IS NOT FIXED HERE AND THAT IS WHY THIS CAN SELF-LAND: order_monitor.py:8821 is a live REAL-MONEY order path, MI-221's mandate is reads-only, and the remediation is Tier-2 \u2014 declared `hold`. This is measurement handed to whoever owns OI-20260908, and it sharpens that row's clears_when (a fix must select explicitly on positionIdx and must NOT be validated by 'the live row came back first', a condition that holds today and proves nothing). OPEN-ITEMS.json was edited by a UNIQUENESS-CHECKED single-string splice, NOT by json.dumps: that file uses non-standard indentation and a naive rewrite reformats 450KB and re-attributes thousands of lines to this PR \u2014 the exact failure scripts/ops/backlog_append.py exists to prevent. The anchor was asserted to occur exactly once, the result was re-parsed as JSON, and the diff is 1 insertion / 1 deletion. verified_at on that row is DELIBERATELY NOT ADVANCED: this measured the MECHANISM, not that row's clears_when, and dating it would assert a check nobody made. No order was placed, modified or cancelled on any account; every venue read was a GET. DOCUMENT-INDEX.md is untouched (no new file), so document_index.py --write was not run and remains prohibited. Guards: check_open_items.py OK (66 items), check_backlog_criteria OK, check_pr_landing clears on the arming push; trainer-capture-watch-guard and operator-owed-guard fail in this sandbox only because pytest is not installed and cannot be affected by a docs-only diff. || PR #11554. RECORDED PROCESS ERROR: this PR was opened by claude-pr-automerge under GITHUB_TOKEN because the content and the arming file went out in ONE push, so it was born with no pull_request event and carried only its own open-and-automerge check \u2014 the zero-check-runs trap CLAUDE.md documents, which reads as 'CI has not started' and never would. #11552 was done correctly (open the PR by hand FIRST, push the arming file SECOND) and this one slipped one PR later. Armed by this commit. Noted here rather than silently worked around, because the trap hit six PRs on 2026-09-08 and a quiet workaround is how it hits a seventh."
}
