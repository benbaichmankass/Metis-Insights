{
  "tier": 1,
  "landing": "self",
  "why": "research tooling + read-only diag pulls, entirely inside TIER1_SURFACE (scripts/research/**, docs/**). No config/, no src/, no deploy/, no unit file, no order path, no live parameter. The script makes no network call of its own -- it prints the diag_fetch plan for a person to run."
}
