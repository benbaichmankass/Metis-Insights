{
  "tier": 1,
  "why": "Tier-1 research only: two scripts/research/ modules wired to the single declared rule, one docs/research/ memo, one row in each of two docs/ registers, and one docs/claude/ backlog edit. Every path is inside TIER1_SURFACE. No src/, no config/, no scripts/ outside scripts/research/, no workflows, no unit file, no order path, nothing enacted on the fleet. BOTH edits are PROVEN ADDITIVE rather than asserted: the pre- and post-patch modules were loaded side by side over one 1000-row journal pull and compared key-by-key. stop_width_counterfactual_2026_09_11.py::dose_response comes back byte-identical (0 keys added, 0 changed, 0 removed) because its inline second copy of the rule and the declaration agree on every package; stop_integrity_both_arms.py adds 356 keys with 0 changed and 0 removed, so every rate this repo has published stays exactly as published. The one-owner change removes a FUTURE drift hazard and fixes no PRESENT disagreement, which the memo states rather than dressing up as a fix. NOTE ON THE LANDING FIELD: this declaration read `landing: hold` with `hold_reason: depends_on_unmerged_pr` until PR #12103 merged at 2026-09-12T22:24:11Z. That hold was real — the first cut of this branch was off main and died at import with FileNotFoundError on fanout_exit_unit.py — and it is now spent: the module is on main, this branch was rebuilt from origin/main plus this unit's own diff alone, and the hold fields are removed rather than left standing as a reason that no longer holds.",
  "landing": "self"
}
