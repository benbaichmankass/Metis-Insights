{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, register-only: ONE detail field appended to ONE existing open backlog row via backlog_append.update_row \u2014 1 insertion, 1 deletion, 0 lines reformatted, 1597 -> 1597 rows, no duplicate ids, and the original 2026-08-30 detail verified still present beside the new instance. No src/, no config/, no deploy/, no workflows, no scripts \u2014 not in LANDING_MACHINERY, so R12 does not apply. IT UPDATES RATHER THAN DUPLICATES: backlog_search scored BL-20260830-A-TRANSIENT-RED-BASE-PERMANENTLY-STRANDS-AN-AUTOMERGE-BRANCH at 0.71 overlap and it is open, so this is the same mechanism with a new and much larger live instance. WHAT IS MEASURED: DUE.json on main is four days stale; error-feed-digest.yml, the HOURLY producer writing 78.3% of its commits, is 0 of 22 since 2026-09-13 (19 failure, 3 cancelled, all event=schedule); each run renders, opens its PR and dies at commit-to-main's 30-minute verify-merged wait; and #12363's guards failed on open-items-guard and artifact-validity-guard while its diff \u2014 a generated JSON artifact \u2014 touches neither, which is the action's own case (b), a red base. 15+ automation PRs are open across six producers. TWO DISTINCT BASE-REDS, NOT ONE: the open-items staleness began 2026-09-16 and the stranding began 2026-09-13, so a fix aimed at the later cause alone leaves the class. THE SWEEPER WAS GREEN 12 OF 12 ACROSS THE WINDOW and this record does NOT conclude it is broken: its triage needs a token this container does not hold, so 'reports and does not act' and 'acts and is outrun' are both live with OPPOSITE remedies, recorded as a we-did-not-look rather than resolved by the plausible guess. IT DELIBERATELY ENACTS NOTHING. Operating the merge queue is a manager base duty, and the sweeper's own docstring warns that refreshing an indiscriminately-chosen PR is WORSE than leaving it stranded, because every one has auto-merge armed and landing a superseded register rewinds main. The remedy is written into the row for the manager to take."
}
