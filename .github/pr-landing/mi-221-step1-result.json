{
  "tier": 1,
  "landing": "self",
  "why": "Docs only. The diff is one appended section (§ 12) to the MI-221 evidence doc under docs/claude/work/, and ONE row appended to docs/claude/health-review-backlog.json through scripts/ops/backlog_append.py (16 insertions, exact serialisation round-tripped, no reformat). No src/, no config/, no deploy/, no .github/workflows/, no tests, no order path, no strategy params, no risk caps — Tier-1 by the canonical doc's own list. It records the STEP 1 MEASUREMENT taken with the instrument merged in #11548: /api/diag/bybit_raw_positions on live sha d87db750 (verified git_sha == git_sha_on_disk, restart_pending false BEFORE the read, so this is the new code and not a stale process). Population stated: 5 queries on bybit_2 plus the same instrument across all 3 Bybit accounts, zero query errors. RESULT: Bybit returns BOTH ETHUSDT hedge books for bybit_2 and both are size_raw '0' with avg_price null — so BOTH branches I pre-registered before the read are FALSIFIED, and § 12 records that explicitly rather than retro-fitting the outcome to whichever branch it resembles. NO REMEDIATION IS IN THIS DIFF and that is why it can self-land: the coverage gap it exposes is filed as a backlog row with resolution_criteria naming the observable condition and an explicit positive control, and any widening of a venue read's scope is Tier-2. No order was placed, modified or cancelled on bybit_2 or any account in this session; every venue read was a GET. DOCUMENT-INDEX.md is untouched (no new file), so document_index.py --write was not run and remains prohibited. Guard state: run_guards.py --base main gave PASS 51 / FAIL 4; artifact-validity-guard failed on this row missing resolution_criteria and was fixed and re-verified green in-session ('backlog-criteria guard: OK'); trainer-capture-watch-guard and operator-owed-guard fail in this sandbox only because pytest is not installed ('No module named pytest') and cannot be affected by a docs-only diff; pr-landing-guard clears on the push that carries this file."
}
