{
  "tier": 1,
  "landing": "self",
  "why": "READ-ONLY DIAGNOSTIC WIDENING. It changes exactly one function, src/units/accounts/clients.py::account_bybit_raw_positions — an accessor added 2026-09-09 (#11548) whose ONLY consumer is the token-gated read-only route /api/diag/bybit_raw_positions. It grades nothing, re-arms nothing, opens no order path and cannot refuse a trade. No production reader's behaviour changes: account_open_positions, account_bybit_open_orders, _bybit_position_protection and every protective sweep are byte-for-byte untouched, so nothing that decides a close, a re-arm or an order is affected. Tier-1 by the canonical doc's own list (observability / read paths). WHY IT IS URGENT: the operator is looking at a real-money ETHUSDT position on the Bybit web UI that NO read in this system returns, and the first version of this instrument reported that emptiness as a fact about the venue — an unprovenanced negative. An empty read is evidence about the READ. Three filters, all still live in the production readers, can each hide a real position and this widening defeats all three: (1) settleCoin was HARDCODED 'USDT' so a USDC-settled linear contract was excluded — USDC is now queried too; (2) category came from a single configured market_type (bybit_2 is pinned linear) so inverse/spot/option were never queried — all four are now swept, and an unsupported (category, param) pair RAISES and is recorded as could_not_look, NEVER folded into no_rows; (3) the per-symbol cross-check iterated only the configured roster and NO position read anywhere passed baseCoin — verified by grep across src/, whose only baseCoin hits are marketUnit='baseCoin' on the ORDER path — so baseCoin is now swept per base, unioned from the wallet coin block rather than bounded by the roster, because bounding it by the roster would reproduce the very filter under test. ALSO FIXED: nothing in src/ followed nextPageCursor (grep returns ZERO hits), so every position read in this repo was page-1-only and a truncated page was indistinguishable from a complete one; this follows the cursor under a 10-page safety bound and REPORTS pages_read, next_page_cursor_seen and pages_truncated so a truncated sweep can never read as a complete one. ADDED: the raw UNIFIED wallet block, preserving the venue's own strings — totalAvailableBalance comes back EMPTY on live bybit_2 and an empty string is the venue declining to compute, not a zero, so it is NOT coerced. That block is the filter-independent check: initial margin in excess of what the visible positions require proves a position exists that no list returned, whatever any query says. VALIDATION: 18 assertions in tests/test_bybit_raw_positions_accessor.py, all executed and passing in-session via a monkeypatch shim because pytest is not installed in this sandbox ('No module named pytest'); the suite was rewritten, not merely extended, and now asserts the sweep REACHES each of the three excluded books — a sweep that inherited the filters it exists to test would be worse than no sweep, because it launders them as a negative. One assertion I wrote was itself wrong ((None or 0) == 0 is trivially true, so it could never pass) and was fixed to assert the real property: an unparseable size is counted in NEITHER size bucket. NO ORDER was placed, modified or cancelled on bybit_2 or any account; every venue call in this diff is a GET. DOCUMENT-INDEX.md untouched (no new file), so document_index.py --write was not run and remains prohibited."
}
