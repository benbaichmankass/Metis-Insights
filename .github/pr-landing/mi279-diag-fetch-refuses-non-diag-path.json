{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain U3 — a real CLOSURE, not a re-validation. Three files: scripts/ops/diag_fetch.sh (an ops READ helper; nothing in src/ imports it and it cannot affect the trader), tests/test_diag_fetch_sh.py (+6 cases, all deterministic with a shimmed curl — no network, no VM), and one backlog row moved open -> resolved via backlog_append.update_row. No src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHAT IT FIXES: BL-20260822-DIAG-FETCH-404-BLAMES-PATH-FORM-NOT-WRONG-SURFACE, open for 21 days. diag_fetch.sh hardcodes the /api/diag/ prefix, so a non-diag route like /api/bot/work/checklist built <base>/api/diag/api/bot/work/checklist and 404'd, and the answered_404 branch advised 'check the path form' — the wrong remedy, since the path form was fine and the TOOL was wrong. It now refuses such a path before any curl, exit 2 (usage) rather than 3, because exit 3 means fall back to the issue relay and the relay is diag-only too. THE CRITERIA SAID PRESENCE OF THE NEW STRING IS INSUFFICIENT — BOTH BRANCHES MUST BE DISTINGUISHABLE — AND THAT IS WHAT IS ASSERTED: the refusal tests require the new string present AND 'check the path form' absent, and a control asserts a genuine diag 404 still gets the path-form advice and NOT the new string. Four more controls assert every legitimate form (version, /api/diag/version, api/diag/version, a journal query) is still dialled with no doubled URL. MUTATION-CHECKED: with the script change stashed exactly the 4 refusal cases fail (4 failed, 26 passed); restored, 30 pass. WHY THE CLOSURE IS HONEST: both criteria were EXECUTED this session, not read. Recorded alongside: this session re-derived the entire defect from scratch and was one step from reporting the operator's live Workflow page as a dead route, before the duplicate probe surfaced the row at 0.85 overlap already carrying the identical measurement from 2026-08-22 — a lesson filed correctly and taught nothing for three weeks."
}
