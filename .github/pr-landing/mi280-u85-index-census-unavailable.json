{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 developer tooling: scripts/ci/check_document_index.py plus one register row closed in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps. R12 does not apply -- checked against LANDING_MACHINERY rather than assumed: check_document_index.py is not in that list, which names the automerge/pr-opener/board-post workflows, check_pr_landing.py, check_automerge_trigger.py, session_registry.py, claim_merge_slot.py and commit-to-main/action.yml. WHAT IT DOES: closes BL-20260912-DOCUMENT-INDEX-GUARD-REPORTS-OK-WHILE-ITS-OWN-ROW-DRIFT-CENSUS-IS-UNAVAILABLE. REPRODUCED FIRST on origin/main with a plant CONFIRMED LIVE (a raise at the top of build_rows, asserted to raise before any verdict was read): --all rc=1 FAIL, --base rc=0 OK, unscoped rc=0 OK, all three printing 'row-drift census UNAVAILABLE'. TWO PARTS BY TWO CHANGES. (1) The UNGATED half was ALREADY fixed by #12136 (2c6eacfde), which landed AFTER the row was filed and made an uncomputable census an R6 UNCOMPUTABLE (--all) finding with its own self-test case; the registry runs a --all step, so the mode CI runs already exited non-zero and this does NOT re-fix it. (2) What was left is the two modes a human runs locally, which still printed a bare OK -- exactly where the row's author was standing. The verdict is now a pure verdict(n_findings, drift_state) -> (state, rc, line) with three states never collapsed: fail / ok / ok_r6_blind. THE SCOPED EXIT CODE IS DELIBERATELY UNCHANGED: #12136's own comment argues that asymmetry, that reasoning postdates the row, and it is not overridden -- a self-test case PINS the scoped exit code at 0 so a later tightening has to argue with the comment rather than silently red every local run. CRITERIA AS WRITTEN: the row rules out asserting on the message and asks for the exit status or a distinct graded state. Six controls assert the verdict triple, including one END-TO-END through the real evaluate() and a vacuity control that a computable EMPTY register is silent. FOUR PLANTED DEFECTS, each observed reddening its own control; the fourth also reddened a PRE-EXISTING r6_all control, which is how that control was discovered -- an earlier draft comment claiming none existed was false and is corrected in the diff rather than shipped. Self-test stated population 37 -> 43, with its hand-written term now a named constant carrying the honest note that nothing computes it and that the original eight were taken on trust. Row integrity: ids 1576 -> 1576, 0 lost, 0 added, exactly 1 row content-changed, 0 duplicate ids."
}
