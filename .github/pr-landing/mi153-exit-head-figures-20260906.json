{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 read-only investigation plus a written finding. The diff is three files, all documentation: one new memo (docs/research/exit-head-figure-reconciliation-2026-09-06.md), a dated figure/provenance correction on the MI-146 audit table, and one appended dated clause on each of three `exit_head_ml` cell `ref` strings in docs/research/exit-refinement-coverage.json. NO cell `status` is changed (a status change is Tier-3 and every one of these cells says so; verified by diffing the parsed JSON — exactly 3 (row, key) pairs move, all `exit_head_ml`, and status/tp_geometry/key-set are byte-identical before and after). Nothing is armed. No src/, config/, deploy/, .github/workflows/, order path, strategy parameter, risk cap, or live-VM runtime file. The two Tier-3-adjacent changes this investigation surfaced — restamping tp_geometry to a new `live_parity_uncapped` legend value, and stamping `basis: harness_only` on the three cells — are PROPOSED in the memo and applied nowhere. Guards run green before opening: check_matrix_corpus_agreement, check_matrix_config_agreement, check_matrix_bracket_values, check_stated_population (against the real diff, not an absent one), check_open_items, and m20_coverage_rollup.validate() (0 problems); 46 assertions pass across the six exit-head evidence test modules."
}
