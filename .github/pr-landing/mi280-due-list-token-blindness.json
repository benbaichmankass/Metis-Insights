{
  "tier": 1,
  "landing": "self",
  "why": "MI-280 unit U7 — taken as 'the next open high/critical health-backlog row touching scripts/ci/** or .github/workflows/**', and named so the manager knows which one. Tier-1: .github/workflows/**, scripts/ci/** and docs/ only — no src/, no config/, no deploy/, no order path. R12 does not apply, checked by IMPORTING check_pr_landing.py and printing all 11 LANDING_MACHINERY entries rather than grepping (this session published a false absence from a truncated grep earlier today, so the method matters): neither error-feed-digest.yml nor scripts/ci/check_due_list_token.py is among them. HOW I GOT HERE, because the row I started on was NOT this one. The queue's top critical in scope was BL-20260911-REPLAY-PREGATE-NIGHTLY-HAS-ZERO-SUCCESSFUL-RUNS-IN-ITS-ENTIRE-LIFE-AND-25-MONTHS-OF-ML-PREGATE-EVIDENCE-HAS-ACCRUED-TO-NOTHING. I read the workflow and it is ALREADY CORRECT: its `undetermined` verdict is honest, part (2) of its criteria has a landed fix from MI-279, and part (1) is blocked on trainer-VM MEMORY PRESSURE, which the workflow's own comments say is 'not a knob in this file' and which is outside this lane's scope. So I did not touch it and did not pretend to. What IS in scope is that row's own stated DEEPER FINDING — that nothing noticed 77 consecutive red scheduled runs over 2.5 months — and that is what this PR fixes. THE DEFECT, MEASURED. The live docs/claude/DUE.json on main (generated_at 06:27:30Z) reads red_crons: could_not_read, note 'no GITHUB_TOKEN - cannot query the Actions API', and the same for unlanded_automation. Two of the nine due-list sources are blind. ROOT CAUSE is a last-writer-wins race between THREE producers, not a renderer bug: due-list.yml (token, daily), probes.yml (token, daily) and error-feed-digest.yml — which had NO env: block at all and runs HOURLY at `35 * * * *`. POPULATION: the last 60 commits to DUE.json on main -> 47 (78.3%) by error-feed-digest, 6 by due-list, 5 by probes, 2 other. The token-less writer is the one that WINS, and it overwrites a good render within the hour. THE RENDERER IS NOT AT FAULT AND IS NOT CHANGED: src_red_crons returns could_not_read deliberately — its own docstring says 'Without a token we say so; we never report no red crons'. The refusal is correct. What was broken is that the refusal got COMMITTED and then read as the state of the world. WHY IT MATTERS: red_crons is the F1 class its docstring names — 'a nightly nobody is waiting on' — and is exactly the instrument that would have surfaced replay-pregate-nightly's 77 red runs. It existed, it was correct, and it was blind in production. THE FIX IS TWO PARTS AND THE SECOND IS THE REAL ONE. (a) the missing env: GITHUB_TOKEN on error-feed-digest.yml's re-render step. (b) scripts/ci/check_due_list_token.py, UNGATED (when: None), requiring every workflow step that runs render_due_list.py in any mode but --self-test to resolve GITHUB_TOKEN from its own env, its job's or the workflow's. (a) alone repairs today's three writers and does nothing about the fourth — which is how this happened: nothing tied 'renders the due-list' to 'must carry a token'. It is ungated precisely because a diff-scoped guard cannot see a fourth workflow added months later. --self-test is EXEMPT deliberately: it collects nothing, and requiring a token there would train authors to paste one wherever the guard complains. VERIFICATION: A/B on the real tree — the guard reports 1 finding against origin/main (naming the exact job, step and remedy) and 0 against this branch. 7 planted defects fail it — env block absent · env present but naming a different variable · a step that self-tests AND writes · a --check invocation · the call buried in a multi-line run block · an unreadable workflow · and an EMPTY POPULATION (a guard that checks nothing is not a clean guard) — and 3 positive controls stay quiet: token inherited from the job, from the workflow, and an extra self-test-only step. One positive control was MALFORMED on its first run: it replaced the only collecting step with a self-test, emptying the fixture's population, and the guard correctly refused it with NOTHING WAS CHECKED. The control was wrong, not the guard; it is fixed and the episode is recorded at the line, because a malformed positive control is how a real one gets deleted. WHAT THIS DOES NOT DO, stated rather than implied: it checks the BINDING, never the token's VALIDITY — it cannot tell a real token from GITHUB_TOKEN: \"\". The live half is the sources.red_crons.state field in the committed DUE.json, and NO post-merge artifact has yet been read with red_crons in a `read` state, so this is landed-not-observed. It also does not fix replay-pregate-nightly, and does not claim to."
}
