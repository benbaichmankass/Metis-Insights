{
  "tier": 1,
  "landing": "self",
  "why": "One guard, scripts/ops/check_backlog_criteria.py, resolved through scripts/ci/_git_base.py so its two diff-scoped checks compare against the branch's fork point rather than the base tip. No src/, no config/, no deploy/, no workflow, and no backlog row -- deliberately, to keep the register conflict surface down while this lane has PRs in flight. THE HOLD IS RELEASED, NOT DEFEATED. It was `changes_landing_machinery` because this branch was STACKED on #12094, so its diff carried that PR's own .github/pr-landing/ file -- and that path IS landing machinery, so R12 was RIGHT to refuse. #12094 merged (6f04d12e0 on main) and the stack dissolved. The diff now carries exactly three paths -- this PR's own landing and merge-slot files plus scripts/ops/check_backlog_criteria.py -- verified by listing it, not assumed. No register file (checked: 0 matches), because the row its diff used to show was #12094's, inherited through the stack and already on main via #12127. This PR's own content touches no landing rule at all."
}
