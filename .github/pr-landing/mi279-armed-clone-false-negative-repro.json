{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane. Docs only: ONE appended `detail` on the already-open BL-20260910-AN-ARMED-CLONES-LOCAL-TEST-MERGE-IS-A-FALSE-NEGATIVE-ON-GITHUB-MERGEABILITY, written through backlog_append.update_row (1 insertion / 1 deletion over 1531 rows, id list identical, exactly one row differing). No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY AN APPEND AND NOT A NEW ROW: the class is precisely the one that row names, and a duplicate would be the row backlog_append.py is built to refuse. WHY IT IS WORTH FILING AT ALL GIVEN THE ROW EXISTS: the instance carries a CONTROL the original did not -- the same merge re-run with `-c merge.jsonregister.driver=false` conflicts, which establishes the direction of the error rather than asserting it -- and it records that backlog_search RETURNED this row at 0.67 overlap, I read it, and I mis-attributed anyway. That is evidence the remedy cannot be another warning: what would have prevented it is the tooling printing the driver state at the moment of the test. NOTHING IS FIXED HERE: the proposed remedy touches a local-test helper and belongs to that tool's owner; this lane files the evidence and the argument rather than taking the item."
}
