{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5. Registers only: one appended `observation` plus `verified_at`/`last_checked` on OI-20260831-RESEARCH-READ-DEBT-11-UNREAD-AND-256-SUPERSEDED-UNREAD, and the session-brief re-render a register change obliges. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY THE ROW STAYS OPEN RATHER THAN CLEARING: the measurement makes clause 1 HARDER to meet, not easier -- the eleven units the row named were superseded on 2026-09-10 without ever being read, so they can no longer carry a terminal verdict at all, which the row's own clears_when names in terms as NOT sufficient. Clause 2 (the 103-vs-117 reconciliation) is untouched and the observation says explicitly that I did not investigate the 117 rather than leaving it ambiguous. WHY IT IS AN OBSERVATION AND NOT A FIX: paying this debt means DISPOSITIONING research units, which is a reading-and-judgement act on research output, not an ops-lane code change; the machinery is healthy (admission states {accruing 5, not_queue_dispatched 409}, nothing stuck) and the constraint is reader attention. The value delivered is that `unread: 11` reading identically at both ends -- which would have let a future session conclude nothing moved -- is now separated by run stamp into three cohorts, with the +44 delta reconciled exactly."
}
