{
  "tier": 1,
  "landing": "self",
  "why": "DOCS ONLY, TWO FILES: the WO object and one existing backlog row. It records a DEFECT IN THE FIX THIS LANE SHIPPED THIS MORNING. .github/workflows/stale-automation-sweep.yml (#11519, on main since 15:09:10Z) declares `17 */2 * * *`, and its own header justifies that 2h against the 6h window of check_digest_liveness so a strand gets '~3 sweeps' before that clock expires. That assumes GitHub runs a cron near its declared time; in this repo it does not, and the repo already knew. RE-MEASURED by the method and the control of BL-20260830-SCHEDULED-WORKFLOW-LAG-WENT-FROM-1H-TO-6-12H-ON-2026-08-27, whose own resolution_criteria clause (1) asks for exactly this. POPULATION: purge-artifacts.yml (`0 3 * * *`), the 40 consecutive event=schedule runs from 2026-08-02 to 2026-09-10, lag computed per run as run_started_at minus the most recent 03:00Z. Pre-break 0.66-3.42h; the 2026-08-27 break reproduces to that row's own four figures (10.88, 12.01, 6.80, 5.98h); the eleven days since are NEW and read 6.43, 5.38, 4.70, 4.85, 4.78, 4.47, 4.70, 5.01, 4.88, 4.93, 4.93h — not reverted, no longer drifting, SETTLED on a ~4.9h plateau. So the sweeper gets about ONE sweep per 6h window rather than three, and 2h buys nothing 4h would not because 2h is below the lag. ⚠️ AND ONE SENTENCE OF THAT ROW IS NOW FALSE FOR PART OF ITS POPULATION — the reassuring one. 'NOTHING IS BEING DROPPED ... the runs arrive, late' holds for a DAILY cron (40 distinct daily boundaries in 40 spanned days, none missing) and fails for a cron declared TIGHTER than the lag: error-feed-digest.yml, declared hourly at :35, delivered 40 runs against ~147 declared slots over 146.9h = 27%, median gap 4.04h, fire minute scattered over 31 distinct values. A constant lag preserves the one-hour SPACING, so four-hour spacing is COALESCING, not lateness. The row's summary is corrected in place and marked, its detail carries the re-measurement, and its severity is raised medium -> high on its own stated grounds (it declined `low` because the exposure was wide but unaudited; one shipped mechanism's margin is now measurably void). Spliced byte-exactly: 1 row of 1441 changed, 3 insertions / 3 deletions, asserted programmatically that every other row is deep-equal, and `backlog_append.py --check-live` reproduces all four live backlogs byte-for-byte. ⚠️ WHAT THIS DOES NOT DO: it does not correct the workflow comment itself, because that file is registered LANDING_MACHINERY and under R12 cannot self-land — that needs a human-read PR and is stated as owed rather than done. It also does not re-declare the cron: a tighter cron is what coalescing eats, so the remedy is a second non-cron trigger, which is a design change and is NOT shipped here. It also reframes what this WO is waiting on: no run at the 15:17Z or 17:17Z slot is NOT evidence of failure against a ~4.9h plateau. Touches no src/, config/, deploy/ or order path."
}
