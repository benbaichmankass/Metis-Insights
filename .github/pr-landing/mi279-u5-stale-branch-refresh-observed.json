{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5. Docs/registers only: one appended `observation` plus `verified_at`/`last_checked` on OI-20260831-SESSION-BRIEF-DIFF-SCOPING-SHIPPED-NEVER-REPORTED-INHERITED, one appended `detail` on the already-open BL-20260830-A-TRANSIENT-RED-BASE-PERMANENTLY-STRANDS-AN-AUTOMERGE-BRANCH, and the mandatory session-brief re-render that a register change obliges. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY IT IS AN OBSERVATION AND NOT A FIX: the monitoring row's clause (a) requires a CI run to print `verdict=inherited`, which `check_verdict` emits only when main's brief is ALREADY stale against a diff that touched neither the registers nor the block -- the UTC-midnight cadence rollover its own docstring describes. Manufacturing that would mean deliberately staling main, i.e. gaming the criterion, so the row stays OPEN with clause (b) recorded as met and clause (a) named as unmet. WHY THE #11952 STRAND IS ROUTED RATHER THAN FIXED: its remedy is a re-arm of auto-merge on an `automation/**` branch or a human merge, and CLAUDE.md assigns the merge queue to the MANAGER, not to a worker lane. Filing evidence onto an open row is the act of NOT taking the item."
}
