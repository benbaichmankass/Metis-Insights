{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "unvouchable_paths",
  "hold_text": "R5 refused `landing: self` and it is RIGHT to: the diff touches src/runtime/manager_status.py and src/web/api/routers/work.py, both Tier-2 BY NAME and outside TIER1_SURFACE. The manager's dispatch anticipated exactly this -- 'If your diff turns out to touch anything that makes it Tier-2, declare the hold and route it back to me -- do not self-land past R5' -- so this is the instructed outcome, not a blocker. ⚠️ NOTE THIS IS THE VALUE MI-226 ADDED, AND HERE IT IS TRUE. On 2026-09-10 I hit the same declaration problem on a docs-only branch and R14 refused `unvouchable_paths` in terms ('every changed path IS inside TIER1_SURFACE'), leaving only a false value that passes -- reported to the manager as a live gap. That gap is real and is NOT this case: these paths are genuinely unvouchable, so the honest declaration exists and is used. BLAST RADIUS, MEASURED NOT ASSUMED: the trader does not import manager_status (zero hits in src/main.py and src/runtime/pipeline.py). Its consumers are the web API checklist route, the Telegram decisions sweep, and operator_commands' /status -- so a fault reaches the web-api and the Telegram bridge, never an order decision. Content-wise this is read surface: a staleness threshold and a read-state field. But 'outside TIER1_SURFACE is not thereby dangerous; it is one the guard cannot certify' -- the guard's own words -- and certification is a human's to give.",
  "why": "MI-262. (1) The Workflow page's checklist-staleness threshold moves 3.0h -> 15 minutes, a recorded operator decision (DEC-20260910-WORKFLOW-PAGE-STALENESS-THRESHOLD), stored minutes-denominated because that is the unit they answered in, plus the unit fix without which the first warning it can emit reads '0.3h ago'. (2) `synced` is made to justify itself: it compares HEAD against the LOCAL origin/main ref while deploy_pull_restart.sh hard-resets HEAD to that ref right after fetching, so the equality holds by construction and treeBehindCommits is guaranteed rather than measured -- measured live on 2026-09-10 reading `synced` while the VM was genuinely one commit behind. TreeProvenance now carries how old our view of main is, read from FETCH_HEAD's mtime with no network call. No order path, no strategy config, no risk caps, no account mode, no service or timer file."
}
