{
  "tier": 1,
  "landing": "self",
  "why": "MI-280 unit U4. Adds scripts/ci/check_register_field_loss.py plus its run_guards.py entry, a test file and the .github/register-removals/ README. Tier-1 by the canonical doc's own list: scripts/ci/**, tests/** and .github/ only - no src/, no config/, no deploy/, no order path, no strategy config, no risk cap, no live-VM runtime file. R12 does NOT apply and that was checked rather than assumed: the new script does not appear in check_pr_landing.py::LANDING_MACHINERY and is not a landing route - it grades a JSON diff and can neither open, arm, nor merge a pull request. THE DEFECT: a shared-register merge can revert a FIELD while every row id survives, so the union-by-id proof everyone reaches for reads clean. MEASURED on the real register rather than a fixture - deleting one key (observed_by) from one real row of the live 231-row docs/claude/work/SESSIONS.json leaves the id sets identical at 231 vs 231 and the guard reports FIELD_LOSS and exits 1, with the positive control quiet on restore. IT DOES NOT DUPLICATE merge_json_register.py, which refuses the same case correctly but is a CLIENT-SIDE merge driver: git config --get merge.jsonregister.driver returns nothing in this container, so every register merge here is line-level, and this grades the ARTIFACT instead. VERIFICATION: 10 planted defects, 10 caught by both probes, positive control quiet on restore; 30 self-test cases and 30 pytest cases. TWO mutations escaped earlier runs and both were real gaps that are now closed and recorded - the ok-computation was reachable only through the live filesystem path until it was extracted as a pure verdict_of(), and the empty-population refusal did not exist until a copy of the script run from the wrong root reported a confident green over ZERO inputs."
}
