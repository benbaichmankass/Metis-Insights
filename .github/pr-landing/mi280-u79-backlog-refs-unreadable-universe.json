{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI/ops tooling: scripts/ops/check_backlog_refs.py plus its test module. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no live-VM runtime file, and no register is written. R12 does not apply: the file is not in LANDING_MACHINERY, verified against that list rather than assumed. filed_ids skipped a register that does not parse and said nothing, so every id that register defines dropped out of the universe and every citation of one read as a reference resolving to NOTHING. MEASURED on origin/main @eb606713d with one line inserted into docs/claude/health-review-backlog.json (1574 rows): 1874 filed ids / 86 dangling -> 299 filed ids / 1660 dangling, exit 0 both times -- +1574, exactly that file's row count, burying the 86 real findings. The banner is printed BEFORE the list (a test asserts the ordering) and the count line carries the caveat, because the banner scrolls away. --all exits non-zero; it is allow_fail in run_guards.py so it still gates NOTHING, and what changes is that it stops claiming a number it cannot stand behind. THE GATING --base MODE IS DELIBERATELY UNCHANGED and that is measured, not assumed: _refs_anywhere_at exempts any id already cited at the base and every real row id is cited at base in its own register row, so the gate cannot false-fail on one -- established by running a positive control, getting OK, and then establishing WHY rather than reading the OK as evidence. A refusal nothing can reach is the decorative branch collapsed-state-guard refuses. Proven: 6 new tests, 29 pass restored, and four planted defects each red their own controls including the over-reporting direction (an empty-but-valid or absent register must NOT be called unreadable) and the keep-the-banner-but-exit-0 shape."
}
