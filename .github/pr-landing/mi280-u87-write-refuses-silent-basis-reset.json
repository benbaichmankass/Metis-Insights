{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 developer tooling: scripts/ops/document_index.py, one new test file, and one register row closed. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps. R12 does not apply -- checked against LANDING_MACHINERY rather than assumed: document_index.py is not in that list, which names the automerge/pr-opener/board-post workflows, check_pr_landing.py, check_automerge_trigger.py, session_registry.py, claim_merge_slot.py and commit-to-main/action.yml. WHAT IT DOES: `--write` now REFUSES, before writing anything, when a committed `basis` differs from what build_rows() computes for that path -- i.e. a value this generator did not produce and would replace. `--reset-unowned-bases` overrides and then PRINTS every basis it replaced. Closes BL-20260913-SESSIONS-HAND-WRITE-A-VERIFICATION-BASIS-INTO-THE-DOCUMENT-INDEX-THAT-THE-GENERATOR-DOES-NOT-OWN-AND-SILENTLY-RESETS-ON-THE-NEXT-WRITE, which this session filed in #12199 with the measurement (35 -> 0 -> 1, and a --write probe on a clean main rewriting exactly the file carrying the survivor). A REFUSAL RATHER THAN A WARNING, on the criterion's own cited precedent: backlog_append was fixed by making the helper refuse, and this hazard was already documented in carry_last_verified's docstring while still costing two PRs and 35 rows. DERIVED, NEVER PATTERN-MATCHED: comparing against the computed basis finds any convention, where matching the motivating self-measured string would catch one habit. MEASURED over all 1081 committed rows on main: exactly ONE differs. IT DOES NOT CARRY THE BASIS FORWARD -- STATUS_EXPLICIT's docstring forbids the generator asserting what it cannot derive, and the parent row refused restoring the 35 on that reasoning. The decision is untouched; only the silence is removed. PROVED TO WRITE NOTHING on a refused run (index hash unchanged, zero other files touched) -- an earlier probe appeared to show otherwise and was wrong because a `git checkout -- .` had reverted the source edit. THREE STATES: committed_bases first collapsed ABSENT and UNREADABLE into {}, which would let a read failure authorise the overwrite; it now returns read / absent / unreadable and --write refuses on unreadable. CONTROLS: 12 tests, and four planted defects each observed reddening its own -- unreadable collapsed into absent; nothing ever flagged; a NEW document flagged; and the gate fed an empty list, caught by a STRUCTURAL assertion that the refusal precedes the write. One plant was a no-op and is recorded as such rather than counted. NO SCHEDULED CALLER AFFECTED -- measured: nothing in any workflow, action or script runs `--write`; only a human prompted by R1's remedy does, so the refusal cannot red a cron. Row integrity: ids 1583 -> 1583, 0 lost, 0 added, exactly 1 row content-changed, 0 duplicate ids."
}
