{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "Routed for a human merge because R5 cannot certify src/** as Tier-1, NOT because the work is reclassified. The approval sought is a manager/operator MERGE of a Tier-1 read-path change; no Tier-2/3 permission is being requested and none is needed for anything in this diff. Reviewer's check is one question: does this diff touch a WRITER, an ORDER PATH, or a CONFIG? It does not -- src/runtime/bracket_outcome.py is a new pure function that writes nothing, src/runtime/r_provenance.py gains a risk-basis resolver, and the two routers are read endpoints.",
  "why": "MI-144, trade-record integrity. Tier-1 by the canonical doc's own list -- 'observability / read paths, retrieving + analyzing state'. The diff touches READ PATHS and analytics only: /api/bot/performance's R denominator, a new pure module that writes nothing, /api/bot/trades/closed's error contract, their tests, two backlog rows, the API reference and a research doc. NO journal writer, NO order path, NO src/runtime/order_monitor.py, NO config/**, NO risk caps, NO deploy/ or unit file, NO account-mode or execution gate. Nothing in it can place, modify, or refuse an order. The argument worth stating rather than assuming: expectancyR FEEDS the promotion gates, so correcting it changes what those gates read -- but the gate is a Tier-3 decision a human makes FROM these numbers, and this makes the numbers correct rather than moving the gate. Leaving a demonstrably sign-inverted figure in place (the 30d real-money window published expectancyR +0.9818 on a window that LOST money) is the riskier option, not the safer one. The two Tier-2 items this work identified -- a historical re-classification of exit_reason, and folding order_monitor._classify_broker_exit into the new pure module -- are PROPOSED in the PR body with their exact shape and are deliberately NOT in this diff."
}
