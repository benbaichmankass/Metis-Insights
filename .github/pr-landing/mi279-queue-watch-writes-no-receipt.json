{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5. Registers only: one new row in docs/claude/health-review-backlog.json via backlog_append, one appended `observation` plus verified_at/last_checked on OI-20260902-DECISION-DRAIN-ROUTINE-DOES-NOT-EXIST-AND-NOTHING-HAS-EVER-DRAINED, and the session-brief re-render a register change obliges. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY A NEW ROW RATHER THAN AN APPEND: the finding is about a DIFFERENT Routine (the Manager Queue Watch) with its own writer, its own grader and its own fix; folding it into the decision-drain row would hide both. A duplicate probe was run first and returned nothing matching. WHY NOTHING IS FIXED HERE: the cause is NOT established -- no checkout, no push credential, no MCP tools, or a failed push reported into a chat nobody reads are different causes with different fixes, and last_run: SUCCEEDED says the FIRE was delivered rather than that the prompt's steps ran. The row directs the next session to read the fired session's own transcript BEFORE choosing a cause, and its resolution_criteria explicitly REFUSE a hand-written receipt (which would arm check_manager_queue_watch.py against a file no Routine maintains) and a local or dispatch run (which proves the script works, never in doubt). The one CODE change it proposes -- that the grader's NEVER_RAN branch stop being unconditionally benign -- is stated as the minimum and deliberately not taken, because it belongs with whoever establishes the cause."
}
