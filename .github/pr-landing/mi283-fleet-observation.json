{
  "tier": 1,
  "landing": "self",
  "why": "REGISTERS ONLY — no src/, no tests/, no config, no workflow. Records the post-merge fleet-observation pass MI-283 owed for OI-20260912-BYBIT-SYMBOL-DEDUPE-REPAIRED-AND-NO-FLEET-READ-HAS-BEEN-SEEN-WITHOUT-THE-DROP, after #11903 merged (73747db4) and deployed. Clause (1) MERGED and clause (2) DEPLOYED are MET and recorded with their method (ancestry via merge-base --is-ancestor plus a positive control, and the running PROCESS identity established from exit_interval_soak rather than from /api/diag/version, which reports the disk sha). Clause (3) is recorded as NOT MET AND NOT SATISFIABLE TODAY with the measured reason: bybit_1 holds five positions on five DISTINCT symbols, so no symbol is two-sided, the dedupe is never reached, and the zero drop-count is vacuous — evidenced by the pre-deploy positive control FAILING in the informative direction (14 of 14 pre-deploy rows already read zero with the fix not deployed). Clause (4)'s probe is built with a passing positive control (3 packages found over ids 5328-5727) and returns zero new pairs over a ~3-minute post-restart window containing zero new trade rows, which is recorded as establishing nothing. The row also carries THREE corrections to its own earlier text, found by running the probe rather than re-reading it: 5712 carries pnl +14.536494 and not None; the SOL chain is FOUR hops (5704->5715->5719->5725) with 5719 recorded nowhere; and the -$762.496 figure is correct only for the two reconciler_filled closes, against -$2,565.67 across the five closed hops. Also files BL-20260912-TICK-COST-FILE-OUTLIVES-THE-PROCESS-IT-NAMES-SO-PROCESS-STARTED-UTC-CAN-MAKE-A-DEPLOY-LOOK-OLDER-THAN-THE-MERGE-IT-CONTAINS, a measured observability defect hit during this very verification: tick_cost's process_started_utc named a process that had already exited, which would have reported a landed deploy as not landed. No row is removed and no shared row is modified — verified against the merge-base with a positive control."
}
