{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 by docs/CLAUDE-RULES-CANONICAL.md § Permission Tiers: tooling, tests and one line-local edit to a JSON backlog register. Files touched: scripts/ops/backlog_append.py (the mandated backlog-filing helper), scripts/ops/check_backlog_criteria.py (a CI guard's own BACKLOGS scope tuple), tests/test_backlog_append.py, docs/claude/health-review-backlog.json. NO src/, NO config/, NO deploy/, NO workflow, NO systemd unit, NO order path, NO strategy or risk parameter -- it cannot influence a live trade. AND IT IS NOT LANDING MACHINERY, checked rather than assumed: neither scripts/ops/backlog_append.py nor scripts/ops/check_backlog_criteria.py appears in check_pr_landing.py's LANDING_MACHINERY, so R12 does not reach this diff -- both decide what may go into a REGISTER, never whether or how a PR merges. WHAT IT DOES: append_row now raises RowNotWorkable on a row check_backlog_criteria would reject, so the writer CLAUDE.md mandates and the guard CI mandates refuse the same thing. The verdict is IMPORTED, not restated -- check_backlog_criteria._verdict is published as workability_verdict and a control asserts the two are THE SAME OBJECT rather than that they agree, because two predicates that merely agree today are exactly what drifts apart, which is the closed row's own subject. Closes BL-20260910-THE-MANDATED-BACKLOG-WRITER-ACCEPTS-A-ROW-THAT-CI-THEN-REJECTS. VERIFIED BY RUNNING THE REFUSAL through append_row itself, as that row's criterion demands and in the shape it demands: four planted defects (no resolution_criteria / no severity / no tier / a placeholder) each raise AND are asserted to leave the file byte-identical -- a refusal that already wrote is not a refusal -- plus a positive control that still appends, which is the arm that would fail if the predicate were ever made unsatisfiable. Mutation arm: removing the refusal fails 5 pytest tests and 8 self-test controls. IT CANNOT REACH A GRANDFATHERED ROW: 393 of the 1601 live health rows would fail the predicate, the guard grades only rows NEW in a diff, and append_row only ever CREATES a row; update_row is deliberately untouched and a test pins that, because a leak there would make those 393 rows un-amendable. One asymmetry was introduced and removed in the same change -- the research backlog was absent from the guard's BACKLOGS scope and graded by nobody since being split out on 2026-08-30 -- fixed in one line on a MEASURED zero blast radius (0 of its 19 rows fail the predicate) and pinned by a set-equality test so the next backlog split out must fail it too. Register integrity: the one row edit went through update_row and the backlog diff is line-local (6 added / 3 deleted); the row sequence is asserted identical to origin/main, and --check-live reports all four live backlogs reproduce byte-for-byte."
}
