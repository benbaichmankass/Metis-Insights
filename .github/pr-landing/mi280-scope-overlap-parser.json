{
  "tier": 1,
  "landing": "self",
  "why": "MI-280 unit U3. Fixes BL-20260911-THE-SCOPE-OVERLAP-PARSER-MISSES-FOUR-OF-FIVE-REAL-DECLARATION-SHAPES (open, high). Tier-1 by the canonical doc's own list: scripts/ci/**, tests/** and docs/claude/health-review-backlog.json only - no src/, no config/, no deploy/, no order path, no strategy config, no risk cap, no workflow. R12 does NOT apply and that was checked rather than assumed: scripts/ci/check_scope_overlap.py does not appear in check_pr_landing.py::LANDING_MACHINERY, and the scope-overlap audit is a REPORTING mechanism - it can neither open, arm, nor merge a pull request. THE DEFECT: parse_declared_paths closed its section on any blank line, so a bold header followed by a BLANK LINE and a bullet list declared NOTHING, and that is the shape ordinary markdown produces; separately the marker vocabulary carried the gerund `touching` while real STARTs write the infinitive `Files I will touch:`. RE-MEASURED ON REAL BOARD STATE as the row's resolution_criteria require, population = the 3 STARTs live on board #11336 at 2026-09-12T05:2xZ: MI-279 goes 0 -> 6 declared paths, the manager's widened START 12 -> 12 declared and 0 -> 6 explicitly excluded, and this lane's own START 8 -> 8 unchanged as a control. END TO END on MI-279's live START against a PR touching docs/claude/health-review-backlog.json, the old module reports state=no_overlap over a denominator of ZERO and the new one reports state=overlap with denominator 4 and 1 hit. VERIFICATION: 14 of 14 real board declaration lines graded correctly; the module self-test went 73 -> 87 planted controls with the original 73 unmodified; tests/test_scope_overlap_attribution.py went 33 -> 51 with the original 33 unmodified, including the #10729 self_declared==3 control this change deliberately does not relax; 7 planted defects, 7 caught by BOTH probes, positive control quiet on restore. The FIRST mutation run caught 6 of 7 and the miss is recorded rather than hidden. One reverted attempt is documented at the constant it would have changed: adding `read-only` to the negation vocabulary broke the _DRAIN3_START control by winning a line that carries a declaration AND a negation, and per-clause negation is what the row itself says no substring parser can represent."
}
