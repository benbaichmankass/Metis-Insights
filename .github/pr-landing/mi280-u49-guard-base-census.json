{
  "tier": 1,
  "landing": "hold",
  "why": "One new module scripts/ci/_git_base.py, one guard scripts/ci/check_backlog_unresolve.py, and one filed row in docs/claude/health-review-backlog.json. Every path is inside TIER1_SURFACE and none is in check_pr_landing.py::LANDING_MACHINERY -- verified by importing the list rather than by reading prose. No src/, no config/, no deploy/, no workflow, no runtime, no order path. ⚠️ It changes a GUARD's behaviour, so state the direction: it can only REMOVE a false failure (an unresolvable merge base falls back to the tip, i.e. the prior behaviour) and it ADDS a failure where the guard previously went silent on an unreadable base. Both directions are asserted by controls, and a real re-open is verified to still fail.",
  "hold_reason": "depends_on_unmerged_pr",
  "hold_text": "HELD ON PR #12093, AND NOT BECAUSE ANYTHING HERE IS WRONG. #12093 (`claude/mi279-unresolve-guard-merge-base`, another session) fixes the SAME defect in the SAME file -- scripts/ci/check_backlog_unresolve.py grading against the base ref's TIP rather than the merge base -- and was armed for auto-merge at the same moment as this one, both green but for pytest-run. Whichever landed first would have left the other `dirty`, and the loser's reconciliation would then be done by whoever happened to hit the conflict rather than by someone who had read both halves. ⚠️ THE TWO ARE NOT THE SAME PATCH, which is why a race would have silently dropped one side. #12093 carries a planted control this branch LACKS -- `run_behind`, a non-linear repo where main resolves the row AFTER the merge base; every control in that file before it built a LINEAR two-commit repo where the tip and the merge base are the same commit, so the distinction the fix is about was invisible to all of them -- plus the measured evidence of the live false positive. This branch carries the shared owner module scripts/ci/_git_base.py and the unreadable-base half (three states, `[unreadable-base]` findings, `reopens` counted separately from `blind`), which #12093 does not have. #12093 is the narrower and more urgent change -- it is reddening other sessions' correct PRs right now -- so it goes first and this one yields. NEXT STEP IS MINE, NOT THE MANAGER'S: once #12093 lands I resync this branch onto it, KEEP its `run_behind` control and its docstring evidence verbatim, layer `_git_base` and the unreadable-base half on top, re-run the self-test in both directions, and re-declare `landing: self`. Nothing is owed to a human here; this is a yield, not an escalation."
}
