{
  "tier": 1,
  "landing": "self",
  "why": "HOLD LIFTED: #12093, the PR this yielded to, has MERGED (a2ea0bc32). This branch is resynced onto it and the two fixes are reconciled in one file -- #12093's `run_behind` planted control kept verbatim, this branch's shared owner module scripts/ci/_git_base.py and unreadable-base half layered on top, and `comparison_point` now DELEGATES to that module rather than re-implementing the resolution (two resolvers in one file is this change's own class, one level up). The self-test passes with BOTH sides' controls. Paths: scripts/ci/_git_base.py, scripts/ci/check_backlog_unresolve.py and docs/claude/health-review-backlog.json -- all inside TIER1_SURFACE, none in check_pr_landing.py::LANDING_MACHINERY, verified by importing the list rather than reading prose. No src/, no config/, no deploy/, no workflow, no runtime, no order path. ⚠️ IT CHANGES A GUARD'S BEHAVIOUR, SO STATE THE DIRECTION: it can only REMOVE a false failure (an unresolvable merge base falls back to the tip, i.e. the prior behaviour) and ADD a failure where the guard previously went silent on an unreadable base. Both directions are asserted by controls, and a real re-open is verified to still fail."
}
