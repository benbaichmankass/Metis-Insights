{
  "tier": 1,
  "landing": "self",
  "why": "Register-only. Updates ONE row's observation/clears_when/verified_at in docs/claude/OPEN-ITEMS.json and re-renders CLAUDE.md's generated SESSION-BRIEF block, which is a derived artifact of that register. No src/, no tests, no scripts, no workflows, no config, no order path, no risk cap, no account mode. It records the FIRST-EVER live read of GET /api/bot/work/checklist after #11651 merged and deployed: 200 at 2026-09-10T09:29:42Z on git_sha == git_sha_on_disk == 221b5dff1 (restart_pending false, ancestry-confirmed to contain merge 4875f407a), returning present:true / readState:'read' / reason:null over 253 items whose byBasis partition sums exactly to 253 on LIVE data. Clauses (1, bot half) and (2) of OI-20260910-WORKFLOW-PAGE-SHIPPED-... are marked met inline so a later session does not re-do a read that is now recorded; clauses (3) render-on-GitHub-Pages and (4) a manager working from the page are explicitly left NOT met, because neither is observable by a session and the SPA half (#212) is still unmerged so there is nothing on Pages to look at. ⚠️ The row is SPLICED AS TEXT, three JSON string literals replaced, with an assertion that every OTHER row is byte-identical: a naive re-dump reformatted 1583 lines of this register earlier in the same session and was reverted. The prior branch for this work is MERGED and was deliberately not reused."
}
