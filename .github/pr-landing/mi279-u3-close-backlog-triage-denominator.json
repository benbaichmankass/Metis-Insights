{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain U3 — a CLOSURE, verified rather than asserted. One file: docs/claude/health-review-backlog.json, one row moved open -> resolved with an appended `detail`, via backlog_append.update_row (2 insertions / 2 deletions, byte-local, row count unchanged). No code changed at all — the machinery this row asks for was BUILT after the row was filed and the row was simply never closed on it. No src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHAT IT CLOSES: BL-20260823-BACKLOG-TRIAGE-SEES-47-PERCENT-OF-THE-LIVE-ROWS, whose four criteria are each executed this session with planted controls. (1) The shared helper exists and is SINGLE-OWNER rather than merely shared — scripts/ops/_backlog.py loads the liveness predicate from scripts/reports/backlog_counts.py rather than defining a second copy, and its docstring cites this row for why. Census: 37 files reference a backlog path, 8 are SKILL.md prose, and every code consumer that needs liveness or an age imports the accessors. (2) kept_open returns True from is_open_status, executed alongside the six other statuses. (3) DATE_FIELDS reads both keys with three never-collapsed states, executed on all four shapes. (4) Both halves of the shape check are planted-controlled: the date half in check_backlog_criteria.py accepts the legacy `opened` and fails loudly on an unparseable date and on no date key; the status half in check_claim_basis.py::check_status_enum grades the whole file and returns a specific finding for a planted 'banana' while passing 'kept_open'. ONE APPARENT COUNTER-EXAMPLE WAS CHECKED AND REFUTED: constraint_readout.py reads opened_at without a fallback, which looks like the same defect, and is not — those reads are over work objects and the operator-owed register, established by RUNNING it and seeing its open_items block render exactly one OO- row. THE RESIDUAL IS STATED RATHER THAN HIDDEN: nothing mechanically stops a NEW consumer re-deriving the filter, the census is a measurement of today rather than a ratchet, and a grep-based guard is explicitly not proposed because it would fire on tests, fixtures and the owner itself."
}
