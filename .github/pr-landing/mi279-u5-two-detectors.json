{
  "tier": 1,
  "landing": "self",
  "why": "U5 of MI-279: OI-20260911-THE-TWO-DETECTORS-ARE-BUILT-AND-NEITHER-HAS-EVER-FIRED-ON-THE-FLEET observed for the first time (its verified_at was null). Two files, both Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs/claude/OPEN-ITEMS.json and the GENERATED SESSION-BRIEF block in CLAUDE.md, re-rendered with render_session_brief.py --write rather than hand-edited. No src/, no config/, no deploy/, no workflow, no scripts, no order path, no VM mutation -- every read a GET against /api/diag/* or /api/bot/notifications. R12 does not apply; neither file is in LANDING_MACHINERY. THE ROW DOES NOT CLEAR and is not claimed to. Clause (2) is MET and was verified rather than trusted: the losing-streak WARN on /api/bot/notifications was re-derived independently from /api/diag/journal over a stated population (bybit_1 closed non-backtest pnl-not-null rows from the 1000 newest trades, 454 rows / 26 closing days) and reproduces to $0.11 -- 17 days, 2026-08-27 to 2026-09-12, -37,044.20 against the detector's -37,044.31 -- with the sign change at 08-27 confirmed against the two preceding positive days. Clause (1) is NOT met and is recorded as NOT YET rather than FAILING: both observed-state files carry __last_check__ 07:49:16Z while /api/diag/tick_cost gives process_started_utc 08:30:56Z, so the stamps are the previous process's and prove nothing about this one, which is precisely what that clause exists to exclude. The re-check instruction and the opposite-finding branch (still 07:49:16Z well past an hour after the restart means a dead hook, and clears the row into a defect) are written into the observation. Coverage is stated with the dollars: pnl_coverage 0.1345, cost_source 'estimate' on all 454 rows."
}
