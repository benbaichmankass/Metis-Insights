{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling only: scripts/ci/check_register_ids.py (a guard) plus one status flip on docs/claude/health-review-backlog.json closing the row this fix resolves. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no live-VM runtime file. R12 does not apply: check_register_ids.py is not in LANDING_MACHINERY -- verified against that list rather than assumed. The change makes the guard FAIL on a register that is present and does not parse, where it previously printed 'register-id-guard: OK' and exited 0; re-confirmed on origin/main @7f832e8c0 before editing, with a conflict marker in a 1574-row register. Three states, never collapsed (present/absent/unparseable); ABSENT stays a non-finding; the parse failure travels as its own return value so it cannot be printed under the id-collision headline, which would name a cause no code path tested. Proven both ways: OLD rc=0 'OK' vs NEW rc=1 naming the parse error on the same corrupt tree, clean tree still rc=0, and three planted defects in the fix each red their own control -- including one that keeps the finding and restores the original exit rule, which passes every other control while still exiting 0."
}
