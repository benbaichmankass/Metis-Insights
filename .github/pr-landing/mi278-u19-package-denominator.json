{
  "tier": 1,
  "why": "Tier-1 research, and every path is inside TIER1_SURFACE (`scripts/research/**`, `docs/**`), so it self-lands. One new read-only re-derivation instrument, an ADDITIVE change to an existing research instrument, a memo, two index rows and two backlog edits. `src/**` and `config/**` are untouched, and the three LIVE DASHBOARD routes MI-278 U12 named as carrying this same defect (`/api/bot/stats` winRate, `/api/bot/performance`, `/api/bot/attribution`) are `src/web/` i.e. Tier-2 and are deliberately NOT touched — changing a number the operator reads is a decision, not a side effect of a re-derivation. ⚠️ THE ONE CLAIM A REVIEWER SHOULD CHECK, since self-landing means nobody has to: the edit to `stop_width_counterfactual_2026_09_11.py::dose_response` is ADDITIVE and no published figure moves. That was PROVEN rather than asserted — the pre-patch and post-patch functions were both loaded and run over one population and compared key by key; every pre-existing key is identical and only `per_package` and `row_denominator_note` appear. If that proof is wrong, the change is wrong. ⚠️ AND THE UNIT CORRECTS ITS OWN AUTHOR: MI-278 U18's headline, published ~40 minutes earlier, goes p 0.0486 -> 0.1984 on a package denominator while its sign-survival criterion flips to PASS by +0.0044. The two criteria disagree; that is reported as the finding, filed as its own backlog row, and NOT resolved by picking the flattering one.",
  "landing": "self"
}
