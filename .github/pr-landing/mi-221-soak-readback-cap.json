{
  "tier": 1,
  "landing": "self",
  "why": "One backlog row appended via scripts/ops/backlog_append.py, plus the merge-slot field. No src/, no config/, no deploy/, no .github/workflows/, no tests, no order path — Tier-1 by the canonical doc's own list. MEASURED WHILE TRYING TO ANSWER MI-221's QUESTION (B): /api/diag/log_file?name=bybit_coverage_soak&lines=1000 returns 1000 rows spanning only 7h05m (2026-09-09T09:24:47Z..16:29:35Z); _MAX_LIMIT is 1000 and _clamp silently reduces any larger request, so at the observed 2.35 rows/min reaching the incident window (2026-09-04..09-08) would need ~16,900 rows, about 17x the ceiling. The window is unreachable through the only surface that exposes the log. IT IS A READ-SURFACE LIMIT, NOT DATA LOSS, and the row says so because it changes the fix: the file on the VM may hold far more, so the remedy is a windowed or paged read rather than a change to what is written. WHY SEVERE: OI-20260902-BYBIT-GRADED-COVERAGE-SOAK-IS-THE-ONLY-EVIDENCE-FOR-WIDENING-AND-NOTHING-WATCHES-IT records that this soak's rows are the ONLY declared evidence for the operator's conditional widening of the graded-coverage gate to bybit_portfolio and then real-money bybit_2, and its clears_when needs 20 graded rows across 3 distinct UTC days plus a verdicts_differ row — accumulable by sampling forward, but NOT auditable retrospectively, which is weak evidence for a real-money arming decision. The row's resolution_criteria demands a time-window or cursor read verified by a POSITIVE CONTROL — an out-of-range window must return an explicit empty state and never silently serve the tail, since that would be the same defect class as the cap — and explicitly rules out raising _MAX_LIMIT alone (a bigger tail is still a tail). ALSO RECORDED as a separate fact so it is not misread: zero bybit_2 ETHUSDT rows appear even in today's window, which is CORRECT because ETH is flat and the sweep grades only symbols carrying a position — so that absence is the expected shape, not evidence about the incident. No order was placed, modified or cancelled on any account; every venue call was a GET. DOCUMENT-INDEX.md untouched (no new file), so document_index.py --write was not run and remains prohibited."
}
