{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U1 (the open critical rows) -- the LAST of the 17 to reach a disposition. Two files. (a) scripts/ops/get_env.py: two names added to ALLOWED_KEYS, a READ allowlist for /proc/<MainPID>/environ, plus the comment that says why. This adds no reader, changes no routing, and cannot change trader behaviour: get_env.py is an ops read tool and nothing in src/ imports it. (b) docs/claude/health-review-backlog.json: one `notes` field replaced on the open critical row BL-20260912-THE-ARBITRATION-FANOUT-APPLY-PATH-HAS-NEVER-DISPATCHED-BECAUSE-APPLY-ROUNDS-DROPS-THE-GEOMETRY-ITS-OWN-READER-VALIDATES, written through backlog_append.update_row (2 insertions / 1 deletion, byte-local, row count unchanged at 1538). No src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY IT IS WORTH LANDING: the row's own criteria open with 'ESTABLISH CENTRALIZED_ALLOCATOR FIRST -- if it is true the pipeline never calls _fanout_apply_rounds at all, so the fan-out would still never dispatch but THIS FIX WOULD NOT BE THE RIGHT ONE', and the row states the value is unmeasured with no read surface. It names this exact edit as the cheapest settlement. WHAT THE DISPOSITION ADDS BEYOND DOING AS ASKED: the row's proposed settlement was necessary and NOT SUFFICIENT, and a session acting on it as written could have reached a confident wrong answer -- runtime_flags._centralized_allocator_enabled reads settings.get(...) FIRST and only falls through to os.environ when that is None, so an absent env var settles nothing unless the settings dict provably cannot carry the key. Measured on main: build_settings_from_env() returns exactly 10 keys and this is not one, and nothing anywhere mutates that dict, so the settings branch is dead for this key and the env IS the sole source. That note rides at the ALLOWED_KEYS entry, not only in the backlog. MULTI_STRATEGY_INTENT_LAYER is added in the same change because it was in the identical state -- declared true in deploy/ict-trader-live.service, documented as the sanctioned no-redeploy rollback, and unreadable -- which is the blindness half of OI-20260909. THE ROW STAYS OPEN: it is Tier-2 by its own criteria, and a read surface is not a measurement."
}
