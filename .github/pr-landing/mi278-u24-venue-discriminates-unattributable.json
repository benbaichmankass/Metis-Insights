{
  "tier": 1,
  "why": "Tier-1 research only: one NEW scripts/research/ module, one new docs/research/ memo, one row in each of two docs/ registers, and one docs/claude/ backlog append. Every path is inside TIER1_SURFACE. No src/, no config/, no scripts/ outside scripts/research/, no workflows, no order path. Every VM read is a GET through the token-gated /api/diag/* surface — nothing was written to the fleet and no VM action was dispatched. It answers the Tier-1 question that should precede a Tier-2 build: which journal state the 'unattributable' closes are actually in, and whether the column the backlog row proposes would reach them. It explicitly CANNOT close that row (the durable surface is a src/ journal-writer change, and the row's other clearing condition needs a later window) and says so in the memo, the register rows and the backlog append rather than implying completion. It proposes no src/ change here. Verified on committed state: 24 self-test controls pass and the harness was shown able to FAIL against five separately planted defects, two of which exposed real gaps that were fixed before shipping — a tautological control that made the docstring's pinning promise false, and a measurement method of my own that could not see a plant caught by crashing.",
  "landing": "self"
}
