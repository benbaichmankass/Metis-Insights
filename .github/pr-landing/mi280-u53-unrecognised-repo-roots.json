{
  "tier": 1,
  "landing": "self",
  "why": "tests/test_pytest_run_filter.py, scripts/ci/run_guards.py, .github/workflows/pytest-run.yml and one backlog row. Every path is inside TIER1_SURFACE. No src/, no config/, no deploy/, no runtime, no order path, no strategy config, no risk caps. \u26a0\ufe0f DIRECTION OF THE CI CHANGE, STATED: the workflow change only WIDENS what counts as relevant, so it can make pytest-run RUN where it previously skipped and can never cause a skip that did not happen before; the negative control test_short_circuit_is_still_possible_at_all asserts the filter has not been widened to everything. The run_guards.py entry ADDS a step that invokes an existing self-test which passes on main today (verified by running it: PASS in 1.0s, stdlib only, no network). The test changes make the scan SEE more, so they can only add a failure. Five planted reverts each go red on the right control and 38 tests are green on the fix. THE HOLD IS RELEASED, NOT DEFEATED: this was held as depends_on_unmerged_pr because it was stacked on #12102 and its diff therefore carried that PR's landing files. #12102 merged (c7ac3f741 on main), the branch was resynced, and the diff now carries ONLY this unit's own landing files -- verified by listing it, not assumed. The two merge conflicts that resync produced were resolved and checked: the workflow regex was reconciled as a computed UNION of both sides' alternatives (ours proved a strict superset, 40 vs 36, zero alternatives dropped), and the test-file conflict was an add-at-end where main's side was asserted EMPTY before ours was kept. 38 tests pass on the merged state; the backlog register kept all 1555 rows from main with zero update-count regressions."
}
