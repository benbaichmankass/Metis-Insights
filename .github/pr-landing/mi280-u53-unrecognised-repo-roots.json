{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "depends_on_unmerged_pr",
  "why": "tests/test_pytest_run_filter.py, scripts/ci/run_guards.py, .github/workflows/pytest-run.yml and one backlog row. Every path is inside TIER1_SURFACE. No src/, no config/, no deploy/, no runtime, no order path, no strategy config, no risk caps. ⚠️ DIRECTION OF THE CI CHANGE, STATED: the workflow change only WIDENS what counts as relevant, so it can make pytest-run RUN where it previously skipped and can never cause a skip that did not happen before; the negative control test_short_circuit_is_still_possible_at_all asserts the filter has not been widened to everything. The run_guards.py entry ADDS a step that invokes an existing self-test which passes on main today (verified by running it: PASS in 1.0s, stdlib only, no network). The test changes make the scan SEE more, so they can only add a failure. Five planted reverts each go red on the right control and 38 tests are green on the fix.",
  "hold_text": "HELD ON #12102, WHICH THIS IS STACKED ON. #12102 closed two blind spots in the same two scans; this branch was cut from its head, so the branch diff carries #12102's landing files under .github/pr-landing/ and the landing guard grades the whole diff rather than this unit's own changes. ⚠️ NOTHING HERE NEEDS A HUMAN AND NOTHING IS ESCALATED. The moment #12102 merges, this branch is resynced onto main, the stack dissolves, the landing files stop appearing in the diff, and the declaration is re-made as `landing: self`. That is mine to do, not the manager's. Holding is the cheap correct state meanwhile: an armed stacked branch would race its own parent, which is the collision I disarmed #12094 for earlier today."
}
