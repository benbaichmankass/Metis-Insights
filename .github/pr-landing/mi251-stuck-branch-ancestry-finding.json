{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "depends_on_unmerged_pr",
  "hold_text": "Sequencing only, not a decision. This appends ONE row to docs/claude/health-review-backlog.json, and #11690 currently holds the R13 merge_slot for this same session. A single session cannot hold the slot for two branches at once, so this waits behind #11690 rather than displacing my own armed branch mid-flight. Flip to landing: self once #11690 is merged or closed.",
  "why": "One appended backlog row, filed through scripts/ops/backlog_append.py::append_row (15 insertions, 0 deletions — an append, not a reformat). No src/, no config/, no tests, no workflows, no order path. It reports a defect in scripts/ops/stuck_automation_branches.py and deliberately does NOT fix it — filing is the act of not taking the item."
}
