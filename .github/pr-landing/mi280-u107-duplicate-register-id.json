{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, REGISTER-ONLY: one file, 14 insertions / 14 deletions in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no workflows, no scripts, no tests. NOT in LANDING_MACHINERY, so R12 does not apply. [!] URGENT: `main` IS RED ON register-id-guard RIGHT NOW and this is the fix. check_register_ids.py exits 1 on origin/main -- the BL-20260913-A-MISSPELLED-REGISTER-REMOVAL-DECLARATION-IS-REPORTED-AS-A-FABRICATED-ONE-SO-THE-REMEDY-ON-SCREEN-IS-THE-WRONG-ONE row appears at rows 1596 AND 1598, the two rows BYTE-IDENTICAL. MECHANISM PROVEN, NOT INFERRED, across all four inputs: my branch head fae00214f = 1598 rows / id x1 / 0 dupes; main at squash time 07f1417e6 = 1598 / x1 / 0; main after the squash of #12374 = 1600 / x2 / 1. Both sides had ONE copy and the merge produced TWO. The repo's row-aware register driver is a LOCAL git merge driver that GitHub's server-side squash cannot execute, so the register falls back to UNION, which keeps a row present on both sides at different POSITIONS. A local `git merge origin/main` on that same branch was verified clean minutes earlier -- the merge you can test is not the merge that runs. [!] I CAUSED IT: this session base-merged that branch to clear a `dirty` state, which moved the row's position relative to main and gave union two positions to keep. Resolving a conflict correctly is itself a trigger. WHY IT MAY SELF-LAND DESPITE TOUCHING A SHARED REGISTER: the deletion is provably LOSSLESS and was established BEFORE anything was written -- the two rows were asserted equal, the opener and closer lines matched literally, and the span was confirmed to contain the id, so the second copy is excised by EXACT LINE SPAN rather than by re-serialising the file. Verified after: 1600 -> 1599 rows (the filed row returns it to 1600), 0 ids lost, 0 duplicates remaining, the surviving row byte-identical to the original, backlog_append --check-live reports all four registers reproduce byte-for-byte, and check_register_ids.py reads OK. [!] IT FIXES THE SYMPTOM ONLY and the PR body and the filed row both say so: the class recurs on the next squash of any register-touching PR, and a green register-id-guard on a PR proves nothing because the guard grades the INPUTS while the duplication happens in the squash afterwards. The routing half -- whether a red main reaches anybody -- is NOT claimed here and is already carried by OI-20260913-THE-DEFAULT-BRANCH-RED-READER-IS-DEPLOYED-AND-HAS-NEVER-QUERIED-THE-ACTIONS-API. [!] THE ROW ID ABOVE IS SPELLED IN FULL DELIBERATELY: an earlier draft of this record elided it to '...-THE-WRONG-ONE' for readability, and check_backlog_refs correctly read the fragment as a reference resolving to nothing -- the very defect PR #12378 fixes in the due-list renderer, committed by me inside the PR that repairs register integrity. Do not shorten it."
}