{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1: docs only — docs/claude/health-review-backlog.json, one in-place row update and one appended row. No src/, no config/, no deploy/, no scripts/, no workflows, no live-VM runtime file. Not landing machinery, so R12 does not apply.\n\nIt records that candidate design (b) of the manager-lease row is ALREADY BUILT — #11932, 5c0a5c28b, which cites that row id three times and never updated it — and keeps the row OPEN, because (b) makes the register honest about why a heartbeat could not land and does not satisfy the headline criterion, which is that a heartbeat can REACH main while main is red.\n\nThe second change files the CLASS: a landed fix that leaves its row open is not merely a mis-report, it is an instruction to a future session to redo work that exists. Measured first-hand — two hours after #11932 landed, the same session re-read the row, found it open, and began redesigning (b) before git log stopped it.\n\nFiled rather than fixed, deliberately, and the filing is the act of NOT taking the item. The row records what is already known to be wrong with each candidate remedy so nobody re-derives them, including that a check asking whether a fix CITES its row is refuted in advance by the second instance."
}
