{
  "tier": 1,
  "landing": "self",
  "why": "One backlog row updated in place, nothing else. The diff is a single line of docs/claude/health-review-backlog.json plus this branch's own landing/arming declarations and the R13 merge_slot claim. Tier-1 by the canonical doc's own list: no order path, no strategy config, no risk caps, no src/, no scripts/, no config/, no deploy/, no .github/workflows/ and no live-VM runtime file. It records a SECOND instance of an already-open high-severity row rather than filing a duplicate, per the filing rule (backlog_search returned it at 0.77 overlap)."
}
