{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "operator_asked_to_hold",
  "hold_text": "⚠️ THE INSTRUCTION CAME FROM THE MANAGER SESSION, NOT FROM THE OPERATOR DIRECTLY — recorded that way deliberately, because HOLD_REASONS has no `manager_asked_to_hold` and quietly calling this an operator directive would launder a claim nobody made. Sub-session session_01C6Lh24HDdkn6p14qsaeNrA was dispatched by manager session_01HrmZ1RRNM4UnEUaFdrPEjj (registry key pending-20260906T100427Z-2) with, verbatim: 'This is Tier-3 order-path code. You PROPOSE the exact diff; you NEVER apply it.' and 'Do not touch config/, do not mutate the VM, do not merge anything.' So this PR is held on that instruction and is deliberately a draft. The manager, or the operator, decides whether it lands.",
  "why": "The DIFF is one new file under docs/research/ (bybit-close-qty-legalization-2026-09-06.md) — the Tier-1 docs surface, with no src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config and no risk caps. Note the deliberate split between the diff's tier and its CONTENT's tier: the note ROOT-CAUSES a live Bybit close-path defect (an un-quantised float reaching POST /v5/order/create as \"qty\": \"33.299999999999955\", which Bybit refuses with `Qty invalid` 10001 so the position cannot be flattened by the normal path) and PROPOSES an exact two-part fix to src/units/accounts/execute.py and qty_legalize.py. That proposed change would be Tier-3 and is NOT in this diff; only the analysis of it is. Declaring tier 1 grades the bytes actually being merged, which is what R5 checks against the diff, while `landing: hold` keeps a human in front of the Tier-3 decision the note asks for. Landing the note itself is genuinely useful — my coordination-board post warns other sessions off close_open_position and apply_intent_reduce_partial_close until this is read — but that is the manager's call, not mine to self-land."
}
