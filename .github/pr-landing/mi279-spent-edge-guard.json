{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane. scripts/ci/ only: extends the existing scripts/ci/check_decision_answers.py with R3, a REPORT of blocked_on edges whose operator_decision has already been answered. Tier-1 by the canonical doc's own list (CI / tooling). No src/, no config/, no deploy/, no workflow, no VM, no order path, no risk caps, and NO WORK OBJECT IS EDITED -- the guard only READS docs/claude/work/objects. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY IT EXTENDS RATHER THAN ADDS A FILE: that guard already owns 'is a written answer readable?' and already parses every object through the canonical normaliser; a second file would be a second parser and a second definition of `answered`, which is how the two would drift. WHY IT REPORTS AND NEVER FAILS: the guard FAILS on findings, and eleven of the twelve operator_decision edges are spent TODAY -- a failing R3 would red every PR in the repo on the day it merged, which is how a guard gets switched off. Same treatment and same stated reason as workflow-push-target-guard's conditional_default rows. Verified: the guard still exits 0 on the live store. WHY `answered` IS normalise_answer AND NOT A DATE FIELD: a first pass keyed on answered_at reported five genuinely-answered requests as stubs because the schema also uses answered_on; a self-test case now pins that an answer R1 grades UNREADABLE must not make an edge count as spent, so R1 and R3 cannot contradict each other. CROSS-CHECKED against the independent sweep in #12030, which joined on date fields: both report 11 spent + 1 unresolvable over 179 objects. NOTHING IS REMEDIATED HERE -- releasing an edge changes an object's lifecycle, which is its owner's or the manager's call and is routed there."
}
