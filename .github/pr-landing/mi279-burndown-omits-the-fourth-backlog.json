{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1: scripts/ops/system_review_checklist.py::backlog_burndown and one existing test file. NO REGISTER TOUCHED, no src/, no config/, no deploy/, no workflows. NOT in LANDING_MACHINERY, so R12 does not apply. WHAT IT FIXES, MEASURED: backlog_burndown() -- the function the backlog-drain skill names as THE metric -- held a hardcoded three-entry dict while docs/claude/research-review-backlog.json was split out on 2026-08-30 and never added. SCOPE-ONLY DELTA, the REAL function read TWICE on ONE tree: rows_total 1864 -> 1883 (+19), open_now 1013 -> 1026 (+13), closed_total 851 -> 857 (+6), reconciling EXACTLY against that register's 19 rows / 13 not-closed / 6 closed. September net moves +479 -> +481, August +338 -> +349. [!] MY FIRST COMPARISON DID NOT RECONCILE and the arithmetic is what caught it: it read +20/+14/+6 because the two readings were taken on DIFFERENT COMMITS -- I changed the scope and the tree at once. Re-reading the numbers would not have found it. [!] I ALSO NEARLY PUBLISHED A MONTHLY SERIES FROM A HAND-TALLY I WROTE TO CROSS-CHECK: it read CLOSE_KEYS only while the real function ALSO reads `updates`, so it under-counted September's closes 97 -> 72. A cross-check that recomputes the thing differently is not a cross-check; both published readings are the real function. [!] THE SHARPER HALF: the test that existed to catch exactly this -- test_open_now_agrees_with_an_INDEPENDENT_status_census, written to be 'a second reading rather than a restatement of the first' for a backlog row whose criterion says a code change alone does not clear it -- HARDCODED THE SAME THREE FILES. It agreed with the function because both were blind to the same register. PROVEN RATHER THAN ASSERTED: restoring the pre-fix census AND the pre-fix scope, i.e. the state that actually shipped, that test PASSES. It now globs the DISK, deliberately NOT BACKLOGS -- reading the same constant as the function under test would restore exactly the property just removed. WHY IT DRIFTED: it sat outside a pin that already existed. LIVE_BACKLOGS is asserted equal to a glob of the review backlogs on disk and BACKLOGS is asserted equal to LIVE_BACKLOGS, so adding a register already failed a test until both learned of it; this third copy was reachable by NEITHER assertion, which is precisely why it was the one that went stale. The chain is now disk-glob -> LIVE_BACKLOGS -> BACKLOGS -> backlog_burndown, every link asserted, with the census anchored outside it. WHAT SHIPPED: the scope is read from the single owner (check_backlog_criteria.BACKLOGS), imported INSIDE the function because check_backlog_criteria runs a sys.path shim at import time and this module is imported by backlog_append -- a module-level import would push that side effect into every writer process for a list only this function needs. No cycle (verified: check_backlog_criteria does not import this module). The docstring's 'across the three backlogs' is CORRECTED, not left as the record. CONTROLS: 2 new tests plus 2 planted defects, each caught by BOTH the scope pin and the now-genuinely-independent census -- (A) the pre-fix three-entry scope re-hardcoded, 2 FAIL; (B) a register silently dropped from the loop, 2 FAIL. The scope pin is BEHAVIOURAL and not merely structural: it asserts rows_total equals the rows actually on disk, so re-hardcoding a narrower list inside the function leaves the structural equality green and this one red. Plus a VACUITY CONTROL reconstructing the pre-fix scope and asserting it still differs from the disk, and a non-vacuity floor on the glob so an empty match cannot make every equality trivially true. VERIFIED ON COMMITTED STATE: pytest over test_backlog_append.py + test_system_review_checklist.py + test_backlog_burndown_creation_key.py = 65 passed; ruff clean."
}
