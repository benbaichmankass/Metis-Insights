{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain. Docs only: two appended `detail` blocks on already-open rows (BL-20260830-BUILDER-EXCEPTION-LATCH-HAS-NEVER-BEEN-WRITTEN and BL-20260908-THE-WEDGE-LEDGERS-VANISH-CLOCK-READS-A-CHANGED-CLASSIFICATION-AS-A-DISAPPEARED-POSITION), written through backlog_append.update_row. No new rows, no code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY APPENDS AND NOT CLOSES: neither row's criteria are met. The latch row's clause (a) was MEASURED not met (present: false on the live diag route) and its clause (c) is UNGRADEABLE from a diag-scoped session -- recorded as *we could not look* rather than collapsed into 'no burst', with the reason named: the clause cites /api/bot/logs, which DIAG_READ_TOKEN does not authorise. The vanish-clock row has ONE clause discharged by #11988 and three that require editing src/runtime/close_wedge_standing.py -- RUNTIME, hence Tier-2 under this lane's dispatch, so they are ROUTED to the manager and not attempted. WHY IT IS WORTH LANDING RATHER THAN DISCARDING: it records a measured hit rate for scripts/ops/backlog_drain_candidates.py (3 shortlisted, 1 genuinely closable, both misses matching on `diag_log_name` i.e. on the NAME resolving), and it records that the latch row's own criterion is written against a surface most sessions structurally cannot read -- which is why it keeps not being checked. Both are facts a future drain session needs before spending time on the same two rows."
}
