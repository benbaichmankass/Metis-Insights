{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1: one OPEN-ITEMS.json row, scripts/ops/probe_file.py (a whole-file JSON corpus reader), and one new test file. No src/, no config/, no deploy/, no workflows, no order path, no strategy config, no risk caps. R12 does not apply -- nothing in LANDING_MACHINERY is touched. It files OI-20260913-THE-DEFAULT-BRANCH-RED-READER-IS-DEPLOYED-AND-HAS-NEVER-QUERIED-THE-ACTIONS-API for the source #12209 (4f3e0c388) shipped an hour earlier, per CLAUDE.md's session-end rule to add a row for anything shipped and not yet proven. MEASURED WITH A POSITIVE CONTROL: the committed docs/claude/DUE.json (generated_at 2026-09-13T05:20:13+00:00, verdict partial) carries NO red_main_runs entry at all -- absent, not `read` with zero rows -- while its sibling red_crons, which needs the SAME token in the SAME workflow, reads state `read` with 6 rows in that same run, so the token half works there and the gap is this source's. Verified on MAIN's own copy after the merge: the function is defined once, registered in SOURCES and in collect()'s token dispatch, the query is event=push&branch=main, 17 tests pass from a worktree at origin/main, and a tokenless call returns could_not_read naming the missing variable -- the correct answer, and not evidence the source works. The row carries a REAL PROBE rather than a reason for having none, which required teaching probe_file.py to read a whole-file JSON corpus: DUE.json is one pretty-printed object and line-by-line yields ZERO rows, which the probe reported as `read and nothing matched` -- a clean negative over a population nobody read, the sub-class C defect that module's own docstring exists to refuse, and invisible because it is byte-identical to a real no-match. A JSONL file of two or more lines cannot parse as one JSON value, so no existing corpus reads differently, and a test pins the one-line case as identical either way. The probe today exits 1 (read, nothing matched) with its positive control matching 1, and exits 2 (could-not-look) when the control is swapped for a source that does not exist -- both asserted. Four planted defects were run: whole-file reading off reddens 6 tests AND the runner's own self-test, an absent file reading as an empty population reddens 1, a missing probe reddens 2, a probe with no positive control reddens 1. Register integrity: OPEN-ITEMS 85 -> 86 items, 0 lost, 0 duplicates, 0 pre-existing rows content-changed. Checked against #12214's in-flight rule over this very diff: 0 observation-loss findings. Local full guard set on committed state: PASS 84 / FAIL 1, the one failure being pr-landing-guard for this very record."
}
