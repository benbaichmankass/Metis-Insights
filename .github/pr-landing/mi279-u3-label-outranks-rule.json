{
  "tier": 1,
  "landing": "self",
  "why": "Closes BL-20260826-FOUR-WRONG-CONCLUSIONS-IN-ONE-SESSION-FROM-READING-A-STORED-LABEL-AS-THE-MEASUREMENT, whose criterion has TWO halves that are closed by different things. HALF (a), MECHANICAL — 'surfacing that discrepancy ON the per-account read path would have caught member 1 mechanically' — shipped 2026-08-26 under the sibling row BL-20260826-JOURNAL-READS-DO-NOT-CONSULT-THE-BROKER-TRUTH-LEDGER (resolved) as src/runtime/broker_truth.py + the `journalTrust` field. SHIPPED IS NOT EXERCISED, so this was NOT closed on the diff: MEASURED on the LIVE route 2026-09-12 via https://ict-bot.duckdns.org (positive control /api/health -> 200 {ok:true}) — /api/bot/trades/closed?account_id=bybit_2 returns journalTrust='known_divergent' 3 of 3, and the NEGATIVE control, without which the field could be a constant, is bybit_1 / alpaca_paper / ib_paper each returning 'no_record' 2 of 2. HALF (b), REVIEW DISCIPLINE — 'written where it will actually be read rather than added as a fifth bullet to a rule that already exists' — is this diff, in exactly that shape. docs/CLAUDE-RULES-CANONICAL.md gains a TOP-LEVEL binding section, 'The row you are reading outranks the label in it', in the binding-rules block beside 'Always state the population' and 'Collapsed states', NOT a bullet under RULE ONE — and it opens by saying so and why: RULE ONE was in force and binding for all four members and prevented none, because a journal query IS a measurement, of the journal. It carries the four members as a table naming the settling field per member, records that in THREE of the four that field was IN THE ROW ALREADY BEING READ, states the population (one session, 2026-08-26; the drafted outputs were a real-money demotion and a Tier-3 entry-gate change, stopped only by the operator), and carries the fifth TIME variant. POINT OF USE, because a canonical section nobody opens is the very failure this row is about: a short pointer sits directly under the data-pull table in BOTH .claude/skills/performance-review/SKILL.md and .claude/skills/health-review/SKILL.md — where a review session is looking at the moment it is about to make this mistake, and binding via skill-first lookup. ml-review is deliberately NOT patched: it reads models and registries rather than trade rows, so a copy there is noise and a third place to drift. EVERY MECHANISM CITED WAS VERIFIED BY RUNNING IT, not by reading its docstring: column_provenance.py exits 2 on trades.stop_loss and exits 1 ('we could not look', explicitly NOT 'nothing writes it') on an invented column; strategy_liveness.py exits 2 on vwap with 'quote it in the PAST TENSE' and 0 on trend_donchian_sol_4h, and its `unknown` state is covered by its own --self-test, which is a PLANTED control and is described as one rather than claimed as a live observation. AND A NUMBER WAS CORRECTED RATHER THAN COPIED: RECURRENCE-LEDGER.json says trades.stop_loss has '10 writing modules'; running the tool today gives 119 write sites across 11 MODULES, where the tool's 'module' is a DIRECTORY not a file (19 distinct .py files). The section quotes the measured figure with its date AND its unit, tells the reader to re-run rather than quote either, and states that the tool greps assignment sites so fixtures are counted — it answers 'is this column singly-owned?', not 'how many production writers are there?'. TWO NEW ROWS FILED, both found by doing this work rather than by looking for work, and both filed SEPARATELY rather than folded in because fixing one does not fix the other: (1) BL-20260912-JOURNALTRUST-DROPS-THE-LEDGERS-AS-OF-AND-MAGNITUDE-AT-BOTH-WIRE-SURFACES-SO-A-61-DAY-OLD-RECONCILIATION-READS-AS-A-FRESH-ONE — journal_trust_for computes `as_of` and `realized_usd` and BOTH consumers discard them, so the committed ledger's 2026-07-13 reconciliation (61 days old, -$262.52) renders byte-identically to one taken today; measured on both live surfaces, PROPOSAL ONLY since the fix is src/** and therefore Tier-2 by this guard's path rule. (2) BL-20260912-THE-RECURRENCE-LEDGERS-LARGEST-CLASS-STANDS-AT-ITS-OWN-ESCALATION-TRIGGER-AND-THE-ESCALATION-WAS-NEVER-BUILT — RC-STORED-FIELD-READ-AS-ITS-NAME reads `occurrences: 10` while conditioning its escalation on 'if a 10th instance occurs'; git shows the file has two commits and read 10 in both, so it was never 9 on disk, and the guard it names does not exist while CI runs only the two --self-tests. TIER: every path is vouched by check_pr_landing's own TIER1_SURFACE, verified by calling _match against it WITH negative controls (src/foo.py False, config/strategies.yaml False+Tier-3, src/runtime/orders.py False+Tier-3). Note the two SKILL.md files vouch via the global `*.md` entry rather than a `.claude/**` entry, since fnmatch's `*` crosses `/` — recorded so the reader knows which rule admitted them. No runtime, no order path, no config, no unit file. The backlog edits went through backlog_append (update_row + append_row) and the diff is 47 insertions / 2 deletions with 0 of main's 1541 rows lost, so the byte round-trip held and nothing was re-attributed to this PR."
}
