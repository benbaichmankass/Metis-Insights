{
  "tier": 1,
  "landing": "self",
  "why": "MI-318, the residual MI-312 left: is a stronger control available for the non-clamping scalp/fvg family, or is the weakness structural? Answer: structural, established as a BOUND over one already-committed artifact. The diff adds one memo (docs/research/scalp-control-design-2026-09-18.md), its JSON, one read-only research script (scripts/research/mi318_scalp_control_bound.py, 15 selftests of which 4 are negative controls), one work object carrying it, one row each in DOCUMENT-INDEX.md and RESEARCH-CAPABILITY-INDEX.md, and one health-review-backlog row filed through scripts/ops/backlog_append.py. Tier-1 by the canonical doc's own list and verifiable against the diff: NO config/ -- no tp_at_r or tp_r value is proposed and per-leg target geometry stays Tier-3 -- and no src/, tests/, .github/workflows/, deploy/, order path, strategy params or risk caps. The script is READ-ONLY and COMMISSIONS NOTHING: it opens exactly one committed JSON (docs/research/mi312-scalp-target-arms-2026-09-18.json) and writes one JSON under docs/research/. No network call, no harness invocation, no candle fetch, no DB write, no exchange client. It is wired into no workflow, cron or runtime path (declared `# wiring: manual-only` with its reason), so landing it changes nothing that executes. The only files outside docs/ and scripts/research/ are this landing declaration plus the arming file and merge-slot claim the self-land route itself requires."
}
