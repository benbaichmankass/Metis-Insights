{
  "tier": 1,
  "landing": "self",
  "why": "CI guard + ops tooling + docs only. Adds scripts/ci/check_register_ids.py (register-id-guard) and registers it in run_guards.py; adds a uniqueness CHECK to scripts/ops/session_registry.py's key mint and makes confirm() refuse an ambiguous key; adds a merge-driver-armed check to scripts/ops/manager_preflight.py; repairs 2 duplicate registry_key values in docs/claude/work/SESSIONS.json (85 rows before and after, all 85 session_ids preserved, 6/4 line diff); files 2 backlog rows; documents the design in docs/claude/work/README.md. No runtime, no order path, no strategy config, no risk caps, no live-VM file, no deploy unit. Every path is inside the Tier-1 allowlist (docs/**, scripts/ci/**, scripts/ops/**, .github/**) except automation/board-posts+board-results, which are the board-post relay's own request and receipt for this branch's mandatory START comment."
}
