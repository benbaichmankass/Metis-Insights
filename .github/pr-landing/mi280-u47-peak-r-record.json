{
  "tier": 1,
  "landing": "self",
  "why": "Two rows in docs/claude/health-review-backlog.json: one resolved on an observed census, one whose resolution_criteria is amended so its `resolved` stops implying the stored column was cleaned. No code, no workflow, no src/, no config/, no deploy/ -- every path is inside TIER1_SURFACE. Recording a measurement and correcting a review record are Tier-1 by the canonical doc's own examples. ⚠️ NO DATA WAS WRITTEN: the census is a READ through the token-gated /api/diag/position_telemetry route, and the row's criterion (1) explicitly asks for a re-run of the census probe rather than a migration. Cleaning the column would have been a Tier-2 DB writeback and was neither needed nor done. ⚠️ PROCESS NOTE, recorded because it is a repeat of a documented trap rather than a one-off: this PR (#12087) was opened by the claude-pr-automerge workflow, NOT by hand, because the content and the arming file went out in ONE push instead of two. The workflow found no open PR, created one under GITHUB_TOKEN, and GitHub fires no pull_request event for that -- so the PR was born with NO CI while auto-merge waited on required checks that did not exist (BL-20260903-A-PR-OPENED-BY-THE-RELAY-CAN-ARM-AUTOMERGE-AND-THEN-WAIT-ON-GREEN-FOREVER). The commit carrying this note is the CI-arming push. CLAUDE.md prescribes the two-push order precisely to prevent this and it was not followed."
}
