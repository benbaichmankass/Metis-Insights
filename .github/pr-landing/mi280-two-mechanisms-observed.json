{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1: docs only — docs/claude/health-review-backlog.json, one appended row and one in-place update to an existing row's detail. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no scripts/, no workflows, no live-VM runtime file. Not landing machinery, so R12 does not apply.\n\nIt RECORDS evidence about the landing machinery; it CHANGES none of it. That distinction is the whole reason this can self-land while #11936, #11930 and #11958 could not: LANDING_MACHINERY is a list of PATHS, and this diff touches none of them.\n\nContent: (1) a resolved row recording that the arming relay's crash is fixed and OBSERVED -- 129/182 failures before the fix, 0/6 after, across six distinct branches -- and that #11936 landed carrying no backlog or OPEN-ITEMS row, so nothing would have made a later session check it; (2) an in-place update discharging sub-case (i) of BL-20260909-A-GREEN-ARMED-AUTOMATION-PR-STALLS-FOREVER-WHEN-MAIN-MOVES-BECAUSE-AUTO-MERGE-DOES-NOT-RESOLVE-CONFLICTS on measured fleet evidence from PR #11941, with the row deliberately left OPEN because sub-case (ii) is untouched.\n\nThe backlog file is rewritten programmatically, never by hand: the exact serialisation (json.dumps(indent=2, ensure_ascii=False) + newline) is asserted byte-exact BEFORE the edit, the insertion is asserted to land exactly once, and len(out) - len(raw) is asserted equal to the inserted length so the only byte change in 1510 rows is that one insertion.\n\n[!] THE ROW ID ABOVE IS WRITTEN OUT IN FULL AND MUST STAY THAT WAY. It read as a truncated prefix on the first attempt and check_backlog_refs.py correctly refused the branch: a partial id resolves to NOTHING, so a declaration saying 'tracked by BL-X' reads as tracked while being tracked by nobody. Shortening it to fit a line is the same defect wearing a tidier shape."
}
