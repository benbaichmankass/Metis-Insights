{
  "tier": 1,
  "landing": "self",
  "why": "One new row plus one `updates` entry in docs/claude/health-review-backlog.json. No code, no workflow, no src/, no config/, no deploy/ -- every path is inside TIER1_SURFACE, so the declaration is `self` because that is the truth about the diff. NOTE FOR THE READER, since the PR itself is about this: claude-pr-automerge will REFUSE to arm it, because it is a draft this workflow did not open -- that refusal is the exact condition this PR documents. A `hold` declaration was considered and rejected as dishonest: none of the six typed hold_reasons fits (`changes_landing_machinery` and `unvouchable_paths` are VERIFIED against the diff and would both fail, it is not Tier-2/3, no operator asked, and no unmerged PR is named), and inventing a fit to dress up a voluntary choice would make the declaration say something the diff does not support. The manager merges this one by hand."
}
