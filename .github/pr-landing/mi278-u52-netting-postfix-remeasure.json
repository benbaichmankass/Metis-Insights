{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U52, RESEARCH lane. Tier-1 by R5's surface test: one new instrument under scripts/research/, one new memo under docs/research/, one additive backlog `updates` entry, one DOCUMENT-INDEX row and one RESEARCH-CAPABILITY-INDEX row. NO src/, NO config/, NO deploy/, NO workflow, no order path. Two live diag READS (journal trades, journal order_packages) and one log_file read; nothing is written to any VM. || WHY A NEW INSTRUMENT RATHER THAN AN EXTENSION, stated because the default is to extend: winner_size_collapse_2026_09_12.py is a declared one-off over a DATED event whose SPLIT is deliberately imported from MI-271 'so the two memos can never silently disagree about where the eras divide'. Moving its boundary would retroactively change what its published memo means. This asks a different question (did a specific DEPLOY change the path's contribution?) on a different boundary, over a scope that module cannot express. It IMPORTS rather than re-derives: risk_usd from that same module, and the corrected scope from U33/U49's rule. || WHAT IT DELIVERS. (1) Option (a) is REACHABLE and the row does not know it -- MI-281's correction records OI-20260908's root cause landed as 46e1efb1f and DEPLOYED 2026-09-08T18:53Z. (2) A SECOND fix to the same chain, PR #11903, merged 2026-09-12T19:47:52Z INSIDE the naive post-fix window, so the eras are four and a pooled result attributes to neither; the pooled arm is labelled as such in the output. (3) MEASURED on bybit_1 n=477: netting share 7.9% -> 11.1% -> 6.3% -> 4.2%, five contrasts computed and reported together with the Bonferroni line, NONE surviving 0.05/5. (4) THE SCOPING RULE DECIDES THE VERDICT: on the row's own exit_reason filter the pooled contrast is p=0.0375 SIGNIFICANT; on the corrected stamp-AND-soak union it is p=0.0681 NOT -- the filter misses 7 of 33 applied soak rows unevenly across eras. A session using this row's own scope would have reported a significant post-fix improvement. (5) The unsound scope is BIASED TOWARD estimated, which softens the row's '39 of 39 ESTIMATED' headline WITHOUT contradicting it: 2 of 39 are measured under the corrected scope and both entered via notes_stamp, never via exit_reason, because a row the filter misses closed under sl_cross and an sl_cross gets a broker-truth exit. (6) The row's own question returns insufficient_n at 4 graded post-fix winners against its own published n of 11, and the wait is converted into DATES (~2026-09-28 share, ~2026-10-01 winner-R) explicitly labelled planning figures. || A FALSE NEGATIVE IN MY OWN FIRST DRAFT IS RECORDED RATHER THAN QUIETLY FIXED: it read a `trades.r` column that does not exist (0 of 1000 rows carry one) and graded both arms `unmeasurable`. R is now imported from the single owner, whose docstring records why trades.stop_loss is the wrong basis. || AND A PLANTED DEFECT SLIPPED A 48-CONTROL SUITE: folding a missing risk basis into the arm as R=0.0 passed, because no fixture row lacked a package. Six controls were added and it is caught; the suite was strengthened, not the module. || THE ROW DOES NOT CLOSE and nothing claims it does: both halves of option (a) are now reachable, neither is answered, and option (b) was not re-attempted. || VALIDATED BEFORE PUSH on committed state: 54 self-test controls, 14 planted defects all caught by a named control, ruff clean, and the guard set run on committed state."
}
