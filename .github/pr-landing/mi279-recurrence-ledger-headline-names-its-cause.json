{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling: scripts/ci/check_recurrence_ledger.py and one NEW test file. No register touched, no src/, no config/, no deploy/, no workflows, no scripts/ops. NOT in LANDING_MACHINERY, so R12 does not apply. SECOND MEMBER of the class #12401 fixed, found by sweeping the ERROR BLOCK rather than the census loop. WHAT IT FIXES, REPRODUCED RATHER THAN CONSTRUCTED: against main @a374d3dd9, over a ledger in which EVERY class names an executable prevention and whose only fault is a duplicate id, the guard printed '::error::docs/claude/RECURRENCE-LEDGER.json -- a repeated mistake with no prevention is a lesson nobody learned:' followed by '- RC-SAME: duplicate id'. Nothing about preventions was established on that run. UNPROVENANCED DIAGNOSTIC OUTPUT sub-class A. 9 of the guard's 13 rules are not about preventions at all, and the worst is a ledger that could not be READ -- the run learned nothing whatsoever and reported that a class had no prevention, i.e. 'we did not look' printed as a finding about preventions. [!] THE HARM IS THE REMEDY: 'no prevention' says BUILD A GUARD, a malformed row says FIX THE ROW'S FIELDS, an unreadable file says RESTORE IT NOTHING WAS CHECKED -- one headline sent all three readers to build a guard. [!] A SECOND DEFECT IN THE SAME SENTENCE: it hardcoded docs/claude/RECURRENCE-LEDGER.json while --path pointed elsewhere, naming a file it had not read alongside a cause it had not tested; the path is a parameter in all three headlines now and a control pins it. WHAT SHIPPED: three causes (UNREADABLE / MALFORMED / NO_PREVENTION), each with its own headline and remedy; check() returns (tag, message); group_by_headline() prints only the causes that fired, in a fixed order, with UNREADABLE first because if nothing was read that is the only thing the run can honestly say. An untagged rule is a hard KeyError, never a silent fallback -- a rule added later quietly inheriting a neighbouring cause is exactly how this arose. [!] I DELIBERATELY DID NOT EXTRACT THE SHARED HELPER and the argument is recorded in the docstring rather than assumed: scripts/ci/_git_base.py, the ONE shared helper under scripts/ci/, was extracted off a MEASURED population of 12 scripts of which 5 were wrong; here the measured population is TWO call sites with no observed divergence, so a shared module would be justified by symmetry rather than evidence. The trigger is written down: extract when a THIRD consumer appears, or the moment the two behave differently. [!] AND I CORRECTED MYSELF ON THE CLASS SIZE BEFORE ACTING: my first sweep called scripts/ops/open_pr_record.py a member; it is NOT. I had classified it from an AST sweep of message strings WITHOUT READING THE PRINT PATH -- it prints four separately-graded sections each with its own state line and per-finding label, so no finding appears under a cause it did not test. What survives there is weaker and different (its refusal headline names two of its three trigger conditions) and is offered on the board, not filed here. POPULATION, STATED: all 47 files under scripts/ci/ + scripts/ops/ printing an ::error:: headline; 14 print exactly one; those 14 read by hand. Members: check_register_ids (#12401) and this one. check_open_items, session_registry and check_document_index are CLEAN -- three of the four I expected to be members were fine once read, so this retires the class at two files and is not claimed as more. CONTROLS: 13 new --self-test cases (21 total) plus 7 end-to-end pytest cases that run the guard AS A SUBPROCESS so what main() PRINTS is what is asserted. FIVE PLANTED DEFECTS, ALL CAUGHT: (A) a duplicate id back under the prevention cause = the pre-fix behaviour, 1 FAIL / 3 FAIL; (B) the prevention headline's distinguishing words deleted = THE POSITIVE CONTROL, 1 FAIL / 2 FAIL, load-bearing because without it deleting that headline would satisfy every negative assertion while destroying the rule the guard exists for; (C) a cause that found nothing still prints its headline, 2 FAIL / 3 FAIL; (D) the canonical path hardcoded back, 1 FAIL / 1 FAIL; (E) an unreadable ledger back under the prevention cause, 1 FAIL in pytest, plus an untagged-rule plant caught by the self-test. [!] ONE OF MY OWN CONTROLS WAS WEAK AND IS RECORDED RATHER THAN QUIETLY FIXED: my order control compared two ORDERINGS of the same input for equality, which proves order-INDEPENDENCE and passes on any fixed permutation including a wrong one; it asserts the actual sequence now. VERIFIED ON COMMITTED STATE, no pipes: --self-test EXIT=0 (21 PASS), the guard against the REAL ledger EXIT=0 ('5 class(es), 0 awaiting a prevention'), pytest 7 passed, ruff clean."
}
