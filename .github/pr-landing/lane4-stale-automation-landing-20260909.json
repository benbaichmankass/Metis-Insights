{
  "tier": 1,
  "landing": "self",
  "why": "DOCS ONLY, ONE FILE: docs/claude/work/objects/WO-20260909-EVERY-PR-IN-THE-REPO-IS-RED.yaml. It corrects a claim that #11519's own merge falsified. Throughout this WO — and in #11519's body and hold_text — I wrote 'the workflow is not on `main`, so it has no cron: the SCRIPT is proven, the MECHANISM is not.' #11519 merged as a1ec4a5c0 at 2026-09-10T15:09:10Z, so the first two clauses are now FALSE and a session reading the object top-to-bottom would carry them forward. VERIFIED ON MAIN rather than taken from the merge report: the script, its tests and .github/workflows/stale-automation-sweep.yml are all present; the workflow carries cron '17 */2 * * *' and workflow_dispatch; both workflow and script are registered in LANDING_MACHINERY (scripts/ci/check_pr_landing.py:296-297); GitHub's own view reads workflow id 355025104, state=active, created 2026-09-10T15:09:12Z, so the cron is REGISTERED and not merely written down. ⚠️ AND THE HALF THAT MUST NOT COLLAPSE INTO THAT ONE: POPULATION = every run of workflow 355025104, all events, all branches, no status filter — list_workflow_runs returns total_count 0. The mechanism is DEPLOYED and has NEVER RUN; a deploy is not a run. The new section states, in advance, how the first run must be graded — it must carry event=schedule (a hand-fired workflow_dispatch proves the file parses and nothing about the cadence, which is the whole class fix); its SWEEP JOB'S own conclusion must be read, never the run's top-level success, per the trap this WO already records (run 34383191261 concluded success with its digest job SKIPPED behind a gate); exit 2 means COULD NOT LOOK; and any refresh must be verified as EFFECT against the merge-base, never from the sweeper's report. It also records what a clean first run would still NOT prove: 0 open PRs grade `refresh` today, so the likely first run does nothing — that is the mechanism observed running, not the refresh path observed running in CI, whose seven end-to-end verifications were all HAND-RUN from this session's tree. The earlier paragraph is marked superseded IN PLACE and kept, not deleted, so the record of what was believed when it was written survives. No code changes: the diff touches no src/, config/, deploy/ or order path, and no landing machinery (this branch's own pr-landing declaration is excluded by R8 for the reason the guard states)."
}
