{
  "tier": 1,
  "landing": "self",
  "why": "Manager state only -- docs/claude/** registers, the manager lease, five rescued pending-pings rows, three new work objects/checklist rows, and the regenerated CLAUDE.md SESSION-BRIEF block. No runtime, no order path, no strategy config, no risk cap, no live-VM file, no workflow. The one file outside docs/claude/ is this declaration. Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own definition (docs, tooling, observability, retrieving and recording state), so it lands itself on green rather than waiting on an approval nobody is owed."
}
