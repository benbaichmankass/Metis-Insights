{
  "tier": 1,
  "landing": "self",
  "why": "Register record only: one surgical text edit to a single OPEN-ITEMS row's `observation`, recording two clauses OBSERVED met and one REFUTED as written, plus its landing/arming files. No code, no runtime, no order path, no workflow. Edited as TEXT rather than round-tripped (manager_preflight.py records that a naive json round-trip of OPEN-ITEMS.json rewrites 100% of the file); the diff is 1 line and exactly 1 row differs from HEAD, verified by comparing every row object against `git show HEAD:`."
}
