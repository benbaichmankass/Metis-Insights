{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain U3 — oldest-first over the open `high` rows, and a real CLOSURE. One file: docs/claude/health-review-backlog.json, one row moved open -> resolved with its `notes` replaced, written through backlog_append.update_row (3 insertions / 2 deletions, byte-local, row count unchanged). No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHAT IT CLOSES: BL-20260730-PR-CI-NOT-ATTACHING, the OLDEST open Tier-1 high row, opened 2026-07-30 and closed wrongly twice before on an existence proof over one or two heads. Its criteria ask for one thing — establish the RATE of PR check attachment over a named window of pushes to claude/** branches, with the denominator stated — and impose three method requirements, each traceable to a specific past error. All three are met: scored from the workflow-run list's `event` field rather than get_check_runs; no head scored as a miss at all, so the 5-minute latency rule cannot have produced a false one; and no workflow_dispatch was issued on any scored sha. MEASURED: 40 graded heads across 4 branches and 4 PRs, 40 attached, 100.0%, latency median 8s and max 253s. Four pre-PR heads are excluded on a stated rule — GitHub creates pull_request runs for the tip at `opened`/`synchronize`, so a commit superseded before the PR existed cannot have one — with the control that on the two branches where the PR opened right after the first push, that first commit DID attach. THE CLOSURE IS BOUNDED RATHER THAN TRIUMPHAL, which is the point: with 0 failures in 40 the 95% upper bound on the per-push failure rate is 7.2%, so the row records that the original intermittency is not present at a rate this sample could detect and that a residual below ~7% is not excluded. It also explicitly does not re-open the original diagnosis, does not claim anything about other actors or the merge_group event, and preserves the row's standing advice that total_count==0 on an older PR is ABSENT rather than pending."
}
