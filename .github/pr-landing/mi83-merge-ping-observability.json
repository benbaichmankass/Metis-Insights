{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "depends_on_unmerged_pr",
  "depends_on": "#10933",
  "why": "Tier-1 by every path in the diff: one INFO log line in scripts/notify_on_pull.py, two tests, and a surgical three-field correction to one OPEN-ITEMS row. No runtime, no order path, no strategy config, no risk caps, no live-VM file. The author graded it Tier-1 and the diff agrees.",
  "hold_text": "WRITTEN BY THE MANAGER, NOT THE AUTHOR — session_018ruZdgEkPZ1XceWzLwuYUU is idle and disconnected, and this branch had NO declaration at all, so R11 would have failed it whatever its tier. Held on QUEUE ORDER, and the dependency is real rather than fitted: #10933 is ahead of it, has already had main merged in (249ff8da) and is running CI, and both PRs touch the shared JSON registers. Landing this one first would dirty #10933 server-side and force a second local merge — measured behaviour, not a worry: GitHub cannot merge a register conflict at all (the row-aware driver is client-side), so every re-dirtying costs a human with an armed clone. THIS PR IS THE HIGHEST-VALUE ITEM IN THE QUEUE BEHIND #10933 and should go next, not last. It converts an UNVERIFIABLE row into a verifiable one: OI-20260902-PER-MERGE-PING has never been observed, and measured again at 13:33Z today the reason is exactly the defect this PR fixes — notify_on_pull logged `Queued 2 ping(s)` with no source named, so a healthy run is indistinguishable from one that never fired. The operator's standing gate is 'finish the workflow overhaul INCLUDING verifying what shipped', and this is the observability that makes one of those verifications possible at all. Un-drafted by the manager at 13:4xZ: a draft is a hold its own author cannot lift (sub-sessions 403 on update_pull_request), and per #10923 the declaration is the hold, not the draft flag."
}
