{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane. Docs only: ONE appended `detail` on the already-open BL-20260912-A-WORK-OBJECT-IS-PARKED-WAITING-ON-AN-OPERATOR-DECISION-THAT-WAS-ANSWERED-THREE-DAYS-EARLIER-AND-NOTHING-CLEARS-A-SPENT-BLOCKED-ON-EDGE, written through backlog_append.update_row (1 insertion / 1 deletion over 1534 rows, id list identical, exactly one row differing). No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY AN APPEND AND NOT A NEW ROW: that row explicitly said the sweep was owed and named the population as UNMEASURED; the answer belongs on the row that posed the question, and a second row would split the instance from its own population. WHY NOTHING IS FIXED: an object's lifecycle and its blocked_on edges are its owner's or the manager's call, and flipping `waiting` to `ready` changes what check_wip_ceiling and the constraint readout compute over -- three objects look releasable today on decisions already taken, which is a PROPOSAL to the manager rather than an edit a worker lane makes. WHAT THE MEASUREMENT ADDS: at n=1 the row carried an instance with a plausible mechanism; at 12 of 12 (with ZERO live) the mechanism is established, which is what moves its own criterion (b) from speculative design to the only fix that reaches all twelve. A correction to my own first pass is recorded in the append rather than quietly fixed: an `answered_at`-only probe reports five genuinely-answered requests as unanswered stubs, because the schema also uses `answered_on` -- so the naive probe UNDERSTATES the spent set, and the next session is told to grade on both keys."
}
