{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5 (the DUE monitoring rows). Docs only: `summary`, `observation`, `verified_at` and `last_checked` on OI-20260901-CLAUDE-CHANNEL-SEPARATION-SHIPPED-BUT-UNPROVEN in docs/claude/OPEN-ITEMS.json, plus the mechanical re-render of CLAUDE.md's generated SESSION-BRIEF block. Byte-local splice read back through a duplicate-key-detecting parse asserting exactly one row changed and only those four fields, with an extra assertion that the ORIGINAL summary text survives verbatim inside the new one; 84 items before and after. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHAT WAS OBSERVED: clause (a) of the row's clears_when is MET -- the live ict-claude-bridge.service startup banner at 2026-09-12T18:16:37Z reads token=TELEGRAM_CLAUDE_BOT_SECRET[dedicated] ... isolated=True, against the isolated=False the row was opened on. This is the FIRST observation ever recorded on the row; verified_at had been null since 2026-09-01. THE ROW DOES NOT CLEAR: clause (b) needs a human to say which Telegram conversation the ping landed in, and the row itself explains why no VM-side read can substitute. WHY THE SUMMARY IS CORRECTED RATHER THAN ONLY THE OBSERVATION: the summary asserted that what remained was one Tier-2 set-env putting TELEGRAM_CLAUDE_BOT_SECRET in the VM .env, and the bridge is now resolving that very key as [dedicated], so that step has been done. The session brief renders summary and clears_when and NOT observation, so a correction left only in observation would not reach the session that would otherwise go and ask the operator for a Tier-2 they have already granted. The original wording is preserved inside the corrected summary as the record. NO PING WAS SENT to test this, deliberately: the row states delivery is not the criterion and that pings have been delivering throughout, so one would add a message to the operator's phone and no evidence."
}
