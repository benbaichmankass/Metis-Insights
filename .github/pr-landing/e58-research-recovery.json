{
  "tier": 1,
  "landing": "self",
  "why": "E58 (checklist row E58, ceiling $50): inventory every research/evidence workflow run whose result never reached main, recover what still exists, re-run what was lost. Two files only. (1) docs/research/e58-research-loss-inventory-2026-09-24.json -- the read-only inventory, docs/**. (2) .github/workflows/research-exit-head-build.yml -- a one-condition fix, .github/**, gating the `research_result` job on the same trigger check `build` already uses, so an unrelated issue (a diag-request, a system-action, a manager dispatch) can no longer make this workflow fabricate a `producer_failed` placeholder result and open a PR for it. Root cause found while doing the inventory: all 13 open `research result: unattributed` PRs this workflow had produced trace to that one collapsed-states bug, not to a real lost measurement -- verified per-run via list_workflow_jobs (`build: skipped` on every one) and via each run's own triggering issue (system-action / trainer-diag-request / manager dispatch, never a research-exit-head-request). Those 13 PRs are closed (not merged) as part of this session, separately from this PR, as garbage rather than evidence. No src/, no config/, no order path, no live surface. The mechanism half (pr-landing-guard R5's allowlist) is E57's row and is not touched here; the 16 genuinely R5-blocked PRs (13 replay-pregate-nightly + 3 research-queue-dispatch) are left open -- auto-merge is already armed on each and they self-land once E57 lands."
}
