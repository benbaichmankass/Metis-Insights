{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 by every path in the diff, checked against check_pr_landing.py's own TIER1_SURFACE allowlist rather than inferred: scripts/ops/** (owner_liveness.py — one function's mapping plus docstrings), scripts/ci/** (check_stale_in_flight.py self-test fixtures + a pinned clock; check_collapsed_states.py — one dated `why` string, no contract added or removed), tests/** (7 tests appended, 1 renamed), docs/** (one section appended to an already-indexed evidence doc), docs/claude/health-review-backlog.json (one row via backlog_append.py::append_row). No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps, no live-VM file. R12 WAS CHECKED AS A LIST, not reasoned from filenames — LANDING_MACHINERY names .github/workflows/{claude-pr-automerge,pr-opener,board-post}.yml, .github/pr-landing/*.json, scripts/ci/check_pr_landing.py, scripts/ci/check_automerge_trigger.py, scripts/ops/session_registry.py, .github/actions/commit-to-main/action.yml, scripts/ops/claim_merge_slot.py and scripts/ops/sweep_stale_automation_prs.py. None of my four scripts is on it; only this branch's OWN declaration is touched, which R12 excludes by construction (p != decl_rel). NO TEST OR GUARD IS WEAKENED: the carrier's self-test GAINS two checks and its clock is pinned (its fixtures carried fixed timestamps against a wall clock, so their ages grew daily), the suite goes 18 -> 25, and BOTH load-bearing invariants are mutation-checked — reverting the fix fails 4 tests + the self-test, making it symmetric fails 6 tests + 3 self-test checks. RATCHET-SAFE BY CONSTRUCTION, verified not asserted: the only verdict transition this change can produce is `supported` -> `could_not_establish`, never anything -> `unsupported`, and `check_stale_in_flight.py`'s ratchet counts `unsupported` — measured base=13 head=13 delta=+0, exit 0, so it cannot red a PR over the pre-existing debt. NOTHING MUTATES A LIFECYCLE FIELD: this object's own design constraint is detect-and-report, and no row's `lifecycle`/`status`/`state` is touched, so no WIP slot changes and no wip-ceiling-exception is needed. Evidence: docs/claude/work/IN-FLIGHT-OWNER-LIVENESS-2026-09-11.md section 7."
}
