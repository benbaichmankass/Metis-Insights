{
  "tier": 3,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "Tier-3 by path: config/accounts.yaml, real-money account bybit_2. ⚠️ THE APPROVAL IS ALREADY GIVEN AND THIS HOLD IS NOT WAITING ON A DECISION — it is R4, which bars any Tier-3 diff from landing itself whatever its approval state, so a human must click merge. Operator approved 2026-09-06 in conversation, on a question that named this exact lever and its tradeoff against `execution: shadow`; scope bybit_2 only, no condition attached. Recorded as a typed decision in docs/claude/work/OPEN-PRS.json (verdict approved, scope 'bybit_2 only; ...'), verified by open_pr_record.py --strict as decisions=graded. Do not re-litigate it; merge it.",
  "why": "one field in config/accounts.yaml — removes ict_scalp_5m from bybit_2's strategies list. config/strategies.yaml untouched, so the leg keeps execution: live and both paper books keep trading it. No order-path code, no strategy params, no risk caps, no regime cell."
}
