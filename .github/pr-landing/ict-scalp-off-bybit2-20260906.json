{
  "tier": 3,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "Tier-3 by path: config/accounts.yaml, real-money account bybit_2. ⚠️ THE APPROVAL IS ALREADY GIVEN AND THIS HOLD IS NOT WAITING ON A DECISION — it is R4, which bars any Tier-3 diff from landing itself whatever its approval state, so a human must click merge. Operator approved 2026-09-06 in conversation, on a question that named this exact lever and its tradeoff against `execution: shadow`; scope bybit_2 only, no condition attached. Recorded as a typed decision in docs/claude/work/OPEN-PRS.json (verdict approved, scope 'bybit_2 only; ...'), verified by open_pr_record.py --strict as decisions=graded. Do not re-litigate it; merge it. ⚠️ ONE THING THE APPROVAL DID NOT LITERALLY COVER, ADDED AFTER CI CAUGHT IT: bybit_portfolio (PAPER) also drops ict_scalp_5m, because tests/test_paper_portfolio_accounts.py::test_bybit_portfolio_mirrors_bybit_2_exactly enforces a ROSTER-SYNC invariant tying its roster to bybit_2's. The approval said 'bybit_2 only', so this is a second account -- but it is a paper book with no money at risk, the mirror exists to reflect the REAL portfolio (leaving the leg on it would make it misreport that), and the soak evidence the lever was chosen to preserve is unaffected because bybit_1 (the full-roster soak book) keeps trading it. Confirm you are content with that when you merge.",
  "why": "one field in config/accounts.yaml on bybit_2 -- removes ict_scalp_5m from its strategies list -- plus the same removal on bybit_portfolio, which the ROSTER-SYNC test requires to follow it. config/strategies.yaml untouched, so the leg keeps execution: live and keeps trading on bybit_1, the full-roster soak book. No order-path code, no strategy params, no risk caps, no regime cell."
}
