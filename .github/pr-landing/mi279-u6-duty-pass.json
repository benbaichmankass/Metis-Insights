{
  "tier": 1,
  "landing": "self",
  "why": "A duty pass: one operator-owed row RESOLVED on verified evidence (its three PRs are ancestors of main and the arming it was about is observed working), one OPEN-ITEMS observation recording a re-measured soak, and the generated session brief re-rendered as session-brief-guard requires. docs/claude/** and *.md only — no src/, no ml/, no config, no scripts, no workflows, no order path. Nothing is enacted and nothing is decided; the resolved row is closed on an observation, not on an assumption."
}
