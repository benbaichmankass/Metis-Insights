{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "Seeking one line of review from the operator (or the MI-157 manager session) before merge. The tier judgement is 1 and I stand behind it, but pr-landing-guard R5 will not certify two of this diff's surfaces for self-landing -- data/ohlcv/*.csv and bin/backtest_ict.py -- and that refusal is CORRECT, so it is being honoured rather than engineered around. The obvious workaround was to widen TIER1_SURFACE in this same PR; that is the 'a guard cheaper to lie to than to satisfy' antipattern CLAUDE.md names, and it would be especially wrong for data/**: a committed corpus IS the instrument every backtest is graded against, which is the entire subject of this change. A session that can swap the corpus under every research verdict without a human read is the failure class MI-157 exists to close, not a convenience to grant. bin/** is a weaker case (research scaffolding that states it does not touch live trading) but narrowing to it alone would not unblock self-landing anyway. What a reviewer is being asked to confirm: that deleting five constant-price candle fixtures, and making both research candle readers refuse a degenerate series, is the intended repair.",
  "why": "Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own examples -- tests, CI guards, tooling and research read paths. Touches no config/, no src/runtime/, no order path, no strategy params, no risk caps, and no live-trader code path. The five deleted files are candle FIXTURES that .gitignore line 75 already declared should never have been committed; the two code edits add a refusal to research-only candle readers (scripts/candle_io.py, bin/backtest_ict.py, the latter self-documented as scaffolding that does not touch live trading). No strategy backtest was run or re-interpreted -- this repairs the instrument and deliberately draws no verdict from it. The reason it is nonetheless HELD is in hold_text: the guard cannot vouch for data/** or bin/**, and I decline to widen a safety allowlist inside the PR it would let through."
}
