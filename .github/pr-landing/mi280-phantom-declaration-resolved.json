{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1: docs only — one row in docs/claude/health-review-backlog.json moves open -> resolved. No src/, no config/, no deploy/, no scripts/, no workflows. Not landing machinery, so R12 does not apply.\n\nThe row is CRITICAL and describes a live repo-wide outage that no longer exists: a consumed register-removal declaration inherited from the base phantom-failing every PR. Criterion (b), scope the read, shipped in #11945 (dbe958c6) the same day; MI-282 measured against origin/main at 66a0035e5, which does NOT contain that commit, so the finding was right for the tree it ran on and stale for the current one.\n\nVerified by a three-arm A/B on identical trees with only the guard file swapped, using the REAL consumed declaration restored from c050b1f9c: inherited + pre-fix guard gives 1 phantom and rc=1, reproducing the outage and naming the exact row; inherited + post-fix gives 0 read / 1 inherited and rc=0; and the same declaration placed IN the diff still gives 1 phantom and rc=1, so the verification is undiminished. That third arm is the row's own caveat about not making the phantom check non-fatal, checked rather than asserted.\n\nLeaving a CRITICAL row open while CI is green is worse than a stale row: it invites the next session to conclude the guard is broken, and the cheapest way to stop a guard reporting an outage that is not happening is to weaken it."
}
