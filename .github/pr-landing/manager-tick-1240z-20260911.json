{
  "tier": 1,
  "landing": "self",
  "why": "Manager registers only: MANAGER-CHECKLIST.json, SESSIONS.json, MANAGER-LEASE.json and one edit to an existing health-review-backlog row. No src/, no config/, no deploy/, no scripts/, no .github/workflows/, no order path -- every path is inside TIER1_SURFACE, which is why this PR declares landing: self and arms rather than reaching for a hold. THE MANAGER DELIBERATELY DID NOT USE ITS OWN NEW MERGE-SLOT ESCAPE HERE. That escape (drop the arming file and the merge_slot claim, keep the declaration, merge by hand) is available only to a PR that can TRUTHFULLY declare one of the six closed HOLD_REASONS -- #11783 could, because the pr-opener and board-post relays had put files under automation/**, outside TIER1_SURFACE. R14 refuses `unvouchable_paths` for a PR whose paths are all inside the allowlist, on the stated grounds that such a PR can simply self-land. This is that PR. Claiming a hold to dodge merge-slot contention would be lying in a declaration to win a merge convenience, which is the same trade #11783's lane correctly refused when it declined to delete its relay artifacts. So this branch arms and contends like everything else, and that limit is recorded on the backlog row rather than discovered again. WHAT THIS PR RECORDS: MI-266 -> landed_unproven (#11783 merged by hand as f14d2c704 on an R5 unvouchable_paths hold) with the row claiming ONLY that the propagation path landed -- the Data Explorer is still dark, and the three remaining facts are kept apart: the operator has not originated the two VALUES, the Tier-2 set-env has not been dispatched and merging did not authorise it, and the SPA cannot send a bearer at all. MI-246 -> landed_unproven (#11787) with its render in the CLAUDE.md brief recorded as clause (1) ONLY, clause (2) still unmet. The stale 2026-09-10 _merge_queue is refreshed onto the real head, and the owed due-list dispatch is marked DISCHARGED (#11794) together with the manager error that nearly dropped it -- a merge check run on a branch without first reading the PR's state/merged, which reported an already-merged PR as next to land. Both engineering lanes are idle/complete, so two of the operator's three slots are free; the next dispatch is NAMED and NOT taken, because the manager does not fill a free slot by picking the next interesting row. The merge-slot contention class was NOT filed as a new backlog row: the cheap check surfaced five open rows already naming it, so filing a sixth would have been the duplicate this repo's own append helper exists to refuse -- the measured escape was added to the existing row instead."
}
