{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U47, RESEARCH lane. Tier-1 by R5's surface test: one new instrument under scripts/research/, one new file under tests/, one new memo under docs/research/, and register/index edits under docs/. NO src/, NO config/, NO deploy/, NO unit file, NO workflow, no order path. src/units/accounts/risk.py, src/core/coordinator.py, src/web/api/routers/diag.py and config/accounts.yaml were READ and are quoted; none is modified. || WHAT IT DELIVERS: BL-20260913's criterion (a) turned from a hunt into a CLOSED enumeration. risk_usd = position_size x risk_per_unit = balance x risk_pct exactly on an unclamped row, and bybit_2 declares ONE risk_pct for all six legs, so the 3.09x dispersion is either a clamp or the balance -- and the clamps are a finite list inside position_size. Four candidates are UNREACHABLE on a config or code read, four are REFUTED with their statistics, one CONTRIBUTES on 4 of 33 rows, and the residue is the decision-time balance, which has no read surface. || NOTHING IS PROPOSED. Per-leg risk sizing is Tier-3; the row is kept OPEN and neither of its two exits is claimed. The instrument's verdict string refuses to let eight eliminations read as an attribution. || VALIDATED BEFORE PUSH on committed state: 40 self-test controls, 27 pytest controls, 6 planted defects all caught by a named control, ruff clean, check_research_index OK."
}
