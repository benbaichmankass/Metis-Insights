{
  "tier": 1,
  "landing": "self",
  "why": "Manager register writes only: a live supervision record on the two lanes spawned at 00:57Z, and a second instance appended to an OPEN backlog row rather than filed as a new one. No src/, tests/, scripts/, config/, deploy/ or workflow path. No order placed, modified or cancelled on any account."
}
