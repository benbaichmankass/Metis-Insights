{
  "tier": 1,
  "landing": "self",
  "why": "MEASUREMENT ONLY, and the diff is entirely on the Tier-1 surface: one new research doc under docs/research/ plus its committed derived counts under docs/research/data/, one row in docs/DOCUMENT-INDEX.md, the close-out of the object this unit was dispatched on (docs/claude/work/objects/WO-20260908-EXIT-MECHANICS-THE-TRAILING-AND-BANKING-HALF.yaml), and three append-only rows in docs/claude/health-review-backlog.json filed through scripts/ops/backlog_append.py::append_row so the file's exact serialisation round-trips. No src/, no config/, no deploy/, no scripts/, no workflow, no live-VM runtime file, no order path, no strategy params, no risk caps. Nothing was armed, flipped or re-graded: the session's hard constraints forbade re-grading any exit-matrix cell, changing TP_VENUE_CAP_PCT, arming ICT_SCALP_EXIT_HEAD_MODE, and arming be_floor_r at any value, and no parameter change is proposed anywhere -- the findings that suggest one are FILED, which is what the three backlog rows are. Every live read was read-only (GET /api/bot/db/table/trades, GET /api/bot/db/table/order_packages, GET /api/diag/version) and no live position was touched. R5 still grades the diff against the Tier-1 surface rather than taking this declaration on trust."
}
