{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 observability tooling and tests. Three workflow install steps, one stale comment corrected in place, one assertion message, one new test file, one backlog row. NOT IN LANDING_MACHINERY \u2014 verified by importing check_pr_landing.LANDING_MACHINERY and testing each touched path against it, not by eye: due-list.yml, error-feed-digest.yml, probes.yml, constraint-readout.yml, render_due_list.py and the new test are none of the twelve entries, so R12 does not apply. No src/, no config/, no deploy/. WHAT IT FIXES: due-list.yml's cron concluded `failure` on 4 of 4 scheduled runs (#104-#107, 2026-09-13 .. 09-16), each in seconds at its self-test gate, on 'scripts/ci/check_decision_answers.py: ModuleNotFoundError: No module named yaml' \u2014 read from run #107's job log, not inferred. Last success #98 on 2026-09-12, BEFORE the dependency became necessary. docs/claude/DUE.json sat four days stale at generated_at 2026-09-13T05:20:13Z. IT IS A RECURRENCE AND THE PREVIOUS FIX'S OWN COMMENT IS WHY IT SPREAD: constraint-readout.yml diagnosed this class on 2026-09-08, fixed ONE call site, and noted 'render_due_list.py and render_session_brief.py are stdlib-only, which is why due-list.yml needs no install step and succeeds'. True when written, false four days later. Corrected in place \u2014 field beats comment. SECOND HALF: spent_decision_edges has never once been read in production. Population = every DUE.json revision carrying it (n=4, all of them): 3 workflow-produced renders all could_not_read, the single `read` came from a session's local run. 0 of 3. THE GATE IS NOT LOOSENED \u2014 the assertions still require `read`; only what they PRINT changes, so the next failure names the ModuleNotFoundError instead of a bare 'could_not_read'. VERIFIED BOTH DIRECTIONS RATHER THAN ASSERTED: 13 tests green here; RED in a detached worktree at pre-fix origin/main (b3cf1f907) naming all three offenders by workflow::job; and the runner failure reproduced locally by blocking `import yaml`, byte-for-byte run #107. A vacuity control re-grades every job as if its install were absent and fails if nothing would flag, so a clean run cannot be a probe that cannot fail. THREE LIVE FALSE POSITIVES WERE MEASURED BEFORE THE RULES WERE ASSERTED, and one candidate turned out to be a TRUE positive I had first called false (run_guards.py really does exec its guards; guards.yml correctly installs) \u2014 recorded in the test as its own control, because the 'fix' for a false positive is to narrow the graph until a real reach disappears. WHAT IT DOES NOT CLAIM: the install fixes the FAILURE, not the LATENESS (these runs started 10:18-10:59Z against a declared `50 5 * * *`), and the test grades PyYAML only \u2014 a job importing another third-party package passes it and still dies. Register integrity: append_row byte-exact, 13 insertions, 0 lines reformatted. Full guard set on COMMITTED state: PASS 85 \u00b7 FAIL 1 \u00b7 COULD-NOT-RUN 0 \u00b7 SKIP 24; the one failure is pr-landing-guard, which this record is the second push for."
}
