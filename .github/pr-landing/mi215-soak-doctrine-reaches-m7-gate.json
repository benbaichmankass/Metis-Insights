{
  "tier": 1,
  "landing": "hold",
  "why": "Tier-1 by every path touched: a CI guard (scripts/check_soak_doctrine.py), its tests (tests/test_soak_doctrine_guard.py), a proposal doc under docs/design/, its one-line DOCUMENT-INDEX.md registration, a new ledger under docs/claude/, and three backlog rows filed/updated through scripts/ops/backlog_append.py's round-tripped serialiser. NOTHING under config/, src/, scripts/ml/, scripts/ops/ or .github/workflows/ is modified. In particular scripts/ml/strategy_review_packet.py -- the Tier-3 surface this work is ABOUT -- is byte-identical to main: the positive control planted a branch in it and restored it, verified by an empty git diff. The Tier-3 repair is PROPOSED in docs/design/strategy-leg-offline-edge-basis-PROPOSAL.md and deliberately not enacted, so nothing here changes what a KILL/DEMOTE/PROMOTE verdict rests on. The guard is armed as an exact-match RATCHET against a declared count of 7, so it passes on the current tree and fails on an 8th -- it cannot red any other PR on merge day, which is the property that makes landing it safe. MERGE SLOT: taken from a STALE claim, not from a live one. docs/claude/session-board.json named branch claude/mgr-record-review-packet-verdict-20260909 held by session_01HrmZ1RRNM4UnEUaFdrPEjj; that branch does not exist on origin and its work landed as #11527 = a77afb8e, which is this branch's own base and whose commit subject matches the claim's stated purpose verbatim. Recorded here rather than done silently so the manager can check the takeover.",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "APPROVAL SOUGHT: one merge click from the manager holding the lease (session_01HrmZ1RRNM4UnEUaFdrPEjj), who routed this unit as MI-215. WHY IT HOLDS, STATED PRECISELY RATHER THAN PAPERED OVER: this is Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own definition ('Docs, tests, CI, tooling'), and the tier field above says 1. The hold is not a tier judgement -- it is R5 declining to CERTIFY scripts/check_soak_doctrine.py for self-landing, because TIER1_SURFACE covers scripts/ci/** but not the scripts/ root where this guard (and its siblings check_provenance_consumers.py, check_diagnostic_provenance.py) have always lived. That refusal is the guard working as designed: an unrecognised path must fail closed rather than arm, and R5's own message says the remedy is to hold and let a human read it. \u26a0\ufe0f I did NOT move the guard into scripts/ci/ to clear this. Relocating a file so a landing check will certify it is changing the code to get green, and it would drag run_guards.py and any other reference along for a reason that is not about where the guard belongs. One line of review is the designed cost and it is the right one to pay. \u26a0\ufe0f THE hold_reason TOKEN IS THE NEAREST AVAILABLE AND IS NOT EXACT: HOLD_REASONS has no value for 'Tier-1 work on a path the guard cannot vouch for', so the typed reason overstates the tier while the free text carries the truth. Filed as BL-20260909-PR-LANDING-HOLD-REASONS-HAS-NO-VALUE-FOR-TIER-1-ON-AN-UNCERTIFIABLE-PATH rather than left as a quiet mismatch. MERGE SLOT: RELEASED. The arming file is removed, so R13 does not apply, and holding a slot this branch does not need is the documented N-lanes-all-hold-it collision. The board is restored to origin/main's value untouched."
}
