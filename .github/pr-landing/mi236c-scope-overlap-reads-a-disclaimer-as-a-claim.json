{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 by every path in the diff: scripts/ci/check_scope_overlap.py (one function classifies on prose instead of on path tokens, plus a comment block), tests/test_scope_overlap_attribution.py (5 tests appended), docs/claude/health-review-backlog.json (one row via backlog_append.py::append_row), and this branch's own landing/arming files. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps, no live-VM file. R12 CHECKED AS A LIST, not reasoned from filenames: LANDING_MACHINERY names claude-pr-automerge.yml, pr-opener.yml, board-post.yml, stale-automation-sweep.yml, .github/pr-landing/*.json, check_pr_landing.py, check_automerge_trigger.py, session_registry.py, commit-to-main/action.yml, claim_merge_slot.py and sweep_stale_automation_prs.py — check_scope_overlap.py is NOT on it (it is an ADVISORY audit that comments on PRs; it gates no landing route), and only this branch's OWN declaration is touched, which R12 excludes by construction. NO TEST OR CONTROL IS WEAKENED — and that is the load-bearing claim here, because an over-broad first version of this fix DID break the module's own #10729 control and was DISCARDED rather than shipped with the control loosened: the module's 73 planted controls all still fire unmodified, the existing suite goes 24 -> 33, and the fix is mutation-checked (reverting it fails 7 tests). CLASSIFICATION-ONLY, verified not asserted: path EXTRACTION still reads the original line, pinned by test_path_EXTRACTION_is_unaffected_so_nothing_stops_being_declared, so no path stops being declared and the change can only ever REMOVE a false declaration. The two larger faults measured in the same pass (missed infinitive declaration headers; a blank line closing a header+bullet-list section) are deliberately NOT fixed here and are filed as BL-20260911-THE-SCOPE-OVERLAP-PARSER-MISSES-FOUR-OF-FIVE-REAL-DECLARATION-SHAPES with the measurement and the reason the naive fix regresses."
}
