{
  "tier": 1,
  "landing": "hold",
  "why": "Docs + register correction only: one dated correction block and two corrected tables in docs/claude/work/BYBIT-HEDGE-BOOK-FLAT-READ-2026-09-08.md, and ONE existing OPEN-ITEMS row's `observation` value extended (1 insertion / 1 deletion). No src/, no config/, no deploy/, no workflow, no scripts/, no order path, no persistent table. It cannot change what the trader does. Tier-1 by content -- but it is declared `hold` rather than `self` DELIBERATELY, for a coordination reason and not a tier one: see hold_text.",
  "hold_reason": "depends_on_unmerged_pr",
  "hold_text": "HELD BEHIND #11384 (MI-196) FOR SEQUENCING, NOT FOR CONTENT -- nothing here depends on that PR's diff. #11384 has already re-resolved SIX merge-conflict rounds on docs/claude/OPEN-ITEMS.json and docs/claude/health-review-backlog.json, and my own merge of #11414 caused its SEVENTH (it is `mergeable_state: dirty` as I write this). This PR touches OPEN-ITEMS.json again. Whoever merges SECOND pays the conflict, and it should be me: my change is a one-line splice that rebases trivially, whereas #11384 carries 15 commits and has paid every round so far. So this branch deliberately arms NO auto-merge (no .github/pr-automerge-requests entry -- R10 would fail if it did) and claims NO merge_slot, which also means it adds ZERO conflict surface on docs/claude/session-board.json. I will rebase and merge it myself once #11384 is in. Offered to MI-196 on coordination board #11336 as text they could instead carry inside their round-7 resolution, which would save this PR entirely -- either outcome is fine and neither costs them a round."
}
