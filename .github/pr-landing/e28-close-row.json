{
  "tier": 1,
  "landing": "self",
  "why": "Register-only follow-up to #12743 (checklist row E28), which merged at 4b0b794a2. ONE file: docs/claude/work/MANAGER-CHECKLIST.json, moving row E28 from landed_unproven to done.\n\nWHY IT IS A SEPARATE PR AT ALL: close-out requires the closing observation to be run ON MAIN, never on a branch, so the row could not honestly read `done` inside the PR that produced it. The three predicates were then RUN against origin/main after the squash -- (1) regime_debt_matrix.classify() returns 'ict_scalp' for all eight ict_scalp_* legs and None for turtle_soup alone, over the 52 enabled legs; (2) all eight comms/strategy_evidence/ict_scalp_*.json carry coverage_state 'measured' with a cost_stack and a RULE-D1-STAGE0-NET-OF-FULL-COST verdict (seven) or 'harness_failed' with a stated error (ict_scalp_mgc_15m), 8/8; (3) fourteen per-trade/summary artifacts are committed under comms/strategy_evidence/runs/2026-09-22-e28/ (7 legs x __trades.jsonl + __bt.json; the candle CSVs are gitignored by design).\n\nIt is a git+CLI observation and the row says so rather than claiming a live one -- nothing in E28 deploys.\n\nIt was pushed as a PR rather than committed straight to main because branch protection declines a direct push, which is the correct behaviour and is recorded here so the next lane does not retry it.\n\nTier 1 by path: docs/** and .github/**. No code, no tests, no config, no runtime."
}
