{
  "tier": 2,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "Touches src/runtime/order_monitor.py (the live trader's monitor tick) and src/units/accounts/alpaca_client.py. Needs one operator OK in chat before merge. Two runtime effects to weigh, neither of which places, cancels or modifies an order: (1) ONE additional account-wide Alpaca positions() read per ALPACA ACCOUNT per sweep, memoized — deliberately not one per position per tick, because this sweep runs on every tick and an unbounded per-row broker call on the live tick is the shape of both June 2026 wedges; (2) a new CRITICAL page, alpaca_partial_stop_coverage, on the shared durable 6h/(account,symbol) cooldown with the uncovered quantity as its severity. It WILL fire on merge for the standing alpaca_portfolio/TLT condition — that is the finding, not a regression.",
  "why": "DETECTION ONLY — the quantity half of BL-20260816-COVERAGE-IS-ONE-SIDED for Alpaca, the venue that got the two-sided half in 2026-08 and never got the quantity half. The re-arm decision is UNCHANGED and still reads protection_state byte-identically: nothing is re-armed, topped up, cancelled or repaired off the new grade. Binding it to the repair path is a separate Tier-2 decision and is deliberately NOT in this PR — BL-20260820-OVERCOVER-REMEDIATION-CANCELLED-THE-JOURNAL-MATCHING-LEG is what automatic remediation of this class did last time."
}
