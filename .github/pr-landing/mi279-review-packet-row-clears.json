{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U5. Registers only: one appended `observation` plus `verified_at` on OI-20260901-REVIEW-PACKET-CANNOT-PROPOSE-AN-ACTION-AND-ITS-EVIDENCE-BLOCK-IS-UNEXERCISED, one new row in docs/claude/health-review-backlog.json via backlog_append, and the session-brief re-render a register change obliges. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY THE ROW IS RECORDED AS SETTLED RATHER THAN PRUNED: THREE other live OPEN-ITEMS rows name it in their `refs`, so pruning would leave three dangling references -- found because a brace-matched prune asserted the row was gone afterwards and the assertion FAILED, having matched a refs entry rather than the id field. The prune is left to whoever re-points those three, and the observation names them. WHY WO-20260901-PHASE-F IS NOT EDITED: its blocked_on edge names a decision answered three days earlier, which is a real defect, but an object's lifecycle and edges are its owner's or the manager's call and flipping `waiting` changes what the WIP ceiling and the constraint readout compute over. Filed instead, with the scope stated -- ONE object was graded, not the store, so the row asks for the sweep before a mechanism is designed. AND THE ONE CLAUSE I COULD NOT EXECUTE IS DECLARED AS DERIVED: fastapi is absent from this container, so clause (2) is established by reading _evidence_block against measured inputs rather than by running the route, and the observation says so instead of claiming a run."
}
