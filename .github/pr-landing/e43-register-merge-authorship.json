{
  "tier": 1,
  "landing": "self",
  "why": "TOOLING + TESTS ONLY. scripts/ops/merge_json_register.py (a client-side git merge driver — it runs on a session's `git merge`, never on GitHub's servers and never in the trading process), its pytest module, one checklist row and one pipeline row. No src/, no config/, no order path, no account mode, no workflow.\n\nTHE CHANGE: `updated_by` was in neither TIMESTAMP_KEYS nor COUPLED_WITH, and both sides of every register conflict always stamp it, so two lanes with provably disjoint row sets still hit Refuse('header scalars disagree and are not bookkeeping timestamps'). It is now resolved as its own kind (AUTHORSHIP_KEYS) by UNIONING both names rather than taking the later one — a union cannot lose a fact, where a pick-a-side rule discards one. The ` + ` separator is the form the E31 lane used resolving this same conflict by hand.\n\nMEASURED, population stated: the 20 adjacent register-touching commit pairs on origin/main dated 2026-09-22, each simulated as base=A^ / ours=A / theirs=B. Driver refusals 5 -> 4; header-only refusals 2 -> 0. Over the 33 pairs since the 2026-09-21 reset: 13 -> 12, header-only 2 -> 0.\n\nTHE REFUSAL DID NOT WEAKEN. Pair f50a25928 -> 4ca43ed4a on main disagrees on `updated_by` AND on row R7 and still refuses, on the row. Two planted mutations were run: the naive fix (later name wins) fails 6 of the new tests, and the forbidden 'ignore any scalar you do not recognise' escape fails the PRE-EXISTING test_REFUSE_non_timestamp_header_divergence. tests/ops/test_merge_json_register.py extended 21 -> 32 cases, all green, every pre-existing case unchanged.\n\nNOT IN THIS DIFF, deliberately, and filed with its numbers as PI-20260922-PHOUDXG4-0001: docs/claude/work/PIPELINE.jsonl is bound to no merge driver at all (plain git conflicts on 23 of 23 adjacent pairs; --union on 0 of 23). That is the larger half and it is a judgement call about the same-id fold, not a mechanical one."
}
