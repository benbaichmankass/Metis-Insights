{
  "tier": 1,
  "landing": "self",
  "why": "One backlog-row disposition proposing a derivable replacement for OPEN-ITEMS.json's `loud` flag, with the arithmetic. docs/claude/health-review-backlog.json only — no src/, no ml/, no config, no scripts, no workflows, no order path. NOTHING IS ENACTED: no row's `loud` flag is edited (the row's own criterion forbids exactly that), no renderer is changed, and the proposal names the existing owner rather than building a second one."
}
