{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U43, RESEARCH lane. ONE backlog row appended to docs/claude/health-review-backlog.json via backlog_append.py::append_row (18 lines added, 0 removed -- the exact-serialisation round-trip held). NO code, NO src/, NO config/, no order path, nothing runnable. Tier-1 by the canonical doc's own list: docs/** is inside TIER1_SURFACE and filing a backlog row is explicitly permitted with no cap. WHAT IT RECORDS, MEASURED rather than argued: clearing merge conflicts on nine of this session's own open PRs, scripts/ops/merge_json_register.py invoked by hand on performance-review-backlog.json resolved SIX cleanly (145 rows, 0 ids lost each) and REFUSED THREE with 'both sides EDITED id=<X> differently'. Inspecting those three field by field against the merge base, IN NONE OF THEM did a single FIELD change on both sides: two were disjoint-field edits (one side's change was to a field the other side left equal to base) and one was two APPENDS to `updates` from an EMPTY base -- two notes from U8 and one from U15. All three were resolved by hand without ranking anything, with an assertion that every id and every per-field edit from both sides survives, and U8's three annotations were read back by their `by` values to confirm none was dropped. WHY IT SELF-LANDS: it proposes nothing and changes nothing. It is a filing. The remedy it describes is deliberately NOT implemented here, because loosening a merge driver's refusal is a design change to a SAFETY mechanism and belongs in its own reviewed unit rather than riding a conflict-clearing session -- particularly this one, where the author had just spent an hour hand-resolving the very files that mechanism protects. THE ROW IS CAREFUL ABOUT THE DIRECTION A FIX MUST NOT GO, and that is most of its value: the broad refusal exists because a union-by-id resolver once reported 'no id lost, none resurrected' while silently dropping an edit -- BL-20260814-HAND-RESOLVED-BACKLOG-MERGE-SILENTLY-REVERTED-SIX-ITEMS-INCLUDING-A-RESOLUTION. So it asks for a NARROWING with the dangerous cases still refusing (same field on both sides; a list reordered on one side; an in-place edit of an existing list entry), requires PLANTED CONTROLS proving they still refuse, and states that a decision to LEAVE the refusal broad -- recorded with the reason -- closes the row equally legitimately. It also names the cost of inaction rather than assuming urgency: a refusal is indistinguishable to the author from a genuine ranking problem, so the likely response is a hand resolution, which is what caused BL-20260814. A SECOND MEASURED FACT IS RECORDED BESIDE IT AND EXPLICITLY NOT FILED AS A DEFECT: performance-review-backlog.json is not bound to the driver in .gitattributes (`git check-attr merge` reads `unspecified` for it against `jsonregister` for MANAGER-CHECKLIST.json), which is why git produced raw conflict markers and the driver had to be invoked by hand at all. PR #12148 binds the other three review backlogs and is open on landing: hold awaiting a human merge click; six of the nine conflicts in this session would have resolved automatically had it landed. That is evidence FOR #12148. VERIFICATION: full guard set run on COMMITTED state -- PASS 68 / FAIL 1, the one being pr-landing, which is the deliberate second push. check_backlog_refs caught two ids this session had shortened for readability inside the row's own prose; restored by grepping the real ones rather than re-typing them."
}
