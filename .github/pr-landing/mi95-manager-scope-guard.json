{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "operator_asked_to_hold",
  "hold_text": "⚠️ DISCLOSED PLAINLY, because the token is imperfect: the instruction is from the MANAGER SESSION that dispatched MI-95 (session_01Nopk1HcpvWBSEbZxEmALkd), not from the operator directly, and the closed hold_reason vocabulary has no token for 'a manager asked'. Quoted verbatim from the dispatch: 'Your PR body should state plainly which of the manager's 2026-09-03 actions your guard would have BLOCKED and which it would have PERMITTED -- that list is the honest test of whether the line is drawn in the right place, and the manager will read it as such.' That is an explicit statement that a reader will judge where the line falls before this becomes a gate, and a guard whose line placement has been submitted for review should not merge itself ahead of that review. The other half of the dispatch -- 'R12 applies to you: if you touch the landing machinery, declare landing: hold and do not self-land' -- was CHECKED rather than assumed and its condition is NOT met: run_guards.py and guard_selftests.py are not on check_pr_landing.py's LANDING_MACHINERY list (verified by running the guard, which refused an earlier changes_landing_machinery declaration on exactly that ground). That omission is itself filed as BL-20260903-RUN-GUARDS-PY-IS-LANDING-MACHINERY-AND-CHECK-PR-LANDING-DOES-NOT-KNOW-IT rather than fixed here.",
  "why": "Tier-1 by the canonical doc's own list (CI scripts, docs, *.md). The diff is one new guard (scripts/ci/check_manager_scope.py) with a 13-case planted-defect self-test carrying 6 positive controls, its registration in run_guards.py + guard_selftests.py, a pending-by-default exception template under docs/claude/work/, one backlog row, and one paragraph in CLAUDE.md pointing the existing rule at the new mechanism. No src/, no runtime tests, no order path, no strategy config, no risk caps, no deploy/, no live-VM file. Merging it adds one CI check and changes no runtime behaviour. NOT A WALL, measured before wiring: replayed over commits on origin/main since 2026-09-01, the 2026-09-02 manager grades 75 clean / 31 failing, the night manager 2 / 0, and the 2026-09-03 manager 5 / 0 on R2 with a single R6 fire on its lease claim."
}
