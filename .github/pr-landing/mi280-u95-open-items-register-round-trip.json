{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 register hygiene, no code. The diff appends ONE update to ONE existing open row in docs/claude/health-review-backlog.json and touches nothing else. No src/, no config/, no deploy/, no .github/workflows/, no scripts/ — so it is not in LANDING_MACHINERY and R12 does not apply. It records a MEASURED defect: backlog_append.detect_format() refuses docs/claude/OPEN-ITEMS.json while round-tripping all three review backlogs (positive control run, 3 pass / 1 fail), because the file carries two independent scars — 2 of 86 item braces spliced a space short, and a MIX of ensure_ascii settings (literal em-dashes beside \\u2014 escapes) that no json.dumps kwargs can reproduce; the best candidate matches 1,206 of 771,220 characters. It files NO new row: backlog_search returned the existing open row at overlap 1.00, and CLAUDE.md's rule is that a duplicate is dropped and the existing row updated. Written through backlog_append.update_row rather than by hand — 7 insertions / 1 deletion on a 9 MB register — and verified 1595 -> 1595 rows, 0 lost, 0 added, 0 duplicates, exactly 1 row content-changed. It deliberately does NOT perform the canonical reserialisation the updated row proposes as remedy (b): that is semantically identical but rewrites 1,671 lines and re-attributes rows, which is BL-20260820's measured harm, and it must be its own reviewed PR rather than a side effect of this one. The cost is now recorded as a number so that decision can be made on evidence. Pre-existing and verified as such so no false finding lands on another session: the refusal is identical at c79b1c29b, at its parent, and at 5a07c6510."
}
