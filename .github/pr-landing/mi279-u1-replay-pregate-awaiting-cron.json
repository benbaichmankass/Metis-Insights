{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, unit U1 (the open critical rows). Docs only: ONE appended `detail` on the open critical row BL-20260911-REPLAY-PREGATE-NIGHTLY-HAS-ZERO-SUCCESSFUL-RUNS-IN-ITS-ENTIRE-LIFE-AND-25-MONTHS-OF-ML-PREGATE-EVIDENCE-HAS-ACCRUED-TO-NOTHING, written through backlog_append.update_row. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY THE ROW STAYS OPEN THOUGH BOTH ROOT CAUSES ARE FIXED: its criterion wants a SUCCESSFUL run, and no scheduled run has occurred since the fixes landed -- today's only scheduled run started 08:26:46Z and both fixes merged at 13:24:54Z and 14:57:46Z, so it ran pre-fix code and is evidence about neither. The next cron slot is 2026-09-13T04:00Z. Closing it today would be asserting an observation nobody has made. WHY THE DISPOSITION IS WORTH LANDING RATHER THAN LEFT IMPLICIT: without it the next session re-diagnoses two already-fixed root causes, and it records two traps that would otherwise cost a false reading -- a workflow_dispatch run does NOT satisfy a criterion about a nightly cadence, and the workflow's total_count of 4434 is overwhelmingly event:issues/skipped, so an unfiltered read makes a dead nightly look like the busiest workflow in the repo while the row claims zero successes."
}
