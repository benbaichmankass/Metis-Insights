{
  "tier": 1,
  "landing": "hold",
  "why": "One new diagnostic document under docs/claude/diagnoses/ plus its DOCUMENT-INDEX registration and these two landing files. Measurement and analysis only: no src/, no config/, no order path, no strategy param, no risk cap, no live-VM file, no workflow. Tier-1 by the canonical doc's own examples (docs + observability + retrieving and analyzing state). landing=hold, NOT self, for two reasons: this session opens PRs as drafts and claude-pr-automerge by design refuses to un-draft a PR it did not open; and the document proposes two Tier-3 order-path changes (the Bybit market-order qty ceiling in precision.py/qty_legalize.py/execute.py, and the over-cover alert gating in order_monitor.py) which are PROPOSED IN PROSE ONLY and deliberately not written as code. A human should read the findings before this lands.",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "The diff itself is Tier-1 (one diagnostic doc + its DOCUMENT-INDEX row), but its operative content is two Tier-3 order-path proposals that only the operator can approve: the Bybit market-order quantity ceiling (precision.py reads lotSizeFilter.maxOrderQty and we send Market orders, which Bybit caps lower via maxMktOrderQty - a field absent from this entire repo), and gating the bybit_over_cover page on the graded verdict it already computes instead of on the side-blind trip. Both are PROPOSED IN PROSE ONLY and deliberately not written as code, per the dispatch's Tier-3 instruction to propose the exact change and stop. A human should read the findings - which include a live naked-short mechanism on ib_paper/MGC - before this lands. This session also opens PRs as drafts, and claude-pr-automerge by design refuses to un-draft a PR it did not itself open, so landing=self could not take effect here in any case."
}
