{
  "tier": 1,
  "landing": "self",
  "why": "U5 of MI-279, second batch: three DUE monitoring rows OBSERVED on live surfaces and their observations recorded. Two files, both Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs/claude/OPEN-ITEMS.json and the GENERATED SESSION-BRIEF block in CLAUDE.md (re-rendered with scripts/ops/render_session_brief.py --write, never hand-edited). No src/, no config/, no deploy/, no workflow, no scripts, no order path, and no VM mutation -- every read was a GET against /api/diag/* or /api/bot/notifications. R12 does not apply: neither file is in LANDING_MACHINERY. NO ROW CLEARS and none is claimed to; each records WHICH half of its criterion was met. The stray-OCA row's clause (b) is explicitly marked NOT GRADEABLE because /api/diag/ib_open_orders returned read_state='could_not_look' with orders=null -- we-did-not-look, recorded as neither a pass nor a failure. One row's own TITLE is now false (the sweep is armed, not unarmed) and that is recorded in its observation rather than by renaming the id, which the generated brief and other rows link by name. The coverage-soak observation deliberately reports its zero as an EMPTY POPULATION rather than a negative result, because other_book_qty was 0 on all 100 rows and the masking requires an other-book leg. OPEN-ITEMS.json was edited surgically (6 insertions / 6 deletions on 1819 lines) because a naive round-trip rewrites 100% of that file; manager_preflight --self-test PASS confirms the non-round-trip canary still holds."
}
