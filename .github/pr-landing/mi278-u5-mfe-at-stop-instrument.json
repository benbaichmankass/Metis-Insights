{
  "tier": 1,
  "landing": "self",
  "why": "One NEW scripts/research/ module plus docs; every changed path is inside TIER1_SURFACE (scripts/research/**, docs/**, .github/**). No src/, no config/, no .github/workflows/, no order path, no strategy parameter, no scheduled surface, nothing enacted on the fleet. MI-278 U5, object WO-20260912-M20-ACTIVE-TRADE-MANAGEMENT-HOLD-WINNERS-LONGER. WHAT IT DOES: OI-20260911's clears_when says in terms that \"'THE MARKET CHOPPED' IS NOT A VERDICT WITHOUT THE NON-e35 CONTROL\", and the excursion measurement that would settle it already existed with no control arm -- stop_width_counterfactual_2026_09_11.py::build_units filters group_of == 'e35' and then drops anything stop_change() does not recognise, so a control leg is excluded twice over by construction. excursion_control_arm.py IMPORTS excursion_regime and fixed_window_excursion UNMODIFIED and runs them on untouched_control. Importing rather than restating is the point: a second implementation of 'MAE in ATR units' would be free to disagree with the one whose numbers are already in the record. POPULATION: /api/diag/journal trades+order_packages limit=1000 pulled 2026-09-12T16:0xZ; unit is the ORDER PACKAGE, never the trade row; population rule and split instant (2026-08-30T08:53:19Z) imported from MI-271/MI-275; treated 18 pre / 17 post packages across 7 trend legs, control 60 / 66 across 7 ict_scalp_* legs, 7 ungraded no_candle_source (MGC is an IB future, genuinely outside the basis). RESULT: MAE in ATR units grew in BOTH arms (post/pre 1.09-1.45x, control slightly more), so a market-wide adverse effect is real; MFE collapsed only in the treated arm (fixed 48h window from entry: 0.195x on e35 against 0.945x on the control) and it survives symbol matching -- ETH 0.03x vs 0.96x, SOL 0.10x vs 0.88x, AVAX 0.33x vs 1.06x -- with BTCUSDT the honest exception at 0.36x in BOTH. A fixed window measured forward from entry ignores the exit, and e35 changed only the stop, so the winner-size collapse is NOT attributable to e35; it points at ENTRY quality on the trend family. e35 IS a real second effect on the STOP side: package-unit stop rate 11.1% -> 47.1% against the control's 50.0% -> 64.8%, DiD +21.2pp. IT DOES NOT CLEAR THE ROW and says so: per-leg is unreachable (the largest e35 leg-era cell is 5 packages), so only the group-level contrast is delivered. THREE INSTRUMENT FINDINGS, each caught by a control rather than by reading, each FILED rather than fixed in someone else's module (this diff is additive; editing a shared research dependency mid-unit would put an unreviewed change under MI-275's recorded verdict): (1) extreme()'s bare else returns max(high) for ANY unrecognised side string, which produced MAE exactly equal to MFE in all eight fixed-window cells at plausible magnitudes with nothing raised; (2) excursion_regime windows on rows[0]'s close and rows[0] is input order, while 5 of 35 packages have a sibling close spread over an hour and the widest is 30.9h -- reported as a FRAGILITY, explicitly NOT as an error in MI-275's published numbers, since the two builders agree exactly once rows are sorted; (3) MI-275's OKX basis, validated fit on trend legs (0 unfit of 31), grades UNFIT on the scalp control (5 of 126, max 120.5bp against a 25bp tolerance), which is the uncomfortable direction because the weaker basis is the arm introduced to test the other. THE BUILDER POSITIVE CONTROL IS THE ADMISSIBILITY GATE AND IT PASSES: --group e35 rebuilds the TREATED arm through this file's own builder and matches MI-275 package-by-package, 0 disagreements over all 31 shared packages, after failing three times -- once per finding above. A CORRECTION TO MY OWN WORKING, recorded rather than dropped: I first concluded the price basis was unreachable from this session because Python urllib got 403 from OKX. That was a probe artifact, not a venue block -- curl returns 200 from the same container at the same moment. api.bybit.com IS genuinely CloudFront country-blocked (explicit country message) and api.binance.com returns 451, so MI-275's choice of a proxy instrument stands and was re-verified. VERIFICATION on COMMITTED state: run_guards.py --event pull_request --base main; check_backlog_refs.py --base origin/main; check_claim_basis.py --base origin/main; check_document_index.py OK at 1064/1064; check_research_index.py 133 of 133 routed; ruff clean; --self-test 13 controls."
}
