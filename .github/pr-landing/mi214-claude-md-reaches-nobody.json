{
  "tier": 1,
  "landing": "self",
  "why": "SESSION-END doc-freshness duty from MI-214. CLAUDE.md's PROP_STATUS_REQUEST row asserted 'AND THAT REFUSAL REACHES NOBODY' while the backlog row it cites by name (BL-20260909-PROP-SIZING-REFUSAL-ON-A-STALE-BALANCE-IS-JOURNALED-BUT-NEVER-PINGED) records that exact claim as FALSE, corrected in place by its own author — me — earlier the same day. A canonical doc contradicting the row it points at, on a safety claim about a real-money-adjacent prop account, is the drift this file's own 'field beats comment' rule exists to kill. One sentence changed; 1 insertion / 1 deletion. || WHAT WAS WRONG: log_rejection_to_journal -> _log_trade_to_journal writes to `trades`, which is exactly what silent_refusal_alert._read_window reads, so the refusal DOES reach an operator-facing detector. I established that by RUNNING the real code (classify_cause on the genuine string from prop_balance.refusal_message returns a real bucket; assess() grades the account signalled_never_placed), which is also how I avoided building a mechanism that already existed. || WHAT IS PRESERVED, because the correction must not read as an all-clear: the dedicated PING path is genuinely never reached (that half of the sentence was right and is kept), and the detector that DOES see it needs verdict == signalled_never_placed AND SILENT_REFUSAL_MIN_ROWS (5 in 24h) with NO CAUSE_MIN_ROWS override for prop_balance_unreported — so one refusal grades below_min_rows and does not alert. The absent override is deliberate and is stated as such: the event has occurred ZERO times ever, so there is nothing to size a lowered floor from, whereas the balance_unreadable precedent was justified by 11 measured occurrences. Lowering a bar on precedent alone is what canon forbids, and whether to lower it is an OPEN OPERATOR DECISION already recorded in the backlog row with three resolution paths. || AND THE WHOLE THING IS UNOBSERVED, stated rather than implied: breakout_1 has ZERO rows in `trades` in the 1000-row window and ZERO rows anywhere contain 'prop_balance' — the prop stale-balance refusal has never fired at all, because tickets are suppressed at breakout_executor's reticket check, which runs BEFORE sizing. || NOT FILED AS A BACKLOG ROW: backlog_search returned the existing corrected row, and this is that row's doc half, not a new finding."
}