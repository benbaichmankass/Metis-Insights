{
  "tier": 1,
  "landing": "self",
  "why": "MI-280. One update to BL-20260911-THE-SHARED-COMMIT-TO-MAIN-LANDING-STEP-IS-THE-LARGEST-SINGLE-CAUSE-OF-DISCARDED-AUTOMATION-OUTPUT. Tier-1, ONE file (docs/claude/health-review-backlog.json), insertion-only. THE ROW STAYS OPEN. WHY IT EXISTS: #11930 gives criterion (a) a mechanism, and the obvious next step -- sweep `refresh-command` across the 15 scheduled commit-to-main callers -- would DESTROY EVIDENCE on at least 3 of them. The row warns that porting blind 'trades one loss for another'; measured, it is worse than that: on a CAPTURE producer a re-derive SUBSTITUTES A DIFFERENT OBSERVATION for the one that was captured. POPULATION: all 142 workflow files, 0 unreadable; 26 call commit-to-main; 15 declare a schedule. DERIVED (safe): due-list.yml reads the repo's registers. CAPTURE (unsafe): macro-valuation-snapshot reads external market data at a point in time; session-reaper reads LIVE session state. WORSE: work-decision-commit CONSUMES operator answers fetched earlier in the run, so re-deriving there loses a decision -- the anti-enforcement failure the decision-roundtrip row names. IT CLASSIFIES 4 OF 15 AND SAYS SO IN TERMS. The other 11 are not graded and must not be read as safe by omission; I read four generation commands and stopped, because an unchecked classification would be the blind port this row warns about, one level up. Criterion (b) is untouched. AGAINST MYSELF: the note was first written with an UNQUOTED heredoc, which executed the backticked script names and silently emptied them -- the same backtick class that ate a word from an arming file earlier today. Redone with a quoted heredoc and the five names ASSERTED PRESENT afterwards. THE EDIT IS VERIFIED INSERTION-ONLY: the file's serialisation was reproduced byte-identically before writing, so the other 1495 rows cannot reformat, and the item count is 1496 before and after."
}
