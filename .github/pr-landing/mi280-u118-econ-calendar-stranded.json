{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, REGISTER-ONLY: one `detail` appended to the open row BL-20260830-A-TRANSIENT-RED-BASE-PERMANENTLY-STRANDS-AN-AUTOMERGE-BRANCH via update_row, 1 insertion / 1 deletion. No src/, no config/, no deploy/, no workflows, no scripts, no tests. NOT in LANDING_MACHINERY. WHAT IT RECORDS: econ-calendar-produce run #1953 (event=schedule, 2026-09-17T00:49:58Z) failed at commit-to-main's 30-minute wait on PR #12356 with stale-branch refresh reading not_needed_up_to_date, after its four prior SCHEDULED runs (#1949-#1952) all succeeded -- a FIRST failure for this producer, not a standing one. CAUSE ESTABLISHED AS (b), NOT ASSUMED. commit-to-main's own text says the two causes look identical from where it stands and to check the PR's check runs first, so that was done (audit, repo-inventory, pytest-collect, pytest-run SUCCESS; guards FAILURE at 00:54:31Z) and then the base was GRADED rather than inferred -- a worktree at 205633831, the exact commit that run's merge ref was built from, on which check_open_items.py exits 1 naming OI-20260826-JOURNAL-TRUST-COVERS-ONE-ACCOUNT at 22 days against a limit of 21. That row was repaired by #12364 at 06:47Z, is nobody's diff, crossed on the CALENDAR, and #12356's diff touches OPEN-ITEMS.json not at all. [!] WHY IT IS NOT INTERCHANGEABLE WITH THE error-feed-digest INSTANCE THE ROW ALREADY CARRIES: that producer re-renders CURRENT state, so 0 of 22 cost cycles and nothing else. This diff is a 62,369-line TIMESTAMPED point-in-time capture (comms/macro/US-20260917T005034Z.fxstreet.json) plus 443 appended snapshot rows, from a DAILY producer -- so tomorrow's run captures a DIFFERENT instant and what the calendar said at 00:50:34Z is not re-derivable. The row's own cost model is wrong for it, which is the reason this is appended rather than left as one more count. [!] IT DID PAGE and must not be filed as a monitoring gap: econ-calendar-produce is in claude-run-failure-alert.yml's watch list. It is UN-ACTIONED, not invisible. [!] AND IT IS REFRESHABLE, established narrowly: base now green, and nothing supersedes #12356 (no newer econ-calendar artifact has landed; the previous are #11047's), so the sweeper's refuse-if-superseded safety does not bind. THAT DISCRIMINATION IS FOR THIS PR ONLY -- the other ~40 open automation/* PRs were NOT graded and the row's standing warning against indiscriminate refreshing is restated unchanged in the appended text. [!] THE LANE DID NOT REFRESH IT: operating the merge queue is a manager base duty and #12376 declined the same action this morning for stated reasons. The diagnosis is on the coordination board (#11336) with the discrimination done so the action is one step. VERIFIED ON COMMITTED STATE: backlog round-trip byte-for-byte on all four registers, artifact-validity EXIT 0, register-id-guard OK, claim-basis 0 findings, stated-population OK."
}
