{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, docs-only: ONE key appended to ONE work object under docs/claude/work/objects/. The stale key it corrects is preserved verbatim rather than edited, so the record of what was true is not destroyed by the correction. No src/, no config/, no deploy/, no workflows, no scripts, no register \u2014 not in LANDING_MACHINERY, so R12 does not apply. WHAT IT CORRECTS: resumed_2026_09_17 ends by naming six PRs as OPEN AND ARMED and all six are settled \u2014 five merged, one (#12365) CLOSED as superseded. A successor would chase PRs needing no chasing and might re-do landed work, the exact failure that key was written one turn earlier to prevent. EACH STATE WAS VERIFIED AGAINST main's OWN COMMIT LOG with a SUBJECT-ANCHORED match, and the method is recorded beside the result because both obvious probes are wrong here: merge-base --is-ancestor reports NOT MERGED for every squash-merged PR in this repo, and a list_pull_requests page drops older numbers past its 30-item window. An UNANCHORED --grep was also measured wrong in the act \u2014 it matched #12366 to #12367's commit, whose body cites it. Both wrong probes cost this lane a public correction earlier today. THE IN-FLIGHT LIST IS DECLARED A DATED SNAPSHOT, with an instruction to re-derive it from main's log rather than quote it \u2014 which is how the sentence being corrected went stale. The file parses as YAML after the append (21 keys, lifecycle: in_flight unchanged) and BOTH keys are present, checked rather than assumed."
}
