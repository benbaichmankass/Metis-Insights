{
  "tier": 1,
  "landing": "self",
  "why": "Register-only observation. Two files: docs/claude/OPEN-ITEMS.json (ONE row -- OI-20260909-POSITION-READ-STATE-SOAK-DECIDES-WHETHER-THE-COLLAPSED-READ-EVER-FIRES -- gains an `observation` and `verified_at: 2026-09-10`; asserted, not assumed: the edit was a single-JSON-string-literal splice, and a field-by-field diff of all 76 parsed rows before/after reports exactly two changed fields, both on that id, for a 2-line numstat) and CLAUDE.md's GENERATED SESSION-BRIEF block, re-rendered by scripts/ops/render_session_brief.py --write because session-brief-guard correctly fails a register change that leaves the brief stale. No src/, tests/, scripts/, workflows, config/, deploy/, or canonical prose outside the generated block. NO ORDER was placed, modified or cancelled on any account, and no journal row was written. WHAT IT RECORDS: the FIRST INTERPRETABLE READ of position_read_state_soak, which the row's own clears_when names as THE DENOMINATOR AND NOT THE FINDING -- it clears NEITHER clause and is written so it cannot be mistaken for clearing (b). The deploy-defect branch IS refuted (the writer is writing: file present, size_bytes 5272380). Population stated because the endpoint caps at 1000 lines however large a `lines=` you pass (measured -- lines=4000 returned 1000): a 1000-row TAIL, 2026-09-10T09:42:17Z..13:36:23Z, ONE UTC day. dropped_symbol_dedupe_count >= 1 on 0 of 1000, against a genuinely non-zero denominator (rows_seen 8-9 on every row, 0 rows at rows_seen==0, could_not_look_count 0, per-query census 3550 rows_returned / 18 no_rows) -- which is the only thing that makes that zero readable rather than a quiet negative. Clause (b) needs >= 3 distinct UTC days and this is one, so the row stays OPEN. Deployment established by ANCESTRY rather than by a merge event: the writer landed in 46e1efb1f (#11435) and /api/diag/version at 13:36:50Z reads git_sha 7fd285536, which contains it.",
  "merge_slot_note": "R13's slot was held by session_01HrmZ1RRNM4UnEUaFdrPEjj for branch claude/mgr-hb-20260910T1326, whose own purpose text says `Released on merge`. That PR (#11698) MERGED at 2026-09-10T13:34:14Z -- verified through pull_request_read (merged: true, merged_at 13:34:14Z), not inferred from main's tip -- and the release write never landed, so the claim in the tree is STALE BY ITS OWN TERMS. Taken on that basis and recorded here rather than silently. It was NOT taken from live work.",
  "ci_arming_note": "THIS PR WAS BORN WITH NO REAL CI AND THAT IS RECORDED HERE RATHER THAN FIXED SILENTLY. The intended order (CLAUDE.md line ~1516, the `claude-pr-automerge` trap) is: open the PR yourself FIRST so it carries a `pull_request` event, THEN push the arming file. Here the arming file rode the SAME push as the content, so the workflow won the race -- `create_pull_request` came back `A pull request already exists`, and #11699 was opened by github-actions[bot] under GITHUB_TOKEN. MEASURED, not assumed: `get_check_runs` on #11699 returned total_count 1 -- the workflow's own `open-and-automerge`, conclusion SUCCESS -- with auto-merge already armed, so that single green check was the entire gate and a PR with ZERO real CI could have squashed on it. The commit carrying this note is the ordinary push that arms the `pull_request` event; `mergeable_state` then read `blocked` (conflict-free, checks pending), which is the healthy state and NOT the `dirty` that also renders as total_count 0. !! CORRECTION, SAME SESSION: an earlier draft of this note claimed this was `a THIRD cause of a low check count that CLAUDE.md does not yet list beside `dirty` and `blocked``. THAT IS FALSE and the commit message that shipped it repeats it -- CLAUDE.md documents this exact mode at line 1522 (`the PR is *born* with no `pull_request` event and carries exactly ONE check run -- its own `open-and-automerge``), naming six occurrences on 2026-09-08 alone. I asserted a documentation gap without grepping for it, which is the failure RULE ONE exists to stop, committed inside a note about being careful. THE ACCURATE FINDING IS WORSE AND MORE USEFUL: this is a RECURRENCE of a trap that is already written down, in the file every session reads, WITH a stated remedy -- and the remedy did not hold because it is an ORDERING rule about two pushes, and the natural move is to commit the content and the arming file together. A rule whose violation is the ergonomic default is a rule that will keep being violated; that is a mechanism gap, not a reading gap, and it is the seventh occurrence."
}
