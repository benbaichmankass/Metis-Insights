{
  "tier": 1,
  "landing": "self",
  "why": "scripts/ci/ only (run_guards.py, check_guard_glob_coverage.py, check_guard_selftest_coverage.py, guard_selftest_exemptions.json) plus one backlog row. All inside TIER1_SURFACE; none in check_pr_landing.py::LANDING_MACHINERY, verified by importing the list rather than reading prose. No src/, no config/, no deploy/, no workflow file, no runtime, no order path. ⚠️ DIRECTION OF EACH CHANGE, STATED SEPARATELY BECAUSE THEY DIFFER: (1) the new run_guards entry ADDS a step that runs an existing --self-test which passes on main today (verified by running it: PASS in 0.4s, stdlib, no network, two controls) -- it can only add a failure, and only if that detector genuinely stops detecting; (2) it runs ONLY --self-test and never the REPORT, so the report's un-triaged LEADS still gate nothing and cannot red a PR -- that separation is asserted by a control that goes red if the bare report is ever added as a second step; (3) deleting the exemption removes a pre-approval the guard itself flagged as stale, so it can only tighten; (4) COVERAGE_FLOOR 58 -> 67 is a RATCHET RAISE and is the one change that can red a future PR -- deliberately, since that is what a ratchet is for, and the consequence is written at the constant: a PR dropping coverage below 67, including one reverting this change, now fails."
}
