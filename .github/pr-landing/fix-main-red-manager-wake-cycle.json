{
  "tier": 1,
  "landing": "self",
  "why": "One test file. tests/test_manager_wake.py stops pinning a superseded cycle id as a string literal and reads the one docs/claude/work/MANAGER-CHECKLIST.json declares. No src/, no config/, no workflows, no deploy, no order path, no register mutation beyond this branch's own landing + merge-slot files. `tests/` is Tier-1 by the canonical tier table (docs, tests, CI, tooling). Self-landing is EXPLICITLY authorised by the manager (session_01HrmZ1RRNM4UnEUaFdrPEjj) in the MI-248 dispatch: 'tests/ is Tier-1 per the tier table, so this can landing: self — arm the route and claim the merge slot.' It unblocks main, which is red on a clean checkout for every open PR in the repo."
}
