{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 developer tooling: scripts/ci/check_open_items.py (a diff-scoped observation-preservation rule), one registry entry's argv in scripts/ci/run_guards.py, one new test file, and two register edits in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps, no workflows. R12 does not apply -- nothing in LANDING_MACHINERY is touched: run_guards.py is named in that list's own exclusions, and check_open_items.py has never been in it. It closes BL-20260911-AN-OPEN-ITEMS-SPLICE-CAN-REPLACE-THE-OBSERVATION-IT-MEANT-TO-EXTEND-AND-EVERY-PROOF-THE-REPO-HAS-PASSES-OVER-IT by its criterion (a), and that row makes the planted control non-optional: run the check against 38ebcb85c and require it to FAIL. It does -- 1 observation-loss finding, 3377 -> 2628 chars, declaration none -- and the same at 37b700631, the merged form of that PR. The paired negative control is 5e081be4f, the REPAIR of that same row, where the count is 0; a test establishes the repair really carries the destroyed text, so the control is not vacuous. Both quiet commits still exit 1 on the SEPARATE duplicate-key rule, a real historical finding of a different rule, so the discriminator is the finding count and not the exit code. Five planted source defects were run and four reddened immediately; the fifth was ABSORBED -- planting the bad-ref branch away still reached the unreadable state by crashing on empty text, and the test only noticed once it asserted the note NAMES the ref. Population for the claim that this is not a one-off: all 233 commits touching the register on main, 232 pairs gradeable, base observation text lost in 38 of 232 (16.4%) across 82 rows, 73 of which kept under 200 characters of the base and only 7 of which said anything about superseding; largest drop 15,720 characters. That 16.4% is also the ongoing cost and is stated in the PR rather than buried. Checked before building that this is NOT register-field-loss-guard: that guard grades DISAPPEARANCE and its own docstring excludes a changed value. Register integrity across the merge with origin/main: 1587 -> 1589 rows, 0 lost from either side, 0 duplicates, exactly 1 pre-existing row content-changed (the one deliberately closed), and backlog round-trip OK on all 4 live backlogs. Both register edits went through backlog_append.py's append_row/update_row after a first hand splice was correctly refused by artifact-validity-guard. Local full guard set on committed state: PASS 107 / FAIL 1, the one failure being pr-landing-guard for this very record."
}
