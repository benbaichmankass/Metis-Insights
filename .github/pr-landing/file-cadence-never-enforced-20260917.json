{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 backlog filing — filing is explicitly the act of NOT taking the item and is permitted to the manager with no cap. ONE file, docs/claude/health-review-backlog.json, appended through the mandated scripts/ops/backlog_append.py::append_row (which refused the first attempt for a missing resolution_criteria, so the writer's own guard did its job). The row records a MEASUREMENT over all 87 rows of OPEN-ITEMS.json at origin/main: 73 carry loud:true and 66 of those are past their OWN declared check_every_days while scripts/ci/check_open_items.py prints OK, because that guard reads check_every_days only to validate it is a positive int and then judges staleness against a flat _STALE_DAYS = 21. Nine of the 66 are ungradeable entirely (no cadence, or a null verified_at), which is the collapsed-state class inside the register that tracks it. This matters because CLAUDE.md justifies the register being uncapped on the claim that the per-row cadence is what the guard enforces — field beats comment. NOTHING IS FIXED HERE: no guard, no code, no workflow is touched, and the row's own proposed_fix warns that arming the per-row gate blind would fail 66 of 73 rows and red every PR on day one, and its resolution_criteria state that widening the cadences to make the count fall does NOT clear it and is the harm."
}
