{
  "tier": 1,
  "landing": "self",
  "why": "Register-only (docs/claude/health-review-backlog.json). No code, no src/, no config, no order path.\n\nADVANCES BL-20260830-BUILDER-EXCEPTION-LATCH-HAS-NEVER-BEEN-WRITTEN by doing the thing its own criterion (c) asks for — reading the writer path — which REFUTES (c)'s presumption. (c) reads: 'if the next burst again leaves no state file, this is re-classified from still_unexercised to a real defect and the writer path is read.' The writer is not broken. src/runtime/pipeline.py::_report_builder_exception tests _is_transient_market_data_error FIRST, reports WARN and **returns**; `cooldown_admits` — the writer, by its own docstring ('Returns True AND commits the new timestamp') — is on the line after that early return. A transient exception therefore cannot write the latch by construction, and the function's docstring says so in terms: 'THE TRANSIENT BRANCH IS THE PRIMARY FIX AND THE COOLDOWN IS THE BACKSTOP.'\n\nAND ON THIS FLEET EVERY BUILDER EXCEPTION TAKES THAT BRANCH. Population: the newest 1000 rows of /api/bot/logs?level=warning, AT THE CAP (so the newest 1000, not a day), span 2026-09-12T08:51:13Z -> 19:53:18Z, ~11h. 780 of 1000 (78.0%) are `strategy_builder exception` and 780 of 780 (100%) carry `transient_market_data_unavailable` — zero cooldown downgrades, zero non-transient first pages. Fourth consecutive absent observation of the latch file (08-30, 09-01, 09-09, 09-12) with the row's own positive control intact: target_naked_alert_state (578 bytes) and stop_over_cover_alert_state (41 bytes) come back populated on the SAME diag call.\n\nCRITERION SHARPENED SO THE ROW CAN END. As written, (a) required an event nobody can cause — a repeating NON-transient builder exception — so the row could accrue forever while being fully understood, which is what `docs/CLAUDE-RULES-CANONICAL.md` § Backlog governance forbids. It now has a second, reachable exit that records the same knowledge, and closing on it needs ONE more independent measurement on a LATER day so a single quiet window cannot close it.\n\n⚠️ TWO OF MY OWN PROBES WERE WRONG AND BOTH ARE RECORDED ON THE ROW RATHER THAN QUIETLY FIXED, because they are that row's own subject matter. (1) I classified branches off a `reason` field; the wire row carries only {id, timestamp, level, message, source}, so `reason` was None on all 780 and my histogram confidently read '780 other/non-transient' — a wrong answer from a field the route does not serve. (2) I read /api/bot/logs?level=error as `[]` and nearly reported 'no ERROR rows'; the POSITIVE CONTROL is what makes it safe to state — limit=5 returns 5 and level=warning returns 5, while level=error and level=critical both return 0.\n\nSECOND EDIT: an independent re-measurement appended to BL-20260911-78-PERCENT-OF-THE-WARN-FEED-IS-ONE-MARKET-DATA-CLASS-AND-IT-TRUNCATES-THE-REVIEWABLE-WINDOW-TO-FIVE-DAYS. It measured 781 of 1000 (78.1%) on 2026-09-11; I measure 780 of 1000 (78.0%) a day later. Deliberately NOT a new row — a third row on the same condition would be the noise that row is about, and backlog_append's duplicate probe surfaced it. It adds the companion fact that the ERROR feed is EMPTY over the same read, so an 11-hour 5-leg 780-event outage escalates to nobody. The remedy there is in src/runtime/ and is Tier-2 by path, so it is flagged for the manager and NOT attempted here.\n\n⚠️ NOTHING IS CLOSED BY THIS PR, and that is deliberate: the latch genuinely has not been exercised, and saying otherwise would be the 'closure with no observation behind it' this lane's instructions call worse than an open row."
}
