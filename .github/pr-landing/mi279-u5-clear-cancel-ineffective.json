{
  "tier": 1,
  "landing": "self",
  "why": "U5 of MI-279. Two files, both Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs/claude/OPEN-ITEMS.json and the GENERATED SESSION-BRIEF block in CLAUDE.md, re-rendered with render_session_brief.py --write. No src/, no config/, no deploy/, no workflow, no scripts, no order path, no VM mutation -- every read a GET against /api/diag/*. R12 does not apply; neither file is in LANDING_MACHINERY. THIS PR REMOVES A ROW, which is the one act in this register that destroys information, so the justification is spelled out: OI-20260908-CANCEL-ACCEPTED-INEFFECTIVE-MERGED-AND-NEVER-EMITTED-ON-THE-FLEET declares three clauses and all three are OBSERVED live -- deployed by ancestry (2547a6a8 an ancestor of the running fb46b5531); emitted, as 12 operator_alerts rows grading share_hold=cancel_accepted_ineffective on alpaca_paper/GLD between 2026-09-11T12:05:13Z and 23:15:47Z, with a POSITIVE CONTROL that orders_still_resting and broker_cancel_wedged appear zero times in the same tail; and suppressed, as those 12 alerts sit 60.5-61.6 minutes apart with page(s)-suppressed climbing 3->14 and the Consecutive-close-failures counter oscillating 3..6 and RESETTING rather than climbing per tick. The register's own contract is that clears_when names the observable condition that REMOVES the row, so removal is the sanctioned act rather than an annotation. The removal is asserted rather than eyeballed: items 85->84, the id absent, and all 84 remaining rows byte-identical. The evidence itself is preserved in this PR body and the commit message so nothing is lost by the deletion. The second row, OI-20260910-IB-PER-PASS-BREAKER-..., is NOT cleared -- it is graded 2 of 3 with clause (2) explicitly recorded as satisfied by an EQUIVALENT route (the running process's own ib_breaker.state='armed' on 37 of 37 soak rows) rather than the get-env read its clause names, which this session could not dispatch."
}
