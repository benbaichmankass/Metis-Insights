{
  "tier": 1,
  "landing": "self",
  "why": "E58 (checklist row E58, ceiling $50): inventory every research/evidence workflow run whose result never reached main, recover what still exists, re-run what was lost. After this branch's own fix to research-exit-head-build.yml, E57 (#12857) merged and independently fixed the SAME root cause with a simpler condition (`needs.build.result != 'skipped'` vs. this branch's fuller trigger mirror) -- rebased onto main and took main's version, dropping this branch's own copy, per the manager's instruction. What remains in this PR's diff against main is now only: (1) docs/research/e58-research-loss-inventory-2026-09-24.json -- the read-only inventory (docs/**), recording that all 13 open `research result: unattributed` PRs research-exit-head-build.yml had produced traced to that one collapsed-states bug (verified per-run via list_workflow_jobs: `build: skipped` on every one, and each run's own triggering issue was unrelated to research -- a system-action, a trainer-diag-request, a manager dispatch, never a research-exit-head-request) and were closed, not merged, as garbage rather than evidence; and that 16 genuinely R5-blocked PRs (13 replay-pregate-nightly + 3 research-queue-dispatch) are safe, sitting on open branches with auto-merge already armed, self-landing now that E57 is on main. (2) one docs/claude/work/PIPELINE.jsonl row (PI-20260924-NKNG2BGS-0001) verifying those 16 actually merge rather than assuming it. No src/, no config/, no order path, no live surface, no workflow change (E57 already carries it)."
}
