{
  "tier": 1,
  "landing": "self",
  "why": "Manager dispatch record for lane 2. Registers under docs/claude/work/** only: the work object moved dormant->in_flight with its owner, the SESSIONS.json registry row, the tool-stamped lease heartbeat, and the R13 merge_slot claim. No src/, tests/, scripts/, .github/workflows/, config/ or deploy/ path is touched — the measurement itself is what the dispatched session will author."
}
