{
  "tier": 1,
  "landing": "self",
  "why": "CI-guard correctness only: scripts/ci/check_manager_scope.py and its --self-test, nothing else in the diff. Tier-1 by the canonical doc's own list (docs/CLAUDE-RULES-CANONICAL.md § Permission Tiers: 'Docs, tests, CI, tooling') — no src/, no tests/, no config/, no deploy/, no .github/workflows/, no strategy params, no risk caps, no order path, and no live-VM runtime file. It changes only WHICH previous heartbeat R7 compares against: the one belonging to the same lease RUN (holder + claimed_at), reached along whichever parent actually carries it rather than sha~1. It does NOT weaken R7 — measured over the 46 reachable commits touching MANAGER-LEASE.json, R7 still fails cb9f0bbb at 131 minutes, and the two commits whose verdict changes are both same-holder re-claims over an EXPIRED lease whose dead interval (100 and 214 minutes) is still reported in full. It self-lands because Tier-1 needs no human OK to merge and routing it through one is the exact convention .github/pr-landing/README.md exists to end; GitHub still merges only on green, and pr-landing-guard still grades the diff against the Tier-1 surface rather than taking this declaration on trust."
}
