{
  "tier": 1,
  "landing": "self",
  "why": "MI-161, the document library the operator asked for on 2026-09-07. Tier-1 by the canonical doc's own list -- 'Docs, tests, CI, tooling, observability / read paths'. The diff is exactly three things: (1) a new generated register docs/DOCUMENT-INDEX.md, (2) a one-line status stamp inserted into the header of 966 markdown documents, and (3) new tooling -- scripts/ops/document_index.py, scripts/ci/check_document_index.py, and its registration in scripts/ci/run_guards.py. NO config/**, NO src/**, NO order path, NO strategy parameter, NO risk cap, NO deploy/ or unit file, NO account-mode or execution gate. Nothing in it can place, modify, or refuse an order. The one claim worth stating rather than assuming: the stamp is INSERTED, never a content rewrite -- no document's substance is edited, and the insertion point is computed to land after YAML frontmatter (32 SKILL.md files, whose frontmatter the harness parses) or after the H1, so no skill or parser is disturbed. Verified: canonical-doc-coherence, skills-index and guard-glob-coverage all still pass on the stamped tree. Two generated documents (docs/claude/READOUT.md, docs/claude/DUE.md) are registered but NOT stamped, because their generators rewrite them wholesale; that waiver is verified at build time rather than presence-only. Statuses this change could not ESTABLISH are written `unknown` (563 of 968) rather than guessed, and the ROADMAP*/WORKPLAN-* rows are deliberately deferred to the concurrent MI-159 (PR #11241) rather than decided here."
}
