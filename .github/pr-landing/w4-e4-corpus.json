{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 tooling/data change (docs/claude/work/MANAGER-CHECKLIST.json row E4). No live order path, no account/strategy config, no service/timer file touched. Wires 16 of the 17 previously-unwired backtest/research harnesses through the existing scripts/ops/backtest_data_source.py::resolve_or_refuse policy (same pattern already shipped and tested on backtest_trend.py/account_compat_matrix.py); adds the E4-mandated behavior that a bare invocation with no --symbol and no --data now REFUSES rather than reaching the 5,001-row/3.5-day BTC fixture by default (allow_implicit_default=False, an opt-in escape hatch for the module's own --self-test). The fixture is unchanged and untouched as a file. The 17th site, scripts/ml/build_calibration_corpus.py, is deliberately DEFERRED to a follow-up: it sits outside check_pr_landing.py's TIER1_SURFACE (scripts/ml/** is not on it) and widening that allowlist is itself LANDING_MACHINERY (R12), which would force this PR to hold for a human -- so symbol_data_binding_census.py is left un-gated in run_guards.py in this PR (it reports 1, not 0) rather than redding CI or widening guard machinery inside a self-land PR. Adds docs/reference/corpus-manifest.json (within TIER1_SURFACE's docs/** -- moved there from data/ for exactly this reason; 46/47 rostered symbol,timeframe pairs, one honest disclosed gap) and scripts/ops/fetch_backtest_corpus.py, an orchestrator over the existing scripts/ops/fetch_backtest_candles.py fetcher -- no new fetch mechanism. Adds scripts/ci/check_corpus_row_floor.py (self-tested, positive+negative controls). Verified: 300+ existing tests across the touched harnesses re-run green (one test, tests/test_risk_grid_sweep.py, updated to keep exercising its own target refusal now that data-source resolution runs first); every new/changed harness's bare-invocation refusal and explicit-data success path smoke-tested directly; no scheduled workflow invocation needed changing (all already pass --symbol/--data explicitly); ruff clean; canonical-config-loaders clean (fetch_backtest_corpus.py now reads accounts.yaml/strategies.yaml through the canonical loaders, not a hand-rolled yaml.safe_load)."
}
