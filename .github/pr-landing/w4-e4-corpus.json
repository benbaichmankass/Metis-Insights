{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 tooling/data change (docs/claude/work/MANAGER-CHECKLIST.json row E4). No live order path, no account/strategy config, no service/timer file touched. Wires all 17 previously-unwired backtest/research harnesses through the existing scripts/ops/backtest_data_source.py::resolve_or_refuse policy (same pattern already shipped and tested on backtest_trend.py/account_compat_matrix.py); adds the E4-mandated behavior that a bare invocation with no --symbol and no --data now REFUSES rather than reaching the 5,001-row/3.5-day BTC fixture by default (allow_implicit_default=False, an opt-in escape hatch for the module's own --self-test). The fixture is unchanged and untouched as a file. Adds data/CORPUS-MANIFEST.json (46/47 rostered symbol,timeframe pairs, one honest disclosed gap) and scripts/ops/fetch_backtest_corpus.py, an orchestrator over the existing scripts/ops/fetch_backtest_candles.py fetcher -- no new fetch mechanism. Adds scripts/ci/check_corpus_row_floor.py (self-tested, positive+negative controls) and gates the pre-existing scripts/ops/symbol_data_binding_census.py in run_guards.py now that it reports 0 (closing BL-20260912-FIFTEEN-MORE-HARNESSES-LET-SYMBOL-BE-A-LABEL-OVER-A-DEFAULTED-DATA-FILE-AND-FOURTEEN-SHARE-ONE-FIXTURE in the pipeline). Verified: 500+ existing tests across the touched harnesses re-run green (one test, tests/test_risk_grid_sweep.py, updated to keep exercising its own target refusal now that data-source resolution runs first); every new/changed harness's bare-invocation refusal and explicit-data success path smoke-tested directly; no scheduled workflow invocation needed changing (all already pass --symbol/--data explicitly)."
}
