{
  "tier": 1,
  "why": "Tier-1 research only, every path inside TIER1_SURFACE: one NEW scripts/research/ module, one new docs/research/ memo, one row each in two docs/ registers, and two docs/claude/ backlog edits. No src/, no config/, no scripts/ outside scripts/research/, no .github/workflows/, no unit file, no order path, nothing enacted on the fleet. The module is read-only over a journal JSON file passed on the command line — it opens no socket and reaches no VM. It DECLARES a measurement rule (what a fan-out order package's exit is) and re-states one previously published research statistic against it; it changes no number any operator-facing surface serves, and deliberately does NOT touch the three live dashboard routes MI-278 U12 named, which are src/web/ and Tier-2. It advances but does NOT close the backlog row it serves, and both the memo and the row update say so, because the row's criteria require the analyses to share the declaration and the two callers stay unwired while PR #12092 has one of them open. Verified on committed state: 54 self-test controls pass, and the harness was shown able to FAIL against five separately planted defects rather than trusted because it was green — two of which exposed real gaps in the harness itself and were fixed before shipping.",
  "landing": "self"
}
