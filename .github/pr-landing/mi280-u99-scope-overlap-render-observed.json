{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, register-only and NO CODE: one update appended to one existing open backlog row, written through backlog_append.update_row (6 insertions, 0 deletions). No src/, no config/, no deploy/, no .github/workflows/, no scripts/ — not in LANDING_MACHINERY, so R12 does not apply. WHAT IT RECORDS: a LIVE observation of U96 (#12369, merged 59da43b9a at 07:46:35Z), with a BEFORE/AFTER pair rather than a single sighting — the scope-overlap audit comment on PR #12369 (07:25Z, before) ends '0 matched your OWN declaration and were not reported', and the one on PR #12372 (07:52Z, after) ends '...and 0 were your own single-writer per-branch file(s) that no other branch can write'. Two runs of the same workflow 27 minutes apart straddling the merge, so the live audit runs main's post-merge render() and the new field reaches the comment a human reads. Verified against the primary source (git show origin/main: — footer at line 893, the field in every assess() return path) rather than against the relayed notification text. THE SUPPRESSED COUNT IS ZERO AND THAT IS CORRECT: the declaring session named no per-branch DIRECTORY, so the suppression branch was never reached. Zero means the condition did not arise, not that the mechanism is inert — and the row's update says so explicitly so a later reader cannot mistake it for a clean pass. THE ROW IS DELIBERATELY LEFT OPEN: its criterion needs a live session declaring one of those directories, with the planted positive control still reporting another branch's slug file. What is newly established is only that the field RENDERS. A FALSE NEGATIVE IS RECORDED RATHER THAN DROPPED: the first probe grepped inside a worktree that had not been created, returned nothing, and read as 'the change is not on main'; the direct blob read returned five matches. A probe returning nothing is not proof of absence."
}
