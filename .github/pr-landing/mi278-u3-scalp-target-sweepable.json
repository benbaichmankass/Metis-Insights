{
  "tier": 1,
  "landing": "hold",
  "hold_reason": "unvouchable_paths",
  "hold_text": "TWO holds, both real. (1) unvouchable_paths (the declared reason, verified against the diff): Tier-1 research tooling touching scripts/backtest_ict_scalp.py, OUTSIDE TIER1_SURFACE, which covers scripts/{ci,ops,research,reports}/** and not top-level scripts/. That polarity is deliberate -- 'a path not matched here cannot self-land' -- and is not routed around; this PR asks for a merge click. (2) It also DEPENDS ON #11886 (MI-278 U5): the memo names BL-20260912-THE-SCALP-FAMILY-S-TARGET-HAS-NO-SWEEP-E35-EXCLUDES-IT-BY-DESIGN-AND-THE-FLEET-SWEEP-DOES-NOT-SWEEP-A-TARGET, which is filed on that branch and is not yet on main, so artifact-validity-guard correctly reads it as a reference resolving to NOTHING. This branch is therefore NOT pushed until #11886 lands and main is merged in -- a dangling tracking ref reads as tracked while being tracked by nobody, and pushing a knowingly-red branch to be fixed later is how that gets normalised. hold_reason carries only the diff-verifiable one because the vocabulary is single-valued; the dependency is recorded here rather than dropped.",
  "why": "Adds --tp-at-r to the ict_scalp BACKTEST HARNESS plus its test and a memo. NO unit file, NO config/, NO src/, NO deploy/, NO workflow, NO order path. The live tp_at_r: 1.5 is UNCHANGED on every leg -- the flag defaults to None, which is byte-for-byte the previous behaviour, and no sweep has been run and no verdict is claimed. It builds the runner that makes a Tier-3 target proposal POSSIBLE; the proposal itself is Tier-3 and is deliberately not made here."
}
