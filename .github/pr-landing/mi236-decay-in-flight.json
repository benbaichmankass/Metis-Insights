{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 by every path in the diff, checked against check_pr_landing.py's own TIER1_SURFACE allowlist rather than inferred: scripts/ops/** (owner_liveness.py, new), scripts/ci/** (check_stale_in_flight.py new; run_guards.py and check_collapsed_states.py each gaining one registration), tests/** (new), and docs/** (one evidence doc plus one DOCUMENT-INDEX row). No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps, no live-VM file. No test or guard is weakened, skipped or quarantined; two guards gain coverage. THE LANDING-MACHINERY LIST WAS CHECKED AS A LIST, not reasoned from filenames -- that is the mistake MI-235's lane made when it declared `self` and was refused by R12. None of the four touched scripts is in LANDING_MACHINERY: it names check_pr_landing.py, check_automerge_trigger.py, scripts/ops/session_registry.py, scripts/ops/claim_merge_slot.py, scripts/ops/sweep_stale_automation_prs.py, three workflows, the commit-to-main action, and .github/pr-landing/*.json -- of which only this branch's OWN declaration is touched, and R12 excludes that file by construction (`p != decl_rel`). scripts/ci/run_guards.py is NOT on the list; it is the registry the guards run FROM, not a landing route. READ-ONLY BY CONSTRUCTION: the new code reads docs/claude/work/MANAGER-CHECKLIST.json, SESSIONS.json and objects/*.yaml and writes to none of them -- MANAGER-CHECKLIST.json and SESSIONS.json are deliberately untouched because the manager has open PRs against both. The new guard's REFUSAL is ratcheted to what a diff itself adds, measured against origin/main, so it cannot red pre-existing debt (17 of 35 rows are already unsupported) and cannot fail a PR that merely records a session going idle -- both verified by mutation, exit 1 and exit 0 respectively, not asserted. No work object changes lifecycle, so no WIP slot is consumed and no wip-ceiling-exception is filed. Evidence: docs/claude/work/IN-FLIGHT-OWNER-LIVENESS-2026-09-11.md."
}
