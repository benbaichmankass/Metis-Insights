{
  "tier": 1,
  "landing": "self",
  "why": "REWRITTEN after the duplicate was found and deleted. scripts/ci/check_decision_answers.py (extract spent_edge_rows + load_objects as the ONE producer, add `lifecycle` and `answer_is_a_choice`, self-test 12 -> 18 cases), scripts/ops/render_due_list.py (a source that IMPORTS that producer), and two rows in docs/claude/health-review-backlog.json. The second grader this branch originally added at scripts/ops/spent_decision_edges.py is DELETED, and its run_guards registration with it. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps, no live-VM file. Every touched path is inside TIER1_SURFACE (scripts/ci/**, scripts/ops/**, docs/**) and none is in check_pr_landing.LANDING_MACHINERY -- verified against that list rather than assumed, so R12 does not apply. It EDITS NO WORK OBJECT and proposes no lifecycle change: re-grading a `waiting` object is the owner's or the manager's call because it moves what the WIP ceiling and the constraint readout compute over."
}
