{
  "tier": 1,
  "landing": "self",
  "why": "A new reporting tool at scripts/ops/spent_decision_edges.py, a due-list source in scripts/ops/render_due_list.py that imports it, its registration in scripts/ci/run_guards.py, and one row's `updates` in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy config, no risk caps, no live-VM file. Every touched path is inside TIER1_SURFACE (scripts/ops/**, scripts/ci/**, docs/**), and none is in check_pr_landing.LANDING_MACHINERY -- verified against that list rather than assumed, so R12 does not apply. It EDITS NO WORK OBJECT and proposes no lifecycle change: re-grading a `waiting` object is the owner's or the manager's call because it moves what the WIP ceiling and the constraint readout compute over, and this change only reports the disagreement."
}
