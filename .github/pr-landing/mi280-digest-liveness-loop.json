{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 by the canonical doc's own list (CI scripts, GitHub Actions workflow fixes): scripts/ci/check_digest_liveness.py, a new scripts/ci/digest_carrier.py, one contract entry in scripts/ci/check_collapsed_states.py, two lines in .github/workflows/guards.yml, and one resolved row in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps, no live-VM runtime file. Not landing machinery -- guards.yml is not in LANDING_MACHINERY, verified by reading the list rather than from memory -- so R12 does not apply.\n\nIT CHANGES A REQUIRED GUARD'S FAILURE BEHAVIOUR, WHICH IS THE ONLY REASON TO HESITATE, AND THE SAFETY PROPERTY IS ASSERTED RATHER THAN CLAIMED: the change can only ever make digest-liveness LESS likely to fail, in exactly one evidenced case -- a receipt newer than main's, READ off an open PR head. Every other path, including every failure of the lookup itself, keeps today's verdict, and a self-test control pins that a stale receipt with no lookup still grades stale and still fails.\n\nThe lookup runs SECOND and only on the path that was already going to fail, so a healthy PR opens no socket; it is bounded to one open-PR list plus at most 8 content reads at 10s each.\n\nThe guards.yml change is the minimum that stops the fix being inert: the job had contents: read and no token, so the lookup could never list PRs and would have graded could_not_look forever -- shipped and unreachable. Added pull-requests: read and the job's own github.token, both read-only.\n\nEvidence: 6 of 6 planted defects caught with a quiet positive control, each named by the control that caught it; collapsed-state-guard clean over 46 contracts with the new digest_liveness.carrier_state reporting all states read; ruff clean; run_guards PASS 99 FAIL 1 SKIP 0, the one failure being pr-landing-guard before this arming push.\n\nNOT OBSERVED ON THE FLEET, and the resolved row says so: no real run has graded stale_unmerged, because that needs main's digest past 6h while a work-digest PR sits unmerged."
}
