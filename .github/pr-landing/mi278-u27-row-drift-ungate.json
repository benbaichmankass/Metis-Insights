{
  "tier": 1,
  "landing": "self",
  "why": "MI-278 U27, RESEARCH lane. Tier-1 CI/tooling: scripts/ci/check_document_index.py, scripts/ci/run_guards.py, one memo, one hand-inserted generated stub row in docs/DOCUMENT-INDEX.md. NOTHING under src/, config/, deploy/ or any unit file; no order path, no strategy or risk parameter, and no change to .github/pr-landing or the auto-merge route, so this is not landing machinery. WHAT IT CHANGES, AND WHY IT IS SAFE TO SELF-LAND: R6 gains an UNGATED step beside its existing diff-scoped one, which is the diagnostic-provenance-guard / api-tier-policy-guard pattern CLAUDE.md names, added only because the residue is MEASURED at zero (46 drifted rows at b209768c2, 0 at origin/main, re-checked as 0 after three further PRs merged; the probe is shown finding 46 positives, so the zero is a reading and not a broken query). R6's own comment gave the 47-row residue as its reason for being diff-scoped, and that reason is gone. R1's remedy line now quotes the generator's own stub for the one unregistered path instead of a command that rewrote 65 files, while still naming --write for the several-documents case. THE COST IS STATED RATHER THAN HIDDEN: a future change to status_for or ACTIVE_DOCS will red every PR until someone runs --write. That is the accepted trade in both cited precedents, and the rollback is deleting one step in run_guards.py -- the diff-scoped rule and the census are untouched. Verified before pushing: --self-test asserts 30 planted rule cases with 0 failures, and five defects were planted against the new code, all five caught; the plant that widens --all into the scoped rule fires SEVEN controls, which is what shows the scoped behaviour is pinned independently. IT REFUSES THE OBVIOUS MOVE AND SAYS SO: the 35 erased hand-written bases are NOT restored into STATUS_EXPLICIT, because that table's own docstring forbids exactly a bulk add, and I can vouch for only 16 of them. It also corrects two of my own published claims -- that the #12122 churn was avoidable (it was the residue drain) and that 19 rows lost a basis (it is 35)."
}
