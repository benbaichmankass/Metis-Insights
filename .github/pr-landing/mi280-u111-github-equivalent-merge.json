{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 TOOLING: one new scripts/ops/ script (github_equivalent_merge.py), one new test file, and one AMENDED backlog row. No src/, no config/, no deploy/, no workflows, no CI wiring. NOT in LANDING_MACHINERY, so R12 does not apply -- it predicts a merge, it does not perform or gate one, and nothing in the repo calls it yet. WHAT IT BUILDS: resolution (a) of BL-20260910-AN-ARMED-CLONES-LOCAL-TEST-MERGE-IS-A-FALSE-NEGATIVE-ON-GITHUB-MERGEABILITY -- a local test merge that predicts GITHUB rather than this clone. AND IT CORRECTS THAT ROW'S OWN PRESCRIBED REMEDY, which is wrong in BOTH stated spellings. MEASURED over three real PRs (#12379 #12384 #12386) against one origin/main (b76997ba9), one FRESH detached checkout per cell with the tree asserted clean before each: core.attributesFile=/dev/null agreed with GitHub 1 of 3 (it names the USER-LEVEL attributes file and leaves the in-repo .gitattributes binding, so an armed clone still runs the driver -- the exact false negative the row exists to prevent, and I published a CLEAN verdict off it on four PRs before catching it); merge.jsonregister.driver= (empty) agreed 2 of 3 and OVER-reports, because an empty driver command is RUN, fails, and a failed driver is booked as a conflict -- measured in a controlled fixture it leaves the path UNMERGED with ZERO conflict markers, against 2 for a genuine textual conflict, so a caller reading only the unmerged-path list cannot tell them apart; an UN-ARMED CLONE agreed 3 of 3, and GitHub independently graded #12384 and #12386 dirty. MECHANISM: git clone --shared gives a fresh LOCAL config with no merge.* at all, which is the state GitHub's server is in, and the merge also neutralises GIT_CONFIG_GLOBAL and GIT_CONFIG_SYSTEM so a driver in ~/.gitconfig cannot reach the throwaway clone. A planted test covers that and CAUGHT IT HAPPENING in the first draft: the armed-check read the ambient environment while the merge read the neutralised one. THREE STATES, NEVER COLLAPSED: clean / would_conflict / could_not_look, exits 0/1/2, so a caller testing only rc == 0 cannot read a failed probe as a green one. VALIDATED AGAINST AN INDEPENDENT IMPLEMENTATION rather than its own say-so: helper vs a hand-built un-armed clone, 4 of 4 agreement at the same main; then dogfooded on the three PRs after their base merges, 3 of 3 CLEAN, and CI agreed. CONTROLS: 14 tests, SEVEN defects planted, six caught immediately. [!] THE SEVENTH WAS ABSORBED AND IS RECORDED RATHER THAN QUIETLY PATCHED -- deleting the guard that refuses to call a conflict git cannot name changed nothing, because no test constructed a merge that fails BEFORE it merges; unrelated histories are that input, the test now builds it, and the re-plant reddens. [!] TWO PLANTS ALSO FAILED TO APPLY on the first attempt (shell quoting) and printed '13 passed', which is indistinguishable from an absorbed plant; the runner now asserts the file actually changed before trusting a result. [!] IT DOES NOT CLEAR BL-20260910, and must not be read as doing so: that row's criteria ask for a check the merge-queue duty ACTUALLY CALLS, and nothing calls this yet -- a tool a session must choose to type is the same shape as the prose that failed to reach the moment of use, which is the row's own finding. The row is amended with the measurement and stays kept_open. Resolution (b) is likewise already built (scripts/ops/pr_mergeability.py) and likewise routed to by nothing. VERIFIED ON COMMITTED STATE, NO PIPES (a guard piped into head can report EXIT=0 while printing ::error:: -- SIGPIPE): 14 tests green, ruff clean, register-id-guard OK, check_backlog_refs EXIT 0, claim-basis EXIT 0, artifact-validity EXIT 0, backlog round-trip byte-for-byte on all four registers. The row was amended through update_row, 1 insertion / 1 deletion."
}
