{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 developer tooling: scripts/ci/run_guards.py, tests/test_guards_uncommitted_work.py, and one register row closed in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no order path, no strategy config, no risk caps. R12 does not apply -- checked against LANDING_MACHINERY rather than assumed: run_guards.py is not in that list, which names the automerge/pr-opener/board-post workflows, check_pr_landing.py, check_automerge_trigger.py, session_registry.py, claim_merge_slot.py and commit-to-main/action.yml. (There is an OPEN row arguing run_guards.py SHOULD be in it -- BL-20260903-RUN-GUARDS-PY-IS-LANDING-MACHINERY-AND-CHECK-PR-LANDING-DOES-NOT-KNOW-IT -- and it is flagged here rather than quietly relied on. As the list stands today R12 does not fire.) WHAT IT DOES: closes BL-20260913-A-GUARD-RUN-BEFORE-COMMITTING-GRADES-THE-WRONG-TREE-AND-ITS-VACUOUS-PASS-IS-INDISTINGUISHABLE-FROM-A-REAL-ONE. #12177 added a NOT GRADED qualifier counting GUARDS relevance dropped; two holes remained, both MEASURED on main @950244f87. (a) A FAILING run with a dirty tree printed ZERO occurrences of 'uncommitted' anywhere, because the notice lived in the all-passed branch that main() reaches only after returning early on failures and on could_not_run. (b) A PASSING run whose dirty file was ALSO in the graded diff had no qualifier, because relevance is a union and the guard had already been selected by a committed change. Ships a pure dirty_tree_lines() printed in the COMMON summary before every early return, a SECOND separate counts-line qualifier N PATH(S) UNCOMMITTED never folded into the first, and a start-of-run line. IT FIXES NOTHING FOR YOU per the row in terms -- no stashing, no committing, no grading the worktree -- and THE EXIT CODE IS UNCHANGED (rc=1 on the failing probe, rc=0 on the passing one). CONTROLS: a clean tree produces nothing new on either surface, confirmed on a real 108-guard run where the new string appears 0 times; five planted defects each reddened their own test, including a STRUCTURAL one asserting the call site precedes the failure early return -- the invariant that actually regressed, which a renderer-only test would have passed against. One co-failure per plant (test_only_a_guard_that_actually_runs_stays_quiet) is pre-existing dirty-tree sensitivity, verified with a positive control. Row integrity: ids 1575 -> 1575, 0 lost, 0 added, exactly 1 row content-changed, 0 duplicate ids."
}
