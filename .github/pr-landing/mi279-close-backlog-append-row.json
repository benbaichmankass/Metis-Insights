{
  "tier": 1,
  "landing": "self",
  "why": "MI-279 ops lane, /backlog-drain. Docs only: BL-20260820-BACKLOG-APPEND-REFORMATS-AND-REATTRIBUTES moves open -> resolved with its evidence appended (through backlog_append.update_row), plus the committed ratchet baseline docs/claude/impossibility-claim-baseline.json regenerated by the guard's own --all --update-baseline. No code, no src/, no config/, no deploy/, no workflow, no order path, no risk caps. R5 grades the diff against the Tier-1 surface rather than taking this on trust. WHY IT IS A CLOSE RATHER THAN AN ANNOTATION: both criteria were VERIFIED BY RUNNING THEM -- tests/test_backlog_append.py (17 passed) plants the exact ensure_ascii=True write criterion (a) demands, and check_impossibility_claims.py --all --ratchet is registered ungated in run_guards.py with a committed per-file baseline, which is criterion (b)'s standing sweep with its count assertion. The four rows criterion (b) named are gone: the sweep reports health-review-backlog.json 8 -> 0. WHY THE BASELINE IS REGENERATED IN THE SAME CHANGE RATHER THAN LEFT: the ratchet grades against the baseline, so leaving the stale one would let the health backlog regrow 0 -> 8 with NO regression reported -- i.e. it would close the row while leaving the exact defect (b) exists to prevent. The delta was asserted by diffing the before and after baselines (exactly two files changed, 36 -> 27 claims), not by reading the tool's summary line. WHAT IT DELIBERATELY DOES NOT CLAIM is stated in the row: the helper refuses only when USED, so a hand-written splice still bypasses it -- a different and still-open class with its own row."
}
