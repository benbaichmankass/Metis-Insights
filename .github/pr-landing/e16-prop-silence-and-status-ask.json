{
  "tier": 2,
  "landing": "hold",
  "hold_reason": "operator_asked_to_hold",
  "hold_text": "This build lane (E16) was dispatched with an explicit instruction, quoted verbatim: \"Open each PR as a draft with .github/pr-landing/<branch-slug>.json declaring its tier honestly (say so if you read it as Tier-2), landing: 'hold', hold_reason: 'operator_asked_to_hold' quoting this instruction - the manager merges; do not arm auto-merge and do not merge yourself.\" The manager session (session_01XQhayAHWdrSYv8jN54P2HH) merges. Nothing here should auto-merge.",
  "why": "E16 - two operator-facing signal changes on a FUNDED prop account (breakout_1). (1) src/prop/prop_status_request.py narrows the status-ask trigger so a ticket that can no longer take exposure stops asking - removes a 12h latch the operator reported. (2) src/prop/prop_balance.py + src/runtime/execution_diagnostics.py route the prop sizing refusal to the operator's pending-pings channel (BL-20260909-PROP-SIZING-REFUSAL-ON-A-STALE-BALANCE-IS-JOURNALED-BUT-NEVER-PINGED). I read this as TIER 2, not Tier 1: src/core/coordinator.py:multi_account_execute gains one additive, exception-isolated note_refusal() call inside _default_balance_fetcher. It changes NO sizing arithmetic, NO routing decision and NO gate - the branch still refuses and still raises the same message - but it is a line on the order path shipped to the live ict-trader-live.service, so it gets the Tier-2 gate by path. The diagnosis half of E16 (root-causing the silence) made NO code change and is reported in the PR body: the fix for the silence is in the intent layer and is Tier-3, deliberately NOT made here."
}
