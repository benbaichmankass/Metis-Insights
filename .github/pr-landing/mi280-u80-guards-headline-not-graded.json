{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI tooling: scripts/ci/run_guards.py plus its test module, and one backlog row closed. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no live-VM runtime file. R12 does not apply: run_guards.py is not in LANDING_MACHINERY -- verified against that list rather than assumed. It puts the NOT-GRADED count on the counts line, closing BL-20260903-RUN-GUARDS-PRINTS-FAIL-0-ON-A-RUN-IT-KNOWS-WAS-INCOMPLETE. RE-CONFIRMED on origin/main @15bb44021 before editing: a tree with one committed change and one uncommitted file printed 'PASS 67 . FAIL 1 . COULD-NOT-RUN 0 . SKIP 40' with the 'NOT SELECTED because the work is UNCOMMITTED (1)' notice forty lines below it. The row's SECOND alternative (a distinct non-pass footer) was deliberately NOT taken as already-met: that footer landed 2026-08-13 in #8948, three weeks BEFORE the row was filed, so closing on it would close the row on the state that prompted it. The line renders through counts_line(), a pure function, so what the headline claims is arguable in a test; it is a QUALIFIER rather than a fifth bucket (those guards are already inside `skipped`) and it renders only when non-zero, so a clean run's line is byte-identical to before and cannot become an always-on decoration. THE EXIT CODE IS UNCHANGED, which the row requires in terms. Proven both ways on the same tree: OLD 'SKIP 1' vs NEW 'SKIP 1 . 1 NOT GRADED (uncommitted)', 15 tests pass, and four planted defects each red their own control including the over-rendering direction and the keep-the-function-but-print-your-own-string shape that changes no output. CAVEAT STATED: HARNESS_PATHS makes any change to run_guards.py set force_all, so this PR's own CI run cannot exercise the end-to-end path -- which is why the row asked for a self-test."
}
