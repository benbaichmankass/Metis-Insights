{
  "tier": 1,
  "landing": "self",
  "why": "U5 of MI-279: two DUE monitoring rows OBSERVED on live surfaces and their observations recorded. Touches three files, all Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs/claude/OPEN-ITEMS.json, docs/claude/health-review-backlog.json, and the GENERATED SESSION-BRIEF block in CLAUDE.md (re-rendered with scripts/ops/render_session_brief.py --write, never hand-edited, which is what doc-freshness requires). No src/, no config/, no deploy/, no workflow, no order path, and no VM mutation -- every read was a GET against /api/diag/*. R12 does not apply: nothing here is in LANDING_MACHINERY. NEITHER ROW CLEARS and neither is claimed to; both stay open with the measurement their own clears_when asks for, and one of them (the ict_scalp_5m demote) is recorded with a POSITIVE CONTROL OF ONE rather than the three I first counted -- two of the three rows in that denominator are adopted_orphan re-adoptions of the third's package, not the leg signalling. The backlog edit is an annotation to an already-open row (the symbol-dedupe false-close flap, now observed as a TRIPLE with its third row still open), filed via the file's own detect_format serialisation; no row changed status, so the open count is unmoved."
}
