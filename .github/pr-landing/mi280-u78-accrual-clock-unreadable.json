{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 CI/ops tooling, one file: scripts/ops/accrual_clock.py. No order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no live-VM runtime file, and no register is written. R12 does not apply: the file is not in LANDING_MACHINERY, verified against that list rather than assumed. It stops a JSON parse failure contributing a confident ZERO to a census whose purpose is keeping a stock visible. MEASURED on origin/main @7f832e8c0 with a conflict marker in docs/claude/health-review-backlog.json: --all printed '0 (row, leg) pair(s), 0 on a clock that cannot tick' against a true reading of 4 and 4, with the TOTAL 4 short (19/10 -> 15/6) and exit 0 either way -- unprovenanced diagnostic output sub-class C, an unasserted denominator. The right instinct was already in this file and applied to the wrong case: it ANNOUNCES an absent file because a quiet skip under-reports the stock, and was silent on a corrupt one, which is the opposite fact and the more urgent. Three states, never collapsed; the unreadable file gets its own wording, the TOTAL says it is incomplete, and 'no accrual-shaped carried rows found' is never printed when the reason nothing was counted is that nothing could be read. census() and _load_rows() keep their signatures. Non-zero on an unreadable file though this census is allow_fail in CI and does not gate -- the blocking guards for a corrupt register are register-id-guard and backlog-criteria. Proven both ways: OLD rc=0 under-reporting vs NEW rc=2 naming the file, clean tree identical, and three planted defects each red their own control including the over-reporting direction and the grade-then-discard shape where a fix looks right and changes no output."
}
