{
  "tier": 1,
  "landing": "hold",
  "why": "Tier-1 BY CONTENT and HELD BY PATH, and those are different facts — recorded rather than argued around. The change is CI/observability only: a fifth `near_miss` state on exit_loop_health's requirement grade, a new pure module (src/runtime/exit_restart_gap.py) grading the restart gap, a collapsed-state-guard registry entry, one branch inside an existing system_invariants function, docstrings, CLAUDE.md's stale four-state enumeration, and tests. It touches NO order path, NO strategy config, NO risk caps, NO account mode, NO env knob, NO deploy/ or .github/workflows/ file, and mutates nothing on a VM. Everything added is observe-only: it reads a log the exit loop has already written and, at most, sends an operator alert.  MI-230's dispatch brief said to self-land. THAT INSTRUCTION IS UNSATISFIABLE FOR THIS DIFF and is not being worked around: R5 grades the DECLARED tier against the PATHS, and src/runtime/** plus src/web/api/routers/** sit outside TIER1_SURFACE, so the guard cannot certify them however Tier-1 the content is. A path outside that surface is not thereby dangerous — it is one the guard declines to vouch for, which is the correct default. So this waits on a human merge click. It is NOT waiting on an operator approval: the decision it implements (WO-20260909-DECISION-M20-EXIT-EVAL-MARGIN-COLLAPSED, chosen: near_miss_and_restart_gap, 2026-09-09) is already recorded in the work object and MUST NOT BE RE-SOUGHT.  Narrowing the PR to earn self-landing was considered and rejected: the only way to do it would be to drop the producer modules, which are the change.",
  "hold_reason": "unvouchable_paths",
  "hold_text": "R5 declines to certify src/runtime/exit_loop_health.py, src/runtime/exit_interval_soak.py, src/runtime/exit_restart_gap.py and src/web/api/routers/exit_interval.py -- all outside TIER1_SURFACE. The content is Tier-1 CI/observability (a state added to a grade, a new PURE module, a guard registry entry, docstrings and tests; no order path, no config, no deploy file, no env knob, no VM mutation), but Tier-1 BY CONTENT and vouchable BY PATH are different facts and the guard is right to grade the second. So a human merges this. !! IT IS NOT WAITING ON AN OPERATOR APPROVAL AND ONE MUST NOT BE RE-SOUGHT: the decision it implements is WO-20260909-DECISION-M20-EXIT-EVAL-MARGIN-COLLAPSED, answered 2026-09-09, chosen: near_miss_and_restart_gap, recorded in the work object. That is why the typed reason here is `unvouchable_paths` and deliberately NOT `tier_2_3_needs_approval` -- reading it as the latter would send a session back to the operator for an OK that has already been given. !! Narrowing the PR to earn self-landing was considered and rejected: the only paths that could be dropped are the producer modules, which ARE the change."
}
