{
  "tier": 1,
  "landing": "self",
  "why": "Backlog filing only — one row appended to docs/claude/health-review-backlog.json, no code change of any kind. Tier-1 by the canonical doc's own list: docs only; no order path, no strategy config, no risk caps, no src/, no config/, no deploy/, no scripts/, no workflows, no live-VM runtime file. Not landing machinery, so R12 does not apply. Records that .github/merge-slots/ landed today with #11891 and already carries 16 claim files with no release and no pruner: graded against `git ls-remote --heads origin`, 13 LIVE / 3 SPENT / 0 ungradeable, and the 3 is a LOWER BOUND because this repo does not delete a branch on merge. The contention harm is genuinely gone (per-branch files cannot conflict, which is what #11891 bought and is not in question); what survives is that a spent claim and a live one are byte-indistinguishable, which is the collapsed-state class. Filed rather than fixed because a --release subcommand cannot serve the normal case — the claim is released by the branch MERGING, after which no session is left to run it — so the mechanism has to be a sweep grading against origin, which is landing machinery and would land behind a human click that two PRs are already queued for. Recorded as a recurrence of the register-removals residue fixed in #11945."
}
