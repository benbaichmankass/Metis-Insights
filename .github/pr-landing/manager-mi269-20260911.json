{
  "tier": 1,
  "landing": "self",
  "why": "Manager registers only: one new MANAGER-CHECKLIST.json row (MI-269), one new work object, the SESSIONS.json row the registry wrote for the spawn, and the spawn-priority exception extended to name the new object. No src/, no config/, no deploy/, no scripts/, no .github/workflows/, no order path -- every path is inside TIER1_SURFACE. MI-269 IS THE #11767 FIX ROUND, operator-approved DIRECTLY as a decision pop-up: three options were offered (all four review findings / the blocking one only, filing the rest / park #11767 entirely) and the operator chose all four. The spec is not invented here -- it is docs/research/mi235-landing-machinery-review-2026-09-11.md, merged as #11799, which is why the lane was pointed at the report rather than handed a paraphrase. THE BLOCKING FINDING IS WHY THIS DISPLACES THE TRADING CYCLE: _latch_key omits the blocker STATE, so a blocker first graded could_not_look never pages when it clears -- which is #11767's OWN motivating case, meaning shipping without it would install a mechanism that looks armed and stays silent exactly where it is most needed, i.e. the failure class this operating layer exists to end, delivered by the thing built to prevent it. And every PR in the repo lands through the machinery #11767 touches, so a defect there reaches the trading work too. THREE CONSTRAINTS WERE PUT ON THE LANE AND ARE RECORDED HERE SO THEY SURVIVE THE SESSION: it must NOT merge #11767 (R12 holds a landing-machinery change for a human read whatever its state, and fixing the findings does not lift that -- the manager does not merge it either); it must NOT modify check_pr_landing.py or the guard self-test harness, because the review's argument depends on those being measurably untouched by the diff; and it must NOT report a green guard self-test as its verification, since the guard is unmodified so an identical green on main is the EXPECTED outcome -- the reviewer ran it and then explicitly refused to lean on it, and the lane was told not to undo that care. The BLOCKING item's test must cover the TRANSITION (could_not_look -> cleared), not the latch key's shape: a shape test would pass against a broken implementation that never re-evaluates."
}
