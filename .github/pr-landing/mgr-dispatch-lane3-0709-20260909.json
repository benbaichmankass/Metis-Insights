{
  "tier": 1,
  "landing": "self",
  "why": "Manager dispatch record for lane 3. Registers under docs/claude/work/** only: the SESSIONS.json registry row (with the observed source recorded via MI-207's new gate), WO-20260901-PHASE-F moved ready->in_flight with its owner, the spawn-priority exception naming that one object, the tool-stamped lease heartbeat, and the R13 merge_slot claim. No src/, tests/, scripts/, .github/workflows/, config/ or deploy/ path is touched — the Phase F work itself is what the dispatched session will author."
}
