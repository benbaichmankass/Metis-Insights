{
  "tier": 1,
  "landing": "self",
  "why": "Adds the missing `disposition`/`terminal` fields to PR #12346's row in docs/claude/work/OPEN-PRS.json (moving it from open_prs[] to settled_prs[], state closed) and nothing else. This is the record-keeping half of MI-313: `open_pr_record.py --strict` grades the WHOLE file on every PR (not diff-scoped), and #12346's row was left `state: open` with no terminal/disposition after the PR was actually closed unmerged on 2026-09-17 -- so `session-registry-guard` REFUSED every single PR in the repo, which is the root cause of the 83-open-PR pileup (automation/reconcile-open-prs-*, error-feed-digest, constraint-readout, probes, replay-pregate, session-reaper, data-commit, work-digest, valuation-snapshots, strategy-reviews, econ-calendar -- all blocked identically since 2026-09-13, none of them the actual defect). The disposition is transcribed from the PR's own closing comment (operator, 2026-09-17T14:40:46Z): closed after six voided refresh->CI->merge cycles (CI ~20min vs main moving every ~4min), content salvaged into #12437/#12412/#12428, nothing lost. Tier-1 (a JSON register edit, docs/claude/work/**, no src/tests/scripts/config/deploy/workflow change). Verified: `python3 scripts/ops/open_pr_record.py --strict` now exits 0 (previously exited 1, the guard's own error named this exact row); `python3 scripts/ops/check_backlog_refs.py --base main` OK, introduces no new tracking ids; `git diff --stat` is a clean move (26 insertions, 20 deletions) with no reformatting of the surrounding 2700+ line file -- confirmed the file round-trips byte-identically through json.dumps(indent=2, ensure_ascii=False) before editing."
}
