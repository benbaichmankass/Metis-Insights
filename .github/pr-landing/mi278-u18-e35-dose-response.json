{
  "tier": 1,
  "why": "Tier-1 research, and every path is inside TIER1_SURFACE — `scripts/research/**` is allowlisted, which is why this self-lands where MI-278 U17 (bare `scripts/` plus `config/**`) correctly could not. One new read-only instrument, one memo, its index row, and one backlog row dispositioned. It READS `/api/diag/journal` and two `/api/diag/bybit_raw_*` endpoints — the diag surface is read-only by contract and carries no exchange credentials — and writes nothing outside this diff. `src/**` and `config/**` are untouched: `config/strategies.yaml` is read only through `git show` at three refs to VERIFY the two control legs are held at atr_stop_mult 2.5, which is a check, not a change. NO CONFIG CHANGE IS PROPOSED — the row itself names routing `htf_pullback_trend_2h` to `execution: live` as the Tier-3 move and this unit repeats its refusal to propose it; the Tier-2 remedy the finding points at (persist `stop_order_type` + `cancel_type` at close time in `order_monitor`) belongs to MI-278 U10 and is not taken here either. ⚠️ THE ONE THING A REVIEWER SHOULD LOOK AT, since self-landing means nobody has to: the headline number is a REFUSAL and the tempting number is right beside it. On the gradeable subset the comparison reads treated 0.786 vs control 0.364 at two-sided Fisher p=0.0486 — nominally significant and the right sign. It is not a finding: 13 of 27 treated closes remain unnameable after the venue was asked, the missingness is correlated with the outcome, and imputing those rows at both extremes gives overlapping bands whose worst case reverses the sign at a flat p=1.0. The instrument grades it `not_gradeable_missingness_dominates` and refuses to print the p without the band, so the number cannot be quoted bare out of the JSON or the report.",
  "landing": "self"
}
