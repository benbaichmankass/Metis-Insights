{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 developer tooling: scripts/ci/check_register_field_loss.py (a third state, `malformed`, derived from _covers rather than asserted), one new test file, and two register edits in docs/claude/health-review-backlog.json. No src/, no config/, no deploy/, no workflows, no order path, no strategy config, no risk caps. R12 does not apply -- nothing in LANDING_MACHINERY is touched. It closes BL-20260912-A-SPENT-REGISTER-REMOVAL-DECLARATION-BECOMES-A-PERMANENT-REPO-WIDE-CI-FAILURE-BECAUSE-NOTHING-PRUNES-IT-AFTER-ITS-PR-LANDS BY MEASUREMENT: four cells run in separate worktrees against origin/main with real commits -- a zero-register diff with an inherited declaration exits 0 (the outage is over); a removal with no declaration exits 1 at `1 loss(es)`; the same removal with a correctly-shaped in-diff declaration exits 0 at `1 declared`; an in-diff declaration naming a removal the diff does not make exits 1 at `1 phantom`, so #11945 SCOPED the silencer check rather than defanging it. The row's prediction that the next deliberate removal re-arms the outage is REFUTED by cell 3, and the prune its criteria ask for is therefore unnecessary rather than outstanding -- stated rather than skipped. ONE OF MY OWN PROBES WAS VACUOUS and is recorded: the first cell-3 attempt chained two heredocs with && inside bash -c, the removal never applied, and the guard reported `0 register(s) compared` -- an exit 0 that proved nothing, caught by reading the denominator rather than the exit code. The second defect, found by walking into it: a declaration spelling the row key `row_id` is structurally inert and was reported as a FALSE CLAIM about the diff rather than as malformed -- sub-class A unprovenanced output. Both states still FAIL and nothing is excused; a test requires a well-formed non-matching declaration to stay a PHANTOM, which is the direction that would soften the blanket-silencer refusal. Three planted defects redden the suite, two of them the guard's own self-test. Register integrity across the merge with main: 1596 rows, 0 lost from either side, 0 duplicates, round-trip OK on all 4 live backlogs. Local guard set PASS 82 / FAIL 3: pr-landing-guard for this very record, plus open-items-guard and digest-liveness-guard, BOTH RED ON origin/main -- the first fixed by #12365, the second grading could_not_look offline for want of a GITHUB_TOKEN and passing in CI."
}
