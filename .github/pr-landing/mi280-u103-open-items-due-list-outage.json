{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, register-only: ONE row appended to docs/claude/OPEN-ITEMS.json plus the GENERATED SESSION-BRIEF block in CLAUDE.md. No canonical prose outside that block, no src/, no config/, no deploy/, no workflows, no scripts \u2014 not in LANDING_MACHINERY, so R12 does not apply. WHY IT EXISTS: CLAUDE.md's own session-end rule is to file an OPEN-ITEMS row for anything shipped that is not yet proven on the fleet, and #12374 is exactly that \u2014 merged is not run, and a green pytest-run is a harness that cannot reach a runner, which is why this class was invisible to CI for four days. The row carries BOTH causes of the outage because fixing either alone leaves DUE.json stale, and says explicitly that cause (2) (the stranded automation queue, #12376) is NOT this row's to fix. IT DECLARES probe_absent_reason RATHER THAN A PROBE, and the reason is the finding: the clearing condition is a cron that fires AND lands, which no file-shaped probe expresses, and a static generated_at match goes stale-TRUE once it passes. The obvious substitute is NAMED so a successor does not reach for it \u2014 requiring spent_decision_edges.state=read cannot tell a workflow render from a session's local one, and the single `read` on record came from a session's machine, so such a probe would have reported the mechanism working on the day it was broken. WRITTEN SURGICALLY, not via append_row, on a measured basis: OPEN-ITEMS.json reproduces under no candidate serialisation (U95/#12368) so append_row refuses rather than reformat ~21k lines and re-attribute every row to this diff. The text insertion touches only the bytes it adds: 13 insertions, 1 deletion, the deletion being the updated_at line. VERIFIED RATHER THAN ASSUMED: open-items-guard OK at 88 items; the observation-preservation rule RUN WITH --base and reading `read` (bare it grades not_graded, a we-did-not-look, which would have been a vacuous check); artifact-validity OK, and the row cites only BL-20260830-\u2026, already on main, so there is no cross-PR dependency of the kind that held #12224 up; and the session brief re-render was checked BOTH WAYS \u2014 with it session-brief reads OK, without it the guard fails closed, so the CLAUDE.md change is required rather than incidental."
}
