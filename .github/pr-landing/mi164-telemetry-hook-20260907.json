{
  "tier": 3,
  "landing": "hold",
  "hold_reason": "tier_2_3_needs_approval",
  "hold_text": "⚠️ I WAS DISPATCHED TO SELF-LAND THIS AND I AM NOT DOING SO, DELIBERATELY. The MI-164 dispatch said 'Tier-3 but APPROVED — self-land on green' and told me to verify `state=declared_self_land`. That state is UNREACHABLE for this diff and asking for it would have been a lie the guard catches: R5 checks the declared tier against the DIFF, `src/units/strategies/**` is in TIER3_PATHS, and R4 says in terms that only Tier-1 lands without a human. check_pr_landing.py's own self-test carries this exact case ('tier-3 diff held with a typed reason') and expects `declared_needs_approval`. The precedent is already written down in .github/pr-landing/ict-scalp-off-bybit2-20260906.json: R4 'bars any Tier-3 diff from landing itself whatever its approval state, so a human must click merge.' ON THE APPROVAL — I FIRST COULD NOT VERIFY IT AND NOW CAN, AND THIS FILE SAID OTHERWISE FOR ABOUT TEN MINUTES. When I wrote this, no repo-side record of 'Approve A now' existed and I said so. #11263 landed on main at 17:30:59Z carrying BOTH the approval block in docs/research/banking-half-2026-09-07.md § 5 AND the typed object docs/claude/work/objects/WO-20260907-PROPOSAL-A-APPROVED-ADD-THE-POSITION-TELEMETRY.yaml (source: operator (conversational)), whose done_condition names ALL FOUR units and additionally requires an OBSERVED telemetry row. So the approval IS recorded, the approved scope IS four units, and this PR implements four. THE HOLD IS NOT WAITING ON THAT DECISION AND NEVER WAS: it is R4, which bars any Tier-3 diff from landing itself whatever its approval state — the same reading .github/pr-landing/ict-scalp-off-bybit2-20260906.json already records. Do not re-litigate the approval; merge it. Note also that #11263's intermediate revision narrowing Proposal A to 'the two units that carry the population' is SUPERSEDED by the operator's verdict — main's own memo says so in terms, so a reviewer should not read this PR's four units as over-scope. THE CHANGE ITSELF IS OBSERVE-ONLY BY CONSTRUCTION and I would say the same if it were Tier-1: 114 inserted lines, zero deleted, every added statement inside a `try/except Exception: pass`, and tests/test_mi164_telemetry_hook_observe_only.py pins that all four units return the byte-identical pre-hook verdict whether the hook succeeds, raises, or cannot be imported at all.",
  "why": "Adds the existing observe-only `record_position_telemetry` hook to the four monitor units that lacked it — squeeze_breakout_4h, ict_scalp, turtle_soup, vwap — as MI-163 § 5 Proposal A specifies, copying trend_donchian.py:823-833's call site rather than inventing one. All four files are src/units/strategies/**, named Tier-3 by path and consumed by ict-trader-live, so the merge gate is Tier-3 however inert the change is. The rest of the diff is Tier-1: a new test file, plus scripts/research/banking_mechanism_audit.py and tests/test_banking_mechanism_audit.py, whose derived-vs-declared tripwire correctly went red when the hooked-unit set changed and is updated here to the re-measured value (n_invisible over the 44: 9 → 0). No config/, no lever, no threshold, no exit behaviour: `git diff` shows insertions only."
}
