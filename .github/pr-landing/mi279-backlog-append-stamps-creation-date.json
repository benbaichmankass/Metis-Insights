{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1 by docs/CLAUDE-RULES-CANONICAL.md § Permission Tiers: tooling and tests plus three edits to a JSON backlog register. Files touched: scripts/ops/backlog_append.py (the backlog-filing helper), tests/test_backlog_append.py, docs/claude/health-review-backlog.json. NO src/, NO config/, NO deploy/, NO workflow, NO systemd unit, NO order path, NO strategy or risk parameter. It cannot influence a live trade. AND IT IS NOT LANDING MACHINERY, checked rather than assumed: scripts/ops/backlog_append.py is absent from check_pr_landing.py's LANDING_MACHINERY, so R12 does not reach it -- backlog_append decides what goes in a REGISTER, never whether or how a PR merges. WHAT IT DOES: append_row now stamps `opened_at` when the caller supplies none of the four creation spellings the burn-down reader consults, and suppresses itself when the caller supplies any of them, so the writer CLAUDE.md mandates can no longer produce a row the guard CI mandates rejects. Closes BL-20260912-BACKLOG-APPEND-STAMPS-NO-CREATION-KEY-SO-THE-ONLY-WRITER-LEAVES-THE-FIELD-EVERY-READER-NEEDS-TO-THE-CALLER and BL-20260903-THREE-CREATION-DATE-KEYS-IN-ONE-BACKLOG-AND-THE-NEW-GUARD-ACCEPTS-ONLY-ONE (severity high). Proven end to end against the real register and the real guard in BOTH arms -- with the stamp, check_register_ids --base origin/main exits 0 on an undated append; with the stamp call removed, the identical append exits 1 naming the row and the keys it expected -- so the pass is caused by the stamp rather than by the rule being inert on this path. Mutation arms run in both directions: removing the stamp fails 1 self-test control and 2 pytest tests, removing the suppression fails 5. Every register edit went through the sanctioned append_row/update_row helpers and the diff on the backlog is line-local (11 added / 5 deleted); --check-live reports all four live backlogs reproduce byte-for-byte."
}
