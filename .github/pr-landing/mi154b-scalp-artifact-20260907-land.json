{
  "tier": 1,
  "landing": "self",
  "why": "Trainer-side ML work plus its written record. The DIFF is four files and every one is on the Tier-1 surface: one new memo (docs/research/MI-154b-scalp-exit-head-artifact-2026-09-07.md), one re-affirmed monitoring row in docs/claude/OPEN-ITEMS.json (verified_at bumped and an observation appended -- the row is NOT cleared, and no row is added or removed: 60 items before and after, open-items-guard OK), one appended backlog row in docs/claude/health-review-backlog.json filed through scripts/ops/backlog_append.py so the file's exact serialisation round-trips, and these two landing files. NO src/, config/, deploy/, .github/workflows/, scripts/, order path, strategy parameter, risk cap, or live-VM runtime file is touched. NOTHING IS ARMED: ICT_SCALP_EXIT_HEAD_MODE is untouched, no leg declares exit_head_action, no cell status in docs/research/exit-refinement-coverage.json is edited (all Tier-3), and nothing was promoted past stage=shadow. The two exit-head artifacts this memo documents were published to the TRAINER MIRROR, which is trainer-autonomous territory up to shadow per docs/claude/trainer-vm-mode.md -- they are not part of this diff and no repo file carries them. The relay request/result scaffolding under automation/** is deliberately NOT in this PR: automation/** is absent from pr-landing-guard's TIER1_SURFACE, so including it would make the diff unvouchable for self-landing; narrowing the PR is the guard's own first-listed remedy and the same one PR #11169 took for scripts/ml/**. That scaffolding stays on branch claude/mi154b-scalp-artifact-20260907 and the measurements it produced are quoted verbatim in the memo."
}
