{
  "tier": 1,
  "landing": "self",
  "why": "Tier-1, REGISTER-ONLY: docs/claude/health-review-backlog.json, 15 insertions / 1 deletion (the deletion being the updated_at line). No src/, no config/, no deploy/, no workflows, no scripts, no tests. NOT in LANDING_MACHINERY, so R12 does not apply. WHAT IT FILES: a RECURRENCE of BL-20260913-A-GUARD-RUN-BEFORE-COMMITTING-GRADES-THE-WRONG-TREE-AND-ITS-VACUOUS-PASS-IS-INDISTINGUISHABLE-FROM-A-REAL-ONE, which was opened on 'three sessions-worth of that happened in one evening' and is RESOLVED. Four MORE instances occurred 2026-09-17, all mine, and the cause is precise: the remedy went into scripts/ci/run_guards.py only. VERIFIED LIVE, both arms on one commit (02f83f21d0d7): the orchestrator's notice WORKS and fires in four places. But a session chasing one failure types the single guard the failure named, not the 15-minute orchestrator. MEASURED: 18 guards accept --base, and a case-insensitive grep for uncommitted/worktree dirty/dirty tree across all 18 returns ZERO. WHY IT IS FILED RATHER THAN FIXED, AND THE REMEDY IS COSTED SO NOBODY RE-DERIVES IT: there is no shared guard library -- all 18 are standalone and stdlib-only -- so the honest implementation is one helper plus 18 call sites, a unit of its own. run_guards.py::dirty_tree_lines is already PURE and is the function to reuse. The row rules out a wrapper script (nothing forces a session to type it, which is exactly why the run_guards fix did not reach these four) and rules out another instruction (that instruction already exists, I had read it, and I did it four times anyway). It must be a NOTICE ONLY: the committed-state reading is what CI does and changing it would make the local run disagree with CI. IT ALSO APPENDS a fresh 2026-09-17 instance to the OPEN row BL-20260912-THE-ARMING-FILE-AND-MERGE-SLOT-CLAIM-ARE-WRITTEN-AFTER-THE-LAST-LOCAL-GUARD-RUN-BY-CONSTRUCTION-SO-A-WHOLE-CLASS-OF-FILE-IS-NEVER-LOCALLY-VERIFIED, which predicted it exactly, and CONFIRMS its 'by construction' claim: the two-push landing protocol REQUIRES the arming files to be written after the last guard run, so the ordering is prescribed rather than careless. [!] WRITING THE NOTE PRODUCED A THIRD TRUNCATED ID, inside the paragraph explaining that eliding an id breaks it, and artifact-validity-guard refused the commit and named it. That is recorded IN the row as its best evidence rather than quietly fixed -- an author who had shipped the fix for that defect the same morning still wrote it while describing it, which is the argument for a check over an instruction. VERIFIED ON COMMITTED STATE, NO PIPES (a guard piped into head can report EXIT=0 while printing ::error:: -- SIGPIPE): artifact-validity EXIT 0, claim-basis EXIT 0, register-id-guard OK, backlog round-trip byte-for-byte on all four registers. Both rows written through append_row/update_row, and the appended note preserves the original text."
}