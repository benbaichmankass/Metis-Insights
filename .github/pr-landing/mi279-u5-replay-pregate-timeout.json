{
  "tier": 1,
  "landing": "self",
  "why": "U5 of MI-279: one DUE monitoring row observed and its observation recorded. Two files, both Tier-1 by docs/CLAUDE-RULES-CANONICAL.md's own list: docs/claude/OPEN-ITEMS.json and the GENERATED SESSION-BRIEF block in CLAUDE.md, re-rendered with render_session_brief.py --write rather than hand-edited. No src/, no config/, no deploy/, no workflow, no scripts, no order path, no VM mutation -- the evidence is a GitHub Actions run history read plus two git queries against origin/main. R12 does not apply; neither file is in LANDING_MACHINERY. THE ROW DOES NOT CLEAR and is not claimed to. What is recorded is that the failure mode CHANGED: the newest scheduled run concluded `cancelled` rather than `failure`, and it was not displaced by a superseding run (concurrency cancel-in-progress is false) but hit the job's own timeout-minutes: 45 at a 45.3-minute run length. Combined with the row's own earlier measurement of 10 of 22 heads in 30 minutes, that makes the shortfall arithmetic -- ~66 minutes needed against a 45-minute cap -- which points at the row's own option (b), re-scoping, or at raising the cap, or at trainer memory. None of those is proposed here because all three are operator decisions. Clause (a) is recorded as failing on BOTH halves, the stronger being that runtime_logs/replay_pregate/ does not exist on main at all (git ls-tree empty, git log returns no commits). It also records an honest limit on this session's own earlier work: the landing-route repair this session made to that workflow is real and still unexercised there, because the job times out before reaching the landing step, so it must not be read as progress on this row."
}
