{
  "tier": 1,
  "landing": "self",
  "why": "Tooling and observability only: scripts/ops/stuck_automation_branches.py gains a --receipt mode, scripts/ops/render_due_list.py gains one source reading it, .github/workflows/probes.yml gains a step and one more path on its existing commit-to-main, plus a new test module and two backlog rows. No src/, no config/, no deploy/, no order path, no live-VM mutation, no secret. Nothing in LANDING_MACHINERY: the twelve entries are commit-to-main/action.yml, .github/pr-landing/*.json, board-post.yml, claude-pr-automerge.yml, pr-opener.yml, stale-automation-sweep.yml, docs/claude/work/approvals/**, check_automerge_trigger.py, check_pr_landing.py, claim_merge_slot.py, session_registry.py and sweep_stale_automation_prs.py -- probes.yml is not among them, and USING commit-to-main is not CHANGING it. Evidence is planted-defect, not a green run: nine mutations of the shipped files, each shown to land (pattern count asserted) and each shown to fail tests/test_stuck_branch_carrier.py, with the control quiet afterwards; three more against the module's own --selftest, 27/27 clean. ruff clean, 17 tests pass, render_due_list --self-test reports all 51 of its planted controls firing."
}
