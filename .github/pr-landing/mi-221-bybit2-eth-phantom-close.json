{
  "tier": 1,
  "landing": "self",
  "why": "Docs only. The diff is an evidence doc under docs/claude/work/, three rows appended to docs/claude/health-review-backlog.json through scripts/ops/backlog_append.py (33 insertions, no reformat), a findings block on the MI-221 work object, and one hand-written row in docs/DOCUMENT-INDEX.md. No src/, no config/, no deploy/, no .github/workflows/, no order path, no strategy params, no risk caps — Tier-1 by the canonical doc's own list, and per .github/pr-landing/README.md routing Tier-1 through a human click is the failure this mechanism exists to end. REMEDIATION IS DELIBERATELY NOT IN THIS DIFF AND THAT IS WHY IT CAN SELF-LAND: the investigation found a critical defect in clients.py::account_open_positions (the bybit branch dedupes positions by SYMBOL and emits no position_idx, so under hedge mode a dropped book makes order_monitor.py:4348 read a live row as flat and CLOSE it), and every candidate fix changes what the reconciler considers open on a live REAL-MONEY order path — Tier-2/3, needing an operator OK. Those fixes are written out as resolution_criteria on the filed backlog rows and are NOT applied here. No order was placed, modified or cancelled on bybit_2 in this session; every venue read was a GET. DOCUMENT-INDEX.md was edited BY HAND rather than via scripts/ops/document_index.py --write, which is prohibited for this session because it rewrites ~430 files and bumps a canonical doc's last-verified date, fabricating an attestation nobody made. Guard state at the time of writing: run_guards.py --base main gave PASS 51 / FAIL 5; the two failures attributable to this change (check_backlog_refs on abbreviated backlog ids, check_document_index on the unregistered doc) were both fixed and re-verified green in-session. trainer-capture-watch-guard and operator-owed-guard fail in this sandbox only because pytest is not installed ('No module named pytest') and cannot be affected by a docs-only diff; pr-landing-guard clears on the push that carries this file."
}
