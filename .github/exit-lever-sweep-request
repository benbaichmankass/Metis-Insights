# Sentinel — touching this file fires m20-exit-lever-sweep from the branch.
# The `legs:` line below is READ BY THE PLAN JOB, not decoration.
#
# 2026-08-10 (6th dispatch) — THE OTHER 33 LEGS. Testing ONE falsifiable claim.
#
# THE CLAIM, from the first 10 legs (corpus committed, analysis run):
#   Zero of the 7 gradeable legs have a base net_R-per-drawdown rate below 1.0
#   (min 1.08). The other 3 have an unprofitable IS base and are already refused
#   by `drawdown_exchange_rate`'s `base_unprofitable` guard — ungradeable is not
#   a pass. So the "Path A is permissive on a weak book" case can only arise on a
#   book that is PROFITABLE BUT EARNS LITTLE PER UNIT OF DRAWDOWN, and no leg in
#   the measured fleet is there. The 0.40 rate that motivated a floor came from a
#   pre-correction base and does not reproduce.
#
# WHAT THIS RUN DECIDES: whether that holds over the WHOLE fleet or only over the
# census top-10. If some leg here is profitable with a rate below ~1.0, the floor
# is a real gap and this run finds the instance that sizes it. If none is, the
# floor is UNNECESSARY rather than underdetermined — a stronger and cheaper
# answer than any number, and one no amount of arguing could have produced.
#
# It also takes the floor analysis's own denominator from 6 distinct legs toward
# ~40, which is the only thing that makes a floor testable at all (the predictor
# is a property of the LEG, so LEGS are the sample size, not cells).
#
# SCOPE: the 33 remaining classifiable+enabled non-scalp legs. The 8 ict_scalp
# legs are DELIBERATELY EXCLUDED — the census timed one full-history scalp run at
# 955s and a sweep of one is base + cells x 2 windows + a 2-full-history
# walk-forward per candidate, so they belong in their own dispatch rather than
# holding this one behind a 4.5h worst case. `trend_donchian_1h` and
# `xauusd_trend_1h` are excluded as `enabled: false`.
#
# Geometry, gate, split and base are UNCHANGED. Nothing here re-opens a verdict,
# and nothing from this run ships without the operator's Tier-3 call.
legs: iwm_trend_long_1d,mes_trend_long_1d,mgc_trend_1h,qld_trend_long_1d,qqq_trend_long_1d,scha_trend_long_1d,slv_trend_1h,splg_trend_long_1d,spy_trend_long_1d,tqqq_trend_long_1d,trend_donchian_ada_4h,trend_donchian_avax_4h,trend_donchian_eth_4h,trend_donchian_sol_4h,trend_donchian_sol_prop,trend_donchian_xrp_4h,uso_trend_1h,ada_pullback_2h,eth_pullback_prop_2h,gdx_pullback_1d,gld_pullback_1d,gld_pullback_1h,iaum_pullback_1d,ief_pullback_1d,mgc_pullback_1d,mhg_pullback_1d,slv_pullback_1d,sol_pullback_2h,tlt_pullback_1d,tlt_pullback_1h,xrp_pullback_2h,squeeze_breakout_4h,fvg_range_15m
dispatch: 2026-08-10 fleet-wide base-rate distribution — does a profitable sub-1.0 leg exist?
