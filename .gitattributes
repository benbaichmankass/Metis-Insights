# Row-aware 3-way merge for the shared JSON registers.
#
# MEASURED, main @1b82ab7: these five files re-conflict every sibling PR, and the
# dominant mechanism is a shared header scalar rather than the rows —
# 74% of MANAGER-CHECKLIST.json conflict pairs and 67% of OPEN-PRS.json ones have
# BOTH sides bumping the same `updated_at`/`as_of` line. PR #10815's entire
# conflict was that one line.
#
# ⚠️ A MERGE DRIVER IS CLIENT-SIDE. Git runs it on `git merge` / `git rebase`
# LOCALLY, and ONLY after `scripts/ops/install_merge_driver.sh` has registered it
# in this clone's git config. GitHub's servers do NOT run custom merge drivers, so
# this does NOT make a `mergeable_state: dirty` PR auto-mergeable on GitHub — it
# removes the cost of resolving the conflict by hand, which is the measured
# consumer of manager time. Saying otherwise would be claiming a fix that is not
# there.
#
# The driver REFUSES (exit 1, markers left) on divergent same-id adds or edits and
# on delete-vs-edit. It never picks a winner. See scripts/ops/merge_json_register.py.
#
# ⚠️ SHIPPING IT IS NOT ARMING IT, AND FOR THE FIRST DAY NOBODY WAS ARMED. Measured
# 2026-09-03 (MI-94): `git config --get merge.jsonregister.driver` was EMPTY in a
# fresh clone, and nothing in the SessionStart hook, any skill, or CI ran the
# installer — so every session container merged these files the old way. Two
# sibling branches each appending one row conflicted in 4 of 4 registers un-armed
# and 0 of 3 armed. `scripts/ops/manager_preflight.py`'s `merge_driver` check now
# FAILS an un-armed clone and names the one-line remedy, so the gap is visible at
# the moment it costs something rather than after.
#
# ⚠️ AND THE DRIVER IS ONLY HALF THE PROBLEM. It resolves the TEXTUAL collision; it
# cannot see a SEMANTIC one — a branch filing new work under an id `main` already
# gave to something else is, to git, one changed value at one key, so the merge
# DELETES the existing row and reports success. `scripts/ci/check_register_ids.py`
# (`register-id-guard`) is that half, and it runs on every PR.

docs/claude/health-review-backlog.json      merge=jsonregister
# The other three review backlogs are the SAME row-register shape and were NOT
# bound — measured 2026-09-13, all four carry `items[]` with a unique `id`, which
# is what ID_FIELDS keys on. Only `health` was listed, so a hand-resolved conflict
# in the other three silently drops rows: on 2026-09-13 resolving
# performance-review-backlog.json by hand dropped BL-20260912-A-1000-ROW-TAIL-PULL-…
# and every guard passed on the result. Running merge_json_register.py over that
# same conflict's three inputs returns the correct 140 items with nothing dropped.
docs/claude/performance-review-backlog.json merge=jsonregister
docs/claude/ml-review-backlog.json          merge=jsonregister
docs/claude/research-review-backlog.json    merge=jsonregister
docs/claude/OPEN-ITEMS.json                 merge=jsonregister
docs/claude/work/MANAGER-CHECKLIST.json     merge=jsonregister
docs/claude/work/SESSIONS.json              merge=jsonregister
docs/claude/work/OPEN-PRS.json              merge=jsonregister

# ── MI-306 triage, 2026-09-18, against BL-20260913-THREE-OF-THE-FOUR-REVIEW-
# BACKLOGS-WERE-NEVER-BOUND-TO-THE-ROW-AWARE-MERGE-DRIVER's own "next_step":
# triage the (then 20, measured 21 today) unmarked candidates
# `check_register_reserialization.py` reports, bind the genuine id-keyed
# multi-session row registers, and record why the rest are not, so the
# residue is a decision rather than a backlog.
#
# BOUND HERE — each carries a top-level array of objects keyed on a field in
# ID_FIELDS (id/session_id/pr/item_id/key) and is APPENDED TO across sessions
# over time, the same shape as the registers above:
docs/claude/operator-owed-register.json      merge=jsonregister
docs/claude/SUNSET-DISPOSITIONS.json         merge=jsonregister
docs/claude/strategy-refinement-queue.json   merge=jsonregister
docs/claude/RECURRENCE-LEDGER.json           merge=jsonregister
docs/claude/work/MERGE-QUEUE.json            merge=jsonregister
#
# NOT BOUND — examined and judged genuinely out of scope, not merely skipped:
#   docs/claude/DUE.json, ERROR-FEED-DIGEST.json, PROBES.json,
#   work/CHECKLIST-ROUTING-AGE.json, UNCARRIED-SPEC-BASELINE.json,
#   work/MANAGER-WAKE.json, work/PR-QUEUE-WATCH.json,
#   work/REAPER-OBSERVATIONS.json
#     -- WHOLESALE-REGENERATED reports: one producer rewrites the entire file
#        each run from a fresh computation. There is no "row a session filed"
#        to lose on a conflict; the correct remedy for a conflict here is
#        regenerate-against-the-new-base (several already do this via
#        commit-to-main's stale-branch refresh), not a row-aware 3-way merge
#        of two computed snapshots that were never meant to be unioned.
#   docs/claude/health-reviews/2026-08-03.json,
#   docs/claude/reviews/2026-06-10-{health,ml,performance}-review.json,
#   docs/claude/work/AUTOMATION-PR-DISPOSITIONS-2026-09-10.json
#     -- DATED, WRITE-ONCE SNAPSHOTS: one file per run, named by date, never
#        edited again. A new review produces a NEW file; it does not append
#        to an old one, so there is no ongoing register here to protect.
#   docs/claude/board-pointer.json
#     -- `previous_boards[]` is real row-shaped history, but it is written on
#        board ROTATION only (measured: 1 entry in the file's whole life) --
#        too rare to be a live conflict source. Left unbound rather than
#        bound-for-symmetry; revisit if rotations become frequent.
#   docs/claude/session-board.json
#     -- Genuinely the most contended file in the repo (CLAUDE.md: 39 of the
#        last 40 commits touching it moved `merge_slot.branch`), but its hot
#        field is a SINGLE object (`merge_slot`), not a row array, and
#        `active_sessions[].session` is not a recognised ID_FIELDS key --
#        binding it here would silently do nothing for the field that
#        actually conflicts. The real fix already shipped is the per-branch
#        claim file under `.github/merge-slots/` (see check_pr_landing.py
#        R13), which sidesteps the shared field instead of merging it.
#   docs/claude/CYCLE-PRIORITY.json
#     -- `history[].cycle_id` is row-shaped but `cycle_id` is not an
#        ID_FIELDS key and the file has had exactly 2 history entries ever;
#        a code change to recognise it is not justified by the contention.

# ── docs/claude/pending-pings.jsonl — merge=union, NOT merge=jsonregister ──
#
# This one is line-delimited JSON, not a JSON object, and the difference decides
# the driver. MEASURED 2026-09-05 against current origin/main @5eb59917, over the
# full population of five ping-carrying PRs whose conflict was observed
# 2026-09-04 (#10888 #10902 #10908 #10914 #10926), test-merged in throwaway
# worktrees with merge.jsonregister ARMED:
#
#     no mapping (today)   5/5 CONFLICT   both sides' text left in markers
#     merge=jsonregister   5/5 CONFLICT   ⚠️ AND THE PR'S ROW IS NOT IN THE FILE
#     merge=union          0/5 CONFLICT   both sides' rows present, read and verified
#
# ⚠️ jsonregister IS NOT MERELY INEFFECTIVE HERE, IT IS WORSE THAN NOTHING. It
# parses the whole file as ONE JSON document, so appended LINES read to it as
# disagreeing header scalars and it refuses — correct instinct, wrong file. On
# the pre-rescue control (base b95ac61f, main before #10974) it left 74 rows and
# NO conflict markers while the PR's ping row was absent from the file entirely:
# a `git add` on that path drops the ping silently. Today's bare conflict at
# least leaves both sides' text where a human can see it.
#
# ⚠️ THE UNION OBJECTION WAS TESTED, NOT ASSUMED. union resurrects deletions, and
# a drained ping could in principle be deleted. It is not: notify_on_pull.py's
# `_drain_pending_pings` LEAVES THE FILE IN PLACE and dedupes by line hash via
# runtime_logs/pending_pings_delivered.txt (gitignored, per-clone) — its docstring
# says this replaced the old truncate-in-a-follow-up-commit contract. Measured
# over the file's whole history: 94 commits touch it; 4 show deletions; 3 predate
# the dedupe contract (350cc391, 2026-05-06) and were that old contract; the only
# post-dedupe one (39e6cbe0) is a pure reflow — 22 lines removed, all 22 re-added
# byte-identical, 0 rows semantically gone. So in 90 post-contract commits,
# nothing has ever removed a row, and union has nothing to resurrect.
#
# ⚠️ WHAT UNION DOES COST, STATED RATHER THAN GLOSSED. If a row was hand-rescued
# onto main by value (as #10974 did for these five) and the PR carrying it later
# merges, union keeps BOTH copies: merging #10888 into current main yields 91
# lines / 90 unique, the night_shift_summary row duplicated byte-for-byte. That
# is one duplicate operator page, because the hash ledger suppresses repeats
# ACROSS drain passes but not WITHIN one — `_drain_pending_pings` never adds to
# `delivered` as it iterates, so two identical lines return two pings (measured).
# Filed as BL-20260905-PENDING-PINGS-WITHIN-PASS-DUPLICATE. A duplicate page is
# noise and recoverable; a dropped ping is loss and is not. That is the trade
# this line takes, deliberately.
#
# ⚠️ CLIENT-SIDE ONLY, same as the drivers above: this does not make a `dirty`
# ping PR auto-mergeable on GitHub. It removes the hand-rescue cost for whoever
# holds an armed clone. union is a git BUILT-IN, so unlike jsonregister it needs
# no installer and works in a clone that never ran install_merge_driver.sh.
docs/claude/pending-pings.jsonl             merge=union

# ── docs/claude/work/PIPELINE.jsonl — merge=union, for the same reason ──
#
# The follow-through intake is line-delimited JSON with an append-only contract,
# exactly the shape the pending-pings block above argues for. It was bound to NO
# driver at all until now — `grep -n PIPELINE .gitattributes` exited 1.
#
# MEASURED by the E43 lane 2026-09-22 over the 23 adjacent PIPELINE-touching
# commit pairs since the 2026-09-21 reset, each simulated as base=A^/ours=A/
# theirs=B:
#
#     no mapping (until now)   23/23 CONFLICT
#     merge=union               0/23 CONFLICT
#
# For comparison, MANAGER-CHECKLIST.json's jsonregister driver refused on 2 of
# 33 pairs. This file conflicted on every single one, and four more instances
# were hit by hand on 2026-09-22 alone (#12740, #12742, #12747 and the manager's
# own branch) — each costing a hand-resolution on the critical path of a merge.
#
# ⚠️ THE UNION OBJECTION WAS TESTED, NOT ASSUMED, the same way. union resurrects
# deletions, so the question is whether a row is ever removed. It is not: 60
# commits touch this file and ZERO have ever removed a line, and
# `scripts/ops/pipeline.py::append` opens with mode 'a' and never rewrites. The
# probe has a positive control — the same measurement over pending-pings.jsonl
# returns 4 deletions — so the zero is a reading, not a blind spot.
#
# ⚠️ WHAT UNION COSTS HERE, STATED. Two sides appending a row with the SAME id
# both survive. That is deliberate and is NOT a dedupe job for a merge driver:
# the file is append-only precisely because a repeated id is a later STATE
# UPDATE of the same item, not a duplicate, and `pipeline.py --check` already
# reports id collisions (3 grandfathered today). A resolver that folded them
# would collapse an item's history into its first mention — a mistake the
# manager made and caught on 2026-09-22 when a hand resolver keyed on id and
# would have dropped 29 lines from the UNCONFLICTED region of this very file.
# Verbose-and-true beats short-and-lossy; a lost finding is not recoverable.
docs/claude/work/PIPELINE.jsonl              merge=union
