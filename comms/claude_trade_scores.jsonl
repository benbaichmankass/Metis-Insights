{"_meta": "Per-trade Claude decision scores from /health-review. APPEND-ONLY: one JSON object per graded trade after this line, keyed by `trade_id` (cross-references trade_journal.db::trades). Skip any row without a `trade_id`. Schema + planned Tier-2 DB-table/app-surface follow-up: comms/schema/claude_trade_scores.schema.json. Appended by .claude/skills/health-review/SKILL.md; ingestible by the trainer via the repo git-pull. schema_version=1"}
